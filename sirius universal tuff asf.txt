--[[

Sirius

© 2026 Corridon Capital. 
All Rights Reserved.

--]]


--[[

Sirius Pre-Hyperion Todo List

High Priority
 - Invisible, Godmode
 - All Scripts buttons and Universal scripts
 - Chat Spam Detection
 - Custom Script Prompts
 - Player Kill, Spectate and ESP via Playerlist
 - http.request support for Sirius Intelligent HTTP Interception
 - Performance Improvements to Roblox itself
 
Moderate Priority
 - Spectate Animation, like GTA serverhop, tween to high in the sky, then tween to other player's head
 - Starlight 
 - Chatlogs
 - GTA Serverhop
 - Anti-Spam (chat) formula, based on text length, caps, emojis etc.
 - Reduce any form of detection of Sirius
 - Automated lowering of graphics on lower FPS, ensure no false positives
 
Potential Future Setting Options
 - Block entire domain or just the specific page in the Sirius Intelligent Flow Interception. Do this on case by case, e.g blocked = {"link.com", true} - true being whether its the domain or not
 - Serverhop type (default/gta)
 - Hook Specific Functions to reduce the need for external scripts
 
--]]

-- Ensure the game is loaded 
if not game:IsLoaded() then
	game.Loaded:Wait()
end

-- Check License Tier
local Pro = true -- We're open sourced now!

-- Create Variables for Roblox Services
local coreGui = game:GetService("CoreGui")
local httpService = game:GetService("HttpService")
local lighting = game:GetService("Lighting")
local players = game:GetService("Players")
local replicatedStorage = game:GetService("ReplicatedStorage")
local runService = game:GetService("RunService")
local guiService = game:GetService("GuiService")
local statsService = game:GetService("Stats")
local starterGui = game:GetService("StarterGui")
local teleportService = game:GetService("TeleportService")
local tweenService = game:GetService("TweenService")
local userInputService = game:GetService('UserInputService')
pathfindingService = game:GetService("PathfindingService")
insertService = game:GetService("InsertService")
local gameSettings = UserSettings():GetService("UserGameSettings")

-- Variables
local camera = workspace.CurrentCamera
local getMessage = replicatedStorage:WaitForChild("DefaultChatSystemChatEvents", 1) and replicatedStorage.DefaultChatSystemChatEvents:WaitForChild("OnMessageDoneFiltering", 1)
local localPlayer = players.LocalPlayer
local notifications = {}
local friendsCooldown = 0
local mouse = localPlayer:GetMouse()
local promptedDisconnected = false
local smartBarOpen = false
local smartBarAnimationId = 0
local smartBarOptionTweens = {}
local debounce = false
local searchingForPlayer = false
local musicLibraryState = {
	source = [=[
1848354536|Relaxed Scene|Themes International|APMOfficial|Discover New Audio / Trending Music|1:37
142376088|Parry Gripp - Raining Tacos|Parry Gripp|ParryGripp|Discover New Audio / Trending Music|1:35
116072294638509|Heliopolis|Sanguinarius|DistrokidOfficial|Discover New Audio / Trending Music|2:20
140704980462451|Midnight Litany of Drones (Ancient Ritual Ambient)|Runaway Audio|DistrokidOfficial|Discover New Audio / Trending Music|3:31
5410086218|Noisestorm - Crab Rave|Noisestorm|Monstercat|Discover New Audio / Trending Music|2:41
140683966043041|Chill Zone Here|Robincrews|DistrokidOfficial|Discover New Audio / Trending Music|2:54
133664122932845|Brainrot Skibidi Sigma 67|Meme Doggy Dogg|DistrokidOfficial|Discover New Audio / Trending Music|3:13
110919391228823|Low Cortisol|wan9850|DistrokidOfficial|Discover New Audio / Trending Music|3:50
5409360995|Dion Timmer - Shiawase|Dion Timmer|Monstercat|Discover New Audio / Trending Music|3:38
140228184971194|Game Lobby Alternate|Samy Ponsar|APMOfficial|Discover New Audio / Trending Music|3:23
139997523791273|Boss Battle Burnout|Workout & Wellness Zone|DistrokidOfficial|Discover New Audio / Trending Music|2:52
1841178209|Black Tide (a)|Robert John Foster|APMOfficial|Vibes / Dark|1:43
9039698277|Much Fear A|MUSICA.IT|APMOfficial|Vibes / Dark|1:06
1837165398|Revolutions (part 1)|Doina Rotar|APMOfficial|Vibes / Dark|0:20
]=],
	library = {},
	sections = {},
	sectionSeen = {},
	ready = false,
	activeSection = "All",
	renderedTracks = {},
	currentTrack = nil,
	currentIndex = 0,
	renderToken = 0,
	resultLimit = 120,
	playGeneration = 0,
	audio = nil,
	searchSerial = 0,
	progressStarted = false,
	durationText = "--:--",
	isSeeking = false,
	nowPlayingTitle = "Not Playing",
}
local lowerName = localPlayer.Name:lower()
local lowerDisplayName = localPlayer.DisplayName:lower()
local placeId = game.PlaceId
local jobId = game.JobId
local checkingForKey = false
local originalTextValues = {}
local creatorId = game.CreatorId
local noclipDefaults = {}
local movers = {}
local creatorType = game.CreatorType
local espContainer = Instance.new("Folder", gethui and gethui() or coreGui)
local locatedPlayers = {} -- per-player ESP toggles, independent from the global ESP action
local espConnections = {} -- [player] = RBXScriptConnection for CharacterAdded
local descendantAddedConn -- top-level DescendantAdded; tracked so we can disconnect on teardown
local playerActionSystem = {}
local oldVolume = gameSettings.MasterVolume
keybindCategoryName = "Keybinds"

function randomString()
	local length = math.random(10, 20)
	local array = {}

	for i = 1, length do
		array[i] = string.char(math.random(32, 126))
	end

	return table.concat(array)
end

do
	local bridge = _G.__SIRIUS_IY_BRIDGE or {}
	_G.__SIRIUS_IY_BRIDGE = bridge
	bridge.pendingCommands = bridge.pendingCommands or {}

	function getSiriusIntegratedIYSearchTokens(fallbackTokens)
		if type(bridge.cmds) == "table" then
			if type(bridge.searchTokens) ~= "table" or #bridge.searchTokens == 0 then
				local tokens = {}
				local seen = {}

				for _, command in pairs(bridge.cmds) do
					if type(command) == "table" then
						local commandName = tostring(command.NAME or ""):lower()

						if commandName ~= "" and not seen[commandName] then
							seen[commandName] = true
							table.insert(tokens, commandName)
						end

						if type(command.ALIAS) == "table" then
							for _, alias in pairs(command.ALIAS) do
								alias = tostring(alias or ""):lower()

								if alias ~= "" and not seen[alias] then
									seen[alias] = true
									table.insert(tokens, alias)
								end
							end
						end
					end
				end

				table.sort(tokens)
				bridge.searchTokens = tokens
			end

			if #bridge.searchTokens > 0 then
				return bridge.searchTokens
			end
		end

		return fallbackTokens or {}
	end

	function runIntegratedIYCommand(commandText, speaker)
		commandText = tostring(commandText or ""):gsub("^%s+", ""):gsub("%s+$", "")
		if commandText == "" then return end

		if type(bridge.execCmd) == "function" then
			local success, err = pcall(bridge.execCmd, commandText, speaker or localPlayer, true)
			if not success then
				warn("Infinite Yield command failed: "..tostring(err))
			end
			return
		end

		if type(execCmd) == "function" then
			local success, err = pcall(execCmd, commandText, speaker or localPlayer)
			if not success then
				warn("Infinite Yield command failed: "..tostring(err))
			end
			return
		end

		table.insert(bridge.pendingCommands, {commandText, speaker or localPlayer})
	end

	local function flushPendingIntegratedIYCommands()
		if type(bridge.execCmd) ~= "function" then
			return
		end

		for _, pending in ipairs(bridge.pendingCommands or {}) do
			pcall(bridge.execCmd, pending[1], pending[2] or localPlayer, true)
		end
		bridge.pendingCommands = {}
	end

	if not bridge.loading and not bridge.loaded then
		bridge.loading = true

		task.spawn(function()
			local success, result = pcall(function()
				return game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source")
			end)

			if not success then
				warn("Failed to fetch Infinite Yield: "..tostring(result))
				bridge.loading = false
				return
			end

			if type(loadstring) ~= "function" then
				warn("loadstring is unavailable; Infinite Yield was not started")
				bridge.loading = false
				return
			end

			result = result:gsub("Main%.ResetOnSpawn = false", "Main.ResetOnSpawn = false\n    Main.Enabled = false")
			result = result:gsub(
				"elseif COREGUI:FindFirstChild%(\"RobloxGui\"%) then\n    PARENT = COREGUI%.RobloxGui",
				[[elseif COREGUI:FindFirstChild("RobloxGui") then
    local Main = Instance.new("ScreenGui")
    Main.Name = randomString()
    Main.ResetOnSpawn = false
    Main.DisplayOrder = MAX_DISPLAY_ORDER
    Main.Enabled = false
    Main.Parent = COREGUI.RobloxGui
    PARENT = Main]],
				1
			)
			result = result:gsub("if %(Key==prefix%) then", "if false and (Key==prefix) then", 1)

			local chunk, loadErr = loadstring(result, "=IntegratedInfiniteYield")
			if not chunk then
				warn("Failed to compile Infinite Yield: "..tostring(loadErr))
				bridge.loading = false
				return
			end

			if type(setfenv) == "function" then
				local baseEnv = (type(getfenv) == "function" and getfenv()) or _G
				bridge.env = setmetatable({
					_G = _G,
					shared = shared,
					script = script,
				}, {__index = baseEnv})
				pcall(setfenv, chunk, bridge.env)
			end

			local runOk, runErr = pcall(chunk)
			if not runOk then
				warn("Failed to start Infinite Yield: "..tostring(runErr))
				bridge.loading = false
				return
			end

			local runtime = type(bridge.env) == "table" and bridge.env or _G
			bridge.execCmd = runtime.execCmd
			bridge.cmds = runtime.cmds
			bridge.loaded = type(bridge.execCmd) == "function"
			bridge.loading = false
			runtime.queueteleport = false
			runtime.KeepInfYield = false
			runtime.notify = function() end

			pcall(function()
				if typeof(runtime.PARENT) == "Instance" and runtime.PARENT:IsA("ScreenGui") then
					runtime.PARENT.Enabled = false
				end
				if typeof(runtime.ScaledHolder) == "Instance" then
					runtime.ScaledHolder.Visible = false
				end
				if typeof(runtime.Holder) == "Instance" then
					runtime.Holder.Visible = false
				end
				if typeof(runtime.Notification) == "Instance" then
					runtime.Notification.Visible = false
				end
				if typeof(runtime.Tooltip) == "Instance" then
					runtime.Tooltip.Visible = false
				end
			end)

			bridge.searchTokens = getSiriusIntegratedIYSearchTokens({})
			flushPendingIntegratedIYCommands()
			end)
		end
	end

characterRuntime = _G.__SIRIUS_CHARACTER_RUNTIME or {
	autoAimAssistStrength = 0,
	clientAuraStrength = 0,
	ghostModeEnabled = false,
	shadersEnabled = false,
	ghostSavedRootCFrame = nil,
	ghostSavedRootAnchored = nil,
	ghostSavedAutoRotate = nil,
	ghostSavedPlatformStand = nil,
	ghostSavedCameraType = nil,
	ghostSavedCameraSubject = nil,
	ghostSavedMouseBehavior = nil,
	ghostSavedMouseIconEnabled = nil,
	ghostCameraCFrame = nil,
	ghostCharacterCFrame = nil,
	ghostPartStates = nil,
	ghostYaw = 0,
	ghostPitch = 0,
	ghostMoveSpeed = 96,
	ghostThirdPersonDistance = 8,
	ghostZoomMin = 0,
	ghostZoomMax = 18,
	ghostZoomStep = 1,
	ghostThirdPersonHeight = 2.3,
	ghostProxyModel = nil,
	ghostProxyRoot = nil,
	invisibilityEnabled = false,
	invisibilityOriginalCharacter = nil,
	invisibilityProxyCharacter = nil,
	invisibilityFixConn = nil,
	invisibilityDeathConn = nil,
	zeroGravityJumpConn = nil,
	zeroGravityJumpBoost = 52,
	lastRefreshPivot = nil,
	auraLastTick = 0,
	walkInAirEnabled = false,
	walkInAirPart = nil,
	walkInAirCharacter = nil,
	walkInAirY = nil,
	autoFindEnabled = false,
	autoFindLastPathTime = 0,
	autoFindJumpCooldown = 0,
	autoFindWaypoints = nil,
	autoFindWaypointIndex = 1,
	autoFindTargetUserId = nil,
	autoFindLastPosition = nil,
	autoFindLastMoveTick = 0,
	autoFindMoveTarget = nil,
	autoFindLastTargetPosition = nil,
	currentAutoAimTarget = nil,
	clickTeleportEnabled = false,
	clickTeleportTool = nil,
	clickTeleportConn = nil,
	autoClickTimer = 0,
	autoClickInterval = 0.083,
	autoClickHadTarget = false,
	autoFindForceRepath = false,
	autoFindJumpLast = 0,
	autoFindPeekLast = 0,
	autoFindPeekActive = false,
	autoFindPathTimer = 0,
	scanTimer = 0,
	scanTargetPlayer = nil,
	scanTargetData = nil,
	scanDistance = math.huge,
	scanIsLOS = false,
	scanIsOnScreen = false,
	autoFindLockedTarget = nil,
	autoFindStuckTimer = 0,
	autoFindStuckPhase = 0,
	autoFindStuckPhaseTime = 0,
	autoFindMoveConn = nil,
	autoFindMoveFired = false,
	antiVoidLoop = nil,
	antiVoidOriginDestroyHeight = workspace.FallenPartsDestroyHeight,
}
_G.__SIRIUS_CHARACTER_RUNTIME = characterRuntime
characterRuntime.layoutAligned = false
if characterRuntime.walkInAirEnabled == nil then
	characterRuntime.walkInAirEnabled = false
end

function characterRuntime.getRootPart(character)
	if not character then
		return nil
	end

	return character:FindFirstChild("HumanoidRootPart")
		or character:FindFirstChild("UpperTorso")
		or character:FindFirstChild("Torso")
end

function characterRuntime.clearInvisibilityConnections()
	if characterRuntime.invisibilityFixConn then
		characterRuntime.invisibilityFixConn:Disconnect()
		characterRuntime.invisibilityFixConn = nil
	end

	if characterRuntime.invisibilityDeathConn then
		characterRuntime.invisibilityDeathConn:Disconnect()
		characterRuntime.invisibilityDeathConn = nil
	end
end

function characterRuntime.restoreInvisibility()
	if not characterRuntime.invisibilityEnabled and not characterRuntime.invisibilityOriginalCharacter then
		characterRuntime.clearInvisibilityConnections()
		return
	end

	characterRuntime.invisibilityEnabled = false
	characterRuntime.clearInvisibilityConnections()

	local originalCharacter = characterRuntime.invisibilityOriginalCharacter
	local invisibleCharacter = characterRuntime.invisibilityProxyCharacter
	local currentCamera = workspace.CurrentCamera or camera
	local proxyRoot = characterRuntime.getRootPart(invisibleCharacter)
	local originalRoot = characterRuntime.getRootPart(originalCharacter)
	local restoreCFrame = proxyRoot and proxyRoot.CFrame or originalRoot and originalRoot.CFrame

	if originalRoot and restoreCFrame then
		originalRoot.CFrame = restoreCFrame
	end

	if invisibleCharacter and invisibleCharacter.Parent then
		invisibleCharacter:Destroy()
	end

	if originalCharacter then
		localPlayer.Character = originalCharacter
		originalCharacter.Parent = workspace

		local animate = originalCharacter:FindFirstChild("Animate")
		if animate then
			animate.Disabled = true
			task.wait()
			animate.Disabled = false
		end

		local humanoid = originalCharacter:FindFirstChildOfClass("Humanoid")
		if currentCamera and humanoid then
			currentCamera.CameraType = Enum.CameraType.Custom
			currentCamera.CameraSubject = humanoid
		end
	end

	characterRuntime.invisibilityOriginalCharacter = nil
	characterRuntime.invisibilityProxyCharacter = nil
end

function characterRuntime.applyInvisibility(enabled)
	if not enabled then
		characterRuntime.restoreInvisibility()
		return
	end

	if characterRuntime.invisibilityEnabled then
		return
	end

	local player = localPlayer
	local character = player.Character
	local humanoid = character and character:FindFirstChildOfClass("Humanoid")
	local rootPart = characterRuntime.getRootPart(character)
	if not character or not humanoid or not rootPart then
		error("Character is not ready for Invisibility")
	end

	character.Archivable = true
	local invisibleCharacter = character:Clone()
	if not invisibleCharacter then
		error("Failed to clone character for Invisibility")
	end

	invisibleCharacter.Name = ""
	invisibleCharacter.Parent = lighting
	for _, instance in ipairs(invisibleCharacter:GetDescendants()) do
		if instance:IsA("BasePart") then
			instance.Transparency = instance.Name == "HumanoidRootPart" and 1 or 0.5
		end
	end

	characterRuntime.invisibilityEnabled = true
	characterRuntime.invisibilityOriginalCharacter = character
	characterRuntime.invisibilityProxyCharacter = invisibleCharacter
	characterRuntime.clearInvisibilityConnections()

	local currentCamera = workspace.CurrentCamera or camera
	local cameraCFrame = currentCamera and currentCamera.CFrame
	local characterCFrame = rootPart.CFrame

	character:MoveTo(Vector3.new(0, math.pi * 1000000, 0))
	if currentCamera then
		currentCamera.CameraType = Enum.CameraType.Scriptable
	end
	task.wait(0.2)
	if currentCamera then
		currentCamera.CameraType = Enum.CameraType.Custom
		if cameraCFrame then
			currentCamera.CFrame = cameraCFrame
		end
	end

	character.Parent = lighting
	invisibleCharacter.Parent = workspace

	local invisibleRoot = characterRuntime.getRootPart(invisibleCharacter)
	if invisibleRoot then
		invisibleRoot.CFrame = characterCFrame
	end

	player.Character = invisibleCharacter

	local invisibleHumanoid = invisibleCharacter:FindFirstChildOfClass("Humanoid")
	if currentCamera and invisibleHumanoid then
		currentCamera.CameraSubject = invisibleHumanoid
	end

	local animate = invisibleCharacter:FindFirstChild("Animate")
	if animate then
		animate.Disabled = true
		task.wait()
		animate.Disabled = false
	end

	characterRuntime.invisibilityDeathConn = invisibleHumanoid and invisibleHumanoid.Died:Connect(function()
		characterRuntime.restoreInvisibility()
	end) or nil

	characterRuntime.invisibilityFixConn = runService.Stepped:Connect(function()
		if not characterRuntime.invisibilityEnabled then return end

		pcall(function()
			local activeRoot = characterRuntime.getRootPart(player.Character)
			if not activeRoot then return end

			local voidHeight = workspace.FallenPartsDestroyHeight
			local belowVoid = tostring(voidHeight):find("-", 1, true) and activeRoot.Position.Y <= voidHeight
			local aboveVoid = not tostring(voidHeight):find("-", 1, true) and activeRoot.Position.Y >= voidHeight
			if belowVoid or aboveVoid then
				characterRuntime.restoreInvisibility()
			end
		end)
	end)

	if notify then
		notify("Invisibility", "You now appear invisible to other players")
	end
end

function characterRuntime.clearWalkInAirLane()
	if characterRuntime.walkInAirPart and characterRuntime.walkInAirPart.Parent then
		characterRuntime.walkInAirPart:Destroy()
	end
	characterRuntime.walkInAirPart = nil
	characterRuntime.walkInAirCharacter = nil
	characterRuntime.walkInAirY = nil
end

function characterRuntime.destroyGhostVisual()
	local model = characterRuntime.ghostProxyModel
	if model and model.Parent then
		model:Destroy()
	end
	characterRuntime.ghostProxyModel = nil
	characterRuntime.ghostProxyRoot = nil
end

function characterRuntime.createGhostVisual(character)
	characterRuntime.destroyGhostVisual()
	do
		local ghostModel = Instance.new("Model")
		ghostModel.Name = "SiriusGhostSpectateSubject"

		local focusPart = Instance.new("Part")
		focusPart.Name = "SpectateSubject"
		focusPart.Anchored = true
		focusPart.CanCollide = false
		focusPart.CanTouch = false
		focusPart.CanQuery = false
		focusPart.Massless = true
		focusPart.CastShadow = false
		focusPart.Transparency = 1
		focusPart.Size = Vector3.new(2, 2, 2)
		focusPart.CFrame = characterRuntime.ghostCharacterCFrame or characterRuntime.ghostCameraCFrame or CFrame.new()
		focusPart.Parent = ghostModel

		ghostModel.PrimaryPart = focusPart
		ghostModel.Parent = workspace

		characterRuntime.ghostProxyModel = ghostModel
		characterRuntime.ghostProxyRoot = focusPart
		return
	end

	local ghostModel
	local core

	if character then
		local oldArchivable = character.Archivable
		character.Archivable = true
		local cloned
		pcall(function()
			cloned = character:Clone()
		end)
		character.Archivable = oldArchivable

		if cloned and cloned:IsA("Model") then
			ghostModel = cloned
			ghostModel.Name = "SiriusGhostSpectateAvatar"
			core = ghostModel:FindFirstChild("HumanoidRootPart") or ghostModel.PrimaryPart or ghostModel:FindFirstChildWhichIsA("BasePart")

			for _, instance in ipairs(ghostModel:GetDescendants()) do
				if instance:IsA("Script") or instance:IsA("LocalScript") or instance:IsA("ModuleScript") or instance:IsA("Tool") then
					instance:Destroy()
				elseif instance:IsA("BasePart") then
					instance.Anchored = true
					instance.CanCollide = false
					instance.CanTouch = false
					instance.CanQuery = false
					instance.Massless = true
					instance.CastShadow = true
					instance.LocalTransparencyModifier = 0
					if instance.Name == "HumanoidRootPart" then
						instance.Transparency = 1
					else
						instance.Transparency = math.clamp(math.max(instance.Transparency, 0.48), 0, 0.86)
					end
				elseif instance:IsA("Decal") or instance:IsA("Texture") then
					instance.Transparency = math.clamp(instance.Transparency + 0.18, 0, 1)
				elseif instance:IsA("BillboardGui") then
					instance.Enabled = false
				end
			end

			local ghostHumanoid = ghostModel:FindFirstChildOfClass("Humanoid")
			if ghostHumanoid then
				ghostHumanoid.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None
				ghostHumanoid.HealthDisplayType = Enum.HumanoidHealthDisplayType.AlwaysOff
				ghostHumanoid.PlatformStand = true
				ghostHumanoid.AutoRotate = false
			end

			if core then
				ghostModel.PrimaryPart = core
			end
		end
	end

	if not ghostModel or not core then
		ghostModel = Instance.new("Model")
		ghostModel.Name = "SiriusGhostSpectateAvatar"

		core = Instance.new("Part")
		core.Name = "HumanoidRootPart"
		core.Anchored = true
		core.CanCollide = false
		core.CanTouch = false
		core.CanQuery = false
		core.Massless = true
		core.CastShadow = false
		core.Material = Enum.Material.SmoothPlastic
		core.Color = Color3.fromRGB(12, 12, 16)
		core.Size = Vector3.new(2.1, 2.8, 1.2)
		core.Transparency = 0.42
		core.Parent = ghostModel

		ghostModel.PrimaryPart = core
	end

	ghostModel.Parent = workspace

	characterRuntime.ghostProxyModel = ghostModel
	characterRuntime.ghostProxyRoot = core

	local auraAttachment = Instance.new("Attachment")
	auraAttachment.Name = "SiriusGhostAuraAttachment"
	auraAttachment.Position = Vector3.new(0, 0.2, 0)
	auraAttachment.Parent = core

	local darkAura = Instance.new("ParticleEmitter")
	darkAura.Name = "SiriusGhostDarkAura"
	darkAura.Texture = "rbxasset://textures/particles/smoke_main.dds"
	darkAura.Rate = 88
	darkAura.Lifetime = NumberRange.new(1, 2.15)
	darkAura.Speed = NumberRange.new(0.3, 2.5)
	darkAura.SpreadAngle = Vector2.new(360, 360)
	darkAura.Rotation = NumberRange.new(0, 360)
	darkAura.RotSpeed = NumberRange.new(-40, 40)
	darkAura.Drag = 1.6
	darkAura.Acceleration = Vector3.new(0, 4.4, 0)
	darkAura.LockedToPart = true
	darkAura.LightEmission = 0
	darkAura.LightInfluence = 0.1
	darkAura.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(10, 10, 14)),
		ColorSequenceKeypoint.new(0.4, Color3.fromRGB(20, 18, 33)),
		ColorSequenceKeypoint.new(0.75, Color3.fromRGB(38, 24, 56)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(9, 9, 12)),
	})
	darkAura.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0.2),
		NumberSequenceKeypoint.new(0.35, 0.45),
		NumberSequenceKeypoint.new(0.8, 0.7),
		NumberSequenceKeypoint.new(1, 1),
	})
	darkAura.Size = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 1.1),
		NumberSequenceKeypoint.new(0.35, 2.4),
		NumberSequenceKeypoint.new(1, 4.2),
	})
	darkAura.Parent = auraAttachment

	local mistAura = Instance.new("ParticleEmitter")
	mistAura.Name = "SiriusGhostMistAura"
	mistAura.Texture = "rbxasset://textures/particles/sparkles_main.dds"
	mistAura.Rate = 18
	mistAura.Lifetime = NumberRange.new(0.65, 1.25)
	mistAura.Speed = NumberRange.new(0.1, 0.8)
	mistAura.SpreadAngle = Vector2.new(360, 360)
	mistAura.Rotation = NumberRange.new(0, 360)
	mistAura.RotSpeed = NumberRange.new(-110, 110)
	mistAura.Drag = 0.8
	mistAura.Acceleration = Vector3.new(0, 2.7, 0)
	mistAura.LockedToPart = true
	mistAura.LightEmission = 0.05
	mistAura.LightInfluence = 0
	mistAura.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(64, 40, 93)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(24, 16, 40)),
	})
	mistAura.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0.35),
		NumberSequenceKeypoint.new(0.55, 0.65),
		NumberSequenceKeypoint.new(1, 1),
	})
	mistAura.Size = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0.2),
		NumberSequenceKeypoint.new(1, 0.7),
	})
	mistAura.Parent = auraAttachment

	local highlight = Instance.new("Highlight")
	highlight.Name = "SiriusGhostAvatarHighlight"
	highlight.Adornee = ghostModel
	highlight.DepthMode = Enum.HighlightDepthMode.Occluded
	highlight.FillColor = Color3.fromRGB(215, 224, 235)
	highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
	highlight.FillTransparency = 0.9
	highlight.OutlineTransparency = 0.56
	highlight.Parent = ghostModel
end

function characterRuntime.applyGhostCharacterVisual(character)
	if not character then return end

	characterRuntime.ghostPartStates = characterRuntime.ghostPartStates or {}

	for _, instance in ipairs(character:GetDescendants()) do
		if instance:IsA("BasePart") then
			if not characterRuntime.ghostPartStates[instance] then
				characterRuntime.ghostPartStates[instance] = {
					LocalTransparencyModifier = instance.LocalTransparencyModifier,
					CanCollide = instance.CanCollide,
					CanTouch = instance.CanTouch,
					CanQuery = instance.CanQuery,
					CastShadow = instance.CastShadow,
					Massless = instance.Massless,
				}
			end

			instance.LocalTransparencyModifier = instance.Name == "HumanoidRootPart" and 1 or 0.48
			instance.CanCollide = false
			instance.CanTouch = false
			instance.CanQuery = false
			instance.CastShadow = true
			instance.Massless = true
		elseif instance:IsA("Decal") or instance:IsA("Texture") then
			if not characterRuntime.ghostPartStates[instance] then
				characterRuntime.ghostPartStates[instance] = {
					Transparency = instance.Transparency,
				}
			end

			local originalTransparency = characterRuntime.ghostPartStates[instance].Transparency or instance.Transparency
			instance.Transparency = math.clamp(originalTransparency + 0.28, 0, 1)
		end
	end

	local highlight = character:FindFirstChild("SiriusGhostOutline")
	if not highlight then
		highlight = Instance.new("Highlight")
		highlight.Name = "SiriusGhostOutline"
		highlight.Parent = character
	end

	highlight.Adornee = character
	highlight.DepthMode = Enum.HighlightDepthMode.Occluded
	highlight.FillColor = Color3.fromRGB(215, 224, 235)
	highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
	highlight.FillTransparency = 0.88
	highlight.OutlineTransparency = 0.74
end

function characterRuntime.restoreGhostCharacterVisual(character)
	local partStates = characterRuntime.ghostPartStates
	if partStates then
		for instance, state in pairs(partStates) do
			if instance and instance.Parent and state then
				pcall(function()
					if instance:IsA("BasePart") then
						if state.LocalTransparencyModifier ~= nil then
							instance.LocalTransparencyModifier = state.LocalTransparencyModifier
						end
						if state.CanCollide ~= nil then instance.CanCollide = state.CanCollide end
						if state.CanTouch ~= nil then instance.CanTouch = state.CanTouch end
						if state.CanQuery ~= nil then instance.CanQuery = state.CanQuery end
						if state.CastShadow ~= nil then instance.CastShadow = state.CastShadow end
						if state.Massless ~= nil then instance.Massless = state.Massless end
					elseif instance:IsA("Decal") or instance:IsA("Texture") then
						if state.Transparency ~= nil then
							instance.Transparency = state.Transparency
						end
					end
				end)
			end
		end
	end

	if character then
		local outline = character:FindFirstChild("SiriusGhostOutline")
		if outline then
			outline:Destroy()
		end
	end

	characterRuntime.ghostPartStates = nil
end

function characterRuntime.applyGhostMode(enabled)
	local wasEnabled = characterRuntime.ghostModeEnabled == true
	characterRuntime.ghostModeEnabled = enabled and true or false
	local character = localPlayer.Character
	local rootPart = characterRuntime.getRootPart(character)
	local humanoid = character and character:FindFirstChildOfClass("Humanoid")
	local currentCamera = workspace.CurrentCamera or camera
	local oldOutline = character and character:FindFirstChild("SiriusGhostOutline")

	if characterRuntime.ghostModeEnabled then
		if oldOutline then
			oldOutline:Destroy()
		end

		characterRuntime.restoreGhostCharacterVisual(character)
		characterRuntime.destroyGhostVisual()

		if rootPart then
			characterRuntime.ghostSavedRootCFrame = rootPart.CFrame
			characterRuntime.ghostSavedRootAnchored = rootPart.Anchored
			characterRuntime.ghostCameraCFrame = rootPart.CFrame
			characterRuntime.ghostCharacterCFrame = rootPart.CFrame
			rootPart.Anchored = true
			rootPart.AssemblyLinearVelocity = Vector3.zero
			rootPart.AssemblyAngularVelocity = Vector3.zero
		end

		characterRuntime.createGhostVisual(character)
		if characterRuntime.ghostProxyModel and characterRuntime.ghostCharacterCFrame then
			pcall(function()
				characterRuntime.ghostProxyModel:PivotTo(characterRuntime.ghostCharacterCFrame)
			end)
		end

		if humanoid then
			characterRuntime.ghostSavedAutoRotate = humanoid.AutoRotate
			characterRuntime.ghostSavedPlatformStand = humanoid.PlatformStand
			humanoid.AutoRotate = false
			humanoid.PlatformStand = true
			pcall(function()
				humanoid:ChangeState(Enum.HumanoidStateType.Physics)
			end)
		end

		if currentCamera then
			characterRuntime.ghostSavedCameraType = currentCamera.CameraType
			characterRuntime.ghostSavedCameraSubject = currentCamera.CameraSubject
			characterRuntime.ghostCameraCFrame = rootPart and rootPart.CFrame or currentCamera.CFrame
			characterRuntime.ghostCharacterCFrame = rootPart and rootPart.CFrame or currentCamera.CFrame

			local look = currentCamera.CFrame.LookVector
			characterRuntime.ghostPitch = math.asin(math.clamp(look.Y, -1, 1))
			characterRuntime.ghostYaw = math.atan2(-look.X, -look.Z)

			currentCamera.CameraType = Enum.CameraType.Custom
			if characterRuntime.ghostProxyRoot then
				currentCamera.CameraSubject = characterRuntime.ghostProxyRoot
			elseif rootPart then
				currentCamera.CameraSubject = rootPart
			elseif humanoid then
				currentCamera.CameraSubject = humanoid
			end
		end

		characterRuntime.ghostSavedMouseBehavior = userInputService.MouseBehavior
		characterRuntime.ghostSavedMouseIconEnabled = userInputService.MouseIconEnabled
		userInputService.MouseBehavior = Enum.MouseBehavior.Default
		userInputService.MouseIconEnabled = true

		if not wasEnabled and notify then
			notify("Ghost Mode", "W A S D move, Space/E up, Shift/Q down, normal camera rotate/zoom, Ctrl boost, Alt slow")
		end
	else
		if oldOutline then
			oldOutline:Destroy()
		end

		if rootPart then
			rootPart.CFrame = characterRuntime.ghostSavedRootCFrame or rootPart.CFrame
			rootPart.AssemblyLinearVelocity = Vector3.zero
			rootPart.AssemblyAngularVelocity = Vector3.zero
			if characterRuntime.ghostSavedRootAnchored ~= nil then
				rootPart.Anchored = characterRuntime.ghostSavedRootAnchored
			else
				rootPart.Anchored = false
			end
		end

		if humanoid then
			if characterRuntime.ghostSavedAutoRotate ~= nil then
				humanoid.AutoRotate = characterRuntime.ghostSavedAutoRotate
			else
				humanoid.AutoRotate = true
			end
			if characterRuntime.ghostSavedPlatformStand ~= nil then
				humanoid.PlatformStand = characterRuntime.ghostSavedPlatformStand
			else
				humanoid.PlatformStand = false
			end
			pcall(function()
				humanoid:ChangeState(Enum.HumanoidStateType.Running)
			end)
		end

		characterRuntime.restoreGhostCharacterVisual(character)

		if currentCamera then
			currentCamera.CameraType = characterRuntime.ghostSavedCameraType or Enum.CameraType.Custom
			if humanoid then
				currentCamera.CameraSubject = humanoid
			elseif rootPart then
				currentCamera.CameraSubject = rootPart
			else
				currentCamera.CameraSubject = characterRuntime.ghostSavedCameraSubject
			end
		end

		userInputService.MouseBehavior = characterRuntime.ghostSavedMouseBehavior or Enum.MouseBehavior.Default
		if characterRuntime.ghostSavedMouseIconEnabled ~= nil then
			userInputService.MouseIconEnabled = characterRuntime.ghostSavedMouseIconEnabled
		else
			userInputService.MouseIconEnabled = true
		end

		characterRuntime.destroyGhostVisual()
		characterRuntime.ghostCameraCFrame = nil
		characterRuntime.ghostCharacterCFrame = nil
	end
end

function characterRuntime.updateGhostMode(deltaTime)
	if not characterRuntime.ghostModeEnabled then
		return
	end

	local currentCamera = workspace.CurrentCamera or camera
	if not currentCamera then
		return
	end

	local character = localPlayer.Character
	local rootPart = characterRuntime.getRootPart(character)
	local humanoid = character and character:FindFirstChildOfClass("Humanoid")
	if not character or not rootPart then
		return
	end

	local dt = deltaTime or (1 / 60)
	local moveVector = Vector3.zero
	local function ghostKeyDown(settingName, fallbackKeyCode)
		local keyCode = fallbackKeyCode
		if type(checkSetting) == "function" then
			local setting = checkSetting(settingName, keybindCategoryName) or checkSetting(settingName, "Keybinds")
			local customName = setting and setting.current or nil
			if customName and customName ~= "" and Enum.KeyCode[customName] then
				keyCode = Enum.KeyCode[customName]
			end
		end
		return userInputService:IsKeyDown(keyCode)
	end

	if ghostKeyDown("Ghost Forward", Enum.KeyCode.W) then moveVector += Vector3.new(0, 0, -1) end
	if ghostKeyDown("Ghost Backward", Enum.KeyCode.S) then moveVector += Vector3.new(0, 0, 1) end
	if ghostKeyDown("Ghost Left", Enum.KeyCode.A) then moveVector += Vector3.new(-1, 0, 0) end
	if ghostKeyDown("Ghost Right", Enum.KeyCode.D) then moveVector += Vector3.new(1, 0, 0) end
	if ghostKeyDown("Ghost Up", Enum.KeyCode.E) then moveVector += Vector3.new(0, 1, 0) end
	if ghostKeyDown("Ghost Down", Enum.KeyCode.Q) then moveVector += Vector3.new(0, -1, 0) end

	local speed = characterRuntime.ghostMoveSpeed or 96
	if ghostKeyDown("Ghost Boost", Enum.KeyCode.LeftControl) then
		speed *= 2
	end
	if ghostKeyDown("Ghost Slow", Enum.KeyCode.LeftAlt) then
		speed *= 0.35
	end

	currentCamera.CameraType = Enum.CameraType.Custom
	if not characterRuntime.ghostProxyModel then
		characterRuntime.createGhostVisual(character)
	end
	if characterRuntime.ghostProxyRoot then
		currentCamera.CameraSubject = characterRuntime.ghostProxyRoot
	elseif rootPart then
		currentCamera.CameraSubject = rootPart
	elseif humanoid then
		currentCamera.CameraSubject = humanoid
	end

	rootPart.Anchored = true
	rootPart.AssemblyLinearVelocity = Vector3.zero
	rootPart.AssemblyAngularVelocity = Vector3.zero
	if characterRuntime.ghostSavedRootCFrame then
		rootPart.CFrame = characterRuntime.ghostSavedRootCFrame
	end
	if humanoid then
		humanoid.AutoRotate = false
		humanoid.PlatformStand = true
		pcall(function()
			humanoid:ChangeState(Enum.HumanoidStateType.Physics)
		end)
	end

	local baseCFrame = characterRuntime.ghostCharacterCFrame or rootPart.CFrame
	local position = baseCFrame.Position
	local cameraLook = currentCamera.CFrame.LookVector
	local cameraRight = currentCamera.CFrame.RightVector
	local cameraUp = Vector3.new(0, 1, 0)
	local flatLook = Vector3.new(cameraLook.X, 0, cameraLook.Z)
	if flatLook.Magnitude <= 0.001 then
		flatLook = Vector3.new(-baseCFrame.LookVector.X, 0, -baseCFrame.LookVector.Z)
	end
	if flatLook.Magnitude <= 0.001 then
		flatLook = Vector3.new(0, 0, -1)
	end
	flatLook = flatLook.Unit
	characterRuntime.ghostPitch = math.asin(math.clamp(cameraLook.Y, -1, 1))
	characterRuntime.ghostYaw = math.atan2(-flatLook.X, -flatLook.Z)

	if moveVector.Magnitude > 0 then
		moveVector = moveVector.Unit
		local worldMove =
			(cameraRight * moveVector.X)
			+ (cameraUp * moveVector.Y)
			+ (cameraLook * -moveVector.Z)
		if worldMove.Magnitude > 0 then
			worldMove = worldMove.Unit
		end
		position += worldMove * speed * dt
	end

	local leanPitch = 0
	local leanRoll = 0
	if moveVector.Magnitude > 0 then
		leanPitch = math.clamp(-moveVector.Z * math.rad(12), math.rad(-12), math.rad(12))
		leanRoll = math.clamp(-moveVector.X * math.rad(10), math.rad(-10), math.rad(10))
	end

	local characterCFrame = CFrame.new(position) * CFrame.fromOrientation(0, characterRuntime.ghostYaw, 0) * CFrame.Angles(leanPitch, 0, leanRoll)
	characterRuntime.ghostCharacterCFrame = characterCFrame
	characterRuntime.ghostCameraCFrame = CFrame.new(position) * CFrame.fromOrientation(0, characterRuntime.ghostYaw, 0)
	if characterRuntime.ghostProxyModel then
		pcall(function()
			characterRuntime.ghostProxyModel:PivotTo(characterCFrame)
		end)
	end
end

function characterRuntime.applyGravitySetting(value)
	local gravityValue = math.clamp(tonumber(value) or workspace.Gravity, 0, 500)
	workspace.Gravity = gravityValue

	if gravityValue <= 1 then
		if not characterRuntime.zeroGravityJumpConn then
			characterRuntime.zeroGravityJumpConn = userInputService.JumpRequest:Connect(function()
				local character = localPlayer.Character
				local humanoid = character and character:FindFirstChildOfClass("Humanoid")
				local rootPart = characterRuntime.getRootPart(character)
				if not humanoid or humanoid.Health <= 0 or not rootPart then
					return
				end

				local velocity = rootPart.AssemblyLinearVelocity
				local jumpBoost = characterRuntime.zeroGravityJumpBoost or 52
				rootPart.AssemblyLinearVelocity = Vector3.new(velocity.X, jumpBoost, velocity.Z)
			end)
		end
	else
		if characterRuntime.zeroGravityJumpConn then
			characterRuntime.zeroGravityJumpConn:Disconnect()
			characterRuntime.zeroGravityJumpConn = nil
		end
	end
end

function characterRuntime.applyAntiVoid(enabled)
	if characterRuntime.antiVoidLoop then
		characterRuntime.antiVoidLoop:Disconnect()
		characterRuntime.antiVoidLoop = nil
	end

	if not enabled then
		return
	end

	characterRuntime.antiVoidOriginDestroyHeight = characterRuntime.antiVoidOriginDestroyHeight or workspace.FallenPartsDestroyHeight
	task.wait()

	characterRuntime.antiVoidLoop = runService.Stepped:Connect(function()
		local character = localPlayer.Character
		local root = characterRuntime.getRootPart(character)
		if root and root.Position.Y <= characterRuntime.antiVoidOriginDestroyHeight + 25 then
			root.Velocity = root.Velocity + Vector3.new(0, 250, 0)
		end
	end)
end

function characterRuntime.applyShaders(enabled)
	characterRuntime.shadersEnabled = enabled and true or false

	local colorCorrection = lighting:FindFirstChild("SiriusShaderColorCorrection")
	if not colorCorrection then
		colorCorrection = Instance.new("ColorCorrectionEffect")
		colorCorrection.Name = "SiriusShaderColorCorrection"
		colorCorrection.Parent = lighting
	end

	local bloom = lighting:FindFirstChild("SiriusShaderBloom")
	if not bloom then
		bloom = Instance.new("BloomEffect")
		bloom.Name = "SiriusShaderBloom"
		bloom.Parent = lighting
	end

	colorCorrection.Enabled = characterRuntime.shadersEnabled
	bloom.Enabled = characterRuntime.shadersEnabled

	if characterRuntime.shadersEnabled then
		colorCorrection.Contrast = 0.24
		colorCorrection.Saturation = 0.18
		colorCorrection.Brightness = 0.04
		colorCorrection.TintColor = Color3.fromRGB(244, 248, 255)

		bloom.Intensity = 0.18
		bloom.Threshold = 1.15
		bloom.Size = 22
	else
		colorCorrection.Contrast = 0
		colorCorrection.Saturation = 0
		colorCorrection.Brightness = 0
		colorCorrection.TintColor = Color3.fromRGB(255, 255, 255)

		bloom.Intensity = 0
		bloom.Threshold = 2
		bloom.Size = 0
	end
end

function characterRuntime.getWalkInAirStandingY(character, rootPart)
	if not rootPart then
		return nil
	end

	local raycastParams = RaycastParams.new()
	raycastParams.FilterType = Enum.RaycastFilterType.Exclude
	local filterInstances = {}
	if character then
		table.insert(filterInstances, character)
	end
	if characterRuntime.walkInAirPart then
		table.insert(filterInstances, characterRuntime.walkInAirPart)
	end
	raycastParams.FilterDescendantsInstances = filterInstances
	raycastParams.IgnoreWater = true

	local result = workspace:Raycast(rootPart.Position, Vector3.new(0, -24, 0), raycastParams)
	if result then
		return result.Position.Y
	end

	local humanoid = character and character:FindFirstChildOfClass("Humanoid")
	local rootHalfHeight = rootPart.Size.Y * 0.5
	return rootPart.Position.Y - rootHalfHeight - (humanoid and humanoid.HipHeight or 2)
end

function characterRuntime.ensureWalkInAirLane(character)
	if not characterRuntime.walkInAirEnabled then
		characterRuntime.clearWalkInAirLane()
		return nil
	end

	local lane = characterRuntime.walkInAirPart
	if lane and characterRuntime.walkInAirCharacter ~= character then
		lane:Destroy()
		lane = nil
	end

	if not lane or not lane.Parent then
		lane = Instance.new("Part")
		lane.Name = "SiriusWalkInAirLane"
		lane.Anchored = true
		lane.CanCollide = true
		lane.CanTouch = false
		lane.CanQuery = false
		lane.Transparency = 1
		lane.CastShadow = false
		lane.Material = Enum.Material.SmoothPlastic
		lane.Size = Vector3.new(100000, 1, 100000)
		lane.Parent = workspace
		characterRuntime.walkInAirPart = lane
		characterRuntime.walkInAirCharacter = character
	end

	return lane
end

function characterRuntime.updateWalkInAir(character, rootPart)
	if not characterRuntime.walkInAirEnabled then
		characterRuntime.clearWalkInAirLane()
		return
	end

	if not character or not rootPart then
		characterRuntime.clearWalkInAirLane()
		return
	end

	local lane = characterRuntime.ensureWalkInAirLane(character)
	if not lane then
		return
	end

	if characterRuntime.walkInAirY == nil then
		characterRuntime.walkInAirY = characterRuntime.getWalkInAirStandingY(character, rootPart)
	end

	local floorY = characterRuntime.walkInAirY or rootPart.Position.Y
	lane.CFrame = CFrame.new(rootPart.Position.X, floorY - (lane.Size.Y * 0.5), rootPart.Position.Z)
end

function characterRuntime.getClosestPlayerTarget()
	local localCharacter = localPlayer.Character
	local localRoot = characterRuntime.getRootPart(localCharacter)
	if not localRoot then
		return nil
	end

	local visibleClosestDistance = math.huge
	local hiddenClosestDistance = math.huge
	local visibleClosestTarget = nil
	local hiddenClosestTarget = nil

	for _, player in ipairs(players:GetPlayers()) do
		if player ~= localPlayer then
			local character = player.Character
			local humanoid = character and character:FindFirstChildOfClass("Humanoid")
			local rootPart = characterRuntime.getRootPart(character)
			local head = character and character:FindFirstChild("Head")

			if humanoid and humanoid.Health > 0 and rootPart and head then
				local distance = (rootPart.Position - localRoot.Position).Magnitude
				local headTip = head.Position + Vector3.new(0, math.max(0.5, head.Size.Y * 0.55), 0)
				local targetData = {
					player = player,
					character = character,
					humanoid = humanoid,
					rootPart = rootPart,
					head = head,
					headTip = headTip,
				}

				if characterRuntime.isTargetVisibleInPOV(character, headTip) then
					if distance < visibleClosestDistance then
						visibleClosestDistance = distance
						visibleClosestTarget = targetData
					end
				elseif distance < hiddenClosestDistance then
					hiddenClosestDistance = distance
					hiddenClosestTarget = targetData
				end
			end
		end
	end

	return visibleClosestTarget or hiddenClosestTarget
end

function characterRuntime.getPredictedAimPoint(targetData)
	if not targetData or not targetData.headTip then
		return nil
	end

	local rootPart = targetData.rootPart
	if not rootPart then
		return targetData.headTip
	end

	local distance = (rootPart.Position - camera.CFrame.Position).Magnitude
	local lead = math.clamp((distance / (80 + distance)) * 0.22, 0, 0.28)
	local velocity = rootPart.AssemblyLinearVelocity or Vector3.zero
	return targetData.headTip + (velocity * lead)
end

function characterRuntime.getClosestPlayerHeadTip()
	local targetData = characterRuntime.getClosestPlayerTarget()
	return targetData and targetData.headTip or nil
end

function characterRuntime.ensureAuraObjects()
	local character = localPlayer.Character
	local rootPart = characterRuntime.getRootPart(character)
	if not character or not rootPart then
		return nil
	end

	local auraFolder = character:FindFirstChild("SiriusMajesticAura")
	if not auraFolder then
		auraFolder = Instance.new("Folder")
		auraFolder.Name = "SiriusMajesticAura"
		auraFolder.Parent = character
	end

	local baseAttachment = auraFolder:FindFirstChild("BaseAttachment")
	if not baseAttachment then
		baseAttachment = Instance.new("Attachment")
		baseAttachment.Name = "BaseAttachment"
		baseAttachment.Position = Vector3.new(0, -1.35, 0)
		baseAttachment.Parent = rootPart
	end

	local crownAttachment = auraFolder:FindFirstChild("CrownAttachment")
	if not crownAttachment then
		crownAttachment = Instance.new("Attachment")
		crownAttachment.Name = "CrownAttachment"
		crownAttachment.Position = Vector3.new(0, 2.75, 0)
		crownAttachment.Parent = rootPart
	end

	local sparkEmitter = auraFolder:FindFirstChild("SparkEmitter")
	if not sparkEmitter then
		sparkEmitter = Instance.new("ParticleEmitter")
		sparkEmitter.Name = "SparkEmitter"
		sparkEmitter.Texture = "rbxasset://textures/particles/sparkles_main.dds"
		sparkEmitter.LightEmission = 1
		sparkEmitter.LightInfluence = 0
		sparkEmitter.LockedToPart = true
		sparkEmitter.SpreadAngle = Vector2.new(360, 360)
		sparkEmitter.Rotation = NumberRange.new(0, 360)
		sparkEmitter.RotSpeed = NumberRange.new(-180, 180)
		sparkEmitter.VelocityInheritance = 0.12
		sparkEmitter.Enabled = false
		sparkEmitter.Parent = baseAttachment
	end

	local mistEmitter = auraFolder:FindFirstChild("MistEmitter")
	if not mistEmitter then
		mistEmitter = Instance.new("ParticleEmitter")
		mistEmitter.Name = "MistEmitter"
		mistEmitter.Texture = "rbxasset://textures/particles/smoke_main.dds"
		mistEmitter.LightEmission = 0.8
		mistEmitter.LightInfluence = 0
		mistEmitter.LockedToPart = true
		mistEmitter.SpreadAngle = Vector2.new(360, 360)
		mistEmitter.Rotation = NumberRange.new(0, 360)
		mistEmitter.RotSpeed = NumberRange.new(-70, 70)
		mistEmitter.Enabled = false
		mistEmitter.Parent = baseAttachment
	end

	local starEmitter = auraFolder:FindFirstChild("StarEmitter")
	if not starEmitter then
		starEmitter = Instance.new("ParticleEmitter")
		starEmitter.Name = "StarEmitter"
		starEmitter.Texture = "rbxasset://textures/particles/sparkles_main.dds"
		starEmitter.LightEmission = 1
		starEmitter.LightInfluence = 0
		starEmitter.LockedToPart = true
		starEmitter.SpreadAngle = Vector2.new(360, 360)
		starEmitter.Rotation = NumberRange.new(0, 360)
		starEmitter.RotSpeed = NumberRange.new(-50, 50)
		starEmitter.Enabled = false
		starEmitter.Parent = crownAttachment
	end

	local auraBeam = auraFolder:FindFirstChild("AuraBeam")
	if not auraBeam then
		auraBeam = Instance.new("Beam")
		auraBeam.Name = "AuraBeam"
		auraBeam.Attachment0 = baseAttachment
		auraBeam.Attachment1 = crownAttachment
		auraBeam.FaceCamera = true
		auraBeam.LightEmission = 1
		auraBeam.LightInfluence = 0
		auraBeam.Transparency = NumberSequence.new({
			NumberSequenceKeypoint.new(0, 0.35),
			NumberSequenceKeypoint.new(0.5, 0.15),
			NumberSequenceKeypoint.new(1, 0.35),
		})
		auraBeam.Enabled = false
		auraBeam.Parent = auraFolder
	end

	local auraLight = auraFolder:FindFirstChild("AuraLight")
	if not auraLight then
		auraLight = Instance.new("PointLight")
		auraLight.Name = "AuraLight"
		auraLight.Enabled = false
		auraLight.Range = 0
		auraLight.Brightness = 0
		auraLight.Shadows = false
		auraLight.Parent = rootPart
	end

	local auraHighlight = character:FindFirstChild("SiriusAuraHighlight")
	if not auraHighlight then
		auraHighlight = Instance.new("Highlight")
		auraHighlight.Name = "SiriusAuraHighlight"
		auraHighlight.Adornee = character
		auraHighlight.DepthMode = Enum.HighlightDepthMode.Occluded
		auraHighlight.FillTransparency = 1
		auraHighlight.OutlineTransparency = 1
		auraHighlight.Enabled = false
		auraHighlight.Parent = character
	end

	return {
		baseAttachment = baseAttachment,
		crownAttachment = crownAttachment,
		sparkEmitter = sparkEmitter,
		mistEmitter = mistEmitter,
		starEmitter = starEmitter,
		auraBeam = auraBeam,
		auraLight = auraLight,
		auraHighlight = auraHighlight,
	}
end

function characterRuntime.applyClientAura(value)
	characterRuntime.clientAuraStrength = math.clamp(tonumber(value) or 0, 0, 100)

	local auraObjects = characterRuntime.ensureAuraObjects()
	if not auraObjects then
		return
	end

	local sparkEmitter = auraObjects.sparkEmitter
	local mistEmitter = auraObjects.mistEmitter
	local starEmitter = auraObjects.starEmitter
	local auraBeam = auraObjects.auraBeam
	local auraLight = auraObjects.auraLight
	local auraHighlight = auraObjects.auraHighlight

	if characterRuntime.clientAuraStrength <= 0 then
		sparkEmitter.Enabled = false
		sparkEmitter.Rate = 0
		mistEmitter.Enabled = false
		mistEmitter.Rate = 0
		starEmitter.Enabled = false
		starEmitter.Rate = 0
		auraBeam.Enabled = false
		auraLight.Enabled = false
		auraLight.Range = 0
		auraLight.Brightness = 0
		auraHighlight.Enabled = false
		auraHighlight.FillTransparency = 1
		auraHighlight.OutlineTransparency = 1
		return
	end

	local alpha = characterRuntime.clientAuraStrength / 100

	sparkEmitter.Enabled = true
	sparkEmitter.Rate = 35 + (alpha * 280)
	sparkEmitter.Lifetime = NumberRange.new(0.55, 1 + alpha)
	sparkEmitter.Speed = NumberRange.new(2 + alpha * 5, 6 + alpha * 12)
	sparkEmitter.Drag = 1.1 - math.min(alpha * 0.6, 0.55)
	sparkEmitter.Acceleration = Vector3.new(0, 10 + alpha * 22, 0)
	sparkEmitter.Size = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0.25 + alpha * 0.7),
		NumberSequenceKeypoint.new(0.45, 0.8 + alpha * 2.2),
		NumberSequenceKeypoint.new(1, 0),
	})
	sparkEmitter.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0.08),
		NumberSequenceKeypoint.new(0.55, 0.22),
		NumberSequenceKeypoint.new(1, 1),
	})

	mistEmitter.Enabled = true
	mistEmitter.Rate = 10 + (alpha * 60)
	mistEmitter.Lifetime = NumberRange.new(1.1, 2.1 + alpha)
	mistEmitter.Speed = NumberRange.new(0.6 + alpha * 1.5, 1.6 + alpha * 4.2)
	mistEmitter.Drag = 1.7
	mistEmitter.Acceleration = Vector3.new(0, 3 + alpha * 8, 0)
	mistEmitter.Size = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 1 + alpha * 1.5),
		NumberSequenceKeypoint.new(0.5, 2.4 + alpha * 3.4),
		NumberSequenceKeypoint.new(1, 0),
	})
	mistEmitter.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0.72),
		NumberSequenceKeypoint.new(0.6, 0.84),
		NumberSequenceKeypoint.new(1, 1),
	})

	starEmitter.Enabled = true
	starEmitter.Rate = 6 + (alpha * 54)
	starEmitter.Lifetime = NumberRange.new(0.8, 1.7)
	starEmitter.Speed = NumberRange.new(0.4 + alpha * 1.4, 1.8 + alpha * 3.2)
	starEmitter.Drag = 0.4
	starEmitter.Acceleration = Vector3.new(0, 5 + alpha * 10, 0)
	starEmitter.Size = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0.18 + alpha * 0.45),
		NumberSequenceKeypoint.new(0.3, 0.45 + alpha * 1.2),
		NumberSequenceKeypoint.new(1, 0),
	})
	starEmitter.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0.02),
		NumberSequenceKeypoint.new(0.4, 0.18),
		NumberSequenceKeypoint.new(1, 1),
	})

	auraBeam.Enabled = true
	auraBeam.Width0 = 0.8 + (alpha * 1.25)
	auraBeam.Width1 = 0.22 + (alpha * 0.5)

	auraLight.Enabled = true
	auraLight.Range = 10 + (alpha * 22)
	auraLight.Brightness = 2 + (alpha * 6)

	auraHighlight.Enabled = true
	auraHighlight.FillTransparency = math.clamp(0.9 - (alpha * 0.46), 0.28, 0.9)
	auraHighlight.OutlineTransparency = math.clamp(0.48 - (alpha * 0.38), 0.06, 0.48)
end

function characterRuntime.clearAutoFindPath()
	if characterRuntime.autoFindMoveConn then
		characterRuntime.autoFindMoveConn:Disconnect()
		characterRuntime.autoFindMoveConn = nil
	end
	characterRuntime.autoFindWaypoints = nil
	characterRuntime.autoFindWaypointIndex = 1
	characterRuntime.autoFindTargetUserId = nil
	characterRuntime.autoFindLastTargetPosition = nil
	characterRuntime.autoFindLastPosition = nil
	characterRuntime.autoFindLastMoveTick = 0
	characterRuntime.autoFindMoveTarget = nil
	characterRuntime.autoFindForceRepath = false
	characterRuntime.autoFindLockedTarget = nil
	characterRuntime.autoFindStuckTimer = 0
	characterRuntime.autoFindStuckPhase = 0
	characterRuntime.autoFindStuckPhaseTime = 0
	characterRuntime.autoFindMoveFired = false
	characterRuntime.autoFindPeekLast = 0
	characterRuntime.autoFindPeekActive = false
	characterRuntime.autoFindPathTimer = 0
end

function characterRuntime.isTargetVisibleInPOV(targetCharacter, targetHeadTip)
	if not targetCharacter or not targetHeadTip then
		return false
	end

	local currentCamera = workspace.CurrentCamera or camera
	if not currentCamera then
		return false
	end

	local localCharacter = localPlayer.Character
	local rayParams = RaycastParams.new()
	rayParams.FilterType = Enum.RaycastFilterType.Blacklist
	rayParams.FilterDescendantsInstances = localCharacter and {localCharacter} or {}

	local origin = currentCamera.CFrame.Position
	local direction = targetHeadTip - origin
	if direction.Magnitude <= 0.01 then
		return true
	end

	local hit = workspace:Raycast(origin, direction, rayParams)
	if not hit then
		return true
	end

	return hit.Instance and hit.Instance:IsDescendantOf(targetCharacter) or false
end

function characterRuntime.isHeadTipOnScreen(targetHeadTip)
	local currentCamera = workspace.CurrentCamera or camera
	if not currentCamera or not targetHeadTip then
		return false
	end

	local viewportPos, onScreen = currentCamera:WorldToViewportPoint(targetHeadTip)
	if not onScreen or viewportPos.Z <= 0 then
		return false
	end

	local viewportSize = currentCamera.ViewportSize
	return viewportPos.X >= 0 and viewportPos.X <= viewportSize.X
		and viewportPos.Y >= 0 and viewportPos.Y <= viewportSize.Y
end

function characterRuntime.getClosestOnScreenVisibleTarget()
	local localCharacter = localPlayer.Character
	local localRoot = characterRuntime.getRootPart(localCharacter)
	if not localRoot then
		return nil
	end

	local closestDistance = math.huge
	local closestPlayer = nil

	for _, player in ipairs(players:GetPlayers()) do
		if player ~= localPlayer then
			local character = player.Character
			local humanoid = character and character:FindFirstChildOfClass("Humanoid")
			local rootPart = characterRuntime.getRootPart(character)
			local head = character and character:FindFirstChild("Head")

			if humanoid and humanoid.Health > 0 and rootPart then
				local focusPart = head or rootPart
				local yOffset = head and math.max(0.5, head.Size.Y * 0.55) or 1.5
				local headTip = focusPart.Position + Vector3.new(0, yOffset, 0)
				if characterRuntime.isHeadTipOnScreen(headTip) then
					local distance = (rootPart.Position - localRoot.Position).Magnitude
					if distance < closestDistance then
						closestDistance = distance
						closestPlayer = player
					end
				end
			end
		end
	end

	return closestPlayer
end

function characterRuntime.getClosestStrictVisibleTargetData()
	local localCharacter = localPlayer.Character
	local localRoot = characterRuntime.getRootPart(localCharacter)
	if not localRoot then
		return nil
	end

	local closestDistance = math.huge
	local closestTargetData = nil

	for _, player in ipairs(players:GetPlayers()) do
		if player ~= localPlayer then
			local character = player.Character
			local humanoid = character and character:FindFirstChildOfClass("Humanoid")
			local rootPart = characterRuntime.getRootPart(character)
			local head = character and character:FindFirstChild("Head")
			if humanoid and humanoid.Health > 0 and rootPart and head then
				local headTip = head.Position + Vector3.new(0, math.max(0.5, head.Size.Y * 0.55), 0)
				if characterRuntime.isHeadTipOnScreen(headTip) and characterRuntime.isTargetVisibleInPOV(character, headTip) then
					local distance = (rootPart.Position - localRoot.Position).Magnitude
					if distance < closestDistance then
						closestDistance = distance
						closestTargetData = {
							player = player,
							character = character,
							humanoid = humanoid,
							rootPart = rootPart,
							head = head,
							headTip = headTip,
						}
					end
				end
			end
		end
	end

	return closestTargetData
end

function characterRuntime.updateTargetScan(deltaTime)
	characterRuntime.scanTimer = (characterRuntime.scanTimer or 0) + (deltaTime or 0)
	if characterRuntime.scanTimer < 0.08 then
		return
	end
	characterRuntime.scanTimer = 0

	local localCharacter = localPlayer.Character
	local localRoot = characterRuntime.getRootPart(localCharacter)
	if not localRoot then
		characterRuntime.scanTargetPlayer = nil
		characterRuntime.scanTargetData = nil
		characterRuntime.scanDistance = math.huge
		characterRuntime.scanIsLOS = false
		characterRuntime.scanIsOnScreen = false
		return
	end

	local bestVisible, bestVisibleD2 = nil, math.huge
	local bestInFov, bestInFovD2 = nil, math.huge
	local bestAny, bestAnyD2 = nil, math.huge
	local full360 = true

	for _, player in ipairs(players:GetPlayers()) do
		if player ~= localPlayer then
			local character = player.Character
			local humanoid = character and character:FindFirstChildOfClass("Humanoid")
			local rootPart = characterRuntime.getRootPart(character)
			local head = character and character:FindFirstChild("Head")
			if humanoid and humanoid.Health > 0 and rootPart and head then
				local headTip = head.Position + Vector3.new(0, math.max(0.5, head.Size.Y * 0.55), 0)
				local deltaVector = rootPart.Position - localRoot.Position
				local distanceSquared = deltaVector.X * deltaVector.X + deltaVector.Y * deltaVector.Y + deltaVector.Z * deltaVector.Z
				local targetData = {
					player = player,
					character = character,
					humanoid = humanoid,
					rootPart = rootPart,
					head = head,
					headTip = headTip,
				}

				if distanceSquared < bestAnyD2 then
					bestAnyD2 = distanceSquared
					bestAny = targetData
				end

				local inFov = true
				if not full360 then
					local look = camera.CFrame.LookVector
					local direction = (headTip - camera.CFrame.Position).Unit
					local angle = math.deg(math.acos(math.clamp(look:Dot(direction), -1, 1)))
					inFov = angle <= 90
				end

				if inFov then
					if distanceSquared < bestInFovD2 then
						bestInFovD2 = distanceSquared
						bestInFov = targetData
					end
					if distanceSquared < bestVisibleD2 and characterRuntime.isTargetVisibleInPOV(character, headTip) then
						bestVisibleD2 = distanceSquared
						bestVisible = targetData
					end
				end
			end
		end
	end

	local selected = bestVisible or bestInFov or bestAny
	if selected then
		characterRuntime.scanTargetPlayer = selected.player
		characterRuntime.scanTargetData = selected
		if bestVisible and selected.player == bestVisible.player then
			characterRuntime.scanDistance = math.sqrt(bestVisibleD2)
			characterRuntime.scanIsLOS = true
		elseif bestInFov and selected.player == bestInFov.player then
			characterRuntime.scanDistance = math.sqrt(bestInFovD2)
			characterRuntime.scanIsLOS = false
		else
			characterRuntime.scanDistance = math.sqrt(bestAnyD2)
			characterRuntime.scanIsLOS = false
		end
		characterRuntime.scanIsOnScreen = characterRuntime.isHeadTipOnScreen(selected.headTip)
	else
		characterRuntime.scanTargetPlayer = nil
		characterRuntime.scanTargetData = nil
		characterRuntime.scanDistance = math.huge
		characterRuntime.scanIsLOS = false
		characterRuntime.scanIsOnScreen = false
	end
end

function characterRuntime.primeTargetScan()
	characterRuntime.scanTimer = 1
	characterRuntime.updateTargetScan(1)
end

function characterRuntime.setAutoAimAssist(value)
	value = tonumber(value) or 0
	characterRuntime.autoAimAssistStrength = math.clamp(value, 0, 100)

	if characterRuntime.autoAimAssistStrength <= 0 then
		characterRuntime.currentAutoAimTarget = nil
	else
		characterRuntime.primeTargetScan()
	end
end

function characterRuntime.setAutoFindEnabled(enabled)
	characterRuntime.autoFindEnabled = enabled and true or false
	characterRuntime.clearAutoFindPath()

	if characterRuntime.autoFindEnabled then
		characterRuntime.autoFindForceRepath = true
		characterRuntime.primeTargetScan()
	end
end

function characterRuntime.isPlayerAlive(player)
	if not player or not player.Character then
		return false
	end
	local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
	return humanoid and humanoid.Health > 0 or false
end

function characterRuntime.autoFindIssueMoveTo(humanoid, position, force)
	if not humanoid or not position then
		return
	end

	local now = os.clock()
	local previousTarget = characterRuntime.autoFindMoveTarget
	if not force and previousTarget and (previousTarget - position).Magnitude <= 1.2 and (now - (characterRuntime.autoFindLastMoveTick or 0)) < 0.12 then
		return
	end

	if characterRuntime.autoFindMoveConn then
		characterRuntime.autoFindMoveConn:Disconnect()
		characterRuntime.autoFindMoveConn = nil
	end

	characterRuntime.autoFindMoveTarget = position
	characterRuntime.autoFindLastMoveTick = now
	characterRuntime.autoFindMoveFired = false
	humanoid:MoveTo(position)
	characterRuntime.autoFindMoveConn = humanoid.MoveToFinished:Connect(function()
		characterRuntime.autoFindMoveFired = true
		characterRuntime.autoFindMoveTarget = nil
		if characterRuntime.autoFindMoveConn then
			characterRuntime.autoFindMoveConn:Disconnect()
			characterRuntime.autoFindMoveConn = nil
		end
	end)
end

function characterRuntime.autoFindDoJump(humanoid)
	if not humanoid then
		return
	end

	local now = os.clock()
	if now - (characterRuntime.autoFindJumpLast or 0) < 0.12 then
		return
	end

	characterRuntime.autoFindJumpLast = now
	humanoid:ChangeState(Enum.HumanoidStateType.Jumping)

	local vim = nil
	pcall(function()
		vim = game:GetService("VirtualInputManager")
	end)
	if vim and vim.SendKeyEvent then
		pcall(function()
			vim:SendKeyEvent(true, Enum.KeyCode.Space, false, game)
		end)
		task.delay(0.09, function()
			pcall(function()
				vim:SendKeyEvent(false, Enum.KeyCode.Space, false, game)
			end)
		end)
	end
end

function characterRuntime.autoFindProbeWall(rootPart, character, distance)
	local probeDistance = distance or 2.6
	if not rootPart then
		return { jump = false, wall = false }
	end

	local direction = Vector3.new(camera.CFrame.LookVector.X, 0, camera.CFrame.LookVector.Z)
	if direction.Magnitude < 0.001 then
		direction = Vector3.new(rootPart.CFrame.LookVector.X, 0, rootPart.CFrame.LookVector.Z)
	end
	if direction.Magnitude < 0.001 then
		direction = Vector3.new(0, 0, -1)
	else
		direction = direction.Unit
	end

	local rayParams = RaycastParams.new()
	rayParams.FilterType = Enum.RaycastFilterType.Blacklist
	rayParams.FilterDescendantsInstances = character and {character} or {}

	local lowHit = workspace:Raycast(rootPart.Position + Vector3.new(0, 0.6, 0), direction * probeDistance, rayParams)
	local midHit = workspace:Raycast(rootPart.Position + Vector3.new(0, 2.4, 0), direction * probeDistance, rayParams)
	local highHit = workspace:Raycast(rootPart.Position + Vector3.new(0, 4.2, 0), direction * probeDistance, rayParams)

	return {
		jump = (lowHit ~= nil and highHit == nil),
		wall = (lowHit ~= nil and midHit ~= nil),
	}
end

function characterRuntime.clickAtScreenCenter()
	local currentCamera = workspace.CurrentCamera or camera
	local centerX, centerY = nil, nil
	if currentCamera then
		centerX = math.floor(currentCamera.ViewportSize.X * 0.5)
		centerY = math.floor(currentCamera.ViewportSize.Y * 0.5)
	end

	if centerX and centerY and type(mousemoveabs) == "function" and type(mouse1press) == "function" and type(mouse1release) == "function" then
		pcall(mousemoveabs, centerX, centerY)
		pcall(mouse1press)
		pcall(mouse1release)
		if type(mouse1click) == "function" then
			pcall(mouse1click)
		end
		return true
	end

	if centerX and centerY and type(mousemoverel) == "function" and type(mouse1press) == "function" and type(mouse1release) == "function" then
		local cursor = userInputService:GetMouseLocation()
		pcall(mousemoverel, centerX - cursor.X, centerY - cursor.Y)
		pcall(mouse1press)
		pcall(mouse1release)
		if type(mouse1click) == "function" then
			pcall(mouse1click)
		end
		return true
	end

	if centerX and centerY then
		local vim = nil
		pcall(function()
			vim = game:GetService("VirtualInputManager")
		end)
		if vim and vim.SendMouseButtonEvent then
			pcall(function()
				vim:SendMouseButtonEvent(centerX, centerY, 0, true, game, 1)
				vim:SendMouseButtonEvent(centerX, centerY, 0, false, game, 1)
			end)
			return true
		end
	end

	if type(mouse1press) == "function" and type(mouse1release) == "function" then
		pcall(mouse1press)
		pcall(mouse1release)
		if type(mouse1click) == "function" then
			pcall(mouse1click)
		end
		return true
	end

	if type(mouse1click) == "function" then
		pcall(mouse1click)
		return true
	end

	return false
end

function characterRuntime.nextAutoClickInterval()
	return math.max(0.016, 1 / 12)
end

function characterRuntime.executeAutoClickSpam()
	local mousePosition = userInputService:GetMouseLocation()
	local vim = nil
	pcall(function()
		vim = game:GetService("VirtualInputManager")
	end)

	if vim and vim.SendMouseButtonEvent then
		pcall(function()
			vim:SendMouseButtonEvent(math.floor(mousePosition.X), math.floor(mousePosition.Y), 0, true, game, 1)
			vim:SendMouseButtonEvent(math.floor(mousePosition.X), math.floor(mousePosition.Y), 0, false, game, 1)
		end)
		return true
	end

	if type(mouse1press) == "function" and type(mouse1release) == "function" then
		pcall(mouse1press)
		pcall(mouse1release)
		return true
	end

	if type(mouse1click) == "function" then
		pcall(mouse1click)
		return true
	end

	return characterRuntime.clickAtScreenCenter()
end

function characterRuntime.rebuildAutoFindPath(rootPosition, targetPosition, targetUserId)
	local pathConfigs = {
		{ radius = 2.1, jumpHeight = 9, slope = 65, spacing = 1.6 },
		{ radius = 1.4, jumpHeight = 11, slope = 78, spacing = 1.2 },
		{ radius = 0.7, jumpHeight = 14, slope = 89, spacing = 0.9 },
	}

	local builtWaypoints = nil
	for _, config in ipairs(pathConfigs) do
		local path = pathfindingService:CreatePath({
			AgentRadius = config.radius,
			AgentHeight = 5,
			AgentCanJump = true,
			AgentCanClimb = true,
			AgentJumpHeight = config.jumpHeight,
			AgentMaxSlope = config.slope,
			WaypointSpacing = config.spacing,
		})

		local success = pcall(function()
			path:ComputeAsync(rootPosition, targetPosition)
		end)

		if success and path.Status == Enum.PathStatus.Success then
			local waypoints = path:GetWaypoints()
			if waypoints and #waypoints > 1 then
				builtWaypoints = waypoints
				break
			end
		end
	end

	if not builtWaypoints then
		characterRuntime.autoFindWaypoints = nil
		characterRuntime.autoFindWaypointIndex = 1
		characterRuntime.autoFindTargetUserId = targetUserId
		return false
	end

	characterRuntime.autoFindWaypoints = builtWaypoints
	characterRuntime.autoFindWaypointIndex = (#builtWaypoints > 1) and 2 or 1
	characterRuntime.autoFindTargetUserId = targetUserId
	return true
end

function characterRuntime.updateAutoFind(character, humanoid, rootPart, flightEnabled, deltaTime)
	local dt = deltaTime or (1 / 60)
	local ARRIVE = 4
	local PATH_CD = 1.05
	local STUCK_CD = 0.32
	local STUCK_DIST = 0.75
	local JUMP_H = 1.5
	local PEEK_IV = 2.4

	if not characterRuntime.autoFindEnabled then
		if humanoid and rootPart then
			humanoid:MoveTo(rootPart.Position)
		end
		if characterRuntime.autoFindMoveConn then
			characterRuntime.autoFindMoveConn:Disconnect()
			characterRuntime.autoFindMoveConn = nil
		end
		characterRuntime.autoFindStuckTimer = 0
		characterRuntime.autoFindStuckPhase = 0
		characterRuntime.autoFindStuckPhaseTime = 0
		characterRuntime.autoFindPeekLast = 0
		characterRuntime.autoFindPeekActive = false
		characterRuntime.autoFindMoveTarget = nil
		return
	end

	if flightEnabled then
		return
	end

	if not character or not humanoid or humanoid.Health <= 0 or not rootPart then
		return
	end

	local visTarget = characterRuntime.scanTargetPlayer
	local visIsLOS = characterRuntime.scanIsLOS == true

	if visIsLOS and visTarget then
		humanoid:MoveTo(rootPart.Position)
		if characterRuntime.autoFindMoveConn then
			characterRuntime.autoFindMoveConn:Disconnect()
			characterRuntime.autoFindMoveConn = nil
		end
		characterRuntime.autoFindStuckTimer = 0
		characterRuntime.autoFindStuckPhase = 0
		characterRuntime.autoFindStuckPhaseTime = 0
		characterRuntime.autoFindPeekLast = 0
		characterRuntime.autoFindPeekActive = false
		characterRuntime.autoFindMoveTarget = nil
		return
	end

	if characterRuntime.autoFindLockedTarget and not characterRuntime.isPlayerAlive(characterRuntime.autoFindLockedTarget) then
		characterRuntime.autoFindLockedTarget = nil
		characterRuntime.autoFindWaypoints = nil
		characterRuntime.autoFindWaypointIndex = 1
		characterRuntime.autoFindForceRepath = false
		characterRuntime.autoFindStuckTimer = 0
		characterRuntime.autoFindStuckPhase = 0
		characterRuntime.autoFindStuckPhaseTime = 0
		characterRuntime.autoFindPathTimer = 0
		characterRuntime.autoFindMoveTarget = nil
		if characterRuntime.autoFindMoveConn then
			characterRuntime.autoFindMoveConn:Disconnect()
			characterRuntime.autoFindMoveConn = nil
		end
	end

	if not characterRuntime.autoFindLockedTarget then
		if visTarget then
			characterRuntime.autoFindLockedTarget = visTarget
			characterRuntime.autoFindWaypoints = nil
			characterRuntime.autoFindWaypointIndex = 1
			characterRuntime.autoFindPathTimer = 0
			characterRuntime.autoFindStuckTimer = 0
			characterRuntime.autoFindStuckPhase = 0
			characterRuntime.autoFindStuckPhaseTime = 0
			characterRuntime.autoFindForceRepath = true
		else
			return
		end
	end

	local targetPlayer = characterRuntime.autoFindLockedTarget
	if not targetPlayer or targetPlayer.Parent ~= players then
		return
	end

	local targetCharacter = targetPlayer.Character
	local targetRoot = characterRuntime.getRootPart(targetCharacter)
	if not targetRoot then
		return
	end

	local targetPosition = targetRoot.Position
	local directDistance = (targetPosition - rootPart.Position).Magnitude
	if directDistance <= ARRIVE then
		if characterRuntime.autoFindMoveConn then
			characterRuntime.autoFindMoveConn:Disconnect()
			characterRuntime.autoFindMoveConn = nil
		end
		humanoid:MoveTo(rootPart.Position)
		characterRuntime.autoFindStuckTimer = 0
		characterRuntime.autoFindStuckPhase = 0
		characterRuntime.autoFindStuckPhaseTime = 0
		return
	end

	if not characterRuntime.autoFindLastPosition then
		characterRuntime.autoFindLastPosition = rootPart.Position
	end

	characterRuntime.autoFindStuckTimer = (characterRuntime.autoFindStuckTimer or 0) + dt
	if characterRuntime.autoFindStuckTimer >= STUCK_CD then
		characterRuntime.autoFindStuckTimer = 0
		local movedDistance = (rootPart.Position - characterRuntime.autoFindLastPosition).Magnitude
		characterRuntime.autoFindLastPosition = rootPart.Position
		if movedDistance < STUCK_DIST and (characterRuntime.autoFindStuckPhase or 0) == 0 then
			characterRuntime.autoFindStuckPhase = math.random(1, 6)
			characterRuntime.autoFindStuckPhaseTime = 0
			characterRuntime.autoFindForceRepath = true
			if characterRuntime.autoFindMoveConn then
				characterRuntime.autoFindMoveConn:Disconnect()
				characterRuntime.autoFindMoveConn = nil
			end
		elseif movedDistance >= STUCK_DIST then
			characterRuntime.autoFindStuckPhase = 0
		end
	end

	if (characterRuntime.autoFindStuckPhase or 0) ~= 0 then
		characterRuntime.autoFindStuckPhaseTime = (characterRuntime.autoFindStuckPhaseTime or 0) + dt
		local phase = characterRuntime.autoFindStuckPhase
		local phaseTime = characterRuntime.autoFindStuckPhaseTime

		local fwd = Vector3.new(camera.CFrame.LookVector.X, 0, camera.CFrame.LookVector.Z)
		if fwd.Magnitude < 0.001 then
			fwd = Vector3.new(rootPart.CFrame.LookVector.X, 0, rootPart.CFrame.LookVector.Z)
		end
		if fwd.Magnitude < 0.001 then
			fwd = Vector3.new(0, 0, -1)
		else
			fwd = fwd.Unit
		end
		local right = Vector3.new(fwd.Z, 0, -fwd.X)
		local base = rootPart.Position

		if phase == 1 then
			humanoid:MoveTo(base + (fwd * 4))
			characterRuntime.autoFindDoJump(humanoid)
			if phaseTime > 0.65 then characterRuntime.autoFindStuckPhase = 0 end
		elseif phase == 2 then
			humanoid:MoveTo(base - (right * 3) + (fwd * 3))
			characterRuntime.autoFindDoJump(humanoid)
			if phaseTime > 0.6 then characterRuntime.autoFindStuckPhase = 0 end
		elseif phase == 3 then
			humanoid:MoveTo(base + (right * 3) + (fwd * 3))
			characterRuntime.autoFindDoJump(humanoid)
			if phaseTime > 0.6 then characterRuntime.autoFindStuckPhase = 0 end
		elseif phase == 4 then
			humanoid:MoveTo(base - (fwd * 4))
			if phaseTime > 0.5 then
				characterRuntime.autoFindStuckPhase = 0
				characterRuntime.autoFindForceRepath = true
			end
		elseif phase == 5 then
			humanoid:MoveTo(base - (right * 2) - (fwd * 2))
			characterRuntime.autoFindDoJump(humanoid)
			if phaseTime > 0.55 then
				characterRuntime.autoFindStuckPhase = 0
				characterRuntime.autoFindForceRepath = true
			end
		elseif phase == 6 then
			humanoid:MoveTo(base + Vector3.new(0, 6, 0) + (fwd * 2))
			characterRuntime.autoFindDoJump(humanoid)
			task.delay(0.18, function()
				characterRuntime.autoFindDoJump(humanoid)
			end)
			if phaseTime > 0.6 then characterRuntime.autoFindStuckPhase = 0 end
		end
		return
	end

	characterRuntime.autoFindPathTimer = (characterRuntime.autoFindPathTimer or 0) + dt
	local targetMoved = 0
	if characterRuntime.autoFindLastTargetPosition then
		targetMoved = (targetPosition - characterRuntime.autoFindLastTargetPosition).Magnitude
	end

	local shouldRepath = characterRuntime.autoFindForceRepath
		or characterRuntime.autoFindPathTimer >= PATH_CD
		or targetMoved >= 5
		or type(characterRuntime.autoFindWaypoints) ~= "table"
		or #characterRuntime.autoFindWaypoints == 0

	if shouldRepath then
		characterRuntime.autoFindPathTimer = 0
		characterRuntime.autoFindLastTargetPosition = targetPosition
		local success = characterRuntime.rebuildAutoFindPath(rootPart.Position, targetPosition, targetPlayer.UserId)
		characterRuntime.autoFindForceRepath = false
		if success and type(characterRuntime.autoFindWaypoints) == "table" and #characterRuntime.autoFindWaypoints > 1 then
			local idx = math.clamp(characterRuntime.autoFindWaypointIndex or 2, 2, #characterRuntime.autoFindWaypoints)
			characterRuntime.autoFindWaypointIndex = idx
			characterRuntime.autoFindIssueMoveTo(humanoid, characterRuntime.autoFindWaypoints[idx].Position)
		else
			characterRuntime.autoFindWaypoints = nil
			characterRuntime.autoFindWaypointIndex = 1
			characterRuntime.autoFindIssueMoveTo(humanoid, targetPosition)
		end
	end

	characterRuntime.autoFindPeekLast = (characterRuntime.autoFindPeekLast or 0) + dt
	if characterRuntime.autoFindPeekLast >= PEEK_IV and not characterRuntime.autoFindPeekActive then
		characterRuntime.autoFindPeekLast = 0
		characterRuntime.autoFindPeekActive = true
		characterRuntime.autoFindDoJump(humanoid)
		task.delay(0.65, function()
			characterRuntime.autoFindPeekActive = false
		end)
	end

	local waypoints = characterRuntime.autoFindWaypoints
	local wpIdx = characterRuntime.autoFindWaypointIndex or 1

	if type(waypoints) == "table" and #waypoints > 0 and wpIdx <= #waypoints then
		local waypoint = waypoints[wpIdx]
		local deltaVector = waypoint.Position - rootPart.Position
		local flatDistance = Vector3.new(deltaVector.X, 0, deltaVector.Z).Magnitude
		local deltaY = waypoint.Position.Y - rootPart.Position.Y

		if flatDistance <= 2.6 or characterRuntime.autoFindMoveFired then
			characterRuntime.autoFindMoveFired = false
			wpIdx += 1
			characterRuntime.autoFindWaypointIndex = wpIdx
			if wpIdx <= #waypoints then
				characterRuntime.autoFindIssueMoveTo(humanoid, waypoints[wpIdx].Position)
			end
			return
		end

		local now = os.clock()
		if not characterRuntime.autoFindMoveConn or (now - (characterRuntime.autoFindLastMoveTick or 0)) >= 0.22 then
			characterRuntime.autoFindIssueMoveTo(humanoid, waypoint.Position)
		end
		local wall = characterRuntime.autoFindProbeWall(rootPart, character, 2.5)
		if waypoint.Action == Enum.PathWaypointAction.Jump or deltaY > JUMP_H or wall.jump then
			characterRuntime.autoFindDoJump(humanoid)
		end
		if wall.wall and waypoint.Action ~= Enum.PathWaypointAction.Jump then
			characterRuntime.autoFindForceRepath = true
		end
	else
		characterRuntime.autoFindIssueMoveTo(humanoid, targetPosition)
		local wall = characterRuntime.autoFindProbeWall(rootPart, character, 2.2)
		if wall.jump or wall.wall then
			characterRuntime.autoFindDoJump(humanoid)
		end
	end
end

function characterRuntime.destroyClickTeleportTool()
	if characterRuntime.clickTeleportConn then
		characterRuntime.clickTeleportConn:Disconnect()
		characterRuntime.clickTeleportConn = nil
	end

	if characterRuntime.clickTeleportTool and characterRuntime.clickTeleportTool.Parent then
		characterRuntime.clickTeleportTool:Destroy()
	end
	characterRuntime.clickTeleportTool = nil

	local backpack = localPlayer:FindFirstChildOfClass("Backpack")
	if backpack then
		local backpackTool = backpack:FindFirstChild("Click Teleport Tool")
		if backpackTool then
			backpackTool:Destroy()
		end
	end

	local character = localPlayer.Character
	if character then
		local equippedTool = character:FindFirstChild("Click Teleport Tool")
		if equippedTool then
			equippedTool:Destroy()
		end
	end
end

function characterRuntime.ensureClickTeleportTool()
	if not characterRuntime.clickTeleportEnabled then
		characterRuntime.destroyClickTeleportTool()
		return
	end

	local backpack = localPlayer:FindFirstChildOfClass("Backpack")
	if not backpack then
		return
	end

	local tool = characterRuntime.clickTeleportTool
	if tool and tool.Parent and (tool.Parent == backpack or tool.Parent == localPlayer.Character) then
		return
	end

	characterRuntime.destroyClickTeleportTool()

	tool = Instance.new("Tool")
	tool.Name = "Click Teleport Tool"
	tool.RequiresHandle = false
	tool.CanBeDropped = false
	tool.ToolTip = "Click to teleport"
	tool.Parent = backpack

	characterRuntime.clickTeleportConn = tool.Activated:Connect(function()
		local character = localPlayer.Character
		local rootPart = characterRuntime.getRootPart(character)
		local hit = mouse and mouse.Hit
		if rootPart and hit then
			rootPart.CFrame = CFrame.new(hit.X, hit.Y + 3, hit.Z, select(4, rootPart.CFrame:components()))
		end
	end)

	characterRuntime.clickTeleportTool = tool
end

-- Configurable Core Values
local siriusValues = {
	siriusVersion = "1.27",
	siriusName = "Sirius",
	releaseType = "Stable",
	betaControl = {
		endpoint = "https://siriusx.vercel.app/api/beta/control",
		clientIdFile = "beta-client-id.srs",
		clientId = nil,
		disabled = false,
		message = "",
		failClosed = false,
		heartbeatInterval = 8,
		lastHeartbeat = 0,
		noticeShown = false,
		lastCommandId = nil,
		ownerAssistTeleportAllowed = false,
		adminEnabled = false,
		adminRole = "",
		adminPermissions = {},
		adminSessions = {},
		adminSelectedClientId = nil,
		disabledFeatures = {}
	},
	siriusFolder = "Sirius",
	settingsFile = "settings.srs",
	serverHistoryFile = "server-history.srs",
	scriptSavedFile = "saved-scripts.srs",
	interfaceAsset = 14183548964,
	cdn = "https://cdn.sirius.menu/SIRIUS-SCRIPT-CORE-ASSETS/",
	icons = "https://cdn.sirius.menu/SIRIUS-SCRIPT-CORE-ASSETS/Icons/",
	enableExperienceSync = false, -- Games are no longer available due to a lack of whitelisting, they may be made open source at a later date, however they are patched as of now and are useless to the end user. Turning this on may introduce "fake functionality".
	games = {
		BreakingPoint = {
			name = "Breaking Point",
			description = "Players are seated around a table. Their only goal? To be the last one standing. Execute this script to gain an unfair advantage.",
			id = 648362523,
			enabled = true,
			raw = "BreakingPoint",
			minimumTier = "Free",
		},
		MurderMystery2 = {
			name = "Murder Mystery 2",
			description = "A murder has occured, will you be the one to find the murderer, or kill your next victim? Execute this script to gain an unfair advantage.",
			id = 142823291,
			enabled = true,
			raw = "MurderMystery2",
			minimumTier = "Free",
		},
		TowerOfHell = {
			name = "Tower Of Hell",
			description = "A difficult popular parkouring game, with random levels and modifiers. Execute this script to gain an unfair advantage.",
			id = 1962086868,
			enabled = true,
			raw = "TowerOfHell",
			minimumTier = "Free",
		},
		Strucid = {
			name = "Strucid",
			description = "Fight friends and enemies in Strucid with building mechanics! Execute this script to gain an unfair advantage.",
			id = 2377868063,
			enabled = true,
			raw = "Strucid",
			minimumTier = "Free",
		},
		PhantomForces = {
			name = "Phantom Forces",
			description = "One of the most popular FPS shooters from the team at StyLiS Studios. Execute this script to gain an unfair advantage.",
			id = 292439477,
			enabled = true,
			raw = "PhantomForces",
			minimumTier = "Pro",
		},
	},
	rawTree = "https://raw.githubusercontent.com/SiriusSoftwareLtd/Sirius/Sirius/games/",
	neonModule = "https://raw.githubusercontent.com/shlexware/Sirius/request/library/neon.lua",
	senseRaw = "https://raw.githubusercontent.com/shlexware/Sirius/request/library/sense/source.lua",
	executors = {"synapse x", "script-ware", "krnl", "scriptware", "comet", "valyse", "fluxus", "electron", "hydrogen"},
	disconnectTypes = { {"ban", {"ban", "perm"}}, {"network", {"internet connection", "network"}} },
	nameGeneration = {
		adjectives = {"Cool", "Awesome", "Epic", "Ninja", "Super", "Mystic", "Swift", "Golden", "Diamond", "Silver", "Mint", "Roblox", "Amazing"},
		nouns = {"Player", "Gamer", "Master", "Legend", "Hero", "Ninja", "Wizard", "Champion", "Warrior", "Sorcerer"}
	},
	administratorRoles = {"mod","moderator","admin","administrator","staff","dev","developer","founder","owner","creator","supervis","supervisor","manager","management","executive","president","chairman","chairwoman","chairperson","director","builder","designer","helper","support","trial","lead","head","senior"},
	transparencyProperties = {
		UIStroke = {'Transparency'},
		Frame = {'BackgroundTransparency'},
		TextButton = {'BackgroundTransparency', 'TextTransparency'},
		TextLabel = {'BackgroundTransparency', 'TextTransparency'},
		TextBox = {'BackgroundTransparency', 'TextTransparency'},
		ImageLabel = {'BackgroundTransparency', 'ImageTransparency'},
		ImageButton = {'BackgroundTransparency', 'ImageTransparency'},
		ScrollingFrame = {'BackgroundTransparency', 'ScrollBarImageTransparency'}
	},
	buttonPositions = {Character = UDim2.new(0.5, -155, 1, -29), Scripts = UDim2.new(0.5, -122, 1, -29), Playerlist = UDim2.new(0.5, -68, 1, -29)},
	chatSpy = {
		enabled = true,
		visual = {
			Color = Color3.fromRGB(26, 148, 255),
			Font = Enum.Font.SourceSansBold,
			TextSize = 18
		},
	},
	pingProfile = {
		recentPings = {},
		adaptiveBaselinePings = {},
		pingNotificationCooldown = 0,
		maxSamples = 12, -- max num of recent pings stored
		spikeThreshold = 1.75, -- high Ping in comparison to average ping (e.g 100 avg would be high at 150)
		adaptiveBaselineSamples = 30, -- how many samples Sirius takes before deciding on a fixed high ping value
		adaptiveHighPingThreshold = 120 -- default value
	},
	frameProfile = {
		frameNotificationCooldown = 0,
		fpsQueueSize = 10,
		lowFPSThreshold = 20, -- what's low fps!??!?!
		totalFPS = 0,
		fpsQueue = {},
	},
	actions = {
		{
			name = "Noclip",
			images = {14385986465, 9134787693},
			color = Color3.fromRGB(0, 170, 127),
			enabled = false,
			rotateWhileEnabled = false,
			callback = function() end,
		},
		{
			name = "Flight",
			images = {9134755504, 14385992605},
			color = Color3.fromRGB(170, 37, 46),
			enabled = false,
			rotateWhileEnabled = false,
			callback = function(value)
				local character = localPlayer.Character
				local humanoid = character and character:FindFirstChildOfClass("Humanoid")
				if humanoid then
					humanoid.PlatformStand = value
				end
			end,
		},
		{
			name = "Refresh",
			images = {9134761478, 9134761478},
			color = Color3.fromRGB(61, 179, 98),
			enabled = false,
			rotateWhileEnabled = true,
			disableAfter = 3,
			callback = function()
				task.spawn(function()
					local character = localPlayer.Character
					if character then
						local cframe = character:GetPivot()
						characterRuntime.lastRefreshPivot = cframe
						local humanoid = character:FindFirstChildOfClass("Humanoid")
						if humanoid then
							humanoid:ChangeState(Enum.HumanoidStateType.Dead)
						end
						character = localPlayer.CharacterAdded:Wait()

						local targetPivot = characterRuntime.lastRefreshPivot or cframe
						task.defer(function()
							pcall(character.PivotTo, character, targetPivot)
						end)
						task.delay(0.25, function()
							pcall(character.PivotTo, character, targetPivot)
						end)
						task.delay(0.65, function()
							pcall(character.PivotTo, character, targetPivot)
						end)
					end
				end)
			end,
		},
		{
			name = "Respawn",
			images = {9134762943, 9134762943},
			color = Color3.fromRGB(49, 88, 193),
			enabled = false,
			rotateWhileEnabled = true,
			disableAfter = 2,
			callback = function()
				local character = localPlayer.Character
				local humanoid = character and character:FindFirstChildOfClass("Humanoid")
				if humanoid then
					humanoid:ChangeState(Enum.HumanoidStateType.Dead)
				end
			end,
		},
			{
				name = "Fling",
				images = {9134785384, 14386226155},
				color = Color3.fromRGB(184, 85, 61),
			enabled = false,
			rotateWhileEnabled = true,
			callback = function(value)
				local character = localPlayer.Character
				local primaryPart = character and character.PrimaryPart
				if primaryPart then
					for _, part in ipairs(character:GetDescendants()) do
						if part:IsA("BasePart") then
							part.Massless = value
							part.CustomPhysicalProperties = PhysicalProperties.new(value and math.huge or 0.7, 0.3, 0.5)
						end
					end

					primaryPart.Anchored = true
					primaryPart.AssemblyLinearVelocity = Vector3.zero
					primaryPart.AssemblyAngularVelocity = Vector3.zero

					movers[3].Parent = value and primaryPart or nil

					task.delay(0.5, function() primaryPart.Anchored = false end)
				end
			end,
		},
		{
			name = "Extrasensory Perception",
			images = {9134780101, 14386232387},
			color = Color3.fromRGB(214, 182, 19),
			enabled = false,
			rotateWhileEnabled = false,
			callback = function(value)
				for _, highlight in ipairs(espContainer:GetChildren()) do
					highlight.Enabled = value
				end
			end,
		},
		{
			name = "Night and Day",
			images = {9134778004, 10137794784},
			color = Color3.fromRGB(102, 75, 190),
			enabled = false,
			rotateWhileEnabled = false,
			callback = function(value)
				tweenService:Create(lighting, TweenInfo.new(0.5), { ClockTime = value and 12 or 24 }):Play()
			end,
		},
		{
			name = "Global Audio",
			images = {9134774810, 14386246782},
			color = Color3.fromRGB(202, 103, 58),
			enabled = false,
			rotateWhileEnabled = false,
			callback = function(value)
				if value then
					oldVolume = gameSettings.MasterVolume
					gameSettings.MasterVolume = 0
				else
					gameSettings.MasterVolume = oldVolume
				end
			end,
		},
				{
					name = "Infinite Jump",
					images = {"96393155905923", "87597060547905"},
					color = Color3.fromRGB(194, 164, 55),
					enabled = false,
					rotateWhileEnabled = false,
				callback = function(value)
				if value then
					if characterRuntime.infJumpConn then
						characterRuntime.infJumpConn:Disconnect()
					end

					characterRuntime.infJumpConn = userInputService.JumpRequest:Connect(function()
						local character = localPlayer.Character
						local humanoid = character and character:FindFirstChildOfClass("Humanoid")
						if humanoid then
							humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
						end
					end)
				elseif characterRuntime.infJumpConn then
					characterRuntime.infJumpConn:Disconnect()
					characterRuntime.infJumpConn = nil
				end
			end,
		},
					{
						name = "Walk in Air",
						images = {"86294388029250", "135251639846809"},
						color = Color3.fromRGB(102, 164, 230),
						enabled = false,
					rotateWhileEnabled = false,
			callback = function(value)
				characterRuntime.walkInAirEnabled = value and true or false
				if characterRuntime.walkInAirEnabled then
					local character = localPlayer.Character
					local rootPart = characterRuntime.getRootPart(character)
					characterRuntime.walkInAirY = characterRuntime.getWalkInAirStandingY(character, rootPart) or characterRuntime.walkInAirY
					characterRuntime.updateWalkInAir(character, rootPart)
				else
					characterRuntime.clearWalkInAirLane()
					end
				end,
			},
						{
							name = "Anti Void",
							images = {"118657808279985", "118871974876506"},
							color = Color3.fromRGB(58, 152, 190),
							enabled = false,
							rotateWhileEnabled = false,
					callback = function(value)
						characterRuntime.applyAntiVoid(value)
					end,
				},
						{
							name = "Auto Find [works with auto aim]",
							images = {"73261468307703", "83838585292321"},
							color = Color3.fromRGB(171, 143, 58),
						enabled = false,
						rotateWhileEnabled = false,
						callback = function(value)
							characterRuntime.setAutoFindEnabled(value)
						end,
					},
						{
							name = "click teleport tool",
							images = {"126108553465711", "118617244060932"},
							color = Color3.fromRGB(156, 112, 219),
							enabled = false,
							rotateWhileEnabled = false,
					callback = function(value)
						characterRuntime.clickTeleportEnabled = value and true or false
						if characterRuntime.clickTeleportEnabled then
							characterRuntime.ensureClickTeleportTool()
						else
							characterRuntime.destroyClickTeleportTool()
						end
					end,
				},
						{
							name = "Ghost Mode / Spectate",
							images = {"125926552247878", "81062378181749"},
							color = Color3.fromRGB(112, 193, 228),
							enabled = false,
							rotateWhileEnabled = false,
					callback = function(value)
						characterRuntime.applyGhostMode(value)
					end,
				},
							{
								name = "Shaders",
								images = {"115339059810290", "116631799053167"},
								color = Color3.fromRGB(186, 120, 58),
								enabled = false,
								rotateWhileEnabled = false,
						callback = function(value)
							characterRuntime.applyShaders(value)
						end,
					},
					{
						name = "Invisibility",
						images = {
							"https://siriusx.vercel.app/icons/character-actions-central/character-invisibility-central-filled.png",
							"https://siriusx.vercel.app/icons/character-actions-central/character-invisibility-central-outline.png"
						},
						color = Color3.fromRGB(126, 132, 150),
						enabled = false,
						rotateWhileEnabled = false,
						callback = function(value)
							characterRuntime.applyInvisibility(value)
						end,
					},
				},
	sliders = {
		{
			name = "player speed",
			color = Color3.fromRGB(44, 153, 93),
			values = {0, 300},
			default = 16,
			value = 16,
			active = false,
			callback = function(value)
				local character = localPlayer.Character
				local humanoid = character and character:FindFirstChildOfClass("Humanoid")
				if character then
					humanoid.WalkSpeed = value
				end
			end,
		},
		{
			name = "jump power",
			color = Color3.fromRGB(59, 126, 184),
			values = {0, 350},
			default = 50,
			value = 50,
			active = false,
			callback = function(value)
				local character = localPlayer.Character
				local humanoid = character and character:FindFirstChildOfClass("Humanoid")
				if character then
					if humanoid.UseJumpPower then
						humanoid.JumpPower = value
					else
						humanoid.JumpHeight = value
					end
				end
			end,
		},
		{
			name = "flight speed",
			color = Color3.fromRGB(177, 45, 45),
			values = {1, 25},
			default = 3,
			value = 3,
			active = false,
			callback = function(value) end,
		},
		{
			name = "field of view",
			color = Color3.fromRGB(198, 178, 75),
			values = {0, 120},
			default = 70,
			value = 70,
			active = false,
			callback = function(value)
				tweenService:Create(camera, TweenInfo.new(0.6, Enum.EasingStyle.Exponential), { FieldOfView = value }):Play()
			end,
		},
		{
			name = "gravity",
			color = Color3.fromRGB(168, 168, 168),
			values = {0, 500},
			default = workspace.Gravity,
			value = workspace.Gravity,
			active = false,
			callback = function(value)
				characterRuntime.applyGravitySetting(value)
			end,
		},
		{
			name = "smooth screen blur",
			color = Color3.fromRGB(149, 149, 149),
			values = {0, 56},
			default = 0,
			value = 0,
			active = false,
			callback = function(value)
				local blur = lighting:FindFirstChild("SiriusSmoothScreenBlur")
				if not blur then
					blur = Instance.new("BlurEffect")
					blur.Name = "SiriusSmoothScreenBlur"
					blur.Size = 0
					blur.Enabled = false
					blur.Parent = lighting
				end

				blur.Enabled = value > 0
				tweenService:Create(blur, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = value}):Play()
			end,
		},
		{
			name = "auto aim",
			color = Color3.fromRGB(117, 203, 255),
			values = {0, 100},
			default = 0,
				value = 0,
				active = false,
				callback = function(value)
					characterRuntime.setAutoAimAssist(value)
				end,
			},
	}
}
do local __g = (type(getgenv)=="function") and getgenv() or _G; __g.__SIRIUSX_BRIDGE = siriusValues; _G.__SIRIUSX_BRIDGE = siriusValues; siriusValues.espContainer = espContainer end -- [NextGen bridge: main]

local dylanfocExecutorPresets = {
	"Synapse X",
	"Script-ware",
	"Wave",
	"Volt",
	"Zenith",
	"RC7",
	"Netflix",
	"Electron",
	"Potassium",
	"NetflixCE",
	"Delta",
	"Codex",
	"Arceux X",
	"HappyMod",
	"Sentinel",
	"Fluxus",
	"FluxusZ",
	"krnl",
	"krnlmobile",
	"Xeno",
	"Solara",
	"Cosmic",
	"AWP",
	"coco Z",
	"synwire",
	"Rune",
	"Vision",
	"Vendal",
	"Yub-x",
	"Seratonin",
	"Real",
	"Evon",
	"Orbit",
	"Valex",
	"Nihon",
	"Atlas",
	"Hydrogen",
	"JJsploit",
	"ChatGPT",
	"ByteBreaker",
	"Zeus"
}

local dylanfocCategoryName = "Pine Protect"
siriusValues.dylanfocUtilityBackgroundImage = "https://siriusx.vercel.app/dylanfoc-main.png"

local siriusSettings = {
	{
		name = 'General',
		description = 'The general settings for Sirius, from simple to unique features.',
		color = Color3.new(0.117647, 0.490196, 0.72549),
		minimumLicense = 'Free',
		categorySettings = {
			{
				name = 'Anonymous Client [Client]',
				description = 'Randomises your username only on your client by replacing local UI text in CoreGui and Sirius. Other players and the server will still see your real Roblox name. This setting can be performance intensive.',
				settingType = 'Boolean',
				current = false,

				id = 'anonmode'
			},
				{
					name = 'Hide Toggle Button',
					description = 'This will remove the option to open the smartBar with the toggle button.',
					settingType = 'Boolean',
				current = false,

				id = 'hidetoggle'
			},
			{
				name = 'Now Playing Notifications',
				description = 'When active, Sirius will notify you when the next song in your Music queue plays.',
				settingType = 'Boolean',
				current = true,

				id = 'nowplaying'
			},
			{
				name = 'ScriptBlox Script Source',
				description = 'Use ScriptBlox as a source when searching scripts inside Sirius.',
				settingType = 'Boolean',
				current = true,

				id = 'scriptsearch_scriptblox_source'
			},
			{
				name = 'Rscripts.net Script Source',
				description = 'Use Rscripts.net as a source when searching scripts inside Sirius.',
				settingType = 'Boolean',
				current = true,

				id = 'scriptsearch_rscripts_source'
			},
			{
				name = 'Roscripts.io Script Source',
				description = 'Use Roscripts.io as a source when searching scripts inside Sirius.',
				settingType = 'Boolean',
				current = true,

				id = 'scriptsearch_roscripts_io_source'
			},
			{
				name = 'Friend Notifications',
				settingType = 'Boolean', 
				current = true,

				id = 'friendnotifs'
			},
			{
				name = 'Load Hidden',
				settingType = 'Boolean',
				current = false,

				id = 'loadhidden'
			}, 
			{
				name = 'Startup Sound Effect',
				settingType = 'Boolean',
				current = true,

				id = 'startupsound'
			}, 
			{
				name = 'Anti Idle',
				description = 'Remove all callbacks and events linked to the LocalPlayer Idled state. This may prompt detection from Adonis or similar anti-cheats.',
				settingType = 'Boolean',
				current = true,

				id = 'antiidle'
			},
			{
				name = 'Client-Based Anti Kick',
				description = 'Cancel any kick request involving you sent by the client. This may prompt detection from Adonis or similar anti-cheats. You will need to rejoin and re-run Sirius to toggle.',
				settingType = 'Boolean',
				current = false,

				id = 'antikick'
			},
			{
				name = 'Muffle audio while unfocused',
				settingType = 'Boolean', 
				current = true,

				id = 'muffleunfocused'
			},
		}
	},
	{
		name = 'Servers',
		description = 'Scan servers, search players, join empty sessions, and revisit recent hops.',
		color = Color3.new(0.760784, 0.282353, 0.784314),
		minimumLicense = 'Free',
		categorySettings = {
			{
				name = 'Search & Scan Servers',
				settingType = 'Servers',
				id = 'servernavigator'
			},
		}
	},
	{
		name = dylanfocCategoryName,
		description = 'Pine Protect utility tools.',
		color = Color3.fromRGB(0, 188, 204),
		minimumLicense = 'Free',
		categorySettings = {
			{
				name = 'Run Original Pine Protect',
				description = 'Run the original Pine Protect script from its official raw source.',
				settingType = 'Action',
				action = 'run_original_dylanfoc',
				id = 'dylanfoc_run_original'
			},
			{
				name = 'HWID',
				settingType = 'Info',
				valueKey = 'hwid',
				id = 'dylanfoc_info_hwid'
			},
			{
				name = 'ClientID',
				settingType = 'Info',
				valueKey = 'clientId',
				id = 'dylanfoc_info_clientid'
			},
			{
				name = 'Executor',
				settingType = 'Info',
				valueKey = 'executor',
				id = 'dylanfoc_info_executor'
			},
			{
				name = 'UserID',
				settingType = 'Info',
				valueKey = 'userId',
				id = 'dylanfoc_info_userid'
			},
			{
				name = 'Username',
				settingType = 'Info',
				valueKey = 'username',
				id = 'dylanfoc_info_username'
			},
			{
				name = 'Copy HWID',
				description = 'Copy your current hardware identifier to the clipboard.',
				settingType = 'Action',
				action = 'copy_hwid',
				id = 'dylanfoc_copy_hwid'
			},
			{
				name = 'Copy ClientID',
				description = 'Copy your current Roblox ClientID to the clipboard.',
				settingType = 'Action',
				action = 'copy_clientid',
				id = 'dylanfoc_copy_clientid'
			},
			{
				name = 'Copy Executor',
				description = 'Copy the executor name reported by your environment.',
				settingType = 'Action',
				action = 'copy_executor',
				id = 'dylanfoc_copy_executor'
			},
			{
				name = 'Copy UserID',
				description = 'Copy the LocalPlayer UserId currently reported by Roblox.',
				settingType = 'Action',
				action = 'copy_userid',
				id = 'dylanfoc_copy_userid'
			},
			{
				name = 'Copy Username',
				description = 'Copy the LocalPlayer name currently reported by Roblox.',
				settingType = 'Action',
				action = 'copy_username',
				id = 'dylanfoc_copy_username'
			},
			{
				name = 'Refresh Client Info',
				description = 'Refresh the values displayed above without reopening Settings.',
				settingType = 'Action',
				action = 'refresh_client_info',
				id = 'dylanfoc_refresh_info'
			},
			{
				name = 'Spoof ClientID Value',
				settingType = 'Input',
				current = '',
				placeholder = 'spoofed ClientID',
				id = 'dylanfoc_spoof_clientid_value'
			},
			{
				name = 'Spoof ClientID',
				description = 'Hook GetClientId so Roblox reads the spoofed ClientID value.',
				settingType = 'Action',
				action = 'spoof_clientid',
				id = 'dylanfoc_spoof_clientid'
			},
			{
				name = 'Spoof HWID Value',
				settingType = 'Input',
				current = '',
				placeholder = 'spoofed HWID',
				id = 'dylanfoc_spoof_hwid_value'
			},
			{
				name = 'Spoof HWID',
				description = 'Hook gethwid so it returns the spoofed HWID value.',
				settingType = 'Action',
				action = 'spoof_hwid',
				id = 'dylanfoc_spoof_hwid'
			},
			{
				name = 'Executor Preset',
				description = 'Tap to cycle through common executor names, then apply the executor spoof.',
				settingType = 'Dropdown',
				current = 'Synapse X',
				values = dylanfocExecutorPresets,
				targetSetting = 'Spoof Executor Value',
				id = 'dylanfoc_executor_preset'
			},
			{
				name = 'Spoof Executor Value',
				settingType = 'Input',
				current = 'Synapse X',
				placeholder = 'executor name',
				id = 'dylanfoc_spoof_executor_value'
			},
			{
				name = 'Spoof Executor',
				description = 'Hook identifyexecutor so it returns the selected executor name.',
				settingType = 'Action',
				action = 'spoof_executor',
				id = 'dylanfoc_spoof_executor'
			},
			{
				name = 'Spoof UserID Value',
				settingType = 'Input',
				current = '',
				placeholder = 'numeric UserId',
				id = 'dylanfoc_spoof_userid_value'
			},
			{
				name = 'Spoof UserID',
				description = 'Attempt to write the LocalPlayer UserId with the spoofed numeric value.',
				settingType = 'Action',
				action = 'spoof_userid',
				id = 'dylanfoc_spoof_userid'
			},
			{
				name = 'Spoof Username Value',
				settingType = 'Input',
				current = '',
				placeholder = 'username',
				id = 'dylanfoc_spoof_username_value'
			},
			{
				name = 'Spoof Username',
				description = 'Attempt to write the LocalPlayer name with the spoofed username value.',
				settingType = 'Action',
				action = 'spoof_username',
				id = 'dylanfoc_spoof_username'
			},
			{
				name = 'Disable restorefunction',
				description = 'Dangerous: hooks restorefunction so hook rollback can no longer be used in this session.',
				settingType = 'Action',
				action = 'disable_restorefunction',
				id = 'dylanfoc_disable_restorefunction'
			},
			{
				name = 'Reset Utility State',
				description = 'Clears stored spoof values. Active hooks may require a rejoin to fully restore.',
				settingType = 'Action',
				action = 'reset_utility_state',
				id = 'dylanfoc_reset_utility_state'
			},
		}
	},
		{
			name = keybindCategoryName,
			description = 'Assign keybinds to actions and customize controls such as the one to open/close Sirius.',
			color = Color3.new(0.0941176, 0.686275, 0.509804),
			minimumLicense = 'Free',
			categorySettings = {
			{
				name = 'Command Search',
				settingType = 'Key',
				current = "Slash",
				id = 'togglesearch',
				callback = function()
					if siriusValues.runKeybindAction then
						siriusValues.runKeybindAction("commandsearch")
						return
					end

					local core = gethui and gethui() or game:GetService("CoreGui")
					local searchContainer = core:FindFirstChild("SiriusRebornInput", true)
					if searchContainer then
						local searchInput = searchContainer:FindFirstChildOfClass("TextBox")
						if searchInput then
							if searchInput:IsFocused() then
								searchInput:ReleaseFocus()
							else
								searchInput:CaptureFocus()
								task.wait()
								searchInput.Text = ""
							end
						end
					end
				end
			},
			{
				name = 'Reset Keybinds',
				description = 'Restore every keybind in this page back to the Sirius defaults.',
				settingType = 'Action',
				action = 'reset_keybinds',
				id = 'resetkeybinds',
				callback = function()
					if siriusValues.resetKeybinds then
						siriusValues.resetKeybinds()
					end
				end
			},
			{
				name = 'Panic Keybind',
				description = 'Stops active abilities and closes open Sirius panels.',
				settingType = 'Key',
				current = nil,
				id = 'panickeybind',
				callback = function()
					if siriusValues.runKeybindAction then
						siriusValues.runKeybindAction("panic")
					end
				end
			},
			{
				name = 'Toggle smartBar',
				settingType = 'Key',
				current = "K",
				id = 'smartbar'
			},
			{
				name = 'Toggle Home Panel',
				settingType = 'Key',
				current = nil,
				id = 'togglehomepanel',
				callback = function()
					if siriusValues.runKeybindAction then siriusValues.runKeybindAction("home") end
				end
			},
			{
				name = 'Toggle Character Panel',
				settingType = 'Key',
				current = nil,
				id = 'togglecharacterpanel',
				callback = function()
					if siriusValues.runKeybindAction then siriusValues.runKeybindAction("character") end
				end
			},
			{
				name = 'Toggle Scripts Panel',
				settingType = 'Key',
				current = nil,
				id = 'togglescriptspanel',
				callback = function()
					if siriusValues.runKeybindAction then siriusValues.runKeybindAction("scripts") end
				end
			},
			{
				name = 'Toggle Players Panel',
				settingType = 'Key',
				current = nil,
				id = 'toggleplayerspanel',
				callback = function()
					if siriusValues.runKeybindAction then siriusValues.runKeybindAction("players") end
				end
			},
			{
				name = 'Toggle Music Panel',
				settingType = 'Key',
				current = nil,
				id = 'togglemusicpanel',
				callback = function()
					if siriusValues.runKeybindAction then siriusValues.runKeybindAction("music") end
				end
			},
			{
				name = 'Toggle Settings Panel',
				settingType = 'Key',
				current = nil,
				id = 'togglesettingspanel',
				callback = function()
					if siriusValues.runKeybindAction then siriusValues.runKeybindAction("settings") end
				end
			},
			{
				name = 'Close Active Panel',
				settingType = 'Key',
				current = nil,
				id = 'closeactivepanel',
				callback = function()
					if siriusValues.runKeybindAction then siriusValues.runKeybindAction("close") end
				end
			},
			{
				name = 'Stop Active Abilities',
				settingType = 'Key',
				current = nil,
				id = 'stopactiveabilities',
				callback = function()
					if siriusValues.runKeybindAction then siriusValues.runKeybindAction("stopabilities") end
				end
			},
			{
				name = 'Open ScriptSearch',
				settingType = 'Key',
				current = "T",
				id = 'scriptsearch'
			},
			{
				name = 'NoClip',
				settingType = 'Key',
				current = nil,
				id = 'noclip',
				callback = function()
					local noclip = siriusValues.actions[1]
					noclip.enabled = not noclip.enabled
					noclip.callback(noclip.enabled)
				end
			},
				{
					name = 'Flight',
					settingType = 'Key',
					current = nil,
					id = 'flight',
					callback = function()
						local flight = siriusValues.actions[2]
						flight.enabled = not flight.enabled
						flight.callback(flight.enabled)
					end
				},
				{
					name = 'Fly Up Key',
					settingType = 'Key',
					current = "Space",
					id = 'flyupkey'
				},
				{
					name = 'Fly Down Key',
					settingType = 'Key',
					current = "LeftShift",
					id = 'flydownkey'
				},
				{
					name = 'Refresh',
					settingType = 'Key',
				current = nil,
				id = 'refresh',
				callback = function()
					local refresh = siriusValues.actions[3]
					if not refresh.enabled then
						refresh.enabled = true
						refresh.callback()
					end
				end
			},
			{
				name = 'Respawn',
				settingType = 'Key',
				current = nil,
				id = 'respawn',
				callback = function()
					local respawn = siriusValues.actions[4]
					if not respawn.enabled then
						respawn.enabled = true
						respawn.callback()
					end
				end
				},
				{
					name = 'Fling',
					settingType = 'Key',
					current = nil,
					id = 'fling',
					callback = function()
						local fling = siriusValues.actions[5]
						fling.enabled = not fling.enabled
						fling.callback(fling.enabled)
					end
			},
			{
				name = 'ESP',
					settingType = 'Key',
					current = nil,
					id = 'esp',
					callback = function()
						local esp = siriusValues.actions[6]
						esp.enabled = not esp.enabled
						esp.callback(esp.enabled)
					end
			},
			{
				name = 'Night and Day',
					settingType = 'Key',
					current = nil,
					id = 'nightandday',
					callback = function()
						local nightandday = siriusValues.actions[7]
						nightandday.enabled = not nightandday.enabled
						nightandday.callback(nightandday.enabled)
					end
			},
				{
					name = 'Global Audio',
						settingType = 'Key',
						current = nil,
						id = 'globalaudio',
						callback = function()
							local globalaudio = siriusValues.actions[8]
							globalaudio.enabled = not globalaudio.enabled
							globalaudio.callback(globalaudio.enabled)
						end
				},
				{
					name = 'Infinite Jump',
						settingType = 'Key',
						current = nil,
						id = 'infjump',
						callback = function()
							local infjump = siriusValues.actions[9]
							infjump.enabled = not infjump.enabled
							infjump.callback(infjump.enabled)
						end
				},
				{
					name = 'Walk in Air',
						settingType = 'Key',
						current = nil,
						id = 'walkinair',
						callback = function()
							local action = siriusValues.actions[10]
							action.enabled = not action.enabled
							action.callback(action.enabled)
						end
				},
				{
					name = 'Anti Void',
						settingType = 'Key',
						current = nil,
						id = 'antivoid',
						callback = function()
							local action = siriusValues.actions[11]
							action.enabled = not action.enabled
							action.callback(action.enabled)
						end
				},
				{
					name = 'Auto Find [works with auto aim]',
						settingType = 'Key',
						current = nil,
						id = 'autofind',
						callback = function()
							local action = siriusValues.actions[12]
							action.enabled = not action.enabled
							action.callback(action.enabled)
						end
				},
				{
					name = 'click teleport tool',
						settingType = 'Key',
						current = nil,
						id = 'clickteleporttool',
						callback = function()
							local action = siriusValues.actions[13]
							action.enabled = not action.enabled
							action.callback(action.enabled)
						end
				},
				{
					name = 'Ghost Mode / Spectate',
						settingType = 'Key',
						current = nil,
						id = 'ghostmodespectate',
						callback = function()
							local action = siriusValues.actions[14]
							action.enabled = not action.enabled
							action.callback(action.enabled)
						end
				},
				{
					name = 'Shaders',
						settingType = 'Key',
						current = nil,
						id = 'shaders',
						callback = function()
							local action = siriusValues.actions[15]
							action.enabled = not action.enabled
							action.callback(action.enabled)
						end
				},
				{
					name = 'Invisibility',
					settingType = 'Key',
					current = nil,
					id = 'invisibility',
					callback = function()
						local action = siriusValues.actions[16]
						if not action then return end
						action.enabled = not action.enabled
						action.callback(action.enabled)
					end
				},
				{
					name = 'Ghost Forward',
					settingType = 'Key',
					current = "W",
					id = 'ghostforward'
				},
				{
					name = 'Ghost Backward',
					settingType = 'Key',
					current = "S",
					id = 'ghostbackward'
				},
				{
					name = 'Ghost Left',
					settingType = 'Key',
					current = "A",
					id = 'ghostleft'
				},
				{
					name = 'Ghost Right',
					settingType = 'Key',
					current = "D",
					id = 'ghostright'
				},
				{
					name = 'Ghost Up',
					settingType = 'Key',
					current = "E",
					id = 'ghostup'
				},
				{
					name = 'Ghost Down',
					settingType = 'Key',
					current = "Q",
					id = 'ghostdown'
				},
				{
					name = 'Ghost Boost',
					settingType = 'Key',
					current = "LeftControl",
					id = 'ghostboost'
				},
				{
					name = 'Ghost Slow',
					settingType = 'Key',
					current = "LeftAlt",
					id = 'ghostslow'
				},
			}
		},
		{
			name = 'Detections',
			description = 'Sirius detects and prevents anything malicious or possibly harmful to your wellbeing.',
			color = Color3.new(0.705882, 0, 0),
			minimumLicense = 'Free',
			categorySettings = {
				{
					name = 'Spatial Shield',
					description = 'Suppress loud sounds played from any audio source in-game, in real-time with Spatial Shield.',
					settingType = 'Boolean',
					minimumLicense = 'Pro',
					current = true,
					id = 'spatialshield'
				},
				{
					name = 'Spatial Shield Threshold',
					description = 'How loud a sound needs to be to be suppressed.',
					settingType = 'Number',
					minimumLicense = 'Pro',
					values = {100, 1000},
					current = 300,
					id = 'spatialshieldthreshold'
				},
				{
					name = 'Moderator / Admin Join Detection',
					description = 'Detect players with group roles like mod, admin, staff, owner, developer, and similar moderation roles.',
					settingType = 'Boolean',
					minimumLicense = 'Pro',
					current = true,
					id = 'moddetection'
				},
				{
					name = 'Known User Watchlist',
					description = 'Notify you when a watched username, display name, or UserId joins the server.',
					settingType = 'Boolean',
					minimumLicense = 'Free',
					current = false,
					id = 'knownuserwatchlist'
				},
				{
					name = 'Watchlist Users',
					description = 'Comma-separated usernames, display names, or UserIds to watch for. Use *name* for partial matching.',
					settingType = 'Input',
					minimumLicense = 'Free',
					current = '',
					placeholder = 'username, userId',
					id = 'watchlistusers'
				},
				{
					name = 'Watchlist Protection Mode',
					description = 'Stop active abilities and show Serverhop / Leave when a watched user is detected.',
					settingType = 'Boolean',
					minimumLicense = 'Free',
					current = true,
					id = 'watchlistprotectionmode'
				},
				{
					name = 'Chat Keyword Detection',
					description = 'Notify you when chat messages contain watched keywords.',
					settingType = 'Boolean',
					minimumLicense = 'Free',
					current = false,
					id = 'chatkeyworddetection'
				},
				{
					name = 'Chat Keywords',
					description = 'Comma-separated keywords to detect in chat.',
					settingType = 'Input',
					minimumLicense = 'Free',
					current = 'admin, mod, staff, owner, developer, report, ban, kick, exploit, hacker',
					placeholder = 'admin, report, ban',
					id = 'chatkeywords'
				},
				{
					name = 'Chat Keyword Protection Mode',
					description = 'Stop active abilities and show Serverhop / Leave when a watched chat keyword is detected.',
					settingType = 'Boolean',
					minimumLicense = 'Free',
					current = false,
					id = 'chatkeywordprotectionmode'
				},
				{
					name = 'Detection Alert Cooldown',
					description = 'How many seconds Sirius waits before repeating the same chat keyword alert.',
					settingType = 'Number',
					minimumLicense = 'Free',
					values = {3, 60},
					current = 10,
					id = 'detectionalertcooldown'
				},
				{
					name = 'Intelligent HTTP Interception',
					description = 'Block external HTTP/HTTPS requests from being sent/received and ask you before allowing it to run.',
					settingType = 'Boolean',
					minimumLicense = 'Essential',
					current = true,
					id = 'intflowintercept'
				},
				{
					name = 'Intelligent Clipboard Interception',
					description = 'Block your clipboard from being set and ask you before allowing it to set your clipboard.',
					settingType = 'Boolean',
					minimumLicense = 'Essential',
					current = true,
					id = 'intflowinterceptclip'
				},
			}
		},
		{
			name = 'Chat Safety',
			description = 'Reduce chat spam and keep things clean.',
			color = Color3.fromRGB(140, 82, 255),
			minimumLicense = 'Free',
			categorySettings = {
				{
					name = 'Anti-Spam Bot',
					description = 'Detect spam, scam, repeated messages, and suspicious chat content, then hide it locally.',
					settingType = 'Boolean',
					current = true,
					id = 'antispam'
				},
				{
					name = 'Block Suspicious Links',
					description = 'Hide chat messages with suspicious links, scam domains, Discord invite spam, or Robux bait.',
					settingType = 'Boolean',
					current = true,
					id = 'blocksuspiciouslinks'
				},
				{
					name = 'Repeated Message Filter',
					description = 'Hide repeated messages from the same player when they are sent too quickly.',
					settingType = 'Boolean',
					current = true,
					id = 'repeatedmessagefilter'
				},
				{
					name = 'Custom Blocked Chat Keywords',
					description = 'Comma-separated words or phrases Sirius should hide from chat.',
					settingType = 'Input',
					current = '',
					placeholder = 'keyword, phrase',
					id = 'customblockedchatkeywords'
				},
				{
					name = 'Auto-Report Spam',
					description = 'Automatically submit a Roblox report for spam/scam when detected.',
					settingType = 'Boolean',
					current = false,
					id = 'autoreportspam'
				},
				{
					name = 'Spam Notifications',
					description = 'Show a Sirius toast when spam is filtered.',
					settingType = 'Boolean',
					current = true,
					id = 'spamnotifs'
				},
				{
					name = 'Chat Safety Alert Cooldown',
					description = 'How many seconds Sirius waits before showing another spam-filter notification.',
					settingType = 'Number',
					values = {1, 15},
					current = 3,
					id = 'chatsafetyalertcooldown'
				},
			}
		},
		{
			name = 'Performance',
		description = 'Tweak and test your performance settings for Roblox in Sirius.',
		color = Color3.new(1, 0.376471, 0.168627),
		minimumLicense = 'Free',
		categorySettings = {
			{
				name = 'Artificial FPS Limit',
				description = 'Sirius will automatically set your FPS to this number when you are tabbed-in to Roblox.',
				settingType = 'Number',
				values = {20, 5000},
				current = 240,

				id = 'fpscap'
			},
			{
				name = 'Limit FPS while unfocused',
				description = 'Sirius will automatically set your FPS to 60 when you tab-out or unfocus from Roblox.',
				settingType = 'Boolean', -- number for the cap below!! with min and max val
				current = true,

				id = 'fpsunfocused'
			},
			{
				name = 'Adaptive Latency Warning',
				description = 'Sirius will check your average latency in the background and notify you if your current latency significantly goes above your average latency.',
				settingType = 'Boolean',
				current = true,

				id = 'latencynotif'
			},
			{
				name = 'Adaptive Performance Warning',
				description = 'Sirius will check your average FPS in the background and notify you if your current FPS goes below a specific number.',
				settingType = 'Boolean',
				current = true,

				id = 'fpsnotif'
			},
		}
		},
		{
			name = 'Logging',
			description = 'Send logs to your specified webhook URL of things like player joins and leaves and messages.',
			color = Color3.new(0.905882, 0.780392, 0.0666667),
		minimumLicense = 'Free',
		categorySettings = {
			{
				name = 'Log Messages',
				description = 'Log messages sent by any player to your webhook.',
				settingType = 'Boolean',
				current = false,

				id = 'logmsg'
			},
			{
				name = 'Message Webhook URL',
				description = 'Discord Webhook URL',
				settingType = 'Input',
				current = 'No Webhook',

				id = 'logmsgurl'
			},
			{
				name = 'Log PlayerAdded and PlayerRemoving',
				description = 'Log whenever any player leaves or joins your session.',
				settingType = 'Boolean',
				current = false,

				id = 'logplrjoinleave'
			},
			{
				name = 'Player Added and Removing Webhook URL',
				description = 'Discord Webhook URL',
				settingType = 'Input',
				current = 'No Webhook',

				id = 'logplrjoinleaveurl'
			},
		}
	},
}

-- Generate random username
local randomAdjective = siriusValues.nameGeneration.adjectives[math.random(1, #siriusValues.nameGeneration.adjectives)]
local randomNoun = siriusValues.nameGeneration.nouns[math.random(1, #siriusValues.nameGeneration.nouns)]
local randomNumber = math.random(100, 3999) -- You can customize the range
local randomUsername = randomAdjective .. randomNoun .. randomNumber

-- Initialise Sirius Client Interface
local guiParent = gethui and gethui() or coreGui
local sirius = guiParent:FindFirstChild("Sirius")
if sirius then
	sirius:Destroy()
end

local UI = game:GetObjects('rbxassetid://'..siriusValues.interfaceAsset)[1]
UI.Name = siriusValues.siriusName
UI.Parent = guiParent
UI.Enabled = false

-- Create Variables for Interface Elements
local characterPanel = UI.Character
local customScriptPrompt = UI.CustomScriptPrompt
local securityPrompt = UI.SecurityPrompt
local disconnectedPrompt = UI.Disconnected
local gameDetectionPrompt = UI.GameDetection
local homeContainer = UI.Home
local moderatorDetectionPrompt = UI.ModeratorDetectionPrompt
local musicPanel = UI.Music
local notificationContainer = UI.Notifications
local playerlistPanel = UI.Playerlist
local scriptSearch = UI.ScriptSearch
local scriptsPanel = UI.Scripts
local settingsPanel = UI.Settings
local smartBar = UI.SmartBar
local toggle = UI.Toggle
local starlight = UI.Starlight
local toastsContainer = UI.Toasts

characterRuntime.characterScrollFadeReady = false
characterRuntime.characterScrollFadeBound = false
characterRuntime.characterScrollFadeOverlay = nil
characterRuntime.characterScrollTopFade = nil
characterRuntime.characterScrollBottomFade = nil
characterRuntime.characterScrollFadeState = nil
characterRuntime.characterScrollFadeBase = setmetatable({}, {__mode = "k"})
characterRuntime.characterHeaderBase = nil
characterRuntime.characterScrollHeaderIcon = nil
characterRuntime.characterScrollHeaderTitle = nil
characterRuntime.characterScrollHeaderActive = false
characterRuntime.characterScrollHeaderToken = 0

function characterRuntime.ensureCharacterPanelScrollFade()
	if not characterPanel or not characterPanel:FindFirstChild("Interactions") then return end
	if characterRuntime.characterScrollFadeReady then return end

	local interactions = characterPanel.Interactions
	if not interactions:IsA("ScrollingFrame") then return end

	for _, name in ipairs({"CharacterScrollFadeOverlay", "CharacterScrollFadeMask", "CharacterScrollTopFade", "CharacterScrollBottomFade"}) do
		local oldFade = characterPanel:FindFirstChild(name)
		if oldFade then
			oldFade:Destroy()
		end

		local oldScrollFade = interactions:FindFirstChild(name)
		if oldScrollFade then
			oldScrollFade:Destroy()
		end
	end

	local overlay = Instance.new("Frame")
	overlay.Name = "CharacterScrollFadeOverlay"
	overlay.AnchorPoint = Vector2.new(0, 0)
	overlay.Position = UDim2.new(0, 0, 0, 0)
	overlay.Size = UDim2.new(1, 0, 1, 0)
	overlay.BackgroundTransparency = 1
	overlay.BorderSizePixel = 0
	overlay.ClipsDescendants = true
	overlay.Active = false
	overlay.ZIndex = 100000
	overlay.Parent = characterPanel

	pcall(function()
		overlay.Selectable = false
	end)
	pcall(function()
		overlay.Interactable = false
	end)

	local overlayCorner = Instance.new("UICorner")
	local panelCorner = characterPanel:FindFirstChildOfClass("UICorner")
	overlayCorner.CornerRadius = panelCorner and panelCorner.CornerRadius or UDim.new(0, 18)
	overlayCorner.Parent = overlay

	local function createFade(name, isTop)
		local fade = Instance.new("Frame")
		fade.Name = name
		fade.AnchorPoint = Vector2.new(0, isTop and 0 or 1)
		fade.Position = isTop and UDim2.new(0, 0, 0, 0) or UDim2.new(0, 0, 1, 0)
		fade.Size = UDim2.new(1, 0, 0, isTop and 72 or 86)
		fade.BackgroundColor3 = characterPanel.BackgroundColor3
		fade.BackgroundTransparency = 1
		fade.BorderSizePixel = 0
		fade.ClipsDescendants = true
		fade.Active = false
		fade.Visible = false
		fade.ZIndex = overlay.ZIndex + 1
		fade.Parent = overlay

		pcall(function()
			fade.Selectable = false
		end)
		pcall(function()
			fade.Interactable = false
		end)

		local fadeCorner = Instance.new("UICorner")
		fadeCorner.CornerRadius = overlayCorner.CornerRadius
		fadeCorner.Parent = fade

		local gradient = Instance.new("UIGradient")
		gradient.Rotation = isTop and 90 or -90
		gradient.Transparency = NumberSequence.new({
			NumberSequenceKeypoint.new(0, 0),
			NumberSequenceKeypoint.new(0.12, 0.02),
			NumberSequenceKeypoint.new(0.28, 0.16),
			NumberSequenceKeypoint.new(0.54, 0.58),
			NumberSequenceKeypoint.new(0.84, 0.94),
			NumberSequenceKeypoint.new(1, 1),
		})
		gradient.Parent = fade

		return fade
	end

	characterRuntime.characterScrollFadeOverlay = overlay
	characterRuntime.characterScrollTopFade = createFade("CharacterScrollTopFade", true)
	characterRuntime.characterScrollBottomFade = createFade("CharacterScrollBottomFade", false)
	characterRuntime.characterScrollFadeState = {Top = -1, Bottom = -1}
	characterRuntime.characterScrollFadeReady = true
end

function characterRuntime.positionCharacterPanelScrollFade()
	if not characterPanel or not characterPanel:FindFirstChild("Interactions") then return end

	local interactions = characterPanel.Interactions
	if not interactions:IsA("ScrollingFrame") then return end
	if not characterRuntime.characterScrollFadeOverlay then return end

	characterRuntime.characterScrollFadeOverlay.Position = UDim2.new(0, 0, 0, 0)
	characterRuntime.characterScrollFadeOverlay.Size = UDim2.new(1, 0, 1, 0)

	if characterRuntime.characterScrollTopFade then
		characterRuntime.characterScrollTopFade.Position = UDim2.new(0, 0, 0, 0)
	end

	if characterRuntime.characterScrollBottomFade then
		characterRuntime.characterScrollBottomFade.Position = UDim2.new(0, 0, 1, 0)
	end
end

function characterRuntime.setCharacterPanelScrollFade(frame, key, strength)
	if not frame then return end
	strength = math.clamp(strength or 0, 0, 1)

	local eased = strength * strength * (3 - 2 * strength)
	characterRuntime.characterScrollFadeState = characterRuntime.characterScrollFadeState or {Top = -1, Bottom = -1}
	if math.abs((characterRuntime.characterScrollFadeState[key] or 0) - eased) < 0.01 then return end
	characterRuntime.characterScrollFadeState[key] = eased

	if eased <= 0.012 then
		frame.BackgroundTransparency = 1
		frame.Visible = false
		return
	end

	frame.Visible = true
	frame.BackgroundTransparency = 1 - math.min(eased * 0.94, 0.94)
end

function characterRuntime.captureCharacterPanelHeaderLayout(force)
	if not characterPanel then return end

	characterRuntime.characterHeaderBase = characterRuntime.characterHeaderBase or {}
	local base = characterRuntime.characterHeaderBase

	if characterPanel:FindFirstChild("Icon") and (force or not base.IconPosition) then
		base.IconPosition = characterPanel.Icon.Position
	end

	if characterPanel:FindFirstChild("Title") and (force or not base.TitlePosition) then
		base.TitlePosition = characterPanel.Title.Position
	end
end

function characterRuntime.ensureCharacterPanelScrollHeader(animated)
	if not characterPanel or not characterPanel:FindFirstChild("Interactions") then return end

	local interactions = characterPanel.Interactions
	if not interactions:IsA("ScrollingFrame") then return end

	characterRuntime.captureCharacterPanelHeaderLayout()
	local base = characterRuntime.characterHeaderBase or {}
	local fixedIcon = characterPanel:FindFirstChild("Icon")
	local fixedTitle = characterPanel:FindFirstChild("Title")
	local headerIconPosition = base.IconPosition and UDim2.new(base.IconPosition.X.Scale, base.IconPosition.X.Offset, 0, 26)
	local headerTitlePosition = base.TitlePosition and UDim2.new(base.TitlePosition.X.Scale, base.TitlePosition.X.Offset, 0, 25)
	local active = characterRuntime.characterScrollHeaderActive == true
	local animateSwap = active and animated == true

	local scrollIcon = interactions:FindFirstChild("CharacterScrollIcon")
	if not scrollIcon and fixedIcon then
		scrollIcon = fixedIcon:Clone()
		scrollIcon.Name = "CharacterScrollIcon"
		scrollIcon.Parent = interactions
	end

	local scrollTitle = interactions:FindFirstChild("CharacterScrollTitle")
	if not scrollTitle and fixedTitle then
		scrollTitle = fixedTitle:Clone()
		scrollTitle.Name = "CharacterScrollTitle"
		scrollTitle.Parent = interactions
	end

	if scrollIcon and fixedIcon and headerIconPosition then
		scrollIcon.AnchorPoint = fixedIcon.AnchorPoint
		scrollIcon.Position = headerIconPosition
		scrollIcon.Size = fixedIcon.Size
		scrollIcon.Image = fixedIcon.Image
		scrollIcon.ImageColor3 = fixedIcon.ImageColor3
		scrollIcon.ImageTransparency = animateSwap and 1 or 0
		scrollIcon.BackgroundTransparency = fixedIcon.BackgroundTransparency
		scrollIcon.ZIndex = math.max(fixedIcon.ZIndex, interactions.ZIndex + 2)
		scrollIcon.Visible = active and characterPanel.Visible
		characterRuntime.characterScrollHeaderIcon = scrollIcon
		if active and not animateSwap then
			fixedIcon.Visible = false
		end
	end

	if scrollTitle and fixedTitle and headerTitlePosition then
		scrollTitle.AnchorPoint = fixedTitle.AnchorPoint
		scrollTitle.Position = headerTitlePosition
		scrollTitle.Size = fixedTitle.Size
		scrollTitle.Text = fixedTitle.Text
		scrollTitle.Font = fixedTitle.Font
		scrollTitle.TextSize = fixedTitle.TextSize
		scrollTitle.TextColor3 = fixedTitle.TextColor3
		scrollTitle.TextTransparency = animateSwap and 1 or 0
		scrollTitle.BackgroundTransparency = fixedTitle.BackgroundTransparency
		scrollTitle.ZIndex = math.max(fixedTitle.ZIndex, interactions.ZIndex + 2)
		scrollTitle.Visible = active and characterPanel.Visible
		characterRuntime.characterScrollHeaderTitle = scrollTitle
		if active and not animateSwap then
			fixedTitle.Visible = false
		end
	end

	if animateSwap then
		local token = characterRuntime.characterScrollHeaderToken
		local fadeIn = TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
		local fadeOut = TweenInfo.new(0.16, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

		if fixedIcon then
			fixedIcon.Visible = true
			tweenService:Create(fixedIcon, fadeOut, {ImageTransparency = 1}):Play()
		end

		if fixedTitle then
			fixedTitle.Visible = true
			tweenService:Create(fixedTitle, fadeOut, {TextTransparency = 1}):Play()
		end

		if scrollIcon then
			scrollIcon.Visible = characterPanel.Visible
			tweenService:Create(scrollIcon, fadeIn, {ImageTransparency = 0}):Play()
		end

		if scrollTitle then
			scrollTitle.Visible = characterPanel.Visible
			tweenService:Create(scrollTitle, fadeIn, {TextTransparency = 0}):Play()
		end

		task.delay(0.18, function()
			if characterRuntime.characterScrollHeaderToken ~= token or not characterRuntime.characterScrollHeaderActive then return end

			if fixedIcon then
				fixedIcon.Visible = false
			end

			if fixedTitle then
				fixedTitle.Visible = false
			end

			if scrollIcon then
				scrollIcon.ImageTransparency = 0
			end

			if scrollTitle then
				scrollTitle.TextTransparency = 0
			end
		end)
	end
end

function characterRuntime.resetCharacterPanelHeaderScroll(forceVisible)
	if not characterPanel then return end
	characterRuntime.characterScrollHeaderActive = false
	characterRuntime.characterScrollHeaderToken = (characterRuntime.characterScrollHeaderToken or 0) + 1
	characterRuntime.captureCharacterPanelHeaderLayout()

	if characterPanel:FindFirstChild("Interactions") then
		local interactions = characterPanel.Interactions
		for _, name in ipairs({"CharacterScrollIcon", "CharacterScrollTitle"}) do
			local scrollHeader = interactions:FindFirstChild(name)
			if scrollHeader then
				scrollHeader.Visible = false
			end
		end
	end

	local base = characterRuntime.characterHeaderBase or {}
	if characterPanel:FindFirstChild("Icon") and base.IconPosition then
		characterPanel.Icon.Visible = true
		characterPanel.Icon.Position = base.IconPosition
		if forceVisible then
			characterPanel.Icon.ImageTransparency = 0
		end
	end

	if characterPanel:FindFirstChild("Title") and base.TitlePosition then
		characterPanel.Title.Visible = true
		characterPanel.Title.Position = base.TitlePosition
		if forceVisible then
			characterPanel.Title.TextTransparency = 0
		end
	end
end

function characterRuntime.activateCharacterPanelScrollHeader(animated)
	if not characterPanel or not characterPanel:FindFirstChild("Interactions") then return end
	if not characterPanel.Interactions:IsA("ScrollingFrame") then return end

	characterRuntime.characterScrollHeaderActive = true
	characterRuntime.characterScrollHeaderToken = (characterRuntime.characterScrollHeaderToken or 0) + 1
	characterRuntime.ensureCharacterPanelScrollHeader(animated)
end

function characterRuntime.applyCharacterScrollObjectFade(root, strength)
	if not root or not root:IsA("GuiObject") then return end

	characterRuntime.characterScrollFadeBase = characterRuntime.characterScrollFadeBase or setmetatable({}, {__mode = "k"})
	strength = math.clamp(strength or 0, 0, 0.9)
	local isClear = strength <= 0.012

	local function applyObject(object)
		local properties = siriusValues.transparencyProperties[object.ClassName]
		if not properties then return end

		local base = characterRuntime.characterScrollFadeBase[object]
		if not base then
			base = {}
			characterRuntime.characterScrollFadeBase[object] = base
		end

		for _, property in ipairs(properties) do
			local current = object[property]
			if base[property] == nil then
				base[property] = current
			end

			local original = base[property]
			if type(original) == "number" then
				if isClear then
					object[property] = original
				else
					object[property] = math.clamp(original + ((1 - original) * strength), 0, 1)
				end
			end
		end
	end

	applyObject(root)
	for _, descendant in ipairs(root:GetDescendants()) do
		if descendant.Name ~= "Interact" then
			applyObject(descendant)
		end
	end
end

function characterRuntime.updateCharacterPanelHeaderScroll()
	if characterRuntime.characterScrollHeaderActive then
		characterRuntime.ensureCharacterPanelScrollHeader()
	end
end

function characterRuntime.refreshCharacterPanelItemFade()
	if not characterRuntime.characterScrollFadeBase then return end

	for object, base in pairs(characterRuntime.characterScrollFadeBase) do
		if object and object.Parent and base then
			for property, value in pairs(base) do
				pcall(function()
					object[property] = value
				end)
			end
		end
	end

	characterRuntime.characterScrollFadeBase = setmetatable({}, {__mode = "k"})
end

function characterRuntime.refreshCharacterPanelScrollFade()
	if not characterPanel or not characterPanel:FindFirstChild("Interactions") then return end
	characterRuntime.ensureCharacterPanelScrollFade()

	local interactions = characterPanel.Interactions
	if not interactions:IsA("ScrollingFrame") then return end

	local maxY = math.max(0, interactions.AbsoluteCanvasSize.Y - interactions.AbsoluteWindowSize.Y)
	local currentY = interactions.CanvasPosition.Y
	local fadeRange = 98

	characterRuntime.positionCharacterPanelScrollFade()
	characterRuntime.setCharacterPanelScrollFade(characterRuntime.characterScrollTopFade, "Top", currentY / fadeRange)
	characterRuntime.setCharacterPanelScrollFade(characterRuntime.characterScrollBottomFade, "Bottom", (maxY - currentY) / fadeRange)
	characterRuntime.updateCharacterPanelHeaderScroll()
	characterRuntime.refreshCharacterPanelItemFade()

	if not characterRuntime.characterScrollFadeBound then
		characterRuntime.characterScrollFadeBound = true
		interactions:GetPropertyChangedSignal("CanvasPosition"):Connect(function()
			characterRuntime.refreshCharacterPanelScrollFade()
		end)
		interactions:GetPropertyChangedSignal("AbsoluteCanvasSize"):Connect(function()
			characterRuntime.refreshCharacterPanelScrollFade()
		end)
		interactions:GetPropertyChangedSignal("AbsoluteWindowSize"):Connect(function()
			characterRuntime.refreshCharacterPanelScrollFade()
		end)
	end
end

function characterRuntime.ensureCharacterPanelScroll()
	if not characterPanel or not characterPanel:FindFirstChild("Interactions") then return end

	if characterPanel.Interactions:IsA("ScrollingFrame") then
		characterPanel.Interactions.AnchorPoint = Vector2.new(0, 0)
		characterPanel.Interactions.Position = UDim2.new(0, 0, 0, 0)
		characterPanel.Interactions.Size = UDim2.new(1, 0, 1, 0)
		characterPanel.Interactions.BackgroundTransparency = 1
		characterPanel.Interactions.ClipsDescendants = true
		characterPanel.Interactions.Active = true
		characterPanel.Interactions.ScrollBarThickness = 3
		characterPanel.Interactions.ScrollBarImageTransparency = 1
		characterRuntime.ensureCharacterPanelScrollFade()
		return
	end

	local oldInteractions = characterPanel.Interactions
	local scroller = Instance.new("ScrollingFrame")
	scroller.Name = "Interactions"
	scroller.AnchorPoint = Vector2.new(0, 0)
	scroller.Position = UDim2.new(0, 0, 0, 0)
	scroller.Size = UDim2.new(1, 0, 1, 0)
	scroller.BackgroundColor3 = oldInteractions.BackgroundColor3
	scroller.BackgroundTransparency = 1
	scroller.BorderColor3 = oldInteractions.BorderColor3
	scroller.BorderSizePixel = oldInteractions.BorderSizePixel
	scroller.ClipsDescendants = true
	scroller.Active = true
	scroller.Visible = oldInteractions.Visible
	scroller.ZIndex = oldInteractions.ZIndex
	scroller.LayoutOrder = oldInteractions.LayoutOrder
	scroller.Rotation = oldInteractions.Rotation
	scroller.ScrollBarThickness = 3
	scroller.ScrollBarImageColor3 = Color3.fromRGB(210, 214, 220)
	scroller.ScrollBarImageTransparency = 1
	scroller.ScrollingDirection = Enum.ScrollingDirection.Y
	scroller.CanvasSize = UDim2.new(0, 0, 0, oldInteractions.AbsoluteSize.Y)
	scroller.Parent = characterPanel

	for _, child in ipairs(oldInteractions:GetChildren()) do
		child.Parent = scroller
	end

	oldInteractions:Destroy()
	characterRuntime.ensureCharacterPanelScrollFade()
end

function characterRuntime.positionCharacterPanelContent()
	if not characterPanel or not characterPanel:FindFirstChild("Interactions") then return end

	local interactions = characterPanel.Interactions
	local titleY = 58
	local contentY = 82

	local function setY(object, y)
		if object and object:IsA("GuiObject") then
			object.Position = UDim2.new(object.Position.X.Scale, object.Position.X.Offset, 0, y)
		end
	end

	setY(interactions:FindFirstChild("PropertiesTitle"), titleY)
	setY(interactions:FindFirstChild("ActionsTitle"), titleY)
	setY(interactions:FindFirstChild("Reset"), titleY - 4)
	setY(interactions:FindFirstChild("Sliders"), contentY)
	setY(interactions:FindFirstChild("Grid"), contentY)

	task.defer(function()
		if not interactions or not interactions.Parent then return end

		local grid = interactions:FindFirstChild("Grid")
		local gridLayout = grid and (grid:FindFirstChildOfClass("UIGridLayout") or grid:FindFirstChildOfClass("UIListLayout"))
		local buttonY = 194

		if grid and gridLayout then
			buttonY = math.max(buttonY, grid.Position.Y.Offset + gridLayout.AbsoluteContentSize.Y + 16)
		end

		setY(interactions:FindFirstChild("Serverhop"), buttonY)
		setY(interactions:FindFirstChild("Rejoin"), buttonY)
	end)
end

function characterRuntime.refreshCharacterPanelScroll()
	if not characterPanel or not characterPanel:FindFirstChild("Interactions") then return end

	local interactions = characterPanel.Interactions
	if not interactions:IsA("ScrollingFrame") then return end
	characterRuntime.positionCharacterPanelContent()

	local function fitHolder(holder)
		if not holder or not holder:IsA("GuiObject") then return end

		local layout = holder:FindFirstChildOfClass("UIGridLayout") or holder:FindFirstChildOfClass("UIListLayout")
		if not layout then return end

		local contentHeight = layout.AbsoluteContentSize.Y
		if contentHeight > 0 then
			holder.Size = UDim2.new(holder.Size.X.Scale, holder.Size.X.Offset, 0, contentHeight + 12)
		end
	end

	fitHolder(interactions:FindFirstChild("Grid"))
	fitHolder(interactions:FindFirstChild("Sliders"))

	task.defer(function()
		if not interactions or not interactions.Parent then return end

		local bottom = interactions.AbsoluteWindowSize.Y
		for _, child in ipairs(interactions:GetChildren()) do
			if child:IsA("GuiObject") and child.Visible and child.Name ~= "CharacterScrollFadeMask" and child.Name ~= "CharacterScrollFadeOverlay" then
				local childBottom = (child.AbsolutePosition.Y - interactions.AbsolutePosition.Y) + child.AbsoluteSize.Y
				bottom = math.max(bottom, childBottom)
			end
		end

		interactions.CanvasSize = UDim2.new(0, 0, 0, bottom + 28)
		characterRuntime.refreshCharacterPanelScrollFade()
	end)
end

characterRuntime.ensureCharacterPanelScroll()

do
	local existingSearch = smartBar:FindFirstChild("SiriusRebornInput")
	if existingSearch then
		existingSearch:Destroy()
	end

	local searchContainer = Instance.new("Frame")
	searchContainer.Name = "SiriusRebornInput"
	searchContainer.Size = UDim2.new(0, 180, 0, 32)
	searchContainer.Position = UDim2.new(1, -85, 0.5, 0)
	searchContainer.AnchorPoint = Vector2.new(1, 0.5)
	searchContainer.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
	searchContainer.BackgroundTransparency = 0.2
	searchContainer.BorderSizePixel = 0
	searchContainer.ZIndex = 10000
	searchContainer.Parent = smartBar

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(1, 0)
	corner.Parent = searchContainer

	local stroke = Instance.new("UIStroke")
	stroke.Color = Color3.fromRGB(100, 100, 100)
	stroke.Thickness = 1
	stroke.Transparency = 0.5
	stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
	stroke.Parent = searchContainer

	local icon = Instance.new("ImageLabel")
	icon.Size = UDim2.new(0, 16, 0, 16)
	icon.Position = UDim2.new(0, 12, 0.5, 0)
	icon.AnchorPoint = Vector2.new(0, 0.5)
	icon.BackgroundTransparency = 1
	icon.Image = "rbxassetid://6031154871"
	icon.ImageColor3 = Color3.fromRGB(200, 200, 200)
	icon.ZIndex = 10002
	icon.Parent = searchContainer

	local input = Instance.new("TextBox")
	input.Name = "Input"
	input.Size = UDim2.new(1, -34, 1, 0)
	input.Position = UDim2.new(0, 34, 0, 0)
	input.BackgroundTransparency = 1
	input.ClearTextOnFocus = false
	input.Text = ""
	input.PlaceholderText = "Search command..."
	input.PlaceholderColor3 = Color3.fromRGB(180, 180, 180)
	input.TextColor3 = Color3.fromRGB(255, 255, 255)
	input.Font = Enum.Font.GothamMedium
	input.TextSize = 14
	input.TextXAlignment = Enum.TextXAlignment.Left
	input.TextYAlignment = Enum.TextYAlignment.Center
	input.ZIndex = 10001
	input.Parent = searchContainer

	function siriusValues.syncSmartBarCommandSearchLayout()
		local width = smartBar.AbsoluteSize.X
		if width <= 0 then
			width = smartBar.Size.X.Offset
		end

		local compact = width < 520
		local adminOpen = siriusValues.betaControl and siriusValues.betaControl.adminEnabled
		local searchWidth = math.clamp(width - (compact and (adminOpen and 374 or 322) or (adminOpen and 462 or 401)), compact and 104 or 128, adminOpen and 158 or 180)

		searchContainer.Size = UDim2.new(0, searchWidth, 0, compact and 30 or 32)
		searchContainer.Position = UDim2.new(1, compact and -72 or -85, 0.5, 0)
		icon.Size = UDim2.new(0, compact and 15 or 16, 0, compact and 15 or 16)
		icon.Position = UDim2.new(0, compact and 10 or 12, 0.5, 0)
		input.Position = UDim2.new(0, compact and 31 or 34, 0, 0)
		input.Size = UDim2.new(1, compact and -31 or -34, 1, 0)
		input.TextSize = compact and 13 or 14
		input.PlaceholderText = compact and "Search" or "Search command..."
	end

	local suggestionShadow = Instance.new("ImageLabel")
	suggestionShadow.Name = "SuggestionShadow"
	suggestionShadow.AnchorPoint = Vector2.new(0.5, 1)
	suggestionShadow.Position = UDim2.new(0.5, 0, 0, -10)
	suggestionShadow.Size = UDim2.new(1, 34, 0, 34)
	suggestionShadow.BackgroundTransparency = 1
	suggestionShadow.Image = "rbxassetid://8992230677"
	suggestionShadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
	suggestionShadow.ImageTransparency = 1
	suggestionShadow.ScaleType = Enum.ScaleType.Slice
	suggestionShadow.SliceCenter = Rect.new(99, 99, 99, 99)
	suggestionShadow.Visible = false
	suggestionShadow.ZIndex = 10004
	suggestionShadow.Parent = searchContainer

	local suggestions = Instance.new("ScrollingFrame")
	suggestions.Name = "SuggestionFrame"
	suggestions.AnchorPoint = Vector2.new(0, 1)
	suggestions.Size = UDim2.new(1, 0, 0, 0)
	suggestions.Position = UDim2.new(0, 0, 0, -10)
	suggestions.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
	suggestions.BackgroundTransparency = 1
	suggestions.BorderSizePixel = 0
	suggestions.ClipsDescendants = true
	suggestions.ScrollingDirection = Enum.ScrollingDirection.XY
	suggestions.ScrollBarThickness = 3
	suggestions.ScrollBarImageColor3 = Color3.fromRGB(170, 170, 175)
	suggestions.ScrollBarImageTransparency = 0.72
	suggestions.Visible = false
	suggestions.ZIndex = 10005
	suggestions.Parent = searchContainer

	local suggestionCorner = Instance.new("UICorner")
	suggestionCorner.CornerRadius = UDim.new(0, 18)
	suggestionCorner.Parent = suggestions

	local suggestionScale = Instance.new("UIScale")
	suggestionScale.Scale = 0.985
	suggestionScale.Parent = suggestions

	local suggestionStroke = Instance.new("UIStroke")
	suggestionStroke.Color = Color3.fromRGB(78, 78, 82)
	suggestionStroke.Thickness = 1
	suggestionStroke.Transparency = 1
	suggestionStroke.Parent = suggestions

	local suggestionList = Instance.new("UIListLayout")
	suggestionList.SortOrder = Enum.SortOrder.LayoutOrder
	suggestionList.Padding = UDim.new(0, 0)
	suggestionList.Parent = suggestions

	local suggestionPadding = Instance.new("UIPadding")
	suggestionPadding.PaddingTop = UDim.new(0, 16)
	suggestionPadding.PaddingBottom = UDim.new(0, 16)
	suggestionPadding.PaddingLeft = UDim.new(0, 16)
	suggestionPadding.PaddingRight = UDim.new(0, 12)
	suggestionPadding.Parent = suggestions

	local fallbackCommands = {
		"noclip", "unnoclip", "clip", "fly", "unfly", "flyspeed", "float", "unfloat",
		"swim", "unswim", "goto", "tweengoto", "tweenspeed", "teleporttool", "tptool",
		"esp", "noesp", "chams", "nochams", "locate", "unlocate", "spectate", "view",
		"unspectate", "unview", "freecam", "unfreecam", "fov", "fixcam", "restorecam",
		"refresh", "re", "respawn", "reset", "god", "invisible", "visible", "speed",
		"walkspeed", "ws", "jumppower", "jp", "gravity", "sit", "jump", "infinitejump",
		"infjump", "uninfinitejump", "uninfjump", "antivoid", "unantivoid", "day",
		"night", "fullbright", "nofog", "rejoin", "rj", "serverhop", "shop", "antiidle",
		"antiafk", "antilag", "boostfps", "lowgraphics", "volume", "ping", "jobid"
	}

	local function clearSuggestions()
		for _, child in ipairs(suggestions:GetChildren()) do
			if child:IsA("TextButton") then
				child:Destroy()
			end
		end
	end

	local function hideSuggestions()
		tweenService:Create(suggestions, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
			Size = UDim2.new(1, 0, 0, 0),
			BackgroundTransparency = 1
		}):Play()
		tweenService:Create(suggestionScale, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Scale = 0.985}):Play()
		tweenService:Create(suggestionStroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 1}):Play()
		tweenService:Create(suggestionShadow, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
			Size = UDim2.new(1, 34, 0, 34),
			ImageTransparency = 1
		}):Play()

		for _, child in ipairs(suggestions:GetChildren()) do
			if child:IsA("TextButton") then
				tweenService:Create(child, TweenInfo.new(0.14, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
					TextTransparency = 1,
					BackgroundTransparency = 1
				}):Play()
			end
		end

		task.delay(0.24, function()
			if input.Text == "" or not input:IsFocused() then
				suggestions.Visible = false
				suggestionShadow.Visible = false
			end
		end)
	end

	local function getActiveSearchTokens()
		if type(getSiriusIntegratedIYSearchTokens) == "function" then
			return getSiriusIntegratedIYSearchTokens(fallbackCommands)
		end

		return fallbackCommands
	end

	local function updateSuggestions(filterText)
		clearSuggestions()

		local query = tostring(filterText or ""):lower():match("^%s*(%S*)") or ""
		if query == "" then
			hideSuggestions()
			return
		end

		local count = 0
		local seen = {}
		local createdButtons = {}
		local rowHeight = 30
		local rowTextSize = 15
		local maxTextWidth = math.max(searchContainer.AbsoluteSize.X - 28, 130)

		for _, commandName in ipairs(getActiveSearchTokens()) do
			commandName = tostring(commandName or ""):lower()
			if commandName ~= "" and not seen[commandName] and commandName:find(query, 1, true) then
				seen[commandName] = true
				count += 1
				local measured = game:GetService("TextService"):GetTextSize(commandName, rowTextSize, Enum.Font.GothamMedium, Vector2.new(2000, rowHeight))
				maxTextWidth = math.max(maxTextWidth, measured.X + 14)

				local button = Instance.new("TextButton")
				button.Name = commandName
				button.Size = UDim2.new(0, maxTextWidth, 0, rowHeight)
				button.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
				button.BackgroundTransparency = 1
				button.AutoButtonColor = false
				button.Text = commandName
				button.TextColor3 = Color3.fromRGB(230, 230, 235)
				button.TextTransparency = 1
				button.TextXAlignment = Enum.TextXAlignment.Left
				button.TextTruncate = Enum.TextTruncate.None
				button.TextWrapped = false
				button.Font = Enum.Font.GothamMedium
				button.TextSize = rowTextSize
				button.LayoutOrder = count
				button.ZIndex = 10006
				button.Parent = suggestions
				table.insert(createdButtons, button)

				local buttonCorner = Instance.new("UICorner")
				buttonCorner.CornerRadius = UDim.new(0, 9)
				buttonCorner.Parent = button

				button.MouseEnter:Connect(function()
					tweenService:Create(button, TweenInfo.new(0.16, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
						BackgroundTransparency = 0.88,
						TextColor3 = Color3.fromRGB(255, 255, 255)
					}):Play()
				end)

				button.MouseLeave:Connect(function()
					tweenService:Create(button, TweenInfo.new(0.16, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
						BackgroundTransparency = 1,
						TextColor3 = Color3.fromRGB(230, 230, 235)
					}):Play()
				end)

				button.MouseButton1Click:Connect(function()
					local args = input.Text:match("^%s*%S+%s+(.+)$")
					input.Text = args and (commandName.." "..args) or commandName
					input.CursorPosition = #input.Text + 1
					input:CaptureFocus()
				end)

				task.delay(count * 0.008, function()
					if button.Parent then
						tweenService:Create(button, TweenInfo.new(0.2, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {TextTransparency = 0.04}):Play()
					end
				end)

				if count >= 10 then
					break
				end
			end
		end

		if count == 0 then
			hideSuggestions()
			return
		end

		suggestions.Visible = true
		suggestionShadow.Visible = true
		suggestionScale.Scale = 0.985
		local viewportWidth = math.max(searchContainer.AbsoluteSize.X, searchContainer.Size.X.Offset, 160)
		local rowWidth = math.max(viewportWidth - 28, maxTextWidth)
		for _, button in ipairs(createdButtons) do
			button.Size = UDim2.new(0, rowWidth, 0, rowHeight)
		end
		local suggestionHeight = count * rowHeight + 28
		suggestions.CanvasPosition = Vector2.new(0, 0)
		suggestions.CanvasSize = UDim2.new(0, rowWidth + 28, 0, suggestionHeight)
		suggestionShadow.Size = UDim2.new(1, 34, 0, 34)
		suggestionShadow.ImageTransparency = 1
		tweenService:Create(suggestions, TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
			Size = UDim2.new(1, 0, 0, math.min(suggestionHeight, 286)),
			BackgroundTransparency = 0.04
		}):Play()
		tweenService:Create(suggestionScale, TweenInfo.new(0.26, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Scale = 1}):Play()
		tweenService:Create(suggestionStroke, TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.12}):Play()
		tweenService:Create(suggestionShadow, TweenInfo.new(0.26, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
			Size = UDim2.new(1, 34, 0, math.min(suggestionHeight, 286) + 34),
			ImageTransparency = 0.72
		}):Play()
	end

	local function runSearchCommand()
		local commandText = input.Text:gsub("^%s+", ""):gsub("%s+$", "")
		if commandText == "" then return end

		if type(runIntegratedIYCommand) == "function" then
			runIntegratedIYCommand(commandText, localPlayer)
		elseif type(execCmd) == "function" then
			execCmd(commandText, localPlayer)
		elseif type(queueNotification) == "function" then
			queueNotification("Command Search", "Command runtime is still loading. Try again in a second.", 4370336704)
		else
			warn("Command runtime is still loading")
		end

		input.Text = ""
	end

	CmdInput = input

	input:GetPropertyChangedSignal("Text"):Connect(function()
		if input:IsFocused() and input.Text ~= "" then
			updateSuggestions(input.Text)
		else
			hideSuggestions()
		end
	end)

	input.Focused:Connect(function()
		if not smartBarOpen then
			input:ReleaseFocus()
			return
		end

		tweenService:Create(searchContainer, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(60, 60, 60)}):Play()
		tweenService:Create(stroke, TweenInfo.new(0.2), {Color = Color3.fromRGB(255, 255, 255)}):Play()

		if input.Text ~= "" then
			updateSuggestions(input.Text)
		end
	end)

	input.FocusLost:Connect(function(enterPressed)
		tweenService:Create(searchContainer, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(45, 45, 45)}):Play()
		tweenService:Create(stroke, TweenInfo.new(0.2), {Color = Color3.fromRGB(100, 100, 100)}):Play()
		hideSuggestions()

		if enterPressed then
			runSearchCommand()
		end
	end)

	local function syncSearchVisibility()
		local isVisible = smartBar.Visible
		local transparency = smartBar.BackgroundTransparency
		local fadeFactor
		local tweenDuration

		if transparency > 0.5 then
			fadeFactor = 3
			tweenDuration = 0.1
			searchContainer.Visible = false
		else
			fadeFactor = 1.5
			tweenDuration = 0.2
			if isVisible then
				searchContainer.Visible = true
			end
		end

		local targetTransparency = transparency * fadeFactor
		if transparency == 0 then targetTransparency = 0.2 end
		if targetTransparency > 1 then targetTransparency = 1 end

		tweenService:Create(searchContainer, TweenInfo.new(tweenDuration), {BackgroundTransparency = targetTransparency}):Play()
		tweenService:Create(stroke, TweenInfo.new(tweenDuration), {Transparency = targetTransparency}):Play()
		tweenService:Create(icon, TweenInfo.new(tweenDuration), {ImageTransparency = targetTransparency}):Play()
		tweenService:Create(input, TweenInfo.new(tweenDuration), {
			TextTransparency = targetTransparency,
			PlaceholderColor3 = Color3.fromRGB(180, 180, 180)
		}):Play()

		if targetTransparency >= 1 then
			input:ReleaseFocus()
			hideSuggestions()
		end
	end

	smartBar:GetPropertyChangedSignal("Visible"):Connect(syncSearchVisibility)
	smartBar:GetPropertyChangedSignal("BackgroundTransparency"):Connect(syncSearchVisibility)
	smartBar:GetPropertyChangedSignal("AbsoluteSize"):Connect(siriusValues.syncSmartBarCommandSearchLayout)
	task.defer(siriusValues.syncSmartBarCommandSearchLayout)
	task.defer(syncSearchVisibility)
end

-- Interface Caching
if typeof(getgenv) ~= "function" then
	getgenv = function()
		return _G
	end
end

if not getgenv().cachedInGameUI then getgenv().cachedInGameUI = {} end
if not getgenv().cachedCoreUI then getgenv().cachedCoreUI = {} end

function siriusValues.getExecutorDisplayName()
	if typeof(identifyexecutor) == "function" then
		local success, value = pcall(identifyexecutor)
		if success and value then
			return tostring(value)
		end
	end

	return "Unknown Executor"
end

if typeof(getgenv()["LPH_NO_VIRTUALIZE"]) ~= "function" then
	getgenv()["LPH_NO_VIRTUALIZE"] = function(callback)
		return callback
	end
end

-- Malicious Behavior Prevention
local indexSetClipboard = "setclipboard"
local originalSetClipboard = getgenv()[indexSetClipboard]

local index = http_request and "http_request" or "request"
local originalRequest = getgenv()[index]

-- put this into siriusValues, like the fps and ping shit
local suppressedSounds = {}
local soundSuppressionNotificationCooldown = 0
local soundInstances = {}
local cachedIds = {}
local cachedText = {}
local scriptImageCache = {}
local scriptRunAccentPalette = {
	Color3.fromRGB(30, 125, 185),
	Color3.fromRGB(24, 164, 130),
	Color3.fromRGB(116, 92, 210),
	Color3.fromRGB(64, 144, 210)
}
local scriptSearchBrand = {
	base = Color3.fromRGB(8, 9, 10),
	panel = Color3.fromRGB(13, 14, 16),
	card = Color3.fromRGB(18, 19, 22),
	cardSoft = Color3.fromRGB(24, 25, 28),
	stroke = Color3.fromRGB(74, 78, 86),
	text = Color3.fromRGB(245, 247, 250),
	muted = Color3.fromRGB(168, 174, 184),
	accent = Color3.fromRGB(30, 125, 185),
	accentAlt = Color3.fromRGB(24, 164, 130)
}
local scriptSaveState = {
	savedMode = false,
	cache = nil,
	index = {},
	topButton = nil,
	header = nil,
	viewToken = 0,
	refreshToken = 0,
	searchToken = 0,
	promptToken = 0,
	viewSwitchToken = 0,
	transition = "closed",
	viewSwitchLocked = false,
	runPromptActive = false,
	outlineIcon = "rbxthumb://type=Asset&id=104884462500593&w=150&h=150",
	filledIcon = "rbxthumb://type=Asset&id=121543597839044&w=150&h=150",
	outlineIconSize = UDim2.new(0, 26, 0, 26),
	filledIconSize = UDim2.new(0, 22, 0, 22),
	searchRevealSettleDelay = 1,
	savedRevealSettleDelay = 0.45,
	listPosition = nil,
	listSize = nil,
	ensureControls = function() end,
	setBookmarkVisual = function() end,
	nextSearchToken = function() end,
	isCurrentSearch = function() end,
	showSavedEmptyState = function() end,
	renderSavedResults = function() end,
	scheduleSavedRefresh = function() end,
	showSavedList = function() end,
	showSearchView = function() end
}
scriptSaveState.searchGradientColor = scriptSearch.UIGradient.Color
scriptSaveState.searchGradientRotation = scriptSearch.UIGradient.Rotation
scriptSaveState.savedGradientColor = ColorSequence.new({
	ColorSequenceKeypoint.new(0, scriptSearchBrand.panel),
	ColorSequenceKeypoint.new(0.58, scriptSearchBrand.base),
	ColorSequenceKeypoint.new(0.86, Color3.fromRGB(9, 18, 24)),
	ColorSequenceKeypoint.new(1, scriptSearchBrand.accent:Lerp(Color3.fromRGB(0, 0, 0), 0.38))
})
scriptSaveState.savedAccentColor = scriptSearchBrand.accent

if not getMessage then siriusValues.chatSpy.enabled = false end

-- Call External Modules

-- httpRequest
local httpRequest = originalRequest

-- Neon Module
--local neonModule = (function() -- Open sourced neon module
--	local module = {}
--	do
--		local function IsNotNaN(x)
--			return x == x
--		end
--		local continued = IsNotNaN(camera:ScreenPointToRay(0,0).Origin.x)
--		while not continued do
--			runService.RenderStepped:wait()
--			continued = IsNotNaN(camera:ScreenPointToRay(0,0).Origin.x)
--		end
--	end

--	local RootParent = camera
--	local root
--	local binds = {}

--	local function getRoot()
--		if root then 
--			return root
--		else
--			root = Instance.new('Folder', RootParent)
--			root.Name = 'neon'
--			return root
--		end
--	end

--	local function destroyRoot()
--		if root then 
--			root:Destroy()
--			root = nil
--		end
--	end

--	local GenUid; do
--		local id = 0
--		function GenUid()
--			id = id + 1
--			return 'neon::'..tostring(id)
--		end
--	end

--	local DrawQuad; do
--		local acos, max, pi, sqrt = math.acos, math.max, math.pi, math.sqrt
--		local sz = 0.2

--		local function DrawTriangle(v1, v2, v3, p0, p1)
--			local s1 = (v1 - v2).magnitude
--			local s2 = (v2 - v3).magnitude
--			local s3 = (v3 - v1).magnitude
--			local smax = max(s1, s2, s3)
--			local A, B, C
--			if s1 == smax then
--				A, B, C = v1, v2, v3
--			elseif s2 == smax then
--				A, B, C = v2, v3, v1
--			elseif s3 == smax then
--				A, B, C = v3, v1, v2
--			end

--			local para = ( (B-A).x*(C-A).x + (B-A).y*(C-A).y + (B-A).z*(C-A).z ) / (A-B).magnitude
--			local perp = sqrt((C-A).magnitude^2 - para*para)
--			local dif_para = (A - B).magnitude - para

--			local st = CFrame.new(B, A)
--			local za = CFrame.Angles(pi/2,0,0)

--			local cf0 = st

--			local Top_Look = (cf0 * za).lookVector
--			local Mid_Point = A + CFrame.new(A, B).LookVector * para
--			local Needed_Look = CFrame.new(Mid_Point, C).LookVector
--			local dot = Top_Look.x*Needed_Look.x + Top_Look.y*Needed_Look.y + Top_Look.z*Needed_Look.z

--			local ac = CFrame.Angles(0, 0, acos(dot))

--			cf0 = cf0 * ac
--			if ((cf0 * za).lookVector - Needed_Look).magnitude > 0.01 then
--				cf0 = cf0 * CFrame.Angles(0, 0, -2*acos(dot))
--			end
--			cf0 = cf0 * CFrame.new(0, perp/2, -(dif_para + para/2))

--			local cf1 = st * ac * CFrame.Angles(0, pi, 0)
--			if ((cf1 * za).lookVector - Needed_Look).magnitude > 0.01 then
--				cf1 = cf1 * CFrame.Angles(0, 0, 2*acos(dot))
--			end
--			cf1 = cf1 * CFrame.new(0, perp/2, dif_para/2)

--			if not p0 then
--				p0 = Instance.new('Part')
--				p0.FormFactor = 'Custom'
--				p0.TopSurface = 0
--				p0.BottomSurface = 0
--				p0.Anchored = true
--				p0.CanCollide = false
--				p0.Material = 'Glass'
--				p0.Size = Vector3.new(sz, sz, sz)
--				local mesh = Instance.new('SpecialMesh', p0)
--				mesh.MeshType = 2
--				mesh.Name = 'WedgeMesh'
--			end
--			p0.WedgeMesh.Scale = Vector3.new(0, perp/sz, para/sz)
--			p0.CFrame = cf0

--			if not p1 then
--				p1 = p0:clone()
--			end
--			p1.WedgeMesh.Scale = Vector3.new(0, perp/sz, dif_para/sz)
--			p1.CFrame = cf1

--			return p0, p1
--		end

--		function DrawQuad(v1, v2, v3, v4, parts)
--			parts[1], parts[2] = DrawTriangle(v1, v2, v3, parts[1], parts[2])
--			parts[3], parts[4] = DrawTriangle(v3, v2, v4, parts[3], parts[4])
--		end
--	end

--	function module:BindFrame(frame, properties)
--		if binds[frame] then
--			return binds[frame].parts
--		end

--		local uid = GenUid()
--		local parts = {}
--		local f = Instance.new('Folder', getRoot())
--		f.Name = frame.Name

--		local parents = {}
--		do
--			local function add(child)
--				if child:IsA'GuiObject' then
--					parents[#parents + 1] = child
--					add(child.Parent)
--				end
--			end
--			add(frame)
--		end

--		local function UpdateOrientation(fetchProps)
--			local zIndex = 1 - 0.05*frame.ZIndex
--			local tl, br = frame.AbsolutePosition, frame.AbsolutePosition + frame.AbsoluteSize
--			local tr, bl = Vector2.new(br.x, tl.y), Vector2.new(tl.x, br.y)
--			do
--				local rot = 0
--				for _, v in ipairs(parents) do
--					rot = rot + v.Rotation
--				end
--				if rot ~= 0 and rot%180 ~= 0 then
--					local mid = tl:lerp(br, 0.5)
--					local s, c = math.sin(math.rad(rot)), math.cos(math.rad(rot))
--					local vec = tl
--					tl = Vector2.new(c*(tl.x - mid.x) - s*(tl.y - mid.y), s*(tl.x - mid.x) + c*(tl.y - mid.y)) + mid
--					tr = Vector2.new(c*(tr.x - mid.x) - s*(tr.y - mid.y), s*(tr.x - mid.x) + c*(tr.y - mid.y)) + mid
--					bl = Vector2.new(c*(bl.x - mid.x) - s*(bl.y - mid.y), s*(bl.x - mid.x) + c*(bl.y - mid.y)) + mid
--					br = Vector2.new(c*(br.x - mid.x) - s*(br.y - mid.y), s*(br.x - mid.x) + c*(br.y - mid.y)) + mid
--				end
--			end
--			DrawQuad(
--				camera:ScreenPointToRay(tl.x, tl.y, zIndex).Origin, 
--				camera:ScreenPointToRay(tr.x, tr.y, zIndex).Origin, 
--				camera:ScreenPointToRay(bl.x, bl.y, zIndex).Origin, 
--				camera:ScreenPointToRay(br.x, br.y, zIndex).Origin, 
--				parts
--			)
--			if fetchProps then
--				for _, pt in pairs(parts) do
--					pt.Parent = f
--				end
--				for propName, propValue in pairs(properties) do
--					for _, pt in pairs(parts) do
--						pt[propName] = propValue
--					end
--				end
--			end
--		end

--		UpdateOrientation(true)
--		runService:BindToRenderStep(uid, 2000, UpdateOrientation)

--		binds[frame] = {
--			uid = uid,
--			parts = parts
--		}
--		return binds[frame].parts
--	end

--	function module:Modify(frame, properties)
--		local parts = module:GetBoundParts(frame)
--		if parts then
--			for propName, propValue in pairs(properties) do
--				for _, pt in pairs(parts) do
--					pt[propName] = propValue
--				end
--			end
--		end
--	end

--	function module:UnbindFrame(frame)
--		if RootParent == nil then return end
--		local cb = binds[frame]
--		if cb then
--			runService:UnbindFromRenderStep(cb.uid)
--			for _, v in pairs(cb.parts) do
--				v:Destroy()
--			end
--			binds[frame] = nil
--		end
--		if getRoot():FindFirstChild(frame.Name) then
--			getRoot()[frame.Name]:Destroy()
--		end
--	end

--	function module:HasBinding(frame)
--		return binds[frame] ~= nil
--	end

--	function module:GetBoundParts(frame)
--		return binds[frame] and binds[frame].parts
--	end


--	return module

--end)()

-- Sirius Functions
local function checkSirius() return UI.Parent end
local function getPing()
	local success, value = pcall(function()
		return statsService.Network.ServerStatsItem["Data Ping"]:GetValue()
	end)

	return math.clamp((success and tonumber(value)) or 60, 10, 700)
end
local function checkFolder()
	if typeof(isfolder) ~= "function" or typeof(makefolder) ~= "function" then return end

	pcall(function()
		local root = siriusValues.siriusFolder
		local assets = root.."/Assets"
		local icons = assets.."/Icons"
		local music = root.."/Music"

		if not isfolder(root) then makefolder(root) end
		if not isfolder(assets) then makefolder(assets) end
		if not isfolder(icons) then makefolder(icons) end
		if not isfolder(music) then
			makefolder(music)
			if typeof(writefile) == "function" then
				writefile(music.."/readme.txt", "Hey there! Place your MP3 or other audio files in this folder, and have the ability to play them through the Sirius Music UI!")
			end
		end
	end)
end
local function isPanel(name) return not table.find({"Home", "Music", "Settings"}, name) end

local function fetchFromCDN(path, write, savePath)
	pcall(function()
		checkFolder()

		local file = game:HttpGet(siriusValues.cdn..path) or nil
		if not file then return end
		if not write then return file end
		if typeof(writefile) ~= "function" then return end


		writefile(siriusValues.siriusFolder.."/"..savePath, file)

		return
	end)
end

local function fetchIcon(iconName)
	pcall(function()
		checkFolder()

		local pathCDN = siriusValues.icons..iconName..".png"
		local path = siriusValues.siriusFolder.."/Assets/"..iconName..".png"

		if not isfile(path) then
			local file = game:HttpGet(pathCDN)
			if not file then return end

			writefile(path, file)
		end

		local imageToReturn = getcustomasset(path)

		return imageToReturn
	end)
end

local function storeOriginalText(element)
	if originalTextValues[element] == nil then
		originalTextValues[element] = element.Text
	end
end

local function undoAnonymousChanges()
	for element, originalText in pairs(originalTextValues) do
		element.Text = originalText
	end
end

local function isHighlightEnabledFor(playerName)
	return siriusValues.actions[6].enabled or locatedPlayers[playerName] == true
end

local function createEsp(player)
	if player == localPlayer or not checkSirius() then
		return
	end

	local highlight = Instance.new("Highlight")
	highlight.FillTransparency = 1
	highlight.OutlineTransparency = 0
	highlight.OutlineColor = Color3.new(1,1,1)
	highlight.Adornee = player.Character
	highlight.Name = player.Name
	highlight.Enabled = isHighlightEnabledFor(player.Name)
	highlight.Parent = espContainer

	if espConnections[player] then
		espConnections[player]:Disconnect()
	end
	espConnections[player] = player.CharacterAdded:Connect(function(character)
		if not checkSirius() then return end
		task.wait()
		highlight.Adornee = character
	end)
end

local function makeDraggable(object)
	local dragging = false
	local relative = nil
	local activeInput = nil
	local pointerPosition = nil

	local offset = Vector2.zero
	local screenGui = object:FindFirstAncestorWhichIsA("ScreenGui")
	if screenGui and screenGui.IgnoreGuiInset then
		offset += guiService:GetGuiInset()
	end

	local function getPointerPosition(input)
		if input and input.UserInputType == Enum.UserInputType.Touch then
			return Vector2.new(input.Position.X, input.Position.Y)
		end

		return userInputService:GetMouseLocation()
	end

	object.InputBegan:Connect(function(input, processed)
		if processed then return end

		local inputType = input.UserInputType.Name
		if inputType == "MouseButton1" or inputType == "Touch" then
			activeInput = input
			pointerPosition = getPointerPosition(input)
			relative = object.AbsolutePosition + object.AbsoluteSize * object.AnchorPoint - pointerPosition
			dragging = true
		end
	end)

	local inputChanged = userInputService.InputChanged:Connect(function(input)
		if not dragging then return end
		if activeInput and activeInput.UserInputType == Enum.UserInputType.Touch and input ~= activeInput then return end
		if input.UserInputType ~= Enum.UserInputType.MouseMovement and input.UserInputType ~= Enum.UserInputType.Touch then return end

		pointerPosition = getPointerPosition(input)
	end)

	local inputEnded = userInputService.InputEnded:Connect(function(input)
		if not dragging then return end

		local inputType = input.UserInputType.Name
		if inputType == "MouseButton1" or inputType == "Touch" then
			dragging = false
			activeInput = nil
			pointerPosition = nil
		end
	end)

	local renderStepped = runService.RenderStepped:Connect(function()
		if dragging then
			local position = (pointerPosition or getPointerPosition(activeInput)) + relative + offset
			object.Position = UDim2.fromOffset(position.X, position.Y)
		end
	end)

	object.Destroying:Connect(function()
		inputChanged:Disconnect()
		inputEnded:Disconnect()
		renderStepped:Disconnect()
	end)
end

local function checkAction(target)
	local toReturn = {}

	for _, action in ipairs(siriusValues.actions) do
		if action.name == target then
			toReturn.action = action
			break
		end
	end

	for _, action in ipairs(characterPanel.Interactions.Grid:GetChildren()) do
		if action.name == target then
			toReturn.object = action
			break
		end
	end

	return toReturn
end

siriusValues.removedSettingDefaults = {
	["Spatial Shield"] = {current = true},
	["Spatial Shield Threshold"] = {current = 300},
	["Moderator Detection"] = {current = true},
	["Moderator / Admin Join Detection"] = {current = true},
	["Known User Watchlist"] = {current = false},
	["Watchlist Users"] = {current = ""},
	["Watchlist Protection Mode"] = {current = true},
	["Chat Keyword Detection"] = {current = false},
	["Chat Keywords"] = {current = "admin, mod, staff, owner, developer, report, ban, kick, exploit, hacker"},
	["Chat Keyword Protection Mode"] = {current = false},
	["Detection Alert Cooldown"] = {current = 10},
	["Block Suspicious Links"] = {current = true},
	["Repeated Message Filter"] = {current = true},
	["Custom Blocked Chat Keywords"] = {current = ""},
	["Chat Safety Alert Cooldown"] = {current = 3},
	["Intelligent HTTP Interception"] = {current = true},
	["Intelligent Clipboard Interception"] = {current = true},
}

local function checkSetting(settingTarget, categoryTarget)
	for _, category in ipairs(siriusSettings) do
		if categoryTarget then
			if category.name == categoryTarget then
				for _, setting in ipairs(category.categorySettings) do
					if setting.name == settingTarget then
						return setting
					end
				end
				return
			end
		else
			for _, setting in ipairs(category.categorySettings) do
				if setting.name == settingTarget then
					return setting
				end
			end
		end
	end

	return siriusValues.removedSettingDefaults and siriusValues.removedSettingDefaults[settingTarget]
end

function getSettingKeyCode(settingName, fallbackKeyCode)
	local setting = checkSetting(settingName, keybindCategoryName) or checkSetting(settingName, "Keybinds") or checkSetting(settingName)
	local keyCode = nil

	if setting and type(setting.current) == "string" and setting.current ~= "" then
		local keyName = string.match(setting.current, "([^+]+)$") or setting.current
		keyCode = Enum.KeyCode[keyName]
	end

	if keyCode and keyCode ~= Enum.KeyCode.Unknown then
		return keyCode
	end

	return fallbackKeyCode
end

function getFlyVerticalControlKeys()
	return getSettingKeyCode("Fly Up Key", Enum.KeyCode.Space), getSettingKeyCode("Fly Down Key", Enum.KeyCode.LeftShift)
end

local function wipeTransparency(ins, target, checkSelf, tween, duration)
	local transparencyProperties = siriusValues.transparencyProperties

	local function applyTransparency(obj)
		local properties = transparencyProperties[obj.className]

		if properties then
			local tweenProperties = {}

			for _, property in ipairs(properties) do
				tweenProperties[property] = target
			end

			for property, transparency in pairs(tweenProperties) do
				if tween then
					tweenService:Create(obj, TweenInfo.new(duration, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {[property] = transparency}):Play()
				else
					obj[property] = transparency
				end

			end
		end
	end

	if checkSelf then
		applyTransparency(ins)
	end

	for _, descendant in ipairs(ins:getDescendants()) do
		applyTransparency(descendant)
	end
end

local scrollbarFadeRegistry = {}

local function attachScrollbarFade(scrollingFrame, visibleTransparency, idleDelay)
	if not scrollingFrame or not scrollingFrame:IsA("ScrollingFrame") or scrollbarFadeRegistry[scrollingFrame] then return end

	scrollbarFadeRegistry[scrollingFrame] = true
	visibleTransparency = visibleTransparency or 0.55
	idleDelay = idleDelay or 0.65

	local fadeId = 0
	scrollingFrame.ScrollBarImageTransparency = 1

	local function hasScrollableContent()
		local canvasHeight = scrollingFrame.AbsoluteCanvasSize.Y
		local visibleHeight = scrollingFrame.AbsoluteWindowSize.Y

		return canvasHeight > visibleHeight + 6
	end

	local function fadeOutScrollbar()
		tweenService:Create(scrollingFrame, TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {ScrollBarImageTransparency = 1}):Play()
	end

	scrollingFrame:GetPropertyChangedSignal("CanvasPosition"):Connect(function()
		fadeId += 1
		local currentFade = fadeId

		if not hasScrollableContent() then
			fadeOutScrollbar()
			return
		end

		tweenService:Create(scrollingFrame, TweenInfo.new(0.12, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {ScrollBarImageTransparency = visibleTransparency}):Play()

		task.delay(idleDelay, function()
			if fadeId == currentFade then
				fadeOutScrollbar()
			end
		end)
	end)

	scrollingFrame:GetPropertyChangedSignal("CanvasSize"):Connect(function()
		if not hasScrollableContent() then
			fadeOutScrollbar()
		end
	end)
end

local settingsCategoryScrollBound = false
local settingsCategoryScrollRefreshId = 0

local function applySettingsCategoryScrollPolish(scroller)
	if not scroller then return end

	scroller.BorderSizePixel = 0
	scroller.ClipsDescendants = true
	scroller.Active = true
	scroller.ScrollingDirection = Enum.ScrollingDirection.Y
	scroller.ScrollingEnabled = true
	scroller.AutomaticCanvasSize = Enum.AutomaticSize.None
	scroller.ElasticBehavior = Enum.ElasticBehavior.WhenScrollable
	scroller.ScrollBarThickness = 0
	scroller.ScrollBarImageColor3 = Color3.fromRGB(225, 225, 225)
	scroller.ScrollBarImageTransparency = 1

	local padding = scroller:FindFirstChild("SettingsCategoryScrollPadding") or scroller:FindFirstChildOfClass("UIPadding")
	if not padding then
		padding = Instance.new("UIPadding")
		padding.Parent = scroller
	end

	padding.Name = "SettingsCategoryScrollPadding"
	padding.PaddingTop = UDim.new(0, 10)
	padding.PaddingBottom = UDim.new(0, 68)
end

local function ensureSettingsCategoryScroller()
	local current = settingsPanel:FindFirstChild("SettingTypes")
	if not current then return end

	if current:IsA("ScrollingFrame") then
		applySettingsCategoryScrollPolish(current)
		return current
	end

	local source = current
	local sourceVisible = source.Visible
	source.Name = "SettingTypesSource"
	source.Visible = false

	local scroller = Instance.new("ScrollingFrame")
	scroller.Name = "SettingTypes"
	scroller.Parent = settingsPanel
	scroller.AnchorPoint = source.AnchorPoint
	scroller.Position = source.Position
	scroller.Size = source.Size
	scroller.Visible = sourceVisible
	scroller.BackgroundTransparency = source.BackgroundTransparency
	scroller.BackgroundColor3 = source.BackgroundColor3
	scroller.ZIndex = source.ZIndex
	scroller.CanvasSize = UDim2.new(0, 0, 0, 0)

	for _, child in ipairs(source:GetChildren()) do
		child.Parent = scroller
	end

	applySettingsCategoryScrollPolish(scroller)
	return scroller
end

local function refreshSettingsCategoryScroll()
	local settingTypes = ensureSettingsCategoryScroller()
	if not settingTypes then return end

	settingsCategoryScrollRefreshId += 1
	local refreshId = settingsCategoryScrollRefreshId

	task.defer(function()
		if refreshId ~= settingsCategoryScrollRefreshId then return end
		if not settingTypes.Parent then return end

		local layout = settingTypes:FindFirstChildOfClass("UIGridLayout") or settingTypes:FindFirstChildOfClass("UIListLayout")
		if not layout then return end

		local padding = settingTypes:FindFirstChild("SettingsCategoryScrollPadding") or settingTypes:FindFirstChildOfClass("UIPadding")
		local paddingTop = padding and padding.PaddingTop.Offset or 0
		local paddingBottom = padding and padding.PaddingBottom.Offset or 0
		local contentHeight = math.ceil(layout.AbsoluteContentSize.Y + paddingTop + paddingBottom + 28)
		local windowHeight = math.max(settingTypes.AbsoluteWindowSize.Y, 1)
		settingTypes.CanvasSize = UDim2.new(0, 0, 0, math.max(contentHeight, windowHeight + 1))
		settingTypes.ScrollBarImageTransparency = 1
	end)
end

local function bindSettingsCategoryScroll()
	local settingTypes = ensureSettingsCategoryScroller()
	if not settingTypes or settingsCategoryScrollBound then return end

	settingsCategoryScrollBound = true
	local layout = settingTypes:FindFirstChildOfClass("UIGridLayout") or settingTypes:FindFirstChildOfClass("UIListLayout")

	if layout then
		layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(refreshSettingsCategoryScroll)
	end

	settingTypes.ChildAdded:Connect(function()
		task.defer(refreshSettingsCategoryScroll)
	end)
	settingTypes.ChildRemoved:Connect(function()
		task.defer(refreshSettingsCategoryScroll)
	end)
	settingTypes:GetPropertyChangedSignal("AbsoluteWindowSize"):Connect(refreshSettingsCategoryScroll)
end

local function blurSignature(value)
	if not value then
		if lighting:FindFirstChild("SiriusBlur") then
			lighting:FindFirstChild("SiriusBlur"):Destroy()
		end
	else
		if not lighting:FindFirstChild("SiriusBlur") then
			local blurLight = Instance.new("DepthOfFieldEffect", lighting)
			blurLight.Name = "SiriusBlur"
			blurLight.Enabled = true
			blurLight.FarIntensity = 0
			blurLight.FocusDistance = 51.6
			blurLight.InFocusRadius = 50
			blurLight.NearIntensity = 0.8
		end
	end
end

local function figureNotifications()
	if checkSirius() then
		local notificationsSize = 0

		if #notifications > 0 then
			blurSignature(true)
		else
			blurSignature(false)
		end

		for i = #notifications, 1, -1 do
			local notification = notifications[i]
			if notification then
				if notificationsSize == 0 then
					notificationsSize = notification.Size.Y.Offset + 2
				else
					notificationsSize += notification.Size.Y.Offset + 5
				end
				local desiredPosition = UDim2.new(0.5, 0, 0, notificationsSize)
				if notification.Position ~= desiredPosition then
					notification:TweenPosition(desiredPosition, "Out", "Quint", 0.8, true)
				end
			end
		end	
	end
end

local contentProvider = game:GetService("ContentProvider")

local function queueNotification(Title, Description, Image)
	task.spawn(function()		
		if checkSirius() then
			local newNotification = notificationContainer.Template:Clone()
			newNotification.Parent = notificationContainer
			newNotification.Name = Title or "Unknown Title"
			newNotification.Visible = true

			newNotification.Title.Text = Title or "Unknown Title"
			newNotification.Description.Text = Description or "Unknown Description"
			newNotification.Time.Text = "now"

			-- Prepare for animation
			newNotification.AnchorPoint = Vector2.new(0.5, 1)
			newNotification.Position = UDim2.new(0.5, 0, -1, 0)
			newNotification.Size = UDim2.new(0, 320, 0, 500)
			newNotification.Description.Size = UDim2.new(0, 241, 0, 400)
			wipeTransparency(newNotification, 1, true)

			newNotification.Description.Size = UDim2.new(0, 241, 0, newNotification.Description.TextBounds.Y)
			newNotification.Size = UDim2.new(0, 100, 0, newNotification.Description.TextBounds.Y + 50)

			table.insert(notifications, newNotification)
			figureNotifications()

			local notificationSound = Instance.new("Sound")
			notificationSound.Parent = UI
			notificationSound.SoundId = "rbxassetid://255881176"
			notificationSound.Name = "notificationSound"
			notificationSound.Volume = 0.65
			notificationSound.PlayOnRemove = true
			notificationSound:Destroy()


			if not tonumber(Image) then
				newNotification.Icon.Image = 'rbxassetid://14317577326'
			else
				newNotification.Icon.Image = 'rbxassetid://'..Image or 0
			end

			newNotification:TweenPosition(UDim2.new(0.5, 0, 0, newNotification.Size.Y.Offset + 2), "Out", "Quint", 0.9, true)
			task.wait(0.1)
			tweenService:Create(newNotification, TweenInfo.new(0.8, Enum.EasingStyle.Exponential), {Size = UDim2.new(0, 320, 0, newNotification.Description.TextBounds.Y + 50)}):Play()
			task.wait(0.05)
			tweenService:Create(newNotification, TweenInfo.new(0.8, Enum.EasingStyle.Exponential), {BackgroundTransparency = 0.35}):Play()
			tweenService:Create(newNotification.UIStroke, TweenInfo.new(0.6, Enum.EasingStyle.Exponential), {Transparency = 0.7}):Play()
			task.wait(0.05)
			tweenService:Create(newNotification.Icon, TweenInfo.new(0.5, Enum.EasingStyle.Exponential), {ImageTransparency = 0}):Play()
			task.wait(0.04)
			tweenService:Create(newNotification.Title, TweenInfo.new(0.5, Enum.EasingStyle.Exponential), {TextTransparency = 0}):Play()
			task.wait(0.04)
			tweenService:Create(newNotification.Description, TweenInfo.new(0.5, Enum.EasingStyle.Exponential), {TextTransparency = 0.15}):Play()
			tweenService:Create(newNotification.Time, TweenInfo.new(0.5, Enum.EasingStyle.Exponential), {TextTransparency = 0.5}):Play()

			--neonModule:BindFrame(newNotification.BlurModule, {
			--	Transparency = 0.98,
			--	BrickColor = BrickColor.new("Institutional white")
			--})

			newNotification.Interact.MouseButton1Click:Connect(function()
				local foundNotification = table.find(notifications, newNotification)
				if foundNotification then table.remove(notifications, foundNotification) end

				tweenService:Create(newNotification, TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.In), {Position = UDim2.new(1.5, 0, 0, newNotification.Position.Y.Offset)}):Play()

				task.wait(0.4)
				newNotification:Destroy()
				figureNotifications()
				return
			end)

			local waitTime = (#newNotification.Description.Text*0.1)+2
			if waitTime <= 1 then waitTime = 2.5 elseif waitTime > 10 then waitTime = 10 end

			task.wait(waitTime)

			local foundNotification = table.find(notifications, newNotification)
			if foundNotification then table.remove(notifications, foundNotification) end

			tweenService:Create(newNotification, TweenInfo.new(0.8, Enum.EasingStyle.Quint, Enum.EasingDirection.In), {Position = UDim2.new(1.5, 0, 0, newNotification.Position.Y.Offset)}):Play()

			task.wait(1.2)
			--neonModule:UnbindFrame(newNotification.BlurModule)
			newNotification:Destroy()
			figureNotifications()
		end
	end)
end

function siriusValues.ensureBetaControlOverlay()
	if siriusValues.betaControlOverlayParts and siriusValues.betaControlOverlayParts.Root and siriusValues.betaControlOverlayParts.Root.Parent then
		return siriusValues.betaControlOverlayParts
	end

	local root = Instance.new("TextButton")
	root.Name = "SiriusBetaControlOverlay"
	root.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	root.BackgroundTransparency = 1
	root.BorderSizePixel = 0
	root.AutoButtonColor = false
	root.Text = ""
	root.Active = true
	root.Visible = false
	root.Size = UDim2.fromScale(1, 1)
	root.ZIndex = 250000
	pcall(function()
		root.Modal = true
	end)
	root.Parent = UI

	local backdropGradient = Instance.new("UIGradient")
	backdropGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(6, 6, 10)),
		ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 0, 2)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(8, 8, 14)),
	})
	backdropGradient.Rotation = 110
	backdropGradient.Parent = root

	local vignette = Instance.new("ImageLabel")
	vignette.Name = "Vignette"
	vignette.AnchorPoint = Vector2.new(0.5, 0.5)
	vignette.Position = UDim2.new(0.5, 0, 0.5, 0)
	vignette.Size = UDim2.new(1.2, 0, 1.2, 0)
	vignette.BackgroundTransparency = 1
	vignette.Image = "rbxassetid://8992230677"
	vignette.ImageColor3 = Color3.fromRGB(0, 0, 0)
	vignette.ImageTransparency = 1
	vignette.ScaleType = Enum.ScaleType.Slice
	vignette.SliceCenter = Rect.new(99, 99, 99, 99)
	vignette.ZIndex = root.ZIndex
	vignette.Parent = root

	local shadow = Instance.new("ImageLabel")
	shadow.Name = "Shadow"
	shadow.AnchorPoint = Vector2.new(0.5, 0.5)
	shadow.Position = UDim2.new(0.5, 0, 0.5, 18)
	shadow.Size = UDim2.new(0.9, 70, 0, 360)
	shadow.BackgroundTransparency = 1
	shadow.Image = "rbxassetid://8992230677"
	shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
	shadow.ImageTransparency = 1
	shadow.ScaleType = Enum.ScaleType.Slice
	shadow.SliceCenter = Rect.new(99, 99, 99, 99)
	shadow.ZIndex = root.ZIndex + 1
	shadow.Parent = root

	local card = Instance.new("CanvasGroup")
	card.Name = "Card"
	card.AnchorPoint = Vector2.new(0.5, 0.5)
	card.Position = UDim2.new(0.5, 0, 0.5, 0)
	card.Size = UDim2.new(0.86, 0, 0, 268)
	card.BackgroundColor3 = Color3.fromRGB(16, 17, 22)
	card.BackgroundTransparency = 0.02
	card.BorderSizePixel = 0
	card.GroupTransparency = 1
	card.ZIndex = root.ZIndex + 2
	card.Parent = root

	local cardGradient = Instance.new("UIGradient")
	cardGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(26, 27, 33)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(14, 14, 18)),
	})
	cardGradient.Rotation = 115
	cardGradient.Parent = card

	local sizeConstraint = Instance.new("UISizeConstraint")
	sizeConstraint.MinSize = Vector2.new(310, 232)
	sizeConstraint.MaxSize = Vector2.new(540, 312)
	sizeConstraint.Parent = card

	local scale = Instance.new("UIScale")
	scale.Scale = 0.965
	scale.Parent = card

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 22)
	corner.Parent = card

	local stroke = Instance.new("UIStroke")
	stroke.Color = Color3.fromRGB(108, 110, 122)
	stroke.Thickness = 1
	stroke.Transparency = 0.6
	stroke.Parent = card

	local sheen = Instance.new("Frame")
	sheen.Name = "Sheen"
	sheen.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	sheen.BackgroundTransparency = 0.92
	sheen.BorderSizePixel = 0
	sheen.Position = UDim2.new(0, 0, 0, 0)
	sheen.Size = UDim2.new(1, 0, 0, 1)
	sheen.ZIndex = card.ZIndex + 1
	sheen.Parent = card

	local accent = Instance.new("Frame")
	accent.Name = "Accent"
	accent.BackgroundColor3 = Color3.fromRGB(239, 68, 68)
	accent.BackgroundTransparency = 0.08
	accent.BorderSizePixel = 0
	accent.Position = UDim2.new(0, 26, 0, 28)
	accent.Size = UDim2.new(0, 28, 0, 3)
	accent.ZIndex = card.ZIndex + 1
	accent.Parent = card

	local accentCorner = Instance.new("UICorner")
	accentCorner.CornerRadius = UDim.new(1, 0)
	accentCorner.Parent = accent

	local accentGradient = Instance.new("UIGradient")
	accentGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(239, 68, 68)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(251, 113, 133)),
	})
	accentGradient.Parent = accent

	local eyebrow = Instance.new("TextLabel")
	eyebrow.Name = "Eyebrow"
	eyebrow.BackgroundTransparency = 1
	eyebrow.Position = UDim2.new(0, 26, 0, 38)
	eyebrow.Size = UDim2.new(1, -52, 0, 18)
	eyebrow.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	eyebrow.Text = "SIRIUS X"
	eyebrow.TextColor3 = Color3.fromRGB(158, 160, 174)
	eyebrow.TextSize = 11
	eyebrow.TextXAlignment = Enum.TextXAlignment.Left
	eyebrow.TextTransparency = 0
	eyebrow.ZIndex = card.ZIndex + 1
	eyebrow.Parent = card

	local title = Instance.new("TextLabel")
	title.Name = "Title"
	title.BackgroundTransparency = 1
	title.Position = UDim2.new(0, 26, 0, 60)
	title.Size = UDim2.new(1, -52, 0, 34)
	title.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	title.Text = "SIRIUS X Disabled"
	title.TextColor3 = Color3.fromRGB(248, 248, 252)
	title.TextSize = 24
	title.TextXAlignment = Enum.TextXAlignment.Left
	title.TextTruncate = Enum.TextTruncate.AtEnd
	title.ZIndex = card.ZIndex + 1
	title.Parent = card

	local message = Instance.new("TextLabel")
	message.Name = "Message"
	message.BackgroundTransparency = 1
	message.Position = UDim2.new(0, 26, 0, 102)
	message.Size = UDim2.new(1, -52, 0, 108)
	message.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Regular)
	message.Text = "This beta build is temporarily disabled."
	message.TextColor3 = Color3.fromRGB(208, 210, 220)
	message.TextSize = 15
	message.LineHeight = 1.35
	message.TextWrapped = true
	message.TextXAlignment = Enum.TextXAlignment.Left
	message.TextYAlignment = Enum.TextYAlignment.Top
	message.ZIndex = card.ZIndex + 1
	message.Parent = card

	local footerLine = Instance.new("Frame")
	footerLine.Name = "FooterLine"
	footerLine.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	footerLine.BackgroundTransparency = 0.93
	footerLine.BorderSizePixel = 0
	footerLine.Position = UDim2.new(0, 26, 1, -54)
	footerLine.Size = UDim2.new(1, -52, 0, 1)
	footerLine.ZIndex = card.ZIndex + 1
	footerLine.Parent = card

	local footerDot = Instance.new("Frame")
	footerDot.Name = "FooterDot"
	footerDot.BackgroundColor3 = Color3.fromRGB(239, 68, 68)
	footerDot.BackgroundTransparency = 0.15
	footerDot.BorderSizePixel = 0
	footerDot.AnchorPoint = Vector2.new(0, 0.5)
	footerDot.Position = UDim2.new(0, 26, 1, -27)
	footerDot.Size = UDim2.new(0, 6, 0, 6)
	footerDot.ZIndex = card.ZIndex + 1
	footerDot.Parent = card
	local footerDotCorner = Instance.new("UICorner")
	footerDotCorner.CornerRadius = UDim.new(1, 0)
	footerDotCorner.Parent = footerDot

	local footer = Instance.new("TextLabel")
	footer.Name = "Footer"
	footer.BackgroundTransparency = 1
	footer.Position = UDim2.new(0, 40, 1, -40)
	footer.Size = UDim2.new(1, -66, 0, 24)
	footer.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium)
	footer.Text = "Waiting for the owner to re-enable this session."
	footer.TextColor3 = Color3.fromRGB(152, 154, 168)
	footer.TextSize = 12.5
	footer.TextXAlignment = Enum.TextXAlignment.Left
	footer.TextTransparency = 0
	footer.ZIndex = card.ZIndex + 1
	footer.Parent = card

	local buttonRow = Instance.new("Frame")
	buttonRow.Name = "ButtonRow"
	buttonRow.BackgroundTransparency = 1
	buttonRow.Position = UDim2.new(0, 28, 1, -56)
	buttonRow.Size = UDim2.new(1, -56, 0, 38)
	buttonRow.Visible = false
	buttonRow.ZIndex = card.ZIndex + 2
	buttonRow.Parent = card

	local rowLayout = Instance.new("UIListLayout")
	rowLayout.FillDirection = Enum.FillDirection.Horizontal
	rowLayout.HorizontalAlignment = Enum.HorizontalAlignment.Right
	rowLayout.VerticalAlignment = Enum.VerticalAlignment.Center
	rowLayout.Padding = UDim.new(0, 10)
	rowLayout.SortOrder = Enum.SortOrder.LayoutOrder
	rowLayout.Parent = buttonRow

	local decline = Instance.new("TextButton")
	decline.Name = "Decline"
	decline.Size = UDim2.new(0, 120, 1, 0)
	decline.BackgroundColor3 = Color3.fromRGB(27, 27, 27)
	decline.BorderSizePixel = 0
	decline.AutoButtonColor = false
	decline.Text = "Decline"
	decline.TextColor3 = Color3.fromRGB(232, 232, 232)
	decline.TextSize = 15
	decline.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	decline.LayoutOrder = 1
	decline.ZIndex = buttonRow.ZIndex + 1
	decline.Parent = buttonRow

	local declineCorner = Instance.new("UICorner")
	declineCorner.CornerRadius = UDim.new(0, 12)
	declineCorner.Parent = decline

	local declineStroke = Instance.new("UIStroke")
	declineStroke.Color = Color3.fromRGB(84, 84, 84)
	declineStroke.Transparency = 0.35
	declineStroke.Thickness = 1
	declineStroke.Parent = decline

	local accept = Instance.new("TextButton")
	accept.Name = "Accept"
	accept.Size = UDim2.new(0, 120, 1, 0)
	accept.BackgroundColor3 = Color3.fromRGB(178, 62, 62)
	accept.BorderSizePixel = 0
	accept.AutoButtonColor = false
	accept.Text = "Accept"
	accept.TextColor3 = Color3.fromRGB(255, 255, 255)
	accept.TextSize = 15
	accept.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	accept.LayoutOrder = 2
	accept.ZIndex = buttonRow.ZIndex + 1
	accept.Parent = buttonRow

	local acceptCorner = Instance.new("UICorner")
	acceptCorner.CornerRadius = UDim.new(0, 12)
	acceptCorner.Parent = accept

	local always = Instance.new("TextButton")
	always.Name = "Always"
	always.Size = UDim2.new(0, 168, 1, 0)
	always.BackgroundColor3 = Color3.fromRGB(236, 236, 236)
	always.BorderSizePixel = 0
	always.AutoButtonColor = false
	always.Text = "Always allow"
	always.TextColor3 = Color3.fromRGB(10, 10, 10)
	always.TextSize = 15
	always.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	always.LayoutOrder = 3
	always.Visible = false
	always.ZIndex = buttonRow.ZIndex + 1
	always.Parent = buttonRow

	local alwaysCorner = Instance.new("UICorner")
	alwaysCorner.CornerRadius = UDim.new(0, 12)
	alwaysCorner.Parent = always

	siriusValues.betaControlOverlayParts = {
		Root = root,
		Vignette = vignette,
		Shadow = shadow,
		Card = card,
		Scale = scale,
		Stroke = stroke,
		Title = title,
		Message = message,
		Eyebrow = eyebrow,
		FooterLine = footerLine,
		FooterDot = footerDot,
		Footer = footer,
		ButtonRow = buttonRow,
		Accept = accept,
		Decline = decline,
		Always = always
	}

	return siriusValues.betaControlOverlayParts
end

function siriusValues.showBetaControlOverlay(title, message, persistent)
	local parts = siriusValues.ensureBetaControlOverlay()
	siriusValues.betaControlOverlayToken = (siriusValues.betaControlOverlayToken or 0) + 1
	local token = siriusValues.betaControlOverlayToken

	UI.Enabled = true
	parts.Title.Text = tostring(title or "SIRIUS X Disabled")
	parts.Message.Text = tostring(message or "This beta build is temporarily disabled.")
	parts.Footer.Text = persistent and "Waiting for the owner to re-enable this session." or "This control was sent from the SIRIUS X website."
	if parts.ButtonRow then parts.ButtonRow.Visible = false end
	if parts.Always then parts.Always.Visible = false end
	if parts.Footer then parts.Footer.Visible = true end
	if parts.FooterLine then parts.FooterLine.Visible = true end
	parts.Root.Visible = true
	parts.Root.BackgroundTransparency = 1
	if parts.Vignette then parts.Vignette.ImageTransparency = 1 end
	parts.Shadow.ImageTransparency = 1
	parts.Card.GroupTransparency = 1
	parts.Scale.Scale = 0.94

	local fadeIn = TweenInfo.new(0.44, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
	local scaleIn = TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
	tweenService:Create(parts.Root, fadeIn, {BackgroundTransparency = 0.4}):Play()
	if parts.Vignette then tweenService:Create(parts.Vignette, fadeIn, {ImageTransparency = 0.45}):Play() end
	tweenService:Create(parts.Shadow, fadeIn, {ImageTransparency = 0.45}):Play()
	tweenService:Create(parts.Card, fadeIn, {GroupTransparency = 0}):Play()
	tweenService:Create(parts.Scale, scaleIn, {Scale = 1}):Play()

	if not persistent then
		task.delay(4.2, function()
			if siriusValues.betaControlOverlayToken ~= token then return end
			local currentParts = siriusValues.betaControlOverlayParts
			if not currentParts then return end

			local fadeOut = TweenInfo.new(0.32, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut)
			tweenService:Create(currentParts.Root, fadeOut, {BackgroundTransparency = 1}):Play()
			if currentParts.Vignette then tweenService:Create(currentParts.Vignette, fadeOut, {ImageTransparency = 1}):Play() end
			tweenService:Create(currentParts.Shadow, fadeOut, {ImageTransparency = 1}):Play()
			tweenService:Create(currentParts.Card, fadeOut, {GroupTransparency = 1}):Play()
			tweenService:Create(currentParts.Scale, fadeOut, {Scale = 0.94}):Play()

			task.delay(0.34, function()
				if siriusValues.betaControlOverlayToken == token and currentParts.Root then
					currentParts.Root.Visible = false
				end
			end)
		end)
	end
end

function siriusValues.showBetaControlChoiceOverlay(title, message, acceptText, declineText, onAccept, onDecline, alwaysText, onAlways)
	local parts = siriusValues.ensureBetaControlOverlay()
	siriusValues.betaControlOverlayToken = (siriusValues.betaControlOverlayToken or 0) + 1

	if siriusValues.betaControlAcceptConn then
		pcall(function() siriusValues.betaControlAcceptConn:Disconnect() end)
		siriusValues.betaControlAcceptConn = nil
	end
	if siriusValues.betaControlDeclineConn then
		pcall(function() siriusValues.betaControlDeclineConn:Disconnect() end)
		siriusValues.betaControlDeclineConn = nil
	end
	if siriusValues.betaControlAlwaysConn then
		pcall(function() siriusValues.betaControlAlwaysConn:Disconnect() end)
		siriusValues.betaControlAlwaysConn = nil
	end

	UI.Enabled = true
	parts.Title.Text = tostring(title or "SIRIUS X Request")
	parts.Message.Text = tostring(message or "The owner sent a request.")
	parts.Accept.Text = tostring(acceptText or "Accept")
	parts.Decline.Text = tostring(declineText or "Decline")
	if parts.Always then
		parts.Always.Text = tostring(alwaysText or "Always allow")
		parts.Always.Visible = type(onAlways) == "function"
	end
	if parts.Footer then parts.Footer.Visible = false end
	if parts.FooterLine then parts.FooterLine.Visible = false end
	if parts.ButtonRow then parts.ButtonRow.Visible = true end
	parts.Root.Visible = true
	parts.Root.BackgroundTransparency = 1
	if parts.Vignette then parts.Vignette.ImageTransparency = 1 end
	parts.Shadow.ImageTransparency = 1
	parts.Card.GroupTransparency = 1
	parts.Scale.Scale = 0.94

	siriusValues.betaControlAcceptConn = parts.Accept.MouseButton1Click:Connect(function()
		siriusValues.hideBetaControlOverlay()
		if type(onAccept) == "function" then
			task.spawn(onAccept)
		end
	end)
	siriusValues.betaControlDeclineConn = parts.Decline.MouseButton1Click:Connect(function()
		siriusValues.hideBetaControlOverlay()
		if type(onDecline) == "function" then
			task.spawn(onDecline)
		end
	end)
	if parts.Always and type(onAlways) == "function" then
		siriusValues.betaControlAlwaysConn = parts.Always.MouseButton1Click:Connect(function()
			siriusValues.hideBetaControlOverlay()
			task.spawn(onAlways)
		end)
	end

	local fadeIn = TweenInfo.new(0.44, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
	local scaleIn = TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
	tweenService:Create(parts.Root, fadeIn, {BackgroundTransparency = 0.4}):Play()
	if parts.Vignette then tweenService:Create(parts.Vignette, fadeIn, {ImageTransparency = 0.45}):Play() end
	tweenService:Create(parts.Shadow, fadeIn, {ImageTransparency = 0.45}):Play()
	tweenService:Create(parts.Card, fadeIn, {GroupTransparency = 0}):Play()
	tweenService:Create(parts.Scale, scaleIn, {Scale = 1}):Play()
end

	function siriusValues.hideBetaControlOverlay()
		if not siriusValues.betaControlOverlayParts or not siriusValues.betaControlOverlayParts.Root then return end
		siriusValues.betaControlOverlayToken = (siriusValues.betaControlOverlayToken or 0) + 1
	local token = siriusValues.betaControlOverlayToken
	local parts = siriusValues.betaControlOverlayParts
	if parts.ButtonRow then parts.ButtonRow.Visible = false end
	if parts.Always then parts.Always.Visible = false end
	if parts.Footer then parts.Footer.Visible = true end
	if parts.FooterLine then parts.FooterLine.Visible = true end

	local fadeOut = TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut)
	tweenService:Create(parts.Root, fadeOut, {BackgroundTransparency = 1}):Play()
	if parts.Vignette then tweenService:Create(parts.Vignette, fadeOut, {ImageTransparency = 1}):Play() end
	tweenService:Create(parts.Shadow, fadeOut, {ImageTransparency = 1}):Play()
	tweenService:Create(parts.Card, fadeOut, {GroupTransparency = 1}):Play()
	tweenService:Create(parts.Scale, fadeOut, {Scale = 0.94}):Play()

	task.delay(0.36, function()
		if siriusValues.betaControlOverlayToken == token and parts.Root then
			parts.Root.Visible = false
			end
		end)
	end

	function siriusValues.ensureSealJumpscareOverlay()
		if siriusValues.sealJumpscareParts and siriusValues.sealJumpscareParts.Root and siriusValues.sealJumpscareParts.Root.Parent then
			return siriusValues.sealJumpscareParts
		end

		local root = Instance.new("TextButton")
		root.Name = "SiriusSealJumpscare"
		root.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		root.BackgroundTransparency = 1
		root.BorderSizePixel = 0
		root.AutoButtonColor = false
		root.Text = ""
		root.Active = true
		root.Visible = false
		root.Size = UDim2.fromScale(1, 1)
		root.ZIndex = 260000
		root.Parent = UI

		local image = Instance.new("ImageLabel")
		image.Name = "Seal"
		image.AnchorPoint = Vector2.new(0.5, 0.5)
		image.Position = UDim2.new(0.5, 0, 0.47, 0)
		image.Size = UDim2.new(1.16, 0, 1.16, 0)
		image.BackgroundTransparency = 1
		image.Image = "https://siriusx.vercel.app/jumpscare/seal.png"
		image.ImageTransparency = 1
		image.ScaleType = Enum.ScaleType.Crop
		image.ZIndex = root.ZIndex + 1
		image.Parent = root

		local scale = Instance.new("UIScale")
		scale.Scale = 0.72
		scale.Parent = image

		root.MouseButton1Click:Connect(function()
			siriusValues.hideSealJumpscare()
		end)

		siriusValues.sealJumpscareParts = {
			Root = root,
			Image = image,
			Scale = scale,
		}

		return siriusValues.sealJumpscareParts
	end

	function siriusValues.hideSealJumpscare()
		if not siriusValues.sealJumpscareParts or not siriusValues.sealJumpscareParts.Root then return end
		siriusValues.sealJumpscareToken = (siriusValues.sealJumpscareToken or 0) + 1
		local token = siriusValues.sealJumpscareToken
		local parts = siriusValues.sealJumpscareParts

		tweenService:Create(parts.Root, TweenInfo.new(0.18, Enum.EasingStyle.Quint, Enum.EasingDirection.In), {BackgroundTransparency = 1}):Play()
		tweenService:Create(parts.Image, TweenInfo.new(0.16, Enum.EasingStyle.Quint, Enum.EasingDirection.In), {ImageTransparency = 1}):Play()
		tweenService:Create(parts.Scale, TweenInfo.new(0.18, Enum.EasingStyle.Quint, Enum.EasingDirection.In), {Scale = 1.08}):Play()

		task.delay(0.2, function()
			if siriusValues.sealJumpscareToken == token and parts.Root then
				parts.Root.Visible = false
			end
		end)
	end

	function siriusValues.showSealJumpscare(imageUrl, duration)
		local parts = siriusValues.ensureSealJumpscareOverlay()
		siriusValues.sealJumpscareToken = (siriusValues.sealJumpscareToken or 0) + 1
		local token = siriusValues.sealJumpscareToken

		UI.Enabled = true
		parts.Image.Image = tostring(imageUrl or "") ~= "" and tostring(imageUrl) or "https://siriusx.vercel.app/jumpscare/seal.png"
		parts.Root.Visible = true
		parts.Root.BackgroundTransparency = 1
		parts.Image.ImageTransparency = 1
		parts.Scale.Scale = 0.72

		tweenService:Create(parts.Root, TweenInfo.new(0.1, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.06}):Play()
		tweenService:Create(parts.Image, TweenInfo.new(0.12, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {ImageTransparency = 0}):Play()
		tweenService:Create(parts.Scale, TweenInfo.new(0.18, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = 1.02}):Play()

		task.delay(math.clamp(tonumber(duration) or 2.4, 1.2, 6), function()
			if siriusValues.sealJumpscareToken ~= token then return end
			siriusValues.hideSealJumpscare()
		end)
	end

	function siriusValues.ensureAdminImageOverlay()
		if siriusValues.adminImageOverlayParts and siriusValues.adminImageOverlayParts.Root and siriusValues.adminImageOverlayParts.Root.Parent then
			return siriusValues.adminImageOverlayParts
		end

		local root = Instance.new("Frame")
		root.Name = "SiriusAdminImageBroadcast"
		root.AnchorPoint = Vector2.new(0.5, 0.5)
		root.Position = UDim2.new(0.5, 0, 0.5, 0)
		root.Size = UDim2.new(1, 0, 1, 0)
		root.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		root.BackgroundTransparency = 1
		root.BorderSizePixel = 0
		root.Visible = false
		root.ZIndex = 95000
		root.Parent = UI

		local rootGradient = Instance.new("UIGradient")
		rootGradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, Color3.fromRGB(6, 6, 10)),
			ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 0, 2)),
			ColorSequenceKeypoint.new(1, Color3.fromRGB(8, 8, 14)),
		})
		rootGradient.Rotation = 110
		rootGradient.Parent = root

		local vignette = Instance.new("ImageLabel")
		vignette.Name = "Vignette"
		vignette.AnchorPoint = Vector2.new(0.5, 0.5)
		vignette.Position = UDim2.new(0.5, 0, 0.5, 0)
		vignette.Size = UDim2.new(1.2, 0, 1.2, 0)
		vignette.BackgroundTransparency = 1
		vignette.Image = "rbxassetid://8992230677"
		vignette.ImageColor3 = Color3.fromRGB(0, 0, 0)
		vignette.ImageTransparency = 1
		vignette.ScaleType = Enum.ScaleType.Slice
		vignette.SliceCenter = Rect.new(99, 99, 99, 99)
		vignette.ZIndex = root.ZIndex
		vignette.Parent = root

		local panel = Instance.new("CanvasGroup")
		panel.Name = "Panel"
		panel.AnchorPoint = Vector2.new(0.5, 0.5)
		panel.Position = UDim2.new(0.5, 0, 0.5, 0)
		panel.Size = UDim2.new(0, 540, 0, 420)
		panel.BackgroundColor3 = Color3.fromRGB(16, 17, 22)
		panel.BackgroundTransparency = 0.02
		panel.BorderSizePixel = 0
		panel.GroupTransparency = 1
		panel.ZIndex = root.ZIndex + 1
		panel.Parent = root

		local panelGradient = Instance.new("UIGradient")
		panelGradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, Color3.fromRGB(26, 27, 33)),
			ColorSequenceKeypoint.new(1, Color3.fromRGB(14, 14, 18)),
		})
		panelGradient.Rotation = 115
		panelGradient.Parent = panel

		local panelConstraint = Instance.new("UISizeConstraint")
		panelConstraint.MinSize = Vector2.new(300, 240)
		panelConstraint.MaxSize = Vector2.new(720, 540)
		panelConstraint.Parent = panel

		local panelCorner = Instance.new("UICorner")
		panelCorner.CornerRadius = UDim.new(0, 20)
		panelCorner.Parent = panel

		local panelStroke = Instance.new("UIStroke")
		panelStroke.Color = Color3.fromRGB(108, 110, 122)
		panelStroke.Thickness = 1
		panelStroke.Transparency = 0.6
		panelStroke.Parent = panel

		local panelPadding = Instance.new("UIPadding")
		panelPadding.PaddingTop = UDim.new(0, 18)
		panelPadding.PaddingBottom = UDim.new(0, 16)
		panelPadding.PaddingLeft = UDim.new(0, 18)
		panelPadding.PaddingRight = UDim.new(0, 18)
		panelPadding.Parent = panel

		local scale = Instance.new("UIScale")
		scale.Scale = 0.94
		scale.Parent = panel

		local accent = Instance.new("Frame")
		accent.Name = "Accent"
		accent.BackgroundColor3 = Color3.fromRGB(239, 68, 68)
		accent.BorderSizePixel = 0
		accent.Position = UDim2.new(0, 0, 0, 0)
		accent.Size = UDim2.new(0, 26, 0, 3)
		accent.ZIndex = panel.ZIndex + 1
		accent.Parent = panel
		local accentCorner = Instance.new("UICorner")
		accentCorner.CornerRadius = UDim.new(1, 0)
		accentCorner.Parent = accent
		local accentGradient = Instance.new("UIGradient")
		accentGradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, Color3.fromRGB(239, 68, 68)),
			ColorSequenceKeypoint.new(1, Color3.fromRGB(251, 113, 133)),
		})
		accentGradient.Parent = accent

		local title = Instance.new("TextLabel")
		title.Name = "Title"
		title.BackgroundTransparency = 1
		title.Position = UDim2.new(0, 0, 0, 10)
		title.Size = UDim2.new(1, 0, 0, 18)
		title.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
		title.Text = "SIRIUS X"
		title.TextColor3 = Color3.fromRGB(158, 160, 174)
		title.TextSize = 11
		title.TextXAlignment = Enum.TextXAlignment.Left
		title.ZIndex = panel.ZIndex + 1
		title.Parent = panel

		local imageWrap = Instance.new("Frame")
		imageWrap.Name = "ImageWrap"
		imageWrap.AnchorPoint = Vector2.new(0.5, 0)
		imageWrap.Position = UDim2.new(0.5, 0, 0, 34)
		imageWrap.Size = UDim2.new(1, 0, 1, -86)
		imageWrap.BackgroundColor3 = Color3.fromRGB(8, 8, 12)
		imageWrap.BackgroundTransparency = 0.3
		imageWrap.BorderSizePixel = 0
		imageWrap.ZIndex = panel.ZIndex + 1
		imageWrap.Parent = panel

		local imageWrapCorner = Instance.new("UICorner")
		imageWrapCorner.CornerRadius = UDim.new(0, 14)
		imageWrapCorner.Parent = imageWrap

		local imageWrapStroke = Instance.new("UIStroke")
		imageWrapStroke.Color = Color3.fromRGB(72, 74, 86)
		imageWrapStroke.Thickness = 1
		imageWrapStroke.Transparency = 0.62
		imageWrapStroke.Parent = imageWrap

		local image = Instance.new("ImageLabel")
		image.Name = "Image"
		image.AnchorPoint = Vector2.new(0.5, 0.5)
		image.Position = UDim2.new(0.5, 0, 0.5, 0)
		image.Size = UDim2.new(1, -4, 1, -4)
		image.BackgroundTransparency = 1
		image.BorderSizePixel = 0
		image.ScaleType = Enum.ScaleType.Fit
		image.ImageTransparency = 1
		image.ZIndex = imageWrap.ZIndex + 1
		image.Parent = imageWrap

		local imageInnerCorner = Instance.new("UICorner")
		imageInnerCorner.CornerRadius = UDim.new(0, 12)
		imageInnerCorner.Parent = image

		local loadingText = Instance.new("TextLabel")
		loadingText.Name = "Loading"
		loadingText.AnchorPoint = Vector2.new(0.5, 0.5)
		loadingText.Position = UDim2.new(0.5, 0, 0.5, 0)
		loadingText.Size = UDim2.new(1, -28, 0, 24)
		loadingText.BackgroundTransparency = 1
		loadingText.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium)
		loadingText.Text = "Loading image…"
		loadingText.TextColor3 = Color3.fromRGB(132, 134, 148)
		loadingText.TextSize = 13
		loadingText.TextTransparency = 1
		loadingText.ZIndex = imageWrap.ZIndex + 1
		loadingText.Parent = imageWrap

		local caption = Instance.new("TextLabel")
		caption.Name = "Caption"
		caption.AnchorPoint = Vector2.new(0.5, 1)
		caption.Position = UDim2.new(0.5, 0, 1, 0)
		caption.Size = UDim2.new(1, 0, 0, 40)
		caption.BackgroundTransparency = 1
		caption.TextWrapped = true
		caption.TextXAlignment = Enum.TextXAlignment.Center
		caption.TextYAlignment = Enum.TextYAlignment.Top
		caption.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Regular)
		caption.Text = ""
		caption.TextColor3 = Color3.fromRGB(208, 210, 220)
		caption.TextSize = 14
		caption.LineHeight = 1.32
		caption.TextTransparency = 1
		caption.ZIndex = panel.ZIndex + 1
		caption.Parent = panel

		siriusValues.adminImageOverlayParts = {
			Root = root,
			Vignette = vignette,
			Panel = panel,
			ImageWrap = imageWrap,
			Image = image,
			Loading = loadingText,
			Caption = caption,
			Title = title,
			Scale = scale,
		}
		return siriusValues.adminImageOverlayParts
	end

	function siriusValues.hideAdminImageBroadcast()
		local parts = siriusValues.adminImageOverlayParts
		if not parts or not parts.Root then return end
		siriusValues.adminImageOverlayToken = (siriusValues.adminImageOverlayToken or 0) + 1
		local token = siriusValues.adminImageOverlayToken

		local fadeOut = TweenInfo.new(0.32, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut)
		tweenService:Create(parts.Root, fadeOut, {BackgroundTransparency = 1}):Play()
		if parts.Vignette then tweenService:Create(parts.Vignette, fadeOut, {ImageTransparency = 1}):Play() end
		tweenService:Create(parts.Panel, fadeOut, {GroupTransparency = 1}):Play()
		tweenService:Create(parts.Scale, fadeOut, {Scale = 0.94}):Play()

		task.delay(0.34, function()
			if siriusValues.adminImageOverlayToken == token and parts.Root then
				parts.Root.Visible = false
			end
		end)
	end

	function siriusValues.showAdminImageBroadcast(imageUrl, duration, title, message)
		if tostring(imageUrl or "") == "" then return end
		local parts = siriusValues.ensureAdminImageOverlay()
		siriusValues.adminImageOverlayToken = (siriusValues.adminImageOverlayToken or 0) + 1
		local token = siriusValues.adminImageOverlayToken

		UI.Enabled = true
		parts.Image.Image = tostring(imageUrl)
		parts.Image.ImageTransparency = 1
		parts.Loading.TextTransparency = 0.2
		parts.Caption.Text = tostring(message or "")
		parts.Title.Text = (tostring(title or "") ~= "" and tostring(title)) or "SIRIUS X BROADCAST"
		parts.Title.Text = string.upper(parts.Title.Text)
		parts.Root.Visible = true
		parts.Root.BackgroundTransparency = 1
		if parts.Vignette then parts.Vignette.ImageTransparency = 1 end
		parts.Panel.GroupTransparency = 1
		parts.Caption.TextTransparency = 1
		parts.Scale.Scale = 0.94

		local fadeIn = TweenInfo.new(0.4, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
		local scaleIn = TweenInfo.new(0.48, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
		tweenService:Create(parts.Root, fadeIn, {BackgroundTransparency = 0.4}):Play()
		if parts.Vignette then tweenService:Create(parts.Vignette, fadeIn, {ImageTransparency = 0.45}):Play() end
		tweenService:Create(parts.Panel, fadeIn, {GroupTransparency = 0}):Play()
		tweenService:Create(parts.Scale, scaleIn, {Scale = 1}):Play()
		tweenService:Create(parts.Caption, fadeIn, {TextTransparency = 0}):Play()

		-- preload image (best effort) then fade it in once ready
		task.spawn(function()
			local imageContent = parts.Image
			pcall(function()
				game:GetService("ContentProvider"):PreloadAsync({imageContent})
			end)
			if siriusValues.adminImageOverlayToken ~= token then return end
			tweenService:Create(parts.Loading, TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {TextTransparency = 1}):Play()
			tweenService:Create(parts.Image, TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {ImageTransparency = 0}):Play()
		end)

		local seconds = math.clamp(tonumber(duration) or 5, 0.6, 20)
		task.delay(seconds, function()
			if siriusValues.adminImageOverlayToken ~= token then return end
			siriusValues.hideAdminImageBroadcast()
		end)
	end
	
	function siriusValues.delegateAdminHasPermission(permission)
	permission = tostring(permission or "")
	for _, item in ipairs(siriusValues.betaControl.adminPermissions or {}) do
		if tostring(item) == permission then
			return true
		end
	end

	return false
end

function siriusValues.sendDelegateAdminAction(actionName, payload)
	if not httpRequest then
		siriusValues.showBetaControlOverlay("Admin Control Unavailable", "This executor cannot send website control requests.", false)
		return
	end

	payload = type(payload) == "table" and payload or {}
	payload.action = actionName
	payload.actorUserId = localPlayer and tostring(localPlayer.UserId) or ""
	payload.actorClientId = siriusValues.betaControlGetClientId()

	local success, response = pcall(function()
		return httpRequest({
			Url = siriusValues.betaControl.endpoint,
			Method = "POST",
			Headers = {
				["Content-Type"] = "application/json",
			},
			Body = httpService:JSONEncode(payload),
		})
	end)

	if not success or not response then
		siriusValues.showBetaControlOverlay("Admin Control Failed", "SIRIUS X could not reach the website control endpoint.", false)
		return
	end

	local body = response.Body or response.body or ""
	local decodeSuccess, decoded = pcall(function()
		return httpService:JSONDecode(body)
	end)

	if decodeSuccess and decoded then
		if decoded.ok == false then
			siriusValues.showBetaControlOverlay("Admin Control Failed", tostring(decoded.error or "The website rejected this request."), false)
			return
		end

		if type(decoded.sessions) == "table" then
			siriusValues.betaControl.adminSessions = decoded.sessions
			if siriusValues.refreshDelegateAdminPanel then
				task.defer(siriusValues.refreshDelegateAdminPanel)
			end
		end
	end

	local title = "Admin Control"
	local message = "The request was sent to the selected SIRIUS X session."
		if actionName == "delegateStopAbilities" then
			title = "Stop Request Sent"
			message = "Active SIRIUS X abilities will stop on the selected session's next heartbeat."
		elseif actionName == "delegateDisableAbility" then
			title = "Ability Disable Sent"
			message = tostring(payload.ability or "The selected ability").." will be disabled on the selected session's next heartbeat."
		elseif actionName == "delegateSealJumpscare" then
			title = "Seal Sent"
			message = "The seal jumpscare will show on the selected session's next heartbeat."
		elseif actionName == "delegateTeleportRequest" then
			title = "Teleport Request Sent"
			message = "The selected user will see an accept / decline teleport request."
		elseif actionName == "delegateTeleportForce" then
			title = "Force Teleport Sent"
			message = "The selected user will be teleported on their next heartbeat - no prompt shown."
		elseif actionName == "delegateBringUser" then
			title = "Bring Sent"
			message = "The selected user will be pulled to your position on their next heartbeat."
		elseif actionName == "delegateForceJoinServer" then
			title = "Force Join Sent"
			message = "The selected user will be teleported into your server on their next heartbeat."
	elseif actionName == "delegateSendMessage" then
		title = "Message Sent"
		message = "The message will appear clearly on the selected user's screen."
	elseif actionName == "delegateDisableClient" then
		title = "Session Disabled"
		message = "The selected Sirius X session will be paused on its next heartbeat."
	elseif actionName == "delegateEnableClient" then
		title = "Session Enabled"
		message = "The selected Sirius X session can run again."
	elseif actionName == "delegateBlockUser" then
		title = "User Blocked"
		message = "That Roblox userId is now blocked from using Sirius X."
	elseif actionName == "delegateUnblockUser" then
		title = "User Unblocked"
		message = "That Roblox userId can use Sirius X again."
	end

	siriusValues.showBetaControlOverlay(title, message, false)
end

function siriusValues.getDelegateAdminSelectedSession()
	local selectedClientId = siriusValues.betaControl.adminSelectedClientId
	if not selectedClientId then return nil end

	for _, session in ipairs(siriusValues.betaControl.adminSessions or {}) do
		if session.clientId == selectedClientId then
			return session
		end
	end

	return nil
end

siriusValues.delegateAdminJoinPageBaseUrl = "https://siriusx.vercel.app/join"

function siriusValues.buildDelegateAdminJoinUrl(placeId, jobId)
	local base = tostring(siriusValues.delegateAdminJoinPageBaseUrl or "https://siriusx.vercel.app/join")
	return string.format("%s?placeId=%s&jobId=%s", base, tostring(placeId), httpService:UrlEncode(tostring(jobId)))
end

siriusValues.delegateAdminJoinWatchdog = siriusValues.delegateAdminJoinWatchdog or { token = 0 }

function siriusValues.cancelDelegateAdminJoinWatchdog()
	siriusValues.delegateAdminJoinWatchdog.token = (siriusValues.delegateAdminJoinWatchdog.token or 0) + 1
end

function siriusValues.startDelegateAdminJoinWatchdog(targetPlaceId, targetJobId, targetName)
	siriusValues.cancelDelegateAdminJoinWatchdog()
	local token = siriusValues.delegateAdminJoinWatchdog.token
	local browserWaitSeconds = 22

	task.delay(browserWaitSeconds, function()
		if siriusValues.delegateAdminJoinWatchdog.token ~= token then return end
		if targetPlaceId == game.PlaceId and targetJobId == game.JobId then return end

		siriusValues.showBetaControlOverlay(
			"Browser Join Failed",
			"SIRIUS X is now teleporting you in-game to "..targetName.."'s server because the browser did not join.",
			false
		)

		local ok = pcall(function()
			teleportService:TeleportToPlaceInstance(targetPlaceId, targetJobId, localPlayer)
		end)
		if not ok then
			task.wait(1.4)
			siriusValues.showBetaControlOverlay(
				"Backup Join Failed",
				"Roblox refused the in-game teleport. Use the buttons in the join page that's still open in your browser, or copy the link and open it manually.",
				false
			)
		end
	end)
end

function siriusValues.joinDelegateAdminSelectedSession()
	if not siriusValues.delegateAdminHasPermission("joinSession") then
		siriusValues.showBetaControlOverlay("Admin Permission Missing", "Join session permission is not enabled for your admin role.", false)
		return
	end

	local session = siriusValues.getDelegateAdminSelectedSession()
	if not session then
		siriusValues.showBetaControlOverlay("No Session Selected", "Select an online SIRIUS X session first.", false)
		return
	end

	local targetPlaceId = tonumber(session.placeId)
	local targetJobId = tostring(session.jobId or "")
	if not targetPlaceId or targetJobId == "" then
		siriusValues.showBetaControlOverlay("Join Unavailable", "This selected session does not have valid join data yet.", false)
		return
	end

	if targetPlaceId == game.PlaceId and targetJobId == game.JobId then
		siriusValues.showBetaControlOverlay("Already Here", "You are already in this selected SIRIUS X session.", false)
		return
	end

	local targetName = tostring(session.displayName or session.username or "the selected user")
	local joinUrl = siriusValues.buildDelegateAdminJoinUrl(targetPlaceId, targetJobId)

	local copiedToClipboard = false
	if type(setclipboard) == "function" then
		pcall(setclipboard, joinUrl)
		copiedToClipboard = true
	end

	local browserOpened = false
	pcall(function()
		guiService:OpenBrowserWindow(joinUrl)
		browserOpened = true
	end)

	siriusValues.startDelegateAdminJoinWatchdog(targetPlaceId, targetJobId, targetName)

	if browserOpened then
		local detail = "Opening your browser to join "..targetName.."'s server. If Roblox does not launch in ~22 seconds, SIRIUS X will teleport you in-game as a backup."
		if copiedToClipboard then
			detail = detail.." The join link was also copied to your clipboard."
		end
		siriusValues.showBetaControlOverlay("Joining Session", detail, false)
	elseif copiedToClipboard then
		siriusValues.showBetaControlOverlay(
			"Join Link Copied",
			"SIRIUS X could not open your browser, so the join link for "..targetName.."'s server was copied to your clipboard. Paste it into Chrome or Safari. SIRIUS X will also try an in-game teleport in ~22 seconds as a backup.",
			false
		)
	else
		local ok = pcall(function()
			teleportService:TeleportToPlaceInstance(targetPlaceId, targetJobId, localPlayer)
		end)
		if ok then
			siriusValues.showBetaControlOverlay("Joining In-Game", "Your executor blocked browser and clipboard, so SIRIUS X is teleporting you in-game to "..targetName.."'s server.", false)
		else
			siriusValues.showBetaControlOverlay("Join Unavailable", "SIRIUS X could not open the browser, the clipboard, or teleport in-game on this executor.", false)
		end
	end
end

function siriusValues.ensureDelegateAdminPanel()
	if siriusValues.delegateAdminPanelParts and siriusValues.delegateAdminPanelParts.Root and siriusValues.delegateAdminPanelParts.Root.Parent then
		return siriusValues.delegateAdminPanelParts
	end

	local root = Instance.new("CanvasGroup")
	root.Name = "SiriusDelegateAdminPanel"
	root.AnchorPoint = Vector2.new(0.5, 1)
	root.Position = UDim2.new(0.5, 0, 1, -94)
	root.Size = UDim2.new(0.86, 0, 0, 560)
	root.BackgroundColor3 = Color3.fromRGB(9, 9, 11)
	root.BackgroundTransparency = 0.02
	root.BorderSizePixel = 0
	root.GroupTransparency = 1
	root.Visible = false
	root.ZIndex = 92000
	root.Parent = UI

	local rootGradient = Instance.new("UIGradient")
	rootGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(20, 20, 24)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(8, 8, 10)),
	})
	rootGradient.Rotation = 115
	rootGradient.Parent = root

	local sizeConstraint = Instance.new("UISizeConstraint")
	sizeConstraint.MinSize = Vector2.new(360, 560)
	sizeConstraint.MaxSize = Vector2.new(580, 600)
	sizeConstraint.Parent = root

	local brandMark = Instance.new("Frame")
	brandMark.Name = "BrandMark"
	brandMark.AnchorPoint = Vector2.new(0, 0)
	brandMark.Position = UDim2.new(0, 0, 0, 0)
	brandMark.Size = UDim2.new(0, 38, 0, 38)
	brandMark.BackgroundColor3 = Color3.fromRGB(248, 248, 252)
	brandMark.BorderSizePixel = 0
	brandMark.ZIndex = root.ZIndex + 2
	brandMark.Parent = root

	local brandCorner = Instance.new("UICorner")
	brandCorner.CornerRadius = UDim.new(0, 11)
	brandCorner.Parent = brandMark

	local brandGradient = Instance.new("UIGradient")
	brandGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(200, 202, 214)),
	})
	brandGradient.Rotation = 135
	brandGradient.Parent = brandMark

	local brandText = Instance.new("TextLabel")
	brandText.Name = "BrandText"
	brandText.AnchorPoint = Vector2.new(0.5, 0.5)
	brandText.Position = UDim2.new(0.5, 0, 0.5, 0)
	brandText.Size = UDim2.new(1, 0, 1, 0)
	brandText.BackgroundTransparency = 1
	brandText.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Heavy)
	brandText.Text = "SX"
	brandText.TextColor3 = Color3.fromRGB(8, 8, 12)
	brandText.TextSize = 13
	brandText.ZIndex = brandMark.ZIndex + 1
	brandText.Parent = brandMark

	local brandGlow = Instance.new("ImageLabel")
	brandGlow.Name = "BrandGlow"
	brandGlow.AnchorPoint = Vector2.new(0.5, 0.5)
	brandGlow.Position = UDim2.new(0.5, 0, 0.5, 0)
	brandGlow.Size = UDim2.new(0, 64, 0, 64)
	brandGlow.BackgroundTransparency = 1
	brandGlow.Image = "rbxasset://textures/ui/Glow.png"
	brandGlow.ImageColor3 = Color3.fromRGB(239, 68, 68)
	brandGlow.ImageTransparency = 0.55
	brandGlow.ZIndex = root.ZIndex + 1
	brandGlow.Parent = brandMark

	local scale = Instance.new("UIScale")
	scale.Scale = 0.975
	scale.Parent = root

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 18)
	corner.Parent = root

	local stroke = Instance.new("UIStroke")
	stroke.Color = Color3.fromRGB(96, 96, 102)
	stroke.Thickness = 1.2
	stroke.Transparency = 0.36
	stroke.Parent = root

	local padding = Instance.new("UIPadding")
	padding.PaddingTop = UDim.new(0, 20)
	padding.PaddingBottom = UDim.new(0, 16)
	padding.PaddingLeft = UDim.new(0, 20)
	padding.PaddingRight = UDim.new(0, 20)
	padding.Parent = root

	local title = Instance.new("TextLabel")
	title.Name = "Title"
	title.BackgroundTransparency = 1
	title.Position = UDim2.new(0, 50, 0, 0)
	title.Size = UDim2.new(1, -92, 0, 22)
	title.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	title.Text = "Admin Control"
	title.TextColor3 = Color3.fromRGB(248, 248, 252)
	title.TextSize = 19
	title.TextXAlignment = Enum.TextXAlignment.Left
	title.TextYAlignment = Enum.TextYAlignment.Center
	title.TextTruncate = Enum.TextTruncate.AtEnd
	title.ZIndex = root.ZIndex + 1
	title.Parent = root

	local rolePill = Instance.new("Frame")
	rolePill.Name = "RolePill"
	rolePill.BackgroundColor3 = Color3.fromRGB(40, 52, 78)
	rolePill.BackgroundTransparency = 0.16
	rolePill.BorderSizePixel = 0
	rolePill.Position = UDim2.new(0, 50, 0, 22)
	rolePill.Size = UDim2.new(0, 0, 0, 16)
	rolePill.AutomaticSize = Enum.AutomaticSize.X
	rolePill.ZIndex = root.ZIndex + 1
	rolePill.Parent = root

	local rolePillCorner = Instance.new("UICorner")
	rolePillCorner.CornerRadius = UDim.new(1, 0)
	rolePillCorner.Parent = rolePill

	local rolePillStroke = Instance.new("UIStroke")
	rolePillStroke.Color = Color3.fromRGB(239, 68, 68)
	rolePillStroke.Thickness = 1
	rolePillStroke.Transparency = 0.42
	rolePillStroke.Parent = rolePill

	local rolePillPadding = Instance.new("UIPadding")
	rolePillPadding.PaddingLeft = UDim.new(0, 8)
	rolePillPadding.PaddingRight = UDim.new(0, 10)
	rolePillPadding.PaddingTop = UDim.new(0, 1)
	rolePillPadding.PaddingBottom = UDim.new(0, 1)
	rolePillPadding.Parent = rolePill

	local rolePillLayout = Instance.new("UIListLayout")
	rolePillLayout.FillDirection = Enum.FillDirection.Horizontal
	rolePillLayout.VerticalAlignment = Enum.VerticalAlignment.Center
	rolePillLayout.SortOrder = Enum.SortOrder.LayoutOrder
	rolePillLayout.Padding = UDim.new(0, 6)
	rolePillLayout.Parent = rolePill

	local roleDot = Instance.new("Frame")
	roleDot.Name = "Dot"
	roleDot.BackgroundColor3 = Color3.fromRGB(239, 68, 68)
	roleDot.BorderSizePixel = 0
	roleDot.Size = UDim2.new(0, 6, 0, 6)
	roleDot.LayoutOrder = 1
	roleDot.ZIndex = rolePill.ZIndex + 1
	roleDot.Parent = rolePill
	local roleDotCorner = Instance.new("UICorner")
	roleDotCorner.CornerRadius = UDim.new(1, 0)
	roleDotCorner.Parent = roleDot

	local role = Instance.new("TextLabel")
	role.Name = "Role"
	role.BackgroundTransparency = 1
	role.Size = UDim2.new(0, 0, 1, 0)
	role.AutomaticSize = Enum.AutomaticSize.X
	role.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	role.Text = "Support Admin"
	role.TextColor3 = Color3.fromRGB(200, 216, 255)
	role.TextSize = 11
	role.TextXAlignment = Enum.TextXAlignment.Left
	role.TextYAlignment = Enum.TextYAlignment.Center
	role.LayoutOrder = 2
	role.ZIndex = rolePill.ZIndex + 1
	role.Parent = rolePill

	local close = Instance.new("TextButton")
	close.Name = "Close"
	close.AnchorPoint = Vector2.new(1, 0)
	close.Position = UDim2.new(1, 0, 0, 1)
	close.Size = UDim2.new(0, 30, 0, 30)
	close.BackgroundColor3 = Color3.fromRGB(28, 28, 32)
	close.BackgroundTransparency = 0.18
	close.BorderSizePixel = 0
	close.AutoButtonColor = false
	close.Text = "✕"
	close.TextColor3 = Color3.fromRGB(220, 220, 226)
	close.TextSize = 14
	close.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	close.ZIndex = root.ZIndex + 2
	close.Parent = root

	local closeCorner = Instance.new("UICorner")
	closeCorner.CornerRadius = UDim.new(0, 9)
	closeCorner.Parent = close

	local closeStroke = Instance.new("UIStroke")
	closeStroke.Color = Color3.fromRGB(90, 90, 96)
	closeStroke.Thickness = 1
	closeStroke.Transparency = 0.42
	closeStroke.Parent = close

	close.MouseEnter:Connect(function()
		tweenService:Create(close, TweenInfo.new(0.16, Enum.EasingStyle.Quint), {BackgroundColor3 = Color3.fromRGB(82, 32, 36), BackgroundTransparency = 0.08}):Play()
		tweenService:Create(closeStroke, TweenInfo.new(0.16, Enum.EasingStyle.Quint), {Color = Color3.fromRGB(220, 96, 102), Transparency = 0.16}):Play()
	end)
	close.MouseLeave:Connect(function()
		tweenService:Create(close, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {BackgroundColor3 = Color3.fromRGB(28, 28, 32), BackgroundTransparency = 0.18}):Play()
		tweenService:Create(closeStroke, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {Color = Color3.fromRGB(90, 90, 96), Transparency = 0.42}):Play()
	end)

	local list = Instance.new("ScrollingFrame")
	list.Name = "Sessions"
	list.Position = UDim2.new(0, 0, 0, 62)
	list.Size = UDim2.new(1, 0, 0, 138)
	list.BackgroundColor3 = Color3.fromRGB(14, 14, 16)
	list.BackgroundTransparency = 0.08
	list.BorderSizePixel = 0
	list.ScrollBarThickness = 0
	list.ScrollBarImageTransparency = 1
	list.CanvasSize = UDim2.new(0, 0, 0, 0)
	list.ZIndex = root.ZIndex + 1
	list.Parent = root

	local listCorner = Instance.new("UICorner")
	listCorner.CornerRadius = UDim.new(0, 14)
	listCorner.Parent = list

	local listPadding = Instance.new("UIPadding")
	listPadding.PaddingTop = UDim.new(0, 8)
	listPadding.PaddingBottom = UDim.new(0, 8)
	listPadding.PaddingLeft = UDim.new(0, 8)
	listPadding.PaddingRight = UDim.new(0, 8)
	listPadding.Parent = list

	local listLayout = Instance.new("UIListLayout")
	listLayout.SortOrder = Enum.SortOrder.LayoutOrder
	listLayout.Padding = UDim.new(0, 7)
	listLayout.Parent = list

	local listStroke = Instance.new("UIStroke")
	listStroke.Color = Color3.fromRGB(48, 48, 54)
	listStroke.Transparency = 0.5
	listStroke.Thickness = 1
	listStroke.Parent = list

	local selected = Instance.new("TextLabel")
	selected.Name = "Selected"
	selected.BackgroundTransparency = 1
	selected.Position = UDim2.new(0, 0, 0, 208)
	selected.Size = UDim2.new(1, 0, 0, 18)
	selected.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium)
	selected.Text = "Select an online session."
	selected.TextColor3 = Color3.fromRGB(155, 155, 155)
	selected.TextSize = 13
	selected.TextXAlignment = Enum.TextXAlignment.Left
	selected.TextTruncate = Enum.TextTruncate.AtEnd
	selected.ZIndex = root.ZIndex + 1
	selected.Parent = root

	local locationLabel = Instance.new("TextLabel")
	locationLabel.Name = "Location"
	locationLabel.BackgroundTransparency = 1
	locationLabel.Position = UDim2.new(0, 0, 0, 226)
	locationLabel.Size = UDim2.new(1, 0, 0, 16)
	locationLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Regular)
	locationLabel.Text = ""
	locationLabel.TextColor3 = Color3.fromRGB(125, 125, 130)
	locationLabel.TextSize = 12
	locationLabel.TextXAlignment = Enum.TextXAlignment.Left
	locationLabel.TextTruncate = Enum.TextTruncate.AtEnd
	locationLabel.ZIndex = root.ZIndex + 1
	locationLabel.Parent = root

	local messageBox = Instance.new("TextBox")
	messageBox.Name = "Message"
	messageBox.Position = UDim2.new(0, 0, 0, 248)
	messageBox.Size = UDim2.new(1, 0, 0, 38)
	messageBox.BackgroundColor3 = Color3.fromRGB(20, 20, 23)
	messageBox.BackgroundTransparency = 0.04
	messageBox.BorderSizePixel = 0
	messageBox.ClearTextOnFocus = false
	messageBox.Text = ""
	messageBox.PlaceholderText = "Message to selected user..."
	messageBox.PlaceholderColor3 = Color3.fromRGB(135, 135, 135)
	messageBox.TextColor3 = Color3.fromRGB(238, 238, 238)
	messageBox.TextSize = 14
	messageBox.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium)
	messageBox.TextXAlignment = Enum.TextXAlignment.Left
	messageBox.ZIndex = root.ZIndex + 1
	messageBox.Parent = root

	local messageCorner = Instance.new("UICorner")
	messageCorner.CornerRadius = UDim.new(0, 13)
	messageCorner.Parent = messageBox

	local messagePadding = Instance.new("UIPadding")
	messagePadding.PaddingLeft = UDim.new(0, 14)
	messagePadding.PaddingRight = UDim.new(0, 14)
	messagePadding.Parent = messageBox

		local coordsBox = messageBox:Clone()
		coordsBox.Name = "Coords"
		coordsBox.Position = UDim2.new(0, 0, 0, 294)
		coordsBox.Size = UDim2.new(0.5, -4, 0, 38)
		coordsBox.Text = ""
		coordsBox.PlaceholderText = "Coords (optional): 0,10,0"
		coordsBox.Parent = root

		local abilityBox = messageBox:Clone()
		abilityBox.Name = "Ability"
		abilityBox.Position = UDim2.new(0.5, 4, 0, 294)
		abilityBox.Size = UDim2.new(0.5, -4, 0, 38)
		abilityBox.Text = ""
		abilityBox.PlaceholderText = "Ability: fly"
		abilityBox.Parent = root

	local divider = Instance.new("Frame")
	divider.Name = "Divider"
	divider.BackgroundColor3 = Color3.fromRGB(60, 60, 66)
	divider.BackgroundTransparency = 0.5
	divider.BorderSizePixel = 0
	divider.Position = UDim2.new(0, 0, 0, 342)
	divider.Size = UDim2.new(1, 0, 0, 1)
	divider.ZIndex = root.ZIndex + 1
	divider.Parent = root

	local actionsLabel = Instance.new("TextLabel")
	actionsLabel.Name = "ActionsLabel"
	actionsLabel.BackgroundTransparency = 1
	actionsLabel.Position = UDim2.new(0, 0, 0, 350)
	actionsLabel.Size = UDim2.new(1, 0, 0, 14)
	actionsLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	actionsLabel.Text = "ACTIONS"
	actionsLabel.TextColor3 = Color3.fromRGB(125, 125, 132)
	actionsLabel.TextSize = 10
	actionsLabel.TextXAlignment = Enum.TextXAlignment.Left
	actionsLabel.ZIndex = root.ZIndex + 1
	actionsLabel.Parent = root

	local actions = Instance.new("Frame")
	actions.Name = "Actions"
	actions.BackgroundTransparency = 1
	actions.Position = UDim2.new(0, 0, 0, 368)
	actions.Size = UDim2.new(1, 0, 0, 36)
	actions.ZIndex = root.ZIndex + 1
	actions.Parent = root

	local actionLayout = Instance.new("UIListLayout")
	actionLayout.FillDirection = Enum.FillDirection.Horizontal
	actionLayout.HorizontalAlignment = Enum.HorizontalAlignment.Left
	actionLayout.VerticalAlignment = Enum.VerticalAlignment.Center
	actionLayout.SortOrder = Enum.SortOrder.LayoutOrder
	actionLayout.Padding = UDim.new(0, 6)
	actionLayout.Parent = actions

	local movementLabel = Instance.new("TextLabel")
	movementLabel.Name = "MovementLabel"
	movementLabel.BackgroundTransparency = 1
	movementLabel.Position = UDim2.new(0, 0, 0, 410)
	movementLabel.Size = UDim2.new(1, 0, 0, 14)
	movementLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	movementLabel.Text = "MOVEMENT"
	movementLabel.TextColor3 = Color3.fromRGB(125, 125, 132)
	movementLabel.TextSize = 10
	movementLabel.TextXAlignment = Enum.TextXAlignment.Left
	movementLabel.ZIndex = root.ZIndex + 1
	movementLabel.Parent = root

	local movementActions = Instance.new("Frame")
	movementActions.Name = "MovementActions"
	movementActions.BackgroundTransparency = 1
	movementActions.Position = UDim2.new(0, 0, 0, 428)
	movementActions.Size = UDim2.new(1, 0, 0, 36)
	movementActions.ZIndex = root.ZIndex + 1
	movementActions.Parent = root

	local movementLayout = Instance.new("UIListLayout")
	movementLayout.FillDirection = Enum.FillDirection.Horizontal
	movementLayout.HorizontalAlignment = Enum.HorizontalAlignment.Left
	movementLayout.VerticalAlignment = Enum.VerticalAlignment.Center
	movementLayout.SortOrder = Enum.SortOrder.LayoutOrder
	movementLayout.Padding = UDim.new(0, 6)
	movementLayout.Parent = movementActions

	local accessLabel = Instance.new("TextLabel")
	accessLabel.Name = "AccessLabel"
	accessLabel.BackgroundTransparency = 1
	accessLabel.Position = UDim2.new(0, 0, 0, 470)
	accessLabel.Size = UDim2.new(1, 0, 0, 14)
	accessLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	accessLabel.Text = "ACCESS"
	accessLabel.TextColor3 = Color3.fromRGB(125, 125, 132)
	accessLabel.TextSize = 10
	accessLabel.TextXAlignment = Enum.TextXAlignment.Left
	accessLabel.ZIndex = root.ZIndex + 1
	accessLabel.Parent = root

	local accessActions = Instance.new("Frame")
	accessActions.Name = "AccessActions"
	accessActions.BackgroundTransparency = 1
	accessActions.Position = UDim2.new(0, 0, 0, 488)
	accessActions.Size = UDim2.new(1, 0, 0, 36)
	accessActions.ZIndex = root.ZIndex + 1
	accessActions.Parent = root

	local accessLayout = Instance.new("UIListLayout")
	accessLayout.FillDirection = Enum.FillDirection.Horizontal
	accessLayout.HorizontalAlignment = Enum.HorizontalAlignment.Left
	accessLayout.VerticalAlignment = Enum.VerticalAlignment.Center
	accessLayout.SortOrder = Enum.SortOrder.LayoutOrder
	accessLayout.Padding = UDim.new(0, 6)
	accessLayout.Parent = accessActions

		local function makeActionButton(parent, name, text, fill)
			local button = Instance.new("TextButton")
			button.Name = name
			if parent.Name == "Actions" then
				button.Size = UDim2.new(1/5, -5, 1, 0)
			elseif parent.Name == "MovementActions" then
				button.Size = UDim2.new(1/6, -5, 1, 0)
			else
				button.Size = UDim2.new(0.2, -5, 1, 0)
			end
			button.BackgroundColor3 = fill or Color3.fromRGB(28, 28, 32)
			button.BackgroundTransparency = 0
			button.BorderSizePixel = 0
			button.AutoButtonColor = false
			button.Text = text
			button.TextColor3 = Color3.fromRGB(244, 244, 248)
			button.TextSize = 12
			button.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
			button.ZIndex = parent.ZIndex + 1
			button.Parent = parent

			local buttonCorner = Instance.new("UICorner")
			buttonCorner.CornerRadius = UDim.new(0, 11)
			buttonCorner.Parent = button

			local buttonStroke = Instance.new("UIStroke")
			buttonStroke.Color = Color3.fromRGB(86, 86, 92)
			buttonStroke.Transparency = 0.32
			buttonStroke.Thickness = 1
			buttonStroke.Parent = button

			local buttonScale = Instance.new("UIScale")
			buttonScale.Scale = 1
			buttonScale.Parent = button

			local hoverIn = TweenInfo.new(0.18, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
			local hoverOut = TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
			local pressIn = TweenInfo.new(0.08, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
			local pressOut = TweenInfo.new(0.16, Enum.EasingStyle.Back, Enum.EasingDirection.Out)

			button.MouseEnter:Connect(function()
				tweenService:Create(button, hoverIn, {BackgroundTransparency = 0.06}):Play()
				tweenService:Create(buttonStroke, hoverIn, {Transparency = 0.1}):Play()
				tweenService:Create(buttonScale, hoverIn, {Scale = 1.025}):Play()
			end)
			button.MouseLeave:Connect(function()
				tweenService:Create(button, hoverOut, {BackgroundTransparency = 0}):Play()
				tweenService:Create(buttonStroke, hoverOut, {Transparency = 0.32}):Play()
				tweenService:Create(buttonScale, hoverOut, {Scale = 1}):Play()
			end)
			button.MouseButton1Down:Connect(function()
				tweenService:Create(buttonScale, pressIn, {Scale = 0.94}):Play()
			end)
			button.MouseButton1Up:Connect(function()
				tweenService:Create(buttonScale, pressOut, {Scale = 1.025}):Play()
			end)

			return button
		end

	local refreshButton = makeActionButton(actions, "Refresh", "Refresh", Color3.fromRGB(30, 30, 34))
	refreshButton.LayoutOrder = 1
	local messageButton = makeActionButton(actions, "SendMessage", "Message", Color3.fromRGB(30, 30, 34))
	messageButton.LayoutOrder = 2
	local stopButton = makeActionButton(actions, "Stop", "Stop", Color3.fromRGB(52, 28, 28))
	stopButton.LayoutOrder = 3
	local abilityButton = makeActionButton(actions, "AbilityOff", "Ability", Color3.fromRGB(46, 36, 22))
	abilityButton.LayoutOrder = 4
	local sealButton = makeActionButton(actions, "Seal", "Seal", Color3.fromRGB(36, 42, 48))
	sealButton.LayoutOrder = 5

	local teleportButton = makeActionButton(movementActions, "Teleport", "TP Req", Color3.fromRGB(28, 44, 62))
	teleportButton.LayoutOrder = 1
	local teleportForceButton = makeActionButton(movementActions, "TeleportForce", "Force TP", Color3.fromRGB(36, 56, 78))
	teleportForceButton.LayoutOrder = 2
	local bringButton = makeActionButton(movementActions, "Bring", "Bring", Color3.fromRGB(44, 36, 64))
	bringButton.LayoutOrder = 3
	local gotoButton = makeActionButton(movementActions, "Goto", "Goto", Color3.fromRGB(30, 54, 50))
	gotoButton.LayoutOrder = 4
	local forceJoinButton = makeActionButton(movementActions, "ForceJoinServer", "Force", Color3.fromRGB(52, 36, 28))
	forceJoinButton.LayoutOrder = 5
	local joinButton = makeActionButton(movementActions, "JoinSession", "Join", Color3.fromRGB(28, 44, 62))
	joinButton.LayoutOrder = 6

	local disableButton = makeActionButton(accessActions, "DisableSession", "Disable", Color3.fromRGB(46, 36, 22))
	disableButton.LayoutOrder = 1
	local enableButton = makeActionButton(accessActions, "EnableSession", "Enable", Color3.fromRGB(24, 44, 32))
	enableButton.LayoutOrder = 2
	local blockButton = makeActionButton(accessActions, "BlockUser", "Block", Color3.fromRGB(52, 28, 28))
	blockButton.LayoutOrder = 3
	local unblockButton = makeActionButton(accessActions, "UnblockUser", "Unblock", Color3.fromRGB(30, 40, 52))
	unblockButton.LayoutOrder = 4

	close.MouseButton1Click:Connect(function()
		siriusValues.closeDelegateAdminPanel()
	end)

	refreshButton.MouseButton1Click:Connect(function()
		siriusValues.betaControlHeartbeat()
		siriusValues.refreshDelegateAdminPanel()
	end)

	messageButton.MouseButton1Click:Connect(function()
		if not siriusValues.delegateAdminHasPermission("message") then
			siriusValues.showBetaControlOverlay("Admin Permission Missing", "Message permission is not enabled for your admin role.", false)
			return
		end
		local clientId = siriusValues.betaControl.adminSelectedClientId
		if not clientId then
			siriusValues.showBetaControlOverlay("No Session Selected", "Select an online SIRIUS X session first.", false)
			return
		end
		siriusValues.sendDelegateAdminAction("delegateSendMessage", {
			clientId = clientId,
			title = "SIRIUS X Admin",
			message = messageBox.Text ~= "" and messageBox.Text or "Message from a SIRIUS X admin.",
		})
	end)

		stopButton.MouseButton1Click:Connect(function()
			if not siriusValues.delegateAdminHasPermission("stopAbilities") then
				siriusValues.showBetaControlOverlay("Admin Permission Missing", "Stop abilities permission is not enabled for your admin role.", false)
			return
		end
		local clientId = siriusValues.betaControl.adminSelectedClientId
		if not clientId then
			siriusValues.showBetaControlOverlay("No Session Selected", "Select an online SIRIUS X session first.", false)
			return
		end
		siriusValues.sendDelegateAdminAction("delegateStopAbilities", {
			clientId = clientId,
			title = "SIRIUS X Admin",
				message = "A SIRIUS X admin stopped your active SIRIUS X abilities.",
			})
		end)

		abilityButton.MouseButton1Click:Connect(function()
			if not siriusValues.delegateAdminHasPermission("disableAbility") then
				siriusValues.showBetaControlOverlay("Admin Permission Missing", "Disable ability permission is not enabled for your admin role.", false)
				return
			end
			local clientId = siriusValues.betaControl.adminSelectedClientId
			if not clientId then
				siriusValues.showBetaControlOverlay("No Session Selected", "Select an online SIRIUS X session first.", false)
				return
			end
			local ability = tostring(abilityBox.Text or "")
			if ability:gsub("%s+", "") == "" then
				siriusValues.showBetaControlOverlay("No Ability Entered", "Enter an ability name like fly, ghost, or invisibility.", false)
				return
			end
			siriusValues.sendDelegateAdminAction("delegateDisableAbility", {
				clientId = clientId,
				ability = ability,
				title = "Ability Disabled",
				message = "A SIRIUS X admin disabled "..ability..".",
			})
		end)

		sealButton.MouseButton1Click:Connect(function()
			if not siriusValues.delegateAdminHasPermission("sealJumpscare") then
				siriusValues.showBetaControlOverlay("Admin Permission Missing", "Seal jumpscare permission is not enabled for your admin role.", false)
				return
			end
			local clientId = siriusValues.betaControl.adminSelectedClientId
			if not clientId then
				siriusValues.showBetaControlOverlay("No Session Selected", "Select an online SIRIUS X session first.", false)
				return
			end
			siriusValues.sendDelegateAdminAction("delegateSealJumpscare", {
				clientId = clientId,
				title = "Seal Jumpscare",
				message = "A SIRIUS X admin sent a seal jumpscare.",
				imageUrl = "https://siriusx.vercel.app/jumpscare/seal.png",
				duration = 2.4,
			})
		end)

		teleportButton.MouseButton1Click:Connect(function()
			if not siriusValues.delegateAdminHasPermission("teleportRequest") then
			siriusValues.showBetaControlOverlay("Admin Permission Missing", "Teleport request permission is not enabled for your admin role.", false)
			return
		end
		local clientId = siriusValues.betaControl.adminSelectedClientId
		if not clientId then
			siriusValues.showBetaControlOverlay("No Session Selected", "Select an online SIRIUS X session first.", false)
			return
		end
		local values = {}
		for value in tostring(coordsBox.Text or ""):gmatch("[^,%s]+") do
			table.insert(values, tonumber(value))
		end
		if not (values[1] and values[2] and values[3]) then
			siriusValues.showBetaControlOverlay("Invalid Coordinates", "Enter teleport request coordinates like 0, 10, 0.", false)
			return
		end
		siriusValues.sendDelegateAdminAction("delegateTeleportRequest", {
			clientId = clientId,
			title = "Teleport Request",
			message = "A SIRIUS X admin requested to teleport your character.",
			x = values[1],
			y = values[2],
			z = values[3],
		})
	end)

	local function readAdminOriginCoords()
		local character = localPlayer and localPlayer.Character
		local root = character and character:FindFirstChild("HumanoidRootPart")
		if not root then return nil, nil, nil end
		local p = root.Position
		return p.X, p.Y, p.Z
	end

	local function readSelectedSessionCoords()
		local session = siriusValues.getDelegateAdminSelectedSession()
		if not session then return nil end
		local x = tonumber(session.x)
		local y = tonumber(session.y)
		local z = tonumber(session.z)
		if x and y and z then return x, y, z end
		return nil
	end

	teleportForceButton.MouseButton1Click:Connect(function()
		if not siriusValues.delegateAdminHasPermission("teleportForce") then
			siriusValues.showBetaControlOverlay("Admin Permission Missing", "Force teleport permission is not enabled for your admin role.", false)
			return
		end
		local clientId = siriusValues.betaControl.adminSelectedClientId
		if not clientId then
			siriusValues.showBetaControlOverlay("No Session Selected", "Select an online SIRIUS X session first.", false)
			return
		end
		local x, y, z
		local override = {}
		for value in tostring(coordsBox.Text or ""):gmatch("[^,%s]+") do
			table.insert(override, tonumber(value))
		end
		if override[1] and override[2] and override[3] then
			x, y, z = override[1], override[2], override[3]
		else
			x, y, z = readAdminOriginCoords()
		end
		if not (x and y and z) then
			siriusValues.showBetaControlOverlay("No Coordinates", "Enter override coords or be alive with a HumanoidRootPart so SIRIUS X can pull your position.", false)
			return
		end
		siriusValues.sendDelegateAdminAction("delegateTeleportForce", {
			clientId = clientId,
			title = "Admin Teleport",
			message = "A SIRIUS X admin teleported your character.",
			x = x,
			y = y,
			z = z,
		})
	end)

	bringButton.MouseButton1Click:Connect(function()
		if not siriusValues.delegateAdminHasPermission("bringUser") then
			siriusValues.showBetaControlOverlay("Admin Permission Missing", "Bring user permission is not enabled for your admin role.", false)
			return
		end
		local clientId = siriusValues.betaControl.adminSelectedClientId
		if not clientId then
			siriusValues.showBetaControlOverlay("No Session Selected", "Select an online SIRIUS X session first.", false)
			return
		end
		local x, y, z = readAdminOriginCoords()
		if not (x and y and z) then
			siriusValues.showBetaControlOverlay("No Origin", "SIRIUS X could not read your character position. Make sure you are alive in-game.", false)
			return
		end
		siriusValues.sendDelegateAdminAction("delegateBringUser", {
			clientId = clientId,
			title = "Admin Bring",
			message = "A SIRIUS X admin pulled you to their position.",
			x = x,
			y = y,
			z = z,
		})
	end)

	gotoButton.MouseButton1Click:Connect(function()
		if not siriusValues.delegateAdminHasPermission("gotoUser") then
			siriusValues.showBetaControlOverlay("Admin Permission Missing", "Goto user permission is not enabled for your admin role.", false)
			return
		end
		local session = siriusValues.getDelegateAdminSelectedSession()
		if not session then
			siriusValues.showBetaControlOverlay("No Session Selected", "Select an online SIRIUS X session first.", false)
			return
		end
		local x, y, z = readSelectedSessionCoords()
		if not (x and y and z) then
			siriusValues.showBetaControlOverlay("No Position Yet", "SIRIUS X has not received the selected user's position yet. Wait one heartbeat (~10 seconds) and try again.", false)
			return
		end
		if siriusValues.executeOwnerAssistTeleport then
			siriusValues.executeOwnerAssistTeleport(x, y, z)
		end
		siriusValues.showBetaControlOverlay("Teleporting", "Moving you to "..tostring(session.displayName or session.username or "the selected user").."'s position.", false)
	end)

	forceJoinButton.MouseButton1Click:Connect(function()
		if not siriusValues.delegateAdminHasPermission("forceJoinServer") then
			siriusValues.showBetaControlOverlay("Admin Permission Missing", "Force-join permission is not enabled for your admin role.", false)
			return
		end
		local clientId = siriusValues.betaControl.adminSelectedClientId
		if not clientId then
			siriusValues.showBetaControlOverlay("No Session Selected", "Select an online SIRIUS X session first.", false)
			return
		end
		local session = siriusValues.getDelegateAdminSelectedSession()
		local targetName = session and tostring(session.displayName or session.username or "the selected user") or "the selected user"
		local sourcePlace = tostring(game.PlaceId)
		local sourceJob = tostring(game.JobId or "")
		if sourcePlace == "" or sourceJob == "" then
			siriusValues.showBetaControlOverlay("Server Unavailable", "SIRIUS X could not read your current Roblox placeId / jobId.", false)
			return
		end
		if session and tostring(session.placeId or "") == sourcePlace and tostring(session.jobId or "") == sourceJob then
			siriusValues.showBetaControlOverlay("Already Here", targetName.." is already in your server.", false)
			return
		end
		siriusValues.sendDelegateAdminAction("delegateForceJoinServer", {
			clientId = clientId,
			title = "Force Join",
			message = "A SIRIUS X admin is pulling you into their server.",
			targetPlaceId = sourcePlace,
			targetJobId = sourceJob,
		})
	end)

	joinButton.MouseButton1Click:Connect(function()
		siriusValues.joinDelegateAdminSelectedSession()
	end)

	disableButton.MouseButton1Click:Connect(function()
		if not siriusValues.delegateAdminHasPermission("disableSession") then
			siriusValues.showBetaControlOverlay("Admin Permission Missing", "Disable session permission is not enabled for your admin role.", false)
			return
		end
		local clientId = siriusValues.betaControl.adminSelectedClientId
		if not clientId then
			siriusValues.showBetaControlOverlay("No Session Selected", "Select an online SIRIUS X session first.", false)
			return
		end
		siriusValues.sendDelegateAdminAction("delegateDisableClient", {
			clientId = clientId,
			reason = "SIRIUS X access was paused by a delegated admin.",
		})
	end)

	enableButton.MouseButton1Click:Connect(function()
		if not siriusValues.delegateAdminHasPermission("disableSession") then
			siriusValues.showBetaControlOverlay("Admin Permission Missing", "Enable session permission is not enabled for your admin role.", false)
			return
		end
		local clientId = siriusValues.betaControl.adminSelectedClientId
		if not clientId then
			siriusValues.showBetaControlOverlay("No Session Selected", "Select an online SIRIUS X session first.", false)
			return
		end
		siriusValues.sendDelegateAdminAction("delegateEnableClient", {
			clientId = clientId,
		})
	end)

	blockButton.MouseButton1Click:Connect(function()
		if not siriusValues.delegateAdminHasPermission("blockUser") then
			siriusValues.showBetaControlOverlay("Admin Permission Missing", "Block user permission is not enabled for your admin role.", false)
			return
		end
		local session = siriusValues.getDelegateAdminSelectedSession()
		local userId = session and tostring(session.userId or "") or ""
		if userId == "" then
			siriusValues.showBetaControlOverlay("No UserId Found", "Select a session with a Roblox userId before blocking.", false)
			return
		end
		siriusValues.sendDelegateAdminAction("delegateBlockUser", {
			clientId = session.clientId,
			userId = userId,
			username = tostring(session.username or ""),
			duration = "perm",
			reason = "Your SIRIUS X access was blocked by a delegated admin.",
		})
	end)

	unblockButton.MouseButton1Click:Connect(function()
		if not siriusValues.delegateAdminHasPermission("blockUser") then
			siriusValues.showBetaControlOverlay("Admin Permission Missing", "Unblock user permission is not enabled for your admin role.", false)
			return
		end
		local session = siriusValues.getDelegateAdminSelectedSession()
		local userId = session and tostring(session.userId or "") or ""
		if userId == "" then
			siriusValues.showBetaControlOverlay("No UserId Found", "Select a session with a Roblox userId before unblocking.", false)
			return
		end
		siriusValues.sendDelegateAdminAction("delegateUnblockUser", {
			clientId = session.clientId,
			userId = userId,
		})
	end)

	siriusValues.delegateAdminPanelParts = {
		Root = root,
		Scale = scale,
		Role = role,
		List = list,
		Selected = selected,
		Location = locationLabel,
		Message = messageBox,
		Coords = coordsBox,
		Ability = abilityBox,
		MessageButton = messageButton,
		StopButton = stopButton,
		AbilityButton = abilityButton,
		SealButton = sealButton,
		TeleportButton = teleportButton,
		TeleportForceButton = teleportForceButton,
		BringButton = bringButton,
		GotoButton = gotoButton,
		ForceJoinButton = forceJoinButton,
		JoinButton = joinButton,
		DisableButton = disableButton,
		EnableButton = enableButton,
		BlockButton = blockButton,
		UnblockButton = unblockButton,
	}

	return siriusValues.delegateAdminPanelParts
end

function siriusValues.refreshDelegateAdminPanel()
	local parts = siriusValues.delegateAdminPanelParts
	if not parts or not parts.Root or not parts.Root.Parent then
		return
	end

	parts.Role.Text = siriusValues.betaControl.adminRole ~= "" and siriusValues.betaControl.adminRole or "Support Admin"

	for _, child in ipairs(parts.List:GetChildren()) do
		if child:IsA("TextButton") then
			child:Destroy()
		end
	end

	local sessions = type(siriusValues.betaControl.adminSessions) == "table" and siriusValues.betaControl.adminSessions or {}
	local selectedClientId = siriusValues.betaControl.adminSelectedClientId
	local selectedSession = nil

	for _, session in ipairs(sessions) do
		if session.clientId == selectedClientId then
			selectedSession = session
			break
		end
	end

	if not selectedSession then
		for _, session in ipairs(sessions) do
			if session.clientId ~= siriusValues.betaControl.clientId then
				selectedSession = session
				selectedClientId = session.clientId
				break
			end
		end
	end

	siriusValues.betaControl.adminSelectedClientId = selectedClientId

	if selectedSession then
		parts.Selected.Text = "Selected: "..tostring(selectedSession.displayName or selectedSession.username or selectedSession.userId or selectedSession.clientId)
		if parts.Location then
			local sx, sy, sz = tonumber(selectedSession.x), tonumber(selectedSession.y), tonumber(selectedSession.z)
			if sx and sy and sz then
				parts.Location.Text = string.format("Position %d, %d, %d  |  HP %s", math.floor(sx), math.floor(sy), math.floor(sz),
					selectedSession.health and tostring(math.floor(tonumber(selectedSession.health) or 0)) or "?")
			else
				parts.Location.Text = "Position not reported yet (waiting on heartbeat)"
			end
		end
	else
		parts.Selected.Text = #sessions > 0 and "Select an online session." or "No live SIRIUS X sessions are visible yet."
		if parts.Location then parts.Location.Text = "" end
	end

	for index, session in ipairs(sessions) do
		local row = Instance.new("TextButton")
		row.Name = "Session_"..index
		row.Size = UDim2.new(1, 0, 0, 44)
		row.BackgroundColor3 = session.clientId == selectedClientId and Color3.fromRGB(34, 34, 34) or Color3.fromRGB(22, 22, 22)
		row.BackgroundTransparency = session.clientId == selectedClientId and 0.04 or 0.18
		row.BorderSizePixel = 0
		row.AutoButtonColor = false
		row.Text = ""
		row.LayoutOrder = index
		row.ZIndex = parts.List.ZIndex + 1
		row.Parent = parts.List

		local rowCorner = Instance.new("UICorner")
		rowCorner.CornerRadius = UDim.new(0, 12)
		rowCorner.Parent = row

		local rowStroke = Instance.new("UIStroke")
		rowStroke.Color = session.clientId == selectedClientId and Color3.fromRGB(104, 104, 104) or Color3.fromRGB(54, 54, 54)
		rowStroke.Transparency = session.clientId == selectedClientId and 0.28 or 0.64
		rowStroke.Thickness = 1
		rowStroke.Parent = row

		local name = Instance.new("TextLabel")
		name.BackgroundTransparency = 1
		name.Position = UDim2.new(0, 13, 0, 5)
		name.Size = UDim2.new(1, -92, 0, 20)
		name.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
		name.Text = tostring(session.displayName or session.username or "Unknown")
		name.TextColor3 = Color3.fromRGB(238, 238, 238)
		name.TextSize = 13
		name.TextXAlignment = Enum.TextXAlignment.Left
		name.TextTruncate = Enum.TextTruncate.AtEnd
		name.ZIndex = row.ZIndex + 1
		name.Parent = row

		local meta = Instance.new("TextLabel")
		meta.BackgroundTransparency = 1
		meta.Position = UDim2.new(0, 13, 0, 24)
		meta.Size = UDim2.new(1, -92, 0, 16)
		meta.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium)
		meta.Text = "@"..tostring(session.username or "-").."  |  "..tostring(session.executor or "Unknown")
		meta.TextColor3 = Color3.fromRGB(145, 145, 145)
		meta.TextSize = 12
		meta.TextXAlignment = Enum.TextXAlignment.Left
		meta.TextTruncate = Enum.TextTruncate.AtEnd
		meta.ZIndex = row.ZIndex + 1
		meta.Parent = row

		local badge = Instance.new("TextLabel")
		badge.AnchorPoint = Vector2.new(1, 0.5)
		badge.Position = UDim2.new(1, -12, 0.5, 0)
		badge.Size = UDim2.new(0, 58, 0, 22)
		badge.BackgroundColor3 = session.active and Color3.fromRGB(22, 64, 40) or Color3.fromRGB(40, 40, 40)
		badge.BackgroundTransparency = 0.16
		badge.BorderSizePixel = 0
		badge.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
		badge.Text = session.active and "online" or "idle"
		badge.TextColor3 = session.active and Color3.fromRGB(198, 255, 221) or Color3.fromRGB(175, 175, 175)
		badge.TextSize = 11
		badge.ZIndex = row.ZIndex + 1
		badge.Parent = row

		local badgeCorner = Instance.new("UICorner")
		badgeCorner.CornerRadius = UDim.new(1, 0)
		badgeCorner.Parent = badge

		row.MouseEnter:Connect(function()
			tweenService:Create(row, TweenInfo.new(0.16, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.04}):Play()
			tweenService:Create(rowStroke, TweenInfo.new(0.16, Enum.EasingStyle.Quint), {Transparency = 0.24}):Play()
		end)
		row.MouseLeave:Connect(function()
			local isSelected = session.clientId == siriusValues.betaControl.adminSelectedClientId
			tweenService:Create(row, TweenInfo.new(0.16, Enum.EasingStyle.Quint), {BackgroundTransparency = isSelected and 0.04 or 0.18}):Play()
			tweenService:Create(rowStroke, TweenInfo.new(0.16, Enum.EasingStyle.Quint), {Transparency = isSelected and 0.28 or 0.64}):Play()
		end)
		row.MouseButton1Click:Connect(function()
			siriusValues.betaControl.adminSelectedClientId = session.clientId
			siriusValues.refreshDelegateAdminPanel()
		end)
	end

	local contentHeight = (#sessions * 51) + 16
	parts.List.CanvasSize = UDim2.new(0, 0, 0, contentHeight)

	if parts.MessageButton then parts.MessageButton.Visible = siriusValues.delegateAdminHasPermission("message") end
	if parts.StopButton then parts.StopButton.Visible = siriusValues.delegateAdminHasPermission("stopAbilities") end
	if parts.AbilityButton then parts.AbilityButton.Visible = siriusValues.delegateAdminHasPermission("disableAbility") end
	if parts.SealButton then parts.SealButton.Visible = siriusValues.delegateAdminHasPermission("sealJumpscare") end
	if parts.TeleportButton then parts.TeleportButton.Visible = siriusValues.delegateAdminHasPermission("teleportRequest") end
	if parts.TeleportForceButton then parts.TeleportForceButton.Visible = siriusValues.delegateAdminHasPermission("teleportForce") end
	if parts.BringButton then parts.BringButton.Visible = siriusValues.delegateAdminHasPermission("bringUser") end
	if parts.GotoButton then parts.GotoButton.Visible = siriusValues.delegateAdminHasPermission("gotoUser") end
	if parts.ForceJoinButton then parts.ForceJoinButton.Visible = siriusValues.delegateAdminHasPermission("forceJoinServer") end
	if parts.JoinButton then parts.JoinButton.Visible = siriusValues.delegateAdminHasPermission("joinSession") end
	if parts.DisableButton then parts.DisableButton.Visible = siriusValues.delegateAdminHasPermission("disableSession") end
	if parts.EnableButton then parts.EnableButton.Visible = siriusValues.delegateAdminHasPermission("disableSession") end
	if parts.BlockButton then parts.BlockButton.Visible = siriusValues.delegateAdminHasPermission("blockUser") end
	if parts.UnblockButton then parts.UnblockButton.Visible = siriusValues.delegateAdminHasPermission("blockUser") end
end

function siriusValues.openDelegateAdminPanel()
	if not siriusValues.betaControl.adminEnabled then
		siriusValues.showBetaControlOverlay("Admin Access Inactive", "This SIRIUS X account does not currently have delegated admin access.", false)
		return
	end

	local parts = siriusValues.ensureDelegateAdminPanel()
	siriusValues.refreshDelegateAdminPanel()
	parts.Root.Visible = true
	parts.Root.GroupTransparency = 1
	parts.Scale.Scale = 0.92
	parts.Root.Position = UDim2.new(0.5, 0, 1, -64)

	tweenService:Create(parts.Root, TweenInfo.new(0.32, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
		GroupTransparency = 0,
		Position = UDim2.new(0.5, 0, 1, -94),
	}):Play()
	tweenService:Create(parts.Scale, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = 1}):Play()
end

function siriusValues.closeDelegateAdminPanel()
	local parts = siriusValues.delegateAdminPanelParts
	if not parts or not parts.Root then return end
	local token = (siriusValues.delegateAdminPanelToken or 0) + 1
	siriusValues.delegateAdminPanelToken = token

	tweenService:Create(parts.Root, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {
		GroupTransparency = 1,
		Position = UDim2.new(0.5, 0, 1, -64),
	}):Play()
	tweenService:Create(parts.Scale, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {Scale = 0.92}):Play()

	task.delay(0.24, function()
		if siriusValues.delegateAdminPanelToken == token and parts.Root then
			parts.Root.Visible = false
		end
	end)
end

function siriusValues.ensureDelegateAdminButton()
	local existing = smartBar.Buttons:FindFirstChild("AdminControl")
	if existing then
		return existing
	end

	local template = smartBar.Buttons:FindFirstChild("Music") or smartBar.Buttons:FindFirstChild("Settings") or smartBar.Buttons:FindFirstChildWhichIsA("ImageButton") or smartBar.Buttons:FindFirstChildWhichIsA("TextButton")
	if not template then
		return nil
	end

	local button = template:Clone()
	button.Name = "AdminControl"
	button.Visible = true
	button.Position = UDim2.new(math.min((template.Position.X.Scale or 0.5) + 0.095, 0.58), 0, smartBarOpen and 0.5 or 1.3, 0)
	button.Size = smartBarOpen and UDim2.new(0, 36, 0, 36) or UDim2.new(0, 30, 0, 30)
	button.BackgroundColor3 = Color3.fromRGB(35, 39, 42)
	button.BackgroundTransparency = smartBarOpen and 0 or 1
	button.Parent = smartBar.Buttons

	if button:FindFirstChild("UIGradient") then
		button.UIGradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, Color3.fromRGB(48, 53, 58)),
			ColorSequenceKeypoint.new(1, Color3.fromRGB(24, 27, 30))
		})
	end
	if button:FindFirstChild("UIStroke") and button.UIStroke:FindFirstChild("UIGradient") then
		button.UIStroke.UIGradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, Color3.fromRGB(132, 142, 150)),
			ColorSequenceKeypoint.new(1, Color3.fromRGB(64, 70, 76))
		})
	end
	if button:FindFirstChild("Icon") then
		button.Icon.Image = "rbxassetid://6031280882"
		button.Icon.ImageColor3 = Color3.fromRGB(255, 255, 255)
		button.Icon.ImageTransparency = smartBarOpen and 0 or 1
	end
	if button:FindFirstChild("UIStroke") then
		button.UIStroke.Transparency = smartBarOpen and 0 or 1
	end

	local clickTarget = button:FindFirstChild("Interact") or button
	if clickTarget and clickTarget:IsA("GuiButton") then
		clickTarget.MouseButton1Click:Connect(function()
			if not siriusValues.betaControl.adminEnabled then return end
			local parts = siriusValues.delegateAdminPanelParts
			if parts and parts.Root and parts.Root.Visible then
				siriusValues.closeDelegateAdminPanel()
			else
				siriusValues.openDelegateAdminPanel()
			end
		end)
	end

	button.MouseEnter:Connect(function()
		if button:FindFirstChild("UIStroke") then
			tweenService:Create(button.UIStroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {Transparency = 0.08}):Play()
		end
		if button:FindFirstChild("Icon") then
			tweenService:Create(button.Icon, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {ImageTransparency = 0}):Play()
		end
	end)
	button.MouseLeave:Connect(function()
		if button:FindFirstChild("UIStroke") then
			tweenService:Create(button.UIStroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {Transparency = smartBarOpen and 0 or 1}):Play()
		end
		if button:FindFirstChild("Icon") then
			tweenService:Create(button.Icon, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {ImageTransparency = smartBarOpen and 0 or 1}):Play()
		end
	end)

	return button
end

function siriusValues.syncDelegateAdminUi()
	if siriusValues.betaControl.adminEnabled then
		local button = siriusValues.ensureDelegateAdminButton()
		if button then
			button.Visible = true
		end
		if siriusValues.delegateAdminPanelParts then
			siriusValues.refreshDelegateAdminPanel()
		end
	else
		siriusValues.closeDelegateAdminPanel()
		local button = smartBar.Buttons:FindFirstChild("AdminControl")
		if button then
			button:Destroy()
		end
		siriusValues.betaControl.adminSelectedClientId = nil
	end

	if smartBarOpen then
		tweenService:Create(smartBar, TweenInfo.new(0.24, Enum.EasingStyle.Quint), {Size = siriusValues.getResponsiveSmartBarSize(true)}):Play()
	else
		smartBar.Size = siriusValues.getResponsiveSmartBarSize(false)
	end

	if siriusValues.syncSmartBarCommandSearchLayout then
		siriusValues.syncSmartBarCommandSearchLayout()
	end
end

do
local spamGuard = {
	keywords = {
		{"blox", "pink", "robux"},
		{"blox", "pink", "reward"},
		{"friend", "used", "robux"},
		{"your", "friend", "reward"},
		{"free", "robux", "claim"},
		{"free", "robux", "reward"},
	},
	scamDomains = {
		"blox.pink",
		"blox.land",
		"bloxflip",
		"rbxflip",
		"rbxgold",
		"rbxwild",
		"robuxfree",
		"free-robux",
		"freebobux",
		"discord.gg",
		"discord.com/invite"
	},
	setup = false,
	lastNotify = 0,
	lastMessages = {},
	lastReports = {},
	processedObjects = setmetatable({}, {__mode = "k"}),
}

function spamGuard:notify()
	local spamNotifs = checkSetting("Spam Notifications")
	if spamNotifs and not spamNotifs.current then
		return
	end

	local now = os.clock()
	local cooldownSetting = checkSetting("Chat Safety Alert Cooldown")
	local cooldown = math.clamp(tonumber(cooldownSetting and cooldownSetting.current) or 3, 1, 15)
	if now - self.lastNotify < cooldown then
		return
	end

	self.lastNotify = now
	task.defer(function()
		queueNotification("Chat Spam Blocked", "Filtered a suspected spam bot message.", 4384402990)
	end)
end

function spamGuard:isEnabled()
	local antiSpam = checkSetting("Anti-Spam Bot")
	return antiSpam and antiSpam.current
end

function spamGuard:normalize(message)
	local lower = string.lower(tostring(message or ""))
	lower = lower:gsub("0", "o"):gsub("1", "i"):gsub("3", "e"):gsub("4", "a"):gsub("5", "s"):gsub("7", "t")
	lower = lower:gsub("[%c]", " "):gsub("%s+", " ")
	return lower
end

function spamGuard:parseList(raw)
	local list = {}
	raw = tostring(raw or ""):gsub("[\r\n;|]+", ",")
	for token in string.gmatch(raw, "([^,]+)") do
		token = string.lower(string.gsub(token, "^%s*(.-)%s*$", "%1"))
		if token ~= "" then
			table.insert(list, token)
		end
	end
	return list
end

function spamGuard:matchesCustomKeyword(lower)
	local setting = checkSetting("Custom Blocked Chat Keywords")
	local keywords = self:parseList(setting and setting.current or "")
	if #keywords == 0 then return false end

	local wordMessage = " "..string.gsub(lower, "[%p%c%s]+", " ").." "
	for _, keyword in ipairs(keywords) do
		if #keyword <= 3 and string.match(keyword, "^%w+$") then
			if string.find(wordMessage, " "..keyword.." ", 1, true) then
				return true
			end
		elseif string.find(lower, keyword, 1, true) then
			return true
		end
	end

	return false
end

function spamGuard:hasSuspiciousLink(lower, compact)
	local linkSetting = checkSetting("Block Suspicious Links")
	if linkSetting and not linkSetting.current then return false end

	local hasUrl = string.find(lower, "https?://") or string.find(lower, "www%.") or string.find(lower, "%w+%.%w+")
	for _, domain in ipairs(self.scamDomains) do
		local domainCompact = string.gsub(domain, "[^%w]", "")
		if string.find(lower, domain, 1, true) or string.find(compact, domainCompact, 1, true) then
			return true
		end
	end

	if hasUrl and (string.find(compact, "robux", 1, true) or string.find(compact, "reward", 1, true) or string.find(compact, "giveaway", 1, true) or string.find(compact, "claim", 1, true)) then
		return true
	end

	if string.find(compact, "freerobux", 1, true) or string.find(compact, "claimrobux", 1, true) or string.find(compact, "getrobux", 1, true) then
		return true
	end

	return false
end

function spamGuard:isSpam(message)
	local lower = self:normalize(message)
	if lower == "" then
		return false
	end

	local compact = string.gsub(lower, "[^%w]", "")

	if self:matchesCustomKeyword(lower) then
		return true
	end

	if self:hasSuspiciousLink(lower, compact) then
		return true
	end

	local normalized = lower:gsub("[%p%c]", " "):gsub("%s+", " ")
	local tokens = {}
	for word in string.gmatch(normalized, "%S+") do
		tokens[word] = true
	end

	local function hasAll(words)
		for _, word in ipairs(words) do
			if not tokens[word] then
				return false
			end
		end
		return true
	end

	for _, words in ipairs(self.keywords) do
		if hasAll(words) then
			return true
		end
	end

	return false
end

function spamGuard:isRepeated(playerOrUserId, message)
	local setting = checkSetting("Repeated Message Filter")
	if setting and not setting.current then return false end

	local key = "unknown"
	if typeof(playerOrUserId) == "Instance" and playerOrUserId:IsA("Player") then
		key = tostring(playerOrUserId.UserId)
	elseif playerOrUserId ~= nil then
		key = tostring(playerOrUserId)
	end

	local normalized = self:normalize(message):gsub("[%p%c]", " "):gsub("%s+", " ")
	normalized = string.gsub(normalized, "^%s*(.-)%s*$", "%1")
	if string.len(normalized) < 5 then return false end

	local now = os.clock()
	local last = self.lastMessages[key]
	if last and last.Text == normalized and now - last.Time <= 8 then
		last.Count += 1
		last.Time = now
		return last.Count >= 3
	end

	self.lastMessages[key] = {Text = normalized, Time = now, Count = 1}
	return false
end

function spamGuard:evaluate(playerOrUserId, message)
	if not self:isEnabled() then return false end
	if self:isSpam(message) then return true, "spam" end
	if self:isRepeated(playerOrUserId, message) then return true, "repeat" end
	return false
end

function spamGuard:destroy(object)
	if not object or self.processedObjects[object] then
		return false
	end

	self.processedObjects[object] = true

	pcall(function()
		local parent = object.Parent
		if parent and parent:IsA("Frame") then
			self.processedObjects[parent] = true
			parent.Visible = false
		elseif object:IsA("GuiObject") then
			object.Visible = false
		end
	end)

	return true
end

function spamGuard:inspect(object)
	if not object or not self:isEnabled() then
		return false
	end

	if (object:IsA("TextLabel") or object:IsA("TextButton") or object:IsA("TextBox")) and self:isSpam(object.Text or "") then
		return self:destroy(object)
	end

	for _, descendant in ipairs(object:GetDescendants()) do
		if (descendant:IsA("TextLabel") or descendant:IsA("TextButton") or descendant:IsA("TextBox")) and self:isSpam(descendant.Text or "") then
			if self:destroy(descendant) then
				return true
			end
		end
	end

	return false
end

function spamGuard:scrub()
	if not self:isEnabled() then
		return
	end

	local chatGuis = {}
	local legacyChat = coreGui:FindFirstChild("Chat")
	local experienceChat = coreGui:FindFirstChild("ExperienceChat")
	if legacyChat then table.insert(chatGuis, legacyChat) end
	if experienceChat then table.insert(chatGuis, experienceChat) end

	for _, chatGui in ipairs(chatGuis) do
		for _, descendant in ipairs(chatGui:GetDescendants()) do
			if (descendant:IsA("TextLabel") or descendant:IsA("TextButton") or descendant:IsA("TextBox")) and self:isSpam(descendant.Text or "") then
				self:destroy(descendant)
			end
		end
	end
end

function spamGuard:report(userId)
	local reportSetting = checkSetting("Auto-Report Spam")
	if not reportSetting or not reportSetting.current or not userId then
		return
	end

	local target = players:GetPlayerByUserId(userId)
	if not target or target == localPlayer then
		return
	end

	local now = os.clock()
	if self.lastReports[target.UserId] and now - self.lastReports[target.UserId] < 45 then
		return
	end
	self.lastReports[target.UserId] = now

	pcall(function()
		players:ReportAbuse(target, "Scamming", "Auto-report: spam/scam message detected.")
	end)
end

function spamGuard:setupScrubber()
	if self.setup then
		return
	end

	self.setup = true

	local function hookChatGui(chatGui)
		if not chatGui then
			return
		end

		chatGui.DescendantAdded:Connect(function(descendant)
			if not self:isEnabled() then
				return
			end

			if descendant:IsA("TextLabel") or descendant:IsA("TextButton") or descendant:IsA("TextBox") then
				task.defer(function()
					if self:isSpam(descendant.Text or "") and self:destroy(descendant) then
						self:notify()
					end
				end)
			end
		end)
	end

	task.spawn(function()
		hookChatGui(coreGui:WaitForChild("Chat", 5))
	end)

	task.spawn(function()
		hookChatGui(coreGui:WaitForChild("ExperienceChat", 5))
	end)

	task.defer(function()
		self:scrub()
	end)
end

task.spawn(function()
	spamGuard:setupScrubber()
end)

if getMessage then
	getMessage.OnClientEvent:Connect(function(packet)
		if not spamGuard:isEnabled() then
			return
		end

		local text = (packet and packet.Message) or ""
		local userId = packet and (packet.SpeakerUserId or packet.FromSpeakerUserId or packet.UserId)
		if not userId and packet and packet.FromSpeaker then
			local speaker = players:FindFirstChild(packet.FromSpeaker)
			userId = speaker and speaker.UserId
		end

		local flagged, reason = spamGuard:evaluate(userId, text)
		if text == "" or not flagged then
			return
		end

		if reason ~= "repeat" then
			spamGuard:report(userId)
		end
		spamGuard:notify()
		task.spawn(function()
			spamGuard:scrub()
		end)
	end)
end

do
	local textChatService = game:FindFirstChildOfClass("TextChatService")
	if textChatService then
		textChatService.MessageReceived:Connect(function(message)
			if not spamGuard:isEnabled() or not message then
				return
			end

			local text = message.Text or ""
			local textSource = message.TextSource
			local flagged, reason = spamGuard:evaluate(textSource and textSource.UserId, text)
			if not flagged then
				return
			end

			if reason ~= "repeat" then
				spamGuard:report(textSource and textSource.UserId)
			end
			spamGuard:notify()
			task.spawn(function()
				spamGuard:scrub()
			end)
		end)
	end
end

siriusValues.spamGuard = spamGuard

end

do
	local playerActionPicker
	local playerActionPickerParts = {}
	local openPlayerActionPicker
	local stopSpectating
	local playerActionPickerState = {
			open = false,
			token = 0,
			targetPlayer = nil,
			selectedSpectate = false,
			selectedTrack = false,
			selectedLiveTrack = false,
			selectedTracer = false,
			selectedHide = false,
			selectedEsp = false,
			restorePanel = nil
		}
	local playerActionPickerDefaultSize = Vector2.new(390, 520)
	local playerActionPickerMinSize = Vector2.new(360, 360)
	local playerActionPickerMaxSize = Vector2.new(440, 540)
	local playerActionPickerCurrentSize = playerActionPickerDefaultSize
	siriusValues.selectActionStyle = {
		panel = Color3.fromRGB(18, 18, 18),
		panelDepth = Color3.fromRGB(14, 14, 14),
		row = Color3.fromRGB(23, 23, 23),
		rowHover = Color3.fromRGB(29, 29, 29),
		icon = Color3.fromRGB(28, 28, 28),
		iconHover = Color3.fromRGB(35, 35, 35),
		stroke = Color3.fromRGB(54, 54, 54),
		strokeHover = Color3.fromRGB(74, 74, 74),
		switch = Color3.fromRGB(28, 28, 28),
		switchActive = Color3.fromRGB(54, 54, 54),
		switchStroke = Color3.fromRGB(64, 64, 64),
		switchStrokeActive = Color3.fromRGB(230, 230, 230),
		text = Color3.fromRGB(242, 242, 242),
		muted = Color3.fromRGB(145, 145, 145),
		panelCorner = 14,
		rowCorner = 12,
		iconCorner = 8
	}
	local selectActionIconUrls = {
		Spectate = "https://siriusx.vercel.app/icons/select-actions-central/spectate.png",
		Track = "https://siriusx.vercel.app/icons/select-actions-central/track.png",
		LiveTrack = "https://siriusx.vercel.app/icons/select-actions-central/live-track.png",
		Tracer = "https://siriusx.vercel.app/icons/select-actions-central/tracer.png",
		Hide = "https://siriusx.vercel.app/icons/select-actions-central/hide.png",
		ESP = "https://siriusx.vercel.app/icons/select-actions-central/esp.png",
		Teleport = "https://siriusx.vercel.app/icons/select-actions-central/teleport.png",
		FlingPlayer = "https://siriusx.vercel.app/icons/select-actions-central/fling-player.png",
		Fling2 = "https://siriusx.vercel.app/icons/select-actions-central/fling-2.png",
		PlatformFly = "https://siriusx.vercel.app/icons/select-actions-central/platform-fly.png",
		MagicCarpet = "https://siriusx.vercel.app/icons/select-actions-central/magic-carpet.png",
		Elevator = "https://siriusx.vercel.app/icons/select-actions-central/elevator.png",
		AntiJump = "https://siriusx.vercel.app/icons/select-actions-central/anti-jump.png",
		InfiniteJump = "https://siriusx.vercel.app/icons/select-actions-central/infinite-jump.png",
		SpeedSlow = "https://siriusx.vercel.app/icons/select-actions-central/speed-slow.png",
		SpeedMedium = "https://siriusx.vercel.app/icons/select-actions-central/speed-medium.png",
		SpeedFast = "https://siriusx.vercel.app/icons/select-actions-central/speed-fast.png",
		MovementBreaker = "https://siriusx.vercel.app/icons/select-actions-central/movement-breaker.png",
		RocketTarget = "https://siriusx.vercel.app/icons/select-actions-central/rocket-target.png",
		GodspeedRocket = "https://siriusx.vercel.app/icons/select-actions-central/godspeed-rocket.png"
	}
	local selectActionIconFiles = {
		Spectate = "select-actions-central-spectate-filled.png",
		Track = "select-actions-central-track-filled.png",
		LiveTrack = "select-actions-central-live-track-filled.png",
		Tracer = "select-actions-central-tracer-filled.png",
		Hide = "select-actions-central-hide-filled.png",
		ESP = "select-actions-central-esp-filled.png",
		Teleport = "select-actions-central-teleport-filled.png",
		FlingPlayer = "select-actions-central-fling-player-filled.png",
		Fling2 = "select-actions-central-fling-2-filled.png",
		PlatformFly = "select-actions-central-platform-fly-filled.png",
		MagicCarpet = "select-actions-central-magic-carpet-filled.png",
		Elevator = "select-actions-central-elevator-filled.png",
		AntiJump = "select-actions-central-anti-jump-filled.png",
		InfiniteJump = "select-actions-central-infinite-jump-filled.png",
		SpeedSlow = "select-actions-central-speed-slow-filled.png",
		SpeedMedium = "select-actions-central-speed-medium-filled.png",
		SpeedFast = "select-actions-central-speed-fast-filled.png",
		MovementBreaker = "select-actions-central-movement-breaker-filled.png",
		RocketTarget = "select-actions-central-rocket-target-filled.png",
		GodspeedRocket = "select-actions-central-godspeed-rocket-filled.png"
	}
	local selectActionIconCache = {}
	local spectateBar
	local spectateBarParts = {}
	local spectateToggleSize = Vector2.new(248, 48)
	local spectateMorphState = {
		token = 0,
		active = false,
		tweens = {},
		ghost = nil,
		parts = nil
	}
	function siriusValues.getPlayerActionPickerRootSize()
		local rootSize = camera and camera.ViewportSize or Vector2.new(1280, 720)
		pcall(function()
			if UI.AbsoluteSize.X > 0 and UI.AbsoluteSize.Y > 0 then
				rootSize = UI.AbsoluteSize
			end
		end)
		return rootSize
	end
	local function clampPlayerActionPickerSize(size)
		local rootSize = siriusValues.getPlayerActionPickerRootSize()
		local availableWidth = math.max(300, rootSize.X - 40)
		local availableHeight = math.max(320, rootSize.Y - 72)
		local minWidth = math.min(playerActionPickerMinSize.X, availableWidth)
		local minHeight = math.min(playerActionPickerMinSize.Y, availableHeight)
		local maxWidth = math.max(minWidth, math.min(playerActionPickerMaxSize.X, availableWidth))
		local maxHeight = math.max(minHeight, math.min(playerActionPickerMaxSize.Y, availableHeight))

		return Vector2.new(
			math.clamp(size.X, minWidth, maxWidth),
			math.clamp(size.Y, minHeight, maxHeight)
		)
	end
	local function offsetUDim2(position, xOffset, yOffset)
		return UDim2.new(
			position.X.Scale,
			position.X.Offset + (xOffset or 0),
			position.Y.Scale,
			position.Y.Offset + (yOffset or 0)
		)
	end
	local function getSpectateTogglePickerGeometry()
		return UDim2.new(0.5, 0, 0, 18 + spectateToggleSize.Y * 0.5), spectateToggleSize
	end
	local function getPlayerActionPickerGeometry()
		playerActionPickerCurrentSize = clampPlayerActionPickerSize(playerActionPickerCurrentSize)
		return UDim2.new(0.5, 0, 0.5, 0), playerActionPickerCurrentSize
	end
	local function getSelectActionIcon(iconName)
		if selectActionIconCache[iconName] then
			return selectActionIconCache[iconName]
		end

		local url = selectActionIconUrls[iconName] or ""
		local resolvedIcon = url
		pcall(function()
			if typeof(getcustomasset) ~= "function" or typeof(writefile) ~= "function" or typeof(game.HttpGet) ~= "function" then return end

			local folder = "SIRIUSXIcons"
			if typeof(makefolder) == "function" then
				if typeof(isfolder) == "function" then
					if not isfolder(folder) then
						makefolder(folder)
					end
				else
					pcall(makefolder, folder)
				end
			end

			local filePath = folder.."/"..(selectActionIconFiles[iconName] or (iconName..".png"))
			if typeof(isfile) ~= "function" or not isfile(filePath) then
				writefile(filePath, game:HttpGet(url))
			end

			resolvedIcon = getcustomasset(filePath)
		end)

		selectActionIconCache[iconName] = resolvedIcon
		return resolvedIcon
	end
	function siriusValues.addSelectActionCorner(parent, radius, scale)
		local corner = Instance.new("UICorner")
		corner.CornerRadius = scale and UDim.new(scale, 0) or UDim.new(0, radius or siriusValues.selectActionStyle.rowCorner)
		corner.Parent = parent
		return corner
	end
	function siriusValues.addSelectActionStroke(parent, color, transparency, name)
		local stroke = Instance.new("UIStroke")
		stroke.Name = name or "stroke"
		stroke.Color = color or siriusValues.selectActionStyle.stroke
		stroke.Thickness = 1
		stroke.Transparency = transparency == nil and 0.18 or transparency
		stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
		stroke.Parent = parent
		return stroke
	end
	local function resetSpectateBarGeometry()
		if not spectateBar then return end

		spectateBar.Size = UDim2.fromOffset(spectateToggleSize.X, spectateToggleSize.Y)
		spectateBar.Position = UDim2.new(0.5, 0, 0, 18)
	end

	local function cancelSpectateMorphTweens()
		for _, tween in ipairs(spectateMorphState.tweens) do
			pcall(function()
				tween:Cancel()
			end)
		end
		spectateMorphState.tweens = {}
	end

	local function playSpectateMorphTween(object, info, properties)
		local tween = tweenService:Create(object, info, properties)
		table.insert(spectateMorphState.tweens, tween)
		tween:Play()
		return tween
	end

	local function ensureSpectateMorphGhost()
		if spectateMorphState.ghost and spectateMorphState.ghost.Parent then
			return spectateMorphState.parts
		end

		local ghost = Instance.new("CanvasGroup")
		ghost.Name = "SiriusSpectateMorphGhost"
		ghost.AnchorPoint = Vector2.new(0, 0)
		ghost.Position = UDim2.fromScale(0, 0)
		ghost.Size = UDim2.fromScale(1, 1)
		ghost.BackgroundTransparency = 1
		ghost.GroupTransparency = 1
		ghost.Visible = false
		ghost.ZIndex = 220
		ghost.Parent = UI

		local shadow = Instance.new("ImageLabel")
		shadow.Name = "Shadow"
		shadow.AnchorPoint = Vector2.new(0.5, 0.5)
		shadow.BackgroundTransparency = 1
		shadow.Image = "rbxassetid://6015897843"
		shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
		shadow.ImageTransparency = 1
		shadow.ScaleType = Enum.ScaleType.Slice
		shadow.SliceCenter = Rect.new(49, 49, 450, 450)
		shadow.ZIndex = 220
		shadow.Parent = ghost

		local shell = Instance.new("Frame")
		shell.Name = "Shell"
		shell.AnchorPoint = Vector2.new(0.5, 0.5)
		shell.BackgroundColor3 = siriusValues.selectActionStyle.panel
		shell.BackgroundTransparency = 0
		shell.ClipsDescendants = true
		shell.ZIndex = 221
		shell.Parent = ghost
		siriusValues.addSelectActionCorner(shell, siriusValues.selectActionStyle.panelCorner)
		local stroke = Instance.new("UIStroke")
		stroke.Color = siriusValues.selectActionStyle.stroke
		stroke.Transparency = 0.32
		stroke.Thickness = 1
		stroke.Parent = shell
		local gradient = Instance.new("UIGradient")
		gradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, Color3.fromRGB(27, 27, 27)),
			ColorSequenceKeypoint.new(0.7, Color3.fromRGB(8, 8, 8)),
			ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0))
		})
		gradient.Rotation = 96
		gradient.Parent = shell

		spectateMorphState.ghost = ghost
		spectateMorphState.parts = {
			Root = ghost,
			Shadow = shadow,
			Shell = shell,
			Stroke = stroke
		}

		return spectateMorphState.parts
	end

	local function applySpectateMorphGeometry(parts, position, size, shadowExtra, shadowYOffset)
		local extra = shadowExtra or 54
		local yOffset = shadowYOffset or 12
		parts.Shell.Position = position
		parts.Shell.Size = UDim2.fromOffset(size.X, size.Y)
		parts.Shadow.Position = offsetUDim2(position, 0, yOffset)
		parts.Shadow.Size = UDim2.fromOffset(size.X + extra, size.Y + extra)
	end

	local function playSpectateGhostMorph(fromPosition, fromSize, toPosition, toSize, duration, style, onComplete)
		cancelSpectateMorphTweens()
		spectateMorphState.token += 1
		local token = spectateMorphState.token
		spectateMorphState.active = true

		local parts = ensureSpectateMorphGhost()
		local toToggle = style == "to-toggle"
		parts.Root.Visible = true
		parts.Root.GroupTransparency = 0
		parts.Shell.BackgroundColor3 = siriusValues.selectActionStyle.panel
		parts.Shell.BackgroundTransparency = toToggle and 0 or 0.55
		parts.Stroke.Transparency = toToggle and 0.34 or 0.72
		parts.Shadow.ImageTransparency = toToggle and 0.82 or 0.95
		applySpectateMorphGeometry(parts, fromPosition, fromSize, toToggle and 58 or 34, toToggle and 12 or 6)

		local info = TweenInfo.new(duration, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut)
		playSpectateMorphTween(parts.Shell, info, {
			Position = toPosition,
			Size = UDim2.fromOffset(toSize.X, toSize.Y),
			BackgroundTransparency = toToggle and 0.55 or 0
		})
		playSpectateMorphTween(parts.Shadow, info, {
			Position = offsetUDim2(toPosition, 0, toToggle and 6 or 12),
			Size = UDim2.fromOffset(toSize.X + (toToggle and 34 or 58), toSize.Y + (toToggle and 34 or 58)),
			ImageTransparency = toToggle and 0.95 or 0.82
		})
		playSpectateMorphTween(parts.Stroke, info, {
			Transparency = toToggle and 0.72 or 0.34
		})

		task.delay(duration + 0.02, function()
			if token ~= spectateMorphState.token then return end
			parts.Root.Visible = false
			parts.Root.GroupTransparency = 1
			spectateMorphState.active = false
			if onComplete then
				onComplete(token)
			end
		end)

		return token
	end
	local spectateCameraToken = 0
	local spectateCameraFovTween
	local spectateState = {
		active = false,
		targetPlayer = nil,
		token = 0,
		characterConnection = nil,
		removingConnection = nil,
		restoreFov = nil
	}
	local trackingFolder
	local trackingBadges = {}
	local trackingConnections = {}
	local playerTracers = {}
	local tracerRenderConnection
	local tracerRemovingConnection
	local tracerUnavailableNotified = false
	local hiddenPlayers = {}
	local profileInfoTools = {SessionStarts = {}}
	for _, player in ipairs(players:GetPlayers()) do
		profileInfoTools.SessionStarts[player] = os.clock()
	end
	players.PlayerAdded:Connect(function(player)
		profileInfoTools.SessionStarts[player] = os.clock()
	end)
	players.PlayerRemoving:Connect(function(player)
		profileInfoTools.SessionStarts[player] = nil
	end)

	function profileInfoTools.FormatDuration(seconds)
		seconds = math.max(0, math.floor(seconds or 0))
		if seconds < 60 then
			return "Just Joined"
		end

		local minutes = math.floor(seconds / 60)
		if minutes < 60 then
			return tostring(minutes).."m"
		end

		local hours = math.floor(minutes / 60)
		minutes = minutes % 60
		return tostring(hours).."h "..tostring(minutes).."m"
	end

	function profileInfoTools.FormatAccountAge(days)
		days = math.max(0, math.floor(days or 0))
		if days >= 365 then
			local years = math.floor(days / 365)
			local months = math.floor((days % 365) / 30)
			return tostring(years).."y "..tostring(months).."mos"
		elseif days >= 30 then
			return tostring(math.floor(days / 30)).."mos"
		end

		return tostring(days).."d"
	end

	function profileInfoTools.GetRole(targetPlayer)
		local role
		pcall(function()
			if game.CreatorType == Enum.CreatorType.User and targetPlayer.UserId == game.CreatorId then
				role = "Owner"
			elseif game.CreatorType == Enum.CreatorType.Group then
				local groupRole = targetPlayer:GetRoleInGroup(game.CreatorId)
				if groupRole and groupRole ~= "" and groupRole ~= "Guest" then
					role = groupRole
				end
			end
		end)

		if role then return role end
		if targetPlayer.Team then
			return targetPlayer.Team.Name
		end
		return "Neutral"
	end

	function profileInfoTools.Refresh(parts, targetPlayer)
		if not parts or not targetPlayer then return end

		profileInfoTools.SessionStarts[targetPlayer] = profileInfoTools.SessionStarts[targetPlayer] or os.clock()

		if parts.ProfileCard then
			parts.ProfileCard.BackgroundColor3 = siriusValues.selectActionStyle.panel
			parts.ProfileCard.BackgroundTransparency = 1
		end
		if parts.ProfileStroke then
			parts.ProfileStroke.Color = siriusValues.selectActionStyle.stroke
			parts.ProfileStroke.Transparency = 0.72
		end
		if parts.ProfileAvatar then
			parts.ProfileAvatar.Image = "https://www.roblox.com/headshot-thumbnail/image?userId="..targetPlayer.UserId.."&width=420&height=420&format=png"
			parts.ProfileAvatar.ImageTransparency = 0
		end
		if parts.ProfileAvatarStroke then
			parts.ProfileAvatarStroke.Transparency = 0.48
		end
		if parts.ProfileName then
			parts.ProfileName.Text = targetPlayer.DisplayName
			parts.ProfileName.TextTransparency = 0
		end
		if parts.ProfileHandle then
			parts.ProfileHandle.Text = "@"..targetPlayer.Name
			parts.ProfileHandle.TextTransparency = 0
		end
		if parts.ProfilePlaytimeValue then
			parts.ProfilePlaytimeValue.Text = profileInfoTools.FormatDuration(os.clock() - profileInfoTools.SessionStarts[targetPlayer])
		end
		if parts.ProfileAccountAgeValue then
			parts.ProfileAccountAgeValue.Text = profileInfoTools.FormatAccountAge(targetPlayer.AccountAge)
		end
		if parts.ProfileRoleValue then
			parts.ProfileRoleValue.Text = profileInfoTools.GetRole(targetPlayer)
		end
	end
	local liveTrackPanel
	local liveTrackParts = {}
	local liveTrackState = {
		active = false,
		targetPlayer = nil,
		token = 0,
		renderConnection = nil,
		characterConnection = nil,
		removingConnection = nil,
		returnToPicker = false
	}
	local liveTrackDefaultSize = Vector2.new(300, 500)
	local liveTrackMinSize = Vector2.new(280, 420)
		local liveTrackMaxSize = Vector2.new(320, 560)
		local liveTrackCurrentSize = liveTrackDefaultSize
		local liveTrackCurrentPosition = UDim2.new(0.5, 0, 0.5, 0)

		local function getLiveTrackShadowPosition(position, yOffset)
			return UDim2.new(position.X.Scale, position.X.Offset, position.Y.Scale, position.Y.Offset + (yOffset or 20))
		end

		local function setLiveTrackPosition(position, shadowOffset)
			liveTrackCurrentPosition = position
			if liveTrackPanel then
				liveTrackPanel.Position = position
			end
			if liveTrackParts.OuterShadow then
				liveTrackParts.OuterShadow.Position = getLiveTrackShadowPosition(position, shadowOffset or 20)
			end
		end

		local function clampLiveTrackSize(size)
			return Vector2.new(
				math.clamp(size.X, liveTrackMinSize.X, liveTrackMaxSize.X),
				math.clamp(size.Y, liveTrackMinSize.Y, liveTrackMaxSize.Y)
		)
	end

	local function getLiveTrackShadowSize(size)
		return Vector2.new(size.X + 56, size.Y + 66)
	end

	local function applyLiveTrackSize(size, animated)
		size = clampLiveTrackSize(size)
		liveTrackCurrentSize = size

		if liveTrackPanel then
			local panelGoal = {Size = UDim2.fromOffset(size.X, size.Y)}
			if animated then
				tweenService:Create(liveTrackPanel, TweenInfo.new(0.14, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), panelGoal):Play()
			else
				liveTrackPanel.Size = panelGoal.Size
			end
		end

		if liveTrackParts.OuterShadow then
			local shadowSize = getLiveTrackShadowSize(size)
			local shadowGoal = {Size = UDim2.fromOffset(shadowSize.X, shadowSize.Y)}
			if animated then
				tweenService:Create(liveTrackParts.OuterShadow, TweenInfo.new(0.14, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), shadowGoal):Play()
			else
				liveTrackParts.OuterShadow.Size = shadowGoal.Size
			end
		end
	end

local function setPlayerActionPickerTransparency(value)
	if not playerActionPicker then return end

	for _, obj in ipairs(playerActionPicker:GetDescendants()) do
		if obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("TextBox") then
			obj.TextTransparency = value
		elseif obj:IsA("ImageLabel") or obj:IsA("ImageButton") then
			obj.ImageTransparency = value
		end

		if obj:IsA("UIStroke") then
			obj.Transparency = value
		end
	end
end

local function getSpectateHumanoid(player)
	if not player or not players:FindFirstChild(player.Name) then return nil end

	local character = player.Character or workspace:FindFirstChild(player.Name)
	if not character then return nil end

	return character:FindFirstChildOfClass("Humanoid") or character:FindFirstChild("Humanoid")
end

local function smoothlySetCameraSubject(humanoid, duration, targetFov)
	if not humanoid or not humanoid.Parent then return end

	local root = humanoid.Parent:FindFirstChild("HumanoidRootPart") or humanoid.Parent:FindFirstChild("Head") or humanoid.Parent.PrimaryPart
	if not root then
		camera.CameraType = Enum.CameraType.Custom
		camera.CameraSubject = humanoid
		if targetFov then
			if spectateCameraFovTween then spectateCameraFovTween:Cancel() end
			spectateCameraFovTween = tweenService:Create(camera, TweenInfo.new(duration or 0.45, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {FieldOfView = targetFov})
			spectateCameraFovTween:Play()
		end
		return
	end

	spectateCameraToken += 1
	local token = spectateCameraToken
	local startCFrame = camera.CFrame
	local lookVector = startCFrame.LookVector
	if lookVector.Magnitude < 0.1 then
		lookVector = Vector3.new(0, 0, -1)
	end
	lookVector = lookVector.Unit

	local focus = root.Position + Vector3.new(0, 2.35, 0)
	local distance = math.clamp((startCFrame.Position - focus).Magnitude, 10, 16)
	local travelTime = duration or 0.68
	local started = os.clock()
	local useZoomDistance = targetFov and targetFov < camera.FieldOfView

	camera.CameraType = Enum.CameraType.Scriptable
	if targetFov then
		if spectateCameraFovTween then spectateCameraFovTween:Cancel() end
		spectateCameraFovTween = tweenService:Create(camera, TweenInfo.new(travelTime, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {FieldOfView = targetFov})
		spectateCameraFovTween:Play()
	end

	task.spawn(function()
		while token == spectateCameraToken and humanoid and humanoid.Parent do
			local elapsed = os.clock() - started
			local alpha = math.clamp(elapsed / travelTime, 0, 1)
			local eased = 0.5 - math.cos(alpha * math.pi) * 0.5
			local currentRoot = humanoid.Parent:FindFirstChild("HumanoidRootPart") or humanoid.Parent:FindFirstChild("Head") or humanoid.Parent.PrimaryPart
			if not currentRoot then break end

			local currentFocus = currentRoot.Position + Vector3.new(0, 2.35, 0)
			local goalDistance = useZoomDistance and math.max(8.5, distance - 2) or distance
			local goal = CFrame.new(currentFocus - (lookVector * goalDistance), currentFocus)
			camera.CFrame = startCFrame:Lerp(goal, eased)

			if alpha >= 1 then
				break
			end

			runService.RenderStepped:Wait()
		end

		if token ~= spectateCameraToken then return end
		if humanoid and humanoid.Parent then
			camera.CameraSubject = humanoid
			runService.RenderStepped:Wait()
			if token ~= spectateCameraToken then return end
			camera.CameraType = Enum.CameraType.Custom
		end
	end)
end

local function restoreLocalCamera()
	local humanoid = getSpectateHumanoid(localPlayer)
	if humanoid then
		smoothlySetCameraSubject(humanoid, 0.5, spectateState.restoreFov or camera.FieldOfView)
	end
end

local function smoothlySpectateHumanoid(humanoid)
	local targetFov = math.max(58, math.min(68, (spectateState.restoreFov or camera.FieldOfView) - 4))
	smoothlySetCameraSubject(humanoid, 0.56, targetFov)
end

local function ensureSpectateBar()
	if spectateBar then return spectateBar end

	spectateBar = Instance.new("CanvasGroup")
	spectateBar.Name = "SiriusSpectateControl"
	spectateBar.AnchorPoint = Vector2.new(0.5, 0)
		spectateBar.Position = UDim2.new(0.5, 0, 0, 18)
		spectateBar.Size = UDim2.fromOffset(spectateToggleSize.X, spectateToggleSize.Y)
		spectateBar.BackgroundColor3 = Color3.fromRGB(13, 13, 13)
		spectateBar.BackgroundTransparency = 1
	spectateBar.BorderSizePixel = 0
	spectateBar.GroupTransparency = 1
	spectateBar.Visible = false
	spectateBar.ZIndex = 75
	spectateBar.Parent = UI

	local barScale = Instance.new("UIScale")
	barScale.Name = "Scale"
	barScale.Scale = 1
	barScale.Parent = spectateBar

		local shadow = Instance.new("ImageLabel")
		shadow.Name = "Shadow"
		shadow.AnchorPoint = Vector2.new(0.5, 0.5)
		shadow.Position = UDim2.new(0.5, 0, 0.5, 5)
		shadow.Size = UDim2.new(1, 34, 1, 26)
	shadow.BackgroundTransparency = 1
	shadow.Image = "rbxassetid://8992230677"
	shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
	shadow.ImageTransparency = 1
	shadow.ScaleType = Enum.ScaleType.Slice
	shadow.SliceCenter = Rect.new(99, 99, 99, 99)
		shadow.ZIndex = 74
		shadow.Parent = spectateBar

		local mainPill = Instance.new("Frame")
		mainPill.Name = "MainPill"
		mainPill.Position = UDim2.new(0, 0, 0, 0)
		mainPill.Size = UDim2.new(1, -50, 1, 0)
		mainPill.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
		mainPill.BackgroundTransparency = 0.64
		mainPill.BorderSizePixel = 0
		mainPill.ZIndex = 75
		mainPill.Parent = spectateBar

		local mainPillCorner = Instance.new("UICorner")
		mainPillCorner.CornerRadius = UDim.new(1, 0)
		mainPillCorner.Parent = mainPill

		local mainPillStroke = Instance.new("UIStroke")
		mainPillStroke.Name = "Stroke"
		mainPillStroke.Color = Color3.fromRGB(115, 120, 122)
		mainPillStroke.Thickness = 1
		mainPillStroke.Transparency = 0.74
		mainPillStroke.Parent = mainPill

		local mainPillGradient = Instance.new("UIGradient")
		mainPillGradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, Color3.fromRGB(26, 27, 28)),
			ColorSequenceKeypoint.new(0.55, Color3.fromRGB(10, 10, 10)),
			ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0))
		})
		mainPillGradient.Transparency = NumberSequence.new({
			NumberSequenceKeypoint.new(0, 0.12),
			NumberSequenceKeypoint.new(0.55, 0.04),
			NumberSequenceKeypoint.new(1, 0.22)
		})
		mainPillGradient.Rotation = 18
		mainPillGradient.Parent = mainPill

		local corner = Instance.new("UICorner")
		corner.CornerRadius = UDim.new(1, 0)
	corner.Parent = spectateBar

	local stroke = Instance.new("UIStroke")
		stroke.Name = "Stroke"
		stroke.Color = Color3.fromRGB(64, 64, 64)
		stroke.Thickness = 1
		stroke.Transparency = 1
	stroke.Parent = spectateBar

	local gradient = Instance.new("UIGradient")
	gradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(18, 18, 18)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(4, 4, 4))
	})
	gradient.Rotation = 18
	gradient.Parent = spectateBar

	local avatar = Instance.new("ImageLabel")
		avatar.Name = "Avatar"
		avatar.AnchorPoint = Vector2.new(0, 0.5)
		avatar.Position = UDim2.new(0, 14, 0.5, 0)
			avatar.Size = UDim2.new(0, 34, 0, 34)
	avatar.BackgroundTransparency = 1
	avatar.ImageTransparency = 1
	avatar.ZIndex = 76
	avatar.Parent = spectateBar

	local avatarCorner = Instance.new("UICorner")
	avatarCorner.CornerRadius = UDim.new(1, 0)
	avatarCorner.Parent = avatar

		local avatarStroke = Instance.new("UIStroke")
		avatarStroke.Color = Color3.fromRGB(80, 80, 80)
		avatarStroke.Transparency = 1
		avatarStroke.Thickness = 1
	avatarStroke.Parent = avatar

		local eyebrow = Instance.new("TextLabel")
		eyebrow.Name = "Eyebrow"
		eyebrow.Position = UDim2.new(0, 112, 0, 12)
		eyebrow.Size = UDim2.new(0, 86, 0, 14)
	eyebrow.BackgroundTransparency = 1
	eyebrow.Text = "SPECTATING"
	eyebrow.TextColor3 = Color3.fromRGB(150, 150, 150)
	eyebrow.TextTransparency = 1
	eyebrow.TextXAlignment = Enum.TextXAlignment.Left
	eyebrow.TextSize = 9
	eyebrow.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	eyebrow.ZIndex = 76
	eyebrow.Parent = spectateBar

		local name = Instance.new("TextLabel")
			name.Name = "TargetName"
			name.AnchorPoint = Vector2.new(0, 0.5)
			name.Position = UDim2.new(0, 60, 0.5, 0)
			name.Size = UDim2.new(0, 94, 0, 28)
	name.BackgroundTransparency = 1
	name.Text = "Player"
	name.TextColor3 = Color3.fromRGB(235, 235, 235)
	name.TextTransparency = 1
	name.TextXAlignment = Enum.TextXAlignment.Left
				name.TextSize = 20
	name.TextTruncate = Enum.TextTruncate.AtEnd
	name.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	name.ZIndex = 76
	name.Parent = spectateBar

	local statusPill = Instance.new("Frame")
		statusPill.Name = "Status"
		statusPill.AnchorPoint = Vector2.new(0, 0.5)
				statusPill.Position = UDim2.new(0, 156, 0.5, 0)
				statusPill.Size = UDim2.new(0, 30, 0, 30)
	statusPill.BackgroundColor3 = Color3.fromRGB(18, 22, 25)
	statusPill.BackgroundTransparency = 1
	statusPill.BorderSizePixel = 0
	statusPill.ZIndex = 76
	statusPill.Parent = spectateBar

	local statusCorner = Instance.new("UICorner")
	statusCorner.CornerRadius = UDim.new(1, 0)
	statusCorner.Parent = statusPill

	local statusStroke = Instance.new("UIStroke")
	statusStroke.Color = Color3.fromRGB(45, 55, 60)
	statusStroke.Thickness = 1
	statusStroke.Transparency = 1
	statusStroke.Parent = statusPill

	local caret = Instance.new("TextLabel")
	caret.Name = "Caret"
	caret.AnchorPoint = Vector2.new(0.5, 0.5)
	caret.Position = UDim2.new(0.5, 0, 0.5, 0)
	caret.Size = UDim2.new(0, 0, 0, 0)
	caret.BackgroundTransparency = 1
	caret.Text = ""
	caret.TextColor3 = Color3.fromRGB(235, 235, 235)
	caret.TextTransparency = 1
	caret.TextSize = 22
	caret.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	caret.ZIndex = 77
	caret.Parent = statusPill

	local liveGlow = Instance.new("ImageLabel")
		liveGlow.Name = "LiveGlow"
		liveGlow.AnchorPoint = Vector2.new(0.5, 0.5)
		liveGlow.Position = UDim2.new(0.5, 0, 0.5, 0)
				liveGlow.Size = UDim2.new(0, 32, 0, 32)
	liveGlow.BackgroundTransparency = 1
	liveGlow.Image = "rbxassetid://8992230677"
	liveGlow.ImageColor3 = Color3.fromRGB(255, 36, 36)
	liveGlow.ImageTransparency = 1
	liveGlow.ScaleType = Enum.ScaleType.Slice
	liveGlow.SliceCenter = Rect.new(99, 99, 99, 99)
	liveGlow.ZIndex = 76
	liveGlow.Parent = statusPill

	local liveDot = Instance.new("Frame")
		liveDot.Name = "LiveDot"
		liveDot.AnchorPoint = Vector2.new(0.5, 0.5)
		liveDot.Position = UDim2.new(0.5, 0, 0.5, 0)
				liveDot.Size = UDim2.new(0, 10, 0, 10)
	liveDot.BackgroundColor3 = Color3.fromRGB(255, 33, 33)
	liveDot.BackgroundTransparency = 1
	liveDot.BorderSizePixel = 0
	liveDot.ZIndex = 77
	liveDot.Parent = statusPill

	local liveDotCorner = Instance.new("UICorner")
	liveDotCorner.CornerRadius = UDim.new(1, 0)
	liveDotCorner.Parent = liveDot

		local close = Instance.new("TextButton")
		close.Name = "Close"
		close.AnchorPoint = Vector2.new(1, 0.5)
		close.Position = UDim2.new(1, -1, 0.5, 0)
			close.Size = UDim2.new(0, 40, 0, 40)
		close.BackgroundColor3 = Color3.fromRGB(16, 19, 21)
			close.BackgroundTransparency = 0.48
	close.BorderSizePixel = 0
		close.Text = "×"
		close.TextColor3 = Color3.fromRGB(238, 238, 238)
		close.TextTransparency = 0.12
			close.TextSize = 23
	close.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium)
	close.ZIndex = 76
	close.Parent = spectateBar

		local closeCorner = Instance.new("UICorner")
		closeCorner.CornerRadius = UDim.new(1, 0)
		closeCorner.Parent = close

		local closeStroke = Instance.new("UIStroke")
		closeStroke.Color = Color3.fromRGB(120, 126, 128)
			closeStroke.Thickness = 1
		closeStroke.Transparency = 0.72
		closeStroke.Parent = close

		close.MouseEnter:Connect(function()
			tweenService:Create(close, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {
					BackgroundTransparency = 0.34,
				TextTransparency = 0
			}):Play()
			tweenService:Create(closeStroke, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {
				Transparency = 0.52
			}):Play()
		end)

		close.MouseLeave:Connect(function()
			tweenService:Create(close, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {
					BackgroundTransparency = 0.48,
				TextTransparency = 0.08
			}):Play()
			tweenService:Create(closeStroke, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {
				Transparency = 0.72
			}):Play()
		end)

	close.MouseButton1Click:Connect(function()
		if stopSpectating then
			stopSpectating()
		end
	end)

	spectateBarParts = {
			Scale = barScale,
			MainPill = mainPill,
			Shadow = shadow,
			Stroke = mainPillStroke,
			Avatar = avatar,
		AvatarStroke = avatarStroke,
		Eyebrow = eyebrow,
		TargetName = name,
		Status = statusPill,
		StatusStroke = statusStroke,
		Caret = caret,
		LiveGlow = liveGlow,
		LiveDot = liveDot,
		Close = close,
		CloseStroke = closeStroke
	}

	return spectateBar
end

	stopSpectating = function(reason)
	if not spectateState.active then return end

	spectateState.token += 1
	local token = spectateState.token
	local targetPlayer = spectateState.targetPlayer
	local shouldRestorePicker = reason == nil and targetPlayer and players:FindFirstChild(targetPlayer.Name)

	if spectateState.characterConnection then spectateState.characterConnection:Disconnect() end
	if spectateState.removingConnection then spectateState.removingConnection:Disconnect() end
	spectateState.characterConnection = nil
	spectateState.removingConnection = nil
	spectateState.active = false
	spectateState.targetPlayer = nil

	local function finishCameraRestore()
		restoreLocalCamera()
		task.delay(0.55, function()
			if token == spectateState.token then
				spectateState.restoreFov = nil
			end
		end)
	end

	if spectateBar then
		if shouldRestorePicker then
			local restoreFade = 0.07
			local restoreInfo = TweenInfo.new(restoreFade, Enum.EasingStyle.Sine, Enum.EasingDirection.In)
				tweenService:Create(spectateBar, restoreInfo, {
					GroupTransparency = 1,
					BackgroundTransparency = 1
				}):Play()
			if spectateBarParts.Shadow then
				tweenService:Create(spectateBarParts.Shadow, restoreInfo, {
					ImageTransparency = 1
				}):Play()
			end
			if spectateBarParts.Scale then tweenService:Create(spectateBarParts.Scale, restoreInfo, {Scale = 0.985}):Play() end

			task.delay(restoreFade + 0.03, function()
				if token == spectateState.token and spectateBar then
					spectateBar.Visible = false
					resetSpectateBarGeometry()
				end
				if token == spectateState.token and shouldRestorePicker and targetPlayer and players:FindFirstChild(targetPlayer.Name) then
					finishCameraRestore()
					openPlayerActionPicker(targetPlayer, "spectate-toggle")
				end
			end)
		else
			local closeFade = 0.16
			tweenService:Create(spectateBar, TweenInfo.new(closeFade, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
				GroupTransparency = 1,
				BackgroundTransparency = 1
			}):Play()
			if spectateBarParts.Shadow then
				tweenService:Create(spectateBarParts.Shadow, TweenInfo.new(closeFade, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
					ImageTransparency = 1
				}):Play()
			end
			if spectateBarParts.Scale then tweenService:Create(spectateBarParts.Scale, TweenInfo.new(closeFade, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Scale = 0.975}):Play() end

			task.delay(closeFade + 0.02, function()
				if token == spectateState.token and spectateBar then
					spectateBar.Visible = false
					resetSpectateBarGeometry()
					finishCameraRestore()
				end
			end)
		end
	else
		finishCameraRestore()
	end

	if reason then
		queueNotification("Spectate Ended", reason)
	end
end

local function startSpectating(targetPlayer, morphFromPicker)
	if not targetPlayer or targetPlayer == localPlayer then return end

	local humanoid = getSpectateHumanoid(targetPlayer)
	if not humanoid then
		queueNotification("Spectate Unavailable", "Sirius cannot find "..targetPlayer.DisplayName.."'s character right now.")
		return
	end

	if spectateState.active then
		spectateState.token += 1
		if spectateState.characterConnection then spectateState.characterConnection:Disconnect() end
		if spectateState.removingConnection then spectateState.removingConnection:Disconnect() end
		spectateState.characterConnection = nil
		spectateState.removingConnection = nil
		spectateState.active = false
		spectateState.targetPlayer = nil
	end

	spectateState.token += 1
	local token = spectateState.token
	spectateState.active = true
	spectateState.targetPlayer = targetPlayer
	spectateState.restoreFov = spectateState.restoreFov or camera.FieldOfView
	smoothlySpectateHumanoid(humanoid)

	spectateState.characterConnection = targetPlayer.CharacterAdded:Connect(function()
		task.wait(0.25)
		if spectateState.active and spectateState.targetPlayer == targetPlayer then
			local newHumanoid = getSpectateHumanoid(targetPlayer)
			if newHumanoid then
				smoothlySpectateHumanoid(newHumanoid)
			end
		end
	end)

	spectateState.removingConnection = players.PlayerRemoving:Connect(function(removingPlayer)
		if removingPlayer == targetPlayer then
			stopSpectating(targetPlayer.DisplayName.." left the server.")
		end
	end)

	ensureSpectateBar()
	spectateBar.Visible = true
	resetSpectateBarGeometry()
		spectateBar.BackgroundTransparency = 1
		spectateBar.GroupTransparency = 1
	if spectateBarParts.Scale then
		spectateBarParts.Scale.Scale = morphFromPicker and 1 or 0.975
	end
	spectateBarParts.Avatar.Image = "https://www.roblox.com/headshot-thumbnail/image?userId="..targetPlayer.UserId.."&width=420&height=420&format=png"
	spectateBarParts.TargetName.Text = targetPlayer.DisplayName
		spectateBarParts.LiveGlow.Size = UDim2.new(0, 32, 0, 32)
		spectateBarParts.LiveGlow.ImageTransparency = 0.58
		spectateBarParts.LiveDot.Size = UDim2.new(0, 10, 0, 10)
		spectateBarParts.LiveDot.BackgroundTransparency = 0
		spectateBarParts.Status.BackgroundTransparency = 1
		spectateBarParts.StatusStroke.Transparency = 1
		if spectateBarParts.MainPill then
			spectateBarParts.MainPill.BackgroundTransparency = 0.6
		end
		spectateBarParts.Close.BackgroundTransparency = 0.48
		spectateBarParts.Close.TextTransparency = 0.08
		if spectateBarParts.CloseStroke then
			spectateBarParts.CloseStroke.Transparency = 0.72
		end
		spectateBarParts.Avatar.ImageTransparency = 0
		spectateBarParts.AvatarStroke.Transparency = 0.48
		spectateBarParts.Eyebrow.TextTransparency = 1
		spectateBarParts.TargetName.TextTransparency = 0
		spectateBarParts.Shadow.ImageTransparency = 1
		spectateBarParts.Stroke.Transparency = 0.74

	local openFade = morphFromPicker and 0.10 or 0.18
	local openInfo = TweenInfo.new(openFade, morphFromPicker and Enum.EasingStyle.Sine or Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
	tweenService:Create(spectateBar, openInfo, {
		GroupTransparency = 0,
		BackgroundTransparency = 1
	}):Play()
	if spectateBarParts.Scale then
		tweenService:Create(spectateBarParts.Scale, openInfo, {Scale = 1}):Play()
	end
	if spectateBarParts.Shadow then
		tweenService:Create(spectateBarParts.Shadow, openInfo, {ImageTransparency = 0.95}):Play()
	end

	task.spawn(function()
		task.wait(morphFromPicker and 0.11 or 0.3)
		while spectateState.active and spectateState.token == token and spectateBarParts.LiveDot and spectateBarParts.LiveGlow do
			tweenService:Create(spectateBarParts.LiveGlow, TweenInfo.new(0.58, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {ImageTransparency = 0.34, Size = UDim2.new(0, 40, 0, 40)}):Play()
			tweenService:Create(spectateBarParts.LiveDot, TweenInfo.new(0.58, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Size = UDim2.new(0, 11, 0, 11), BackgroundTransparency = 0}):Play()
			task.wait(0.58)
			if not spectateState.active or spectateState.token ~= token then break end
			tweenService:Create(spectateBarParts.LiveGlow, TweenInfo.new(0.58, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {ImageTransparency = 0.72, Size = UDim2.new(0, 26, 0, 26)}):Play()
			tweenService:Create(spectateBarParts.LiveDot, TweenInfo.new(0.58, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Size = UDim2.new(0, 9, 0, 9), BackgroundTransparency = 0.02}):Play()
			task.wait(0.58)
		end
	end)

	queueNotification("Spectating", "Now watching "..targetPlayer.DisplayName..".")
end

local function ensureTrackingFolder()
	if trackingFolder then return trackingFolder end

	trackingFolder = Instance.new("Folder")
	trackingFolder.Name = "SiriusTracking"
	trackingFolder.Parent = UI

	return trackingFolder
end

local _picker = {}

function _picker.getTrackingRoot(player)
	if not player or not players:FindFirstChild(player.Name) then return nil end

	local character = player.Character or workspace:FindFirstChild(player.Name)
	if not character then return nil end

	return character:FindFirstChild("HumanoidRootPart") or character:FindFirstChild("Head") or character.PrimaryPart
end

local function getTrackingDelta(targetPlayer)
	local localRoot = _picker.getTrackingRoot(localPlayer)
	local targetRoot = _picker.getTrackingRoot(targetPlayer)

	if not localRoot or not targetRoot then return nil end

	return targetRoot.Position - localRoot.Position
end

local function hasDrawingTracerSupport()
	return typeof(Drawing) == "table" and typeof(Drawing.new) == "function"
end

local function removeTracerObject(object)
	if not object then return end
	pcall(function()
		if typeof(object.Remove) == "function" then
			object:Remove()
		elseif typeof(object.Destroy) == "function" then
			object:Destroy()
		end
	end)
end

local function getTracerColor(targetPlayer)
	if targetPlayer and targetPlayer.Team and localPlayer.Team and targetPlayer.Team == localPlayer.Team then
		return Color3.new(0, 1, 0)
	end

	return Color3.new(1, 0, 0)
end

local function cleanupTracerLoopIfIdle()
	if next(playerTracers) ~= nil then return end

	if tracerRenderConnection then
		tracerRenderConnection:Disconnect()
		tracerRenderConnection = nil
	end

	if tracerRemovingConnection then
		tracerRemovingConnection:Disconnect()
		tracerRemovingConnection = nil
	end
end

local function destroyPlayerTracer(targetPlayer)
	if not targetPlayer then return end

	local tracer = playerTracers[targetPlayer]
	if not tracer then return end

	playerTracers[targetPlayer] = nil
	removeTracerObject(tracer.Line)
	removeTracerObject(tracer.Outline)
	cleanupTracerLoopIfIdle()
end

local function updatePlayerTracers()
	if not checkSirius() then
		for targetPlayer in pairs(playerTracers) do
			destroyPlayerTracer(targetPlayer)
		end
		return
	end

	local viewportSize = camera.ViewportSize
	local origin = Vector2.new(viewportSize.X * 0.5, viewportSize.Y * 0.5)

	for targetPlayer, tracer in pairs(playerTracers) do
		local visible = false
		if targetPlayer and players:FindFirstChild(targetPlayer.Name) then
			local root = _picker.getTrackingRoot(targetPlayer)
			local character = targetPlayer.Character
			local humanoid = character and character:FindFirstChildOfClass("Humanoid")
			if root and (not humanoid or humanoid.Health > 0) then
				local screenPoint, onScreen = camera:WorldToViewportPoint(root.Position)
				if onScreen and screenPoint.Z > 0 then
					local destination = Vector2.new(screenPoint.X, screenPoint.Y)
					local color = getTracerColor(targetPlayer)

					if tracer.Outline then
						tracer.Outline.From = origin
						tracer.Outline.To = destination
						tracer.Outline.Color = Color3.new(0, 0, 0)
						tracer.Outline.Thickness = 3
						tracer.Outline.Transparency = 0.58
						tracer.Outline.Visible = true
					end
					if tracer.Line then
						tracer.Line.From = origin
						tracer.Line.To = destination
						tracer.Line.Color = color
						tracer.Line.Thickness = 1.4
						tracer.Line.Transparency = 0.9
						tracer.Line.Visible = true
					end

					visible = true
				end
			end
		else
			destroyPlayerTracer(targetPlayer)
		end

		if not visible then
			if tracer.Line then tracer.Line.Visible = false end
			if tracer.Outline then tracer.Outline.Visible = false end
		end
	end
end

local function ensureTracerRenderLoop()
	if tracerRenderConnection then return end

	tracerRenderConnection = runService.RenderStepped:Connect(updatePlayerTracers)
	if not tracerRemovingConnection then
		tracerRemovingConnection = players.PlayerRemoving:Connect(destroyPlayerTracer)
	end
end

local function setPlayerTracer(targetPlayer, enabled)
	if not targetPlayer or targetPlayer == localPlayer or not players:FindFirstChild(targetPlayer.Name) then
		return false
	end

	if not enabled then
		destroyPlayerTracer(targetPlayer)
		return false
	end

	if playerTracers[targetPlayer] then
		return true
	end

	if not hasDrawingTracerSupport() then
		if not tracerUnavailableNotified then
			tracerUnavailableNotified = true
			queueNotification("Tracer Unavailable", "Your executor does not support Drawing lines.")
		end
		return false
	end

	local outline = Drawing.new("Line")
	outline.Visible = false
	outline.Color = Color3.new(0, 0, 0)
	outline.Thickness = 3
	outline.Transparency = 0.58

	local line = Drawing.new("Line")
	line.Visible = false
	line.Color = getTracerColor(targetPlayer)
	line.Thickness = 1.4
	line.Transparency = 0.9

	playerTracers[targetPlayer] = {
		Line = line,
		Outline = outline
	}

	ensureTracerRenderLoop()
	return true
end

local function togglePlayerTracer(targetPlayer)
	if not targetPlayer or targetPlayer == localPlayer or not players:FindFirstChild(targetPlayer.Name) then return false end

	local wantsEnable = playerTracers[targetPlayer] == nil
	local enabled = setPlayerTracer(targetPlayer, wantsEnable)
	if wantsEnable and not enabled then
		return false
	end

	queueNotification(
		enabled and "Tracer Enabled" or "Tracer Disabled",
		enabled and ("Drawing a live tracer to "..targetPlayer.DisplayName..".") or (targetPlayer.DisplayName.."'s tracer was removed.")
	)
	return enabled
end

local hiddenPlayerTools = {}

function hiddenPlayerTools.RememberOriginal(state, object, property)
	if not state or not object then return false end

	local originals = state.Originals
	if not originals then
		originals = {}
		state.Originals = originals
	end

	local objectOriginals = originals[object]
	if not objectOriginals then
		objectOriginals = {}
		originals[object] = objectOriginals
	end

	if objectOriginals[property] == nil then
		local success, value = pcall(function()
			return object[property]
		end)
		if not success then return false end
		objectOriginals[property] = value
	end

	return true
end

function hiddenPlayerTools.SetObjectProperty(state, object, property, value)
	if not hiddenPlayerTools.RememberOriginal(state, object, property) then return end

	pcall(function()
		object[property] = value
	end)
end

function hiddenPlayerTools.ApplyObject(state, object)
	if not state or not object then return end

	if object:IsA("BasePart") then
		hiddenPlayerTools.SetObjectProperty(state, object, "LocalTransparencyModifier", 1)
	elseif object:IsA("Decal") or object:IsA("Texture") then
		hiddenPlayerTools.SetObjectProperty(state, object, "Transparency", 1)
	elseif object:IsA("BillboardGui") or object:IsA("SurfaceGui") or object:IsA("ParticleEmitter") or object:IsA("Trail") or object:IsA("Beam") or object:IsA("Highlight") or object:IsA("Fire") or object:IsA("Smoke") or object:IsA("Sparkles") then
		hiddenPlayerTools.SetObjectProperty(state, object, "Enabled", false)
	end
end

function hiddenPlayerTools.RestoreState(state)
	if not state or not state.Originals then return end

	for object, properties in pairs(state.Originals) do
		if object and object.Parent then
			for property, value in pairs(properties) do
				pcall(function()
					object[property] = value
				end)
			end
		end
	end

	state.Originals = {}
end

function hiddenPlayerTools.DisconnectCharacter(state)
	if state and state.DescendantAddedConnection then
		state.DescendantAddedConnection:Disconnect()
		state.DescendantAddedConnection = nil
	end
end

function hiddenPlayerTools.ApplyCharacter(targetPlayer)
	local state = hiddenPlayers[targetPlayer]
	if not state then return end

	local character = targetPlayer and targetPlayer.Character
	if not character then return end

	if state.Character ~= character then
		hiddenPlayerTools.DisconnectCharacter(state)
		hiddenPlayerTools.RestoreState(state)
		state.Character = character
	end

	for _, object in ipairs(character:GetDescendants()) do
		hiddenPlayerTools.ApplyObject(state, object)
	end

	if not state.DescendantAddedConnection then
		state.DescendantAddedConnection = character.DescendantAdded:Connect(function(object)
			task.defer(function()
				if hiddenPlayers[targetPlayer] == state and state.Character == character then
					hiddenPlayerTools.ApplyObject(state, object)
				end
			end)
		end)
	end
end

function hiddenPlayerTools.Destroy(targetPlayer)
	local state = targetPlayer and hiddenPlayers[targetPlayer]
	if not state then return end

	hiddenPlayers[targetPlayer] = nil
	hiddenPlayerTools.DisconnectCharacter(state)
	if state.CharacterAddedConnection then state.CharacterAddedConnection:Disconnect() end
	if state.RemovingConnection then state.RemovingConnection:Disconnect() end
	hiddenPlayerTools.RestoreState(state)
end

function hiddenPlayerTools.Set(targetPlayer, enabled)
	if not targetPlayer or targetPlayer == localPlayer or not players:FindFirstChild(targetPlayer.Name) then
		return false
	end

	if not enabled then
		hiddenPlayerTools.Destroy(targetPlayer)
		return false
	end

	local state = hiddenPlayers[targetPlayer]
	if not state then
		state = {
			Originals = {},
			Character = nil,
			CharacterAddedConnection = nil,
			DescendantAddedConnection = nil,
			RemovingConnection = nil
		}
		hiddenPlayers[targetPlayer] = state

		state.CharacterAddedConnection = targetPlayer.CharacterAdded:Connect(function()
			task.wait(0.2)
			if hiddenPlayers[targetPlayer] == state then
				hiddenPlayerTools.ApplyCharacter(targetPlayer)
			end
		end)

		state.RemovingConnection = players.PlayerRemoving:Connect(function(removingPlayer)
			if removingPlayer == targetPlayer then
				hiddenPlayerTools.Destroy(targetPlayer)
			end
		end)
	end

	hiddenPlayerTools.ApplyCharacter(targetPlayer)
	return hiddenPlayers[targetPlayer] ~= nil
end

function hiddenPlayerTools.Toggle(targetPlayer)
	if not targetPlayer or targetPlayer == localPlayer or not players:FindFirstChild(targetPlayer.Name) then return false end

	local enabled = hiddenPlayerTools.Set(targetPlayer, hiddenPlayers[targetPlayer] == nil)
	queueNotification(
		enabled and "Hide Enabled" or "Hide Disabled",
		enabled and (targetPlayer.DisplayName.." is hidden locally.") or (targetPlayer.DisplayName.." is visible again.")
	)
	return enabled
end

playerActionPickerParts.TeleportToSelected = function(targetPlayer)
	if not targetPlayer or targetPlayer == localPlayer or not players:FindFirstChild(targetPlayer.Name) then return false end

	local localCharacter = localPlayer.Character
	local localRoot = localCharacter and localCharacter:FindFirstChild("HumanoidRootPart")
	local targetCharacter = targetPlayer.Character
	local targetRoot = targetCharacter and targetCharacter:FindFirstChild("HumanoidRootPart")
	if not localRoot or not targetRoot then
		queueNotification("Teleport Failed", targetPlayer.DisplayName.." is unavailable right now.")
		return false
	end

	pcall(function()
		localRoot.AssemblyLinearVelocity = Vector3.zero
		localRoot.AssemblyAngularVelocity = Vector3.zero
		localRoot.Velocity = Vector3.zero
		localRoot.RotVelocity = Vector3.zero
	end)

	local behindTarget = targetRoot.CFrame * CFrame.new(0, 1.5, 3)
	localRoot.CFrame = CFrame.new(behindTarget.Position, targetRoot.Position)
	queueNotification("Teleported", "Moved to "..targetPlayer.DisplayName..".")
	return true
end

local wieldingTools = {
	Active = nil,
	RocketToken = 0,
	Abilities = {
		FlingPlayer = {Label = "Fling Player", Mode = "position", Offset = Vector3.new(0, 0, 0), Velocity = Vector3.zero},
		Fling2 = {Label = "Fling 2.0", Mode = "position", Offset = Vector3.new(0, -2.5, 0), Velocity = Vector3.new(500, 500, 500)},
		PlatformFly = {Label = "Platform Fly", Mode = "position", Offset = Vector3.new(0, -4, 0), Velocity = Vector3.new(500, 500, 500)},
		MagicCarpet = {Label = "Magic Carpet", Mode = "position", Offset = Vector3.new(0, -3.6, 0), Velocity = Vector3.new(500, 500, 500)},
		Elevator = {Label = "Elevator", Mode = "position", Offset = Vector3.new(0, -3, 0), Velocity = Vector3.new(500, 500, 500)},
		AntiJump = {Label = "Anti Jump", Mode = "position", Offset = Vector3.new(0, 3.5, 0), Velocity = Vector3.zero},
		InfiniteJump = {Label = "Infinite Jump", Mode = "cframe", OffsetCFrame = CFrame.new(0, -5.75, 0)},
		SpeedSlow = {Label = "Speed Boost Slow", Mode = "cframe", OffsetCFrame = CFrame.new(0, 0.7, 1)},
		SpeedMedium = {Label = "Speed Boost Medium", Mode = "cframe", OffsetCFrame = CFrame.new(0, 0.7, 0.5)},
		SpeedFast = {Label = "Speed Boost Fast", Mode = "cframe", OffsetCFrame = CFrame.new(0, 0.7, 0.35)},
		MovementBreaker = {Label = "Movement Breaker", Mode = "cframe", OffsetCFrame = CFrame.new(0, 0.7, -0.8)}
	}
}

function wieldingTools.GetLocalRoot()
	local character = localPlayer.Character
	local humanoid = character and character:FindFirstChildOfClass("Humanoid")
	local root = character and character:FindFirstChild("HumanoidRootPart")
	return character, humanoid, root
end

function wieldingTools.GetTargetRoot(targetPlayer)
	local character = targetPlayer and targetPlayer.Character
	return character and character:FindFirstChild("HumanoidRootPart")
end

function wieldingTools.ZeroRoot(root)
	if not root then return end
	pcall(function()
		root.Velocity = Vector3.zero
		root.RotVelocity = Vector3.zero
		root.AssemblyLinearVelocity = Vector3.zero
		root.AssemblyAngularVelocity = Vector3.zero
	end)
end

function wieldingTools.SetPhysicsRoot(root, targetRoot)
	if not root then return end
	if typeof(sethiddenproperty) == "function" then
		pcall(function()
			sethiddenproperty(root, "PhysicsRepRootPart", targetRoot or root)
		end)
	end
end

function wieldingTools.Stop(silent)
	local active = wieldingTools.Active
	if not active then return false end

	wieldingTools.Active = nil
	if active.Connection then
		active.Connection:Disconnect()
	end

	local _, _, root = wieldingTools.GetLocalRoot()
	if root then
		if active.SavedCFrame then
			root.CFrame = active.SavedCFrame
		end
		wieldingTools.ZeroRoot(root)
		wieldingTools.SetPhysicsRoot(root, root)
	end

	if not silent then
		queueNotification("Wielding Disabled", active.Label.." stopped.")
	end
	return false
end

function wieldingTools.IsActive(targetPlayer, abilityName)
	local active = wieldingTools.Active
	return active ~= nil and active.TargetPlayer == targetPlayer and active.AbilityName == abilityName
end

function wieldingTools.GetActiveName(targetPlayer)
	local active = wieldingTools.Active
	if active and active.TargetPlayer == targetPlayer then
		return active.AbilityName
	end
	return nil
end

function wieldingTools.Toggle(targetPlayer, abilityName)
	if not targetPlayer or targetPlayer == localPlayer or not players:FindFirstChild(targetPlayer.Name) then return false end
	local ability = wieldingTools.Abilities[abilityName]
	if not ability then return false end

	if wieldingTools.IsActive(targetPlayer, abilityName) then
		return wieldingTools.Stop(false)
	end

	wieldingTools.Stop(true)

	local _, _, root = wieldingTools.GetLocalRoot()
	local targetRoot = wieldingTools.GetTargetRoot(targetPlayer)
	if not root or not targetRoot then
		queueNotification("Wielding Failed", "Target character is unavailable.")
		return false
	end

	local active = {
		AbilityName = abilityName,
		Label = ability.Label,
		TargetPlayer = targetPlayer,
		SavedCFrame = root.CFrame,
		Connection = nil
	}
	wieldingTools.Active = active

	active.Connection = runService.Heartbeat:Connect(function()
		if wieldingTools.Active ~= active then return end
		if not targetPlayer or not players:FindFirstChild(targetPlayer.Name) then
			wieldingTools.Stop(true)
			return
		end

		local _, _, playerRoot = wieldingTools.GetLocalRoot()
		local currentTargetRoot = wieldingTools.GetTargetRoot(targetPlayer)
		if not playerRoot or not currentTargetRoot then return end

		if ability.Mode == "cframe" then
			playerRoot.CFrame = currentTargetRoot.CFrame * (ability.OffsetCFrame or CFrame.new())
			wieldingTools.ZeroRoot(playerRoot)
		else
			local offset = ability.Offset or Vector3.zero
			playerRoot.CFrame = CFrame.new(currentTargetRoot.Position + offset) * CFrame.Angles(-math.pi / 2, 0, 0)
			local velocity = ability.Velocity or Vector3.zero
			pcall(function()
				playerRoot.Velocity = velocity
				playerRoot.RotVelocity = velocity
				playerRoot.AssemblyLinearVelocity = velocity
				playerRoot.AssemblyAngularVelocity = velocity
			end)
		end

		wieldingTools.SetPhysicsRoot(playerRoot, currentTargetRoot)
	end)

	queueNotification("Wielding Enabled", ability.Label.." is active on "..targetPlayer.DisplayName..".")
	return true
end

function wieldingTools.Rocket(targetPlayer, power, label)
	if not targetPlayer or targetPlayer == localPlayer or not players:FindFirstChild(targetPlayer.Name) then return false end

	local _, _, root = wieldingTools.GetLocalRoot()
	local targetRoot = wieldingTools.GetTargetRoot(targetPlayer)
	if not root or not targetRoot then
		queueNotification("Wielding Failed", "Target character is unavailable.")
		return false
	end

	wieldingTools.Stop(true)
	wieldingTools.RocketToken += 1
	local token = wieldingTools.RocketToken
	local savedCFrame = root.CFrame
	local rocketConnection
	rocketConnection = runService.Heartbeat:Connect(function()
		if token ~= wieldingTools.RocketToken then
			if rocketConnection then rocketConnection:Disconnect() end
			return
		end

		local _, _, playerRoot = wieldingTools.GetLocalRoot()
		local currentTargetRoot = wieldingTools.GetTargetRoot(targetPlayer)
		if not playerRoot or not currentTargetRoot then return end

		local targetBelow = currentTargetRoot.Position + Vector3.new(0, -3, 0)
		playerRoot.CFrame = CFrame.new(targetBelow, targetBelow + Vector3.new(0, 1, 0))
		pcall(function()
			playerRoot.Velocity = Vector3.new(0, power or 50, 0)
			playerRoot.RotVelocity = Vector3.zero
			playerRoot.AssemblyLinearVelocity = Vector3.new(0, power or 50, 0)
			playerRoot.AssemblyAngularVelocity = Vector3.zero
		end)
		wieldingTools.SetPhysicsRoot(playerRoot, currentTargetRoot)
	end)

	task.delay(0.22, function()
		if token ~= wieldingTools.RocketToken then return end
		if rocketConnection then rocketConnection:Disconnect() end
		local _, _, playerRoot = wieldingTools.GetLocalRoot()
		if playerRoot then
			playerRoot.CFrame = savedCFrame
			wieldingTools.ZeroRoot(playerRoot)
			wieldingTools.SetPhysicsRoot(playerRoot, playerRoot)
		end
	end)

	queueNotification("Wielding", (label or "Rocket Target").." fired at "..targetPlayer.DisplayName..".")
	return true
end

local function setPlayerActionSwitchVisual(switch, switchStroke, knob, enabled, animated)
	if not switch then return end
	local switchGoal = {
		BackgroundColor3 = enabled and siriusValues.selectActionStyle.switchActive or siriusValues.selectActionStyle.switch
	}
	local strokeGoal = {
		Color = enabled and siriusValues.selectActionStyle.switchStrokeActive or siriusValues.selectActionStyle.switchStroke,
		Transparency = enabled and 0.12 or 0.28
	}
	local knobGoal = {
		Position = enabled and UDim2.new(1, -13, 0.5, 0) or UDim2.new(0, 13, 0.5, 0),
		BackgroundColor3 = enabled and Color3.fromRGB(245, 245, 245) or Color3.fromRGB(110, 110, 110)
	}

	if animated then
		local info = TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out)
		tweenService:Create(switch, info, switchGoal):Play()
		if switchStroke then tweenService:Create(switchStroke, info, strokeGoal):Play() end
		if knob then tweenService:Create(knob, info, knobGoal):Play() end
		return
	end

	for property, value in pairs(switchGoal) do switch[property] = value end
	if switchStroke then
		for property, value in pairs(strokeGoal) do switchStroke[property] = value end
	end
	if knob then
		for property, value in pairs(knobGoal) do knob[property] = value end
	end
end

local function refreshWieldingActionRows(targetPlayer, animated)
	if not playerActionPickerParts.WieldRows then return end

	for _, rowParts in pairs(playerActionPickerParts.WieldRows) do
		if rowParts.Switch then
			setPlayerActionSwitchVisual(
				rowParts.Switch,
				rowParts.SwitchStroke,
				rowParts.Knob,
				wieldingTools.IsActive(targetPlayer, rowParts.AbilityName),
				animated
			)
		end
	end
end

function _picker.destroyTrackingBadge(targetPlayer)
	if not targetPlayer then return end

	local existing = trackingBadges[targetPlayer]
	if existing then
		trackingBadges[targetPlayer] = nil
		tweenService:Create(existing, TweenInfo.new(0.16, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Size = UDim2.fromOffset(0, 0)}):Play()
		task.delay(0.18, function()
			if existing then
				existing:Destroy()
			end
		end)
	end

	if trackingConnections[targetPlayer] then
		for _, connection in ipairs(trackingConnections[targetPlayer]) do
			if connection then connection:Disconnect() end
		end
		trackingConnections[targetPlayer] = nil
	end
end

function _picker.closeLiveTrackPanel(restorePicker)
	if not liveTrackState.active and (not liveTrackPanel or not liveTrackPanel.Visible) then return end

	liveTrackState.token += 1
	local token = liveTrackState.token
	local targetPlayer = liveTrackState.targetPlayer
	local shouldRestorePicker = restorePicker
	if shouldRestorePicker == nil then
		shouldRestorePicker = liveTrackState.returnToPicker
	end
	liveTrackState.active = false
	liveTrackState.targetPlayer = nil
	liveTrackState.returnToPicker = false

	if liveTrackState.renderConnection then liveTrackState.renderConnection:Disconnect() end
	if liveTrackState.characterConnection then liveTrackState.characterConnection:Disconnect() end
	if liveTrackState.removingConnection then liveTrackState.removingConnection:Disconnect() end
	liveTrackState.renderConnection = nil
	liveTrackState.characterConnection = nil
	liveTrackState.removingConnection = nil

	if liveTrackPanel then
		local closeWidth = math.max(220, (liveTrackPanel.AbsoluteSize.X > 0 and liveTrackPanel.AbsoluteSize.X or liveTrackCurrentSize.X) - 34)
		local closeSize = UDim2.fromOffset(closeWidth, 18)
		local closeShadowSize = UDim2.fromOffset(closeWidth + 40, 54)
		local contentFadeTween = TweenInfo.new(0.14, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
		local panelCloseTween = TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut)
		local panelFadeTween = TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.In)

		tweenService:Create(liveTrackPanel, contentFadeTween, {
			GroupTransparency = 1
		}):Play()
		tweenService:Create(liveTrackPanel, panelCloseTween, {
			Size = closeSize
		}):Play()
		tweenService:Create(liveTrackPanel, panelFadeTween, {
			BackgroundTransparency = 1
		}):Play()
		if liveTrackParts.Stroke then
			tweenService:Create(liveTrackParts.Stroke, panelFadeTween, {Transparency = 1}):Play()
		end
		if liveTrackParts.Scale then
			tweenService:Create(liveTrackParts.Scale, panelCloseTween, {Scale = 1}):Play()
		end
		if liveTrackParts.Shadow then
			tweenService:Create(liveTrackParts.Shadow, panelFadeTween, {ImageTransparency = 1}):Play()
		end
		if liveTrackParts.OuterShadow then
			tweenService:Create(liveTrackParts.OuterShadow, panelCloseTween, {
				Position = getLiveTrackShadowPosition(liveTrackCurrentPosition, 16),
				Size = closeShadowSize
			}):Play()
			tweenService:Create(liveTrackParts.OuterShadow, panelFadeTween, {ImageTransparency = 1}):Play()
		end

		task.delay(0.32, function()
			if token == liveTrackState.token and liveTrackPanel then
				liveTrackPanel.Visible = false
				liveTrackPanel.BackgroundTransparency = 0
				liveTrackPanel.GroupTransparency = 1
				applyLiveTrackSize(liveTrackCurrentSize, false)
				if liveTrackParts.Scale then
					liveTrackParts.Scale.Scale = 1
				end
				if liveTrackParts.OuterShadow then
					liveTrackParts.OuterShadow.Visible = false
				end
				if targetPlayer and shouldRestorePicker and players:FindFirstChild(targetPlayer.Name) and openPlayerActionPicker then
					openPlayerActionPicker(targetPlayer)
				end
			end
		end)
	end
end

local function ensureLiveTrackPanel()
	if liveTrackPanel then return liveTrackPanel end

	local liveTrackSize = clampLiveTrackSize(liveTrackCurrentSize)
	local liveTrackShadowSize = getLiveTrackShadowSize(liveTrackSize)

		local outerShadow = Instance.new("ImageLabel")
		outerShadow.Name = "SiriusLiveTrackPanelShadow"
		outerShadow.AnchorPoint = Vector2.new(0.5, 0.5)
		outerShadow.Position = getLiveTrackShadowPosition(liveTrackCurrentPosition, 18)
	outerShadow.Size = UDim2.fromOffset(liveTrackShadowSize.X, liveTrackShadowSize.Y)
	outerShadow.BackgroundTransparency = 1
	outerShadow.Image = "rbxassetid://8992230677"
	outerShadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
	outerShadow.ImageTransparency = 1
	outerShadow.ScaleType = Enum.ScaleType.Slice
	outerShadow.SliceCenter = Rect.new(99, 99, 99, 99)
	outerShadow.Visible = false
	outerShadow.ZIndex = 80
	outerShadow.Parent = UI

		liveTrackPanel = Instance.new("CanvasGroup")
		liveTrackPanel.Name = "SiriusLiveTrackPanel"
		liveTrackPanel.AnchorPoint = Vector2.new(0.5, 0.5)
		liveTrackPanel.Position = liveTrackCurrentPosition
	liveTrackPanel.Size = UDim2.fromOffset(liveTrackSize.X, liveTrackSize.Y)
	liveTrackPanel.BackgroundColor3 = Color3.fromRGB(32, 32, 32)
	liveTrackPanel.BackgroundTransparency = 0
	liveTrackPanel.BorderSizePixel = 0
	liveTrackPanel.GroupTransparency = 1
	liveTrackPanel.Visible = false
	liveTrackPanel.ZIndex = 82
		liveTrackPanel.ClipsDescendants = true
		liveTrackPanel.Parent = UI

		local dragArea = Instance.new("Frame")
		dragArea.Name = "DragArea"
		dragArea.Position = UDim2.fromOffset(0, 0)
		dragArea.Size = UDim2.new(1, -96, 0, 118)
		dragArea.BackgroundTransparency = 1
		dragArea.BorderSizePixel = 0
		dragArea.Active = true
		dragArea.ZIndex = 82
		dragArea.Parent = liveTrackPanel

	local scale = Instance.new("UIScale")
	scale.Name = "Scale"
	scale.Scale = 0.985
	scale.Parent = liveTrackPanel

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 24)
	corner.Parent = liveTrackPanel

	local stroke = Instance.new("UIStroke")
	stroke.Name = "Stroke"
	stroke.Color = Color3.fromRGB(70, 70, 70)
	stroke.Thickness = 1
	stroke.Transparency = 0.48
	stroke.Parent = liveTrackPanel

	local gradient = Instance.new("UIGradient")
	gradient.Rotation = 105
	gradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(36, 36, 36)),
		ColorSequenceKeypoint.new(0.58, Color3.fromRGB(32, 32, 32)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(29, 29, 29))
	})
	gradient.Parent = liveTrackPanel

	local shadow = Instance.new("ImageLabel")
	shadow.Name = "Shadow"
	shadow.AnchorPoint = Vector2.new(0.5, 0.5)
		shadow.Position = UDim2.new(0.5, 0, 0.5, 16)
			shadow.Size = UDim2.new(1, 44, 1, 44)
		shadow.BackgroundTransparency = 1
		shadow.Image = "rbxassetid://8992230677"
		shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
		shadow.ImageTransparency = 1
	shadow.ScaleType = Enum.ScaleType.Slice
	shadow.SliceCenter = Rect.new(99, 99, 99, 99)
	shadow.ZIndex = 81
	shadow.Parent = liveTrackPanel

		local eyebrow = Instance.new("TextLabel")
		eyebrow.Name = "Eyebrow"
			eyebrow.Position = UDim2.new(0, 34, 0, 32)
			eyebrow.Size = UDim2.new(1, -116, 0, 22)
	eyebrow.BackgroundTransparency = 1
	eyebrow.Text = "TRACKING"
	eyebrow.TextColor3 = Color3.fromRGB(150, 150, 150)
	eyebrow.TextXAlignment = Enum.TextXAlignment.Left
		eyebrow.TextSize = 16
		eyebrow.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
		eyebrow.Active = true
		eyebrow.ZIndex = 83
		eyebrow.Parent = liveTrackPanel

		local name = Instance.new("TextLabel")
		name.Name = "TargetName"
		name.Position = UDim2.new(0, 34, 0, 58)
		name.Size = UDim2.new(1, -88, 0, 36)
	name.BackgroundTransparency = 1
	name.Text = "Player"
	name.TextColor3 = Color3.fromRGB(245, 245, 245)
	name.TextXAlignment = Enum.TextXAlignment.Left
	name.TextSize = 24
		name.TextTruncate = Enum.TextTruncate.AtEnd
		name.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
		name.Active = true
		name.ZIndex = 83
		name.Parent = liveTrackPanel

	local pin = Instance.new("TextButton")
	pin.Name = "Pin"
	pin.AnchorPoint = Vector2.new(1, 0)
	pin.Position = UDim2.new(1, -74, 0, 30)
	pin.Size = UDim2.new(0, 34, 0, 34)
	pin.BackgroundColor3 = Color3.fromRGB(42, 42, 42)
	pin.BackgroundTransparency = 0.18
	pin.BorderSizePixel = 0
	pin.Text = "⌖"
	pin.TextColor3 = Color3.fromRGB(205, 205, 205)
	pin.TextSize = 17
	pin.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	pin.AutoButtonColor = false
	pin.Visible = false
	pin.ZIndex = 84
	pin.Parent = liveTrackPanel

	local pinCorner = Instance.new("UICorner")
	pinCorner.CornerRadius = UDim.new(1, 0)
	pinCorner.Parent = pin

	pin.MouseEnter:Connect(function()
		tweenService:Create(pin, TweenInfo.new(0.16, Enum.EasingStyle.Quint), {
			BackgroundTransparency = 0.08,
			TextColor3 = Color3.fromRGB(245, 245, 245)
		}):Play()
	end)

	pin.MouseLeave:Connect(function()
		tweenService:Create(pin, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {
			BackgroundTransparency = 0.18,
			TextColor3 = Color3.fromRGB(205, 205, 205)
		}):Play()
	end)

		local close = Instance.new("TextButton")
		close.Name = "Close"
		close.AnchorPoint = Vector2.new(1, 0)
		close.Position = UDim2.new(1, -30, 0, 28)
		close.Size = UDim2.new(0, 36, 0, 36)
		close.BackgroundColor3 = Color3.fromRGB(42, 42, 42)
		close.BackgroundTransparency = 1
		close.BorderSizePixel = 0
		close.Text = ""
		close.AutoButtonColor = false
		close.ZIndex = 84
		close.Parent = liveTrackPanel

	local closeIcon = Instance.new("TextLabel")
	closeIcon.Name = "Icon"
	closeIcon.BackgroundTransparency = 1
	closeIcon.Size = UDim2.fromScale(1, 1)
	closeIcon.Text = "×"
	closeIcon.TextColor3 = Color3.fromRGB(220, 220, 220)
	closeIcon.TextTransparency = 0.08
	closeIcon.TextSize = 23
	closeIcon.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium)
	closeIcon.ZIndex = 85
	closeIcon.Parent = close

	close.MouseEnter:Connect(function()
		close.BackgroundTransparency = 1
		tweenService:Create(closeIcon, TweenInfo.new(0.16, Enum.EasingStyle.Quint), {
			TextColor3 = Color3.fromRGB(255, 255, 255),
			TextTransparency = 0
		}):Play()
	end)

	close.MouseLeave:Connect(function()
		close.BackgroundTransparency = 1
		tweenService:Create(closeIcon, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {
			TextColor3 = Color3.fromRGB(220, 220, 220),
			TextTransparency = 0.08
		}):Play()
	end)

				local directionCanvas = Instance.new("Frame")
					directionCanvas.Name = "Canvas"
					directionCanvas.AnchorPoint = Vector2.new(0.5, 0.5)
					directionCanvas.Position = UDim2.fromScale(0.5, 0.54)
					directionCanvas.Size = UDim2.fromOffset(300, 300)
				directionCanvas.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
				directionCanvas.BackgroundTransparency = 1
				directionCanvas.BorderSizePixel = 0
				directionCanvas.ClipsDescendants = false
				directionCanvas.ZIndex = 83
				directionCanvas.Parent = liveTrackPanel

				local directionViewport = Instance.new("ViewportFrame")
			directionViewport.Name = "Direction"
			directionViewport.AnchorPoint = Vector2.new(0.5, 0.5)
			directionViewport.Position = UDim2.fromScale(0.5, 0.5)
			directionViewport.Size = UDim2.fromScale(1, 1)
			directionViewport.BackgroundTransparency = 1
				directionViewport.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
				directionViewport.BorderSizePixel = 0
				directionViewport.ClipsDescendants = false
				directionViewport.Ambient = Color3.fromRGB(255, 255, 255)
				directionViewport.LightColor = Color3.fromRGB(255, 255, 255)
			directionViewport.LightDirection = Vector3.new(-1, -1, -1)
			directionViewport.ZIndex = 84
			directionViewport.Parent = directionCanvas

					local liveTrackCamera = Instance.new("Camera")
					liveTrackCamera.FieldOfView = 40
				liveTrackCamera.CFrame = CFrame.new(Vector3.new(0, 20, 20), Vector3.new(0, 0, 0))
			liveTrackCamera.Parent = directionViewport
			directionViewport.CurrentCamera = liveTrackCamera

			local directionScale = Instance.new("UIScale")
			directionScale.Name = "scale"
			directionScale.Scale = 1
			directionScale.Parent = directionViewport

					local arrow = Instance.new("MeshPart")
					arrow.Name = "Arrow"
					arrow.MeshId = "rbxassetid://83099961898319"
					arrow.TextureID = ""
					arrow.Size = Vector3.new(0.98, 0.05, 0.86)
					arrow.Color = Color3.fromRGB(255, 255, 255)
					arrow.Material = Enum.Material.Neon
					arrow.Anchored = true
					arrow.CastShadow = false
					arrow.CanCollide = false
			arrow.CanTouch = false
			arrow.CanQuery = false
			arrow.Transparency = 1
				arrow.CFrame = CFrame.new()
				arrow.Parent = directionViewport

			local nearbyCircle = Instance.new("ImageLabel")
			nearbyCircle.Name = "Circle"
			nearbyCircle.AnchorPoint = Vector2.new(0.5, 0.5)
			nearbyCircle.Position = UDim2.fromScale(0.5, 0.5)
			nearbyCircle.Size = UDim2.fromOffset(60, 60)
			nearbyCircle.BackgroundTransparency = 1
			nearbyCircle.BorderSizePixel = 0
			nearbyCircle.Image = "rbxassetid://138426819549450"
			nearbyCircle.ImageColor3 = Color3.fromRGB(255, 255, 255)
			nearbyCircle.ImageTransparency = 1
			nearbyCircle.ZIndex = 86
			nearbyCircle.Parent = directionCanvas

			local nearbyBound = Instance.new("ImageLabel")
			nearbyBound.Name = "Bound"
			nearbyBound.AnchorPoint = Vector2.new(0.5, 0.5)
			nearbyBound.Position = UDim2.fromScale(0.5, 0.5)
			nearbyBound.Size = UDim2.fromOffset(60, 60)
			nearbyBound.BackgroundTransparency = 1
			nearbyBound.BorderSizePixel = 0
			nearbyBound.Image = "rbxassetid://138426819549450"
			nearbyBound.ImageColor3 = Color3.fromRGB(255, 255, 255)
			nearbyBound.ImageTransparency = 1
			nearbyBound.ScaleType = Enum.ScaleType.Fit
			nearbyBound.ZIndex = 87
			nearbyBound.Parent = nearbyCircle

			local nearbyBoundScale = Instance.new("UIScale")
			nearbyBoundScale.Name = "scale"
			nearbyBoundScale.Scale = 0
			nearbyBoundScale.Parent = nearbyBound

		local distance = Instance.new("TextLabel")
	distance.Name = "Distance"
	distance.AnchorPoint = Vector2.new(0, 1)
	distance.Position = UDim2.new(0, 34, 1, -28)
	distance.Size = UDim2.new(1, -68, 0, 40)
	distance.BackgroundTransparency = 1
	distance.Text = "Locating..."
	distance.RichText = true
	distance.TextColor3 = Color3.fromRGB(245, 245, 245)
	distance.TextXAlignment = Enum.TextXAlignment.Left
	distance.TextSize = 22
	distance.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
	distance.ZIndex = 83
	distance.Parent = liveTrackPanel

	local resizeHandle = Instance.new("ImageButton")
	resizeHandle.Name = "Resize"
	resizeHandle.AnchorPoint = Vector2.new(1, 1)
	resizeHandle.Position = UDim2.new(1, -8, 1, -8)
	resizeHandle.Size = UDim2.fromOffset(48, 48)
	resizeHandle.BackgroundTransparency = 1
	resizeHandle.BorderSizePixel = 0
	resizeHandle.Image = ""
	resizeHandle.AutoButtonColor = false
	resizeHandle.ZIndex = 88
	resizeHandle.Parent = liveTrackPanel

	local resizeIcon = Instance.new("ImageLabel")
	resizeIcon.Name = "Icon"
	resizeIcon.AnchorPoint = Vector2.new(1, 1)
	resizeIcon.Position = UDim2.new(1, 0, 1, 0)
	resizeIcon.Size = UDim2.fromOffset(18, 18)
	resizeIcon.BackgroundTransparency = 1
	resizeIcon.Image = "rbxassetid://86527207319523"
	resizeIcon.ImageColor3 = Color3.fromRGB(82, 82, 82)
	resizeIcon.ImageTransparency = 0.62
	resizeIcon.ZIndex = 89
	resizeIcon.Parent = resizeHandle

	local liveTrackResizeHover = false
	local liveTrackResizeDragging = false
	local liveTrackResizeStartPointer
	local liveTrackResizeStartSize
		local liveTrackResizeTargetSize = liveTrackCurrentSize
		local liveTrackResizeVisualSize = liveTrackCurrentSize
		local liveTrackResizeConnection
		local liveTrackDragging = false
		local liveTrackDragInput
		local liveTrackDragStartPointer
		local liveTrackDragStartPosition

	local function getLiveTrackPointer(input)
		if input and input.UserInputType == Enum.UserInputType.Touch then
			return Vector2.new(input.Position.X, input.Position.Y)
		end

		return userInputService:GetMouseLocation()
	end

		local function getLiveTrackScreenMax()
		local rootSize = UI.AbsoluteSize
		if rootSize.X <= 0 or rootSize.Y <= 0 then
			rootSize = camera.ViewportSize
		end

			return Vector2.new(
				math.clamp(rootSize.X - 80, liveTrackMinSize.X, liveTrackMaxSize.X),
				math.clamp(rootSize.Y - 80, liveTrackMinSize.Y, liveTrackMaxSize.Y)
			)
		end

		local function beginLiveTrackDrag(input)
			if liveTrackResizeDragging then return end
			liveTrackDragging = true
			liveTrackDragInput = input
			liveTrackDragStartPointer = getLiveTrackPointer(input)
			liveTrackDragStartPosition = liveTrackCurrentPosition
		end

		local function updateLiveTrackDrag(input)
			if not liveTrackDragging or liveTrackResizeDragging or not liveTrackDragStartPointer or not liveTrackDragStartPosition then return end

			local pointer = getLiveTrackPointer(input)
			local delta = pointer - liveTrackDragStartPointer
			setLiveTrackPosition(UDim2.new(
				liveTrackDragStartPosition.X.Scale,
				liveTrackDragStartPosition.X.Offset + delta.X,
				liveTrackDragStartPosition.Y.Scale,
				liveTrackDragStartPosition.Y.Offset + delta.Y
			), 20)
		end

		local function endLiveTrackDrag()
			if not liveTrackDragging then return end
			liveTrackDragging = false
			liveTrackDragInput = nil
			liveTrackDragStartPointer = nil
			liveTrackDragStartPosition = nil
		end

		local function bindLiveTrackDrag(object)
			object.InputBegan:Connect(function(input)
				if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
					beginLiveTrackDrag(input)
				end
			end)

			object.InputChanged:Connect(function(input)
				if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
					liveTrackDragInput = input
				end
			end)
		end

	local function setLiveTrackResizeVisual(mode)
		local size = 18
		local color = Color3.fromRGB(82, 82, 82)
		local transparency = 0.62

		if mode == "drag" then
			size = 23
			color = Color3.fromRGB(112, 112, 112)
			transparency = 0.08
		elseif mode == "hover" then
			size = 21
			color = Color3.fromRGB(98, 98, 98)
			transparency = 0.28
		end

		tweenService:Create(resizeIcon, TweenInfo.new(0.16, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
			Size = UDim2.fromOffset(size, size),
			ImageColor3 = color,
			ImageTransparency = transparency
		}):Play()
	end

	local function stopLiveTrackResizeLoop()
		if liveTrackResizeConnection then
			liveTrackResizeConnection:Disconnect()
			liveTrackResizeConnection = nil
		end
	end

	local function startLiveTrackResizeLoop()
		stopLiveTrackResizeLoop()
		liveTrackResizeVisualSize = liveTrackCurrentSize

		liveTrackResizeConnection = runService.RenderStepped:Connect(function(deltaTime)
			local targetSize = clampLiveTrackSize(liveTrackResizeTargetSize)
			local alpha = 1 - math.exp(-deltaTime * 28)
			liveTrackResizeVisualSize = liveTrackResizeVisualSize + ((targetSize - liveTrackResizeVisualSize) * alpha)
			applyLiveTrackSize(liveTrackResizeVisualSize, false)

			if not liveTrackResizeDragging and (liveTrackResizeVisualSize - targetSize).Magnitude <= 0.45 then
				applyLiveTrackSize(targetSize, false)
				stopLiveTrackResizeLoop()
			end
		end)
	end

	local function beginLiveTrackResize(input)
		liveTrackResizeDragging = true
		liveTrackResizeStartPointer = getLiveTrackPointer(input)
		liveTrackResizeStartSize = liveTrackCurrentSize
		liveTrackResizeTargetSize = liveTrackCurrentSize
		liveTrackResizeVisualSize = liveTrackCurrentSize
		setLiveTrackResizeVisual("drag")
		startLiveTrackResizeLoop()
	end

	local function updateLiveTrackResize(input)
		if not liveTrackResizeDragging or not liveTrackResizeStartPointer or not liveTrackResizeStartSize then return end

		local pointer = getLiveTrackPointer(input)
		local delta = pointer - liveTrackResizeStartPointer
		local maxSize = getLiveTrackScreenMax()
		liveTrackResizeTargetSize = Vector2.new(
			math.clamp(liveTrackResizeStartSize.X + (delta.X * 2), liveTrackMinSize.X, maxSize.X),
			math.clamp(liveTrackResizeStartSize.Y + (delta.Y * 2), liveTrackMinSize.Y, maxSize.Y)
		)
	end

	local function endLiveTrackResize()
		if not liveTrackResizeDragging then return end
		liveTrackResizeDragging = false
		setLiveTrackResizeVisual(liveTrackResizeHover and "hover" or "idle")
	end

	resizeHandle.MouseEnter:Connect(function()
		liveTrackResizeHover = true
		if not liveTrackResizeDragging then
			setLiveTrackResizeVisual("hover")
		end
	end)

	resizeHandle.MouseLeave:Connect(function()
		liveTrackResizeHover = false
		if not liveTrackResizeDragging then
			setLiveTrackResizeVisual("idle")
		end
	end)

		resizeHandle.InputBegan:Connect(function(input)
			if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
				beginLiveTrackResize(input)
			end
		end)

		bindLiveTrackDrag(dragArea)
		bindLiveTrackDrag(eyebrow)
		bindLiveTrackDrag(name)

		userInputService.InputChanged:Connect(function(input)
			if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
				if liveTrackDragging and (not liveTrackDragInput or input == liveTrackDragInput or input.UserInputType == Enum.UserInputType.Touch) then
					updateLiveTrackDrag(input)
				end
				if liveTrackResizeDragging then
					updateLiveTrackResize(input)
				end
			end
		end)

		userInputService.InputEnded:Connect(function(input)
			if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
				endLiveTrackDrag()
				endLiveTrackResize()
			end
		end)

		close.MouseButton1Click:Connect(_picker.closeLiveTrackPanel)

	liveTrackParts = {
		Scale = scale,
		OuterShadow = outerShadow,
		Shadow = shadow,
			Stroke = stroke,
			Eyebrow = eyebrow,
				TargetName = name,
				Canvas = directionCanvas,
				Direction = directionViewport,
					DirectionScale = directionScale,
					LiveTrackCamera = liveTrackCamera,
					Arrow = arrow,
					Circle = nearbyCircle,
					Bound = nearbyBound,
					BoundScale = nearbyBoundScale,
					Distance = distance,
					Pin = pin,
					Close = close,
					ResizeHandle = resizeHandle,
					ResizeIcon = resizeIcon
	}

	return liveTrackPanel
end

	local function getTrackObjectCFrame(targetPlayer)
		local root = _picker.getTrackingRoot(targetPlayer)
		if not root then return nil end
		return root.CFrame
	end

	local function getGuideDistance(targetPlayer)
		local objCFrame = getTrackObjectCFrame(targetPlayer)
		if not objCFrame or not camera then return nil end

		local offset = objCFrame.Position - camera.CFrame.Position
		local distance = math.round(offset.Magnitude)
		local relative = camera.CFrame:VectorToObjectSpace(offset)
		local x, y, z = relative.X, relative.Y, relative.Z
		local absX, absY, absZ = math.abs(x), math.abs(y), math.abs(z)
		local direction

		if absZ >= absX and absZ >= absY then
			direction = z > 0 and "behind" or "ahead"
		elseif absX >= absY and absX >= absZ then
			direction = x > 0 and "to your right" or "to your left"
		else
			direction = y > 0 and "above" or "below"
		end

		return distance, direction, objCFrame
	end

	local function pointLiveTrackArrow(objCFrame)
		if not objCFrame or not liveTrackParts.Arrow then return end
		local offset = objCFrame.Position - camera.CFrame.Position
		if offset.Magnitude <= 0 then return end

		local direction = offset.Unit
		local lookAt = CFrame.new(Vector3.zero, direction)
		local rotated = lookAt * CFrame.Angles(0, math.rad(90), 0)
		tweenService:Create(liveTrackParts.Arrow, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
			CFrame = rotated
		}):Play()
	end

	local function updateLiveTrackReadout(targetPlayer, deltaTime)
		if not liveTrackPanel or not liveTrackState.active then return end

			local distance, direction, objCFrame = getGuideDistance(targetPlayer)
			if not distance or not objCFrame then
				liveTrackParts.Distance.Text = "Target unavailable"
				if liveTrackParts.Arrow then
					tweenService:Create(liveTrackParts.Arrow, TweenInfo.new(0.16, Enum.EasingStyle.Sine), {
						Size = Vector3.zero,
						Transparency = 1
					}):Play()
				end
				if liveTrackParts.Canvas then
					liveTrackParts.Canvas.BackgroundTransparency = 1
				end
				if liveTrackParts.DirectionScale then
					liveTrackParts.DirectionScale.Scale = 1
				end
				if liveTrackParts.Circle then
					tweenService:Create(liveTrackParts.Circle, TweenInfo.new(0.16, Enum.EasingStyle.Sine), {
						ImageTransparency = 1
					}):Play()
				end
				if liveTrackParts.Bound then
					tweenService:Create(liveTrackParts.Bound, TweenInfo.new(0.16, Enum.EasingStyle.Sine), {
						Size = UDim2.fromScale(0, 0),
						ImageTransparency = 1
					}):Play()
				end
				if liveTrackParts.BoundScale then
					tweenService:Create(liveTrackParts.BoundScale, TweenInfo.new(0.16, Enum.EasingStyle.Sine), {Scale = 0}):Play()
				end
				return
			end

		if liveTrackParts.LiveTrackCamera and liveTrackParts.Arrow then
			local focus = liveTrackParts.Arrow.CFrame.Position
			local goal = CFrame.new(focus - (camera.CFrame.LookVector * 8), focus)
			liveTrackParts.LiveTrackCamera.CFrame = liveTrackParts.LiveTrackCamera.CFrame:Lerp(goal, math.min(1, (deltaTime or 0.016) * 20))
		end

		if distance > 20 then
				pointLiveTrackArrow(objCFrame)
					liveTrackParts.Distance.Text = string.format("%d studs <font color=\"rgb(150,150,150)\">%s</font>", distance, direction)
					if liveTrackParts.Arrow then
						tweenService:Create(liveTrackParts.Arrow, TweenInfo.new(0.18, Enum.EasingStyle.Sine), {
							Size = Vector3.new(0.98, 0.05, 0.86),
							Transparency = 0
						}):Play()
					end
				if liveTrackParts.Canvas then
					liveTrackParts.Canvas.BackgroundTransparency = 1
			end
			if liveTrackParts.DirectionScale then
				tweenService:Create(liveTrackParts.DirectionScale, TweenInfo.new(0.18, Enum.EasingStyle.Sine), {Scale = 1}):Play()
			end
			if liveTrackParts.Circle then
				tweenService:Create(liveTrackParts.Circle, TweenInfo.new(0.18, Enum.EasingStyle.Sine), {
					ImageTransparency = 1
				}):Play()
			end
			if liveTrackParts.Bound then
				tweenService:Create(liveTrackParts.Bound, TweenInfo.new(0.18, Enum.EasingStyle.Sine), {
					Size = UDim2.fromScale(0, 0),
					ImageTransparency = 1
				}):Play()
			end
			if liveTrackParts.BoundScale then
				tweenService:Create(liveTrackParts.BoundScale, TweenInfo.new(0.18, Enum.EasingStyle.Sine), {Scale = 0}):Play()
			end
		else
			liveTrackParts.Distance.Text = "Nearby"
			if liveTrackParts.Arrow then
				tweenService:Create(liveTrackParts.Arrow, TweenInfo.new(0.18, Enum.EasingStyle.Sine), {
						Size = Vector3.zero,
						Transparency = 1
					}):Play()
				end
			if liveTrackParts.Canvas then
				liveTrackParts.Canvas.BackgroundTransparency = 1
			end
			if liveTrackParts.DirectionScale then
				tweenService:Create(liveTrackParts.DirectionScale, TweenInfo.new(0.18, Enum.EasingStyle.Sine), {Scale = 1}):Play()
			end
			if liveTrackParts.Circle then
				tweenService:Create(liveTrackParts.Circle, TweenInfo.new(0.22, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
					ImageTransparency = 0
				}):Play()
			end
			if liveTrackParts.Bound then
				tweenService:Create(liveTrackParts.Bound, TweenInfo.new(0.22, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
					Size = UDim2.fromOffset(60, 60),
					ImageTransparency = 0.5
				}):Play()
			end
			if liveTrackParts.BoundScale then
				tweenService:Create(liveTrackParts.BoundScale, TweenInfo.new(0.22, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
					Scale = distance * 0.15
				}):Play()
			end
	end

	if liveTrackParts.Direction then
		liveTrackParts.Direction.BackgroundTransparency = 1
	end
end

function _picker.openLiveTrackPanel(targetPlayer, returnToPicker)
	if not targetPlayer or targetPlayer == localPlayer or not players:FindFirstChild(targetPlayer.Name) then return end

	ensureLiveTrackPanel()
	liveTrackState.token += 1
	local token = liveTrackState.token
	liveTrackState.active = true
	liveTrackState.targetPlayer = targetPlayer
	liveTrackState.returnToPicker = returnToPicker == true

	if liveTrackState.renderConnection then liveTrackState.renderConnection:Disconnect() end
	if liveTrackState.characterConnection then liveTrackState.characterConnection:Disconnect() end
	if liveTrackState.removingConnection then liveTrackState.removingConnection:Disconnect() end

	liveTrackParts.TargetName.Text = targetPlayer.DisplayName
	liveTrackPanel.Visible = true
	local liveTrackOpenSize = clampLiveTrackSize(liveTrackCurrentSize)
	local liveTrackOpenShadowSize = getLiveTrackShadowSize(liveTrackOpenSize)
	local liveTrackStartSize = Vector2.new(
		math.max(220, liveTrackOpenSize.X - 42),
		math.max(18, math.min(42, liveTrackOpenSize.Y * 0.08))
	)
	liveTrackPanel.Size = UDim2.fromOffset(liveTrackStartSize.X, liveTrackStartSize.Y)
	liveTrackPanel.Position = liveTrackCurrentPosition
	liveTrackPanel.BackgroundTransparency = 1
	if liveTrackParts.OuterShadow then
		liveTrackParts.OuterShadow.Visible = true
		liveTrackParts.OuterShadow.ImageTransparency = 1
		liveTrackParts.OuterShadow.Position = getLiveTrackShadowPosition(liveTrackCurrentPosition, 10)
		liveTrackParts.OuterShadow.Size = UDim2.fromOffset(liveTrackStartSize.X + 38, liveTrackStartSize.Y + 36)
	end
	liveTrackPanel.GroupTransparency = 1
	liveTrackParts.Scale.Scale = 1
	if liveTrackParts.Stroke then
		liveTrackParts.Stroke.Transparency = 1
	end
	if liveTrackParts.Shadow then
		liveTrackParts.Shadow.ImageTransparency = 1
	end
	updateLiveTrackReadout(targetPlayer)

	local panelOpenTween = TweenInfo.new(0.46, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
	local panelFadeTween = TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
	local contentTween = TweenInfo.new(0.32, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
	tweenService:Create(liveTrackPanel, panelOpenTween, {
		Size = UDim2.fromOffset(liveTrackOpenSize.X, liveTrackOpenSize.Y)
	}):Play()
	tweenService:Create(liveTrackPanel, panelFadeTween, {
		BackgroundTransparency = 0
	}):Play()
	if liveTrackParts.Shadow then
		tweenService:Create(liveTrackParts.Shadow, panelFadeTween, {
			ImageTransparency = 1
		}):Play()
	end
	if liveTrackParts.Stroke then
		tweenService:Create(liveTrackParts.Stroke, panelFadeTween, {Transparency = 0.48}):Play()
	end
	if liveTrackParts.OuterShadow then
		tweenService:Create(liveTrackParts.OuterShadow, panelOpenTween, {
			Size = UDim2.fromOffset(liveTrackOpenShadowSize.X, liveTrackOpenShadowSize.Y),
			Position = getLiveTrackShadowPosition(liveTrackCurrentPosition, 20)
		}):Play()
		tweenService:Create(liveTrackParts.OuterShadow, panelFadeTween, {
			ImageTransparency = 0.78
		}):Play()
	end

	task.delay(0.1, function()
		if token == liveTrackState.token and liveTrackState.active and liveTrackPanel then
			tweenService:Create(liveTrackPanel, contentTween, {GroupTransparency = 0}):Play()
		end
	end)

		liveTrackState.renderConnection = runService.RenderStepped:Connect(function(deltaTime)
			if token ~= liveTrackState.token or not liveTrackState.active then return end
			updateLiveTrackReadout(targetPlayer, deltaTime)
		end)

	liveTrackState.characterConnection = targetPlayer.CharacterAdded:Connect(function()
		task.wait(0.2)
		if token == liveTrackState.token and liveTrackState.active and liveTrackState.targetPlayer == targetPlayer then
			updateLiveTrackReadout(targetPlayer)
		end
	end)

	liveTrackState.removingConnection = players.PlayerRemoving:Connect(function(removingPlayer)
			if removingPlayer == targetPlayer then
				_picker.closeLiveTrackPanel(false)
				queueNotification("Live Track Ended", targetPlayer.DisplayName.." left the server.")
			end
		end)
	end

function _picker.createTrackingBadge(targetPlayer)
	if not targetPlayer or targetPlayer == localPlayer or not players:FindFirstChild(targetPlayer.Name) then return end

	ensureTrackingFolder()

	if trackingBadges[targetPlayer] then
		local root = _picker.getTrackingRoot(targetPlayer)
		if root then
			trackingBadges[targetPlayer].Adornee = root
		end
		return trackingBadges[targetPlayer]
	end

	local root = _picker.getTrackingRoot(targetPlayer)
	if not root then
		queueNotification("Track Unavailable", "Sirius cannot find "..targetPlayer.DisplayName.."'s character right now.")
		return
	end

		local badge = Instance.new("BillboardGui")
		badge.Name = "SiriusTrackBadge_"..targetPlayer.Name
		badge.Adornee = root
		badge.AlwaysOnTop = true
		badge.Size = UDim2.fromOffset(100, 100)
		badge.StudsOffsetWorldSpace = Vector3.new(0, 5, 0)
		badge.LightInfluence = 1
		badge.MaxDistance = math.huge
		badge.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
		badge.Parent = trackingFolder

		local shadow = Instance.new("ImageLabel")
		shadow.Name = "Shadow"
		shadow.AnchorPoint = Vector2.new(0.5, 0.5)
		shadow.Position = UDim2.fromScale(0.5, 0.5)
		shadow.Size = UDim2.fromOffset(70, 70)
		shadow.BackgroundTransparency = 1
		shadow.Image = "rbxassetid://122971705612181"
		shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
		shadow.ImageTransparency = 1
		shadow.ScaleType = Enum.ScaleType.Fit
		shadow.ZIndex = 1
		shadow.Parent = badge

		local shadowScale = Instance.new("UIScale")
		shadowScale.Name = "scale"
		shadowScale.Scale = 1
		shadowScale.Parent = shadow

		local button = Instance.new("ImageButton")
		button.Name = "Badge"
		button.AnchorPoint = Vector2.new(0.5, 0.5)
		button.Position = UDim2.fromScale(0.5, 0.5)
		button.Size = UDim2.fromOffset(50, 50)
		button.BackgroundTransparency = 1
		button.BorderSizePixel = 0
		button.Image = "rbxassetid://92562308197003"
		button.ImageColor3 = Color3.fromRGB(27, 27, 27)
		button.ImageTransparency = 0
		button.ScaleType = Enum.ScaleType.Slice
		button.SliceCenter = Rect.new(50, 50, 50, 50)
		button.AutoButtonColor = false
		button.ZIndex = 2
		button.Parent = shadow

		local buttonScale = Instance.new("UIScale")
		buttonScale.Scale = 0.6
		buttonScale.Parent = button

		local photo = Instance.new("ImageLabel")
		photo.Name = "Photo"
		photo.AnchorPoint = Vector2.new(0.5, 0.5)
		photo.Position = UDim2.fromScale(0.5, 0.5)
		photo.Size = UDim2.fromOffset(30, 30)
		photo.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
		photo.BackgroundTransparency = 0.9
		photo.BorderSizePixel = 0
		photo.Image = "https://www.roblox.com/headshot-thumbnail/image?userId="..targetPlayer.UserId.."&width=420&height=420&format=png"
		photo.ZIndex = 3
		photo.Parent = button

		local photoCorner = Instance.new("UICorner")
		photoCorner.CornerRadius = UDim.new(1, 0)
		photoCorner.Parent = photo

		button.MouseEnter:Connect(function()
			tweenService:Create(shadow, TweenInfo.new(0.2, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {ImageTransparency = 0.18, Size = UDim2.fromOffset(82, 82)}):Play()
			tweenService:Create(shadowScale, TweenInfo.new(0.2, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Scale = 1.08}):Play()
			tweenService:Create(buttonScale, TweenInfo.new(0.2, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Scale = 1.08}):Play()
			tweenService:Create(photo, TweenInfo.new(0.2, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Size = UDim2.fromOffset(33, 33)}):Play()
		end)

		button.MouseLeave:Connect(function()
			tweenService:Create(shadow, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {ImageTransparency = 1, Size = UDim2.fromOffset(70, 70)}):Play()
			tweenService:Create(shadowScale, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Scale = 1}):Play()
			tweenService:Create(buttonScale, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Scale = 1}):Play()
			tweenService:Create(photo, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Size = UDim2.fromOffset(30, 30)}):Play()
		end)

	button.MouseButton1Click:Connect(function()
		tweenService:Create(buttonScale, TweenInfo.new(0.08, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Scale = 0.94}):Play()
		task.delay(0.08, function()
			if trackingBadges[targetPlayer] then
				tweenService:Create(buttonScale, TweenInfo.new(0.12, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {Scale = 1.06}):Play()
				_picker.openLiveTrackPanel(targetPlayer)
			end
		end)
	end)

	trackingBadges[targetPlayer] = badge
	trackingConnections[targetPlayer] = {
		targetPlayer.CharacterAdded:Connect(function()
			task.wait(0.2)
			local newRoot = _picker.getTrackingRoot(targetPlayer)
			if newRoot and trackingBadges[targetPlayer] then
				trackingBadges[targetPlayer].Adornee = newRoot
			end
		end),
		players.PlayerRemoving:Connect(function(removingPlayer)
			if removingPlayer == targetPlayer then
				_picker.destroyTrackingBadge(targetPlayer)
				if liveTrackState.targetPlayer == targetPlayer then
					_picker.closeLiveTrackPanel()
				end
			end
		end)
	}

	tweenService:Create(buttonScale, TweenInfo.new(0.24, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {Scale = 1}):Play()

	return badge
end

function _picker.startTrackingPlayer(targetPlayer)
	if not targetPlayer or targetPlayer == localPlayer or not players:FindFirstChild(targetPlayer.Name) then return end

	if trackingBadges[targetPlayer] then
		_picker.destroyTrackingBadge(targetPlayer)
		if liveTrackState.targetPlayer == targetPlayer then
			_picker.closeLiveTrackPanel()
		end
		queueNotification("Tracking Disabled", targetPlayer.DisplayName.."'s marker was removed.")
		return
	end

	_picker.createTrackingBadge(targetPlayer)
	queueNotification("Tracking Enabled", targetPlayer.DisplayName.." now has a live marker.")
end

function _picker.startLiveTrackingPlayer(targetPlayer, returnToPicker)
	if not targetPlayer or targetPlayer == localPlayer or not players:FindFirstChild(targetPlayer.Name) then return end

	_picker.createTrackingBadge(targetPlayer)
	_picker.openLiveTrackPanel(targetPlayer, returnToPicker)
end

function _picker.setPlayerEsp(targetPlayer, enabled)
	if not targetPlayer or targetPlayer == localPlayer then return false end

	locatedPlayers[targetPlayer.Name] = enabled == true or nil

	local highlight = espContainer and espContainer:FindFirstChild(targetPlayer.Name)
	if not highlight then
		createEsp(targetPlayer)
		highlight = espContainer and espContainer:FindFirstChild(targetPlayer.Name)
	end

	if highlight then
		highlight.Enabled = isHighlightEnabledFor(targetPlayer.Name)
	end

	return locatedPlayers[targetPlayer.Name] == true
end

function _picker.togglePlayerEsp(targetPlayer)
	if not targetPlayer or targetPlayer == localPlayer or not players:FindFirstChild(targetPlayer.Name) then return false end

	local enabled = _picker.setPlayerEsp(targetPlayer, locatedPlayers[targetPlayer.Name] ~= true)
	queueNotification(
		enabled and "ESP Enabled" or "ESP Disabled",
		enabled and (targetPlayer.DisplayName.." is now highlighted.") or (targetPlayer.DisplayName.."'s personal highlight was removed.")
	)
	return enabled
end

function _picker.updatePlayerActionPickerSelection()
	return
end

	function _picker.closePlayerActionPicker(skipRestore, afterClose)
		if type(skipRestore) == "function" then
			afterClose = skipRestore
			skipRestore = nil
		end
		if not playerActionPicker or not playerActionPickerState.open then return end

		playerActionPickerState.token += 1
		local token = playerActionPickerState.token
		playerActionPickerState.open = false
		playerActionPickerState.targetPlayer = nil
		playerActionPickerState.restorePanel = nil

			local panel = playerActionPickerParts.Panel
			local contentFadeTween = TweenInfo.new(0.14, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
			local panelCloseTween = TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut)
			local panelFadeTween = TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.In)
			local currentWidth = panel.AbsoluteSize.X > 0 and panel.AbsoluteSize.X or playerActionPickerCurrentSize.X
			local closeWidth = math.max(260, currentWidth - 28)
			local closeSize = UDim2.fromOffset(closeWidth, 18)
			local closeShadowSize = UDim2.fromOffset(closeWidth + 40, 56)

			if playerActionPickerParts.Content then
				tweenService:Create(playerActionPickerParts.Content, contentFadeTween, {
					GroupTransparency = 1
				}):Play()
			end
			tweenService:Create(panel, panelCloseTween, {
				Size = closeSize
			}):Play()
			tweenService:Create(panel, panelFadeTween, {
				BackgroundTransparency = 1
			}):Play()
			if playerActionPickerParts.Shadow then
				tweenService:Create(playerActionPickerParts.Shadow, panelCloseTween, {
					Position = UDim2.new(0.5, 0, 0.5, 16),
					Size = closeShadowSize
				}):Play()
				tweenService:Create(playerActionPickerParts.Shadow, panelFadeTween, {
					ImageTransparency = 1
				}):Play()
			end
			if playerActionPickerParts.Stroke then
				tweenService:Create(playerActionPickerParts.Stroke, panelFadeTween, {Transparency = 1}):Play()
			end
			if playerActionPickerParts.PanelScale then
				tweenService:Create(playerActionPickerParts.PanelScale, panelCloseTween, {Scale = 1}):Play()
			end

			task.delay(0.32, function()
	                if token == playerActionPickerState.token and playerActionPicker then
	                    playerActionPicker.Visible = false
					panel.Size = UDim2.fromOffset(playerActionPickerCurrentSize.X, playerActionPickerCurrentSize.Y)
					panel.BackgroundTransparency = 0
					if playerActionPickerParts.PanelScale then
						playerActionPickerParts.PanelScale.Scale = 1
					end
	                    if afterClose then
						afterClose()
					end
				end
		end)
	end

	function _picker.closePlayerActionPickerToSpectate(targetPlayer)
		if not targetPlayer or not players:FindFirstChild(targetPlayer.Name) then return end
		if spectateMorphState.active then return end
		if not playerActionPicker or not playerActionPickerState.open then
			startSpectating(targetPlayer, false)
			return
		end

		ensureSpectateBar()
		spectateBar.Visible = false
		resetSpectateBarGeometry()

		playerActionPickerState.token += 1
		local token = playerActionPickerState.token
		playerActionPickerState.open = false
		playerActionPickerState.targetPlayer = nil
		playerActionPickerState.restorePanel = nil

		local panel = playerActionPickerParts.Panel
		local content = playerActionPickerParts.Content
		local targetPosition, targetSize = getSpectateTogglePickerGeometry()
		local fadeDuration = 0.08
		local morphDuration = 0.26
		local startPosition = panel.Position
		local startSize = Vector2.new(panel.AbsoluteSize.X, panel.AbsoluteSize.Y)
		if startSize.X <= 0 or startSize.Y <= 0 then
			startPosition, startSize = getPlayerActionPickerGeometry()
		end

		if content then
			tweenService:Create(content, TweenInfo.new(fadeDuration, Enum.EasingStyle.Sine, Enum.EasingDirection.In), {
				GroupTransparency = 1
			}):Play()
		end

		task.delay(fadeDuration + 0.01, function()
			if token ~= playerActionPickerState.token or not playerActionPicker then return end

			playerActionPicker.Visible = false
			panel.Position = UDim2.new(0.5, 0, 0.5, 0)
			panel.Size = UDim2.fromOffset(playerActionPickerCurrentSize.X, playerActionPickerCurrentSize.Y)
			panel.BackgroundColor3 = siriusValues.selectActionStyle.panel
			panel.BackgroundTransparency = 0
			if playerActionPickerParts.Stroke then
				playerActionPickerParts.Stroke.Transparency = 0.18
			end
			if playerActionPickerParts.Shadow then
				playerActionPickerParts.Shadow.Position = UDim2.new(0.5, 0, 0.5, 12)
				playerActionPickerParts.Shadow.Size = UDim2.fromOffset(playerActionPickerCurrentSize.X + 58, playerActionPickerCurrentSize.Y + 58)
				playerActionPickerParts.Shadow.ImageTransparency = 0.82
			end
			if playerActionPickerParts.PanelScale then
				playerActionPickerParts.PanelScale.Scale = 1
			end

			playSpectateGhostMorph(startPosition, startSize, targetPosition, targetSize, morphDuration, "to-toggle", function()
				if token ~= playerActionPickerState.token then return end
				if targetPlayer and players:FindFirstChild(targetPlayer.Name) then
					startSpectating(targetPlayer, true)
				end
			end)
		end)
	end

	playerActionPickerParts.TeleportCallback = function(targetPlayer)
		if targetPlayer and players:FindFirstChild(targetPlayer.Name) then
			return playerActionPickerParts.TeleportToSelected(targetPlayer)
		end

		return false
	end

local function ensurePlayerActionPicker()
	if playerActionPicker then return playerActionPicker end

	do
		playerActionPicker = Instance.new("Frame")
		playerActionPicker.Name = "SiriusPlayerActionPicker"
		playerActionPicker.Size = UDim2.new(1, 0, 1, 0)
		playerActionPicker.BackgroundTransparency = 1
		playerActionPicker.BorderSizePixel = 0
		playerActionPicker.Visible = false
		playerActionPicker.ZIndex = 68
		playerActionPicker.Parent = UI

		local panel = Instance.new("Frame")
		panel.Name = "SelectActionsPanel"
		panel.AnchorPoint = Vector2.new(0.5, 0.5)
		panel.Position = UDim2.new(0.5, 0, 0.5, 0)
		panel.Size = UDim2.fromOffset(390, 520)
		panel.BackgroundColor3 = siriusValues.selectActionStyle.panel
		panel.BackgroundTransparency = 0
		panel.BorderSizePixel = 0
		panel.ClipsDescendants = true
		panel.Active = true
		panel.ZIndex = 69
		panel.Parent = playerActionPicker

		local panelScale = Instance.new("UIScale")
		panelScale.Name = "scale"
		panelScale.Scale = 1
		panelScale.Parent = panel

		siriusValues.addSelectActionCorner(panel, siriusValues.selectActionStyle.panelCorner)

		local panelStroke = Instance.new("UIStroke")
		panelStroke.Name = "stroke"
		panelStroke.Color = siriusValues.selectActionStyle.stroke
		panelStroke.Thickness = 1
		panelStroke.Transparency = 0.18
		panelStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
		panelStroke.Parent = panel

		local panelGradient = Instance.new("UIGradient")
		panelGradient.Rotation = 105
		panelGradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, siriusValues.selectActionStyle.panel),
			ColorSequenceKeypoint.new(0.62, siriusValues.selectActionStyle.panel),
			ColorSequenceKeypoint.new(1, siriusValues.selectActionStyle.panelDepth)
		})
		panelGradient.Parent = panel

		local bottomGlow = Instance.new("Frame")
		bottomGlow.Name = "BottomGlow"
		bottomGlow.AnchorPoint = Vector2.new(0.5, 1)
		bottomGlow.Position = UDim2.new(0.5, 0, 1, 0)
		bottomGlow.Size = UDim2.new(1, 0, 0, 58)
		bottomGlow.BackgroundColor3 = siriusValues.selectActionStyle.panel
		bottomGlow.BackgroundTransparency = 1
		bottomGlow.BorderSizePixel = 0
		bottomGlow.ZIndex = 70
		bottomGlow.Parent = panel

		local bottomGlowGradient = Instance.new("UIGradient")
		bottomGlowGradient.Rotation = -90
		bottomGlowGradient.Transparency = NumberSequence.new({
			NumberSequenceKeypoint.new(0, 0.16),
			NumberSequenceKeypoint.new(0.55, 0.72),
			NumberSequenceKeypoint.new(1, 1)
		})
		bottomGlowGradient.Parent = bottomGlow

		local shadow = Instance.new("ImageLabel")
		shadow.Name = "Shadow"
		shadow.AnchorPoint = Vector2.new(0.5, 0.5)
		shadow.Position = UDim2.new(0.5, 0, 0.5, 14)
		shadow.Size = UDim2.fromOffset(430, 560)
		shadow.BackgroundTransparency = 1
		shadow.Image = "rbxassetid://8992230677"
		shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
		shadow.ImageTransparency = 1
		shadow.ScaleType = Enum.ScaleType.Slice
		shadow.SliceCenter = Rect.new(99, 99, 99, 99)
		shadow.ZIndex = 68
		shadow.Parent = playerActionPicker

		local content = Instance.new("CanvasGroup")
		content.Name = "content"
		content.Size = UDim2.new(1, 0, 1, 0)
		content.BackgroundTransparency = 1
		content.GroupTransparency = 1
		content.ZIndex = 70
		content.Parent = panel

		local dragArea = Instance.new("Frame")
		dragArea.Name = "DragArea"
		dragArea.Position = UDim2.new(0, 0, 0, 0)
		dragArea.Size = UDim2.new(1, -74, 0, 60)
		dragArea.BackgroundTransparency = 1
		dragArea.BorderSizePixel = 0
		dragArea.Active = true
		dragArea.ZIndex = 86
		dragArea.Parent = content

		local title = Instance.new("TextLabel")
		title.Name = "Title"
		title.Position = UDim2.new(0, 26, 0, 24)
		title.Size = UDim2.new(1, -110, 0, 30)
		title.BackgroundTransparency = 1
		title.Text = ""
		title.TextColor3 = siriusValues.selectActionStyle.text
		title.TextSize = 24
		title.TextXAlignment = Enum.TextXAlignment.Left
		title.TextYAlignment = Enum.TextYAlignment.Center
		title.FontFace = Font.new("rbxassetid://12187365364", Enum.FontWeight.Bold)
		title.Visible = false
		title.ZIndex = 73
		title.Parent = content

		local close = Instance.new("TextButton")
		close.Name = "Close"
		close.AnchorPoint = Vector2.new(1, 0)
		close.Position = UDim2.new(1, -18, 0, 14)
		close.Size = UDim2.new(0, 34, 0, 34)
		close.BackgroundColor3 = Color3.fromRGB(32, 32, 32)
		close.BackgroundTransparency = 1
		close.BorderSizePixel = 0
		close.Text = "×"
		close.TextColor3 = Color3.fromRGB(220, 220, 220)
		close.TextTransparency = 0.08
		close.TextSize = 24
		close.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium)
		close.AutoButtonColor = false
		close.ZIndex = 82
		close.Parent = content

		close.MouseEnter:Connect(function()
			tweenService:Create(close, TweenInfo.new(0.16, Enum.EasingStyle.Quint), {
				BackgroundTransparency = 1,
				TextColor3 = Color3.fromRGB(255, 255, 255),
				TextTransparency = 0
			}):Play()
		end)

		close.MouseLeave:Connect(function()
			tweenService:Create(close, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {
				BackgroundTransparency = 1,
				TextColor3 = Color3.fromRGB(220, 220, 220),
				TextTransparency = 0.08
			}):Play()
		end)

		close.MouseButton1Click:Connect(_picker.closePlayerActionPicker)

		local profile = {}
		profile.Card = Instance.new("Frame")
		profile.Card.Name = "ProfileCard"
		profile.Card.Position = UDim2.new(0, 26, 0, 18)
		profile.Card.Size = UDim2.new(1, -52, 0, 150)
		profile.Card.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
		profile.Card.BackgroundTransparency = 1
		profile.Card.BorderSizePixel = 0
		profile.Card.ZIndex = 71
		profile.Card.Parent = content

		do
			siriusValues.addSelectActionCorner(profile.Card, siriusValues.selectActionStyle.panelCorner)

			local stroke = Instance.new("UIStroke")
			stroke.Name = "stroke"
			stroke.Color = siriusValues.selectActionStyle.stroke
			stroke.Thickness = 1
			stroke.Transparency = 0.72
			stroke.Parent = profile.Card

			profile.Stroke = stroke
		end

		profile.Avatar = Instance.new("ImageLabel")
		profile.Avatar.Name = "Avatar"
		profile.Avatar.Position = UDim2.new(0, 6, 0, 4)
		profile.Avatar.Size = UDim2.fromOffset(34, 34)
		profile.Avatar.BackgroundTransparency = 1
		profile.Avatar.ImageTransparency = 0
		profile.Avatar.ScaleType = Enum.ScaleType.Crop
		profile.Avatar.ZIndex = 72
		profile.Avatar.Parent = profile.Card

		do
			siriusValues.addSelectActionCorner(profile.Avatar, nil, 1)

			local avatarStroke = Instance.new("UIStroke")
			avatarStroke.Name = "stroke"
			avatarStroke.Color = Color3.fromRGB(70, 70, 70)
			avatarStroke.Thickness = 1
			avatarStroke.Transparency = 0.48
			avatarStroke.Parent = profile.Avatar

			profile.AvatarStroke = avatarStroke
		end

		profile.Name = Instance.new("TextLabel")
		profile.Name.Name = "DisplayName"
		profile.Name.Position = UDim2.new(0, 50, 0, 5)
		profile.Name.Size = UDim2.new(1, -118, 0, 18)
		profile.Name.BackgroundTransparency = 1
		profile.Name.Text = "Player"
		profile.Name.TextColor3 = siriusValues.selectActionStyle.text
		profile.Name.TextSize = 13
		profile.Name.TextXAlignment = Enum.TextXAlignment.Left
		profile.Name.TextTruncate = Enum.TextTruncate.AtEnd
		profile.Name.FontFace = Font.new("rbxassetid://12187365364", Enum.FontWeight.Bold)
		profile.Name.ZIndex = 72
		profile.Name.Parent = profile.Card

		profile.Handle = Instance.new("TextLabel")
		profile.Handle.Name = "Handle"
		profile.Handle.Position = UDim2.new(0, 50, 0, 23)
		profile.Handle.Size = UDim2.new(1, -118, 0, 14)
		profile.Handle.BackgroundTransparency = 1
		profile.Handle.Text = "@Player"
		profile.Handle.TextColor3 = siriusValues.selectActionStyle.muted
		profile.Handle.TextSize = 10
		profile.Handle.TextXAlignment = Enum.TextXAlignment.Left
		profile.Handle.TextTruncate = Enum.TextTruncate.AtEnd
		profile.Handle.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium)
		profile.Handle.ZIndex = 72
		profile.Handle.Parent = profile.Card

		profile.PlaytimeCard = Instance.new("Frame")
		profile.PlaytimeCard.Name = "Playtime"
		profile.PlaytimeCard.Position = UDim2.new(0, 0, 0, 50)
		profile.PlaytimeCard.Size = UDim2.new(0.5, -5, 0, 44)
		profile.PlaytimeCard.BackgroundColor3 = siriusValues.selectActionStyle.row
		profile.PlaytimeCard.BackgroundTransparency = 0.04
		profile.PlaytimeCard.BorderSizePixel = 0
		profile.PlaytimeCard.ZIndex = 72
		profile.PlaytimeCard.Parent = profile.Card

		do
			siriusValues.addSelectActionCorner(profile.PlaytimeCard, siriusValues.selectActionStyle.rowCorner)
			siriusValues.addSelectActionStroke(profile.PlaytimeCard, siriusValues.selectActionStyle.stroke, 0.62)
		end

		profile.PlaytimeLabel = Instance.new("TextLabel")
		profile.PlaytimeLabel.Name = "Label"
		profile.PlaytimeLabel.Position = UDim2.new(0, 10, 0, 6)
		profile.PlaytimeLabel.Size = UDim2.new(1, -20, 0, 12)
		profile.PlaytimeLabel.BackgroundTransparency = 1
		profile.PlaytimeLabel.Text = "Playtime"
		profile.PlaytimeLabel.TextColor3 = Color3.fromRGB(112, 112, 112)
		profile.PlaytimeLabel.TextSize = 9
		profile.PlaytimeLabel.TextXAlignment = Enum.TextXAlignment.Left
		profile.PlaytimeLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium)
		profile.PlaytimeLabel.ZIndex = 73
		profile.PlaytimeLabel.Parent = profile.PlaytimeCard

		profile.PlaytimeValue = Instance.new("TextLabel")
		profile.PlaytimeValue.Name = "Value"
		profile.PlaytimeValue.Position = UDim2.new(0, 10, 0, 19)
		profile.PlaytimeValue.Size = UDim2.new(1, -20, 0, 18)
		profile.PlaytimeValue.BackgroundTransparency = 1
		profile.PlaytimeValue.Text = "Just Joined"
		profile.PlaytimeValue.TextColor3 = siriusValues.selectActionStyle.text
		profile.PlaytimeValue.TextSize = 13
		profile.PlaytimeValue.TextXAlignment = Enum.TextXAlignment.Left
		profile.PlaytimeValue.TextTruncate = Enum.TextTruncate.AtEnd
		profile.PlaytimeValue.FontFace = Font.new("rbxassetid://12187365364", Enum.FontWeight.Bold)
		profile.PlaytimeValue.ZIndex = 73
		profile.PlaytimeValue.Parent = profile.PlaytimeCard

		profile.AccountAgeCard = Instance.new("Frame")
		profile.AccountAgeCard.Name = "AccountAge"
		profile.AccountAgeCard.Position = UDim2.new(0.5, 5, 0, 50)
		profile.AccountAgeCard.Size = UDim2.new(0.5, -5, 0, 44)
		profile.AccountAgeCard.BackgroundColor3 = siriusValues.selectActionStyle.row
		profile.AccountAgeCard.BackgroundTransparency = 0.04
		profile.AccountAgeCard.BorderSizePixel = 0
		profile.AccountAgeCard.ZIndex = 72
		profile.AccountAgeCard.Parent = profile.Card

		do
			siriusValues.addSelectActionCorner(profile.AccountAgeCard, siriusValues.selectActionStyle.rowCorner)
			siriusValues.addSelectActionStroke(profile.AccountAgeCard, siriusValues.selectActionStyle.stroke, 0.62)
		end

		profile.AccountAgeLabel = Instance.new("TextLabel")
		profile.AccountAgeLabel.Name = "Label"
		profile.AccountAgeLabel.Position = UDim2.new(0, 10, 0, 6)
		profile.AccountAgeLabel.Size = UDim2.new(1, -20, 0, 12)
		profile.AccountAgeLabel.BackgroundTransparency = 1
		profile.AccountAgeLabel.Text = "Account Age"
		profile.AccountAgeLabel.TextColor3 = Color3.fromRGB(112, 112, 112)
		profile.AccountAgeLabel.TextSize = 9
		profile.AccountAgeLabel.TextXAlignment = Enum.TextXAlignment.Left
		profile.AccountAgeLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium)
		profile.AccountAgeLabel.ZIndex = 73
		profile.AccountAgeLabel.Parent = profile.AccountAgeCard

		profile.AccountAgeValue = Instance.new("TextLabel")
		profile.AccountAgeValue.Name = "Value"
		profile.AccountAgeValue.Position = UDim2.new(0, 10, 0, 19)
		profile.AccountAgeValue.Size = UDim2.new(1, -20, 0, 18)
		profile.AccountAgeValue.BackgroundTransparency = 1
		profile.AccountAgeValue.Text = "0d"
		profile.AccountAgeValue.TextColor3 = siriusValues.selectActionStyle.text
		profile.AccountAgeValue.TextSize = 13
		profile.AccountAgeValue.TextXAlignment = Enum.TextXAlignment.Left
		profile.AccountAgeValue.TextTruncate = Enum.TextTruncate.AtEnd
		profile.AccountAgeValue.FontFace = Font.new("rbxassetid://12187365364", Enum.FontWeight.Bold)
		profile.AccountAgeValue.ZIndex = 73
		profile.AccountAgeValue.Parent = profile.AccountAgeCard

		profile.RoleCard = Instance.new("Frame")
		profile.RoleCard.Name = "Role"
		profile.RoleCard.Position = UDim2.new(0, 0, 0, 104)
		profile.RoleCard.Size = UDim2.new(1, 0, 0, 46)
		profile.RoleCard.BackgroundColor3 = siriusValues.selectActionStyle.row
		profile.RoleCard.BackgroundTransparency = 0.04
		profile.RoleCard.BorderSizePixel = 0
		profile.RoleCard.ZIndex = 72
		profile.RoleCard.Parent = profile.Card

		do
			siriusValues.addSelectActionCorner(profile.RoleCard, siriusValues.selectActionStyle.rowCorner)
			siriusValues.addSelectActionStroke(profile.RoleCard, siriusValues.selectActionStyle.stroke, 0.62)
		end

		profile.RoleLabel = Instance.new("TextLabel")
		profile.RoleLabel.Name = "Label"
		profile.RoleLabel.Position = UDim2.new(0, 10, 0, 7)
		profile.RoleLabel.Size = UDim2.new(1, -20, 0, 12)
		profile.RoleLabel.BackgroundTransparency = 1
		profile.RoleLabel.Text = "Team Role"
		profile.RoleLabel.TextColor3 = Color3.fromRGB(112, 112, 112)
		profile.RoleLabel.TextSize = 9
		profile.RoleLabel.TextXAlignment = Enum.TextXAlignment.Left
		profile.RoleLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium)
		profile.RoleLabel.ZIndex = 73
		profile.RoleLabel.Parent = profile.RoleCard

		profile.RoleValue = Instance.new("TextLabel")
		profile.RoleValue.Name = "Value"
		profile.RoleValue.Position = UDim2.new(0, 10, 0, 21)
		profile.RoleValue.Size = UDim2.new(1, -20, 0, 18)
		profile.RoleValue.BackgroundTransparency = 1
		profile.RoleValue.Text = "Neutral"
		profile.RoleValue.TextColor3 = siriusValues.selectActionStyle.text
		profile.RoleValue.TextSize = 13
		profile.RoleValue.TextXAlignment = Enum.TextXAlignment.Left
		profile.RoleValue.TextTruncate = Enum.TextTruncate.AtEnd
		profile.RoleValue.FontFace = Font.new("rbxassetid://12187365364", Enum.FontWeight.Bold)
		profile.RoleValue.ZIndex = 73
		profile.RoleValue.Parent = profile.RoleCard

		local pickerDragging = false
		local pickerResizing = false
		local dragStart
		local dragStartPosition
		local activeResizeInput
		local resizeHandleHover = false
		local resizeStartPointer
		local resizePointerPosition
		local resizeStartSize
		local resizeStartCenter
		local activeResizeIcon
		local resizePanelTween
		local resizeShadowTween
		local resizeSmoothConnection
		local resizeTargetCenter
		local resizeTargetSize
		local resizeVisualCenter
		local resizeVisualSize
		local resizeGuiInsetOffset = Vector2.zero
		local resetActiveResizeIcon = function() end
		local resizeStyle = {
			idleSize = UDim2.fromOffset(18, 18),
			baseSize = 20,
			growRange = 30,
			idleTransparency = 0.72,
			hoverTransparency = 0,
			pressTransparency = 0,
			color = Color3.fromRGB(59, 59, 59),
			pressColor = Color3.fromRGB(82, 82, 82),
			followSpeed = 26,
			settleSpeed = 18,
		}

		local function getPickerRootSize()
			local rootSize = camera.ViewportSize
			pcall(function()
				if UI.AbsoluteSize.X > 0 and UI.AbsoluteSize.Y > 0 then
					rootSize = UI.AbsoluteSize
				end
			end)
			return rootSize
		end

		local function isDragInput(input)
			return input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch
		end

		local function syncPickerShadow()
			if not shadow or not panel then return end
			shadow.Position = UDim2.fromOffset(panel.AbsolutePosition.X + panel.AbsoluteSize.X * 0.5, panel.AbsolutePosition.Y + panel.AbsoluteSize.Y * 0.5 + 16)
			shadow.Size = UDim2.fromOffset(panel.AbsoluteSize.X + 76, panel.AbsoluteSize.Y + 78)
		end

		local function lerpVector2(from, to, alpha)
			return from + (to - from) * alpha
		end

		local function refreshPickerGuiInsetOffset()
			resizeGuiInsetOffset = Vector2.zero
			local ownerGui = panel:FindFirstAncestorWhichIsA("ScreenGui")
			if ownerGui and ownerGui.IgnoreGuiInset then
				local inset = guiService:GetGuiInset()
				resizeGuiInsetOffset += inset
			end
		end

		local function pointerVectorFromInput(input)
			if input and input.UserInputType == Enum.UserInputType.Touch then
				return Vector2.new(input.Position.X, input.Position.Y) + resizeGuiInsetOffset
			end

			return userInputService:GetMouseLocation() + resizeGuiInsetOffset
		end

		local function currentResizePointer()
			local mouseLocation = userInputService:GetMouseLocation()
			local guiInset = guiService:GetGuiInset()
			return Vector2.new(mouseLocation.X, mouseLocation.Y - guiInset.Y)
		end

		local function resizeVisualPointerFromInput(input)
			if input and input.UserInputType == Enum.UserInputType.Touch then
				local guiInset = guiService:GetGuiInset()
				return Vector2.new(input.Position.X, input.Position.Y - guiInset.Y)
			end

			return currentResizePointer()
		end

		local function applyPickerResizeGeometry(center, size, animated)
			size = clampPlayerActionPickerSize(size)
			playerActionPickerCurrentSize = size
			if resizePanelTween then
				resizePanelTween:Cancel()
				resizePanelTween = nil
			end
			if resizeShadowTween then
				resizeShadowTween:Cancel()
				resizeShadowTween = nil
			end

			local panelGoal = {
				Position = UDim2.fromOffset(center.X, center.Y),
				Size = UDim2.fromOffset(size.X, size.Y)
			}
			local shadowGoal = {
				Position = UDim2.fromOffset(center.X, center.Y + 16),
				Size = UDim2.fromOffset(size.X + 76, size.Y + 78)
			}

			if animated then
				local resizeInfo = TweenInfo.new(0.045, Enum.EasingStyle.Sine, Enum.EasingDirection.Out)
				resizePanelTween = tweenService:Create(panel, resizeInfo, panelGoal)
				resizePanelTween:Play()
				if shadow then
					resizeShadowTween = tweenService:Create(shadow, resizeInfo, shadowGoal)
					resizeShadowTween:Play()
				end
			else
				panel.Position = panelGoal.Position
				panel.Size = panelGoal.Size
				if shadow then
					shadow.Position = shadowGoal.Position
					shadow.Size = shadowGoal.Size
				end
			end
		end

		local function stopPickerResizeSmoothing(applyFinal)
			if resizeSmoothConnection then
				resizeSmoothConnection:Disconnect()
				resizeSmoothConnection = nil
			end
			if applyFinal and resizeTargetCenter and resizeTargetSize then
				applyPickerResizeGeometry(resizeTargetCenter, resizeTargetSize, false)
			end
			resizeTargetCenter = nil
			resizeTargetSize = nil
			resizeVisualCenter = nil
			resizeVisualSize = nil
		end

		local function startPickerResizeSmoothing()
			if resizeSmoothConnection then return end
			resizeSmoothConnection = runService.RenderStepped:Connect(function(deltaTime)
				if not resizeTargetCenter or not resizeTargetSize then
					stopPickerResizeSmoothing(false)
					return
				end
				resizeVisualCenter = resizeVisualCenter or resizeTargetCenter
				resizeVisualSize = resizeVisualSize or resizeTargetSize
				local speed = pickerResizing and resizeStyle.followSpeed or resizeStyle.settleSpeed
				local alpha = math.clamp(1 - math.exp(-deltaTime * speed), 0, 1)
				resizeVisualCenter = lerpVector2(resizeVisualCenter, resizeTargetCenter, alpha)
				resizeVisualSize = lerpVector2(resizeVisualSize, resizeTargetSize, alpha)
				if (resizeVisualSize - resizeTargetSize).Magnitude <= 0.75 then
					resizeVisualCenter = resizeTargetCenter
					resizeVisualSize = resizeTargetSize
					applyPickerResizeGeometry(resizeVisualCenter, resizeVisualSize, false)
					if not pickerResizing then
						stopPickerResizeSmoothing(false)
					end
					return
				end
				applyPickerResizeGeometry(resizeVisualCenter, resizeVisualSize, false)
			end)
		end

		dragArea.InputBegan:Connect(function(input, processed)
			if processed or not isDragInput(input) then return end
			if pickerResizing then return end
			pickerDragging = true
			dragStart = Vector2.new(input.Position.X, input.Position.Y)
			dragStartPosition = panel.Position
		end)

		userInputService.InputChanged:Connect(function(input)
			if not pickerDragging or not playerActionPickerState.open then return end
			if input.UserInputType ~= Enum.UserInputType.MouseMovement and input.UserInputType ~= Enum.UserInputType.Touch then return end
			if not dragStart or not dragStartPosition then return end

			local rootSize = getPickerRootSize()
			local panelSize = panel.AbsoluteSize
			local delta = Vector2.new(input.Position.X, input.Position.Y) - dragStart
			local startCenter = Vector2.new(dragStartPosition.X.Offset, dragStartPosition.Y.Offset)

			if dragStartPosition.X.Scale ~= 0 or dragStartPosition.Y.Scale ~= 0 then
				startCenter = Vector2.new(rootSize.X * dragStartPosition.X.Scale + dragStartPosition.X.Offset, rootSize.Y * dragStartPosition.Y.Scale + dragStartPosition.Y.Offset)
			end

			local nextCenter = startCenter + delta
			nextCenter = Vector2.new(
				math.clamp(nextCenter.X, panelSize.X * 0.5 - 80, rootSize.X - panelSize.X * 0.5 + 80),
				math.clamp(nextCenter.Y, panelSize.Y * 0.5 - 40, rootSize.Y - panelSize.Y * 0.5 + 80)
			)
			panel.Position = UDim2.fromOffset(nextCenter.X, nextCenter.Y)
			syncPickerShadow()
		end)

		userInputService.InputEnded:Connect(function(input)
			if not isDragInput(input) then return end
			if activeResizeInput and activeResizeInput.UserInputType == Enum.UserInputType.Touch and input ~= activeResizeInput then
				return
			end
			pickerDragging = false
			if pickerResizing then
				pickerResizing = false
				activeResizeInput = nil
				if resizeHandleHover then
					if activeResizeIcon then
						local pointer = currentResizePointer()
						local handleSize = activeResizeIcon.Parent and activeResizeIcon.Parent.AbsoluteSize or Vector2.zero
						local relative = activeResizeIcon.Parent and (pointer - activeResizeIcon.Parent.AbsolutePosition) or Vector2.zero
						local x = 1 - math.clamp(relative.X / math.max(handleSize.X, 1), 0, 1)
						local y = 1 - math.clamp(relative.Y / math.max(handleSize.Y, 1), 0, 1)
						tweenService:Create(activeResizeIcon, TweenInfo.new(0.5, Enum.EasingStyle.Exponential, Enum.EasingDirection.Out), {
							ImageColor3 = resizeStyle.color,
							ImageTransparency = resizeStyle.hoverTransparency,
							Size = UDim2.fromOffset(
								math.floor(resizeStyle.baseSize + (x * resizeStyle.growRange) + 0.5),
								math.floor(resizeStyle.baseSize + (y * resizeStyle.growRange) + 0.5)
							)
						}):Play()
					end
				else
					resetActiveResizeIcon()
				end
			end
			dragStart = nil
			dragStartPosition = nil
			resizeStartPointer = nil
			resizePointerPosition = nil
			resizeStartSize = nil
			resizeStartCenter = nil
			activeResizeIcon = nil
		end)

		local function normalizedResizeCursorPosition(button, pointer)
			local sample = pointer or currentResizePointer()
			local buttonPosition = button.AbsolutePosition
			local buttonSize = button.AbsoluteSize
			local relativeX = (sample.X - buttonPosition.X) / math.max(buttonSize.X, 1)
			local relativeY = (sample.Y - buttonPosition.Y) / math.max(buttonSize.Y, 1)
			return Vector2.new(1 - math.clamp(relativeX, 0, 1), 1 - math.clamp(relativeY, 0, 1))
		end

		local function resizeIconSizeFromPointer(button, pointer)
			local normals = normalizedResizeCursorPosition(button, pointer)
			return UDim2.fromOffset(
				math.floor(resizeStyle.baseSize + (normals.X * resizeStyle.growRange) + 0.5),
				math.floor(resizeStyle.baseSize + (normals.Y * resizeStyle.growRange) + 0.5)
			)
		end

		local function setResizeHandleVisual(icon, mode, duration, pointer)
			if not icon or not icon.Parent then return end
			local goals
			if mode == "pressed" then
				goals = {
					ImageColor3 = resizeStyle.pressColor,
					ImageTransparency = resizeStyle.pressTransparency,
					Size = resizeIconSizeFromPointer(icon.Parent, pointer),
				}
			elseif mode == "hover" then
				goals = {
					ImageColor3 = resizeStyle.color,
					ImageTransparency = resizeStyle.hoverTransparency,
					Size = resizeIconSizeFromPointer(icon.Parent, pointer),
				}
			else
				goals = {
					ImageColor3 = resizeStyle.color,
					ImageTransparency = resizeStyle.idleTransparency,
					Size = resizeStyle.idleSize,
				}
			end

			local animationDuration = duration
			if animationDuration == nil then
				animationDuration = 0.5
			end
			if animationDuration == 0 then
				for property, value in pairs(goals) do
					icon[property] = value
				end
				return
			end

			tweenService:Create(icon, TweenInfo.new(animationDuration, Enum.EasingStyle.Exponential, Enum.EasingDirection.Out), goals):Play()
		end

		resetActiveResizeIcon = function()
			setResizeHandleVisual(activeResizeIcon, "idle", 0.18)
		end

		local function beginPickerResize(input, direction, icon)
			pickerDragging = false
			pickerResizing = true
			activeResizeIcon = icon
			activeResizeInput = input
			if resizePanelTween then
				resizePanelTween:Cancel()
				resizePanelTween = nil
			end
			if resizeShadowTween then
				resizeShadowTween:Cancel()
				resizeShadowTween = nil
			end
			refreshPickerGuiInsetOffset()
			resizeStartPointer = pointerVectorFromInput(input)
			resizePointerPosition = resizeStartPointer
			resizeStartSize = panel.AbsoluteSize
			local rootSize = getPickerRootSize()
			resizeStartCenter = Vector2.new(panel.Position.X.Offset, panel.Position.Y.Offset)
			if panel.Position.X.Scale ~= 0 or panel.Position.Y.Scale ~= 0 then
				resizeStartCenter = Vector2.new(
					rootSize.X * panel.Position.X.Scale + panel.Position.X.Offset,
					rootSize.Y * panel.Position.Y.Scale + panel.Position.Y.Offset
				)
			end
			resizeTargetCenter = resizeStartCenter
			resizeTargetSize = resizeStartSize
			resizeVisualCenter = resizeStartCenter
			resizeVisualSize = resizeStartSize
			startPickerResizeSmoothing()
		end

		local function createResizeHandle(name, anchor, position, iconAnchor, iconPosition, rotation, direction)
			local handle = Instance.new("ImageButton")
			handle.Name = name
			handle.AnchorPoint = anchor
			handle.Position = position
			handle.Size = UDim2.new(0, 50, 0, 50)
			handle.BackgroundTransparency = 1
			handle.BorderSizePixel = 0
			handle.AutoButtonColor = false
			handle.Image = ""
			handle.ImageTransparency = 1
			handle.Active = true
			handle.ZIndex = 83
			handle.Parent = content

			local icon = Instance.new("ImageLabel")
			icon.Name = "Icon"
			icon.AnchorPoint = iconAnchor
			icon.Position = iconPosition
			icon.Size = UDim2.fromOffset(18, 18)
			icon.BackgroundTransparency = 1
			icon.Image = "rbxassetid://86527207319523"
			icon.ImageColor3 = resizeStyle.color
			icon.ImageTransparency = resizeStyle.idleTransparency
			icon.Rotation = rotation
			icon.ScaleType = Enum.ScaleType.Slice
			icon.SliceCenter = Rect.new(51, 52, 51, 52)
			icon.SliceScale = 0.5
			icon.ZIndex = 84
			icon.Parent = handle

			handle.InputBegan:Connect(function(input, processed)
				if processed or not isDragInput(input) then return end
				beginPickerResize(input, direction, icon)
				setResizeHandleVisual(icon, "pressed", nil, resizeVisualPointerFromInput(input))
			end)

			handle.MouseEnter:Connect(function()
				resizeHandleHover = true
				if pickerResizing then return end
				setResizeHandleVisual(icon, "hover", nil, currentResizePointer())
			end)

			handle.MouseLeave:Connect(function()
				resizeHandleHover = false
				if pickerResizing then return end
				setResizeHandleVisual(icon, "idle")
			end)

			handle.InputChanged:Connect(function(input)
				if input.UserInputType ~= Enum.UserInputType.MouseMovement and input.UserInputType ~= Enum.UserInputType.Touch then return end
				if pickerResizing then return end
				setResizeHandleVisual(icon, "hover", nil, resizeVisualPointerFromInput(input))
			end)

			return handle, icon
		end

		local resizeBottomRight, resizeBottomRightIcon = createResizeHandle("ResizeBottomRight", Vector2.new(1, 1), UDim2.fromScale(1, 1), Vector2.new(1, 1), UDim2.new(1, -8, 1, -8), 0, "BottomRight")

		userInputService.InputChanged:Connect(function(input)
			if not pickerResizing or not playerActionPickerState.open then return end
			if input.UserInputType ~= Enum.UserInputType.MouseMovement and input.UserInputType ~= Enum.UserInputType.Touch then return end
			if activeResizeInput and activeResizeInput.UserInputType == Enum.UserInputType.Touch and input ~= activeResizeInput then
				return
			end
			if not resizeStartPointer or not resizeStartSize or not resizeStartCenter then return end

			local rootSize = getPickerRootSize()
			resizePointerPosition = pointerVectorFromInput(input)
			local delta = resizePointerPosition - resizeStartPointer
			local maxWidth = math.min(playerActionPickerMaxSize.X, math.max(playerActionPickerMinSize.X, rootSize.X - 80))
			local maxHeight = math.min(playerActionPickerMaxSize.Y, math.max(playerActionPickerMinSize.Y, rootSize.Y - 80))
			local nextWidth = math.clamp(resizeStartSize.X + (delta.X * 2), playerActionPickerMinSize.X, maxWidth)
			local nextHeight = math.clamp(resizeStartSize.Y + (delta.Y * 2), playerActionPickerMinSize.Y, maxHeight)
			local aspect = nextWidth / math.max(nextHeight, 1)
			if aspect > 1.16 then
				nextWidth = math.clamp(nextHeight * 1.16, playerActionPickerMinSize.X, maxWidth)
			elseif aspect < 0.62 then
				nextHeight = math.clamp(nextWidth / 0.62, playerActionPickerMinSize.Y, maxHeight)
			end
			local nextCenter = Vector2.new(
				math.clamp(resizeStartCenter.X, nextWidth * 0.5 - 80, rootSize.X - nextWidth * 0.5 + 80),
				math.clamp(resizeStartCenter.Y, nextHeight * 0.5 - 40, rootSize.Y - nextHeight * 0.5 + 80)
			)
			setResizeHandleVisual(activeResizeIcon, "pressed", nil, resizeVisualPointerFromInput(input))
			resizeTargetCenter = nextCenter
			resizeTargetSize = Vector2.new(nextWidth, nextHeight)
			startPickerResizeSmoothing()
		end)

		local actionScroll = Instance.new("ScrollingFrame")
		actionScroll.Name = "ActionScroll"
		actionScroll.Position = UDim2.new(0, 26, 0, 18)
		actionScroll.Size = UDim2.new(1, -52, 1, -36)
		actionScroll.BackgroundTransparency = 1
		actionScroll.BorderSizePixel = 0
		actionScroll.CanvasSize = UDim2.fromOffset(0, 1480)
		actionScroll.ScrollBarThickness = 0
		actionScroll.ScrollingDirection = Enum.ScrollingDirection.Y
		actionScroll.ClipsDescendants = true
		actionScroll.Active = true
		actionScroll.ZIndex = 71
		actionScroll.Parent = content

		profile.Card.Position = UDim2.new(0, 0, 0, 0)
		profile.Card.Size = UDim2.new(1, 0, 0, 150)
		profile.Card.Parent = actionScroll

		local actionScrollFadeState = {Top = -1, Bottom = -1}

		local function getActionRowFadeGroup(row)
			if not row then return nil end
			local existing = row:FindFirstChild("FadeGroup")
			if existing and existing:IsA("CanvasGroup") then
				return existing
			end

			local fadeGroup = Instance.new("CanvasGroup")
			fadeGroup.Name = "FadeGroup"
			fadeGroup.Size = UDim2.fromScale(1, 1)
			fadeGroup.BackgroundTransparency = 1
			fadeGroup.BorderSizePixel = 0
			fadeGroup.GroupTransparency = 0
			fadeGroup.ClipsDescendants = true
			fadeGroup.Active = false
			fadeGroup.ZIndex = row.ZIndex + 1
			fadeGroup.Parent = row

			for _, child in ipairs(row:GetChildren()) do
				if child ~= fadeGroup and child:IsA("GuiObject") then
					child.Parent = fadeGroup
				end
			end

			return fadeGroup
		end

		local function getActionRowVisualParent(row)
			return getActionRowFadeGroup(row) or row
		end

		local function createActionScrollBlurEdge(name, anchorPoint, position, size, direction)
			local isTop = direction == "Top"
			local edgeColor = siriusValues.selectActionStyle.panel

			local edge = Instance.new("Frame")
			edge.Name = name
			edge.AnchorPoint = anchorPoint
			edge.Position = position
			edge.Size = size
			edge.BackgroundTransparency = 1
			edge.BorderSizePixel = 0
			edge.ClipsDescendants = true
			edge.Active = false
			edge.Visible = false
			edge.ZIndex = 77
			edge.Parent = content

			siriusValues.addSelectActionCorner(edge, siriusValues.selectActionStyle.panelCorner)

			-- Soft depth halo so rows appear to sink under the edge without duplicating text.
			local blurSmear = Instance.new("ImageLabel")
			blurSmear.Name = "BlurSmear"
			blurSmear.AnchorPoint = Vector2.new(0.5, isTop and 0 or 1)
			blurSmear.Position = UDim2.new(0.5, 0, isTop and 0 or 1, 0)
			blurSmear.Size = UDim2.fromScale(1, 0.28)
			blurSmear.BackgroundTransparency = 1
			blurSmear.Image = "rbxassetid://8992230677"
			blurSmear.ImageColor3 = Color3.fromRGB(10, 10, 10)
			blurSmear.ImageTransparency = 1
			blurSmear.ScaleType = Enum.ScaleType.Slice
			blurSmear.SliceCenter = Rect.new(99, 99, 99, 99)
			blurSmear.ZIndex = 77
			blurSmear.Parent = edge

			-- Primary scrim: dissolves the scroll content cleanly into the panel colour.
			local base = Instance.new("Frame")
			base.Name = "Base"
			base.Size = UDim2.fromScale(1, 1)
			base.BackgroundColor3 = edgeColor
			base.BackgroundTransparency = 1
			base.BorderSizePixel = 0
			base.ZIndex = 78
			base.Parent = edge

			siriusValues.addSelectActionCorner(base, siriusValues.selectActionStyle.panelCorner)

			-- Solid at the very edge, easing out smoothly toward the centre of the list.
			local baseGradient = Instance.new("UIGradient")
			baseGradient.Rotation = isTop and 90 or -90
			baseGradient.Transparency = NumberSequence.new({
				NumberSequenceKeypoint.new(0, 0),
				NumberSequenceKeypoint.new(0.04, 0.01),
				NumberSequenceKeypoint.new(0.09, 0.14),
				NumberSequenceKeypoint.new(0.14, 0.42),
				NumberSequenceKeypoint.new(0.20, 0.76),
				NumberSequenceKeypoint.new(0.27, 1),
				NumberSequenceKeypoint.new(1, 1)
			})
			baseGradient.Parent = base

			-- Frosted sheen for a subtle glass read hugging the edge.
			local glass = Instance.new("Frame")
			glass.Name = "Glass"
			glass.Size = UDim2.fromScale(1, 1)
			glass.BackgroundColor3 = Color3.fromRGB(32, 32, 34)
			glass.BackgroundTransparency = 1
			glass.BorderSizePixel = 0
			glass.ZIndex = 79
			glass.Parent = edge

			siriusValues.addSelectActionCorner(glass, siriusValues.selectActionStyle.panelCorner)

			local glassGradient = Instance.new("UIGradient")
			glassGradient.Rotation = isTop and 90 or -90
			glassGradient.Transparency = NumberSequence.new({
				NumberSequenceKeypoint.new(0, 0.86),
				NumberSequenceKeypoint.new(0.16, 0.96),
				NumberSequenceKeypoint.new(1, 1)
			})
			glassGradient.Parent = glass

			-- Hairline catch-light that tapers off toward the sides.
			local highlight = Instance.new("Frame")
			highlight.Name = "Highlight"
			highlight.AnchorPoint = Vector2.new(0.5, isTop and 0 or 1)
			highlight.Position = UDim2.new(0.5, 0, isTop and 0 or 1, 0)
			highlight.Size = UDim2.new(1, -22, 0, 1)
			highlight.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
			highlight.BackgroundTransparency = 1
			highlight.BorderSizePixel = 0
			highlight.ZIndex = 80
			highlight.Parent = edge

			local highlightGradient = Instance.new("UIGradient")
			highlightGradient.Transparency = NumberSequence.new({
				NumberSequenceKeypoint.new(0, 1),
				NumberSequenceKeypoint.new(0.5, 0),
				NumberSequenceKeypoint.new(1, 1)
			})
			highlightGradient.Parent = highlight

			return edge
		end

		local actionScrollTopFade = createActionScrollBlurEdge(
			"ActionScrollTopBlur",
			Vector2.new(0, 0),
			UDim2.new(0, 0, 0, 0),
			UDim2.fromScale(1, 1),
			"Top"
		)

		local actionScrollBottomFade = createActionScrollBlurEdge(
			"ActionScrollBottomBlur",
			Vector2.new(0, 0),
			UDim2.new(0, 0, 0, 0),
			UDim2.fromScale(1, 1),
			"Bottom"
		)

		local function setActionScrollFade(frame, key, strength)
			strength = math.clamp(strength or 0, 0, 1)
			-- Smoothstep so the edge eases in as content leaves the boundary,
			-- instead of ramping in linearly with the raw scroll distance.
			local eased = strength * strength * (3 - 2 * strength)
			if math.abs((actionScrollFadeState[key] or 0) - eased) < 0.01 then return end
			actionScrollFadeState[key] = eased

			local base = frame:FindFirstChild("Base")
			local blurSmear = frame:FindFirstChild("BlurSmear")
			local glass = frame:FindFirstChild("Glass")
			local highlight = frame:FindFirstChild("Highlight")

			if eased <= 0.012 then
				if base then base.BackgroundTransparency = 1 end
				if blurSmear then blurSmear.ImageTransparency = 1 end
				if glass then glass.BackgroundTransparency = 1 end
				if highlight then highlight.BackgroundTransparency = 1 end
				frame.Visible = false
				return
			end

			frame.Visible = true
			-- The scrim hides clipping while the glass/shadow layers give a soft-focus edge.
			if base then base.BackgroundTransparency = 1 - math.min(eased * 0.98, 0.98) end
			if blurSmear then blurSmear.ImageTransparency = 1 end
			if glass then glass.BackgroundTransparency = 1 - (eased * 0.035) end
			if highlight then highlight.BackgroundTransparency = 1 - (eased * 0.035) end
		end

		local actionScrollBlurRows = {}

		local function setActionRowEdgeBlur(rowState, strength)
			local row = rowState.Row
			if not row or not row.Parent then return end

			if rowState.Blur then
				rowState.Blur.Visible = false
				rowState.Blur.ImageTransparency = 1
				rowState.Blur.BackgroundTransparency = 1
			end
			if rowState.BlurContent then
				rowState.BlurContent.Visible = false
			end

			local fadeGroup = rowState.FadeGroup
			if not fadeGroup or not fadeGroup.Parent then
				fadeGroup = getActionRowFadeGroup(row)
				rowState.FadeGroup = fadeGroup
			end
			if not fadeGroup then return end

			strength = math.clamp(strength or 0, 0, 1)
			local eased = strength * strength * (3 - 2 * strength)
			local targetTransparency = math.clamp(eased * 0.72, 0, 0.72)
			if math.abs((rowState.FadeStrength or -1) - targetTransparency) < 0.018 then return end

			rowState.FadeStrength = targetTransparency
			row.Active = targetTransparency < 0.68
			if rowState.FadeTween then
				rowState.FadeTween:Cancel()
			end
			rowState.FadeTween = tweenService:Create(fadeGroup, TweenInfo.new(0.12, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
				GroupTransparency = targetTransparency
			})
			rowState.FadeTween:Play()
		end

		local function registerActionScrollBlurRow(row)
			if not row then return end

			local blur = row:FindFirstChild("EdgeBlur")
			if blur then
				blur:Destroy()
			end

			local blurContent = row:FindFirstChild("EdgeBlurContent")
			if blurContent then
				blurContent:Destroy()
			end

			row.ClipsDescendants = true
			local fadeGroup = getActionRowFadeGroup(row)
			table.insert(actionScrollBlurRows, {
				Row = row,
				FadeGroup = fadeGroup,
				FadeStrength = -1,
				Switch = row:FindFirstChild("Switch", true),
				Knob = row:FindFirstChild("Knob", true),
				Strength = -1,
				FadeTween = nil
			})
		end

		local function refreshActionScrollBlurRows()
			if not actionScroll or not actionScroll.Parent then return end
			local viewportTop = actionScroll.AbsolutePosition.Y
			local viewportBottom = viewportTop + actionScroll.AbsoluteWindowSize.Y
			local edgeRange = 96

			for _, rowState in ipairs(actionScrollBlurRows) do
				local row = rowState.Row
				if row and row.Parent then
					rowState.Switch = row:FindFirstChild("Switch", true)
					rowState.Knob = row:FindFirstChild("Knob", true)

					local rowTop = row.AbsolutePosition.Y
					local rowBottom = rowTop + row.AbsoluteSize.Y
					local topStrength = (viewportTop + edgeRange - rowTop) / edgeRange
					local bottomStrength = (rowBottom - (viewportBottom - edgeRange)) / edgeRange
					local strength = math.max(topStrength, bottomStrength, 0)
					setActionRowEdgeBlur(rowState, strength)
				end
			end
		end

		local function refreshActionScrollFades()
			if not actionScroll or not actionScroll.Parent then return end
			local maxY = math.max(0, actionScroll.AbsoluteCanvasSize.Y - actionScroll.AbsoluteWindowSize.Y)
			local currentY = actionScroll.CanvasPosition.Y
			local fadeRange = 76
			setActionScrollFade(actionScrollTopFade, "Top", currentY / fadeRange)
			setActionScrollFade(actionScrollBottomFade, "Bottom", (maxY - currentY) / fadeRange)
			refreshActionScrollBlurRows()
		end

		actionScroll:GetPropertyChangedSignal("CanvasPosition"):Connect(refreshActionScrollFades)
		actionScroll:GetPropertyChangedSignal("AbsoluteCanvasSize"):Connect(refreshActionScrollFades)
		actionScroll:GetPropertyChangedSignal("AbsoluteWindowSize"):Connect(refreshActionScrollFades)
		task.defer(refreshActionScrollFades)

		local actionRow = Instance.new("TextButton")
		actionRow.Name = "Spectate"
		actionRow.Position = UDim2.new(0, 0, 0, 180)
		actionRow.Size = UDim2.new(1, 0, 0, 50)
		actionRow.BackgroundColor3 = siriusValues.selectActionStyle.row
		actionRow.BackgroundTransparency = 0.04
		actionRow.BorderSizePixel = 0
		actionRow.AutoButtonColor = false
		actionRow.Text = ""
		actionRow.ZIndex = 71
		actionRow.Parent = actionScroll

		siriusValues.addSelectActionCorner(actionRow, siriusValues.selectActionStyle.rowCorner)

		local actionStroke = Instance.new("UIStroke")
		actionStroke.Name = "stroke"
		actionStroke.Color = siriusValues.selectActionStyle.stroke
		actionStroke.Thickness = 1
		actionStroke.Transparency = 0.18
		actionStroke.Parent = actionRow

		local actionIcon = Instance.new("Frame")
		actionIcon.Name = "Icon"
		actionIcon.AnchorPoint = Vector2.new(0, 0.5)
		actionIcon.Position = UDim2.new(0, 15, 0.5, 0)
		actionIcon.Size = UDim2.new(0, 34, 0, 34)
		actionIcon.BackgroundColor3 = siriusValues.selectActionStyle.icon
		actionIcon.BackgroundTransparency = 0
		actionIcon.BorderSizePixel = 0
		actionIcon.ZIndex = 72
		actionIcon.Parent = actionRow

		siriusValues.addSelectActionCorner(actionIcon, siriusValues.selectActionStyle.iconCorner)
		siriusValues.addSelectActionStroke(actionIcon, siriusValues.selectActionStyle.stroke, 0.62)

		local actionDot = Instance.new("Frame")
		actionDot.Name = "Dot"
		actionDot.AnchorPoint = Vector2.new(0.5, 0.5)
		actionDot.Position = UDim2.new(0.5, 0, 0.5, 0)
		actionDot.Size = UDim2.new(0, 24, 0, 24)
		actionDot.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
		actionDot.BackgroundTransparency = 1
		actionDot.BorderSizePixel = 0
		actionDot.ZIndex = 73
		actionDot.Parent = actionIcon

		siriusValues.addSelectActionCorner(actionDot, nil, 1)

		local actionGlyph = Instance.new("TextLabel")
		actionGlyph.Name = "Glyph"
		actionGlyph.AnchorPoint = Vector2.new(0.5, 0.5)
		actionGlyph.Position = UDim2.new(0.5, 0, 0.5, -1)
		actionGlyph.Size = UDim2.new(1, 0, 1, 0)
		actionGlyph.BackgroundTransparency = 1
		actionGlyph.Text = ""
		actionGlyph.TextColor3 = Color3.fromRGB(255, 255, 255)
		actionGlyph.TextTransparency = 1
		actionGlyph.TextSize = 19
		actionGlyph.FontFace = Font.new("rbxassetid://12187365364", Enum.FontWeight.Bold)
		actionGlyph.ZIndex = 74
		actionGlyph.Parent = actionIcon

		local actionGlyphImage = Instance.new("ImageLabel")
		actionGlyphImage.Name = "GlyphImage"
		actionGlyphImage.AnchorPoint = Vector2.new(0.5, 0.5)
		actionGlyphImage.Position = UDim2.new(0.5, 0, 0.5, 0)
		actionGlyphImage.Size = UDim2.fromOffset(21, 21)
		actionGlyphImage.BackgroundTransparency = 1
		actionGlyphImage.Image = getSelectActionIcon("Spectate")
		actionGlyphImage.ImageColor3 = Color3.fromRGB(255, 255, 255)
		actionGlyphImage.ImageTransparency = 0.04
		actionGlyphImage.ScaleType = Enum.ScaleType.Fit
		actionGlyphImage.ZIndex = 75
		actionGlyphImage.Parent = actionIcon

		local actionText = Instance.new("TextLabel")
		actionText.Name = "Title"
		actionText.AnchorPoint = Vector2.new(0, 0.5)
		actionText.Position = UDim2.new(0, 64, 0.5, 0)
		actionText.Size = UDim2.new(1, -76, 0, 24)
		actionText.BackgroundTransparency = 1
		actionText.Text = "Spectate"
		actionText.TextColor3 = siriusValues.selectActionStyle.text
		actionText.TextSize = 15
		actionText.TextXAlignment = Enum.TextXAlignment.Left
		actionText.FontFace = Font.new("rbxassetid://12187365364", Enum.FontWeight.Medium)
		actionText.ZIndex = 72
		actionText.Parent = actionRow

		actionRow.MouseEnter:Connect(function()
			if not playerActionPickerState.open then return end
			tweenService:Create(actionRow, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {
				BackgroundColor3 = siriusValues.selectActionStyle.rowHover,
				BackgroundTransparency = 0
			}):Play()
			tweenService:Create(actionStroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {
				Color = siriusValues.selectActionStyle.strokeHover,
				Transparency = 0.08
			}):Play()
			tweenService:Create(actionIcon, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {
				BackgroundColor3 = siriusValues.selectActionStyle.iconHover
			}):Play()
		end)

		actionRow.MouseLeave:Connect(function()
			tweenService:Create(actionRow, TweenInfo.new(0.24, Enum.EasingStyle.Quint), {
				BackgroundColor3 = siriusValues.selectActionStyle.row,
				BackgroundTransparency = 0.04
			}):Play()
			tweenService:Create(actionStroke, TweenInfo.new(0.24, Enum.EasingStyle.Quint), {
				Color = siriusValues.selectActionStyle.stroke,
				Transparency = 0.18
			}):Play()
			tweenService:Create(actionIcon, TweenInfo.new(0.24, Enum.EasingStyle.Quint), {
				BackgroundColor3 = siriusValues.selectActionStyle.icon
			}):Play()
		end)

		actionRow.MouseButton1Click:Connect(function()
			local targetPlayer = playerActionPickerState.targetPlayer
			if targetPlayer and players:FindFirstChild(targetPlayer.Name) then
				_picker.closePlayerActionPickerToSpectate(targetPlayer)
			end
		end)

		registerActionScrollBlurRow(actionRow)

		local function cloneActionRow(name, label, yPosition, accentColor, iconName, iconSize, callback)
			local clonedRow = actionRow:Clone()
			clonedRow.Name = name
			clonedRow.Position = UDim2.new(0, 0, 0, yPosition)
			clonedRow.Parent = actionScroll

			local clonedStroke = clonedRow:FindFirstChild("stroke")
			local clonedIcon = clonedRow:FindFirstChild("Icon", true)
			local clonedDot = clonedIcon and clonedIcon:FindFirstChild("Dot", true)
			local clonedGlyph = clonedIcon and clonedIcon:FindFirstChild("Glyph", true)
			local clonedGlyphImage = clonedIcon and clonedIcon:FindFirstChild("GlyphImage", true)
			local clonedText = clonedRow:FindFirstChild("Title", true)

			if clonedText then
				clonedText.Text = label
			end
			if clonedDot then
				clonedDot.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
				clonedDot.BackgroundTransparency = 1
			end
			if clonedGlyph then
				clonedGlyph.Text = ""
				clonedGlyph.TextTransparency = 1
				clonedGlyph.TextSize = 19
			end
			if clonedGlyphImage then
				clonedGlyphImage.Image = getSelectActionIcon(iconName)
				clonedGlyphImage.Size = UDim2.fromOffset(iconSize or 21, iconSize or 21)
				clonedGlyphImage.ImageColor3 = Color3.fromRGB(255, 255, 255)
				clonedGlyphImage.ImageTransparency = 0.04
			end

			registerActionScrollBlurRow(clonedRow)

			clonedRow.MouseEnter:Connect(function()
				if not playerActionPickerState.open then return end
				tweenService:Create(clonedRow, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {
					BackgroundColor3 = siriusValues.selectActionStyle.rowHover,
					BackgroundTransparency = 0
				}):Play()
				if clonedStroke then
					tweenService:Create(clonedStroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {
						Color = siriusValues.selectActionStyle.strokeHover,
						Transparency = 0.08
					}):Play()
				end
				if clonedIcon then
					tweenService:Create(clonedIcon, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {
						BackgroundColor3 = siriusValues.selectActionStyle.iconHover
					}):Play()
				end
			end)

			clonedRow.MouseLeave:Connect(function()
				tweenService:Create(clonedRow, TweenInfo.new(0.24, Enum.EasingStyle.Quint), {
					BackgroundColor3 = siriusValues.selectActionStyle.row,
					BackgroundTransparency = 0.04
				}):Play()
				if clonedStroke then
					tweenService:Create(clonedStroke, TweenInfo.new(0.24, Enum.EasingStyle.Quint), {
						Color = siriusValues.selectActionStyle.stroke,
						Transparency = 0.18
					}):Play()
				end
				if clonedIcon then
					tweenService:Create(clonedIcon, TweenInfo.new(0.24, Enum.EasingStyle.Quint), {
						BackgroundColor3 = siriusValues.selectActionStyle.icon
					}):Play()
				end
			end)

			clonedRow.MouseButton1Click:Connect(function()
				local targetPlayer = playerActionPickerState.targetPlayer
				if name == "Track" then
					if targetPlayer and players:FindFirstChild(targetPlayer.Name) then
						callback(targetPlayer)
						local isTracked = trackingBadges[targetPlayer] ~= nil
						if playerActionPickerParts.TrackSwitch then
							tweenService:Create(playerActionPickerParts.TrackSwitch, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
								BackgroundColor3 = isTracked and siriusValues.selectActionStyle.switchActive or siriusValues.selectActionStyle.switch
							}):Play()
						end
						if playerActionPickerParts.TrackSwitchStroke then
							tweenService:Create(playerActionPickerParts.TrackSwitchStroke, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
								Color = isTracked and siriusValues.selectActionStyle.switchStrokeActive or siriusValues.selectActionStyle.switchStroke,
								Transparency = isTracked and 0.08 or 0.28
							}):Play()
						end
						if playerActionPickerParts.TrackKnob then
							tweenService:Create(playerActionPickerParts.TrackKnob, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
								Position = isTracked and UDim2.new(1, -13, 0.5, 0) or UDim2.new(0, 13, 0.5, 0),
								BackgroundColor3 = isTracked and Color3.fromRGB(245, 245, 245) or Color3.fromRGB(110, 110, 110)
							}):Play()
						end
					end
					return
				end
				if name == "Tracer" then
					if targetPlayer and players:FindFirstChild(targetPlayer.Name) then
						local isEnabled = callback(targetPlayer)
						if playerActionPickerParts.TracerSwitch then
							tweenService:Create(playerActionPickerParts.TracerSwitch, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
								BackgroundColor3 = isEnabled and siriusValues.selectActionStyle.switchActive or siriusValues.selectActionStyle.switch
							}):Play()
						end
						if playerActionPickerParts.TracerSwitchStroke then
							tweenService:Create(playerActionPickerParts.TracerSwitchStroke, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
								Color = isEnabled and siriusValues.selectActionStyle.switchStrokeActive or siriusValues.selectActionStyle.switchStroke,
								Transparency = isEnabled and 0.12 or 0.28
							}):Play()
						end
						if playerActionPickerParts.TracerKnob then
							tweenService:Create(playerActionPickerParts.TracerKnob, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
								Position = isEnabled and UDim2.new(1, -13, 0.5, 0) or UDim2.new(0, 13, 0.5, 0),
								BackgroundColor3 = isEnabled and Color3.fromRGB(245, 245, 245) or Color3.fromRGB(110, 110, 110)
							}):Play()
						end
					end
					return
				end
				if name == "Hide" then
					if targetPlayer and players:FindFirstChild(targetPlayer.Name) then
						local isEnabled = callback(targetPlayer)
						if playerActionPickerParts.HideSwitch then
							tweenService:Create(playerActionPickerParts.HideSwitch, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
								BackgroundColor3 = isEnabled and siriusValues.selectActionStyle.switchActive or siriusValues.selectActionStyle.switch
							}):Play()
						end
						if playerActionPickerParts.HideSwitchStroke then
							tweenService:Create(playerActionPickerParts.HideSwitchStroke, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
								Color = isEnabled and siriusValues.selectActionStyle.switchStrokeActive or siriusValues.selectActionStyle.switchStroke,
								Transparency = isEnabled and 0.12 or 0.28
							}):Play()
						end
						if playerActionPickerParts.HideKnob then
							tweenService:Create(playerActionPickerParts.HideKnob, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
								Position = isEnabled and UDim2.new(1, -13, 0.5, 0) or UDim2.new(0, 13, 0.5, 0),
								BackgroundColor3 = isEnabled and Color3.fromRGB(245, 245, 245) or Color3.fromRGB(110, 110, 110)
							}):Play()
						end
					end
					return
				end
				if name == "ESP" then
					if targetPlayer and players:FindFirstChild(targetPlayer.Name) then
						local isEnabled = callback(targetPlayer)
						if playerActionPickerParts.EspSwitch then
							tweenService:Create(playerActionPickerParts.EspSwitch, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
								BackgroundColor3 = isEnabled and siriusValues.selectActionStyle.switchActive or siriusValues.selectActionStyle.switch
							}):Play()
						end
						if playerActionPickerParts.EspSwitchStroke then
							tweenService:Create(playerActionPickerParts.EspSwitchStroke, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
								Color = isEnabled and siriusValues.selectActionStyle.switchStrokeActive or siriusValues.selectActionStyle.switchStroke,
								Transparency = isEnabled and 0.08 or 0.28
							}):Play()
						end
						if playerActionPickerParts.EspKnob then
							tweenService:Create(playerActionPickerParts.EspKnob, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
								Position = isEnabled and UDim2.new(1, -13, 0.5, 0) or UDim2.new(0, 13, 0.5, 0),
								BackgroundColor3 = isEnabled and Color3.fromRGB(245, 245, 245) or Color3.fromRGB(110, 110, 110)
							}):Play()
						end
					end
					return
				end
				if string.sub(name, 1, 5) == "Wield" then
					if targetPlayer and players:FindFirstChild(targetPlayer.Name) then
						callback(targetPlayer)
						refreshWieldingActionRows(targetPlayer, true)
					end
					return
				end
				if name == "LiveTrack" then
					if not targetPlayer or not players:FindFirstChild(targetPlayer.Name) then return end
					_picker.closePlayerActionPicker(function()
						if targetPlayer and players:FindFirstChild(targetPlayer.Name) then
							callback(targetPlayer, true)
						end
					end)
					return
				end

				_picker.closePlayerActionPicker()
				task.delay(0.12, function()
					if targetPlayer and players:FindFirstChild(targetPlayer.Name) then
						callback(targetPlayer)
					end
				end)
			end)

			return clonedRow, clonedStroke, clonedIcon, clonedText, clonedDot, clonedGlyph, clonedGlyphImage
		end

		local trackRow, trackStroke, trackIcon, trackText, trackDot, trackGlyph, trackGlyphImage = cloneActionRow("Track", "Track", 240, Color3.fromRGB(255, 255, 255), "Track", 21, _picker.startTrackingPlayer)
		playerActionPickerParts.LiveTrackRow, playerActionPickerParts.LiveTrackStroke, playerActionPickerParts.LiveTrackIcon, playerActionPickerParts.LiveTrackText, playerActionPickerParts.LiveTrackDot, playerActionPickerParts.LiveTrackGlyph, playerActionPickerParts.LiveTrackGlyphImage = cloneActionRow("LiveTrack", "Live Track", 300, Color3.fromRGB(255, 255, 255), "LiveTrack", 22, _picker.startLiveTrackingPlayer)
		local tracerRow, tracerStroke, tracerIcon, tracerText, tracerDot, tracerGlyph, tracerGlyphImage = cloneActionRow("Tracer", "Tracer", 360, Color3.fromRGB(255, 255, 255), "Tracer", 21, togglePlayerTracer)
		local hideRow, hideStroke, hideIcon, hideText, hideDot, hideGlyph, hideGlyphImage = cloneActionRow("Hide", "Hide", 420, Color3.fromRGB(255, 255, 255), "Hide", 21, hiddenPlayerTools.Toggle)
		local espRow, espStroke, espIcon, espText, espDot, espGlyph, espGlyphImage = cloneActionRow("ESP", "Extrasensory Perception", 480, Color3.fromRGB(255, 255, 255), "ESP", 21, _picker.togglePlayerEsp)

		local wieldingSection = Instance.new("TextLabel")
		wieldingSection.Name = "WieldingSection"
		wieldingSection.Position = UDim2.new(0, 8, 0, 540)
		wieldingSection.Size = UDim2.new(1, -16, 0, 16)
		wieldingSection.BackgroundTransparency = 1
		wieldingSection.Text = "Wielding"
		wieldingSection.TextColor3 = Color3.fromRGB(112, 112, 112)
		wieldingSection.TextSize = 10
		wieldingSection.TextXAlignment = Enum.TextXAlignment.Left
		wieldingSection.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
		wieldingSection.ZIndex = 72
		wieldingSection.Parent = actionScroll

		local function createCompactSwitch(parent)
			local visualParent = getActionRowVisualParent(parent)
			local switch = Instance.new("Frame")
			switch.Name = "Switch"
			switch.AnchorPoint = Vector2.new(1, 0.5)
			switch.Position = UDim2.new(1, -18, 0.5, 0)
			switch.Size = UDim2.new(0, 48, 0, 24)
			switch.BackgroundColor3 = siriusValues.selectActionStyle.switch
			switch.BackgroundTransparency = 0.02
			switch.BorderSizePixel = 0
			switch.ZIndex = 73
			switch.Parent = visualParent

			siriusValues.addSelectActionCorner(switch, nil, 1)

			local switchStroke = Instance.new("UIStroke")
			switchStroke.Name = "stroke"
			switchStroke.Color = siriusValues.selectActionStyle.switchStroke
			switchStroke.Thickness = 1
			switchStroke.Transparency = 0.28
			switchStroke.Parent = switch

			local knob = Instance.new("Frame")
			knob.Name = "Knob"
			knob.AnchorPoint = Vector2.new(0.5, 0.5)
			knob.Position = UDim2.new(0, 13, 0.5, 0)
			knob.Size = UDim2.new(0, 18, 0, 18)
			knob.BackgroundColor3 = Color3.fromRGB(110, 110, 110)
			knob.BorderSizePixel = 0
			knob.ZIndex = 74
			knob.Parent = switch

			siriusValues.addSelectActionCorner(knob, nil, 1)

			return switch, switchStroke, knob
		end

		local wieldingRows = {}
		local function addWieldingRow(abilityName, rowName, label, yPosition, iconName, hasSwitch, callback)
			local row, stroke, icon, text, dot, glyph, glyphImage = cloneActionRow(rowName, label, yPosition, Color3.fromRGB(255, 255, 255), iconName, 21, callback)
			local switch, switchStroke, knob
			if hasSwitch then
				switch, switchStroke, knob = createCompactSwitch(row)
				if text then
					text.Size = UDim2.new(1, -138, 0, 24)
				end
			end

			wieldingRows[rowName] = {
				AbilityName = abilityName,
				IconName = iconName,
				Row = row,
				Stroke = stroke,
				Icon = icon,
				Text = text,
				Dot = dot,
				Glyph = glyph,
				GlyphImage = glyphImage,
				Switch = switch,
				SwitchStroke = switchStroke,
				Knob = knob
			}
		end

		addWieldingRow("TeleportTarget", "WieldTeleport", "Teleport", 570, "Teleport", false, playerActionPickerParts.TeleportCallback)
		addWieldingRow("FlingPlayer", "WieldFlingPlayer", "Fling Player", 630, "FlingPlayer", true, function(targetPlayer)
			return wieldingTools.Toggle(targetPlayer, "FlingPlayer")
		end)
		addWieldingRow("Fling2", "WieldFling2", "Fling 2.0", 690, "Fling2", true, function(targetPlayer)
			return wieldingTools.Toggle(targetPlayer, "Fling2")
		end)
		addWieldingRow("PlatformFly", "WieldPlatformFly", "Platform Fly", 750, "PlatformFly", true, function(targetPlayer)
			return wieldingTools.Toggle(targetPlayer, "PlatformFly")
		end)
		addWieldingRow("MagicCarpet", "WieldMagicCarpet", "Magic Carpet", 810, "MagicCarpet", true, function(targetPlayer)
			return wieldingTools.Toggle(targetPlayer, "MagicCarpet")
		end)
		addWieldingRow("Elevator", "WieldElevator", "Elevator", 870, "Elevator", true, function(targetPlayer)
			return wieldingTools.Toggle(targetPlayer, "Elevator")
		end)
		addWieldingRow("AntiJump", "WieldAntiJump", "Anti Jump", 930, "AntiJump", true, function(targetPlayer)
			return wieldingTools.Toggle(targetPlayer, "AntiJump")
		end)
		addWieldingRow("InfiniteJump", "WieldInfiniteJump", "Infinite Jump", 990, "InfiniteJump", true, function(targetPlayer)
			return wieldingTools.Toggle(targetPlayer, "InfiniteJump")
		end)
		addWieldingRow("SpeedSlow", "WieldSpeedSlow", "Speed Boost Slow", 1050, "SpeedSlow", true, function(targetPlayer)
			return wieldingTools.Toggle(targetPlayer, "SpeedSlow")
		end)
		addWieldingRow("SpeedMedium", "WieldSpeedMedium", "Speed Boost Medium", 1110, "SpeedMedium", true, function(targetPlayer)
			return wieldingTools.Toggle(targetPlayer, "SpeedMedium")
		end)
		addWieldingRow("SpeedFast", "WieldSpeedFast", "Speed Boost Fast", 1170, "SpeedFast", true, function(targetPlayer)
			return wieldingTools.Toggle(targetPlayer, "SpeedFast")
		end)
		addWieldingRow("MovementBreaker", "WieldMovementBreaker", "Movement Breaker", 1230, "MovementBreaker", true, function(targetPlayer)
			return wieldingTools.Toggle(targetPlayer, "MovementBreaker")
		end)
		addWieldingRow("RocketTarget", "WieldRocketTarget", "Rocket Target", 1290, "RocketTarget", false, function(targetPlayer)
			return wieldingTools.Rocket(targetPlayer, 50, "Rocket Target")
		end)
		addWieldingRow("GodspeedRocket", "WieldGodspeedRocket", "GODSPEED Rocket", 1350, "GodspeedRocket", false, function(targetPlayer)
			return wieldingTools.Rocket(targetPlayer, 5000, "GODSPEED Rocket")
		end)

		local trackSwitch = Instance.new("Frame")
		trackSwitch.Name = "Switch"
		trackSwitch.AnchorPoint = Vector2.new(1, 0.5)
		trackSwitch.Position = UDim2.new(1, -18, 0.5, 0)
		trackSwitch.Size = UDim2.new(0, 48, 0, 24)
		trackSwitch.BackgroundColor3 = siriusValues.selectActionStyle.switch
		trackSwitch.BackgroundTransparency = 0.02
		trackSwitch.BorderSizePixel = 0
		trackSwitch.ZIndex = 73
		trackSwitch.Parent = getActionRowVisualParent(trackRow)

		siriusValues.addSelectActionCorner(trackSwitch, nil, 1)

		local trackSwitchStroke = Instance.new("UIStroke")
		trackSwitchStroke.Name = "stroke"
		trackSwitchStroke.Color = siriusValues.selectActionStyle.switchStroke
		trackSwitchStroke.Thickness = 1
		trackSwitchStroke.Transparency = 0.28
		trackSwitchStroke.Parent = trackSwitch

		local trackKnob = Instance.new("Frame")
		trackKnob.Name = "Knob"
		trackKnob.AnchorPoint = Vector2.new(0.5, 0.5)
		trackKnob.Position = UDim2.new(0, 13, 0.5, 0)
		trackKnob.Size = UDim2.new(0, 18, 0, 18)
		trackKnob.BackgroundColor3 = Color3.fromRGB(110, 110, 110)
		trackKnob.BorderSizePixel = 0
		trackKnob.ZIndex = 74
		trackKnob.Parent = trackSwitch

		siriusValues.addSelectActionCorner(trackKnob, nil, 1)

		if trackText then
			trackText.Size = UDim2.new(1, -138, 0, 24)
		end

		local tracerSwitch = Instance.new("Frame")
		tracerSwitch.Name = "Switch"
		tracerSwitch.AnchorPoint = Vector2.new(1, 0.5)
		tracerSwitch.Position = UDim2.new(1, -18, 0.5, 0)
		tracerSwitch.Size = UDim2.new(0, 48, 0, 24)
		tracerSwitch.BackgroundColor3 = siriusValues.selectActionStyle.switch
		tracerSwitch.BackgroundTransparency = 0.02
		tracerSwitch.BorderSizePixel = 0
		tracerSwitch.ZIndex = 73
		tracerSwitch.Parent = getActionRowVisualParent(tracerRow)

		siriusValues.addSelectActionCorner(tracerSwitch, nil, 1)

		local tracerSwitchStroke = Instance.new("UIStroke")
		tracerSwitchStroke.Name = "stroke"
		tracerSwitchStroke.Color = siriusValues.selectActionStyle.switchStroke
		tracerSwitchStroke.Thickness = 1
		tracerSwitchStroke.Transparency = 0.28
		tracerSwitchStroke.Parent = tracerSwitch

		local tracerKnob = Instance.new("Frame")
		tracerKnob.Name = "Knob"
		tracerKnob.AnchorPoint = Vector2.new(0.5, 0.5)
		tracerKnob.Position = UDim2.new(0, 13, 0.5, 0)
		tracerKnob.Size = UDim2.new(0, 18, 0, 18)
		tracerKnob.BackgroundColor3 = Color3.fromRGB(110, 110, 110)
		tracerKnob.BorderSizePixel = 0
		tracerKnob.ZIndex = 74
		tracerKnob.Parent = tracerSwitch

		siriusValues.addSelectActionCorner(tracerKnob, nil, 1)

		if tracerText then
			tracerText.Size = UDim2.new(1, -138, 0, 24)
		end

		local hideSwitch = Instance.new("Frame")
		hideSwitch.Name = "Switch"
		hideSwitch.AnchorPoint = Vector2.new(1, 0.5)
		hideSwitch.Position = UDim2.new(1, -18, 0.5, 0)
		hideSwitch.Size = UDim2.new(0, 48, 0, 24)
		hideSwitch.BackgroundColor3 = siriusValues.selectActionStyle.switch
		hideSwitch.BackgroundTransparency = 0.02
		hideSwitch.BorderSizePixel = 0
		hideSwitch.ZIndex = 73
		hideSwitch.Parent = getActionRowVisualParent(hideRow)

		siriusValues.addSelectActionCorner(hideSwitch, nil, 1)

		local hideSwitchStroke = Instance.new("UIStroke")
		hideSwitchStroke.Name = "stroke"
		hideSwitchStroke.Color = siriusValues.selectActionStyle.switchStroke
		hideSwitchStroke.Thickness = 1
		hideSwitchStroke.Transparency = 0.28
		hideSwitchStroke.Parent = hideSwitch

		local hideKnob = Instance.new("Frame")
		hideKnob.Name = "Knob"
		hideKnob.AnchorPoint = Vector2.new(0.5, 0.5)
		hideKnob.Position = UDim2.new(0, 13, 0.5, 0)
		hideKnob.Size = UDim2.new(0, 18, 0, 18)
		hideKnob.BackgroundColor3 = Color3.fromRGB(110, 110, 110)
		hideKnob.BorderSizePixel = 0
		hideKnob.ZIndex = 74
		hideKnob.Parent = hideSwitch

		siriusValues.addSelectActionCorner(hideKnob, nil, 1)

		if hideText then
			hideText.Size = UDim2.new(1, -138, 0, 24)
		end

		local espSwitch = Instance.new("Frame")
		espSwitch.Name = "Switch"
		espSwitch.AnchorPoint = Vector2.new(1, 0.5)
		espSwitch.Position = UDim2.new(1, -18, 0.5, 0)
		espSwitch.Size = UDim2.new(0, 48, 0, 24)
		espSwitch.BackgroundColor3 = siriusValues.selectActionStyle.switch
		espSwitch.BackgroundTransparency = 0.02
		espSwitch.BorderSizePixel = 0
		espSwitch.ZIndex = 73
		espSwitch.Parent = getActionRowVisualParent(espRow)

		siriusValues.addSelectActionCorner(espSwitch, nil, 1)

		local espSwitchStroke = Instance.new("UIStroke")
		espSwitchStroke.Name = "stroke"
		espSwitchStroke.Color = siriusValues.selectActionStyle.switchStroke
		espSwitchStroke.Thickness = 1
		espSwitchStroke.Transparency = 0.28
		espSwitchStroke.Parent = espSwitch

		local espKnob = Instance.new("Frame")
		espKnob.Name = "Knob"
		espKnob.AnchorPoint = Vector2.new(0.5, 0.5)
		espKnob.Position = UDim2.new(0, 13, 0.5, 0)
		espKnob.Size = UDim2.new(0, 18, 0, 18)
		espKnob.BackgroundColor3 = Color3.fromRGB(110, 110, 110)
		espKnob.BorderSizePixel = 0
		espKnob.ZIndex = 74
		espKnob.Parent = espSwitch

		siriusValues.addSelectActionCorner(espKnob, nil, 1)

		if espText then
			espText.Size = UDim2.new(1, -138, 0, 24)
		end

		playerActionPickerParts = {
			Panel = panel,
			PanelScale = panelScale,
			Content = content,
			Shadow = shadow,
			Stroke = panelStroke,
			Close = close,
			ActionScroll = actionScroll,
			ActionScrollTopFade = actionScrollTopFade,
			ActionScrollBottomFade = actionScrollBottomFade,
			RefreshActionScrollFades = refreshActionScrollFades,
			TeleportToSelected = playerActionPickerParts.TeleportToSelected,
			TeleportCallback = playerActionPickerParts.TeleportCallback,
			WieldingSection = wieldingSection,
			WieldRows = wieldingRows,
			ProfileCard = profile.Card,
			ProfileStroke = profile.Stroke,
			ProfileAvatar = profile.Avatar,
			ProfileAvatarStroke = profile.AvatarStroke,
			ProfileName = profile.Name,
			ProfileHandle = profile.Handle,
			ProfilePlaytimeValue = profile.PlaytimeValue,
			ProfileAccountAgeValue = profile.AccountAgeValue,
			ProfileRoleValue = profile.RoleValue,
			SpectateRow = actionRow,
			SpectateStroke = actionStroke,
			SpectateIcon = actionIcon,
			SpectateGlyphImage = actionGlyphImage,
			TrackRow = trackRow,
			TrackStroke = trackStroke,
			TrackIcon = trackIcon,
			TrackText = trackText,
			TrackDot = trackDot,
			TrackGlyph = trackGlyph,
			TrackGlyphImage = trackGlyphImage,
			TrackSwitch = trackSwitch,
			TrackSwitchStroke = trackSwitchStroke,
			TrackKnob = trackKnob,
			LiveTrackRow = playerActionPickerParts.LiveTrackRow,
			LiveTrackStroke = playerActionPickerParts.LiveTrackStroke,
			LiveTrackIcon = playerActionPickerParts.LiveTrackIcon,
			LiveTrackGlyphImage = playerActionPickerParts.LiveTrackGlyphImage,
			TracerRow = tracerRow,
			TracerStroke = tracerStroke,
			TracerIcon = tracerIcon,
			TracerText = tracerText,
			TracerDot = tracerDot,
			TracerGlyph = tracerGlyph,
			TracerGlyphImage = tracerGlyphImage,
			TracerSwitch = tracerSwitch,
			TracerSwitchStroke = tracerSwitchStroke,
			TracerKnob = tracerKnob,
			HideRow = hideRow,
			HideStroke = hideStroke,
			HideIcon = hideIcon,
			HideText = hideText,
			HideDot = hideDot,
			HideGlyph = hideGlyph,
			HideGlyphImage = hideGlyphImage,
			HideSwitch = hideSwitch,
			HideSwitchStroke = hideSwitchStroke,
			HideKnob = hideKnob,
			EspRow = espRow,
			EspStroke = espStroke,
			EspIcon = espIcon,
			EspText = espText,
			EspDot = espDot,
			EspGlyph = espGlyph,
			EspGlyphImage = espGlyphImage,
			EspSwitch = espSwitch,
			EspSwitchStroke = espSwitchStroke,
			EspKnob = espKnob,
			ResizeHandle = resizeBottomRight,
			ResizeIcon = resizeBottomRightIcon
		}

		return playerActionPicker
	end

	playerActionPicker = Instance.new("Frame")
	playerActionPicker.Name = "SiriusPlayerActionPicker"
	playerActionPicker.Size = UDim2.new(1, 0, 1, 0)
	playerActionPicker.BackgroundTransparency = 1
	playerActionPicker.BorderSizePixel = 0
	playerActionPicker.Visible = false
	playerActionPicker.ZIndex = 68
	playerActionPicker.Parent = UI

	local panel = Instance.new("ImageButton")
	panel.Name = "ParameterEnum"
	panel.AnchorPoint = Vector2.new(0.5, 0.5)
	panel.Position = UDim2.new(0.5, 0, 0.5, 0)
	panel.Size = UDim2.fromOffset(playerActionPickerDefaultSize.X, playerActionPickerDefaultSize.Y)
	panel.BackgroundTransparency = 1
	panel.BorderSizePixel = 0
	panel.AutoButtonColor = false
	panel.Modal = false
	panel.Image = "rbxassetid://72548733587158"
	panel.ImageColor3 = Color3.fromRGB(0, 0, 0)
	panel.ScaleType = Enum.ScaleType.Slice
	panel.SliceCenter = Rect.new(300, 300, 300, 300)
	panel.SliceScale = 0.5
	panel.ZIndex = 69
	panel.Parent = playerActionPicker

	local panelScale = Instance.new("UIScale")
	panelScale.Name = "scale"
	panelScale.Scale = 1
	panelScale.Parent = panel

	local shadow = Instance.new("ImageLabel")
	shadow.Name = "Shadow"
	shadow.AnchorPoint = Vector2.new(0.5, 0.5)
	shadow.Position = UDim2.new(0.5, 0, 0.5, 18)
	shadow.Size = UDim2.new(1, 74, 1, 76)
	shadow.BackgroundTransparency = 1
	shadow.Image = "rbxassetid://8992230677"
	shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
	shadow.ImageTransparency = 0.64
	shadow.ScaleType = Enum.ScaleType.Slice
	shadow.SliceCenter = Rect.new(99, 99, 99, 99)
	shadow.ZIndex = 68
	shadow.Parent = panel

	local frame = Instance.new("ImageButton")
	frame.Name = "frame"
	frame.AnchorPoint = Vector2.new(0.5, 0.5)
	frame.Position = UDim2.new(0.5, 0, 0.5, 0)
	frame.Size = UDim2.new(1, 0, 1, 0)
	frame.BackgroundTransparency = 1
	frame.BorderSizePixel = 0
	frame.ClipsDescendants = true
	frame.AutoButtonColor = false
	frame.Modal = true
	frame.Image = "rbxassetid://125088425775676"
	frame.ImageColor3 = Color3.fromRGB(13, 13, 13)
	frame.ScaleType = Enum.ScaleType.Slice
	frame.SliceCenter = Rect.new(512, 512, 512, 512)
	frame.SliceScale = 0.08
	frame.ZIndex = 70
	frame.Parent = panel

	local frameCorner = Instance.new("UICorner")
	frameCorner.CornerRadius = UDim.new(0, 24)
	frameCorner.Parent = frame

	local frameGradient = Instance.new("UIGradient")
	frameGradient.Rotation = 90
	frameGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(13, 13, 13)),
		ColorSequenceKeypoint.new(0.68, Color3.fromRGB(13, 13, 13)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(17, 31, 40))
	})
	frameGradient.Parent = frame

	local content = Instance.new("CanvasGroup")
	content.Name = "content"
	content.AnchorPoint = Vector2.new(0.5, 0.5)
	content.Position = UDim2.new(0.5, 0, 0.5, 0)
	content.Size = UDim2.new(1, 0, 1, 0)
	content.BackgroundTransparency = 1
	content.GroupTransparency = 1
	content.ClipsDescendants = true
	content.ZIndex = 71
	content.Parent = frame

	local contentCorner = Instance.new("UICorner")
	contentCorner.CornerRadius = UDim.new(0, 24)
	contentCorner.Parent = content

	local contentPadding = Instance.new("UIPadding")
	contentPadding.PaddingTop = UDim.new(0, 10)
	contentPadding.PaddingLeft = UDim.new(0, 10)
	contentPadding.PaddingRight = UDim.new(0, 10)
	contentPadding.Parent = content

	local scroll = Instance.new("ScrollingFrame")
	scroll.Name = "scroll"
	scroll.Position = UDim2.new(0.5, 0, 0, 0)
	scroll.AnchorPoint = Vector2.new(0.5, 0)
	scroll.Size = UDim2.new(1, 0, 1, -70)
	scroll.BackgroundTransparency = 1
	scroll.BorderSizePixel = 0
	scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
	scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
	scroll.ScrollBarImageTransparency = 1
	scroll.ScrollBarImageColor3 = Color3.fromRGB(0, 0, 0)
	scroll.ScrollBarThickness = 4
	scroll.ScrollingDirection = Enum.ScrollingDirection.Y
	scroll.ZIndex = 71
	scroll.Parent = content

	attachScrollbarFade(scroll, 0.68, 0.62)

	local scrollPadding = Instance.new("UIPadding")
	scrollPadding.PaddingTop = UDim.new(0, 76)
	scrollPadding.PaddingLeft = UDim.new(0, 10)
	scrollPadding.PaddingRight = UDim.new(0, 10)
	scrollPadding.PaddingBottom = UDim.new(0, 10)
	scrollPadding.Parent = scroll

	local scrollList = Instance.new("UIListLayout")
	scrollList.Name = "list"
	scrollList.FillDirection = Enum.FillDirection.Vertical
	scrollList.HorizontalAlignment = Enum.HorizontalAlignment.Center
	scrollList.VerticalAlignment = Enum.VerticalAlignment.Top
	scrollList.SortOrder = Enum.SortOrder.LayoutOrder
	scrollList.Padding = UDim.new(0, 10)
	scrollList.Parent = scroll

	local row = Instance.new("ImageButton")
	row.Name = "Spectate"
	row.Size = UDim2.new(1, 0, 0, 58)
	row.BackgroundColor3 = Color3.fromRGB(26, 26, 26)
	row.BackgroundTransparency = 0.08
	row.BorderSizePixel = 0
	row.AutoButtonColor = false
	row.Image = ""
	row.ClipsDescendants = true
	row.ZIndex = 71
	row.Parent = scroll

	local rowCorner = Instance.new("UICorner")
	rowCorner.CornerRadius = UDim.new(0, 16)
	rowCorner.Parent = row

	local rowPadding = Instance.new("UIPadding")
	rowPadding.PaddingTop = UDim.new(0, 8)
	rowPadding.PaddingBottom = UDim.new(0, 8)
	rowPadding.PaddingLeft = UDim.new(0, 14)
	rowPadding.PaddingRight = UDim.new(0, 50)
	rowPadding.Parent = row

	local rowList = Instance.new("UIListLayout")
	rowList.FillDirection = Enum.FillDirection.Horizontal
	rowList.HorizontalAlignment = Enum.HorizontalAlignment.Left
	rowList.VerticalAlignment = Enum.VerticalAlignment.Center
	rowList.SortOrder = Enum.SortOrder.LayoutOrder
	rowList.Padding = UDim.new(0, 12)
	rowList.Parent = row

	local rowStroke = Instance.new("UIStroke")
	rowStroke.Name = "stroke"
	rowStroke.Color = Color3.fromRGB(47, 47, 47)
	rowStroke.Thickness = 1
	rowStroke.Transparency = 0.12
	rowStroke.Parent = row

	local rowStrokeGradient = Instance.new("UIGradient")
	rowStrokeGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(54, 54, 54)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(25, 25, 25))
	})
	rowStrokeGradient.Rotation = 90
	rowStrokeGradient.Parent = rowStroke

	local emblem = Instance.new("Frame")
	emblem.Name = "emblem"
	emblem.Size = UDim2.new(0, 36, 0, 36)
	emblem.BackgroundColor3 = Color3.fromRGB(34, 34, 34)
	emblem.BackgroundTransparency = 0.02
	emblem.BorderSizePixel = 0
	emblem.ZIndex = 72
	emblem.Parent = row

	local emblemCorner = Instance.new("UICorner")
	emblemCorner.CornerRadius = UDim.new(0, 12)
	emblemCorner.Parent = emblem

	local emblemStroke = Instance.new("UIStroke")
	emblemStroke.Color = Color3.fromRGB(48, 48, 48)
	emblemStroke.Thickness = 1
	emblemStroke.Transparency = 0.1
	emblemStroke.Parent = emblem

	local emblemIcon = Instance.new("ImageLabel")
	emblemIcon.Name = "icon"
	emblemIcon.AnchorPoint = Vector2.new(0.5, 0.5)
	emblemIcon.Position = UDim2.new(0.5, 0, 0.5, 0)
	emblemIcon.Size = UDim2.new(0, 20, 0, 20)
	emblemIcon.BackgroundTransparency = 1
	emblemIcon.Image = "rbxassetid://138426819549450"
	emblemIcon.ImageColor3 = Color3.fromRGB(255, 116, 40)
	emblemIcon.ImageTransparency = 0.05
	emblemIcon.ZIndex = 73
	emblemIcon.Parent = emblem

	local value = Instance.new("TextLabel")
	value.Name = "value"
	value.AutomaticSize = Enum.AutomaticSize.Y
	value.Size = UDim2.new(1, -34, 0, 0)
	value.BackgroundTransparency = 1
	value.Text = "Spectate"
	value.TextColor3 = Color3.fromRGB(255, 255, 255)
	value.TextSize = 17
	value.TextWrapped = true
	value.TextXAlignment = Enum.TextXAlignment.Left
	value.TextYAlignment = Enum.TextYAlignment.Top
	value.FontFace = Font.new("rbxassetid://12187365364", Enum.FontWeight.Medium)
	value.ZIndex = 72
	value.Parent = row

		local valueFlex = Instance.new("UIFlexItem")
		valueFlex.FlexMode = Enum.UIFlexMode.Fill
		valueFlex.Parent = value

		local util = Instance.new("Folder")
		util.Name = "util"
		util.Parent = row

		local badge = Instance.new("Frame")
		badge.Name = "badge"
		badge.AnchorPoint = Vector2.new(1, 0.5)
		badge.Position = UDim2.new(1, 40, 0.5, 0)
		badge.Size = UDim2.new(0, 40, 1, 0)
		badge.BackgroundTransparency = 1
		badge.ZIndex = 72
		badge.Parent = util

	local selection = Instance.new("ImageLabel")
	selection.Name = "selection"
	selection.AnchorPoint = Vector2.new(0.5, 0.5)
	selection.Position = UDim2.new(0.5, 0, 0.5, 0)
	selection.Size = UDim2.new(0, 20, 0, 20)
	selection.BackgroundTransparency = 1
	selection.Image = "rbxassetid://91811711741572"
	selection.ImageColor3 = Color3.fromRGB(0, 140, 255)
	selection.ImageTransparency = 1
	selection.ZIndex = 73
	selection.Parent = badge

	local selectionScale = Instance.new("UIScale")
	selectionScale.Name = "scale"
	selectionScale.Scale = 0.000001
	selectionScale.Parent = selection

	local bottom = Instance.new("Frame")
	bottom.Name = "bottom"
	bottom.AnchorPoint = Vector2.new(0.5, 1)
	bottom.Position = UDim2.new(0.5, 0, 1, 0)
	bottom.Size = UDim2.new(1, 0, 0, 90)
	bottom.BackgroundColor3 = Color3.fromRGB(34, 34, 34)
	bottom.BorderSizePixel = 0
	bottom.ZIndex = 72
	bottom.Parent = content

	local bottomGradient = Instance.new("UIGradient")
	bottomGradient.Rotation = -90
	bottomGradient.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0),
		NumberSequenceKeypoint.new(1, 1)
	})
	bottomGradient.Parent = bottom

	local bottomPadding = Instance.new("UIPadding")
	bottomPadding.Parent = bottom

	local selections = Instance.new("ImageLabel")
	selections.Name = "Selections"
	selections.Size = UDim2.new(0, 94, 0, 70)
	selections.BackgroundTransparency = 1
	selections.Image = "rbxassetid://72548733587158"
	selections.ImageColor3 = Color3.fromRGB(0, 0, 0)
	selections.ImageTransparency = 0.5
	selections.ScaleType = Enum.ScaleType.Slice
	selections.SliceCenter = Rect.new(300, 300, 300, 300)
		selections.LayoutOrder = 1
		selections.ZIndex = 73
		selections.Visible = false
		selections.Parent = bottom

	local selectionsButton = Instance.new("ImageButton")
	selectionsButton.Name = "Button"
	selectionsButton.AnchorPoint = Vector2.new(0, 0.5)
	selectionsButton.Position = UDim2.new(0, 0, 0.5, 0)
	selectionsButton.Size = UDim2.new(0, 50, 0, 34)
	selectionsButton.BackgroundColor3 = Color3.fromRGB(27, 27, 27)
	selectionsButton.BorderSizePixel = 0
	selectionsButton.AutoButtonColor = false
	selectionsButton.Image = ""
	selectionsButton.ZIndex = 74
	selectionsButton.Parent = selections

	local selectionsButtonCorner = Instance.new("UICorner")
	selectionsButtonCorner.CornerRadius = UDim.new(1, 0)
	selectionsButtonCorner.Parent = selectionsButton

	local countLabel = Instance.new("TextLabel")
	countLabel.Name = "count"
	countLabel.AnchorPoint = Vector2.new(0.5, 0.5)
	countLabel.Position = UDim2.new(0.5, 10, 0.5, 5)
	countLabel.Size = UDim2.new(0, 0, 0, 20)
	countLabel.AutomaticSize = Enum.AutomaticSize.X
	countLabel.BackgroundTransparency = 1
	countLabel.Text = "1"
	countLabel.TextColor3 = Color3.fromRGB(0, 140, 255)
	countLabel.TextTransparency = 0.3
	countLabel.TextSize = 14
	countLabel.TextXAlignment = Enum.TextXAlignment.Right
	countLabel.FontFace = Font.new("rbxassetid://12187365364", Enum.FontWeight.SemiBold)
	countLabel.ZIndex = 75
	countLabel.Parent = selectionsButton

	local search = Instance.new("ImageLabel")
	search.Name = "Search"
	search.AnchorPoint = Vector2.new(0, 0)
	search.Position = UDim2.new(0, 20, 0, 10)
	search.Size = UDim2.new(1, -178, 0, 70)
	search.BackgroundTransparency = 1
	search.Image = "rbxassetid://72548733587158"
	search.ImageColor3 = Color3.fromRGB(0, 0, 0)
	search.ImageTransparency = 0.5
	search.ScaleType = Enum.ScaleType.Slice
	search.SliceCenter = Rect.new(300, 300, 300, 300)
	search.LayoutOrder = 2
	search.ZIndex = 73
	search.Parent = bottom

	local searchField = Instance.new("ImageButton")
	searchField.Name = "Field"
	searchField.AnchorPoint = Vector2.new(0, 0.5)
	searchField.Position = UDim2.new(0, 0, 0.5, 0)
	searchField.Size = UDim2.new(1, 0, 0, 34)
	searchField.BackgroundColor3 = Color3.fromRGB(27, 27, 27)
	searchField.BorderSizePixel = 0
	searchField.AutoButtonColor = false
	searchField.Image = ""
	searchField.ZIndex = 74
	searchField.Parent = search

	local searchFieldCorner = Instance.new("UICorner")
	searchFieldCorner.CornerRadius = UDim.new(1, 0)
	searchFieldCorner.Parent = searchField

	local searchPadding = Instance.new("UIPadding")
	searchPadding.PaddingLeft = UDim.new(0, 20)
	searchPadding.PaddingRight = UDim.new(0, 20)
	searchPadding.Parent = searchField

	local searchBox = Instance.new("TextBox")
	searchBox.Name = "textbox"
	searchBox.Size = UDim2.new(1, 0, 1, 0)
	searchBox.BackgroundTransparency = 1
	searchBox.ClearTextOnFocus = true
	searchBox.PlaceholderText = "Search..."
	searchBox.PlaceholderColor3 = Color3.fromRGB(56, 56, 56)
	searchBox.Text = ""
	searchBox.TextColor3 = Color3.fromRGB(255, 255, 255)
	searchBox.TextSize = 15
	searchBox.TextXAlignment = Enum.TextXAlignment.Left
	searchBox.FontFace = Font.new("rbxassetid://12187365364", Enum.FontWeight.Regular)
	searchBox.ZIndex = 75
	searchBox.Parent = searchField

	local select = Instance.new("ImageLabel")
	select.Name = "Select"
	select.AnchorPoint = Vector2.new(1, 0)
	select.Position = UDim2.new(1, -20, 0, 10)
	select.Size = UDim2.new(0, 128, 0, 70)
	select.BackgroundTransparency = 1
	select.Image = "rbxassetid://72548733587158"
	select.ImageColor3 = Color3.fromRGB(0, 0, 0)
	select.ImageTransparency = 0.5
	select.ScaleType = Enum.ScaleType.Slice
	select.SliceCenter = Rect.new(300, 300, 300, 300)
	select.LayoutOrder = 3
	select.ZIndex = 73
	select.Parent = bottom

	local selectButton = Instance.new("TextButton")
	selectButton.Name = "Button"
	selectButton.AnchorPoint = Vector2.new(0.5, 0.5)
	selectButton.Position = UDim2.new(0.5, 0, 0.5, 0)
	selectButton.AutomaticSize = Enum.AutomaticSize.X
	selectButton.Size = UDim2.new(0, 0, 0, 34)
	selectButton.BackgroundColor3 = Color3.fromRGB(0, 140, 255)
	selectButton.BorderSizePixel = 0
	selectButton.Text = "Select"
	selectButton.TextColor3 = Color3.fromRGB(255, 255, 255)
	selectButton.TextSize = 15
	selectButton.FontFace = Font.new("rbxassetid://12187365364", Enum.FontWeight.SemiBold)
	selectButton.AutoButtonColor = false
	selectButton.ZIndex = 74
	selectButton.Parent = select

	local selectPadding = Instance.new("UIPadding")
	selectPadding.PaddingLeft = UDim.new(0, 24)
	selectPadding.PaddingRight = UDim.new(0, 24)
	selectPadding.Parent = selectButton

		local selectCorner = Instance.new("UICorner")
		selectCorner.CornerRadius = UDim.new(1, 0)
		selectCorner.Parent = selectButton

		local selectScale = Instance.new("UIScale")
		selectScale.Name = "scale"
		selectScale.Scale = 1
		selectScale.Parent = selectButton

	local top = Instance.new("Frame")
	top.Name = "Top"
	top.AnchorPoint = Vector2.new(0.5, 0)
	top.Position = UDim2.new(0.5, 0, 0, 0)
	top.Size = UDim2.new(1, 0, 0, 74)
	top.BackgroundTransparency = 1
	top.ZIndex = 99999
	top.Parent = frame

	local topPadding = Instance.new("UIPadding")
	topPadding.PaddingTop = UDim.new(0, 18)
	topPadding.PaddingLeft = UDim.new(0, 22)
	topPadding.PaddingRight = UDim.new(0, 18)
	topPadding.Parent = top

	local titlePill = Instance.new("ImageLabel")
	titlePill.Name = "Title"
	titlePill.AnchorPoint = Vector2.new(0, 0)
	titlePill.Position = UDim2.new(0, 0, 0, 0)
	titlePill.Size = UDim2.new(0, 260, 0, 42)
	titlePill.BackgroundTransparency = 1
	titlePill.Image = ""
	titlePill.ImageTransparency = 1
	titlePill.LayoutOrder = 1
	titlePill.ZIndex = 100000
	titlePill.Parent = top

	local titleText = Instance.new("TextButton")
	titleText.Name = "Container"
	titleText.AnchorPoint = Vector2.new(0, 0.5)
	titleText.Position = UDim2.new(0, 0, 0.5, 0)
	titleText.Size = UDim2.new(1, 0, 0, 34)
	titleText.BackgroundTransparency = 1
	titleText.BorderSizePixel = 0
	titleText.AutoButtonColor = false
	titleText.Text = ""
	titleText.TextColor3 = Color3.fromRGB(255, 255, 255)
	titleText.TextSize = 20
	titleText.TextXAlignment = Enum.TextXAlignment.Left
	titleText.FontFace = Font.new("rbxassetid://12187365364", Enum.FontWeight.Bold)
	titleText.Visible = false
	titleText.ZIndex = 100001
	titleText.Parent = titlePill

	local titleTextCorner = Instance.new("UICorner")
	titleTextCorner.CornerRadius = UDim.new(0, 0)
	titleTextCorner.Parent = titleText

	local titlePadding = Instance.new("UIPadding")
	titlePadding.PaddingLeft = UDim.new(0, 0)
	titlePadding.PaddingRight = UDim.new(0, 0)
	titlePadding.Parent = titleText

	local closePill = Instance.new("ImageLabel")
	closePill.Name = "Close"
	closePill.AnchorPoint = Vector2.new(1, 0)
	closePill.Position = UDim2.new(1, 0, 0, 0)
	closePill.Size = UDim2.new(0, 40, 0, 40)
	closePill.BackgroundTransparency = 1
	closePill.Image = "rbxassetid://72548733587158"
	closePill.ImageColor3 = Color3.fromRGB(0, 0, 0)
	closePill.ImageTransparency = 1
	closePill.ScaleType = Enum.ScaleType.Slice
	closePill.LayoutOrder = 2
	closePill.ZIndex = 100000
	closePill.Parent = top

	local close = Instance.new("ImageButton")
	close.Name = "Button"
	close.AnchorPoint = Vector2.new(0.5, 0.5)
	close.Position = UDim2.new(0.5, 0, 0.5, 0)
	close.Size = UDim2.new(0, 30, 0, 30)
	close.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
	close.BackgroundTransparency = 1
	close.BorderSizePixel = 0
	close.AutoButtonColor = false
	close.Image = "rbxassetid://10747384394"
	close.ImageColor3 = Color3.fromRGB(210, 210, 210)
	close.ZIndex = 100001
	close.Parent = closePill

	local closeCorner = Instance.new("UICorner")
	closeCorner.CornerRadius = UDim.new(1, 0)
	closeCorner.Parent = close

	local resizeHandle = Instance.new("ImageButton")
	resizeHandle.Name = "Resize"
	resizeHandle.AnchorPoint = Vector2.new(1, 1)
	resizeHandle.Position = UDim2.new(1, -8, 1, -8)
	resizeHandle.Size = UDim2.new(0, 54, 0, 54)
	resizeHandle.BackgroundTransparency = 1
	resizeHandle.BorderSizePixel = 0
	resizeHandle.AutoButtonColor = false
	resizeHandle.Image = ""
	resizeHandle.ZIndex = 100000
	resizeHandle.Parent = frame

	local resizeIcon = Instance.new("ImageLabel")
	resizeIcon.Name = "icon"
	resizeIcon.AnchorPoint = Vector2.new(1, 1)
	resizeIcon.Position = UDim2.new(1, 0, 1, 0)
	resizeIcon.Size = UDim2.fromOffset(18, 18)
	resizeIcon.BackgroundTransparency = 1
	resizeIcon.Image = "rbxassetid://86527207319523"
	resizeIcon.ImageColor3 = Color3.fromRGB(59, 59, 59)
	resizeIcon.ImageTransparency = 0
	resizeIcon.ScaleType = Enum.ScaleType.Slice
	resizeIcon.SliceCenter = Rect.new(51, 52, 51, 52)
	resizeIcon.SliceScale = 0.5
	resizeIcon.ZIndex = 100001
	resizeIcon.Parent = resizeHandle

	local pickerDragging = false
	local pickerResizing = false
	local dragOffset
	local resizeStartPosition
	local resizeStartSize
	local resizeStartTopLeft

	local function pickerRootSize()
		local rootSize = camera.ViewportSize
		pcall(function()
			if UI.AbsoluteSize.X > 0 and UI.AbsoluteSize.Y > 0 then
				rootSize = UI.AbsoluteSize
			end
		end)
		return rootSize
	end

	local function inputIsPointer(input)
		return input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch
	end

	local function normalizedResizeCursorPosition(button)
		local mouseLocation = userInputService:GetMouseLocation()
		local guiInsetY = 0
		pcall(function()
			guiInsetY = game:GetService("GuiService"):GetGuiInset().Y
		end)
		local buttonPosition = button.AbsolutePosition
		local buttonSize = button.AbsoluteSize
		local correctedMouseY = mouseLocation.Y - guiInsetY
		local relativeX = (mouseLocation.X - buttonPosition.X) / buttonSize.X
		local relativeY = (correctedMouseY - buttonPosition.Y) / buttonSize.Y
		return Vector2.new(1 - math.clamp(relativeX, 0, 1), 1 - math.clamp(relativeY, 0, 1))
	end

	local function resetResizeIcon()
		tweenService:Create(resizeIcon, TweenInfo.new(0.18, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
			Size = UDim2.fromOffset(18, 18),
			ImageColor3 = Color3.fromRGB(59, 59, 59)
		}):Play()
	end

	top.InputBegan:Connect(function(input, processed)
		if processed or not inputIsPointer(input) then return end
		local inputPosition = Vector2.new(input.Position.X, input.Position.Y)
		if inputPosition.X > close.AbsolutePosition.X - 8 then return end

		pickerDragging = true
		dragOffset = inputPosition - panel.AbsolutePosition
	end)

	resizeHandle.InputBegan:Connect(function(input, processed)
		if processed or not inputIsPointer(input) then return end

		pickerResizing = true
		resizeStartPosition = Vector2.new(input.Position.X, input.Position.Y)
		resizeStartSize = panel.AbsoluteSize
		resizeStartTopLeft = panel.AbsolutePosition
		tweenService:Create(resizeIcon, TweenInfo.new(0.12, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
			Size = UDim2.fromOffset(30, 30),
			ImageColor3 = Color3.fromRGB(72, 72, 72)
		}):Play()
	end)

	userInputService.InputEnded:Connect(function(input)
		if not inputIsPointer(input) then return end

		pickerDragging = false
		pickerResizing = false
		resetResizeIcon()
	end)

	userInputService.InputChanged:Connect(function(input)
		if not playerActionPickerState.open then return end

		if pickerDragging and not pickerResizing and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			local rootSize = pickerRootSize()
			local panelSize = panel.AbsoluteSize
			local mousePosition = Vector2.new(input.Position.X, input.Position.Y)
			local topLeft = mousePosition - dragOffset
			local clampedTopLeft = Vector2.new(
				math.clamp(topLeft.X, -80, rootSize.X - panelSize.X + 80),
				math.clamp(topLeft.Y, -40, rootSize.Y - panelSize.Y + 80)
			)
			local center = clampedTopLeft + panelSize / 2
			panel.Position = UDim2.fromOffset(center.X, center.Y)
		elseif pickerResizing and not pickerDragging and resizeStartPosition and resizeStartSize and resizeStartTopLeft then
			local rootSize = pickerRootSize()
			local delta = Vector2.new(input.Position.X, input.Position.Y) - resizeStartPosition
			local maxWidth = math.min(playerActionPickerMaxSize.X, math.max(playerActionPickerMinSize.X, rootSize.X - 80))
			local maxHeight = math.min(playerActionPickerMaxSize.Y, math.max(playerActionPickerMinSize.Y, rootSize.Y - 80))
			local newWidth = math.clamp(resizeStartSize.X + delta.X, playerActionPickerMinSize.X, maxWidth)
			local newHeight = math.clamp(resizeStartSize.Y + delta.Y, playerActionPickerMinSize.Y, maxHeight)
			local nextCenter = resizeStartTopLeft + Vector2.new(newWidth, newHeight) / 2
			playerActionPickerCurrentSize = clampPlayerActionPickerSize(Vector2.new(newWidth, newHeight))
			panel.Position = UDim2.fromOffset(nextCenter.X, nextCenter.Y)
			panel.Size = UDim2.fromOffset(newWidth, newHeight)
		end
	end)

	resizeHandle.MouseEnter:Connect(function()
		local normals = normalizedResizeCursorPosition(resizeHandle)
		tweenService:Create(resizeIcon, TweenInfo.new(0.18, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
			Size = UDim2.fromOffset(20 + (normals.X * 30), 20 + (normals.Y * 30)),
			ImageColor3 = Color3.fromRGB(70, 70, 70)
		}):Play()
	end)

	resizeHandle.MouseLeave:Connect(function()
		if pickerResizing then return end
		resetResizeIcon()
	end)

	resizeHandle.InputChanged:Connect(function(input)
		if input.UserInputType ~= Enum.UserInputType.MouseMovement and input.UserInputType ~= Enum.UserInputType.Touch then return end
		local normals = normalizedResizeCursorPosition(resizeHandle)
		tweenService:Create(resizeIcon, TweenInfo.new(0.14, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
			Size = UDim2.fromOffset(20 + (normals.X * 30), 20 + (normals.Y * 30)),
			ImageColor3 = Color3.fromRGB(70, 70, 70)
		}):Play()
	end)

	close.MouseButton1Click:Connect(_picker.closePlayerActionPicker)

	close.MouseEnter:Connect(function()
		tweenService:Create(close, TweenInfo.new(0.16, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.04, ImageColor3 = Color3.fromRGB(255, 255, 255)}):Play()
	end)

	close.MouseLeave:Connect(function()
		tweenService:Create(close, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.14, ImageColor3 = Color3.fromRGB(238, 238, 238)}):Play()
	end)

	row.MouseEnter:Connect(function()
		if not playerActionPickerState.open then return end
		tweenService:Create(row, TweenInfo.new(0.36, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0, BackgroundColor3 = Color3.fromRGB(31, 31, 31)}):Play()
		tweenService:Create(rowStroke, TweenInfo.new(0.42, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0, Color = Color3.fromRGB(66, 66, 66)}):Play()
		tweenService:Create(emblem, TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0}):Play()
		tweenService:Create(emblemStroke, TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0}):Play()
	end)

	row.MouseLeave:Connect(function()
		tweenService:Create(row, TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.08, BackgroundColor3 = Color3.fromRGB(26, 26, 26)}):Play()
		tweenService:Create(rowStroke, TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.12, Color = Color3.fromRGB(47, 47, 47)}):Play()
		tweenService:Create(emblem, TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.02}):Play()
		tweenService:Create(emblemStroke, TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.1}):Play()
	end)

	row.MouseButton1Click:Connect(function()
		playerActionPickerState.selectedSpectate = not playerActionPickerState.selectedSpectate
		_picker.updatePlayerActionPickerSelection()
	end)

	searchBox:GetPropertyChangedSignal("Text"):Connect(function()
		local query = searchBox.Text:lower()
		row.Visible = query == "" or string.find("spectate", query, 1, true) ~= nil
	end)

		searchBox.Focused:Connect(function()
			tweenService:Create(bottomPadding, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {PaddingLeft = UDim.new(0, 15), PaddingRight = UDim.new(0, 15)}):Play()
			tweenService:Create(search, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Position = UDim2.new(0, 15, 0, 10), Size = UDim2.new(1, -30, 0, 70), ImageTransparency = 0.5}):Play()
			tweenService:Create(select, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Position = UDim2.new(1, -15, 0, 10)}):Play()
			tweenService:Create(select, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Size = UDim2.new(0, 0, 0, 70), ImageTransparency = 1}):Play()
			tweenService:Create(selectScale, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Scale = 0.000001}):Play()
		end)

		searchBox.FocusLost:Connect(function()
			tweenService:Create(bottomPadding, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {PaddingLeft = UDim.new(0, 0), PaddingRight = UDim.new(0, 0)}):Play()
			tweenService:Create(search, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Position = UDim2.new(0, 20, 0, 10), Size = UDim2.new(1, -178, 0, 70), ImageTransparency = 0.5}):Play()
			tweenService:Create(select, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Position = UDim2.new(1, -20, 0, 10), Size = UDim2.new(0, 128, 0, 70), ImageTransparency = 0.5}):Play()
			tweenService:Create(selectScale, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Scale = 1}):Play()
		end)

	selectButton.MouseEnter:Connect(function()
		tweenService:Create(selectButton, TweenInfo.new(0.16, Enum.EasingStyle.Quint), {BackgroundColor3 = Color3.fromRGB(40, 155, 255)}):Play()
	end)

	selectButton.MouseLeave:Connect(function()
		tweenService:Create(selectButton, TweenInfo.new(0.16, Enum.EasingStyle.Quint), {BackgroundColor3 = Color3.fromRGB(0, 140, 255)}):Play()
	end)

	selectButton.MouseButton1Click:Connect(function()
		local targetPlayer = playerActionPickerState.targetPlayer
		if not playerActionPickerState.selectedSpectate then
			queueNotification("Selection Failed", "Select at least 1 action.")
			return
		end

		_picker.closePlayerActionPicker()

		task.delay(0.12, function()
			if targetPlayer and players:FindFirstChild(targetPlayer.Name) then
				startSpectating(targetPlayer)
			end
		end)
	end)

	playerActionPickerParts = {
		Panel = panel,
		Shadow = shadow,
		Frame = frame,
		PanelScale = panelScale,
		Content = content,
		Scroll = scroll,
		SpectateRow = row,
		SelectionImage = selection,
			SelectionScale = selectionScale,
			CountLabel = countLabel,
			BottomPadding = bottomPadding,
			SearchBox = searchBox,
			Search = search,
			Select = select,
			SelectButton = selectButton,
			SelectScale = selectScale
		}

	return playerActionPicker
end

		openPlayerActionPicker = function(targetPlayer, source, restorePanel)
		if not targetPlayer or targetPlayer == localPlayer then return end

		ensurePlayerActionPicker()

		playerActionPickerState.token += 1
		local openToken = playerActionPickerState.token
		playerActionPickerState.open = true
		playerActionPickerState.targetPlayer = targetPlayer
		playerActionPickerState.selectedSpectate = false
		playerActionPickerState.selectedTrack = false
		playerActionPickerState.selectedLiveTrack = false
		playerActionPickerState.selectedTracer = false
		playerActionPickerState.selectedHide = false
		playerActionPickerState.selectedEsp = false
		playerActionPickerState.restorePanel = nil

		local panel = playerActionPickerParts.Panel

		playerActionPicker.Visible = true
		panel.BackgroundTransparency = 1
		panel.BackgroundColor3 = siriusValues.selectActionStyle.panel
		if playerActionPickerParts.Shadow then
			playerActionPickerParts.Shadow.ImageTransparency = 1
			playerActionPickerParts.Shadow.Position = UDim2.new(0.5, 0, 0.5, 14)
		end
		if playerActionPickerParts.Stroke then
			playerActionPickerParts.Stroke.Transparency = 1
		end
		playerActionPickerParts.Content.GroupTransparency = 1
		profileInfoTools.Refresh(playerActionPickerParts, targetPlayer)
		if playerActionPickerParts.ActionScroll then
			playerActionPickerParts.ActionScroll.CanvasPosition = Vector2.zero
		end
		if playerActionPickerParts.RefreshActionScrollFades then
			task.defer(playerActionPickerParts.RefreshActionScrollFades)
		end
		if playerActionPickerParts.SpectateRow then
			playerActionPickerParts.SpectateRow.BackgroundColor3 = siriusValues.selectActionStyle.row
			playerActionPickerParts.SpectateRow.BackgroundTransparency = 0.04
		end
		if playerActionPickerParts.SpectateStroke then
			playerActionPickerParts.SpectateStroke.Color = siriusValues.selectActionStyle.stroke
			playerActionPickerParts.SpectateStroke.Transparency = 0.18
		end
		if playerActionPickerParts.SpectateIcon then
			playerActionPickerParts.SpectateIcon.BackgroundColor3 = siriusValues.selectActionStyle.icon
		end
		if playerActionPickerParts.SpectateGlyphImage then
			playerActionPickerParts.SpectateGlyphImage.Image = getSelectActionIcon("Spectate")
			playerActionPickerParts.SpectateGlyphImage.ImageTransparency = 0.04
		end
		local function resetActionRow(rowObject, strokeObject, iconObject)
			if rowObject then
				rowObject.BackgroundColor3 = siriusValues.selectActionStyle.row
				rowObject.BackgroundTransparency = 0.04
			end
			if strokeObject then
				strokeObject.Color = siriusValues.selectActionStyle.stroke
				strokeObject.Transparency = 0.18
			end
			if iconObject then
				iconObject.BackgroundColor3 = siriusValues.selectActionStyle.icon
			end
		end
		resetActionRow(playerActionPickerParts.TrackRow, playerActionPickerParts.TrackStroke, playerActionPickerParts.TrackIcon)
		resetActionRow(playerActionPickerParts.LiveTrackRow, playerActionPickerParts.LiveTrackStroke, playerActionPickerParts.LiveTrackIcon)
		resetActionRow(playerActionPickerParts.TracerRow, playerActionPickerParts.TracerStroke, playerActionPickerParts.TracerIcon)
		resetActionRow(playerActionPickerParts.HideRow, playerActionPickerParts.HideStroke, playerActionPickerParts.HideIcon)
		resetActionRow(playerActionPickerParts.EspRow, playerActionPickerParts.EspStroke, playerActionPickerParts.EspIcon)
		if playerActionPickerParts.WieldRows then
			for _, rowParts in pairs(playerActionPickerParts.WieldRows) do
				resetActionRow(rowParts.Row, rowParts.Stroke, rowParts.Icon)
				if rowParts.GlyphImage then
					rowParts.GlyphImage.Image = getSelectActionIcon(rowParts.IconName)
					rowParts.GlyphImage.ImageTransparency = 0.04
				end
				if rowParts.Dot then
					rowParts.Dot.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
					rowParts.Dot.BackgroundTransparency = 1
				end
				if rowParts.Glyph then
					rowParts.Glyph.Text = ""
					rowParts.Glyph.TextTransparency = 1
					rowParts.Glyph.TextSize = 19
				end
			end
		end
		if playerActionPickerParts.TrackGlyphImage then
			playerActionPickerParts.TrackGlyphImage.Image = getSelectActionIcon("Track")
			playerActionPickerParts.TrackGlyphImage.ImageTransparency = 0.04
		end
		if playerActionPickerParts.LiveTrackGlyphImage then
			playerActionPickerParts.LiveTrackGlyphImage.Image = getSelectActionIcon("LiveTrack")
			playerActionPickerParts.LiveTrackGlyphImage.ImageTransparency = 0.04
		end
		if playerActionPickerParts.TracerGlyphImage then
			playerActionPickerParts.TracerGlyphImage.Image = getSelectActionIcon("Tracer")
			playerActionPickerParts.TracerGlyphImage.ImageTransparency = 0.04
		end
		if playerActionPickerParts.HideGlyphImage then
			playerActionPickerParts.HideGlyphImage.Image = getSelectActionIcon("Hide")
			playerActionPickerParts.HideGlyphImage.ImageTransparency = 0.04
		end
		if playerActionPickerParts.EspGlyphImage then
			playerActionPickerParts.EspGlyphImage.Image = getSelectActionIcon("ESP")
			playerActionPickerParts.EspGlyphImage.ImageTransparency = 0.04
		end
		local isTracked = trackingBadges[targetPlayer] ~= nil
		local isTracerEnabled = playerTracers[targetPlayer] ~= nil
		local isHidden = hiddenPlayers[targetPlayer] ~= nil
		local isEspEnabled = locatedPlayers[targetPlayer.Name] == true
		refreshWieldingActionRows(targetPlayer, false)
		if playerActionPickerParts.TrackText then
			playerActionPickerParts.TrackText.Text = "Track"
		end
		if playerActionPickerParts.TrackDot then
			playerActionPickerParts.TrackDot.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
			playerActionPickerParts.TrackDot.BackgroundTransparency = 1
		end
		if playerActionPickerParts.TrackGlyph then
			playerActionPickerParts.TrackGlyph.Text = ""
			playerActionPickerParts.TrackGlyph.TextTransparency = 1
			playerActionPickerParts.TrackGlyph.TextSize = 19
		end
		if playerActionPickerParts.TrackSwitch then
			playerActionPickerParts.TrackSwitch.BackgroundColor3 = isTracked and siriusValues.selectActionStyle.switchActive or siriusValues.selectActionStyle.switch
		end
		if playerActionPickerParts.TrackSwitchStroke then
			playerActionPickerParts.TrackSwitchStroke.Color = isTracked and siriusValues.selectActionStyle.switchStrokeActive or siriusValues.selectActionStyle.switchStroke
			playerActionPickerParts.TrackSwitchStroke.Transparency = isTracked and 0.08 or 0.28
		end
		if playerActionPickerParts.TrackKnob then
			playerActionPickerParts.TrackKnob.Position = isTracked and UDim2.new(1, -13, 0.5, 0) or UDim2.new(0, 13, 0.5, 0)
			playerActionPickerParts.TrackKnob.BackgroundColor3 = isTracked and Color3.fromRGB(245, 245, 245) or Color3.fromRGB(110, 110, 110)
		end
		if playerActionPickerParts.TracerText then
			playerActionPickerParts.TracerText.Text = "Tracer"
		end
		if playerActionPickerParts.TracerDot then
			playerActionPickerParts.TracerDot.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
			playerActionPickerParts.TracerDot.BackgroundTransparency = 1
		end
		if playerActionPickerParts.TracerGlyph then
			playerActionPickerParts.TracerGlyph.Text = ""
			playerActionPickerParts.TracerGlyph.TextTransparency = 1
			playerActionPickerParts.TracerGlyph.TextSize = 19
		end
		if playerActionPickerParts.TracerSwitch then
			playerActionPickerParts.TracerSwitch.BackgroundColor3 = isTracerEnabled and siriusValues.selectActionStyle.switchActive or siriusValues.selectActionStyle.switch
		end
		if playerActionPickerParts.TracerSwitchStroke then
			playerActionPickerParts.TracerSwitchStroke.Color = isTracerEnabled and siriusValues.selectActionStyle.switchStrokeActive or siriusValues.selectActionStyle.switchStroke
			playerActionPickerParts.TracerSwitchStroke.Transparency = isTracerEnabled and 0.12 or 0.28
		end
		if playerActionPickerParts.TracerKnob then
			playerActionPickerParts.TracerKnob.Position = isTracerEnabled and UDim2.new(1, -13, 0.5, 0) or UDim2.new(0, 13, 0.5, 0)
			playerActionPickerParts.TracerKnob.BackgroundColor3 = isTracerEnabled and Color3.fromRGB(245, 245, 245) or Color3.fromRGB(110, 110, 110)
		end
		if playerActionPickerParts.HideText then
			playerActionPickerParts.HideText.Text = "Hide"
		end
		if playerActionPickerParts.HideDot then
			playerActionPickerParts.HideDot.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
			playerActionPickerParts.HideDot.BackgroundTransparency = 1
		end
		if playerActionPickerParts.HideGlyph then
			playerActionPickerParts.HideGlyph.Text = ""
			playerActionPickerParts.HideGlyph.TextTransparency = 1
			playerActionPickerParts.HideGlyph.TextSize = 19
		end
		if playerActionPickerParts.HideSwitch then
			playerActionPickerParts.HideSwitch.BackgroundColor3 = isHidden and siriusValues.selectActionStyle.switchActive or siriusValues.selectActionStyle.switch
		end
		if playerActionPickerParts.HideSwitchStroke then
			playerActionPickerParts.HideSwitchStroke.Color = isHidden and siriusValues.selectActionStyle.switchStrokeActive or siriusValues.selectActionStyle.switchStroke
			playerActionPickerParts.HideSwitchStroke.Transparency = isHidden and 0.12 or 0.28
		end
		if playerActionPickerParts.HideKnob then
			playerActionPickerParts.HideKnob.Position = isHidden and UDim2.new(1, -13, 0.5, 0) or UDim2.new(0, 13, 0.5, 0)
			playerActionPickerParts.HideKnob.BackgroundColor3 = isHidden and Color3.fromRGB(245, 245, 245) or Color3.fromRGB(110, 110, 110)
		end
		if playerActionPickerParts.EspText then
			playerActionPickerParts.EspText.Text = "Extrasensory Perception"
		end
		if playerActionPickerParts.EspDot then
			playerActionPickerParts.EspDot.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
			playerActionPickerParts.EspDot.BackgroundTransparency = 1
		end
		if playerActionPickerParts.EspGlyph then
			playerActionPickerParts.EspGlyph.Text = ""
			playerActionPickerParts.EspGlyph.TextTransparency = 1
			playerActionPickerParts.EspGlyph.TextSize = 19
		end
		if playerActionPickerParts.EspSwitch then
			playerActionPickerParts.EspSwitch.BackgroundColor3 = isEspEnabled and siriusValues.selectActionStyle.switchActive or siriusValues.selectActionStyle.switch
		end
		if playerActionPickerParts.EspSwitchStroke then
			playerActionPickerParts.EspSwitchStroke.Color = isEspEnabled and siriusValues.selectActionStyle.switchStrokeActive or siriusValues.selectActionStyle.switchStroke
			playerActionPickerParts.EspSwitchStroke.Transparency = isEspEnabled and 0.08 or 0.28
		end
		if playerActionPickerParts.EspKnob then
			playerActionPickerParts.EspKnob.Position = isEspEnabled and UDim2.new(1, -13, 0.5, 0) or UDim2.new(0, 13, 0.5, 0)
			playerActionPickerParts.EspKnob.BackgroundColor3 = isEspEnabled and Color3.fromRGB(245, 245, 245) or Color3.fromRGB(110, 110, 110)
		end

				playerActionPickerCurrentSize = clampPlayerActionPickerSize(playerActionPickerCurrentSize)

				local targetPosition = UDim2.new(0.5, 0, 0.5, 0)
				local targetSize = playerActionPickerCurrentSize
				local restoreFromSpectateToggle = source == "spectate-toggle"

				if restoreFromSpectateToggle then
					local startPosition, startSize = getSpectateTogglePickerGeometry()
					local morphDuration = 0.28
					playerActionPicker.Visible = false
					panel.Position = targetPosition
					panel.Size = UDim2.fromOffset(targetSize.X, targetSize.Y)
					panel.BackgroundColor3 = siriusValues.selectActionStyle.panel
					panel.BackgroundTransparency = 0
					playerActionPickerParts.Content.GroupTransparency = 1
					if playerActionPickerParts.Shadow then
						playerActionPickerParts.Shadow.Position = UDim2.new(0.5, 0, 0.5, 16)
						playerActionPickerParts.Shadow.Size = UDim2.fromOffset(targetSize.X + 76, targetSize.Y + 78)
						playerActionPickerParts.Shadow.ImageTransparency = 0.82
					end
					if playerActionPickerParts.Stroke then
						playerActionPickerParts.Stroke.Transparency = 0.18
					end
					if playerActionPickerParts.PanelScale then
						playerActionPickerParts.PanelScale.Scale = 1
					end
					if spectateBar then
						spectateBar.Visible = false
						resetSpectateBarGeometry()
					end

					playSpectateGhostMorph(startPosition, startSize, targetPosition, targetSize, morphDuration, "to-panel", function()
						if not playerActionPicker or not playerActionPickerState.open or playerActionPickerState.token ~= openToken then return end
						playerActionPicker.Visible = true
						panel.Position = targetPosition
						panel.Size = UDim2.fromOffset(targetSize.X, targetSize.Y)
						panel.BackgroundColor3 = siriusValues.selectActionStyle.panel
						panel.BackgroundTransparency = 0
						if playerActionPickerParts.Shadow then
							playerActionPickerParts.Shadow.Position = UDim2.new(0.5, 0, 0.5, 16)
							playerActionPickerParts.Shadow.Size = UDim2.fromOffset(targetSize.X + 76, targetSize.Y + 78)
							playerActionPickerParts.Shadow.ImageTransparency = 0.82
						end
						if playerActionPickerParts.Stroke then
							playerActionPickerParts.Stroke.Transparency = 0.18
						end
						if playerActionPickerParts.PanelScale then
							playerActionPickerParts.PanelScale.Scale = 1
						end
						playerActionPickerParts.Content.GroupTransparency = 1
						tweenService:Create(playerActionPickerParts.Content, TweenInfo.new(0.12, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
							GroupTransparency = 0
						}):Play()
					end)

					task.delay(math.max(0.04, morphDuration - 0.035), function()
						if playerActionPickerState.open and playerActionPickerState.token == openToken and playerActionPicker then
							playerActionPicker.Visible = true
							playerActionPickerParts.Content.GroupTransparency = 1
						end
					end)
					return
				end

					local startSize = Vector2.new(
						math.max(260, targetSize.X - 42),
						math.max(18, math.min(42, targetSize.Y * 0.08))
					)
					local panelOpenTween = TweenInfo.new(0.48, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
					local panelFadeTween = TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
					local contentTween = TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

					panel.Position = targetPosition
					panel.Size = UDim2.fromOffset(startSize.X, startSize.Y)
					panel.BackgroundTransparency = 1
					playerActionPickerParts.PanelScale.Scale = 1

					if playerActionPickerParts.Shadow then
						playerActionPickerParts.Shadow.Position = UDim2.new(0.5, 0, 0.5, 10)
						playerActionPickerParts.Shadow.Size = UDim2.fromOffset(startSize.X + 38, startSize.Y + 36)
						playerActionPickerParts.Shadow.ImageTransparency = 1
					end

					tweenService:Create(panel, panelOpenTween, {
						Size = UDim2.fromOffset(targetSize.X, targetSize.Y)
					}):Play()
					tweenService:Create(panel, panelFadeTween, {
						BackgroundTransparency = 0
					}):Play()
					if playerActionPickerParts.Shadow then
						tweenService:Create(playerActionPickerParts.Shadow, panelOpenTween, {
							Position = UDim2.new(0.5, 0, 0.5, 16),
							Size = UDim2.fromOffset(targetSize.X + 76, targetSize.Y + 78)
						}):Play()
						tweenService:Create(playerActionPickerParts.Shadow, panelFadeTween, {
							ImageTransparency = 0.82
						}):Play()
					end
					if playerActionPickerParts.Stroke then
						tweenService:Create(playerActionPickerParts.Stroke, panelFadeTween, {Transparency = 0.18}):Play()
					end

					task.delay(0.1, function()
						if playerActionPickerState.open and playerActionPickerState.token == openToken then
							tweenService:Create(playerActionPickerParts.Content, contentTween, {GroupTransparency = 0}):Play()
						end
					end)
		end

	playerActionSystem.openPicker = openPlayerActionPicker
	playerActionSystem.closePicker = _picker.closePlayerActionPicker
	playerActionSystem.stopSpectating = stopSpectating
	playerActionSystem.stopAllPlayerActions = function(reason)
		if stopSpectating then
			pcall(stopSpectating, reason or "Moderator / admin detected.")
		end

		if _picker.closeLiveTrackPanel then
			pcall(_picker.closeLiveTrackPanel, false)
		end

		if _picker.closePlayerActionPicker then
			pcall(_picker.closePlayerActionPicker, true)
		end

		for targetPlayer in pairs(trackingBadges) do
			_picker.destroyTrackingBadge(targetPlayer)
		end

		for targetPlayer in pairs(playerTracers) do
			destroyPlayerTracer(targetPlayer)
		end

		for targetPlayer in pairs(hiddenPlayers) do
			hiddenPlayerTools.Destroy(targetPlayer)
		end

		if wieldingTools then
			wieldingTools.RocketToken += 1
			wieldingTools.Stop(true)
		end
	end
	playerActionSystem.isPickerOpen = function()
		return playerActionPickerState.open
	end
	playerActionSystem.getPickerTarget = function()
		return playerActionPickerState.targetPlayer
	end
	playerActionSystem.getSpectateTarget = function()
		return spectateState.targetPlayer
	end
do local b = _G.__SIRIUSX_BRIDGE; if b then b.playerActions = { spectate = startSpectating, stopSpectate = stopSpectating, esp = _picker.togglePlayerEsp, tracer = togglePlayerTracer, hide = hiddenPlayerTools.Toggle, track = _picker.startTrackingPlayer, liveTrack = _picker.startLiveTrackingPlayer, wieldingTools = wieldingTools, stopAll = playerActionSystem.stopAllPlayerActions } end end -- [NextGen bridge: players]
end

local function checkLastVersion()
	checkFolder()

	local lastVersion = isfile and isfile(siriusValues.siriusFolder.."/".."version.srs") and readfile(siriusValues.siriusFolder.."/".."version.srs") or nil

	if lastVersion then
		if lastVersion ~= siriusValues.siriusVersion then queueNotification("Sirius has been updated", "Sirius has been updated to version "..siriusValues.siriusVersion..", check our Discord for all new features and changes.", 4400701828)  end
	end

	if writefile then writefile(siriusValues.siriusFolder.."/".."version.srs", siriusValues.siriusVersion) end
end

local function removeReverbs(timing)
	timing = timing or 0.65

	for index, sound in next, soundInstances do
		if sound:FindFirstChild("SiriusAudioProfile") then
			local reverb = sound:FindFirstChild("SiriusAudioProfile")
			tweenService:Create(reverb, TweenInfo.new(timing, Enum.EasingStyle.Exponential), {HighGain = 0}):Play()
			tweenService:Create(reverb, TweenInfo.new(timing, Enum.EasingStyle.Exponential), {LowGain = 0}):Play()
			tweenService:Create(reverb, TweenInfo.new(timing, Enum.EasingStyle.Exponential), {MidGain = 0}):Play()

			task.delay(timing + 0.03, reverb.Destroy, reverb)
		end
	end
end

do
local function truncateMusicText(text, maxLength)
	text = tostring(text or "")
	if string.len(text) <= maxLength then return text end
	return string.sub(text, 1, maxLength - 3).."..."
end

local function parseRobloxMusicLibrary()
	if musicLibraryState.ready then return end
	musicLibraryState.ready = true

	for line in string.gmatch(musicLibraryState.source, "[^\n]+") do
		local id, title, artist, creator, section, duration = string.match(line, "^([^|]+)|([^|]*)|([^|]*)|([^|]*)|([^|]*)|([^|]*)$")
		if id and title and section then
			local track = {
				id = id,
				title = title,
				artist = artist ~= "" and artist or creator,
				creator = creator,
				section = section,
				duration = duration,
				soundId = "rbxassetid://"..id,
			}

			track.searchText = string.lower(track.title.." "..track.artist.." "..track.creator.." "..track.section)
			table.insert(musicLibraryState.library, track)

			if not musicLibraryState.sectionSeen[section] then
				musicLibraryState.sectionSeen[section] = true
				table.insert(musicLibraryState.sections, section)
			end
		end
	end
end

local musicLibraryList

local function getMusicLibraryList()
	return musicLibraryList or musicPanel.Queue.List
end

local function clearMusicLibraryRows()
	for _, child in ipairs(getMusicLibraryList():GetChildren()) do
		if child.Name ~= "Template" and child.Name ~= "Placeholder" and not child:IsA("UIListLayout") and not child:IsA("UIPadding") then
			child:Destroy()
		end
	end
end

local musicLibraryTabButtons = {}
local musicLibraryStatusLabel
local musicLibraryTabs
local musicPlayerStrip
local musicProgressBar
local musicProgressFill
local musicProgressKnob
local musicProgressTime
local musicProgressHitbox
local musicCustomRoot
local musicSearchInput
local musicTopToggle
local musicArtworkImage
local musicArtworkFallback
local musicNowPlayingLabel
local musicTopArtistLabel
local musicPlayButton
local musicNextButton
local playMusicTrack
local stopMusicTrack
local openMusicPlayerForTrack
local updateMusicLibraryActiveRows
local musicLibraryZIndex = 1
local musicSectionGrid
local musicTrackList
local musicHeaderIcon
local musicHeaderTitle
local musicHeaderSubtitle
local musicBackButton
local musicRootGradient
local musicTopPlayerOpen = false
local musicSettingsLibraryBuilt = false
local musicSectionAccents = {}

local function showCustomMusicGui()
	if not musicCustomRoot then return end

	for _, descendant in ipairs(musicCustomRoot:GetDescendants()) do
		if descendant:IsA("GuiObject") then
			descendant.Visible = true
		end
	end
end

local function formatMusicTime(seconds)
	seconds = math.max(0, math.floor(tonumber(seconds) or 0))
	return tostring(math.floor(seconds / 60))..":"..string.format("%02d", seconds % 60)
end

local function cleanMusicQuery(text)
	return string.gsub(string.lower(tostring(text or "")), "^%s*(.-)%s*$", "%1")
end

local function parseMusicQuery(text)
	local query = cleanMusicQuery(text)
	local key, value = string.match(query, "^(%w+)%s*:%s*(.+)$")

	if key and value then
		key = cleanMusicQuery(key)
		value = cleanMusicQuery(value)

		if key == "genre" or key == "vibe" or key == "artist" or key == "creator" or key == "source" then
			return key, value, ""
		end
	end

	return nil, nil, query
end

local function musicTrackMatches(track, key, value, plainQuery)
	if key == "genre" then
		return string.find(string.lower(track.section), "genres /", 1, true) and string.find(string.lower(track.section), value, 1, true)
	elseif key == "vibe" then
		return string.find(string.lower(track.section), "vibes /", 1, true) and string.find(string.lower(track.section), value, 1, true)
	elseif key == "artist" then
		return string.find(string.lower(track.artist), value, 1, true)
	elseif key == "creator" or key == "source" then
		return string.find(string.lower(track.creator), value, 1, true)
	end

	return plainQuery == "" or string.find(track.searchText, plainQuery, 1, true)
end

local function updateMusicProgress()
	if not musicProgressFill or not musicProgressTime then return end

	local audio = musicLibraryState.audio
	local duration = audio and audio.TimeLength or 0
	local position = audio and audio.TimePosition or 0
	local ratio = duration > 0 and math.clamp(position / duration, 0, 1) or 0
	local progressMotion = TweenInfo.new(musicLibraryState.isSeeking and 0.08 or 0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

	tweenService:Create(musicProgressFill, progressMotion, {
		Size = UDim2.new(ratio, 0, 1, 0),
		BackgroundTransparency = duration > 0 and 0.08 or 0.38,
	}):Play()
	if musicProgressKnob then
		tweenService:Create(musicProgressKnob, progressMotion, {
			Position = UDim2.new(ratio, -1, 0, 0),
			BackgroundTransparency = duration > 0 and 0.14 or 1,
		}):Play()
	end

	local timeText = formatMusicTime(position).." / "..(duration > 0 and formatMusicTime(duration) or musicLibraryState.durationText)
	musicProgressTime.Text = timeText
	if musicNowPlayingLabel then
		musicNowPlayingLabel.Text = truncateMusicText(musicLibraryState.nowPlayingTitle or "Not Playing", 36)
	end
	if musicTopArtistLabel then
		local track = musicLibraryState.currentTrack
		musicTopArtistLabel.Text = track and truncateMusicText(track.artist.." • "..track.section, 42) or "Select a track from the library"
	end
	if musicPanel and musicPanel:FindFirstChild("Playing") then
		musicPanel.Playing.Text = truncateMusicText(musicLibraryState.nowPlayingTitle or "Not Playing", 30).."  •  "..timeText
	end
end
musicLibraryState.updateProgress = updateMusicProgress

local function seekMusicProgress(screenX)
	local audio = musicLibraryState.audio
	if not audio or not musicProgressBar or audio.TimeLength <= 0 then return end

	local ratio = math.clamp((screenX - musicProgressBar.AbsolutePosition.X) / math.max(musicProgressBar.AbsoluteSize.X, 1), 0, 1)
	audio.TimePosition = audio.TimeLength * ratio
	updateMusicProgress()
end

local function startMusicProgressLoop()
	if musicLibraryState.progressStarted then return end
	musicLibraryState.progressStarted = true

	task.spawn(function()
		while musicLibraryState.progressStarted do
			task.wait(0.2)
			updateMusicProgress()
		end
	end)
end

local function showMusicTopPlayer()
	if not musicPlayerStrip then return end
	if musicTopPlayerOpen then
		tweenService:Create(musicPlayerStrip, TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
			Position = UDim2.new(0, 18, 0, 70),
			BackgroundTransparency = 0.05,
		}):Play()
		return
	end

	musicTopPlayerOpen = true
	musicPlayerStrip.Visible = true
	musicPlayerStrip.Position = UDim2.new(0, 18, 0, 52)
	musicPlayerStrip.BackgroundTransparency = 1
	wipeTransparency(musicPlayerStrip, 1, true)

	local openMotion = TweenInfo.new(0.46, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
	tweenService:Create(musicPlayerStrip, openMotion, {
		Position = UDim2.new(0, 18, 0, 70),
		BackgroundTransparency = 0.05,
	}):Play()

	for _, descendant in ipairs(musicPlayerStrip:GetDescendants()) do
		if descendant:IsA("TextButton") then
			if descendant.Name == "ProgressHitbox" then
				descendant.Text = ""
				descendant.TextTransparency = 1
				descendant.BackgroundTransparency = 1
				continue
			end
			local buttonTransparency = 1
			if descendant.Name == "Play" then
				buttonTransparency = 0.16
			elseif descendant.Name == "Forward" then
				buttonTransparency = 0.26
			end
			tweenService:Create(descendant, TweenInfo.new(0.34, Enum.EasingStyle.Quint), {
				BackgroundTransparency = buttonTransparency,
				TextTransparency = 0.04,
			}):Play()
		elseif descendant:IsA("TextLabel") then
			tweenService:Create(descendant, TweenInfo.new(0.34, Enum.EasingStyle.Quint), {TextTransparency = descendant.Name == "Time" and 0.2 or 0.04}):Play()
		elseif descendant:IsA("ImageLabel") then
			tweenService:Create(descendant, TweenInfo.new(0.34, Enum.EasingStyle.Quint), {ImageTransparency = 0.08}):Play()
		elseif descendant:IsA("Frame") then
			local targetTransparency = 1
			if descendant.Name == "ProgressBar" then
				targetTransparency = 0.24
			elseif descendant.Name == "Fill" then
				targetTransparency = musicLibraryState.currentTrack and 0.08 or 0.38
			elseif descendant.Name == "ProgressKnob" then
				targetTransparency = musicLibraryState.currentTrack and 0.14 or 1
			elseif descendant.Name == "ArtworkShell" or descendant.Name == "ButtonShell" then
				targetTransparency = 0.1
			end
			tweenService:Create(descendant, TweenInfo.new(0.34, Enum.EasingStyle.Quint), {BackgroundTransparency = targetTransparency}):Play()
		elseif descendant:IsA("UIStroke") then
			tweenService:Create(descendant, TweenInfo.new(0.34, Enum.EasingStyle.Quint), {Transparency = 0.42}):Play()
		end
	end
end

local function hideMusicTopPlayer(stopPlayback)
	if not musicPlayerStrip or not musicTopPlayerOpen then return end
	musicTopPlayerOpen = false
	if stopPlayback and stopMusicTrack then
		stopMusicTrack()
	end
	if musicTopToggle and musicTopToggle.Visible then
		tweenService:Create(musicTopToggle, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {
			BackgroundTransparency = 0.08,
			TextTransparency = 0.03,
		}):Play()
		if musicTopToggle:FindFirstChild("UIStroke") then
			tweenService:Create(musicTopToggle.UIStroke, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {Transparency = 0.62}):Play()
		end
	end

	local closeMotion = TweenInfo.new(0.32, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut)
	tweenService:Create(musicPlayerStrip, closeMotion, {
		Position = UDim2.new(0, 18, 0, 52),
		BackgroundTransparency = 1,
	}):Play()
	task.delay(0.34, function()
		if not musicTopPlayerOpen and musicPlayerStrip then
			musicPlayerStrip.Visible = false
		end
	end)
end

local function ensureMusicTopPlayer()
	if musicPlayerStrip and musicPlayerStrip.Parent and musicPlayerStrip.Name == "TopMusicPlayer" then return end
	if musicPlayerStrip and musicPlayerStrip.Name ~= "TopMusicPlayer" then
		musicPlayerStrip = nil
	end

	musicPlayerStrip = Instance.new("Frame")
	musicPlayerStrip.Name = "TopMusicPlayer"
	musicPlayerStrip.Parent = UI
	musicPlayerStrip.AnchorPoint = Vector2.new(0, 0)
	musicPlayerStrip.Position = UDim2.new(0, 18, 0, 52)
	musicPlayerStrip.Size = UDim2.new(0, 326, 0, 132)
	musicPlayerStrip.BackgroundColor3 = Color3.fromRGB(13, 13, 14)
	musicPlayerStrip.BackgroundTransparency = 1
	musicPlayerStrip.BorderSizePixel = 0
	musicPlayerStrip.ClipsDescendants = true
	musicPlayerStrip.Visible = false
	musicPlayerStrip.ZIndex = 95

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 18)
	corner.Parent = musicPlayerStrip

	local stroke = Instance.new("UIStroke")
	stroke.Color = Color3.fromRGB(215, 112, 116)
	stroke.Thickness = 1.1
	stroke.Transparency = 0.36
	stroke.Parent = musicPlayerStrip

	local gradient = Instance.new("UIGradient")
	gradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(95, 35, 39)),
		ColorSequenceKeypoint.new(0.45, Color3.fromRGB(15, 11, 13)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(6, 19, 30)),
	})
	gradient.Rotation = 125
	gradient.Parent = musicPlayerStrip

	local artworkShell = Instance.new("Frame")
	artworkShell.Name = "ArtworkShell"
	artworkShell.Parent = musicPlayerStrip
	artworkShell.BackgroundColor3 = Color3.fromRGB(32, 18, 20)
	artworkShell.BackgroundTransparency = 1
	artworkShell.BorderSizePixel = 0
	artworkShell.ClipsDescendants = true
	artworkShell.Position = UDim2.new(0, 18, 0, 18)
	artworkShell.Size = UDim2.new(0, 68, 0, 68)
	artworkShell.ZIndex = 96
	local artworkCorner = Instance.new("UICorner")
	artworkCorner.CornerRadius = UDim.new(1, 0)
	artworkCorner.Parent = artworkShell
	local artworkStroke = Instance.new("UIStroke")
	artworkStroke.Color = Color3.fromRGB(224, 129, 134)
	artworkStroke.Thickness = 1.2
	artworkStroke.Transparency = 0.34
	artworkStroke.Parent = artworkShell

	musicArtworkFallback = Instance.new("TextLabel")
	musicArtworkFallback.Name = "ArtworkFallback"
	musicArtworkFallback.Parent = artworkShell
	musicArtworkFallback.BackgroundTransparency = 1
	musicArtworkFallback.Size = UDim2.new(1, 0, 1, 0)
	musicArtworkFallback.Font = Enum.Font.GothamBold
	musicArtworkFallback.Text = "♫"
	musicArtworkFallback.TextColor3 = Color3.fromRGB(242, 214, 216)
	musicArtworkFallback.TextSize = 34
	musicArtworkFallback.TextTransparency = 0.15
	musicArtworkFallback.ZIndex = 97

	musicArtworkImage = Instance.new("ImageLabel")
	musicArtworkImage.Name = "Artwork"
	musicArtworkImage.Parent = artworkShell
	musicArtworkImage.BackgroundTransparency = 1
	musicArtworkImage.Size = UDim2.new(1, 0, 1, 0)
	musicArtworkImage.Image = ""
	musicArtworkImage.ImageTransparency = 1
	musicArtworkImage.ScaleType = Enum.ScaleType.Crop
	musicArtworkImage.ZIndex = 98
	local artworkImageCorner = Instance.new("UICorner")
	artworkImageCorner.CornerRadius = UDim.new(1, 0)
	artworkImageCorner.Parent = musicArtworkImage

	musicNowPlayingLabel = Instance.new("TextLabel")
	musicNowPlayingLabel.Name = "NowPlaying"
	musicNowPlayingLabel.Parent = musicPlayerStrip
	musicNowPlayingLabel.BackgroundTransparency = 1
	musicNowPlayingLabel.Position = UDim2.new(0, 102, 0, 22)
	musicNowPlayingLabel.Size = UDim2.new(1, -148, 0, 22)
	musicNowPlayingLabel.Font = Enum.Font.GothamBold
	musicNowPlayingLabel.Text = musicLibraryState.nowPlayingTitle or "Not Playing"
	musicNowPlayingLabel.TextColor3 = Color3.fromRGB(244, 244, 244)
	musicNowPlayingLabel.TextSize = 15
	musicNowPlayingLabel.TextTransparency = 1
	musicNowPlayingLabel.TextXAlignment = Enum.TextXAlignment.Left
	musicNowPlayingLabel.TextTruncate = Enum.TextTruncate.AtEnd
	musicNowPlayingLabel.ZIndex = 96

	musicTopArtistLabel = Instance.new("TextLabel")
	musicTopArtistLabel.Name = "Artist"
	musicTopArtistLabel.Parent = musicPlayerStrip
	musicTopArtistLabel.BackgroundTransparency = 1
	musicTopArtistLabel.Position = UDim2.new(0, 102, 0, 45)
	musicTopArtistLabel.Size = UDim2.new(1, -148, 0, 16)
	musicTopArtistLabel.Font = Enum.Font.Gotham
	musicTopArtistLabel.Text = "Select a track from the library"
	musicTopArtistLabel.TextColor3 = Color3.fromRGB(211, 188, 190)
	musicTopArtistLabel.TextSize = 10
	musicTopArtistLabel.TextTransparency = 1
	musicTopArtistLabel.TextXAlignment = Enum.TextXAlignment.Left
	musicTopArtistLabel.TextTruncate = Enum.TextTruncate.AtEnd
	musicTopArtistLabel.ZIndex = 96

	musicProgressBar = Instance.new("Frame")
	musicProgressBar.Name = "ProgressBar"
	musicProgressBar.Parent = musicPlayerStrip
	musicProgressBar.BackgroundColor3 = Color3.fromRGB(38, 18, 20)
	musicProgressBar.BackgroundTransparency = 1
	musicProgressBar.BorderSizePixel = 0
	musicProgressBar.Position = UDim2.new(0, 18, 1, -34)
	musicProgressBar.Size = UDim2.new(1, -36, 0, 27)
	musicProgressBar.ZIndex = 96
	local progressCorner = Instance.new("UICorner")
	progressCorner.CornerRadius = UDim.new(1, 0)
	progressCorner.Parent = musicProgressBar
	local progressStroke = Instance.new("UIStroke")
	progressStroke.Color = Color3.fromRGB(183, 78, 86)
	progressStroke.Thickness = 1
	progressStroke.Transparency = 0.48
	progressStroke.Parent = musicProgressBar

	musicProgressTime = Instance.new("TextLabel")
	musicProgressTime.Name = "Time"
	musicProgressTime.Parent = musicProgressBar
	musicProgressTime.BackgroundTransparency = 1
	musicProgressTime.Position = UDim2.new(1, -94, 0, 0)
	musicProgressTime.Size = UDim2.new(0, 82, 1, 0)
	musicProgressTime.Font = Enum.Font.GothamBold
	musicProgressTime.Text = "0:00 / --:--"
	musicProgressTime.TextColor3 = Color3.fromRGB(232, 202, 205)
	musicProgressTime.TextSize = 10
	musicProgressTime.TextTransparency = 1
	musicProgressTime.TextXAlignment = Enum.TextXAlignment.Right
	musicProgressTime.ZIndex = 100

	musicProgressFill = Instance.new("Frame")
	musicProgressFill.Name = "Fill"
	musicProgressFill.Parent = musicProgressBar
	musicProgressFill.BackgroundColor3 = Color3.fromRGB(185, 74, 84)
	musicProgressFill.BackgroundTransparency = 1
	musicProgressFill.BorderSizePixel = 0
	musicProgressFill.Size = UDim2.new(0, 0, 1, 0)
	musicProgressFill.ZIndex = 97
	local fillCorner = Instance.new("UICorner")
	fillCorner.CornerRadius = UDim.new(1, 0)
	fillCorner.Parent = musicProgressFill
	local fillGradient = Instance.new("UIGradient")
	fillGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(205, 89, 99)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(117, 45, 52)),
	})
	fillGradient.Rotation = 0
	fillGradient.Parent = musicProgressFill

	musicProgressKnob = Instance.new("Frame")
	musicProgressKnob.Name = "ProgressKnob"
	musicProgressKnob.Parent = musicProgressBar
	musicProgressKnob.BackgroundColor3 = Color3.fromRGB(230, 138, 146)
	musicProgressKnob.BackgroundTransparency = 1
	musicProgressKnob.BorderSizePixel = 0
	musicProgressKnob.Position = UDim2.new(0, -1, 0, 0)
	musicProgressKnob.Size = UDim2.new(0, 3, 1, 0)
	musicProgressKnob.ZIndex = 99
	local knobCorner = Instance.new("UICorner")
	knobCorner.CornerRadius = UDim.new(1, 0)
	knobCorner.Parent = musicProgressKnob
	local knobStroke = Instance.new("UIStroke")
	knobStroke.Color = Color3.fromRGB(195, 86, 92)
	knobStroke.Thickness = 0
	knobStroke.Transparency = 1
	knobStroke.Parent = musicProgressKnob

	musicProgressHitbox = Instance.new("TextButton")
	musicProgressHitbox.Name = "ProgressHitbox"
	musicProgressHitbox.Parent = musicProgressBar
	musicProgressHitbox.BackgroundTransparency = 1
	musicProgressHitbox.TextTransparency = 1
	musicProgressHitbox.Text = ""
	musicProgressHitbox.Position = UDim2.new(0, -8, 0.5, -18)
	musicProgressHitbox.Size = UDim2.new(1, 16, 0, 36)
	musicProgressHitbox.ZIndex = 98
	musicProgressHitbox.MouseButton1Down:Connect(function(x)
		musicLibraryState.isSeeking = true
		seekMusicProgress(x)
	end)
	musicProgressHitbox.MouseButton1Up:Connect(function()
		musicLibraryState.isSeeking = false
	end)

	musicPlayButton = Instance.new("TextButton")
	musicPlayButton.Name = "Play"
	musicPlayButton.Parent = musicPlayerStrip
	musicPlayButton.BackgroundColor3 = Color3.fromRGB(41, 18, 20)
	musicPlayButton.BackgroundTransparency = 1
	musicPlayButton.BorderSizePixel = 0
	musicPlayButton.Position = UDim2.new(0, 116, 0, 70)
	musicPlayButton.Size = UDim2.new(0, 42, 0, 34)
	musicPlayButton.AutoButtonColor = false
	musicPlayButton.Font = Enum.Font.GothamBold
	musicPlayButton.Text = "▶"
	musicPlayButton.TextColor3 = Color3.fromRGB(255, 255, 255)
	musicPlayButton.TextSize = 20
	musicPlayButton.TextTransparency = 1
	musicPlayButton.ZIndex = 96
	local playCorner = Instance.new("UICorner")
	playCorner.CornerRadius = UDim.new(1, 0)
	playCorner.Parent = musicPlayButton
	local playStroke = Instance.new("UIStroke")
	playStroke.Color = Color3.fromRGB(220, 120, 125)
	playStroke.Thickness = 1
	playStroke.Transparency = 0.74
	playStroke.Parent = musicPlayButton
	musicPlayButton.MouseButton1Click:Connect(function()
		if musicLibraryState.audio then
			if musicLibraryState.audio.Playing then
				musicLibraryState.audio:Pause()
			else
				pcall(function()
					musicLibraryState.audio:Resume()
				end)
				if not musicLibraryState.audio.Playing then
					musicLibraryState.audio:Play()
				end
			end
			musicPlayButton.Text = musicLibraryState.audio.Playing and "Ⅱ" or "▶"
			updateMusicProgress()
			updateMusicLibraryActiveRows()
		elseif musicLibraryState.renderedTracks and #musicLibraryState.renderedTracks > 0 then
			playMusicTrack(musicLibraryState.renderedTracks[1], 1)
		end
	end)

	musicNextButton = Instance.new("TextButton")
	musicNextButton.Name = "Forward"
	musicNextButton.Parent = musicPlayerStrip
	musicNextButton.BackgroundColor3 = Color3.fromRGB(41, 18, 20)
	musicNextButton.BackgroundTransparency = 1
	musicNextButton.BorderSizePixel = 0
	musicNextButton.Position = UDim2.new(0, 168, 0, 72)
	musicNextButton.Size = UDim2.new(0, 42, 0, 30)
	musicNextButton.AutoButtonColor = false
	musicNextButton.Font = Enum.Font.GothamBold
	musicNextButton.Text = "+10"
	musicNextButton.TextColor3 = Color3.fromRGB(255, 255, 255)
	musicNextButton.TextSize = 13
	musicNextButton.TextTransparency = 1
	musicNextButton.ZIndex = 96
	local nextCorner = Instance.new("UICorner")
	nextCorner.CornerRadius = UDim.new(1, 0)
	nextCorner.Parent = musicNextButton
	local nextStroke = Instance.new("UIStroke")
	nextStroke.Color = Color3.fromRGB(220, 120, 125)
	nextStroke.Thickness = 1
	nextStroke.Transparency = 0.82
	nextStroke.Parent = musicNextButton
	musicNextButton.MouseButton1Click:Connect(function()
		local audio = musicLibraryState.audio
		if not audio then return end
		local targetPosition = audio.TimePosition + 10
		if audio.TimeLength and audio.TimeLength > 0 then
			targetPosition = math.min(targetPosition, math.max(audio.TimeLength - 0.05, 0))
		end
		audio.TimePosition = math.max(targetPosition, 0)
		updateMusicProgress()
	end)

	local function wireTopPlayerButton(button)
		button.MouseEnter:Connect(function()
			tweenService:Create(button, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {
				BackgroundTransparency = 0.22,
				TextTransparency = 0,
			}):Play()
		end)
		button.MouseLeave:Connect(function()
			local idleTransparency = button == musicPlayButton and 0.16 or 0.26
			tweenService:Create(button, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {
				BackgroundTransparency = idleTransparency,
				TextTransparency = 0.04,
			}):Play()
		end)
	end
	wireTopPlayerButton(musicPlayButton)
	wireTopPlayerButton(musicNextButton)

	local close = Instance.new("TextButton")
	close.Name = "Close"
	close.Parent = musicPlayerStrip
	close.BackgroundTransparency = 1
	close.Position = UDim2.new(1, -32, 0, 8)
	close.Size = UDim2.new(0, 24, 0, 24)
	close.AutoButtonColor = false
	close.Font = Enum.Font.GothamBold
	close.Text = "×"
	close.TextColor3 = Color3.fromRGB(218, 176, 178)
	close.TextSize = 23
	close.TextTransparency = 1
	close.ZIndex = 96
	close.MouseButton1Click:Connect(function()
		hideMusicTopPlayer(false)
	end)

	userInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			musicLibraryState.isSeeking = false
		end
	end)
	userInputService.InputChanged:Connect(function(input)
		if not musicLibraryState.isSeeking then return end
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			seekMusicProgress(input.Position.X)
		end
	end)
end

local function ensureMusicTopToggle()
	if musicTopToggle and musicTopToggle.Parent then return end

	musicTopToggle = Instance.new("TextButton")
	musicTopToggle.Name = "MusicTopToggle"
	musicTopToggle.Parent = UI
	musicTopToggle.BackgroundColor3 = Color3.fromRGB(28, 28, 30)
	musicTopToggle.BackgroundTransparency = 1
	musicTopToggle.BorderSizePixel = 0
	musicTopToggle.Position = UDim2.new(0, 184, 0, 12)
	musicTopToggle.Size = UDim2.new(0, 46, 0, 46)
	musicTopToggle.AutoButtonColor = false
	musicTopToggle.Font = Enum.Font.GothamBold
	musicTopToggle.Text = "♫"
	musicTopToggle.TextColor3 = Color3.fromRGB(255, 255, 255)
	musicTopToggle.TextSize = 24
	musicTopToggle.TextTransparency = 1
	musicTopToggle.Visible = false
	musicTopToggle.ZIndex = 94

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(1, 0)
	corner.Parent = musicTopToggle

	local stroke = Instance.new("UIStroke")
	stroke.Color = Color3.fromRGB(216, 104, 111)
	stroke.Thickness = 1
	stroke.Transparency = 0.72
	stroke.Parent = musicTopToggle

	local gradient = Instance.new("UIGradient")
	gradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(78, 28, 32)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(18, 18, 20)),
	})
	gradient.Rotation = 125
	gradient.Parent = musicTopToggle

	musicTopToggle.MouseEnter:Connect(function()
		tweenService:Create(musicTopToggle, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {
			BackgroundTransparency = 0.02,
			TextTransparency = 0,
		}):Play()
		tweenService:Create(stroke, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {Transparency = 0.42}):Play()
	end)
	musicTopToggle.MouseLeave:Connect(function()
		tweenService:Create(musicTopToggle, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {
			BackgroundTransparency = musicTopPlayerOpen and 0.02 or 0.08,
			TextTransparency = 0.03,
		}):Play()
		tweenService:Create(stroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {
			Transparency = musicTopPlayerOpen and 0.38 or 0.62,
		}):Play()
	end)
	musicTopToggle.MouseButton1Click:Connect(function()
		ensureMusicTopPlayer()
		if musicTopPlayerOpen then
			hideMusicTopPlayer(false)
		else
			showMusicTopPlayer()
		end
	end)
end

local function showMusicTopToggle()
	ensureMusicTopToggle()
	if musicTopToggle.Visible then
		tweenService:Create(musicTopToggle, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {
			BackgroundTransparency = musicTopPlayerOpen and 0.02 or 0.08,
			TextTransparency = 0.03,
		}):Play()
		return
	end

	musicTopToggle.Visible = true
	musicTopToggle.Position = UDim2.new(0, 184, 0, -44)
	musicTopToggle.BackgroundTransparency = 1
	musicTopToggle.TextTransparency = 1
	tweenService:Create(musicTopToggle, TweenInfo.new(0.38, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
		Position = UDim2.new(0, 184, 0, 12),
		BackgroundTransparency = 0.08,
		TextTransparency = 0.03,
	}):Play()
	if musicTopToggle:FindFirstChild("UIStroke") then
		tweenService:Create(musicTopToggle.UIStroke, TweenInfo.new(0.38, Enum.EasingStyle.Quint), {Transparency = 0.62}):Play()
	end
end

local function hideMusicTopToggle()
	if not musicTopToggle or not musicTopToggle.Visible then return end
	tweenService:Create(musicTopToggle, TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {
		Position = UDim2.new(0, 184, 0, -44),
		BackgroundTransparency = 1,
		TextTransparency = 1,
	}):Play()
	task.delay(0.3, function()
		if musicTopToggle and not musicLibraryState.currentTrack then
			musicTopToggle.Visible = false
		end
	end)
end

local function syncMusicTopToggle()
	if musicLibraryState.currentTrack then
		showMusicTopToggle()
	else
		hideMusicTopPlayer(false)
		hideMusicTopToggle()
	end
end

local function updateMusicTopPlayerDetails(track)
	if not musicPlayerStrip then return end

	if musicNowPlayingLabel then
		musicNowPlayingLabel.Text = track and truncateMusicText(track.title, 36) or "Not Playing"
	end
	if musicTopArtistLabel then
		musicTopArtistLabel.Text = track and truncateMusicText(track.artist.." • "..track.section, 42) or "Select a track from the library"
	end
	if musicArtworkImage then
		if track and track.id then
			musicArtworkImage.Image = "rbxthumb://type=Asset&id="..track.id.."&w=150&h=150"
			tweenService:Create(musicArtworkImage, TweenInfo.new(0.24, Enum.EasingStyle.Quint), {ImageTransparency = 0.08}):Play()
		else
			tweenService:Create(musicArtworkImage, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
		end
	end
	if musicArtworkFallback then
		tweenService:Create(musicArtworkFallback, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {
			TextTransparency = track and 0.62 or 0.15,
		}):Play()
	end
end

local function setQueueHeaderText()
	for _, descendant in ipairs(musicPanel:GetDescendants()) do
		if descendant:IsA("TextLabel") and descendant.Text == "QUEUE" then
			descendant.Text = "LIBRARY"
			descendant.TextTransparency = 0.32
			break
		end
	end
end

local function updateMusicLibraryTabs()
	for section, button in pairs(musicLibraryTabButtons) do
		local selected = section == musicLibraryState.activeSection
		tweenService:Create(button, TweenInfo.new(0.24, Enum.EasingStyle.Quint), {
			BackgroundTransparency = selected and 0.15 or 0.58,
			TextTransparency = selected and 0.02 or 0.24,
		}):Play()

		if button:FindFirstChild("UIStroke") then
			tweenService:Create(button.UIStroke, TweenInfo.new(0.24, Enum.EasingStyle.Quint), {
				Transparency = selected and 0.18 or 0.75,
			}):Play()
		end
	end
end

local function ensureMusicLibraryUI()
	parseRobloxMusicLibrary()

	if not musicPanel:GetAttribute("CustomMusicLibrarySize") then
		local viewport = workspace.CurrentCamera and workspace.CurrentCamera.ViewportSize or Vector2.new(1280, 720)
		musicPanel.AnchorPoint = Vector2.new(0.5, 0.5)
		musicPanel.Position = UDim2.new(0.5, 0, 0.48, 0)
		musicPanel.Size = UDim2.new(0, math.min(620, viewport.X - 42), 0, math.min(570, viewport.Y - 132))
		musicPanel:SetAttribute("CustomMusicLibrarySize", true)
	end

	if not musicCustomRoot then
		for _, child in ipairs(musicPanel:GetChildren()) do
			child:Destroy()
		end

		musicCustomRoot = musicPanel
		musicCustomRoot.Name = "Music"
		musicCustomRoot.BackgroundColor3 = Color3.fromRGB(18, 7, 8)
		musicCustomRoot.BackgroundTransparency = 0
		musicCustomRoot.BorderSizePixel = 0
		musicCustomRoot.ClipsDescendants = true
		musicCustomRoot.ZIndex = musicLibraryZIndex

		local rootCorner = Instance.new("UICorner")
		rootCorner.CornerRadius = UDim.new(0, 18)
		rootCorner.Parent = musicCustomRoot

		local rootStroke = Instance.new("UIStroke")
		rootStroke.Color = Color3.fromRGB(211, 106, 111)
		rootStroke.Thickness = 1.15
		rootStroke.Transparency = 0.28
		rootStroke.Parent = musicCustomRoot

		local rootGradient = Instance.new("UIGradient")
		rootGradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, Color3.fromRGB(157, 65, 68)),
			ColorSequenceKeypoint.new(0.42, Color3.fromRGB(54, 20, 22)),
			ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0))
		})
		rootGradient.Rotation = 135
		rootGradient.Parent = musicCustomRoot

		local icon = Instance.new("TextLabel")
		icon.Name = "MusicIcon"
		icon.Parent = musicCustomRoot
		icon.BackgroundTransparency = 1
		icon.Position = UDim2.new(0, 46, 0, 34)
		icon.Size = UDim2.new(0, 30, 0, 34)
		icon.Font = Enum.Font.GothamBold
		icon.Text = "♫"
		icon.TextColor3 = Color3.fromRGB(255, 255, 255)
		icon.TextSize = 32
		icon.TextTransparency = 0.02
		icon.ZIndex = musicLibraryZIndex + 2

		local title = Instance.new("TextLabel")
		title.Name = "Title"
		title.Parent = musicCustomRoot
		title.BackgroundTransparency = 1
		title.Position = UDim2.new(0, 86, 0, 38)
		title.Size = UDim2.new(0, 260, 0, 34)
		title.Font = Enum.Font.GothamBold
		title.Text = "Music"
		title.TextColor3 = Color3.fromRGB(255, 255, 255)
		title.TextSize = 24
		title.TextXAlignment = Enum.TextXAlignment.Left
		title.ZIndex = musicLibraryZIndex + 2

		local close = Instance.new("TextButton")
		close.Name = "Close"
		close.Parent = musicCustomRoot
		close.BackgroundTransparency = 1
		close.Position = UDim2.new(1, -64, 0, 35)
		close.Size = UDim2.new(0, 34, 0, 34)
		close.AutoButtonColor = false
		close.Font = Enum.Font.Gotham
		close.Text = "×"
		close.TextColor3 = Color3.fromRGB(230, 177, 180)
		close.TextSize = 32
		close.TextTransparency = 0.22
		close.ZIndex = musicLibraryZIndex + 4
		close.MouseButton1Click:Connect(function()
			musicLibraryState.close()
		end)

		local searchShell = Instance.new("Frame")
		searchShell.Name = "SearchShell"
		searchShell.Parent = musicCustomRoot
		searchShell.BackgroundColor3 = Color3.fromRGB(35, 11, 13)
		searchShell.BackgroundTransparency = 0.18
		searchShell.BorderSizePixel = 0
		searchShell.Position = UDim2.new(0, 46, 0, 94)
		searchShell.Size = UDim2.new(1, -140, 0, 44)
		searchShell.ZIndex = musicLibraryZIndex + 2

		local searchCorner = Instance.new("UICorner")
		searchCorner.CornerRadius = UDim.new(1, 0)
		searchCorner.Parent = searchShell

		musicSearchInput = Instance.new("TextBox")
		musicSearchInput.Name = "SearchInput"
		musicSearchInput.Parent = searchShell
		musicSearchInput.BackgroundTransparency = 1
		musicSearchInput.ClearTextOnFocus = false
		musicSearchInput.Font = Enum.Font.GothamBold
		musicSearchInput.PlaceholderText = "Search song, artist, genre"
		musicSearchInput.PlaceholderColor3 = Color3.fromRGB(192, 148, 150)
		musicSearchInput.Position = UDim2.new(0, 22, 0, 0)
		musicSearchInput.Size = UDim2.new(1, -36, 1, 0)
		musicSearchInput.Text = ""
		musicSearchInput.TextColor3 = Color3.fromRGB(244, 226, 226)
		musicSearchInput.TextSize = 15
		musicSearchInput.TextTransparency = 0.1
		musicSearchInput.TextXAlignment = Enum.TextXAlignment.Left
		musicSearchInput.ZIndex = musicLibraryZIndex + 3
		musicSearchInput:GetPropertyChangedSignal("Text"):Connect(function()
			if not musicCustomRoot.Visible then return end
			musicLibraryState.searchSerial += 1
			local thisSerial = musicLibraryState.searchSerial
			task.delay(0.1, function()
				if thisSerial ~= musicLibraryState.searchSerial or not musicCustomRoot.Visible then return end
				musicLibraryState.render()
			end)
		end)

		local add = Instance.new("TextButton")
		add.Name = "PlayRandom"
		add.Parent = musicCustomRoot
		add.BackgroundColor3 = Color3.fromRGB(44, 14, 16)
		add.BackgroundTransparency = 0.18
		add.BorderSizePixel = 0
		add.Position = UDim2.new(1, -76, 0, 94)
		add.Size = UDim2.new(0, 44, 0, 44)
		add.AutoButtonColor = false
		add.Font = Enum.Font.Gotham
		add.Text = "+"
		add.TextColor3 = Color3.fromRGB(239, 220, 220)
		add.TextSize = 34
		add.ZIndex = musicLibraryZIndex + 3
		local addCorner = Instance.new("UICorner")
		addCorner.CornerRadius = UDim.new(1, 0)
		addCorner.Parent = add
		add.MouseButton1Click:Connect(function()
			musicSearchInput:ReleaseFocus()
			musicLibraryState.addToQueue()
		end)

		local libraryLabel = Instance.new("TextLabel")
		libraryLabel.Name = "LibraryLabel"
		libraryLabel.Parent = musicCustomRoot
		libraryLabel.BackgroundTransparency = 1
		libraryLabel.Position = UDim2.new(0, 50, 0, 156)
		libraryLabel.Size = UDim2.new(0, 150, 0, 22)
		libraryLabel.Font = Enum.Font.GothamBold
		libraryLabel.Text = "LIBRARY"
		libraryLabel.TextColor3 = Color3.fromRGB(230, 202, 202)
		libraryLabel.TextSize = 16
		libraryLabel.TextTransparency = 0.16
		libraryLabel.TextXAlignment = Enum.TextXAlignment.Left
		libraryLabel.ZIndex = musicLibraryZIndex + 2

		musicLibraryStatusLabel = Instance.new("TextLabel")
		musicLibraryStatusLabel.Name = "LibraryStatus"
		musicLibraryStatusLabel.Parent = musicCustomRoot
		musicLibraryStatusLabel.BackgroundTransparency = 1
		musicLibraryStatusLabel.Position = UDim2.new(0, 205, 0, 157)
		musicLibraryStatusLabel.Size = UDim2.new(1, -255, 0, 18)
		musicLibraryStatusLabel.Font = Enum.Font.GothamBold
		musicLibraryStatusLabel.Text = "All sections"
		musicLibraryStatusLabel.TextColor3 = Color3.fromRGB(213, 166, 168)
		musicLibraryStatusLabel.TextSize = 11
		musicLibraryStatusLabel.TextTransparency = 0.34
		musicLibraryStatusLabel.TextXAlignment = Enum.TextXAlignment.Right
		musicLibraryStatusLabel.ZIndex = musicLibraryZIndex + 2

		musicLibraryTabs = Instance.new("ScrollingFrame")
		musicLibraryTabs.Name = "LibraryTabs"
		musicLibraryTabs.Parent = musicCustomRoot
		musicLibraryTabs.BackgroundTransparency = 1
		musicLibraryTabs.BorderSizePixel = 0
		musicLibraryTabs.Position = UDim2.new(0, 46, 0, 184)
		musicLibraryTabs.Size = UDim2.new(1, -92, 0, 30)
		musicLibraryTabs.ScrollBarThickness = 0
		musicLibraryTabs.ScrollingDirection = Enum.ScrollingDirection.X
		musicLibraryTabs.CanvasSize = UDim2.new(0, 0, 0, 0)
		musicLibraryTabs.ZIndex = musicLibraryZIndex + 2

		local tabLayout = Instance.new("UIListLayout")
		tabLayout.Parent = musicLibraryTabs
		tabLayout.FillDirection = Enum.FillDirection.Horizontal
		tabLayout.Padding = UDim.new(0, 7)
		tabLayout.SortOrder = Enum.SortOrder.LayoutOrder
		tabLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
			musicLibraryTabs.CanvasSize = UDim2.new(0, tabLayout.AbsoluteContentSize.X + 8, 0, 0)
		end)

		local function createTab(section, label, layoutOrder)
			local tab = Instance.new("TextButton")
			tab.Name = "Tab_"..string.gsub(label, "[^%w]", "")
			tab.Parent = musicLibraryTabs
			tab.BackgroundColor3 = Color3.fromRGB(118, 44, 48)
			tab.BackgroundTransparency = 0.64
			tab.BorderSizePixel = 0
			tab.LayoutOrder = layoutOrder
			tab.Size = UDim2.new(0, math.clamp(string.len(label) * 7 + 28, 54, 142), 0, 26)
			tab.AutoButtonColor = false
			tab.Font = Enum.Font.GothamBold
			tab.Text = label
			tab.TextColor3 = Color3.fromRGB(255, 245, 245)
			tab.TextSize = 10
			tab.TextTransparency = 0.24
			tab.ZIndex = musicLibraryZIndex + 3
			local corner = Instance.new("UICorner")
			corner.CornerRadius = UDim.new(1, 0)
			corner.Parent = tab
			local stroke = Instance.new("UIStroke")
			stroke.Color = Color3.fromRGB(205, 101, 106)
			stroke.Thickness = 1
			stroke.Transparency = 0.82
			stroke.Parent = tab
			tab.MouseButton1Click:Connect(function()
				musicLibraryState.activeSection = section
				updateMusicLibraryTabs()
				musicLibraryState.render()
			end)
			musicLibraryTabButtons[section] = tab
		end

		createTab("All", "All", 0)
		for index, section in ipairs(musicLibraryState.sections) do
			createTab(section, string.gsub(section, "^.+ / ", ""), index)
		end

		musicLibraryList = Instance.new("ScrollingFrame")
		musicLibraryList.Name = "LibraryList"
		musicLibraryList.Parent = musicCustomRoot
		musicLibraryList.BackgroundTransparency = 1
		musicLibraryList.BorderSizePixel = 0
		musicLibraryList.Position = UDim2.new(0, 46, 0, 224)
		musicLibraryList.Size = UDim2.new(1, -92, 1, -314)
		musicLibraryList.ScrollBarThickness = 3
		musicLibraryList.ScrollBarImageColor3 = Color3.fromRGB(218, 112, 118)
		musicLibraryList.CanvasSize = UDim2.new(0, 0, 0, 0)
		musicLibraryList.ClipsDescendants = true
		musicLibraryList.ZIndex = musicLibraryZIndex + 2

		local listPadding = Instance.new("UIPadding")
		listPadding.PaddingTop = UDim.new(0, 2)
		listPadding.PaddingBottom = UDim.new(0, 8)
		listPadding.Parent = musicLibraryList

		local listLayout = Instance.new("UIListLayout")
		listLayout.Parent = musicLibraryList
		listLayout.Padding = UDim.new(0, 7)
		listLayout.SortOrder = Enum.SortOrder.LayoutOrder
		listLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
			musicLibraryList.CanvasSize = UDim2.new(0, 0, 0, listLayout.AbsoluteContentSize.Y + 14)
		end)
		attachScrollbarFade(musicLibraryList, 0.62, 0.65)

		musicPlayerStrip = Instance.new("Frame")
		musicPlayerStrip.Name = "PlayerStrip"
		musicPlayerStrip.Parent = musicCustomRoot
		musicPlayerStrip.BackgroundColor3 = Color3.fromRGB(11, 5, 6)
		musicPlayerStrip.BackgroundTransparency = 0.18
		musicPlayerStrip.BorderSizePixel = 0
		musicPlayerStrip.Position = UDim2.new(0, 46, 1, -78)
		musicPlayerStrip.Size = UDim2.new(1, -92, 0, 54)
		musicPlayerStrip.ZIndex = musicLibraryZIndex + 3
		local stripCorner = Instance.new("UICorner")
		stripCorner.CornerRadius = UDim.new(0, 15)
		stripCorner.Parent = musicPlayerStrip
		local stripStroke = Instance.new("UIStroke")
		stripStroke.Color = Color3.fromRGB(164, 79, 83)
		stripStroke.Thickness = 1
		stripStroke.Transparency = 0.58
		stripStroke.Parent = musicPlayerStrip

		musicNowPlayingLabel = Instance.new("TextLabel")
		musicNowPlayingLabel.Name = "NowPlaying"
		musicNowPlayingLabel.Parent = musicPlayerStrip
		musicNowPlayingLabel.BackgroundTransparency = 1
		musicNowPlayingLabel.Position = UDim2.new(0, 18, 0, 8)
		musicNowPlayingLabel.Size = UDim2.new(1, -158, 0, 18)
		musicNowPlayingLabel.Font = Enum.Font.GothamBold
		musicNowPlayingLabel.Text = musicLibraryState.nowPlayingTitle or "Not Playing"
		musicNowPlayingLabel.TextColor3 = Color3.fromRGB(232, 220, 220)
		musicNowPlayingLabel.TextSize = 12
		musicNowPlayingLabel.TextTransparency = 0.16
		musicNowPlayingLabel.TextXAlignment = Enum.TextXAlignment.Left
		musicNowPlayingLabel.ZIndex = musicLibraryZIndex + 4

		musicProgressBar = Instance.new("Frame")
		musicProgressBar.Name = "ProgressBar"
		musicProgressBar.Parent = musicPlayerStrip
		musicProgressBar.BackgroundColor3 = Color3.fromRGB(148, 82, 87)
		musicProgressBar.BackgroundTransparency = 0.02
		musicProgressBar.BorderSizePixel = 0
		musicProgressBar.Position = UDim2.new(0, 18, 0, 31)
		musicProgressBar.Size = UDim2.new(1, -36, 0, 4)
		musicProgressBar.ZIndex = musicLibraryZIndex + 4
		local progressCorner = Instance.new("UICorner")
		progressCorner.CornerRadius = UDim.new(1, 0)
		progressCorner.Parent = musicProgressBar

		musicProgressFill = Instance.new("Frame")
		musicProgressFill.Name = "Fill"
		musicProgressFill.Parent = musicProgressBar
		musicProgressFill.BackgroundColor3 = Color3.fromRGB(255, 232, 234)
		musicProgressFill.BorderSizePixel = 0
		musicProgressFill.Size = UDim2.new(0, 0, 1, 0)
		musicProgressFill.ZIndex = musicLibraryZIndex + 5
		local fillCorner = Instance.new("UICorner")
		fillCorner.CornerRadius = UDim.new(1, 0)
		fillCorner.Parent = musicProgressFill

		musicProgressHitbox = Instance.new("TextButton")
		musicProgressHitbox.Name = "ProgressHitbox"
		musicProgressHitbox.Parent = musicProgressBar
		musicProgressHitbox.BackgroundTransparency = 1
		musicProgressHitbox.TextTransparency = 1
		musicProgressHitbox.Position = UDim2.new(0, -8, 0.5, -10)
		musicProgressHitbox.Size = UDim2.new(1, 16, 0, 20)
		musicProgressHitbox.ZIndex = musicLibraryZIndex + 6
		musicProgressHitbox.MouseButton1Down:Connect(function(x)
			musicLibraryState.isSeeking = true
			seekMusicProgress(x)
		end)
		musicProgressHitbox.MouseButton1Up:Connect(function()
			musicLibraryState.isSeeking = false
		end)

		musicProgressTime = Instance.new("TextLabel")
		musicProgressTime.Name = "ProgressTime"
		musicProgressTime.Parent = musicPlayerStrip
		musicProgressTime.BackgroundTransparency = 1
		musicProgressTime.Position = UDim2.new(1, -132, 0, 8)
		musicProgressTime.Size = UDim2.new(0, 112, 0, 16)
		musicProgressTime.Font = Enum.Font.GothamBold
		musicProgressTime.Text = "0:00 / --:--"
		musicProgressTime.TextColor3 = Color3.fromRGB(235, 190, 193)
		musicProgressTime.TextSize = 10
		musicProgressTime.TextTransparency = 0.16
		musicProgressTime.TextXAlignment = Enum.TextXAlignment.Right
		musicProgressTime.ZIndex = musicLibraryZIndex + 4

		musicPlayButton = Instance.new("TextButton")
		musicPlayButton.Name = "Play"
		musicPlayButton.Parent = musicPlayerStrip
		musicPlayButton.BackgroundTransparency = 1
		musicPlayButton.Position = UDim2.new(0.5, -38, 1, -23)
		musicPlayButton.Size = UDim2.new(0, 30, 0, 22)
		musicPlayButton.AutoButtonColor = false
		musicPlayButton.Font = Enum.Font.GothamBold
		musicPlayButton.Text = "▶"
		musicPlayButton.TextColor3 = Color3.fromRGB(255, 255, 255)
		musicPlayButton.TextSize = 21
		musicPlayButton.ZIndex = musicLibraryZIndex + 4
		musicPlayButton.MouseButton1Click:Connect(function()
			if musicLibraryState.audio then
				musicLibraryState.audio.Playing = not musicLibraryState.audio.Playing
				musicPlayButton.Text = musicLibraryState.audio.Playing and "Ⅱ" or "▶"
				updateMusicProgress()
				updateMusicLibraryActiveRows()
			else
				musicLibraryState.addToQueue()
			end
		end)

		musicNextButton = Instance.new("TextButton")
		musicNextButton.Name = "Next"
		musicNextButton.Parent = musicPlayerStrip
		musicNextButton.BackgroundTransparency = 1
		musicNextButton.Position = UDim2.new(0.5, 8, 1, -23)
		musicNextButton.Size = UDim2.new(0, 30, 0, 22)
		musicNextButton.AutoButtonColor = false
		musicNextButton.Font = Enum.Font.GothamBold
		musicNextButton.Text = "▶▌"
		musicNextButton.TextColor3 = Color3.fromRGB(255, 255, 255)
		musicNextButton.TextSize = 20
		musicNextButton.ZIndex = musicLibraryZIndex + 4
		musicNextButton.MouseButton1Click:Connect(function()
			musicLibraryState.playNext()
		end)

		userInputService.InputEnded:Connect(function(input)
			if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
				musicLibraryState.isSeeking = false
			end
		end)
		userInputService.InputChanged:Connect(function(input)
			if not musicLibraryState.isSeeking then return end
			if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
				seekMusicProgress(input.Position.X)
			end
		end)
	end

	musicCustomRoot.Visible = true
	musicCustomRoot.ZIndex = musicLibraryZIndex
	showCustomMusicGui()

	startMusicProgressLoop()
	updateMusicLibraryTabs()
	updateMusicProgress()
end

local musicSectionPalette = {
	Color3.fromRGB(29, 116, 182),
	Color3.fromRGB(172, 48, 137),
	Color3.fromRGB(18, 154, 113),
	Color3.fromRGB(190, 87, 28),
	Color3.fromRGB(178, 48, 59),
	Color3.fromRGB(150, 132, 12),
	Color3.fromRGB(93, 71, 166),
	Color3.fromRGB(25, 143, 156),
}

local function getMusicSectionName(section)
	local name = string.gsub(tostring(section or ""), "^.+ / ", "")
	return name ~= "" and name or tostring(section or "Music")
end

local function getMusicSectionKind(section)
	section = tostring(section or "")
	if string.find(section, "Genres /", 1, true) then
		return "GENRE"
	elseif string.find(section, "Vibes /", 1, true) then
		return "VIBE"
	elseif string.find(section, "Discover New Audio", 1, true) then
		return "DISCOVER"
	end

	return "SECTION"
end

local function countMusicSection(section)
	local count = 0
	for _, track in ipairs(musicLibraryState.library) do
		if track.section == section then
			count += 1
		end
	end
	return count
end

local function getMusicSectionAccent(section, index)
	if musicSectionAccents[section] then return musicSectionAccents[section] end
	local accent = musicSectionPalette[((index or 1) - 1) % #musicSectionPalette + 1]
	musicSectionAccents[section] = accent
	return accent
end

local function clearMusicContainer(container)
	if not container then return end
	for _, child in ipairs(container:GetChildren()) do
		if child.Name ~= "Placeholder" and not child:IsA("UIGridLayout") and not child:IsA("UIListLayout") and not child:IsA("UIPadding") then
			child:Destroy()
		end
	end
end

local function setMusicHeader(titleText, subtitleText, showBack, accent)
	if musicHeaderTitle then
		musicHeaderTitle.Text = titleText
		musicHeaderTitle.Position = showBack and UDim2.new(0.091, 0, 0.057, 0) or UDim2.new(0.045, 0, 0.057, 0)
	end
	if musicHeaderSubtitle then
		musicHeaderSubtitle.Text = subtitleText
		musicHeaderSubtitle.Position = showBack and UDim2.new(0.045, 0, 0.115, 0) or UDim2.new(0.045, 0, 0.13, 0)
	end
	if musicHeaderIcon then musicHeaderIcon.Visible = false end
	if musicBackButton then musicBackButton.Visible = showBack end
	if musicRootGradient then
		if showBack and accent then
			musicRootGradient.Color = ColorSequence.new({
				ColorSequenceKeypoint.new(0, Color3.new(0.0470588, 0.0470588, 0.0470588)),
				ColorSequenceKeypoint.new(1, accent),
			})
			musicRootGradient.Offset = Vector2.new(0, 0.65)
		else
			musicRootGradient.Color = ColorSequence.new({
				ColorSequenceKeypoint.new(0, Color3.new(0.0470588, 0.0470588, 0.0470588)),
				ColorSequenceKeypoint.new(1, Color3.new(0.0470588, 0.0470588, 0.0470588)),
			})
			musicRootGradient.Offset = Vector2.new(0, 1.05)
		end
	end
end

local function showMusicSectionCards()
	musicLibraryState.activeSection = nil
	if musicSectionGrid then
		musicSectionGrid.Visible = true
		musicSectionGrid.CanvasPosition = Vector2.new(0, 0)
	end
	if musicTrackList then
		musicTrackList.Visible = false
		clearMusicContainer(musicTrackList)
	end
	setMusicHeader("Music", "Browse the music library by genre, vibe, and trending sections.", false, Color3.fromRGB(12, 12, 12))
end

local function openMusicSection(section, accent)
	musicLibraryState.activeSection = section
	if musicSectionGrid then musicSectionGrid.Visible = false end
	if musicTrackList then
		musicTrackList.Visible = true
		musicTrackList.CanvasPosition = Vector2.new(0, 0)
	end

	local count = countMusicSection(section)
	setMusicHeader(getMusicSectionName(section), tostring(count).." tracks from "..string.lower(getMusicSectionKind(section)).." music.", true, accent or Color3.fromRGB(165, 68, 72))
	musicLibraryState.render()
end

local function ensureMusicSettingsLibraryUI()
	parseRobloxMusicLibrary()

	if not musicPanel:GetAttribute("SettingsStyleMusicSize") then
		local viewport = workspace.CurrentCamera and workspace.CurrentCamera.ViewportSize or Vector2.new(1280, 720)
		musicPanel.AnchorPoint = settingsPanel.AnchorPoint
		musicPanel.Position = settingsPanel.Position
		musicPanel.Size = UDim2.new(0, math.min(613, viewport.X - 42), 0, math.min(384, viewport.Y - 132))
		musicPanel:SetAttribute("SettingsStyleMusicSize", true)
	end

	if musicSettingsLibraryBuilt then
		return
	end

	musicSettingsLibraryBuilt = true
	for _, child in ipairs(musicPanel:GetChildren()) do
		child:Destroy()
	end

	musicCustomRoot = musicPanel
	musicCustomRoot.Name = "Music"
	musicCustomRoot.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	musicCustomRoot.BackgroundTransparency = 0
	musicCustomRoot.BorderSizePixel = 0
	musicCustomRoot.ClipsDescendants = false
	musicCustomRoot.ZIndex = musicLibraryZIndex

	local rootCorner = settingsPanel:FindFirstChildOfClass("UICorner") and settingsPanel:FindFirstChildOfClass("UICorner"):Clone() or Instance.new("UICorner")
	rootCorner.CornerRadius = UDim.new(0, 18)
	rootCorner.Parent = musicCustomRoot

	local settingsShadow = settingsPanel:FindFirstChild("Shadow")
	if settingsShadow then
		local shadow = settingsShadow:Clone()
		shadow.Parent = musicCustomRoot
		shadow.ImageTransparency = 0.66
		shadow.ZIndex = musicLibraryZIndex
	end

	musicRootGradient = settingsPanel:FindFirstChildOfClass("UIGradient") and settingsPanel:FindFirstChildOfClass("UIGradient"):Clone() or Instance.new("UIGradient")
	musicRootGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.new(0.0470588, 0.0470588, 0.0470588)),
		ColorSequenceKeypoint.new(1, Color3.new(0.0470588, 0.0470588, 0.0470588)),
	})
	musicRootGradient.Rotation = settingsPanel:FindFirstChildOfClass("UIGradient") and settingsPanel:FindFirstChildOfClass("UIGradient").Rotation or 90
	musicRootGradient.Offset = Vector2.new(0, 1.05)
	musicRootGradient.Parent = musicCustomRoot

	musicBackButton = settingsPanel:FindFirstChild("Back") and settingsPanel.Back:Clone() or Instance.new("TextButton")
	musicBackButton.Name = "Back"
	musicBackButton.Parent = musicCustomRoot
	musicBackButton.BackgroundTransparency = 1
	musicBackButton.Position = settingsPanel:FindFirstChild("Back") and settingsPanel.Back.Position or UDim2.new(0.041, 0, 0.052, 0)
	musicBackButton.Size = settingsPanel:FindFirstChild("Back") and settingsPanel.Back.Size or UDim2.new(0, 28, 0, 28)
	musicBackButton.AutoButtonColor = false
	if musicBackButton:IsA("TextButton") then
		musicBackButton.Font = Enum.Font.GothamBold
		musicBackButton.Text = "←"
		musicBackButton.TextColor3 = Color3.fromRGB(255, 255, 255)
		musicBackButton.TextSize = 26
		musicBackButton.TextTransparency = 0.02
	elseif musicBackButton:IsA("ImageButton") then
		musicBackButton.ImageTransparency = 0
	end
	musicBackButton.Visible = false
	musicBackButton.ZIndex = musicLibraryZIndex + 4
	musicBackButton.MouseButton1Click:Connect(showMusicSectionCards)

	musicHeaderIcon = Instance.new("TextLabel")
	musicHeaderIcon.Name = "MusicIcon"
	musicHeaderIcon.Parent = musicCustomRoot
	musicHeaderIcon.BackgroundTransparency = 1
	musicHeaderIcon.Position = UDim2.new(0, 42, 0, 36)
	musicHeaderIcon.Size = UDim2.new(0, 30, 0, 34)
	musicHeaderIcon.Font = Enum.Font.GothamBold
	musicHeaderIcon.Text = "♫"
	musicHeaderIcon.TextColor3 = Color3.fromRGB(255, 255, 255)
	musicHeaderIcon.TextSize = 29
	musicHeaderIcon.Visible = false
	musicHeaderIcon.ZIndex = musicLibraryZIndex + 2

	musicHeaderTitle = settingsPanel:FindFirstChild("Title") and settingsPanel.Title:Clone() or Instance.new("TextLabel")
	musicHeaderTitle.Name = "Title"
	musicHeaderTitle.Parent = musicCustomRoot
	musicHeaderTitle.BackgroundTransparency = 1
	musicHeaderTitle.Position = UDim2.new(0.045, 0, 0.057, 0)
	musicHeaderTitle.Size = settingsPanel:FindFirstChild("Title") and settingsPanel.Title.Size or UDim2.new(1, -150, 0, 30)
	musicHeaderTitle.Text = "Music"
	musicHeaderTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
	musicHeaderTitle.TextXAlignment = Enum.TextXAlignment.Left
	musicHeaderTitle.TextTransparency = 0
	musicHeaderTitle.TextTruncate = Enum.TextTruncate.AtEnd
	musicHeaderTitle.ZIndex = musicLibraryZIndex + 2

	musicHeaderSubtitle = settingsPanel:FindFirstChild("Subtitle") and settingsPanel.Subtitle:Clone() or Instance.new("TextLabel")
	musicHeaderSubtitle.Name = "Subtitle"
	musicHeaderSubtitle.Parent = musicCustomRoot
	musicHeaderSubtitle.BackgroundTransparency = 1
	musicHeaderSubtitle.Position = UDim2.new(0.045, 0, 0.13, 0)
	musicHeaderSubtitle.Size = settingsPanel:FindFirstChild("Subtitle") and settingsPanel.Subtitle.Size or UDim2.new(0, 370, 0, 38)
	musicHeaderSubtitle.Text = "Choose a library section, then play any track with one tap."
	musicHeaderSubtitle.TextColor3 = Color3.fromRGB(214, 196, 196)
	musicHeaderSubtitle.TextTransparency = 0.1
	musicHeaderSubtitle.TextXAlignment = Enum.TextXAlignment.Left
	musicHeaderSubtitle.TextYAlignment = Enum.TextYAlignment.Top
	musicHeaderSubtitle.TextWrapped = true
	musicHeaderSubtitle.TextTruncate = Enum.TextTruncate.AtEnd
	musicHeaderSubtitle.ZIndex = musicLibraryZIndex + 2

	local close = Instance.new("TextButton")
	close.Name = "Close"
	close.Parent = musicCustomRoot
	close.BackgroundTransparency = 1
	close.Position = UDim2.new(1, -58, 0, 33)
	close.Size = UDim2.new(0, 34, 0, 34)
	close.AutoButtonColor = false
	close.Font = Enum.Font.Gotham
	close.Text = "×"
	close.TextColor3 = Color3.fromRGB(230, 177, 180)
	close.TextSize = 30
	close.TextTransparency = 0.22
	close.Visible = false
	close.ZIndex = musicLibraryZIndex + 4
	close.MouseButton1Click:Connect(function()
		musicLibraryState.close()
	end)

	musicSectionGrid = Instance.new("ScrollingFrame")
	musicSectionGrid.Name = "SectionCards"
	musicSectionGrid.Parent = musicCustomRoot
	musicSectionGrid.BackgroundTransparency = 1
	musicSectionGrid.BorderSizePixel = 0
	musicSectionGrid.Position = settingsPanel.SettingTypes.Position
	musicSectionGrid.Size = settingsPanel.SettingTypes.Size
	musicSectionGrid.CanvasSize = UDim2.new(0, 0, 0, 0)
	musicSectionGrid.ScrollBarThickness = 3
	musicSectionGrid.ScrollBarImageColor3 = Color3.fromRGB(215, 112, 116)
	musicSectionGrid.ScrollBarImageTransparency = 1
	musicSectionGrid.ZIndex = musicLibraryZIndex + 2
	attachScrollbarFade(musicSectionGrid, 0.55, 0.65)

	local sourceGridPadding = settingsPanel.SettingTypes:FindFirstChildOfClass("UIPadding")
	if sourceGridPadding then
		sourceGridPadding:Clone().Parent = musicSectionGrid
	end

	local gridLayout = settingsPanel.SettingTypes:FindFirstChildOfClass("UIGridLayout") and settingsPanel.SettingTypes:FindFirstChildOfClass("UIGridLayout"):Clone() or Instance.new("UIGridLayout")
	gridLayout.Parent = musicSectionGrid
	if not settingsPanel.SettingTypes:FindFirstChildOfClass("UIGridLayout") then
		gridLayout.CellSize = UDim2.new(0, 181, 0, 100)
		gridLayout.CellPadding = UDim2.new(0, 8, 0, 12)
		gridLayout.SortOrder = Enum.SortOrder.LayoutOrder
	end
	gridLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
		musicSectionGrid.CanvasSize = UDim2.new(0, 0, 0, gridLayout.AbsoluteContentSize.Y + 14)
	end)

	local settingsListTemplate = settingsPanel.SettingLists:FindFirstChild("Template")
	musicTrackList = settingsListTemplate and settingsListTemplate:Clone() or Instance.new("ScrollingFrame")
	musicTrackList.Name = "TrackList"
	musicTrackList.Parent = musicCustomRoot
	musicTrackList.BackgroundTransparency = 1
	musicTrackList.BorderSizePixel = 0
	musicTrackList.Position = settingsPanel.SettingLists.Position
	musicTrackList.Size = settingsPanel.SettingLists.Size
	musicTrackList.Visible = false
	musicTrackList.CanvasSize = UDim2.new(0, 0, 0, 0)
	musicTrackList.ScrollBarThickness = 3
	musicTrackList.ScrollBarImageColor3 = Color3.fromRGB(215, 112, 116)
	musicTrackList.ScrollBarImageTransparency = 1
	musicTrackList.ZIndex = musicLibraryZIndex + 2
	attachScrollbarFade(musicTrackList, 0.55, 0.65)
	musicLibraryList = musicTrackList

	for _, child in ipairs(musicTrackList:GetChildren()) do
		if child.Name ~= "Placeholder" and not child:IsA("UIListLayout") and not child:IsA("UIPadding") then
			child:Destroy()
		end
	end
	if musicTrackList:FindFirstChild("Placeholder") then
		musicTrackList.Placeholder.Visible = true
	end

	local trackLayout = musicTrackList:FindFirstChildOfClass("UIListLayout") or Instance.new("UIListLayout")
	trackLayout.Parent = musicTrackList
	trackLayout.SortOrder = Enum.SortOrder.LayoutOrder
	trackLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
		musicTrackList.CanvasSize = UDim2.new(0, 0, 0, math.max(trackLayout.AbsoluteContentSize.Y + 26, 340))
	end)

	local sectionCardTemplate = settingsPanel.SettingTypes:FindFirstChild("Template")
	for index, section in ipairs(musicLibraryState.sections) do
		local accent = getMusicSectionAccent(section, index)
		local card = sectionCardTemplate:Clone()
		card.Name = "Section_"..string.gsub(getMusicSectionName(section), "[^%w]", "")
		card.Parent = musicSectionGrid
		card.BackgroundTransparency = 0
		card.LayoutOrder = index
		card.Visible = true

		local hue, sat, val = Color3.toHSV(accent)
		hue = math.clamp(hue + 0.01, 0, 1)
		sat = math.clamp(sat + 0.1, 0, 1)
		val = math.clamp(val + 0.2, 0, 1)
		local strokeAccent = Color3.fromHSV(hue, sat, val)

		if card:FindFirstChild("Title") then
			card.Title.Text = string.upper(truncateMusicText(getMusicSectionName(section), 18))
			card.Title.TextTransparency = 0.2
		end
		if card:FindFirstChild("UIGradient") then
			card.UIGradient.Color = ColorSequence.new({
				ColorSequenceKeypoint.new(0, Color3.new(0.0392157, 0.0392157, 0.0392157)),
				ColorSequenceKeypoint.new(1, accent),
			})
		end
		if card:FindFirstChild("UIStroke") then
			card.UIStroke.Transparency = 0
			if card.UIStroke:FindFirstChild("UIGradient") then
				card.UIStroke.UIGradient.Color = ColorSequence.new({
					ColorSequenceKeypoint.new(0, Color3.new(0.117647, 0.117647, 0.117647)),
					ColorSequenceKeypoint.new(1, strokeAccent),
				})
			end
		end
		if card:FindFirstChild("Shadow") then
			card.Shadow.ImageTransparency = 0.66
			if card.Shadow:FindFirstChild("UIGradient") then
				card.Shadow.UIGradient.Color = ColorSequence.new({
					ColorSequenceKeypoint.new(0, Color3.new(0.117647, 0.117647, 0.117647)),
					ColorSequenceKeypoint.new(1, strokeAccent),
				})
			end
		end

		card.MouseEnter:Connect(function()
			if card:FindFirstChild("Title") then
				tweenService:Create(card.Title, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
			end
			if card:FindFirstChild("UIGradient") then
				tweenService:Create(card.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 0.4)}):Play()
			end
			if card:FindFirstChild("UIStroke") and card.UIStroke:FindFirstChild("UIGradient") then
				tweenService:Create(card.UIStroke.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 0.2)}):Play()
			end
			if card:FindFirstChild("Shadow") and card.Shadow:FindFirstChild("UIGradient") then
				tweenService:Create(card.Shadow.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 0.2)}):Play()
			end
		end)
		card.MouseLeave:Connect(function()
			if card:FindFirstChild("Title") then
				tweenService:Create(card.Title, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0.2}):Play()
			end
			if card:FindFirstChild("UIGradient") then
				tweenService:Create(card.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 0.65)}):Play()
			end
			if card:FindFirstChild("UIStroke") and card.UIStroke:FindFirstChild("UIGradient") then
				tweenService:Create(card.UIStroke.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 0.4)}):Play()
			end
			if card:FindFirstChild("Shadow") and card.Shadow:FindFirstChild("UIGradient") then
				tweenService:Create(card.Shadow.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 0.4)}):Play()
			end
		end)
		local interact = card:FindFirstChild("Interact") or card:FindFirstChildWhichIsA("TextButton", true)
		interact.MouseButton1Click:Connect(function()
			openMusicSection(section, accent)
		end)
	end

	showMusicSectionCards()
end
updateMusicLibraryActiveRows = function()
	for _, row in ipairs(getMusicLibraryList():GetChildren()) do
		if row:GetAttribute("MusicTrackId") then
			local active = musicLibraryState.currentTrack and row:GetAttribute("MusicTrackId") == musicLibraryState.currentTrack.id
			local playing = active and musicLibraryState.audio and musicLibraryState.audio.Playing
			tweenService:Create(row, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {
				BackgroundTransparency = 0.9,
				BackgroundColor3 = Color3.fromRGB(27, 27, 27),
			}):Play()

			if row:FindFirstChild("UIStroke") then
				tweenService:Create(row.UIStroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {
					Transparency = 0.6,
				}):Play()
			end

			local switchFrame = row:FindFirstChild("Switch")
			if switchFrame then
				switchFrame.Visible = false
			end

			local controls = row:FindFirstChild("MusicControls")
			local playButton = (controls and controls:FindFirstChild("Play")) or row:FindFirstChild("Play")

			if playButton then
				playButton.Text = "▶"
				tweenService:Create(playButton, TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
					TextColor3 = active and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(226, 226, 226),
					TextTransparency = active and 0.02 or 0.16,
				}):Play()
			end
		end
	end
end

local function ensureMusicAudio()
	if musicLibraryState.audio then return musicLibraryState.audio end

	musicLibraryState.audio = Instance.new("Sound")
	musicLibraryState.audio.Parent = UI
	musicLibraryState.audio.Name = "SiriusMusicAudio"
	musicLibraryState.audio.Volume = 0.75

	return musicLibraryState.audio
end

stopMusicTrack = function()
	musicLibraryState.playGeneration += 1
	if musicLibraryState.audio then
		musicLibraryState.audio:Stop()
		musicLibraryState.audio.SoundId = ""
	end
	musicLibraryState.currentTrack = nil
	musicLibraryState.currentIndex = 0
	musicLibraryState.durationText = "--:--"
	musicLibraryState.nowPlayingTitle = "Not Playing"
	updateMusicTopPlayerDetails(nil)
	if musicPlayButton then musicPlayButton.Text = "▶" end
	syncMusicTopToggle()
	updateMusicProgress()
	updateMusicLibraryActiveRows()
end

playMusicTrack = function(track, index)
	if not track then return end

	ensureMusicTopPlayer()

	local audio = ensureMusicAudio()
	musicLibraryState.playGeneration += 1
	local thisGen = musicLibraryState.playGeneration

	musicLibraryState.currentTrack = track
	musicLibraryState.currentIndex = index or musicLibraryState.currentIndex
	musicLibraryState.durationText = track.duration ~= "" and track.duration or "--:--"
	musicLibraryState.nowPlayingTitle = track.title
	updateMusicTopPlayerDetails(track)
	syncMusicTopToggle()
	if musicTopPlayerOpen then
		showMusicTopPlayer()
	end
	audio.SoundId = track.soundId
	audio.TimePosition = 0
	audio:Play()

	if musicNowPlayingLabel then
		tweenService:Create(musicNowPlayingLabel, TweenInfo.new(0.12, Enum.EasingStyle.Quint), {TextTransparency = 0.62}):Play()
	end
	task.delay(0.08, function()
		if musicLibraryState.currentTrack ~= track then return end
		updateMusicProgress()
		if musicNowPlayingLabel then
			tweenService:Create(musicNowPlayingLabel, TweenInfo.new(0.26, Enum.EasingStyle.Quint), {TextTransparency = 0.1}):Play()
		end
	end)
	updateMusicProgress()
	task.delay(0.45, updateMusicProgress)
	if musicPlayButton then musicPlayButton.Text = "Ⅱ" end
	updateMusicLibraryActiveRows()

	if checkSetting("Now Playing Notifications").current then
		queueNotification("Now Playing", track.title.." • "..track.artist, 4400695581)
	end

	task.spawn(function()
		audio.Ended:Wait()
		if thisGen ~= musicLibraryState.playGeneration or not musicLibraryState.audio or musicLibraryState.audio.SoundId ~= track.soundId then return end
		musicLibraryState.currentTrack = nil
		musicLibraryState.currentIndex = 0
		musicLibraryState.durationText = "--:--"
		musicLibraryState.nowPlayingTitle = "Not Playing"
		updateMusicTopPlayerDetails(nil)
		if musicPlayButton then musicPlayButton.Text = "▶" end
		syncMusicTopToggle()
		updateMusicProgress()
		updateMusicLibraryActiveRows()
	end)
end

openMusicPlayerForTrack = function(track, index)
	if not track then return end

	ensureMusicTopPlayer()
	local active = musicLibraryState.currentTrack and musicLibraryState.currentTrack.id == track.id
	if active then
		local audio = ensureMusicAudio()
		if not audio.Playing then
			pcall(function()
				audio:Resume()
			end)
			if not audio.Playing then
				audio:Play()
			end
		end
		showMusicTopPlayer()
		updateMusicTopPlayerDetails(track)
		if musicPlayButton then musicPlayButton.Text = audio.Playing and "Ⅱ" or "▶" end
		updateMusicProgress()
		updateMusicLibraryActiveRows()
		return
	end

	playMusicTrack(track, index)
end
do local b = _G.__SIRIUSX_BRIDGE; if b then b.music = { state = musicLibraryState, play = playMusicTrack, stop = stopMusicTrack, openOrResume = openMusicPlayerForTrack, parse = parseRobloxMusicLibrary, ensureAudio = ensureMusicAudio } end end -- [NextGen bridge: music]

local function createMusicLibraryRow(track, index, token)
	local rowTemplate = settingsPanel.SettingLists.Template:FindFirstChild("SwitchTemplate")
	local row = rowTemplate and rowTemplate:Clone() or Instance.new("Frame")
	row.Parent = getMusicLibraryList()
	row.Name = "LibraryTrack_"..track.id
	row:SetAttribute("MusicTrackId", track.id)
	row.LayoutOrder = index
	row.Visible = true
	row.BackgroundTransparency = 1
	row.ZIndex = musicLibraryZIndex + 4

	local stroke = row:FindFirstChild("UIStroke")
	if stroke then
		stroke.Transparency = 1
	end

	if row:FindFirstChild("LicenseDisplay") then
		row.LicenseDisplay.Visible = false
	end

	local title = row:FindFirstChild("Title") or Instance.new("TextLabel")
	title.Name = "Title"
	title.Parent = row
	title.Visible = true
	title.Text = truncateMusicText(track.title, 34)
	title.TextTransparency = 1
	title.TextTruncate = Enum.TextTruncate.AtEnd

	local subtitle = row:FindFirstChild("Description") or row:FindFirstChild("Subtitle") or Instance.new("TextLabel")
	subtitle.Name = "Description"
	subtitle.Parent = row
	subtitle.Visible = true
	subtitle.TextWrapped = true
	subtitle.Size = UDim2.new(0, 333, 0, 999)
	subtitle.Text = "Roblox audio by "..track.artist..". Duration "..track.duration.."."
	subtitle.TextTransparency = 1
	subtitle.Size = UDim2.new(0, 333, 0, subtitle.TextBounds.Y + 10)
	row.Size = UDim2.new(0, 558, 0, subtitle.TextBounds.Y + 44)

	local switchFrame = row:FindFirstChild("Switch")
	if switchFrame then
		switchFrame.Visible = false
	end

	if row:FindFirstChild("Interact") then
		row.Interact.ZIndex = musicLibraryZIndex + 5
	end

	local controls = Instance.new("Frame")
	controls.Name = "MusicControls"
	controls.Parent = row
	controls.BackgroundTransparency = 1
	controls.Position = UDim2.new(1, -64, 0.5, -17)
	controls.Size = UDim2.new(0, 38, 0, 34)
	controls.ZIndex = musicLibraryZIndex + 9

	local playButton = Instance.new("TextButton")
	playButton.Name = "Play"
	playButton.Parent = controls
	playButton.BackgroundTransparency = 1
	playButton.Position = UDim2.new(0, 0, 0, 0)
	playButton.Size = UDim2.new(1, 0, 1, 0)
	playButton.AutoButtonColor = false
	playButton.Font = Enum.Font.GothamBold
	playButton.Text = "▶"
	playButton.TextColor3 = Color3.fromRGB(226, 226, 226)
	playButton.TextSize = 19
	playButton.TextTransparency = 0.18
	playButton.ZIndex = musicLibraryZIndex + 10

	local function openTrackPlayer()
		openMusicPlayerForTrack(track, index)
	end

	local function wireIconHover(button)
		button.MouseEnter:Connect(function()
			tweenService:Create(button, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
		end)
		button.MouseLeave:Connect(function()
			updateMusicLibraryActiveRows()
		end)
	end

	wireIconHover(playButton)

	playButton.MouseButton1Click:Connect(openTrackPlayer)

	local rowTouching = false
	row.MouseEnter:Connect(function()
		if musicLibraryState.renderToken ~= token then return end
		rowTouching = true
		if stroke then
			tweenService:Create(stroke, TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.45}):Play()
		end
		tweenService:Create(row, TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.83}):Play()
	end)

	row.MouseLeave:Connect(function()
		if musicLibraryState.renderToken ~= token then return end
		rowTouching = false
		updateMusicLibraryActiveRows()
	end)

	if row:FindFirstChild("Interact") then
		row.Interact.MouseButton1Click:Connect(function()
			if stroke then
				tweenService:Create(stroke, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 1}):Play()
			end
			tweenService:Create(row, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.8}):Play()
			openTrackPlayer()
			task.wait(0.1)
			if rowTouching then
				if stroke then
					tweenService:Create(stroke, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.45}):Play()
				end
				tweenService:Create(row, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.83}):Play()
			else
				updateMusicLibraryActiveRows()
			end
		end)
	else
		row.InputBegan:Connect(function(input)
			if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
				openTrackPlayer()
			end
		end)
	end

	task.delay(math.min(index * 0.012, 0.36), function()
		if musicLibraryState.renderToken ~= token or not row.Parent then return end
		tweenService:Create(row, TweenInfo.new(0.32, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.9}):Play()
		if stroke then
			tweenService:Create(stroke, TweenInfo.new(0.32, Enum.EasingStyle.Quint), {Transparency = 0.6}):Play()
		end
		tweenService:Create(title, TweenInfo.new(0.32, Enum.EasingStyle.Quint), {TextTransparency = 0.02}):Play()
		tweenService:Create(subtitle, TweenInfo.new(0.32, Enum.EasingStyle.Quint), {TextTransparency = 0.22}):Play()
		updateMusicLibraryActiveRows()
	end)
end

local function createMusicLibraryFallbackRow(track, index, token)
	local row = Instance.new("Frame")
	row.Parent = getMusicLibraryList()
	row.Name = "LibraryTrack_"..track.id
	row:SetAttribute("MusicTrackId", track.id)
	row.LayoutOrder = index
	row.Visible = true
	row.Size = UDim2.new(0, 558, 0, 82)
	row.BackgroundColor3 = Color3.fromRGB(27, 27, 27)
	row.BackgroundTransparency = 0.9
	row.BorderSizePixel = 0
	row.ZIndex = musicLibraryZIndex + 4

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 8)
	corner.Parent = row

	local stroke = Instance.new("UIStroke")
	stroke.Color = Color3.fromRGB(66, 66, 66)
	stroke.Transparency = 0.6
	stroke.Parent = row

	local title = Instance.new("TextLabel")
	title.Name = "Title"
	title.Parent = row
	title.BackgroundTransparency = 1
	title.Position = UDim2.new(0, 28, 0, 16)
	title.Size = UDim2.new(1, -160, 0, 22)
	title.Font = Enum.Font.GothamBold
	title.Text = truncateMusicText(track.title, 34)
	title.TextColor3 = Color3.fromRGB(235, 235, 235)
	title.TextSize = 15
	title.TextXAlignment = Enum.TextXAlignment.Left
	title.TextTruncate = Enum.TextTruncate.AtEnd
	title.ZIndex = musicLibraryZIndex + 5

	local subtitle = Instance.new("TextLabel")
	subtitle.Name = "Description"
	subtitle.Parent = row
	subtitle.BackgroundTransparency = 1
	subtitle.Position = UDim2.new(0, 28, 0, 41)
	subtitle.Size = UDim2.new(1, -170, 0, 24)
	subtitle.Font = Enum.Font.Gotham
	subtitle.Text = "Roblox audio by "..track.artist..". Duration "..track.duration.."."
	subtitle.TextColor3 = Color3.fromRGB(205, 205, 205)
	subtitle.TextSize = 12
	subtitle.TextTransparency = 0.22
	subtitle.TextXAlignment = Enum.TextXAlignment.Left
	subtitle.TextTruncate = Enum.TextTruncate.AtEnd
	subtitle.ZIndex = musicLibraryZIndex + 5

	local playButton = Instance.new("TextButton")
	playButton.Name = "Play"
	playButton.Parent = row
	playButton.BackgroundTransparency = 1
	playButton.Position = UDim2.new(1, -62, 0.5, -16)
	playButton.Size = UDim2.new(0, 36, 0, 32)
	playButton.AutoButtonColor = false
	playButton.Font = Enum.Font.GothamBold
	playButton.Text = "▶"
	playButton.TextColor3 = Color3.fromRGB(235, 235, 235)
	playButton.TextSize = 19
	playButton.ZIndex = musicLibraryZIndex + 6

	local function openTrackPlayer()
		openMusicPlayerForTrack(track, index)
	end

	playButton.MouseButton1Click:Connect(openTrackPlayer)
	row.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			openTrackPlayer()
		end
	end)

	task.delay(math.min(index * 0.012, 0.36), function()
		if musicLibraryState.renderToken ~= token or not row.Parent then return end
		tweenService:Create(row, TweenInfo.new(0.32, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.9}):Play()
	end)
end

musicLibraryState.render = function()
	ensureMusicSettingsLibraryUI()
	musicLibraryState.renderToken += 1
	local token = musicLibraryState.renderToken
	local matches = {}
	local selectedSection = musicLibraryState.activeSection

	if not selectedSection then return end
	for _, track in ipairs(musicLibraryState.library) do
		if track.section == selectedSection then
			table.insert(matches, track)
		end
	end

	musicLibraryState.renderedTracks = matches
	clearMusicLibraryRows()

	if #matches == 0 then
		return
	end

	for index, track in ipairs(matches) do
		local ok = pcall(function()
			createMusicLibraryRow(track, index, token)
		end)
		if not ok then
			local brokenRow = getMusicLibraryList():FindFirstChild("LibraryTrack_"..track.id)
			if brokenRow then brokenRow:Destroy() end
			createMusicLibraryFallbackRow(track, index, token)
		end
	end

	task.defer(function()
		if musicLibraryState.renderToken ~= token then return end
		local list = getMusicLibraryList()
		local layout = list:FindFirstChildOfClass("UIListLayout")
		if layout then
			list.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + 14)
		end
	end)
end

musicLibraryState.playNext = function()
	if #musicLibraryState.renderedTracks == 0 then
		musicLibraryState.render()
	end

	if #musicLibraryState.renderedTracks == 0 then
		if musicLibraryState.audio then musicLibraryState.audio.Playing = false musicLibraryState.audio.SoundId = "" end
		musicLibraryState.currentTrack = nil
		musicLibraryState.currentIndex = 0
		musicLibraryState.nowPlayingTitle = "Not Playing"
		if musicNowPlayingLabel then musicNowPlayingLabel.Text = "Not Playing" end
		if musicProgressTime then musicProgressTime.Text = "0:00 / --:--" end
		if musicPlayButton then musicPlayButton.Text = "▶" end
		return
	end

	local nextIndex = musicLibraryState.currentIndex + 1
	if nextIndex > #musicLibraryState.renderedTracks then nextIndex = 1 end
	playMusicTrack(musicLibraryState.renderedTracks[nextIndex], nextIndex)
end

musicLibraryState.addToQueue = function()
	if #musicLibraryState.renderedTracks == 0 then
		musicLibraryState.render()
	end

	if #musicLibraryState.renderedTracks > 0 then
		local randomIndex = math.random(1, #musicLibraryState.renderedTracks)
		playMusicTrack(musicLibraryState.renderedTracks[randomIndex], randomIndex)
	end
end

local lastMusicDevelopmentNotice = 0
local function showMusicDevelopmentNotice()
	local now = os.clock()
	if now - lastMusicDevelopmentNotice < 1.4 then
		return
	end

	lastMusicDevelopmentNotice = now
	queueNotification("Music In Development", "The Music panel is currently being rebuilt and will be available in a future Sirius X update.", 4400696294)
end

musicLibraryState.open = function()
	if true then
		if musicPanel.Visible then
			musicPanel.Visible = false
		end

		showMusicDevelopmentNotice()
		return
	end

	debounce = true
	ensureMusicSettingsLibraryUI()
	musicPanel.Visible = true
	showMusicSectionCards()

	debounce = false
end

musicLibraryState.close = function()
	debounce = true
	musicPanel.Visible = false

	debounce = false
end
end

local function createReverb(timing)
	for index, sound in next, soundInstances do
		if not sound:FindFirstChild("SiriusAudioProfile") then
			local reverb = Instance.new("EqualizerSoundEffect")

			reverb.Name = "SiriusAudioProfile"
			reverb.Parent = sound

			reverb.Enabled = false

			reverb.HighGain = 0
			reverb.LowGain = 0
			reverb.MidGain = 0
			reverb.Enabled = true

			if timing then
				tweenService:Create(reverb, TweenInfo.new(timing, Enum.EasingStyle.Exponential), {HighGain = -20}):Play()
				tweenService:Create(reverb, TweenInfo.new(timing, Enum.EasingStyle.Exponential), {LowGain = 5}):Play()
				tweenService:Create(reverb, TweenInfo.new(timing, Enum.EasingStyle.Exponential), {MidGain = -20}):Play()
			end
		end
	end
end

local function runScript(raw)
	loadstring(game:HttpGet(raw))()
end

local function syncExperienceInformation()
	siriusValues.currentCreator = creatorId

	if creatorType == Enum.CreatorType.Group then
		siriusValues.currentGroup = creatorId
		siriusValues.currentCreator = "group"
	end

	for _, gameFound in pairs(siriusValues.games) do
		if gameFound.id == placeId and gameFound.enabled then

			local minimumTier = gameFound.minimumTier

			if minimumTier == "Essential" then
				if not (Essential or Pro) then
					return
				end
			elseif minimumTier == "Pro" then
				if not Pro then
					return
				end
			end

			local rawFile = siriusValues.rawTree..gameFound.raw
			siriusValues.currentGame = gameFound

			gameDetectionPrompt.ScriptTitle.Text = gameFound.name
			gameDetectionPrompt.Layer.ScriptSubtitle.Text = gameFound.description
			gameDetectionPrompt.Thumbnail.Image = "https://assetgame.roblox.com/Game/Tools/ThumbnailAsset.ashx?aid="..tostring(placeId).."&fmt=png&wd=420&ht=420"

			gameDetectionPrompt.Size = UDim2.new(0, 550, 0, 0)
			gameDetectionPrompt.Position = UDim2.new(0.5, 0, 0, 120)
			gameDetectionPrompt.UICorner.CornerRadius = UDim.new(0, 9)
			gameDetectionPrompt.Thumbnail.UICorner.CornerRadius = UDim.new(0, 9)
			gameDetectionPrompt.ScriptTitle.Position = UDim2.new(0, 30, 0.5, 0)
			gameDetectionPrompt.Layer.Visible = false
			gameDetectionPrompt.Warning.Visible = false

			wipeTransparency(gameDetectionPrompt, 1, true)

			gameDetectionPrompt.Visible = true

			tweenService:Create(gameDetectionPrompt, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {BackgroundTransparency = 0}):Play()
			tweenService:Create(gameDetectionPrompt.Thumbnail, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {ImageTransparency = 0.4}):Play()
			tweenService:Create(gameDetectionPrompt.ScriptTitle, TweenInfo.new(0.6, Enum.EasingStyle.Quint),  {TextTransparency = 0}):Play()

			tweenService:Create(gameDetectionPrompt, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Size = UDim2.new(0, 587, 0, 44)}):Play()
			tweenService:Create(gameDetectionPrompt, TweenInfo.new(1, Enum.EasingStyle.Exponential), {Position = UDim2.new(0.5, 0, 0, 150)}):Play()

			task.wait(1)

			wipeTransparency(gameDetectionPrompt.Layer, 1, true)

			gameDetectionPrompt.Layer.Visible = true

			tweenService:Create(gameDetectionPrompt, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Size = UDim2.new(0, 473, 0, 154)}):Play()
			tweenService:Create(gameDetectionPrompt.ScriptTitle, TweenInfo.new(1, Enum.EasingStyle.Exponential), {Position = UDim2.new(0, 23, 0.352, 0)}):Play()
			tweenService:Create(gameDetectionPrompt, TweenInfo.new(1, Enum.EasingStyle.Exponential), {Position = UDim2.new(0.5, 0, 0, 200)}):Play()
			tweenService:Create(gameDetectionPrompt.UICorner, TweenInfo.new(1, Enum.EasingStyle.Exponential), {CornerRadius = UDim.new(0, 13)}):Play()
			tweenService:Create(gameDetectionPrompt.Thumbnail.UICorner, TweenInfo.new(1, Enum.EasingStyle.Exponential), {CornerRadius = UDim.new(0, 13)}):Play()
			tweenService:Create(gameDetectionPrompt.Thumbnail, TweenInfo.new(1, Enum.EasingStyle.Exponential), {ImageTransparency = 0.5}):Play()

			task.wait(0.3)
			tweenService:Create(gameDetectionPrompt.Layer.ScriptSubtitle, TweenInfo.new(0.6, Enum.EasingStyle.Quint),  {TextTransparency = 0.3}):Play()
			tweenService:Create(gameDetectionPrompt.Layer.Run, TweenInfo.new(0.6, Enum.EasingStyle.Quint),  {TextTransparency = 0}):Play()
			tweenService:Create(gameDetectionPrompt.Layer.Run.UIStroke, TweenInfo.new(0.6, Enum.EasingStyle.Quint),  {Transparency = 0.85}):Play()
			tweenService:Create(gameDetectionPrompt.Layer.Run, TweenInfo.new(0.6, Enum.EasingStyle.Quint),  {BackgroundTransparency = 0.6}):Play()

			task.wait(0.2)

			tweenService:Create(gameDetectionPrompt.Layer.Close, TweenInfo.new(0.7, Enum.EasingStyle.Exponential), {ImageTransparency = 0.6}):Play()

			task.wait(0.3)

			local function closeGameDetection()
				tweenService:Create(gameDetectionPrompt.Layer.ScriptSubtitle, TweenInfo.new(0.3, Enum.EasingStyle.Quint),  {TextTransparency = 1}):Play()
				tweenService:Create(gameDetectionPrompt.Layer.Run, TweenInfo.new(0.3, Enum.EasingStyle.Quint),  {TextTransparency = 1}):Play()
				tweenService:Create(gameDetectionPrompt.Layer.Run, TweenInfo.new(0.3, Enum.EasingStyle.Quint),  {BackgroundTransparency = 1}):Play()
				tweenService:Create(gameDetectionPrompt.Layer.Close, TweenInfo.new(0.3, Enum.EasingStyle.Exponential), {ImageTransparency = 1}):Play()
				tweenService:Create(gameDetectionPrompt.Thumbnail, TweenInfo.new(0.3, Enum.EasingStyle.Quint),  {ImageTransparency = 1}):Play()
				tweenService:Create(gameDetectionPrompt.ScriptTitle, TweenInfo.new(0.3, Enum.EasingStyle.Quint),  {TextTransparency = 1}):Play()
				tweenService:Create(gameDetectionPrompt.Layer.Run.UIStroke, TweenInfo.new(0.3, Enum.EasingStyle.Quint),  {Transparency = 1}):Play()
				task.wait(0.05)
				tweenService:Create(gameDetectionPrompt, TweenInfo.new(0.4, Enum.EasingStyle.Quint), {Size = UDim2.new(0, 400, 0, 0)}):Play()
				tweenService:Create(gameDetectionPrompt.UICorner, TweenInfo.new(0.2, Enum.EasingStyle.Exponential), {CornerRadius = UDim.new(0, 5)}):Play()
				tweenService:Create(gameDetectionPrompt.Thumbnail.UICorner, TweenInfo.new(0.2, Enum.EasingStyle.Exponential), {CornerRadius = UDim.new(0, 5)}):Play()
				task.wait(0.41)
				gameDetectionPrompt.Visible = false
			end

			gameDetectionPrompt.Layer.Run.MouseButton1Click:Connect(function()
				closeGameDetection()
				queueNotification("Running "..gameFound.name, "Now running Sirius' "..gameFound.name.." script, this may take a moment.", 4400701828)
				runScript(rawFile)

			end)

			gameDetectionPrompt.Layer.Close.MouseButton1Click:Connect(function()
				closeGameDetection()
			end)

			break
		end
	end
end

local function refreshSliderPadding(data)
	if not data or not data.object then
		return false
	end

	local interact = data.object:FindFirstChild("Interact")
	if not interact then
		return false
	end

	local left = interact.AbsolutePosition.X
	local right = left + interact.AbsoluteSize.X
	if right <= left then
		return false
	end

	data.padding = {left, right}
	return true
end

local function updateSliderPadding()
	for _, v in pairs(siriusValues.sliders) do
		refreshSliderPadding(v)
	end
end

function siriusValues.getSliderPointerX(input)
	if input and input.UserInputType == Enum.UserInputType.Touch then
		return input.Position.X
	end

	return userInputService:GetMouseLocation().X
end

local function updateSlider(data, setValue, forceValue, pointerX)
	local inverse_interpolation

	if setValue then
		setValue = math.clamp(setValue, data.values[1], data.values[2])
		inverse_interpolation = (setValue - data.values[1]) / (data.values[2] - data.values[1])
	else
		refreshSliderPadding(data)
		if not data.padding or data.padding[2] <= data.padding[1] then
			return
		end

		local posX = math.clamp(pointerX or data.pointerX or siriusValues.getSliderPointerX(), data.padding[1], data.padding[2])
		inverse_interpolation = (posX - data.padding[1]) / (data.padding[2] - data.padding[1])
	end

	tweenService:Create(data.object.Progress, TweenInfo.new(.5, Enum.EasingStyle.Quint), {Size = UDim2.new(inverse_interpolation, 0, 1, 0)}):Play()

	local value = math.floor(data.values[1] + (data.values[2] - data.values[1]) * inverse_interpolation + .5)
	data.object.Information.Text = value.." "..data.name
	data.value = value

	if data.callback and ((not setValue) or forceValue) then
		data.callback(value)
	end
end

local function resetSliders()
	for _, v in pairs(siriusValues.sliders) do
		updateSlider(v, v.default, true)
	end
end

local actionIconIdleTransparency = 0.22
local actionIconActiveTransparency = 0
local actionIconColor = Color3.fromRGB(255, 255, 255)
local actionVisualTweens = {}
local actionIconSwapIds = {}

local function applySharpIcon(icon, transparency)
	icon.ImageColor3 = actionIconColor
	icon.ImageTransparency = transparency

	pcall(function()
		icon.ScaleType = Enum.ScaleType.Fit
	end)

	pcall(function()
		icon.ResampleMode = Enum.ResamplerMode.Default
	end)
end

local function resolveActionIcon(iconSource)
	if type(iconSource) == "number" then
		local numericId = string.format("%.0f", iconSource)
		if string.len(numericId) > 11 then
			return "rbxthumb://type=Asset&id=" .. numericId .. "&w=150&h=150"
		end
		return "rbxassetid://" .. numericId
	end

	if type(iconSource) == "string" then
		if string.match(iconSource, "^%d+$") then
			if string.len(iconSource) > 11 then
				return "rbxthumb://type=Asset&id=" .. iconSource .. "&w=150&h=150"
			end
			return "rbxassetid://" .. iconSource
		end

		if string.find(iconSource, "rbxthumb://", 1, true) == 1 then
			return iconSource
		end

		if string.find(iconSource, "rbxassetid://", 1, true) == 1 then
			return iconSource
		end

		if string.find(iconSource, "https://", 1, true) == 1 or string.find(iconSource, "http://", 1, true) == 1 then
			local resolvedIcon = iconSource
			pcall(function()
				if typeof(getcustomasset) ~= "function" or typeof(writefile) ~= "function" or typeof(game.HttpGet) ~= "function" then return end

				local folder = "SIRIUSXIcons"
				if typeof(makefolder) == "function" then
					if typeof(isfolder) == "function" then
						if not isfolder(folder) then
							makefolder(folder)
						end
					else
						pcall(makefolder, folder)
					end
				end

				local fileName = iconSource:match("([^/%?]+)%.png") or "character-action-icon"
				local filePath = folder.."/"..fileName..".png"
				if typeof(isfile) ~= "function" or not isfile(filePath) then
					writefile(filePath, game:HttpGet(iconSource))
				end

				resolvedIcon = getcustomasset(filePath)
			end)
			return resolvedIcon
		end
	end

	return tostring(iconSource or "")
end

local function playActionVisualTween(object, channel, tweenInfo, properties)
	actionVisualTweens[object] = actionVisualTweens[object] or {}

	if actionVisualTweens[object][channel] then
		actionVisualTweens[object][channel]:Cancel()
	end

	local tween = tweenService:Create(object, tweenInfo, properties)
	actionVisualTweens[object][channel] = tween
	tween.Completed:Connect(function()
		if actionVisualTweens[object] and actionVisualTweens[object][channel] == tween then
			actionVisualTweens[object][channel] = nil
		end
	end)
	tween:Play()

	return tween
end

local function transitionActionIcon(icon, imageId, targetTransparency)
	actionIconSwapIds[icon] = (actionIconSwapIds[icon] or 0) + 1
	local swapId = actionIconSwapIds[icon]
	local midTransparency = math.max(targetTransparency, 0.44)

	playActionVisualTween(icon, "clarity", TweenInfo.new(0.08, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
		ImageColor3 = actionIconColor,
		ImageTransparency = midTransparency
	})

	task.delay(0.07, function()
		if actionIconSwapIds[icon] ~= swapId then return end
		icon.Image = resolveActionIcon(imageId)
		applySharpIcon(icon, midTransparency)

		playActionVisualTween(icon, "clarity", TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
			ImageColor3 = actionIconColor,
			ImageTransparency = targetTransparency
		})
		end)
	end
	
	siriusValues.adminAbilityAliases = {
		all = "all", stopall = "all", stopabilities = "all", disableall = "all",
		noclip = "Noclip", clip = "Noclip",
		fly = "Flight", unfly = "Flight", flight = "Flight",
		fling = "Fling",
		esp = "Extrasensory Perception", extrasensory = "Extrasensory Perception", extrasensoryperception = "Extrasensory Perception",
		infjump = "Infinite Jump", infinitejump = "Infinite Jump",
		walkinair = "Walk in Air", airwalk = "Walk in Air",
		antivoid = "Anti Void",
		autofind = "Auto Find [works with auto aim]", find = "Auto Find [works with auto aim]",
		clicktp = "click teleport tool", clickteleport = "click teleport tool", clickteleporttool = "click teleport tool",
		ghost = "Ghost Mode / Spectate", ghostmode = "Ghost Mode / Spectate", spectate = "Ghost Mode / Spectate", ghostspectate = "Ghost Mode / Spectate",
		shader = "Shaders", shaders = "Shaders",
		invis = "Invisibility", invisible = "Invisibility", invisibility = "Invisibility",
		autoaim = "auto aim", aim = "auto aim",
		blur = "smooth screen blur", screenblur = "smooth screen blur", smoothscreenblur = "smooth screen blur",
	}

	function siriusValues.normaliseAdminAbilityKey(value)
		return tostring(value or ""):lower():gsub("[^%w]", "")
	end

	function siriusValues.resolveAdminAbilityName(value)
		local key = siriusValues.normaliseAdminAbilityKey(value)
		if key == "" then return nil end

		local alias = siriusValues.adminAbilityAliases[key]
		if alias then return alias end

		for _, action in ipairs(siriusValues.actions) do
			if siriusValues.normaliseAdminAbilityKey(action.name) == key then return action.name end
		end
		for _, slider in ipairs(siriusValues.sliders) do
			if siriusValues.normaliseAdminAbilityKey(slider.name) == key then return slider.name end
		end

		return nil
	end

	function siriusValues.setAdminActionVisualOff(action)
		local resolved = checkAction(action.name)
		local object = resolved and resolved.object
		if object then
			transitionActionIcon(object.Icon, action.images[2], actionIconIdleTransparency)
			playActionVisualTween(object, "surface", TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.55})
			playActionVisualTween(object.UIStroke, "stroke", TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.4})
		end
	end

	function siriusValues.disableSiriusAbilityByName(abilityName, context)
		local resolvedName = siriusValues.resolveAdminAbilityName(abilityName)
		if not resolvedName then
			return false, "Unknown ability"
		end

		if resolvedName == "all" then
			if siriusValues.stopActiveAbilitiesForModeratorDetection then
				pcall(siriusValues.stopActiveAbilitiesForModeratorDetection, context or "remote beta control")
			end
			return true, "all abilities"
		end

		for _, action in ipairs(siriusValues.actions) do
			if action.name == resolvedName then
				if action.enabled then
					action.enabled = false
					if action.callback then
						pcall(action.callback, false)
					end
				end
				siriusValues.setAdminActionVisualOff(action)
				return true, action.name
			end
		end

		for _, slider in ipairs(siriusValues.sliders) do
			if slider.name == resolvedName then
				local targetValue = slider.name == "auto aim" and 0 or slider.default
				if slider.object then
					updateSlider(slider, targetValue, true)
				else
					slider.value = targetValue
					if slider.callback then
						pcall(slider.callback, targetValue)
					end
				end
				return true, slider.name
			end
		end

		return false, "Unknown ability"
	end

	siriusValues.stopActiveAbilitiesForModeratorDetection = function(context)
		local stopped = 0
		local reason = tostring(context or "moderator/admin role was detected")

	if playerActionSystem.stopAllPlayerActions then
		pcall(playerActionSystem.stopAllPlayerActions, reason)
	end

	for _, action in ipairs(siriusValues.actions) do
		if action.enabled and not action.disableAfter and action.name ~= "Refresh" and action.name ~= "Respawn" then
			action.enabled = false
			stopped += 1

			if action.callback then
				pcall(action.callback, false)
			end

			local resolved = checkAction(action.name)
			local object = resolved and resolved.object
			if object then
				transitionActionIcon(object.Icon, action.images[2], actionIconIdleTransparency)
				playActionVisualTween(object, "surface", TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.55})
				playActionVisualTween(object.UIStroke, "stroke", TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.4})
			end
		end
	end

	pcall(function()
		characterRuntime.setAutoFindEnabled(false)
	end)
	pcall(function()
		characterRuntime.walkInAirEnabled = false
		characterRuntime.clearWalkInAirLane()
	end)
	pcall(function()
		characterRuntime.clickTeleportEnabled = false
		characterRuntime.destroyClickTeleportTool()
	end)
	pcall(function()
		if characterRuntime.ghostModeEnabled then
			characterRuntime.applyGhostMode(false)
		end
	end)
	pcall(function()
		if characterRuntime.invisibilityEnabled then
			characterRuntime.applyInvisibility(false)
		end
	end)

	if stopped > 0 and reason ~= "remote beta control" then
		queueNotification("Abilities Stopped", "Active abilities were disabled after "..reason..".", 3944670656)
	end
end

local function sortActions()	
	characterPanel.Interactions.Grid.Template.Visible = false
	characterPanel.Interactions.Sliders.Template.Visible = false

	for _, action in ipairs(siriusValues.actions) do
		if action.hidden then
			continue
		end

		local newAction = characterPanel.Interactions.Grid.Template:Clone()
		newAction.Name = action.name
		newAction.Parent = characterPanel.Interactions.Grid
		newAction.BackgroundColor3 = action.color
		newAction.UIStroke.Color = action.color
		newAction.Icon.Image = resolveActionIcon(action.images[2])
		applySharpIcon(newAction.Icon, actionIconIdleTransparency)
		newAction.Visible = true

		newAction.BackgroundTransparency = 0.8
		newAction.Transparency = 0.7


		newAction.MouseEnter:Connect(function()
			characterPanel.Interactions.ActionsTitle.Text = string.upper(action.name)
			if action.enabled or debounce then return end
			playActionVisualTween(newAction, "surface", TweenInfo.new(0.36, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.4})
			playActionVisualTween(newAction.UIStroke, "stroke", TweenInfo.new(0.42, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.6})
		end)

		newAction.MouseLeave:Connect(function()
			if action.enabled or debounce then return end
			playActionVisualTween(newAction, "surface", TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.55})
			playActionVisualTween(newAction.UIStroke, "stroke", TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.4})
		end)

		characterPanel.Interactions.Grid.MouseLeave:Connect(function()
			characterPanel.Interactions.ActionsTitle.Text = "PLAYER ACTIONS"
		end)

		newAction.Interact.MouseButton1Click:Connect(function()
			local success, response = pcall(function()
				action.enabled = not action.enabled
				action.callback(action.enabled)

				if action.enabled then
					transitionActionIcon(newAction.Icon, action.images[1], actionIconActiveTransparency)
					playActionVisualTween(newAction, "surface", TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.1})
					playActionVisualTween(newAction.UIStroke, "stroke", TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 1})

					if action.disableAfter then
						task.delay(action.disableAfter, function()
							action.enabled = false
							transitionActionIcon(newAction.Icon, action.images[2], actionIconIdleTransparency)
							playActionVisualTween(newAction, "surface", TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.55})
							playActionVisualTween(newAction.UIStroke, "stroke", TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.4})
						end)
					end

					if action.rotateWhileEnabled then
						repeat
							newAction.Icon.Rotation = 0
							tweenService:Create(newAction.Icon, TweenInfo.new(0.75, Enum.EasingStyle.Quint), {Rotation = 360}):Play()
							task.wait(1)
						until not action.enabled
						newAction.Icon.Rotation = 0
					end
				else
					transitionActionIcon(newAction.Icon, action.images[2], actionIconIdleTransparency)
					playActionVisualTween(newAction, "surface", TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.55})
					playActionVisualTween(newAction.UIStroke, "stroke", TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.4})
				end
			end)

			if not success then
				queueNotification("Action Error", "This action ('"..(action.name).."') had an error while running, please report this to the Sirius team at sirius.menu/discord", 4370336704)
				action.enabled = false
				transitionActionIcon(newAction.Icon, action.images[2], actionIconIdleTransparency)
				playActionVisualTween(newAction, "surface", TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.55})
				playActionVisualTween(newAction.UIStroke, "stroke", TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.4})
			end
		end)
	end

	if localPlayer.Character then
		if not localPlayer.Character:FindFirstChildOfClass('Humanoid').UseJumpPower then
			siriusValues.sliders[2].name = "jump height"
			siriusValues.sliders[2].default = 7.2
			siriusValues.sliders[2].values = {0, 120}
		end
	end


	for _, slider in ipairs(siriusValues.sliders) do
		local newSlider = characterPanel.Interactions.Sliders.Template:Clone()
		newSlider.Name = slider.name.." Slider"
		newSlider.Parent = characterPanel.Interactions.Sliders
		newSlider.BackgroundColor3 = slider.color
		newSlider.Progress.BackgroundColor3 = slider.color
		newSlider.UIStroke.Color = slider.color
		newSlider.Information.Text = slider.name
		newSlider.Visible = true

		slider.object = newSlider

		slider.padding = {
			newSlider.Interact.AbsolutePosition.X,
			newSlider.Interact.AbsolutePosition.X + newSlider.Interact.AbsoluteSize.X
		}

		newSlider.MouseEnter:Connect(function()
			if debounce or slider.active then return end
			tweenService:Create(newSlider, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {BackgroundTransparency = 0.85}):Play()
			tweenService:Create(newSlider.UIStroke, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {Transparency = 0.6}):Play()
			tweenService:Create(newSlider.Information, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {TextTransparency = 0.2}):Play()
		end)

		newSlider.MouseLeave:Connect(function()
			if debounce or slider.active then return end
			tweenService:Create(newSlider, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {BackgroundTransparency = 0.8}):Play()
			tweenService:Create(newSlider.UIStroke, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {Transparency = 0.5}):Play()
			tweenService:Create(newSlider.Information, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {TextTransparency = 0.3}):Play()
		end)

		newSlider.Interact.InputBegan:Connect(function(input, processed)
			if processed then return end
			if input.UserInputType ~= Enum.UserInputType.MouseButton1 and input.UserInputType ~= Enum.UserInputType.Touch then return end
			if debounce or not checkSirius() then return end

			refreshSliderPadding(slider)
			slider.active = true
			slider.activeInput = input
			slider.pointerX = siriusValues.getSliderPointerX(input)
			updateSlider(slider, nil, nil, slider.pointerX)

			tweenService:Create(slider.object, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {BackgroundTransparency = 0.9}):Play()
			tweenService:Create(slider.object.UIStroke, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {Transparency = 1}):Play()
			tweenService:Create(slider.object.Information, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {TextTransparency = 0.05}):Play()
		end)

			updateSlider(slider, slider.default)
		end

		task.defer(function()
			characterRuntime.refreshCharacterPanelScroll()
			updateSliderPadding()
		end)
	end

local function getAdaptiveHighPingThreshold()
	local adaptiveBaselinePings = siriusValues.pingProfile.adaptiveBaselinePings

	if #adaptiveBaselinePings == 0 then
		return siriusValues.pingProfile.adaptiveHighPingThreshold
	end

	table.sort(adaptiveBaselinePings)
	local median
	if #adaptiveBaselinePings % 2 == 0 then
		median = (adaptiveBaselinePings[#adaptiveBaselinePings/2] + adaptiveBaselinePings[#adaptiveBaselinePings/2 + 1]) / 2
	else
		median = adaptiveBaselinePings[math.ceil(#adaptiveBaselinePings/2)]
	end

	return median * siriusValues.pingProfile.spikeThreshold
end

local function checkHighPing()
	local recentPings = siriusValues.pingProfile.recentPings
	local adaptiveBaselinePings = siriusValues.pingProfile.adaptiveBaselinePings

	local currentPing = getPing()
	table.insert(recentPings, currentPing)

	if #recentPings > siriusValues.pingProfile.maxSamples then
		table.remove(recentPings, 1)
	end

	if #adaptiveBaselinePings < siriusValues.pingProfile.adaptiveBaselineSamples then
		if currentPing >= 350 then currentPing = 300 end

		table.insert(adaptiveBaselinePings, currentPing)

		return false
	end

	local averagePing = 0
	for _, ping in ipairs(recentPings) do
		averagePing = averagePing + ping
	end
	averagePing = averagePing / #recentPings

	if averagePing > getAdaptiveHighPingThreshold() then
		return true
	end

	return false
end

local function checkTools()
	task.wait(0.03)
	if localPlayer.Backpack and localPlayer.Character then
		if localPlayer.Backpack:FindFirstChildOfClass('Tool') or localPlayer.Character:FindFirstChildOfClass('Tool') then
			return true
		end
	else
		return false
	end
end

local function closePanel(panelName, openingOther)
	debounce = true

	local button = smartBar.Buttons:FindFirstChild(panelName)
	local panel = UI:FindFirstChild(panelName)

	if not isPanel(panelName) then return end
	if not (panel and button) then return end

	local panelSize = UDim2.new(0, 581, 0, 246)

	if panel.Name == "Character" and openingOther then
		characterRuntime.resetCharacterPanelHeaderScroll(true)
	end

	if not openingOther then
		if panel.Name == "Character" then -- Character Panel Animation
			characterRuntime.resetCharacterPanelHeaderScroll(true)

			tweenService:Create(characterPanel.Interactions.PropertiesTitle, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()

			for _, slider in ipairs(characterPanel.Interactions.Sliders:GetChildren()) do
				if slider.ClassName == "Frame" then 
					tweenService:Create(slider, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
					tweenService:Create(slider.Progress, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
					tweenService:Create(slider.UIStroke, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {Transparency = 1}):Play()
					tweenService:Create(slider.Shadow, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
					tweenService:Create(slider.Information, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play() -- tween the text after
				end
			end

			tweenService:Create(characterPanel.Interactions.Reset, TweenInfo.new(0.25, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
			tweenService:Create(characterPanel.Interactions.ActionsTitle, TweenInfo.new(0.25, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()

			for _, gridButton in ipairs(characterPanel.Interactions.Grid:GetChildren()) do
				if gridButton.ClassName == "Frame" then 
					tweenService:Create(gridButton, TweenInfo.new(0.21, Enum.EasingStyle.Exponential), {BackgroundTransparency = 1}):Play()
					tweenService:Create(gridButton.UIStroke, TweenInfo.new(0.1, Enum.EasingStyle.Exponential), {Transparency = 1}):Play()
					tweenService:Create(gridButton.Icon, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
					tweenService:Create(gridButton.Shadow, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
				end
			end

			tweenService:Create(characterPanel.Interactions.Serverhop, TweenInfo.new(.15,Enum.EasingStyle.Quint),  {BackgroundTransparency = 1}):Play()
			tweenService:Create(characterPanel.Interactions.Serverhop.Title, TweenInfo.new(.15,Enum.EasingStyle.Quint),  {TextTransparency = 1}):Play()
			tweenService:Create(characterPanel.Interactions.Serverhop.UIStroke, TweenInfo.new(.15,Enum.EasingStyle.Quint),  {Transparency = 1}):Play()

			tweenService:Create(characterPanel.Interactions.Rejoin, TweenInfo.new(.15,Enum.EasingStyle.Quint),  {BackgroundTransparency = 1}):Play()
			tweenService:Create(characterPanel.Interactions.Rejoin.Title, TweenInfo.new(.15,Enum.EasingStyle.Quint),  {TextTransparency = 1}):Play()
			tweenService:Create(characterPanel.Interactions.Rejoin.UIStroke, TweenInfo.new(.15,Enum.EasingStyle.Quint),  {Transparency = 1}):Play()

		elseif panel.Name == "Scripts" then -- Scripts Panel Animation

			for _, scriptButton in ipairs(scriptsPanel.Interactions.Selection:GetChildren()) do
				if scriptButton.ClassName == "Frame" then
					tweenService:Create(scriptButton, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
					if scriptButton:FindFirstChild('Icon') then tweenService:Create(scriptButton.Icon, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play() end
					tweenService:Create(scriptButton.Title, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
					if scriptButton:FindFirstChild('Subtitle') then	tweenService:Create(scriptButton.Subtitle, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play() end
					tweenService:Create(scriptButton.UIStroke, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {Transparency = 1}):Play()
				end
			end

		elseif panel.Name == "Playerlist" then -- Playerlist Panel Animation

			for _, playerIns in ipairs(playerlistPanel.Interactions.List:GetDescendants()) do
				if playerIns.ClassName == "Frame" then
					tweenService:Create(playerIns, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
				elseif playerIns.ClassName == "TextLabel" or playerIns.ClassName == "TextButton" then
					if playerIns.Name == "DisplayName" then
						tweenService:Create(playerIns, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
					else
						tweenService:Create(playerIns, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
					end
				elseif playerIns.ClassName == "ImageLabel" or playerIns.ClassName == "ImageButton" then
					tweenService:Create(playerIns, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
					if playerIns.Name == "Avatar" then tweenService:Create(playerIns, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play() end
				elseif playerIns.ClassName == "UIStroke" then
					tweenService:Create(playerIns, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {Transparency = 1}):Play()
				end
			end

			tweenService:Create(playerlistPanel.Interactions.SearchFrame, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
			tweenService:Create(playerlistPanel.Interactions.SearchFrame.Icon, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
			tweenService:Create(playerlistPanel.Interactions.SearchFrame.SearchBox, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
			tweenService:Create(playerlistPanel.Interactions.SearchFrame.UIStroke, TweenInfo.new(0.15, Enum.EasingStyle.Quint), {Transparency = 1}):Play()
			tweenService:Create(playerlistPanel.Interactions.List, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {ScrollBarImageTransparency = 1}):Play()

		end

		tweenService:Create(panel.Icon, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
		tweenService:Create(panel.Title, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
		tweenService:Create(panel.UIStroke, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {Transparency = 1}):Play()
		tweenService:Create(panel.Shadow, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
		task.wait(0.03)

		tweenService:Create(panel, TweenInfo.new(0.75, Enum.EasingStyle.Exponential, Enum.EasingDirection.InOut), {BackgroundTransparency = 1}):Play()
		tweenService:Create(panel, TweenInfo.new(1.1, Enum.EasingStyle.Exponential, Enum.EasingDirection.Out), {Size = button.Size}):Play()
		tweenService:Create(panel, TweenInfo.new(0.65, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {Position = siriusValues.buttonPositions[panelName]}):Play()
		tweenService:Create(toggle, TweenInfo.new(0.6, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {Position = UDim2.new(0.5, 0, 1, -85)}):Play()
	end

	-- Animate interactive elements
	if openingOther then
		tweenService:Create(panel, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Position = UDim2.new(0.5, 350, 1, -90)}):Play()
		wipeTransparency(panel, 1, true, true, 0.3)
	end

	task.wait(0.5)

	panel.Size = panelSize
	if panel.Name == "Character" then
		characterRuntime.resetCharacterPanelHeaderScroll()
	end
	panel.Visible = false

	debounce = false
end

local function openPanel(panelName)
	if debounce then return end
	if siriusValues.betaControl and siriusValues.betaControl.disabled then
		siriusValues.showBetaControlOverlay("SIRIUS X Disabled", siriusValues.betaControl.message ~= "" and siriusValues.betaControl.message or "This beta build is temporarily disabled.", true)
		return
	end
	debounce = true

	local button = smartBar.Buttons:FindFirstChild(panelName)
	local panel = UI:FindFirstChild(panelName)

	if not isPanel(panelName) then return end
	if not (panel and button) then return end

	for _, otherPanel in ipairs(UI:GetChildren()) do
		if smartBar.Buttons:FindFirstChild(otherPanel.Name) then
			if isPanel(otherPanel.Name) and otherPanel.Visible then
				task.spawn(closePanel, otherPanel.Name, true)
				task.wait()
			end
		end
	end

	local panelSize = UDim2.new(0, 581, 0, 246)

	panel.Size = button.Size
	panel.Position = siriusValues.buttonPositions[panelName]

	wipeTransparency(panel, 1, true)

	panel.Visible = true
	if panel.Name == "Character" then
		characterRuntime.resetCharacterPanelHeaderScroll()
	end

	tweenService:Create(toggle, TweenInfo.new(0.65, Enum.EasingStyle.Quint), {Position = UDim2.new(0.5, 0, 1, -(panelSize.Y.Offset + 95))}):Play()

	tweenService:Create(panel, TweenInfo.new(0.1, Enum.EasingStyle.Quint), {BackgroundTransparency = 0}):Play()
	tweenService:Create(panel, TweenInfo.new(0.8, Enum.EasingStyle.Exponential), {Size = panelSize}):Play()
	tweenService:Create(panel, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Position = UDim2.new(0.5, 0, 1, -90)}):Play()
	task.wait(0.1)
	tweenService:Create(panel.Shadow, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {ImageTransparency = 0.7}):Play()
	tweenService:Create(panel.Icon, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {ImageTransparency = 0}):Play()
	task.wait(0.05)
	tweenService:Create(panel.Title, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
	tweenService:Create(panel.UIStroke, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Transparency = 0.95}):Play()
	task.wait(0.05)

		-- Animate interactive elements
		if panel.Name == "Character" then -- Character Panel Animation

			characterRuntime.ensureCharacterPanelScroll()
				characterRuntime.refreshCharacterPanelScroll()
				if characterPanel.Interactions:IsA("ScrollingFrame") then
					characterPanel.Interactions.CanvasPosition = Vector2.new(0, 0)
					local headerOpenToken = characterRuntime.characterScrollHeaderToken
					task.defer(characterRuntime.refreshCharacterPanelScrollFade)
					task.delay(0.42, function()
						if characterRuntime.characterScrollHeaderToken ~= headerOpenToken then return end
						if not characterPanel.Visible then return end
						if not characterPanel:FindFirstChild("Interactions") or not characterPanel.Interactions:IsA("ScrollingFrame") then return end

						characterRuntime.activateCharacterPanelScrollHeader(true)
						characterRuntime.refreshCharacterPanelScrollFade()
					end)
				end

				tweenService:Create(characterPanel.Interactions.PropertiesTitle, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {TextTransparency = 0.65}):Play()

		local sliderInfo = {}
		for _, slider in ipairs(characterPanel.Interactions.Sliders:GetChildren()) do
			if slider.ClassName == "Frame" then 
				table.insert(sliderInfo, {slider.Name, slider.Progress.Size, slider.Information.Text})
				slider.Progress.Size = UDim2.new(0, 0, 1, 0)
				slider.Progress.BackgroundTransparency = 0

				tweenService:Create(slider, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.8}):Play()
				tweenService:Create(slider.UIStroke, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {Transparency = 0.5}):Play()
				tweenService:Create(slider.Shadow, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {ImageTransparency = 0.6}):Play()
				tweenService:Create(slider.Information, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {TextTransparency = 0.3}):Play()
			end
		end

		for _, sliderV in pairs(sliderInfo) do
			if characterPanel.Interactions.Sliders:FindFirstChild(sliderV[1]) then
				local slider = characterPanel.Interactions.Sliders:FindFirstChild(sliderV[1])
				local tweenValue = Instance.new("IntValue", UI)
				local tweenTo
				local name

				for _, sliderFound in ipairs(siriusValues.sliders) do
					if sliderFound.name.." Slider" == slider.Name then
						tweenTo = sliderFound.value
						name = sliderFound.name
						break
					end
				end

				tweenService:Create(slider.Progress, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {Size = sliderV[2]}):Play()

				local function animateNumber(n)
					tweenService:Create(tweenValue, TweenInfo.new(0.35, Enum.EasingStyle.Exponential), {Value = n}):Play()
					task.delay(0.4, tweenValue.Destroy, tweenValue)
				end

				tweenValue:GetPropertyChangedSignal("Value"):Connect(function()
					slider.Information.Text = tostring(tweenValue.Value).." "..name
				end)

				animateNumber(tweenTo)
			end
		end

		tweenService:Create(characterPanel.Interactions.Reset, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {ImageTransparency = 0.7}):Play()
		tweenService:Create(characterPanel.Interactions.ActionsTitle, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {TextTransparency = 0.65}):Play()

		for _, gridButton in ipairs(characterPanel.Interactions.Grid:GetChildren()) do
			if gridButton.ClassName == "Frame" then 
				for _, action in ipairs(siriusValues.actions) do
					if action.name == gridButton.Name then
						if action.enabled then
							applySharpIcon(gridButton.Icon, actionIconActiveTransparency)
							tweenService:Create(gridButton, TweenInfo.new(0.6, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.1}):Play()
							tweenService:Create(gridButton.UIStroke, TweenInfo.new(0.6, Enum.EasingStyle.Quint), {Transparency = 1}):Play()
							tweenService:Create(gridButton.Icon, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {ImageColor3 = actionIconColor, ImageTransparency = actionIconActiveTransparency}):Play()
						else
							applySharpIcon(gridButton.Icon, actionIconIdleTransparency)
							tweenService:Create(gridButton, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {BackgroundTransparency = 0.55}):Play()
							tweenService:Create(gridButton.UIStroke, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {Transparency = 0.4}):Play()
							tweenService:Create(gridButton.Icon, TweenInfo.new(0.25, Enum.EasingStyle.Quint), {ImageColor3 = actionIconColor, ImageTransparency = actionIconIdleTransparency}):Play()
						end
						break
					end
				end

				tweenService:Create(gridButton.Shadow, TweenInfo.new(0.6, Enum.EasingStyle.Quint), {ImageTransparency = 0.6}):Play()
			end
		end

		tweenService:Create(characterPanel.Interactions.Serverhop, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {BackgroundTransparency = 0}):Play()
		tweenService:Create(characterPanel.Interactions.Serverhop.Title, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 0.5}):Play()
		tweenService:Create(characterPanel.Interactions.Serverhop.UIStroke, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {Transparency = 0}):Play()

			tweenService:Create(characterPanel.Interactions.Rejoin, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {BackgroundTransparency = 0}):Play()
			tweenService:Create(characterPanel.Interactions.Rejoin.Title, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 0.5}):Play()
			tweenService:Create(characterPanel.Interactions.Rejoin.UIStroke, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {Transparency = 0}):Play()
			task.delay(0.12, characterRuntime.refreshCharacterPanelScrollFade)
			task.delay(0.42, characterRuntime.refreshCharacterPanelScrollFade)
			task.delay(0.82, function()
				characterRuntime.characterScrollFadeBase = setmetatable({}, {__mode = "k"})
				characterRuntime.refreshCharacterPanelScrollFade()
			end)

		elseif panel.Name == "Scripts" then -- Scripts Panel Animation

		for _, scriptButton in ipairs(scriptsPanel.Interactions.Selection:GetChildren()) do
			if scriptButton.ClassName == "Frame" then
				tweenService:Create(scriptButton, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {BackgroundTransparency = 0}):Play()
				if scriptButton:FindFirstChild('Icon') then tweenService:Create(scriptButton.Icon, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {ImageTransparency = 0}):Play() end
				tweenService:Create(scriptButton.Title, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
				if scriptButton:FindFirstChild('Subtitle') then	tweenService:Create(scriptButton.Subtitle, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {TextTransparency = 0.3}):Play() end
				tweenService:Create(scriptButton.UIStroke, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Transparency = 0.2}):Play()
			end
		end

	elseif panel.Name == "Playerlist" then -- Playerlist Panel Animation

		for _, playerIns in ipairs(playerlistPanel.Interactions.List:GetDescendants()) do
			local inActionSlot = playerIns.Name == "PlayerInteractions" or playerIns.Name == "Select"
			if not inActionSlot and playerIns.Parent then
				inActionSlot = playerIns.Parent.Name == "PlayerInteractions"
			end

			if playerIns.Name ~= "Interact" and playerIns.Name ~= "Role" and not inActionSlot then 
				if playerIns.ClassName == "Frame" then
					tweenService:Create(playerIns, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {BackgroundTransparency = 0}):Play()
				elseif playerIns.ClassName == "TextLabel" or playerIns.ClassName == "TextButton" then
					tweenService:Create(playerIns, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
				elseif playerIns.ClassName == "ImageLabel" or playerIns.ClassName == "ImageButton" then
					tweenService:Create(playerIns, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {ImageTransparency = 0}):Play()
					if playerIns.Name == "Avatar" then tweenService:Create(playerIns, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {BackgroundTransparency = 0}):Play() end
				elseif playerIns.ClassName == "UIStroke" then
					tweenService:Create(playerIns, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Transparency = 0}):Play()
				end
			end
		end

		tweenService:Create(playerlistPanel.Interactions.SearchFrame, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {BackgroundTransparency = 0}):Play()
		tweenService:Create(playerlistPanel.Interactions.SearchFrame.Icon, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {ImageTransparency = 0}):Play()
		task.wait(0.01)
		tweenService:Create(playerlistPanel.Interactions.SearchFrame.SearchBox, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
		tweenService:Create(playerlistPanel.Interactions.SearchFrame.UIStroke, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Transparency = 0.2}):Play()
		task.wait(0.05)
		tweenService:Create(playerlistPanel.Interactions.List, TweenInfo.new(0.35, Enum.EasingStyle.Quint), {ScrollBarImageTransparency = 1}):Play()

	end

	task.wait(0.45)
	debounce = false
end

local cachedServerHistory

local function getServerHistoryPath()
	return siriusValues.siriusFolder.."/"..siriusValues.serverHistoryFile
end

local function loadServerHistory()
	if cachedServerHistory then return cachedServerHistory end

	cachedServerHistory = {}

	if isfile and isfile(getServerHistoryPath()) then
		local success, history = pcall(function()
			return httpService:JSONDecode(readfile(getServerHistoryPath()))
		end)

		if success and type(history) == "table" then
			cachedServerHistory = history
		end
	end

	return cachedServerHistory
end

local function saveServerHistory()
	if not writefile then return end

	pcall(checkFolder)
	pcall(function()
		writefile(getServerHistoryPath(), httpService:JSONEncode(loadServerHistory()))
	end)
end

local function rememberServer(serverId, label, playing, maxPlayers)
	if not serverId or serverId == "" then return end

	local history = loadServerHistory()

	for index = #history, 1, -1 do
		if history[index].id == serverId then
			table.remove(history, index)
		end
	end

	table.insert(history, 1, {
		id = serverId,
		placeId = placeId,
		label = label or "Previous server",
		playing = playing or #players:GetPlayers(),
		maxPlayers = maxPlayers or players.MaxPlayers,
		time = os.date("%H:%M")
	})

	while #history > 8 do
		table.remove(history)
	end

	saveServerHistory()
end

local function teleportToServer(serverId, label, playing, maxPlayers)
	if not serverId or serverId == "" then
		return queueNotification("Server Unavailable", "Sirius could not find a joinable server id for that action.", 4370317928)
	end

	if serverId ~= jobId then
		rememberServer(jobId, "Previous session", #players:GetPlayers(), players.MaxPlayers)
	end

	queueNotification("Teleporting", label or "Joining selected server, give us a moment.", 4335479121)
	task.wait(0.2)
	teleportService:TeleportToPlaceInstance(placeId, serverId, localPlayer)
end

local function teleportFreshServer()
	rememberServer(jobId, "Previous session", #players:GetPlayers(), players.MaxPlayers)
	queueNotification("Creating Fresh Server", "Sirius is asking Roblox for a fresh session. Roblox may still route you to a low-population server.", 4335479121)
	task.wait(0.2)
	teleportService:Teleport(placeId, localPlayer)
end

local function requestJson(url, method, body)
	method = method or "GET"

	local success, responseBody = pcall(function()
		if method == "POST" then
			local encodedBody = httpService:JSONEncode(body or {})

			if httpRequest then
				local response = httpRequest({
					Url = url,
					Method = "POST",
					Headers = {
						["Content-Type"] = "application/json"
					},
					Body = encodedBody
				})

				return response.Body
			end

			if game.HttpPostAsync then
				return game:HttpPostAsync(url, encodedBody, Enum.HttpContentType.ApplicationJson)
			elseif game.HttpPost then
				return game:HttpPost(url, encodedBody, Enum.HttpContentType.ApplicationJson)
			end

			return httpService:PostAsync(url, encodedBody, Enum.HttpContentType.ApplicationJson)
		end

		return game:HttpGetAsync(url)
	end)

	if not success then
		return nil, responseBody
	end

	local decodeSuccess, decoded = pcall(function()
		return httpService:JSONDecode(responseBody)
	end)

	if not decodeSuccess then
		return nil, decoded
	end

	return decoded
end

local function fetchServerPage(sortOrder, cursor)
	local url = "https://games.roblox.com/v1/games/"..placeId.."/servers/Public?sortOrder="..sortOrder.."&limit=100"

	if cursor and cursor ~= "" then
		url = url.."&cursor="..httpService:UrlEncode(cursor)
	end

	return requestJson(url)
end

local function getUserIdFromUsername(username)
	local response = requestJson("https://users.roblox.com/v1/usernames/users", "POST", {
		usernames = {username},
		excludeBannedUsers = false
	})

	if response and response.data and response.data[1] then
		return response.data[1].id, response.data[1].name
	end
end

local function getUserHeadshot(userId)
	local response = requestJson("https://thumbnails.roblox.com/v1/users/avatar-headshot?userIds="..userId.."&size=48x48&format=Png&isCircular=false")

	if response and response.data and response.data[1] then
		return response.data[1].imageUrl
	end
end

local function rejoin()
	queueNotification("Rejoining Session", "We're queueing a rejoin to this session, give us a moment.", 4400696294)

	if #players:GetPlayers() <= 1 then
		task.wait()
		teleportService:Teleport(placeId, localPlayer)
	else
		teleportService:TeleportToPlaceInstance(placeId, jobId, localPlayer)
	end
end

local function serverhop()
	local highestPlayers = 0
	local selectedServer

	for _, v in ipairs(httpService:JSONDecode(game:HttpGetAsync("https://games.roblox.com/v1/games/" .. placeId .. "/servers/Public?sortOrder=Asc&limit=100")).data) do
		if type(v) == "table" and v.maxPlayers > v.playing and v.id ~= jobId then
			if v.playing > highestPlayers then
				highestPlayers = v.playing
				selectedServer = v
			end
		end
	end

	if selectedServer then
		teleportToServer(selectedServer.id, "Serverhop is moving you to a new session.", selectedServer.playing, selectedServer.maxPlayers)
	else
		return queueNotification("No Servers Found", "We couldn't find another server, this may be the only server.", 4370317928)
	end

end

local function createServersBrowserLegacy(newList)
	local serverResults = {}
	local sortAscending = true
	local activeScanId = 0
	local foundPlayerServer

	for _, obj in ipairs(newList:GetChildren()) do
		if obj.Name == "Placeholder" then
			obj.Visible = false
			obj.LayoutOrder = 999
			obj.Size = UDim2.new(0, 0, 0, 0)
		elseif obj.Name ~= "UIListLayout" then
			obj:Destroy()
		end
	end

	local container = Instance.new("Frame")
	container.Name = "ServerNavigator"
	container.Parent = newList
	container.LayoutOrder = 20
	container.Size = UDim2.new(0, 558, 0, 640)
	container.BackgroundColor3 = Color3.fromRGB(18, 14, 22)
	container.BackgroundTransparency = 0.08
	container.Visible = true

	local containerStroke = Instance.new("UIStroke")
	containerStroke.Color = Color3.fromRGB(170, 78, 190)
	containerStroke.Transparency = 0.42
	containerStroke.Parent = container

	local containerCorner = Instance.new("UICorner")
	containerCorner.CornerRadius = UDim.new(0, 8)
	containerCorner.Parent = container

	if newList:IsA("ScrollingFrame") then
		newList.CanvasSize = UDim2.new(0, 0, 0, 1120)
	end

	local function createActionRow(title, description, layoutOrder)
		local row = settingsPanel.SettingLists.Template.SwitchTemplate:Clone()
		row.Name = title
		row.LayoutOrder = layoutOrder
		row.Parent = newList
		row.Visible = true
		row.Title.Text = title
		row.Description.Visible = true
		row.Description.TextWrapped = true
		row.Description.Text = description
		row.Description.Size = UDim2.new(0, 333, 0, row.Description.TextBounds.Y + 10)
		row.Size = UDim2.new(0, 558, 0, math.max(54, row.Description.TextBounds.Y + 44))

		if row:FindFirstChild("Switch") then
			row.Switch.Visible = false
		end

		return row
	end

	local scanRow = createActionRow("Scan Now", "Search public servers for this game and show joinable sessions.", 1)
	local emptyRow = createActionRow("Join 0 Player Server", "Find a listed empty server first, then fall back to the fresh-server option if none appear.", 2)
	local freshRow = createActionRow("Create Fresh Server", "Ask Roblox to place you into a new low-population session.", 3)
	local searchRow = settingsPanel.SettingLists.Template.InputTemplate:Clone()
	searchRow.Name = "Search Player"
	searchRow.LayoutOrder = 4
	searchRow.Parent = newList
	searchRow.Visible = true
	searchRow.Title.Text = "Search Player Username"
	searchRow.Description.Visible = true
	searchRow.Description.TextWrapped = true
	searchRow.Description.Text = "Type a username and press Enter or use Search Now below to find their public server."
	searchRow.Description.Size = UDim2.new(0, 333, 0, searchRow.Description.TextBounds.Y + 10)
	searchRow.Size = UDim2.new(0, 558, 0, math.max(68, searchRow.Description.TextBounds.Y + 44))
	searchRow.InputFrame.InputBox.Text = ""
	searchRow.InputFrame.InputBox.PlaceholderText = "username"
	searchRow.InputFrame.InputBox.ClearTextOnFocus = false
	searchRow.InputFrame.Size = UDim2.new(0, 120, 0, 30)
	local searchNowRow = createActionRow("Search Now", "Scan public server avatar tokens for the username above.", 5)
	local joinFoundRow = createActionRow("Join Found Player", "Join the server found by the latest player search.", 6)

	local function createLabel(parent, text, position, size, textSize, transparency)
		local label = Instance.new("TextLabel")
		label.BackgroundTransparency = 1
		label.Font = Enum.Font.GothamMedium
		label.Text = text
		label.TextColor3 = Color3.fromRGB(235, 235, 235)
		label.TextSize = textSize or 14
		label.TextTransparency = transparency or 0.25
		label.TextXAlignment = Enum.TextXAlignment.Left
		label.TextYAlignment = Enum.TextYAlignment.Center
		label.Position = position
		label.Size = size
		label.Parent = parent
		return label
	end

	local function createButton(parent, text, position, size, color)
		local button = Instance.new("TextButton")
		button.AutoButtonColor = false
		button.BackgroundColor3 = color or Color3.fromRGB(36, 36, 36)
		button.BackgroundTransparency = 0.18
		button.Font = Enum.Font.GothamBold
		button.Text = string.upper(text)
		button.TextColor3 = Color3.fromRGB(245, 245, 245)
		button.TextSize = 11
		button.TextTransparency = 0.12
		button.Position = position
		button.Size = size
		button.Parent = parent

		local corner = Instance.new("UICorner")
		corner.CornerRadius = UDim.new(0, 8)
		corner.Parent = button

		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(110, 110, 110)
		stroke.Transparency = 0.62
		stroke.Parent = button

		button.MouseEnter:Connect(function()
			tweenService:Create(button, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.05}):Play()
			tweenService:Create(stroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.35}):Play()
		end)

		button.MouseLeave:Connect(function()
			tweenService:Create(button, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.18}):Play()
			tweenService:Create(stroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.62}):Play()
		end)

		return button
	end

	local function createInput(parent, placeholder, position, size)
		local box = Instance.new("TextBox")
		box.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
		box.BackgroundTransparency = 0.12
		box.ClearTextOnFocus = false
		box.Font = Enum.Font.GothamMedium
		box.PlaceholderText = placeholder
		box.PlaceholderColor3 = Color3.fromRGB(135, 135, 135)
		box.Text = ""
		box.TextColor3 = Color3.fromRGB(240, 240, 240)
		box.TextSize = 12
		box.TextXAlignment = Enum.TextXAlignment.Left
		box.Position = position
		box.Size = size
		box.Parent = parent

		local padding = Instance.new("UIPadding")
		padding.PaddingLeft = UDim.new(0, 12)
		padding.PaddingRight = UDim.new(0, 12)
		padding.Parent = box

		local corner = Instance.new("UICorner")
		corner.CornerRadius = UDim.new(0, 8)
		corner.Parent = box

		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(90, 115, 160)
		stroke.Transparency = 0.62
		stroke.Parent = box

		box.Focused:Connect(function()
			tweenService:Create(stroke, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.25}):Play()
		end)

		box.FocusLost:Connect(function()
			tweenService:Create(stroke, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.62}):Play()
		end)

		return box
	end

	local header = createLabel(container, "SEARCH & SCAN SERVERS", UDim2.new(0, 16, 0, 12), UDim2.new(0, 270, 0, 20), 13, 0.12)
	header.Font = Enum.Font.GothamBold

	local statusLabel = createLabel(container, "Choose an action first: scan public servers, search a username, join an empty server, or create a fresh session.", UDim2.new(0, 16, 0, 35), UDim2.new(0, 520, 0, 36), 12, 0.25)
	statusLabel.TextWrapped = true

	local scanButton = createButton(container, "Scan Now", UDim2.new(0, 16, 0, 82), UDim2.new(0, 126, 0, 34), Color3.fromRGB(115, 50, 138))
	local sortButton = createButton(container, "Sort: Low", UDim2.new(0, 150, 0, 82), UDim2.new(0, 102, 0, 34), Color3.fromRGB(43, 43, 43))
	local emptyButton = createButton(container, "Join 0 Player", UDim2.new(0, 260, 0, 82), UDim2.new(0, 126, 0, 34), Color3.fromRGB(36, 110, 78))
	local freshButton = createButton(container, "Fresh Server", UDim2.new(0, 394, 0, 82), UDim2.new(0, 126, 0, 34), Color3.fromRGB(110, 72, 36))

	local playerSearch = createInput(container, "username to find across public servers", UDim2.new(0, 16, 0, 128), UDim2.new(0, 334, 0, 34))
	local searchButton = createButton(container, "Search Now", UDim2.new(0, 358, 0, 128), UDim2.new(0, 96, 0, 34), Color3.fromRGB(128, 58, 150))
	local joinFoundButton = createButton(container, "Join Found", UDim2.new(0, 462, 0, 128), UDim2.new(0, 72, 0, 34), Color3.fromRGB(48, 96, 116))
	joinFoundButton.TextTransparency = 0.55

	local resultsTitle = createLabel(container, "SERVERS", UDim2.new(0, 16, 0, 178), UDim2.new(0, 180, 0, 20), 12, 0.22)
	resultsTitle.Font = Enum.Font.GothamBold

	local serverRows = Instance.new("Frame")
	serverRows.Name = "ServerRows"
	serverRows.BackgroundTransparency = 1
	serverRows.Position = UDim2.new(0, 16, 0, 204)
	serverRows.Size = UDim2.new(0, 526, 0, 80)
	serverRows.Parent = container

	local historyTitle = createLabel(container, "RECENT SERVERS", UDim2.new(0, 16, 0, 300), UDim2.new(0, 200, 0, 20), 12, 0.22)
	historyTitle.Font = Enum.Font.GothamBold

	local historyRows = Instance.new("Frame")
	historyRows.Name = "HistoryRows"
	historyRows.BackgroundTransparency = 1
	historyRows.Position = UDim2.new(0, 16, 0, 326)
	historyRows.Size = UDim2.new(0, 526, 0, 80)
	historyRows.Parent = container

	local function setStatus(text)
		statusLabel.Text = text
	end

	local function updateCanvas(height)
		container.Size = UDim2.new(0, 558, 0, height)
		if newList:IsA("ScrollingFrame") then
			newList.CanvasSize = UDim2.new(0, 0, 0, height + 430)
		end
	end

	local function clearRows(parent)
		for _, child in ipairs(parent:GetChildren()) do
			child:Destroy()
		end
	end

	local function styleRow(row)
		local corner = Instance.new("UICorner")
		corner.CornerRadius = UDim.new(0, 8)
		corner.Parent = row

		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(80, 80, 80)
		stroke.Transparency = 0.64
		stroke.Parent = row
	end

	local function createServerRow(parent, y, serverData, index, isHistory)
		local row = Instance.new("Frame")
		row.Name = serverData.id or "Server"
		row.BackgroundColor3 = isHistory and Color3.fromRGB(21, 21, 21) or Color3.fromRGB(18, 18, 18)
		row.BackgroundTransparency = 0.18
		row.Position = UDim2.new(0, 0, 0, y)
		row.Size = UDim2.new(0, 526, 0, 44)
		row.Parent = parent
		styleRow(row)

		local isCurrent = serverData.id == jobId
		local playingText = tostring(serverData.playing or "?").."/"..tostring(serverData.maxPlayers or "?")
		local title = isHistory and (serverData.label or "Previous server") or ("Server "..index)
		if isCurrent then title = title.."  CURRENT" end

		local titleLabel = createLabel(row, title, UDim2.new(0, 12, 0, 5), UDim2.new(0, 250, 0, 18), 12, 0.12)
		titleLabel.Font = Enum.Font.GothamBold
		createLabel(row, playingText.." players  id "..string.sub(tostring(serverData.id or ""), 1, 10), UDim2.new(0, 12, 0, 22), UDim2.new(0, 300, 0, 16), 11, 0.42)

		if originalSetClipboard then
			local copyButton = createButton(row, "Copy", UDim2.new(1, -144, 0, 8), UDim2.new(0, 54, 0, 28), Color3.fromRGB(44, 44, 44))
			copyButton.MouseButton1Click:Connect(function()
				originalSetClipboard(tostring(serverData.id or ""))
				queueNotification("Server ID Copied", "The server JobId is now on your clipboard.", 4400696294)
			end)
		end

		local joinButton = createButton(row, isCurrent and "Here" or "Join", UDim2.new(1, -82, 0, 8), UDim2.new(0, 66, 0, 28), isCurrent and Color3.fromRGB(42, 42, 42) or Color3.fromRGB(42, 90, 125))
		joinButton.MouseButton1Click:Connect(function()
			if isCurrent then
				return setStatus("You are already in this server.")
			end

			teleportToServer(serverData.id, "Joining server with "..playingText.." players.", serverData.playing, serverData.maxPlayers)
		end)
	end

	local function renderHistory()
		clearRows(historyRows)
		local history = loadServerHistory()

		if #history == 0 then
			createLabel(historyRows, "No previous server hops yet.", UDim2.new(0, 2, 0, 0), UDim2.new(0, 400, 0, 32), 12, 0.45)
			historyRows.Size = UDim2.new(0, 526, 0, 36)
			return 36
		end

		local y = 0
		for index, entry in ipairs(history) do
			if index > 5 then break end
			createServerRow(historyRows, y, entry, index, true)
			y = y + 50
		end

		historyRows.Size = UDim2.new(0, 526, 0, y)
		return y
	end

	local function sortServers()
		table.sort(serverResults, function(a, b)
			local aPlayers = tonumber(a.playing) or 0
			local bPlayers = tonumber(b.playing) or 0

			if sortAscending then
				return aPlayers < bPlayers
			end

			return aPlayers > bPlayers
		end)
	end

	local function renderServers()
		clearRows(serverRows)
		sortServers()

		local y = 0
		if #serverResults == 0 then
			createLabel(serverRows, "No server results yet. Run a scan or search for a player.", UDim2.new(0, 2, 0, 0), UDim2.new(0, 430, 0, 34), 12, 0.45)
			y = 40
		else
			for index, serverData in ipairs(serverResults) do
				if index > 60 then break end
				createServerRow(serverRows, y, serverData, index, false)
				y = y + 50
			end
		end

		serverRows.Size = UDim2.new(0, 526, 0, y)
		historyTitle.Position = UDim2.new(0, 16, 0, 214 + y)
		historyRows.Position = UDim2.new(0, 16, 0, 240 + y)
		local historyHeight = renderHistory()
		updateCanvas(260 + y + historyHeight)
	end

	local function scanServers(maxPages)
		activeScanId = activeScanId + 1
		local scanId = activeScanId
		serverResults = {}
		foundPlayerServer = nil
		joinFoundButton.TextTransparency = 0.55
		renderServers()

		task.spawn(function()
			local cursor
			local page = 0

			while page < maxPages do
				page = page + 1
				if activeScanId ~= scanId then return end
				setStatus("Scanning servers page "..page.."...")

				local response, err = fetchServerPage(sortAscending and "Asc" or "Desc", cursor)
				if not response then
					setStatus("Server scan failed: "..tostring(err))
					return
				end

				for _, serverData in ipairs(response.data or {}) do
					if type(serverData) == "table" and serverData.id and serverData.id ~= jobId then
						table.insert(serverResults, serverData)
					end
				end

				renderServers()
				cursor = response.nextPageCursor

				if not cursor or cursor == "" then break end
				task.wait()
			end

			setStatus("Scan complete. Found "..#serverResults.." public servers for this experience.")
		end)
	end

	local function joinEmptyServer()
		activeScanId = activeScanId + 1
		local scanId = activeScanId

		task.spawn(function()
			setStatus("Searching for a listed 0-player server...")

			for _, serverData in ipairs(serverResults) do
				if serverData.playing == 0 and serverData.id ~= jobId then
					return teleportToServer(serverData.id, "Joining a listed 0-player server.", serverData.playing, serverData.maxPlayers)
				end
			end

			local cursor
			for page = 1, 30 do
				if activeScanId ~= scanId then return end
				setStatus("Searching empty servers page "..page.."...")

				local response = fetchServerPage("Asc", cursor)
				if not response then break end

				for _, serverData in ipairs(response.data or {}) do
					if type(serverData) == "table" and serverData.id ~= jobId and serverData.playing == 0 then
						return teleportToServer(serverData.id, "Joining a listed 0-player server.", serverData.playing, serverData.maxPlayers)
					end
				end

				cursor = response.nextPageCursor
				if not cursor or cursor == "" then break end
				task.wait()
			end

			setStatus("No listed 0-player server found. Use Fresh Server to ask Roblox for a new one.")
		end)
	end

	local function searchPlayer(username)
		username = string.gsub(username or "", "^%s*(.-)%s*$", "%1")

		if username == "" then
			return setStatus("Type a username before searching.")
		end

		activeScanId = activeScanId + 1
		local scanId = activeScanId
		foundPlayerServer = nil
		joinFoundButton.TextTransparency = 0.55

		task.spawn(function()
			setStatus("Resolving username "..username.."...")
			local userId, resolvedName = getUserIdFromUsername(username)

			if not userId then
				return setStatus("Could not find that Roblox username.")
			end

			local targetHeadshot = getUserHeadshot(userId)
			if not targetHeadshot then
				return setStatus("Could not load avatar data for "..resolvedName..".")
			end

			local cursor
			local checkedServers = 0
			local batch = {}
			local batchMap = {}

			local function flushBatch()
				if #batch == 0 then return end

				local response = requestJson("https://thumbnails.roblox.com/v1/batch", "POST", batch)
				batch = {}

				if response and response.data then
					for _, thumb in ipairs(response.data) do
						if thumb.imageUrl == targetHeadshot and batchMap[thumb.requestId] then
							return batchMap[thumb.requestId]
						end
					end
				end

				batchMap = {}
			end

			for page = 1, 50 do
				if activeScanId ~= scanId then return end
				setStatus("Searching for "..resolvedName.." on public servers page "..page.."...")

				local response, err = fetchServerPage("Asc", cursor)
				if not response then
					return setStatus("Player search failed: "..tostring(err))
				end

				for _, serverData in ipairs(response.data or {}) do
					checkedServers = checkedServers + 1

					for tokenIndex, token in ipairs(serverData.playerTokens or {}) do
						local requestId = tostring(serverData.id).."|"..tokenIndex
						batchMap[requestId] = serverData
						table.insert(batch, {
							requestId = requestId,
							type = "AvatarHeadShot",
							targetId = 0,
							token = token,
							format = "Png",
							size = "48x48",
							isCircular = false
						})

						if #batch >= 100 then
							local found = flushBatch()
							if found then
								foundPlayerServer = found
								serverResults = {found}
								renderServers()
								joinFoundButton.TextTransparency = 0.08
								setStatus("Found "..resolvedName.." in a public server with "..tostring(found.playing).."/"..tostring(found.maxPlayers).." players.")
								return
							end
						end
					end
				end

				local found = flushBatch()
				if found then
					foundPlayerServer = found
					serverResults = {found}
					renderServers()
					joinFoundButton.TextTransparency = 0.08
					setStatus("Found "..resolvedName.." in a public server with "..tostring(found.playing).."/"..tostring(found.maxPlayers).." players.")
					return
				end

				cursor = response.nextPageCursor
				if not cursor or cursor == "" then break end
				task.wait()
			end

			setStatus("Could not find "..resolvedName.." after checking "..checkedServers.." public servers.")
		end)
	end

	local function getSearchText()
		local topSearchText = searchRow.InputFrame.InputBox.Text
		if topSearchText and topSearchText ~= "" then
			return topSearchText
		end

		return playerSearch.Text
	end

	scanButton.MouseButton1Click:Connect(function()
		scanServers(12)
	end)
	scanRow.Interact.MouseButton1Click:Connect(function()
		scanServers(12)
	end)

	sortButton.MouseButton1Click:Connect(function()
		sortAscending = not sortAscending
		sortButton.Text = sortAscending and "SORT: LOW" or "SORT: HIGH"
		renderServers()
	end)

	emptyButton.MouseButton1Click:Connect(joinEmptyServer)
	emptyRow.Interact.MouseButton1Click:Connect(joinEmptyServer)
	freshButton.MouseButton1Click:Connect(teleportFreshServer)
	freshRow.Interact.MouseButton1Click:Connect(teleportFreshServer)
	searchButton.MouseButton1Click:Connect(function()
		searchPlayer(getSearchText())
	end)
	searchNowRow.Interact.MouseButton1Click:Connect(function()
		searchPlayer(getSearchText())
	end)
	playerSearch.FocusLost:Connect(function(enterPressed)
		if enterPressed then
			searchPlayer(getSearchText())
		end
	end)
	searchRow.InputFrame.InputBox.FocusLost:Connect(function(enterPressed)
		playerSearch.Text = searchRow.InputFrame.InputBox.Text

		if enterPressed then
			searchPlayer(getSearchText())
		end
	end)
	joinFoundButton.MouseButton1Click:Connect(function()
		if foundPlayerServer then
			teleportToServer(foundPlayerServer.id, "Joining the found player's public server.", foundPlayerServer.playing, foundPlayerServer.maxPlayers)
		else
			setStatus("Search for a player first.")
		end
	end)
	joinFoundRow.Interact.MouseButton1Click:Connect(function()
		if foundPlayerServer then
			teleportToServer(foundPlayerServer.id, "Joining the found player's public server.", foundPlayerServer.playing, foundPlayerServer.maxPlayers)
		else
			setStatus("Search for a player first.")
		end
	end)

	renderServers()
end

local function createServersBrowser(newList)
	local serverResults = {}
	local sortAscending = true
	local activeScanId = 0
	local foundPlayerServer
	local dropdowns = {}
	local activeDropdown

	for _, obj in ipairs(newList:GetChildren()) do
		if obj.Name == "Placeholder" then
			obj.Visible = false
			obj.LayoutOrder = 999
			obj.Size = UDim2.new(0, 0, 0, 0)
		elseif obj.Name ~= "UIListLayout" then
			obj:Destroy()
		end
	end

	local listLayout = newList:FindFirstChildOfClass("UIListLayout")

	if not listLayout then
		listLayout = Instance.new("UIListLayout")
		listLayout.Parent = newList
	end

	listLayout.SortOrder = Enum.SortOrder.LayoutOrder
	listLayout.FillDirection = Enum.FillDirection.Vertical
	listLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
	listLayout.VerticalAlignment = Enum.VerticalAlignment.Top
	listLayout.Padding = UDim.new(0, 10)

	if newList:IsA("ScrollingFrame") then
		newList.CanvasPosition = Vector2.new(0, 0)
		newList.CanvasSize = UDim2.new(0, 0, 0, 360)
		newList.ScrollingDirection = Enum.ScrollingDirection.Y
		newList.ScrollBarImageTransparency = 1
	end

	local infoCard = Instance.new("Frame")
	infoCard.Name = "ServersComingSoon"
	infoCard.LayoutOrder = 1
	infoCard.Parent = newList
	infoCard.Size = UDim2.new(0, 558, 0, 124)
	infoCard.BackgroundColor3 = Color3.fromRGB(18, 14, 22)
	infoCard.BackgroundTransparency = 0.08
	infoCard.Visible = true

	local infoCorner = Instance.new("UICorner")
	infoCorner.CornerRadius = UDim.new(0, 8)
	infoCorner.Parent = infoCard

	local infoStroke = Instance.new("UIStroke")
	infoStroke.Color = Color3.fromRGB(170, 78, 190)
	infoStroke.Transparency = 0.45
	infoStroke.Parent = infoCard

	local infoTitle = Instance.new("TextLabel")
	infoTitle.BackgroundTransparency = 1
	infoTitle.Font = Enum.Font.GothamBold
	infoTitle.Text = "SERVERS"
	infoTitle.TextColor3 = Color3.fromRGB(245, 245, 245)
	infoTitle.TextSize = 15
	infoTitle.TextTransparency = 0.08
	infoTitle.TextXAlignment = Enum.TextXAlignment.Left
	infoTitle.Position = UDim2.new(0, 18, 0, 18)
	infoTitle.Size = UDim2.new(1, -36, 0, 22)
	infoTitle.Parent = infoCard

	local infoBody = Instance.new("TextLabel")
	infoBody.BackgroundTransparency = 1
	infoBody.Font = Enum.Font.GothamMedium
	infoBody.Text = "Server tools will be added here later. For now this page is ready and stable."
	infoBody.TextColor3 = Color3.fromRGB(230, 230, 230)
	infoBody.TextSize = 12
	infoBody.TextTransparency = 0.28
	infoBody.TextWrapped = true
	infoBody.TextXAlignment = Enum.TextXAlignment.Left
	infoBody.TextYAlignment = Enum.TextYAlignment.Top
	infoBody.Position = UDim2.new(0, 18, 0, 48)
	infoBody.Size = UDim2.new(1, -36, 0, 48)
	infoBody.Parent = infoCard

	local infoLine = Instance.new("Frame")
	infoLine.BackgroundColor3 = Color3.fromRGB(170, 78, 190)
	infoLine.BackgroundTransparency = 0.35
	infoLine.BorderSizePixel = 0
	infoLine.Position = UDim2.new(0, 18, 1, -18)
	infoLine.Size = UDim2.new(0, 82, 0, 2)
	infoLine.Parent = infoCard

	if newList:IsA("ScrollingFrame") then
		newList.CanvasSize = UDim2.new(0, 0, 0, 160)
	end

	-- Server tools are paused for now. The page intentionally shows a stable placeholder.
	--[[
	local function clearChildren(parent)
		for _, child in ipairs(parent:GetChildren()) do
			child:Destroy()
		end
	end

	local function updateCanvas()
		if not newList:IsA("ScrollingFrame") then return end

		local contentHeight = 0
		local activeLayout = newList:FindFirstChildOfClass("UIListLayout")

		if activeLayout then
			contentHeight = activeLayout.AbsoluteContentSize.Y
		end

		if contentHeight <= 0 then
			for _, dropdown in ipairs(dropdowns) do
				contentHeight = contentHeight + dropdown.row.Size.Y.Offset + 10
			end
		end

		newList.CanvasSize = UDim2.new(0, 0, 0, contentHeight + 26)
		newList.ScrollBarImageTransparency = contentHeight > newList.AbsoluteWindowSize.Y + 8 and 0.55 or 1
	end

	local function createLabel(parent, text, position, size, textSize, transparency, bold)
		local label = Instance.new("TextLabel")
		label.BackgroundTransparency = 1
		label.Font = bold and Enum.Font.GothamBold or Enum.Font.GothamMedium
		label.Text = text
		label.TextColor3 = Color3.fromRGB(238, 238, 238)
		label.TextSize = textSize or 12
		label.TextTransparency = transparency or 0.22
		label.TextXAlignment = Enum.TextXAlignment.Left
		label.TextYAlignment = Enum.TextYAlignment.Center
		label.TextWrapped = true
		label.Position = position
		label.Size = size
		label.Parent = parent

		return label
	end

	local function createButton(parent, text, position, size, color)
		local button = Instance.new("TextButton")
		button.AutoButtonColor = false
		button.BackgroundColor3 = color or Color3.fromRGB(40, 40, 40)
		button.BackgroundTransparency = 0.18
		button.Font = Enum.Font.GothamBold
		button.Text = string.upper(text)
		button.TextColor3 = Color3.fromRGB(245, 245, 245)
		button.TextSize = 11
		button.TextTransparency = 0.1
		button.Position = position
		button.Size = size
		button.Parent = parent

		local corner = Instance.new("UICorner")
		corner.CornerRadius = UDim.new(0, 8)
		corner.Parent = button

		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(175, 86, 190)
		stroke.Transparency = 0.64
		stroke.Parent = button

		button.MouseEnter:Connect(function()
			tweenService:Create(button, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.04}):Play()
			tweenService:Create(stroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.35}):Play()
		end)

		button.MouseLeave:Connect(function()
			tweenService:Create(button, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.18}):Play()
			tweenService:Create(stroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.64}):Play()
		end)

		return button
	end

	local function createInput(parent, placeholder, position, size)
		local box = Instance.new("TextBox")
		box.BackgroundColor3 = Color3.fromRGB(24, 21, 27)
		box.BackgroundTransparency = 0.08
		box.ClearTextOnFocus = false
		box.Font = Enum.Font.GothamMedium
		box.PlaceholderText = placeholder
		box.PlaceholderColor3 = Color3.fromRGB(150, 140, 154)
		box.Text = ""
		box.TextColor3 = Color3.fromRGB(240, 240, 240)
		box.TextSize = 12
		box.TextXAlignment = Enum.TextXAlignment.Left
		box.Position = position
		box.Size = size
		box.Parent = parent

		local padding = Instance.new("UIPadding")
		padding.PaddingLeft = UDim.new(0, 12)
		padding.PaddingRight = UDim.new(0, 12)
		padding.Parent = box

		local corner = Instance.new("UICorner")
		corner.CornerRadius = UDim.new(0, 8)
		corner.Parent = box

		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(175, 86, 190)
		stroke.Transparency = 0.62
		stroke.Parent = box

		box.Focused:Connect(function()
			tweenService:Create(stroke, TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.24}):Play()
		end)

		box.FocusLost:Connect(function()
			tweenService:Create(stroke, TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.62}):Play()
		end)

		return box
	end

	local function createDropdown(title, description, layoutOrder, defaultExpandedHeight)
		local dropdown = {
			collapsedHeight = 74,
			expandedHeight = defaultExpandedHeight,
			open = false
		}

		local row = Instance.new("Frame")
		row.Name = title
		row.LayoutOrder = layoutOrder
		row.Parent = newList
		row.Size = UDim2.new(0, 558, 0, dropdown.collapsedHeight)
		row.BackgroundColor3 = Color3.fromRGB(18, 14, 22)
		row.BackgroundTransparency = 0.1
		row.ClipsDescendants = true
		row.Visible = true
		dropdown.row = row

		local corner = Instance.new("UICorner")
		corner.CornerRadius = UDim.new(0, 8)
		corner.Parent = row

		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(170, 78, 190)
		stroke.Transparency = 0.52
		stroke.Parent = row
		dropdown.stroke = stroke

		local titleLabel = createLabel(row, title, UDim2.new(0, 18, 0, 10), UDim2.new(1, -70, 0, 22), 13, 0.08, true)
		local descriptionLabel = createLabel(row, description, UDim2.new(0, 18, 0, 33), UDim2.new(1, -80, 0, 30), 11, 0.42)

		local arrow = Instance.new("TextLabel")
		arrow.BackgroundTransparency = 1
		arrow.Font = Enum.Font.GothamBold
		arrow.Text = ">"
		arrow.TextColor3 = Color3.fromRGB(232, 232, 232)
		arrow.TextSize = 22
		arrow.TextTransparency = 0.12
		arrow.Position = UDim2.new(1, -48, 0, 21)
		arrow.Size = UDim2.new(0, 26, 0, 26)
		arrow.Parent = row
		dropdown.arrow = arrow

		local content = Instance.new("Frame")
		content.Name = "DropdownContent"
		content.BackgroundTransparency = 1
		content.Position = UDim2.new(0, 16, 0, 78)
		content.Size = UDim2.new(1, -32, 0, defaultExpandedHeight - 88)
		content.Parent = row
		dropdown.content = content

		local hitbox = Instance.new("TextButton")
		hitbox.Name = "DropdownInteract"
		hitbox.BackgroundTransparency = 1
		hitbox.Text = ""
		hitbox.AutoButtonColor = false
		hitbox.Position = UDim2.new(0, 0, 0, 0)
		hitbox.Size = UDim2.new(1, 0, 0, dropdown.collapsedHeight)
		hitbox.Parent = row

		hitbox.MouseEnter:Connect(function()
			tweenService:Create(row, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.05}):Play()
			tweenService:Create(stroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.35}):Play()
			tweenService:Create(titleLabel, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {TextTransparency = 0}):Play()
		end)

		hitbox.MouseLeave:Connect(function()
			tweenService:Create(row, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.1}):Play()
			tweenService:Create(stroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = dropdown.open and 0.28 or 0.52}):Play()
			tweenService:Create(titleLabel, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {TextTransparency = 0.08}):Play()
		end)

		function dropdown:SetExpandedHeight(height)
			self.expandedHeight = math.max(height, self.collapsedHeight + 60)
			self.content.Size = UDim2.new(1, -32, 0, self.expandedHeight - 88)

			if self.open then
				tweenService:Create(self.row, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Size = UDim2.new(0, 558, 0, self.expandedHeight)}):Play()
				task.delay(0.27, updateCanvas)
			else
				updateCanvas()
			end
		end

		local function closeDropdown(target)
			target.open = false
			tweenService:Create(target.row, TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Size = UDim2.new(0, 558, 0, target.collapsedHeight)}):Play()
			tweenService:Create(target.arrow, TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Rotation = 0}):Play()
			tweenService:Create(target.stroke, TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.52}):Play()
		end

		function dropdown:Open(forceOpen)
			if activeDropdown and activeDropdown ~= self then
				closeDropdown(activeDropdown)
			end

			if self.open and not forceOpen then
				closeDropdown(self)
				activeDropdown = nil
				task.delay(0.3, updateCanvas)
				return
			end

			self.open = true
			activeDropdown = self
			tweenService:Create(self.row, TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Size = UDim2.new(0, 558, 0, self.expandedHeight)}):Play()
			tweenService:Create(self.arrow, TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Rotation = 90}):Play()
			tweenService:Create(self.stroke, TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.18}):Play()
			task.delay(0.36, updateCanvas)
		end

		hitbox.MouseButton1Click:Connect(function()
			dropdown:Open()
		end)

		table.insert(dropdowns, dropdown)
		return dropdown
	end

	local serverDropdown = createDropdown("Server Search", "Scan public servers, sort by players, then join or copy JobIds.", 1, 230)
	local playerDropdown = createDropdown("Player Finder", "Search a username across public server avatar tokens and join their server.", 2, 205)
	local emptyDropdown = createDropdown("Empty / Fresh Server", "Find a listed empty server or ask Roblox for a fresh session.", 3, 168)
	local historyDropdown = createDropdown("Recent Servers", "Rejoin servers you visited before this session.", 4, 150)

	local serverStatus = createLabel(serverDropdown.content, "Tap Scan Now to load public servers.", UDim2.new(0, 0, 0, 0), UDim2.new(1, 0, 0, 28), 11, 0.32)
	local scanButton = createButton(serverDropdown.content, "Scan Now", UDim2.new(0, 0, 0, 36), UDim2.new(0, 110, 0, 32), Color3.fromRGB(115, 50, 138))
	local sortButton = createButton(serverDropdown.content, "Sort: Low", UDim2.new(0, 118, 0, 36), UDim2.new(0, 100, 0, 32), Color3.fromRGB(43, 43, 43))
	local serverRows = Instance.new("Frame")
	serverRows.Name = "ServerRows"
	serverRows.BackgroundTransparency = 1
	serverRows.Position = UDim2.new(0, 0, 0, 80)
	serverRows.Size = UDim2.new(1, 0, 0, 40)
	serverRows.Parent = serverDropdown.content

	local playerStatus = createLabel(playerDropdown.content, "Type a username, then search public servers.", UDim2.new(0, 0, 0, 0), UDim2.new(1, 0, 0, 28), 11, 0.32)
	local playerSearch = createInput(playerDropdown.content, "username", UDim2.new(0, 0, 0, 40), UDim2.new(0, 260, 0, 32))
	local searchButton = createButton(playerDropdown.content, "Search Now", UDim2.new(0, 270, 0, 40), UDim2.new(0, 108, 0, 32), Color3.fromRGB(128, 58, 150))
	local joinFoundButton = createButton(playerDropdown.content, "Join Found", UDim2.new(0, 386, 0, 40), UDim2.new(0, 104, 0, 32), Color3.fromRGB(48, 96, 116))
	joinFoundButton.TextTransparency = 0.55
	createLabel(playerDropdown.content, "The found server will appear in Server Search results too.", UDim2.new(0, 0, 0, 88), UDim2.new(1, 0, 0, 30), 11, 0.46)

	local emptyStatus = createLabel(emptyDropdown.content, "Choose how Sirius should move you.", UDim2.new(0, 0, 0, 0), UDim2.new(1, 0, 0, 28), 11, 0.32)
	local emptyButton = createButton(emptyDropdown.content, "Join 0 Player", UDim2.new(0, 0, 0, 42), UDim2.new(0, 148, 0, 34), Color3.fromRGB(36, 110, 78))
	local freshButton = createButton(emptyDropdown.content, "Fresh Server", UDim2.new(0, 158, 0, 42), UDim2.new(0, 138, 0, 34), Color3.fromRGB(110, 72, 36))

	local historyStatus = createLabel(historyDropdown.content, "Recent hops will appear here.", UDim2.new(0, 0, 0, 0), UDim2.new(1, 0, 0, 26), 11, 0.32)
	local historyRows = Instance.new("Frame")
	historyRows.Name = "HistoryRows"
	historyRows.BackgroundTransparency = 1
	historyRows.Position = UDim2.new(0, 0, 0, 34)
	historyRows.Size = UDim2.new(1, 0, 0, 40)
	historyRows.Parent = historyDropdown.content

	local function setJoinFoundEnabled(enabled)
		joinFoundButton.TextTransparency = enabled and 0.08 or 0.55
		joinFoundButton.BackgroundTransparency = enabled and 0.12 or 0.32
	end

	local function setServerStatus(text)
		serverStatus.Text = text
	end

	local function setPlayerStatus(text)
		playerStatus.Text = text
	end

	local function setEmptyStatus(text)
		emptyStatus.Text = text
	end

	local function styleMiniRow(row)
		local corner = Instance.new("UICorner")
		corner.CornerRadius = UDim.new(0, 8)
		corner.Parent = row

		local stroke = Instance.new("UIStroke")
		stroke.Color = Color3.fromRGB(92, 74, 98)
		stroke.Transparency = 0.6
		stroke.Parent = row
	end

	local function createServerMiniRow(parent, y, serverData, index, isHistory)
		local row = Instance.new("Frame")
		row.Name = serverData.id or "Server"
		row.BackgroundColor3 = Color3.fromRGB(22, 19, 25)
		row.BackgroundTransparency = isHistory and 0.14 or 0.1
		row.Position = UDim2.new(0, 0, 0, y)
		row.Size = UDim2.new(1, -4, 0, 44)
		row.Parent = parent
		styleMiniRow(row)

		local isCurrent = serverData.id == jobId
		local playingText = tostring(serverData.playing or "?").."/"..tostring(serverData.maxPlayers or "?")
		local title = isHistory and (serverData.label or "Previous server") or ("Server "..index)
		if isCurrent then title = title.."  CURRENT" end

		createLabel(row, title, UDim2.new(0, 12, 0, 5), UDim2.new(0, 250, 0, 17), 11, 0.1, true)
		createLabel(row, playingText.." players  id "..string.sub(tostring(serverData.id or ""), 1, 10), UDim2.new(0, 12, 0, 22), UDim2.new(0, 300, 0, 16), 10, 0.42)

		if originalSetClipboard then
			local copyButton = createButton(row, "Copy", UDim2.new(1, -142, 0, 8), UDim2.new(0, 54, 0, 28), Color3.fromRGB(44, 44, 44))
			copyButton.MouseButton1Click:Connect(function()
				originalSetClipboard(tostring(serverData.id or ""))
				queueNotification("Server ID Copied", "The server JobId is now on your clipboard.", 4400696294)
			end)
		end

		local joinButton = createButton(row, isCurrent and "Here" or "Join", UDim2.new(1, -80, 0, 8), UDim2.new(0, 64, 0, 28), isCurrent and Color3.fromRGB(42, 42, 42) or Color3.fromRGB(74, 58, 118))
		joinButton.MouseButton1Click:Connect(function()
			if isCurrent then
				setServerStatus("You are already in this server.")
				return
			end

			teleportToServer(serverData.id, "Joining server with "..playingText.." players.", serverData.playing, serverData.maxPlayers)
		end)
	end

	local function sortServers()
		table.sort(serverResults, function(a, b)
			local aPlayers = tonumber(a.playing) or 0
			local bPlayers = tonumber(b.playing) or 0

			if sortAscending then
				return aPlayers < bPlayers
			end

			return aPlayers > bPlayers
		end)
	end

	local function renderServers()
		clearChildren(serverRows)
		sortServers()

		local y = 0
		if #serverResults == 0 then
			createLabel(serverRows, "No server results yet.", UDim2.new(0, 0, 0, 0), UDim2.new(1, 0, 0, 28), 11, 0.45)
			y = 34
		else
			for index, serverData in ipairs(serverResults) do
				if index > 20 then break end
				createServerMiniRow(serverRows, y, serverData, index, false)
				y = y + 50
			end

			if #serverResults > 20 then
				createLabel(serverRows, "Showing first 20 of "..#serverResults.." results. Sort or rescan to refine.", UDim2.new(0, 0, 0, y), UDim2.new(1, 0, 0, 24), 10, 0.48)
				y = y + 28
			end
		end

		serverRows.Size = UDim2.new(1, 0, 0, y)
		serverDropdown:SetExpandedHeight(174 + y)
	end

	local function renderHistory()
		clearChildren(historyRows)
		local history = loadServerHistory()
		local y = 0

		if #history == 0 then
			historyStatus.Text = "No previous server hops yet."
			createLabel(historyRows, "Serverhop once and Sirius will remember the old session here.", UDim2.new(0, 0, 0, 0), UDim2.new(1, 0, 0, 34), 11, 0.45)
			y = 42
		else
			historyStatus.Text = "Tap a remembered session to rejoin it."
			for index, entry in ipairs(history) do
				if index > 6 then break end
				createServerMiniRow(historyRows, y, entry, index, true)
				y = y + 50
			end
		end

		historyRows.Size = UDim2.new(1, 0, 0, y)
		historyDropdown:SetExpandedHeight(120 + y)
	end

	local function scanServers(maxPages)
		serverDropdown:Open(true)
		activeScanId = activeScanId + 1
		local scanId = activeScanId
		serverResults = {}
		foundPlayerServer = nil
		setJoinFoundEnabled(false)
		renderServers()

		task.spawn(function()
			local cursor
			local page = 0

			while page < maxPages do
				page = page + 1
				if activeScanId ~= scanId then return end
				setServerStatus("Scanning page "..page.."...")

				local response, err = fetchServerPage(sortAscending and "Asc" or "Desc", cursor)
				if not response then
					setServerStatus("Server scan failed: "..tostring(err))
					return
				end

				for _, serverData in ipairs(response.data or {}) do
					if type(serverData) == "table" and serverData.id and serverData.id ~= jobId then
						table.insert(serverResults, serverData)
					end
				end

				renderServers()
				cursor = response.nextPageCursor

				if not cursor or cursor == "" then break end
				task.wait()
			end

			setServerStatus("Found "..#serverResults.." public servers.")
		end)
	end

	local function joinEmptyServer()
		emptyDropdown:Open(true)
		activeScanId = activeScanId + 1
		local scanId = activeScanId

		task.spawn(function()
			setEmptyStatus("Checking scanned results for a 0-player server...")

			for _, serverData in ipairs(serverResults) do
				if serverData.playing == 0 and serverData.id ~= jobId then
					return teleportToServer(serverData.id, "Joining a listed 0-player server.", serverData.playing, serverData.maxPlayers)
				end
			end

			local cursor
			for page = 1, 30 do
				if activeScanId ~= scanId then return end
				setEmptyStatus("Searching empty servers page "..page.."...")

				local response = fetchServerPage("Asc", cursor)
				if not response then break end

				for _, serverData in ipairs(response.data or {}) do
					if type(serverData) == "table" and serverData.id ~= jobId and serverData.playing == 0 then
						return teleportToServer(serverData.id, "Joining a listed 0-player server.", serverData.playing, serverData.maxPlayers)
					end
				end

				cursor = response.nextPageCursor
				if not cursor or cursor == "" then break end
				task.wait()
			end

			setEmptyStatus("No listed 0-player server found. Use Fresh Server instead.")
		end)
	end

	local function searchPlayer(username)
		username = string.gsub(username or "", "^%s*(.-)%s*$", "%1")

		if username == "" then
			playerDropdown:Open(true)
			setPlayerStatus("Type a username before searching.")
			return
		end

		playerDropdown:Open(true)
		activeScanId = activeScanId + 1
		local scanId = activeScanId
		foundPlayerServer = nil
		setJoinFoundEnabled(false)

		task.spawn(function()
			setPlayerStatus("Resolving username "..username.."...")
			local userId, resolvedName = getUserIdFromUsername(username)

			if not userId then
				setPlayerStatus("Could not find that Roblox username.")
				return
			end

			local targetHeadshot = getUserHeadshot(userId)
			if not targetHeadshot then
				setPlayerStatus("Could not load avatar data for "..resolvedName..".")
				return
			end

			local cursor
			local checkedServers = 0
			local batch = {}
			local batchMap = {}

			local function flushBatch()
				if #batch == 0 then return end

				local response = requestJson("https://thumbnails.roblox.com/v1/batch", "POST", batch)
				batch = {}

				if response and response.data then
					for _, thumb in ipairs(response.data) do
						if thumb.imageUrl == targetHeadshot and batchMap[thumb.requestId] then
							return batchMap[thumb.requestId]
						end
					end
				end

				batchMap = {}
			end

			for page = 1, 50 do
				if activeScanId ~= scanId then return end
				setPlayerStatus("Searching "..resolvedName.." on page "..page.."...")

				local response, err = fetchServerPage("Asc", cursor)
				if not response then
					setPlayerStatus("Player search failed: "..tostring(err))
					return
				end

				for _, serverData in ipairs(response.data or {}) do
					checkedServers = checkedServers + 1

					for tokenIndex, token in ipairs(serverData.playerTokens or {}) do
						local requestId = tostring(serverData.id).."|"..tokenIndex
						batchMap[requestId] = serverData
						table.insert(batch, {
							requestId = requestId,
							type = "AvatarHeadShot",
							targetId = 0,
							token = token,
							format = "Png",
							size = "48x48",
							isCircular = false
						})

						if #batch >= 100 then
							local found = flushBatch()
							if found then
								foundPlayerServer = found
								serverResults = {found}
								renderServers()
								setJoinFoundEnabled(true)
								setPlayerStatus("Player found in a server with "..tostring(found.playing).."/"..tostring(found.maxPlayers).." players.")
								return
							end
						end
					end
				end

				local found = flushBatch()
				if found then
					foundPlayerServer = found
					serverResults = {found}
					renderServers()
					setJoinFoundEnabled(true)
					setPlayerStatus("Player found in a server with "..tostring(found.playing).."/"..tostring(found.maxPlayers).." players.")
					return
				end

				cursor = response.nextPageCursor
				if not cursor or cursor == "" then break end
				task.wait()
			end

			setPlayerStatus("Could not find "..resolvedName.." after checking "..checkedServers.." servers.")
		end)
	end

	scanButton.MouseButton1Click:Connect(function()
		scanServers(12)
	end)

	sortButton.MouseButton1Click:Connect(function()
		sortAscending = not sortAscending
		sortButton.Text = sortAscending and "SORT: LOW" or "SORT: HIGH"
		renderServers()
	end)

	searchButton.MouseButton1Click:Connect(function()
		searchPlayer(playerSearch.Text)
	end)

	playerSearch.FocusLost:Connect(function(enterPressed)
		if enterPressed then
			searchPlayer(playerSearch.Text)
		end
	end)

	joinFoundButton.MouseButton1Click:Connect(function()
		if foundPlayerServer then
			teleportToServer(foundPlayerServer.id, "Joining the found player's public server.", foundPlayerServer.playing, foundPlayerServer.maxPlayers)
		else
			setPlayerStatus("Search for a player first.")
		end
	end)

	emptyButton.MouseButton1Click:Connect(joinEmptyServer)
	freshButton.MouseButton1Click:Connect(function()
		emptyDropdown:Open(true)
		setEmptyStatus("Creating fresh server...")
		teleportFreshServer()
	end)

	historyDropdown.row.DropdownInteract.MouseButton1Click:Connect(function()
		task.defer(renderHistory)
	end)

	setJoinFoundEnabled(false)
	renderServers()
	renderHistory()
	serverDropdown:Open(true)
	updateCanvas()
	]]
end

local function ensureFrameProperties()
	UI.Enabled = true
	characterPanel.Visible = false
	customScriptPrompt.Visible = false
	disconnectedPrompt.Visible = false
	playerlistPanel.Interactions.List.Template.Visible = false
	gameDetectionPrompt.Visible = false
	homeContainer.Visible = false
	moderatorDetectionPrompt.Visible = false
	musicPanel.Visible = false
	notificationContainer.Visible = true
	playerlistPanel.Visible = false
	scriptSearch.Visible = false
	scriptsPanel.Visible = false
	settingsPanel.Visible = false
	smartBar.Visible = false
	musicPanel.Playing.Text = "Not Playing"
	smartBar.Buttons.Music.Visible = true
	toastsContainer.Visible = true
	attachScrollbarFade(playerlistPanel.Interactions.List, 0.68, 0.62)
	attachScrollbarFade(scriptSearch.List, 0.42, 0.68)
	attachScrollbarFade(musicPanel.Queue.List, 0.62, 0.65)
	makeDraggable(settingsPanel)
	makeDraggable(musicPanel)
end

local function checkFriends()
	if friendsCooldown == 0 then

		friendsCooldown = 25

		local playersFriends = {}
		local success, page = pcall(players.GetFriendsAsync, players, localPlayer.UserId)

		if success then
			repeat
				local info = page:GetCurrentPage()
				for i, friendInfo in pairs(info) do
					table.insert(playersFriends, friendInfo)
				end
				if not page.IsFinished then 
					page:AdvanceToNextPageAsync()
				end
			until page.IsFinished
		end

		local friendsInTotal = 0
		local onlineFriends = 0 
		local friendsInGame = 0 

		for i,v in pairs(playersFriends) do
			friendsInTotal  = friendsInTotal + 1

			if v.IsOnline then
				onlineFriends = onlineFriends + 1
			end

			if players:FindFirstChild(v.Username) then
				friendsInGame = friendsInGame + 1
			end
		end

		if not checkSirius() then return end

		homeContainer.Interactions.Friends.All.Value.Text = tostring(friendsInTotal).." friends"
		homeContainer.Interactions.Friends.Offline.Value.Text = tostring(friendsInTotal - onlineFriends).." friends"
		homeContainer.Interactions.Friends.Online.Value.Text = tostring(onlineFriends).." friends"
		homeContainer.Interactions.Friends.InGame.Value.Text = tostring(friendsInGame).." friends"

	else
		friendsCooldown -= 1
	end
end

function promptModerator(player, role, promptTitle, promptSubtitle)
	local serversAvailable = false
	local promptClosed = false

	if moderatorDetectionPrompt.Visible then return end

	siriusValues.moderatorPromptConnections = siriusValues.moderatorPromptConnections or {}
	for _, connection in ipairs(siriusValues.moderatorPromptConnections) do
		if connection and connection.Connected then
			connection:Disconnect()
		end
	end
	table.clear(siriusValues.moderatorPromptConnections)

	moderatorDetectionPrompt.Size = UDim2.new(0, 283, 0, 175)
	moderatorDetectionPrompt.UIGradient.Offset = Vector2.new(0, 1)
	wipeTransparency(moderatorDetectionPrompt, 1, true)
	moderatorDetectionPrompt.Serverhop.Visible = true
	moderatorDetectionPrompt.ServersAvailableFade.Visible = false
	moderatorDetectionPrompt.ServersAvailableFade.TextTransparency = 1

	moderatorDetectionPrompt.Title.Text = promptTitle or "Moderator / Admin Joined"
	moderatorDetectionPrompt.Subtitle.Text = promptSubtitle or "Sirius detected a staff-like group role and disabled active abilities."
	moderatorDetectionPrompt.DisplayName.Text = player.DisplayName
	moderatorDetectionPrompt.Rank.Text = role
	moderatorDetectionPrompt.Avatar.Image = "https://www.roblox.com/headshot-thumbnail/image?userId="..player.UserId.."&width=420&height=420&format=png"

	moderatorDetectionPrompt.Visible = true

	local serverListSuccess, serverList = pcall(function()
		return httpService:JSONDecode(game:HttpGetAsync("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"))
	end)

	if serverListSuccess and type(serverList) == "table" and type(serverList.data) == "table" then
		for _, v in ipairs(serverList.data) do
			if type(v) == "table" and tonumber(v.maxPlayers) and tonumber(v.playing) and v.maxPlayers > v.playing and v.id ~= game.JobId then
				serversAvailable = true
				break
			end
		end
	end

	if not serversAvailable then
		moderatorDetectionPrompt.Serverhop.Visible = false
	else
		moderatorDetectionPrompt.ServersAvailableFade.Visible = true
	end

	tweenService:Create(moderatorDetectionPrompt, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 0}):Play()
	tweenService:Create(moderatorDetectionPrompt, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {Size = UDim2.new(0, 300, 0, 186)}):Play()
	tweenService:Create(moderatorDetectionPrompt.UIGradient, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 0.65)}):Play()
	tweenService:Create(moderatorDetectionPrompt.Title, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
	tweenService:Create(moderatorDetectionPrompt.Subtitle, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
	tweenService:Create(moderatorDetectionPrompt.Avatar, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.7}):Play()
	tweenService:Create(moderatorDetectionPrompt.Avatar, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {ImageTransparency = 0}):Play()
	tweenService:Create(moderatorDetectionPrompt.DisplayName, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
	tweenService:Create(moderatorDetectionPrompt.Rank, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
	tweenService:Create(moderatorDetectionPrompt.Serverhop, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.7}):Play()
	tweenService:Create(moderatorDetectionPrompt.Leave, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.7}):Play()
	task.wait(0.2)
	tweenService:Create(moderatorDetectionPrompt.Serverhop, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
	tweenService:Create(moderatorDetectionPrompt.Leave, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
	task.wait(0.3)
	tweenService:Create(moderatorDetectionPrompt.Close, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {ImageTransparency = 0.6}):Play()

	local function closeModPrompt()
		if promptClosed then return end
		promptClosed = true

		for _, connection in ipairs(siriusValues.moderatorPromptConnections) do
			if connection and connection.Connected then
				connection:Disconnect()
			end
		end
		table.clear(siriusValues.moderatorPromptConnections)

		tweenService:Create(moderatorDetectionPrompt, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
		tweenService:Create(moderatorDetectionPrompt, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Size = UDim2.new(0, 283, 0, 175)}):Play()
		tweenService:Create(moderatorDetectionPrompt.UIGradient, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 1)}):Play()
		tweenService:Create(moderatorDetectionPrompt.Title, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
		tweenService:Create(moderatorDetectionPrompt.Subtitle, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
		tweenService:Create(moderatorDetectionPrompt.Avatar, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
		tweenService:Create(moderatorDetectionPrompt.Avatar, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
		tweenService:Create(moderatorDetectionPrompt.DisplayName, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
		tweenService:Create(moderatorDetectionPrompt.Rank, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
		tweenService:Create(moderatorDetectionPrompt.Serverhop, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
		tweenService:Create(moderatorDetectionPrompt.Leave, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
		tweenService:Create(moderatorDetectionPrompt.Serverhop, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
		tweenService:Create(moderatorDetectionPrompt.Leave, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
		tweenService:Create(moderatorDetectionPrompt.Close, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
		task.wait(0.5)
		moderatorDetectionPrompt.Visible = false
		moderatorDetectionPrompt.Serverhop.Visible = true
		moderatorDetectionPrompt.ServersAvailableFade.Visible = false
	end

	table.insert(siriusValues.moderatorPromptConnections, moderatorDetectionPrompt.Leave.MouseButton1Click:Connect(function()
		closeModPrompt()
		game:Shutdown()
	end))

	table.insert(siriusValues.moderatorPromptConnections, moderatorDetectionPrompt.Serverhop.MouseEnter:Connect(function()
		tweenService:Create(moderatorDetectionPrompt.ServersAvailableFade, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0.5}):Play()
	end))

	table.insert(siriusValues.moderatorPromptConnections, moderatorDetectionPrompt.Serverhop.MouseLeave:Connect(function()
		tweenService:Create(moderatorDetectionPrompt.ServersAvailableFade, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
	end))

	table.insert(siriusValues.moderatorPromptConnections, moderatorDetectionPrompt.Serverhop.MouseButton1Click:Connect(function()
		if promptClosed then return end
		pcall(serverhop)
		closeModPrompt()
	end))

	table.insert(siriusValues.moderatorPromptConnections, moderatorDetectionPrompt.Close.MouseButton1Click:Connect(function()
		closeModPrompt()
	end))
end

local function UpdateHome()
	if not checkSirius() then return end

	local function format(Int)
		return string.format("%02i", Int)
	end

	local function convertToHMS(Seconds)
		local Minutes = (Seconds - Seconds%60)/60
		Seconds = Seconds - Minutes*60
		local Hours = (Minutes - Minutes%60)/60
		Minutes = Minutes - Hours*60
		return format(Hours)..":"..format(Minutes)..":"..format(Seconds)
	end

	-- Home Title
	homeContainer.Title.Text = "Welcome home, "..localPlayer.DisplayName

	-- Players
	homeContainer.Interactions.Server.Players.Value.Text = #players:GetPlayers().." playing"
	homeContainer.Interactions.Server.MaxPlayers.Value.Text = players.MaxPlayers.." players can join this server"

	-- Ping
	homeContainer.Interactions.Server.Latency.Value.Text = math.floor(getPing()).."ms"

	-- Time
	homeContainer.Interactions.Server.Time.Value.Text = convertToHMS(time())

	-- Region
	homeContainer.Interactions.Server.Region.Value.Text = "Unable to retrieve region"

	-- Player Information
	homeContainer.Interactions.User.Avatar.Image = "https://www.roblox.com/headshot-thumbnail/image?userId="..localPlayer.UserId.."&width=420&height=420&format=png"
	homeContainer.Interactions.User.Title.Text = localPlayer.DisplayName
	homeContainer.Interactions.User.Subtitle.Text = localPlayer.Name

	-- Update Executor
	homeContainer.Interactions.Client.Title.Text = siriusValues.getExecutorDisplayName()
	if not table.find(siriusValues.executors, string.lower(siriusValues.getExecutorDisplayName())) then
		homeContainer.Interactions.Client.Subtitle.Text = "This executor is not verified as supported - but may still work just fine."
	end

	-- Update Friends Statuses
	checkFriends()
end

local function openHome()
	if debounce then return end
	debounce = true
	homeContainer.Visible = true

	local homeBlur = Instance.new("BlurEffect", lighting)
	homeBlur.Size = 0
	homeBlur.Name = "HomeBlur"

	homeContainer.BackgroundTransparency = 1
	homeContainer.Title.TextTransparency = 1
	homeContainer.Subtitle.TextTransparency = 1

	for _, homeItem in ipairs(homeContainer.Interactions:GetChildren()) do

		wipeTransparency(homeItem, 1, true)

		homeItem.Position = UDim2.new(0, homeItem.Position.X.Offset - 20, 0, homeItem.Position.Y.Offset - 20)
		homeItem.Size = UDim2.new(0, homeItem.Size.X.Offset + 30, 0, homeItem.Size.Y.Offset + 20)

		if homeItem.UIGradient.Offset.Y > 0 then
			homeItem.UIGradient.Offset = Vector2.new(0, homeItem.UIGradient.Offset.Y + 3)
			homeItem.UIStroke.UIGradient.Offset = Vector2.new(0, homeItem.UIStroke.UIGradient.Offset.Y + 3)
		else
			homeItem.UIGradient.Offset = Vector2.new(0, homeItem.UIGradient.Offset.Y - 3)
			homeItem.UIStroke.UIGradient.Offset = Vector2.new(0, homeItem.UIStroke.UIGradient.Offset.Y - 3)
		end
	end

	tweenService:Create(homeContainer, TweenInfo.new(0.3, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.9}):Play()
	tweenService:Create(homeBlur, TweenInfo.new(0.3, Enum.EasingStyle.Quint), {Size = 5}):Play()

	tweenService:Create(camera, TweenInfo.new(0.3, Enum.EasingStyle.Quint), {FieldOfView = camera.FieldOfView + 5}):Play()

	task.wait(0.25)

	for _, inGameUI in ipairs(localPlayer:FindFirstChildWhichIsA("PlayerGui"):GetChildren()) do
		if inGameUI:IsA("ScreenGui") then
			if inGameUI.Enabled then
				if not table.find(getgenv().cachedInGameUI, inGameUI.Name) then
					table.insert(getgenv().cachedInGameUI, #getgenv().cachedInGameUI+1, inGameUI.Name)
				end

				inGameUI.Enabled = false
			end
		end
	end

	table.clear(getgenv().cachedCoreUI)

	for _, coreUI in pairs({"PlayerList", "Chat", "EmotesMenu", "Health", "Backpack"}) do
		if game:GetService("StarterGui"):GetCoreGuiEnabled(coreUI) then
			table.insert(getgenv().cachedCoreUI, #getgenv().cachedCoreUI+1, coreUI)
		end
	end

	for _, coreUI in pairs(getgenv().cachedCoreUI) do
		game:GetService("StarterGui"):SetCoreGuiEnabled(coreUI, false)
	end

	createReverb(0.8)

	tweenService:Create(camera, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {FieldOfView = camera.FieldOfView - 40}):Play()

	tweenService:Create(homeContainer, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.7}):Play()
	tweenService:Create(homeContainer.Title, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
	tweenService:Create(homeContainer.Subtitle, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {TextTransparency = 0.4}):Play()
	tweenService:Create(homeBlur, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {Size = 20}):Play()

	for _, homeItem in ipairs(homeContainer.Interactions:GetChildren()) do
		for _, otherHomeItem in ipairs(homeItem:GetDescendants()) do
			if otherHomeItem.ClassName == "Frame" then
				tweenService:Create(otherHomeItem, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.7}):Play()
			elseif otherHomeItem.ClassName == "TextLabel" then
				if otherHomeItem.Name == "Title" then
					tweenService:Create(otherHomeItem, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
				else
					tweenService:Create(otherHomeItem, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0.3}):Play()
				end
			elseif otherHomeItem.ClassName == "ImageLabel" then
				tweenService:Create(otherHomeItem, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.8}):Play()
				tweenService:Create(otherHomeItem, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {ImageTransparency = 0}):Play()
			end
		end

		tweenService:Create(homeItem, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 0}):Play()
		tweenService:Create(homeItem.UIStroke, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Transparency = 0}):Play()
		tweenService:Create(homeItem, TweenInfo.new(0.5, Enum.EasingStyle.Back), {Position = UDim2.new(0, homeItem.Position.X.Offset + 20, 0, homeItem.Position.Y.Offset + 20)}):Play()
		tweenService:Create(homeItem, TweenInfo.new(0.5, Enum.EasingStyle.Back), {Size = UDim2.new(0, homeItem.Size.X.Offset - 30, 0, homeItem.Size.Y.Offset - 20)}):Play()

		task.delay(0.03, function()
			if homeItem.UIGradient.Offset.Y > 0 then
				tweenService:Create(homeItem.UIGradient, TweenInfo.new(1, Enum.EasingStyle.Exponential), {Offset = Vector2.new(0, homeItem.UIGradient.Offset.Y - 3)}):Play()
				tweenService:Create(homeItem.UIStroke.UIGradient, TweenInfo.new(0.5, Enum.EasingStyle.Exponential), {Offset = Vector2.new(0, homeItem.UIStroke.UIGradient.Offset.Y - 3)}):Play()
			else
				tweenService:Create(homeItem.UIGradient, TweenInfo.new(1, Enum.EasingStyle.Exponential), {Offset = Vector2.new(0, homeItem.UIGradient.Offset.Y + 3)}):Play()
				tweenService:Create(homeItem.UIStroke.UIGradient, TweenInfo.new(0.5, Enum.EasingStyle.Exponential), {Offset = Vector2.new(0, homeItem.UIStroke.UIGradient.Offset.Y + 3)}):Play()
			end
		end)

		task.wait(0.02)
	end

	task.wait(0.85)

	debounce = false
end

local function closeHome()
	if debounce then return end
	debounce = true

	tweenService:Create(camera, TweenInfo.new(0.6, Enum.EasingStyle.Quint), {FieldOfView = camera.FieldOfView + 35}):Play()

	for _, obj in ipairs(lighting:GetChildren()) do
		if obj.Name == "HomeBlur" then
			tweenService:Create(obj, TweenInfo.new(0.6, Enum.EasingStyle.Quint), {Size = 0}):Play()
			task.delay(0.6, obj.Destroy, obj)
		end
	end

	tweenService:Create(homeContainer, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
	tweenService:Create(homeContainer.Title, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
	tweenService:Create(homeContainer.Subtitle, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()

	for _, homeItem in ipairs(homeContainer.Interactions:GetChildren()) do
		for _, otherHomeItem in ipairs(homeItem:GetDescendants()) do
			if otherHomeItem.ClassName == "Frame" then
				tweenService:Create(otherHomeItem, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
			elseif otherHomeItem.ClassName == "TextLabel" then
				if otherHomeItem.Name == "Title" then
					tweenService:Create(otherHomeItem, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
				else
					tweenService:Create(otherHomeItem, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
				end
			elseif otherHomeItem.ClassName == "ImageLabel" then
				tweenService:Create(otherHomeItem, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
				tweenService:Create(otherHomeItem, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
			end
		end
		tweenService:Create(homeItem, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
		tweenService:Create(homeItem.UIStroke, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Transparency = 1}):Play()
	end

	task.wait(0.2)

	for _, cachedInGameUIObject in pairs(getgenv().cachedInGameUI) do
		for _, currentPlayerUI in ipairs(localPlayer:FindFirstChildWhichIsA("PlayerGui"):GetChildren()) do
			if table.find(getgenv().cachedInGameUI, currentPlayerUI.Name) then
				currentPlayerUI.Enabled = true
			end 
		end
	end

	for _, coreUI in pairs(getgenv().cachedCoreUI) do
		game:GetService("StarterGui"):SetCoreGuiEnabled(coreUI, true)
	end

	removeReverbs(0.5)

	task.wait(0.52)

	homeContainer.Visible = false
	debounce = false
end

local function openScriptSearch()
	if scriptSaveState.runPromptActive then return end
	if siriusValues.betaControl and (siriusValues.betaControl.disabled or siriusValues.betaControlFeatureDisabled("scriptsearch")) then
		local disabledMessage = siriusValues.betaControl.message ~= "" and siriusValues.betaControl.message or "ScriptSearch is temporarily disabled for this beta."
		siriusValues.showBetaControlOverlay(siriusValues.betaControl.disabled and "SIRIUS X Disabled" or "ScriptSearch Disabled", disabledMessage, siriusValues.betaControl.disabled)
		return
	end

	debounce = true
	scriptSaveState.ensureControls()
	local openToken = scriptSaveState.nextViewToken()
	scriptSaveState.viewSwitchLocked = false
	scriptSaveState.transition = "opening"
	scriptSaveState.savedMode = false
	scriptSaveState.setListPresentation(false)
	scriptSaveState.header.Visible = false
	scriptSaveState.topButton.Visible = true
	scriptSaveState.topButton.Position = UDim2.new(1, -48, 0, 5)
	scriptSaveState.topButton.Size = UDim2.new(0, 36, 0, 34)
	scriptSaveState.topButton.BackgroundTransparency = 1

	scriptSearch.Size = UDim2.new(0, 480, 0, 23)
	scriptSearch.Position = UDim2.new(0.5, 0, 0.5, 0)
	scriptSearch.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	scriptSearch.BackgroundTransparency = 1
	scriptSearch.SearchBox.Position = UDim2.new(0.509, 0, 0.5, 0)
	scriptSearch.Icon.Position = UDim2.new(0.04, 0, 0.5, 0)
	scriptSearch.SearchBox.Text = ""
	scriptSearch.SearchBox.TextColor3 = Color3.fromRGB(150, 150, 150)
	scriptSearch.Icon.ImageColor3 = Color3.fromRGB(150, 150, 150)
	scriptSearch.UIGradient.Enabled = true
	scriptSearch.UIGradient.Color = scriptSaveState.searchGradientColor
	scriptSearch.UIGradient.Rotation = scriptSaveState.searchGradientRotation
	scriptSearch.UIGradient.Offset = Vector2.new(0, 2)
	scriptSearch.SearchBox.PlaceholderText = "Search scripts"
	scriptSearch.Icon.Visible = true
	scriptSearch.SearchBox.Visible = true
	scriptSearch.List.Template.Visible = false
	scriptSearch.List.Visible = false
	scriptSearch.List.Position = scriptSaveState.listPosition
	scriptSearch.List.Size = scriptSaveState.listSize
	scriptSearch.List.CanvasPosition = Vector2.new(0, 0)
	scriptSearch.List.ScrollBarImageTransparency = 1
	scriptSearch.NoScriptsTitle.TextTransparency = 1
	scriptSearch.NoScriptsDesc.TextTransparency = 1
	scriptSearch.Visible = true

	wipeTransparency(scriptSearch, 1, true)
	scriptSaveState.setBookmarkVisual(scriptSaveState.topButton.BookmarkVisual, false, true)

	tweenService:Create(scriptSearch, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {BackgroundTransparency = 0}):Play()
	tweenService:Create(scriptSearch, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {Size = UDim2.new(0, 580, 0, 43)}):Play()
	tweenService:Create(scriptSearch.Shadow, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {ImageTransparency = 0.85}):Play()
	task.wait(0.03)
	tweenService:Create(scriptSearch.Icon, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {ImageTransparency = 0}):Play()
	task.wait(0.02)
	tweenService:Create(scriptSearch.SearchBox, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 0}):Play()
	scriptSaveState.setBookmarkVisual(scriptSaveState.topButton.BookmarkVisual, false, false)

	task.wait(0.3)
	if not scriptSaveState.isCurrentView(openToken) or scriptSaveState.savedMode then
		if scriptSaveState.transition == "opening" then
			scriptSaveState.transition = scriptSearch.Visible and "open" or "closed"
		end
		debounce = false
		return
	end

	scriptSearch.SearchBox:CaptureFocus()
	task.wait(0.2)
	if scriptSaveState.isCurrentView(openToken) and not scriptSaveState.savedMode then
		scriptSaveState.transition = "open"
	end
	debounce = false
end

local function closeScriptSearch()
	if scriptSaveState.transition == "closed" and not scriptSearch.Visible then return end
	if scriptSaveState.transition == "closing" then return end

	debounce = true
	scriptSaveState.transition = "closing"
	local closeToken = scriptSaveState.nextViewToken()
	scriptSaveState.savedMode = false

	local function abortClose()
		if scriptSaveState.transition == "closing" then
			scriptSaveState.transition = scriptSearch.Visible and "open" or "closed"
		end
		debounce = false
	end

	wipeTransparency(scriptSearch, 1, false)

	task.wait(0.1)
	if scriptSaveState.viewToken ~= closeToken then
		abortClose()
		return
	end

	scriptSearch.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	scriptSearch.UIGradient.Enabled = false
	tweenService:Create(scriptSearch, TweenInfo.new(0.4, Enum.EasingStyle.Quint),  {Size = UDim2.new(0, 520, 0, 0)}):Play()
	scriptSearch.SearchBox:ReleaseFocus()

	task.wait(0.5)
	if scriptSaveState.viewToken ~= closeToken then
		abortClose()
		return
	end

	for _, createdScript in ipairs(scriptSearch.List:GetChildren()) do
		if createdScript.Name ~= "Placeholder" and createdScript.Name ~= "Template" and createdScript.ClassName == "Frame" then
			createdScript:Destroy()
		end
	end

	task.wait(0.1)
	if scriptSaveState.viewToken ~= closeToken then
		abortClose()
		return
	end
	scriptSearch.BackgroundColor3 = Color3.fromRGB(255 ,255, 255)
	scriptSearch.Visible = false
	scriptSearch.UIGradient.Enabled = true
	scriptSaveState.savedMode = false
	if scriptSaveState.topButton then scriptSaveState.topButton.Visible = false end
	if scriptSaveState.header then scriptSaveState.header.Visible = false end
	scriptSearch.Icon.Visible = true
	scriptSearch.SearchBox.Visible = true
	scriptSaveState.transition = "closed"
	debounce = false
end

local function extractRoscriptsRawUrl(pageBody)
	if not pageBody then return end

	local rawUrl = pageBody:match('href="(https://script%.roscripts%.io/[^"]+)"')
	if rawUrl then
		return rawUrl:gsub("&amp;", "&")
	end
end

local function fetchRoscriptsRawUrl(result)
	if result.rawScriptUrl then return result.rawScriptUrl end
	if not result.detailUrl then return end

	local responseRequest = httpRequest({
		Url = result.detailUrl,
		Method = "GET"
	})

	local rawUrl = extractRoscriptsRawUrl(responseRequest and responseRequest.Body)
	if rawUrl then
		result.rawScriptUrl = rawUrl
	end

	return rawUrl
end

local function normaliseScriptImageUrl(url, baseUrl)
	if not url or url == "" then return end
	url = tostring(url)
	if url == "0" or url == "nil" or url == "null" then return end

	if string.sub(url, 1, 2) == "//" then
		return "https:"..url
	elseif string.sub(url, 1, 1) == "/" and baseUrl then
		return baseUrl..url
	end

	return url
end

local function getScriptImageUrl(result)
	if result.provider == "ScriptBlox" then
		if result.image then
			return normaliseScriptImageUrl(result.image, "https://scriptblox.com")
		elseif result.game and result.game.imageUrl then
			return normaliseScriptImageUrl(result.game.imageUrl, "https://scriptblox.com")
		end
	elseif result.provider == "Rscripts.net" then
		return normaliseScriptImageUrl(result.image or (result.user and result.user.image), "https://rscripts.net")
	elseif result.provider == "Roscripts.io" then
		return normaliseScriptImageUrl(result.thumbUrl or result.thumb, "https://roscripts.io")
	end
end

local function hashString(value)
	local hash = 0

	for i = 1, #value do
		hash = (hash * 31 + string.byte(value, i)) % 1000000007
	end

	return tostring(hash)
end

local function getCachedScriptImage(imageUrl)
	if not imageUrl then return end
	if scriptImageCache[imageUrl] then return scriptImageCache[imageUrl] end

	if not (writefile and isfile and isfolder and makefolder and getcustomasset and httpRequest) then
		return imageUrl
	end

	local extension = string.lower(imageUrl:match("%.([%w]+)%??") or "png")
	if not table.find({"png", "jpg", "jpeg", "webp"}, extension) then
		extension = "png"
	end

	checkFolder()

	local assetsPath = siriusValues.siriusFolder.."/Assets"
	local scriptImagesPath = assetsPath.."/ScriptImages"

	if not isfolder(assetsPath) then makefolder(assetsPath) end
	if not isfolder(scriptImagesPath) then makefolder(scriptImagesPath) end

	local imagePath = scriptImagesPath.."/"..hashString(imageUrl).."."..extension

	if not isfile(imagePath) then
		local success = pcall(function()
			local responseRequest = httpRequest({
				Url = imageUrl,
				Method = "GET"
			})

			if responseRequest and responseRequest.Body then
				writefile(imagePath, responseRequest.Body)
			end
		end)

		if not success or not isfile(imagePath) then
			return imageUrl
		end
	end

	local success, asset = pcall(function()
		return getcustomasset(imagePath)
	end)

	if success and asset then
		scriptImageCache[imageUrl] = asset
		return asset
	end

	return imageUrl
end

local function confirmScriptRunMorph(scriptTitle, providerName, imageUrl, result)
	do
		if not checkSirius() or not scriptSearch.Visible then return nil end

		for _, child in ipairs(scriptSearch:GetChildren()) do
			if string.sub(child.Name, 1, 5) == "Morph" then
				child:Destroy()
			end
		end

		local accentColor = scriptSearchBrand.accent
		local accentSoft = accentColor:Lerp(Color3.fromRGB(255, 255, 255), 0.28)
		local accentDeep = accentColor:Lerp(scriptSearchBrand.base, 0.74)
		local morphToken = scriptSaveState.nextPromptToken()
		scriptSaveState.nextSearchToken()

		local previous = {
			size = scriptSearch.Size,
			position = scriptSearch.Position,
			backgroundTransparency = scriptSearch.BackgroundTransparency,
			gradientColor = scriptSearch.UIGradient.Color,
			gradientOffset = scriptSearch.UIGradient.Offset,
			gradientRotation = scriptSearch.UIGradient.Rotation,
			gradientEnabled = scriptSearch.UIGradient.Enabled,
			shadowTransparency = scriptSearch.Shadow.ImageTransparency,
			clips = scriptSearch.ClipsDescendants,
			listVisible = scriptSearch.List.Visible,
			iconVisible = scriptSearch.Icon.Visible,
			searchBoxVisible = scriptSearch.SearchBox.Visible,
			topButtonVisible = scriptSaveState.topButton and scriptSaveState.topButton.Visible or false,
			headerVisible = scriptSaveState.header and scriptSaveState.header.Visible or false,
			noTitleTransparency = scriptSearch.NoScriptsTitle.TextTransparency,
			noDescTransparency = scriptSearch.NoScriptsDesc.TextTransparency
		}

		local morphShadowGradient = scriptSearch.Shadow:FindFirstChildOfClass("UIGradient")
		if morphShadowGradient then
			previous.shadowGradientColor = morphShadowGradient.Color
			previous.shadowGradientOffset = morphShadowGradient.Offset
			previous.shadowGradientRotation = morphShadowGradient.Rotation
		end

		local function restoreMorphShadowGradient()
			if morphShadowGradient and previous.shadowGradientColor then
				morphShadowGradient.Color = previous.shadowGradientColor
				morphShadowGradient.Offset = previous.shadowGradientOffset
				morphShadowGradient.Rotation = previous.shadowGradientRotation
			end
		end

		scriptSaveState.transition = "confirming"
		debounce = true
		scriptSearch.SearchBox:ReleaseFocus()
		scriptSearch.ClipsDescendants = true

		local fadeOut = TweenInfo.new(0.18, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
		tweenService:Create(scriptSearch.Icon, fadeOut, {ImageTransparency = 1}):Play()
		tweenService:Create(scriptSearch.SearchBox, fadeOut, {TextTransparency = 1}):Play()
		tweenService:Create(scriptSearch.NoScriptsTitle, fadeOut, {TextTransparency = 1}):Play()
		tweenService:Create(scriptSearch.NoScriptsDesc, fadeOut, {TextTransparency = 1}):Play()
		tweenService:Create(scriptSearch.List, fadeOut, {ScrollBarImageTransparency = 1}):Play()

		if scriptSaveState.topButton then
			tweenService:Create(scriptSaveState.topButton, fadeOut, {BackgroundTransparency = 1}):Play()
			if scriptSaveState.topButton:FindFirstChild("BookmarkVisual") then
				tweenService:Create(scriptSaveState.topButton.BookmarkVisual, fadeOut, {ImageTransparency = 1}):Play()
			end
		end

		if scriptSaveState.header then
			tweenService:Create(scriptSaveState.header.Title, fadeOut, {TextTransparency = 1}):Play()
			tweenService:Create(scriptSaveState.header.Description, fadeOut, {TextTransparency = 1}):Play()
			tweenService:Create(scriptSaveState.header.Back.BackArrowIcon, fadeOut, {ImageTransparency = 1}):Play()
		end

		task.wait(0.12)
		if scriptSaveState.promptToken ~= morphToken or not scriptSearch.Visible then
			scriptSearch.UIGradient.Color = previous.gradientColor
			scriptSearch.UIGradient.Offset = previous.gradientOffset
			scriptSearch.UIGradient.Rotation = previous.gradientRotation
			scriptSearch.UIGradient.Enabled = previous.gradientEnabled
			restoreMorphShadowGradient()
			scriptSearch.ClipsDescendants = previous.clips
			scriptSearch.Icon.Visible = previous.iconVisible
			scriptSearch.SearchBox.Visible = previous.searchBoxVisible
			scriptSearch.List.Visible = previous.listVisible
			if scriptSaveState.topButton then scriptSaveState.topButton.Visible = previous.topButtonVisible end
			if scriptSaveState.header then scriptSaveState.header.Visible = previous.headerVisible end
			scriptSaveState.transition = "open"
			debounce = false
			return false
		end

		scriptSearch.Icon.Visible = false
		scriptSearch.SearchBox.Visible = false
		scriptSearch.List.Visible = false
		if scriptSaveState.topButton then scriptSaveState.topButton.Visible = false end
		if scriptSaveState.header then scriptSaveState.header.Visible = false end

		scriptSearch.UIGradient.Enabled = true
		scriptSearch.UIGradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, scriptSearchBrand.panel),
			ColorSequenceKeypoint.new(0.62, scriptSearchBrand.base),
			ColorSequenceKeypoint.new(0.88, accentDeep),
			ColorSequenceKeypoint.new(1, accentColor:Lerp(Color3.fromRGB(0, 0, 0), 0.52))
		})
		scriptSearch.UIGradient.Rotation = 88
		scriptSearch.UIGradient.Offset = Vector2.new(0, 1.18)

		if morphShadowGradient then
			morphShadowGradient.Color = ColorSequence.new({
				ColorSequenceKeypoint.new(0, Color3.fromRGB(20, 21, 24)),
				ColorSequenceKeypoint.new(0.62, scriptSearchBrand.base),
				ColorSequenceKeypoint.new(1, accentColor:Lerp(scriptSearchBrand.base, 0.52))
			})
			morphShadowGradient.Rotation = 88
			morphShadowGradient.Offset = Vector2.new(0, 1.1)
		end

		local createdObjects = {}
		local function track(object)
			table.insert(createdObjects, object)
			return object
		end

		local function makeText(parent, name, text, position, size, font, textSize, color, zIndex)
			local label = track(Instance.new("TextLabel"))
			label.Name = name
			label.Parent = parent
			label.BackgroundTransparency = 1
			label.Font = font
			label.Position = position
			label.Size = size
			label.Text = text
			label.TextColor3 = color
			label.TextSize = textSize
			label.TextTransparency = 1
			label.TextWrapped = true
			label.TextXAlignment = Enum.TextXAlignment.Left
			label.TextYAlignment = Enum.TextYAlignment.Top
			label.ZIndex = zIndex
			return label
		end

		local function makeCard(name, position, size)
			local card = track(Instance.new("Frame"))
			card.Name = name
			card.Parent = scriptSearch
			card.BackgroundColor3 = scriptSearchBrand.card
			card.BackgroundTransparency = 1
			card.BorderSizePixel = 0
			card.Position = position
			card.Size = size
			card.ZIndex = 43

			local corner = Instance.new("UICorner")
			corner.CornerRadius = UDim.new(0, 8)
			corner.Parent = card

			local stroke = Instance.new("UIStroke")
			stroke.Color = scriptSearchBrand.stroke
			stroke.Transparency = 1
			stroke.Parent = card

			return card, stroke
		end

		local function makeAccentFrame(parent, name, position, size, zIndex, color)
			local frame = track(Instance.new("Frame"))
			frame.Name = name
			frame.Parent = parent
			frame.BackgroundColor3 = color or accentColor
			frame.BackgroundTransparency = 1
			frame.BorderSizePixel = 0
			frame.Position = position
			frame.Size = size
			frame.ZIndex = zIndex

			local corner = Instance.new("UICorner")
			corner.CornerRadius = UDim.new(1, 0)
			corner.Parent = frame

			return frame
		end

		local decision
		local promptClosed = false
		local promptClosing = false
		local backButton = track(Instance.new("TextButton"))
		backButton.Name = "MorphBack"
		backButton.Parent = scriptSearch
		backButton.AutoButtonColor = false
		backButton.BackgroundTransparency = 1
		backButton.Position = UDim2.new(0, 24, 0, 22)
		backButton.Size = UDim2.new(0, 34, 0, 28)
		backButton.Text = ""
		backButton.Visible = false
		backButton.ZIndex = 46

		local backIcon = track(Instance.new("ImageLabel"))
		backIcon.Name = "BackArrowIcon"
		backIcon.Parent = backButton
		backIcon.AnchorPoint = Vector2.new(0.5, 0.5)
		backIcon.BackgroundTransparency = 1
		backIcon.Image = settingsPanel.Back.Image
		backIcon.ImageColor3 = settingsPanel.Back.ImageColor3
		backIcon.ImageRectOffset = settingsPanel.Back.ImageRectOffset
		backIcon.ImageRectSize = settingsPanel.Back.ImageRectSize
		backIcon.ImageTransparency = 1
		backIcon.Position = UDim2.new(0.5, 0, 0.5, 0)
		backIcon.ScaleType = settingsPanel.Back.ScaleType
		backIcon.Size = UDim2.new(0, 30, 0, 30)
		backIcon.Visible = false
		backIcon.ZIndex = 47

		local title = makeText(scriptSearch, "MorphTitle", "ScriptSearch", UDim2.new(0, 28, 0, 22), UDim2.new(1, -56, 0, 24), Enum.Font.GothamBold, 19, scriptSearchBrand.text, 44)
		local subtitle = makeText(scriptSearch, "MorphSubtitle", "", UDim2.new(0, 28, 0, 50), UDim2.new(1, -56, 0, 0), Enum.Font.Gotham, 1, scriptSearchBrand.muted, 44)
		local headerAccent = makeAccentFrame(scriptSearch, "MorphHeaderAccent", UDim2.new(0, 28, 1, -2), UDim2.new(0, 0, 0, 2), 44, accentColor)
		headerAccent.Visible = false

		local panelStroke = track(Instance.new("UIStroke"))
		panelStroke.Name = "MorphPanelStroke"
		panelStroke.Parent = scriptSearch
		panelStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
		panelStroke.Color = accentColor
		panelStroke.Thickness = 1.25
		panelStroke.Transparency = 1

		local panelStrokeGradient = Instance.new("UIGradient")
		panelStrokeGradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, accentColor:Lerp(Color3.fromRGB(255, 255, 255), 0.18)),
			ColorSequenceKeypoint.new(0.44, accentColor:Lerp(scriptSearchBrand.base, 0.45)),
			ColorSequenceKeypoint.new(0.74, accentColor:Lerp(scriptSearchBrand.base, 0.1)),
			ColorSequenceKeypoint.new(1, accentColor:Lerp(Color3.fromRGB(255, 255, 255), 0.12))
		})
		panelStrokeGradient.Rotation = 38
		panelStrokeGradient.Offset = Vector2.new(-0.32, -0.18)
		panelStrokeGradient.Parent = panelStroke

		local scriptCard, scriptCardStroke = makeCard("MorphScriptCard", UDim2.new(0, 24, 0, 58), UDim2.new(1, -48, 0, 100))
		local scriptCardGradient = Instance.new("UIGradient")
		scriptCardGradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, scriptSearchBrand.cardSoft),
			ColorSequenceKeypoint.new(0.78, scriptSearchBrand.card),
			ColorSequenceKeypoint.new(1, scriptSearchBrand.base)
		})
		scriptCardGradient.Rotation = 90
		scriptCardGradient.Offset = Vector2.new(0, 1)
		scriptCardGradient.Parent = scriptCard
		local cardAccent = makeAccentFrame(scriptCard, "MorphCardAccent", UDim2.new(0, 18, 1, -13), UDim2.new(0, 0, 0, 2), 45, accentColor)
		cardAccent.Visible = false
		local preview = track(Instance.new("ImageLabel"))
		preview.Name = "MorphScriptPreview"
		preview.Parent = scriptCard
		preview.BackgroundColor3 = Color3.fromRGB(10, 11, 13)
		preview.BackgroundTransparency = 1
		preview.ImageTransparency = 1
		preview.ScaleType = Enum.ScaleType.Crop
		preview.Size = UDim2.new(0, 86, 0, 86)
		preview.Position = UDim2.new(0, 0, 0, 8)
		preview.ZIndex = 44

		local previewCorner = Instance.new("UICorner")
		previewCorner.CornerRadius = UDim.new(0, 10)
		previewCorner.Parent = preview

		local previewStroke = Instance.new("UIStroke")
		previewStroke.Color = accentSoft
		previewStroke.Transparency = 1
		previewStroke.Parent = preview

		local textOffset = imageUrl and 110 or 0
		local scriptName = makeText(scriptCard, "MorphScriptName", scriptTitle, UDim2.new(0, textOffset, 0, 10), UDim2.new(1, -textOffset - 8, 0, 26), Enum.Font.GothamBold, 19, Color3.fromRGB(244, 246, 249), 44)
		scriptName.TextTruncate = Enum.TextTruncate.AtEnd
		local description = makeText(scriptCard, "MorphDescription", "Review this source before running. Sirius will execute code from "..providerName.." in your current session.", UDim2.new(0, textOffset, 0, 41), UDim2.new(1, -textOffset - 8, 0, 34), Enum.Font.Gotham, 12, Color3.fromRGB(184, 188, 197), 44)
		local sourcePill = track(Instance.new("Frame"))
		sourcePill.Name = "MorphSourcePill"
		sourcePill.Parent = scriptCard
		sourcePill.BackgroundColor3 = Color3.fromRGB(10, 11, 13)
		sourcePill.BackgroundTransparency = 1
		sourcePill.BorderSizePixel = 0
		sourcePill.Position = UDim2.new(0, textOffset, 0, 78)
		sourcePill.Size = UDim2.new(1, -textOffset - 8, 0, 18)
		sourcePill.ZIndex = 44
		local sourcePillCorner = Instance.new("UICorner")
		sourcePillCorner.CornerRadius = UDim.new(1, 0)
		sourcePillCorner.Parent = sourcePill
		local sourcePillStroke = Instance.new("UIStroke")
		sourcePillStroke.Color = accentSoft
		sourcePillStroke.Transparency = 1
		sourcePillStroke.Parent = sourcePill
		local sourceLabel = makeText(sourcePill, "MorphSource", "Source  •  "..providerName, UDim2.new(0, 0, 0, 0), UDim2.new(1, 0, 1, 0), Enum.Font.GothamMedium, 11, Color3.fromRGB(190, 196, 204), 45)

		if imageUrl then
			preview.Image = imageUrl
			task.spawn(function()
				local cachedImage = getCachedScriptImage(imageUrl)

				if preview.Parent and cachedImage then
					preview.Image = cachedImage
				end
			end)
		else
			preview.Visible = false
		end

		local function destroyPromptObjects()
			for _, object in ipairs(createdObjects) do
				if object and object.Parent then
					object:Destroy()
				end
			end
		end

		local function restoreSearchPanel()
			scriptSearch.UIGradient.Color = previous.gradientColor
			scriptSearch.UIGradient.Offset = previous.gradientOffset
			scriptSearch.UIGradient.Rotation = previous.gradientRotation
			scriptSearch.UIGradient.Enabled = previous.gradientEnabled
			restoreMorphShadowGradient()
			scriptSearch.Position = previous.position
			scriptSearch.Icon.Visible = previous.iconVisible
			scriptSearch.SearchBox.Visible = previous.searchBoxVisible
			scriptSearch.List.Visible = previous.listVisible
			if scriptSaveState.topButton then scriptSaveState.topButton.Visible = previous.topButtonVisible end
			if scriptSaveState.header then scriptSaveState.header.Visible = previous.headerVisible end

			local restoreMotion = TweenInfo.new(0.42, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
			tweenService:Create(scriptSearch, restoreMotion, {Size = previous.size, BackgroundTransparency = previous.backgroundTransparency}):Play()
			tweenService:Create(scriptSearch.Shadow, restoreMotion, {ImageTransparency = previous.shadowTransparency}):Play()
			tweenService:Create(scriptSearch.Icon, restoreMotion, {ImageTransparency = 0}):Play()
			tweenService:Create(scriptSearch.SearchBox, restoreMotion, {TextTransparency = 0}):Play()
			tweenService:Create(scriptSearch.NoScriptsTitle, restoreMotion, {TextTransparency = previous.noTitleTransparency}):Play()
			tweenService:Create(scriptSearch.NoScriptsDesc, restoreMotion, {TextTransparency = previous.noDescTransparency}):Play()
			if scriptSaveState.topButton and previous.topButtonVisible and scriptSaveState.topButton:FindFirstChild("BookmarkVisual") then
				tweenService:Create(scriptSaveState.topButton, restoreMotion, {BackgroundTransparency = 1}):Play()
				scriptSaveState.setBookmarkVisual(scriptSaveState.topButton.BookmarkVisual, false, false)
			end
			if scriptSaveState.header and previous.headerVisible then
				tweenService:Create(scriptSaveState.header.Title, restoreMotion, {TextTransparency = 0}):Play()
				tweenService:Create(scriptSaveState.header.Description, restoreMotion, {TextTransparency = 0.12}):Play()
				tweenService:Create(scriptSaveState.header.Back.BackArrowIcon, restoreMotion, {ImageTransparency = 0}):Play()
			end
		end

		local function fadePromptObjects()
			local fadeMotion = TweenInfo.new(0.2, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

			for _, object in ipairs(createdObjects) do
				if object:IsA("TextLabel") or object:IsA("TextButton") or object:IsA("TextBox") then
					tweenService:Create(object, fadeMotion, {TextTransparency = 1, BackgroundTransparency = 1}):Play()
				elseif object:IsA("ImageLabel") or object:IsA("ImageButton") then
					tweenService:Create(object, fadeMotion, {ImageTransparency = 1, BackgroundTransparency = 1}):Play()
				elseif object:IsA("ScrollingFrame") then
					tweenService:Create(object, fadeMotion, {BackgroundTransparency = 1, ScrollBarImageTransparency = 1}):Play()
				elseif object:IsA("Frame") then
					tweenService:Create(object, fadeMotion, {BackgroundTransparency = 1}):Play()
				elseif object:IsA("UIStroke") then
					tweenService:Create(object, fadeMotion, {Transparency = 1}):Play()
				end

				if object:FindFirstChildOfClass("UIStroke") then
					tweenService:Create(object:FindFirstChildOfClass("UIStroke"), fadeMotion, {Transparency = 1}):Play()
				end
			end
		end

		local function finishPrompt(decisionValue)
			if promptClosing then return end
			promptClosing = true
			decision = decisionValue
			fadePromptObjects()

			task.wait(0.16)
			if scriptSaveState.promptToken ~= morphToken then
				destroyPromptObjects()
				debounce = false
				promptClosed = true
				return
			end
			destroyPromptObjects()

			if decisionValue then
				scriptSearch.UIGradient.Color = previous.gradientColor
				scriptSearch.UIGradient.Offset = previous.gradientOffset
				scriptSearch.UIGradient.Rotation = previous.gradientRotation
				scriptSearch.UIGradient.Enabled = previous.gradientEnabled
				restoreMorphShadowGradient()
				scriptSearch.ClipsDescendants = previous.clips
				scriptSaveState.transition = "open"
				debounce = false
				promptClosed = true
				return
			end

			restoreSearchPanel()
			task.wait(0.42)
			scriptSearch.ClipsDescendants = previous.clips
			scriptSaveState.transition = "open"
			debounce = false
			promptClosed = true
		end

		backButton.MouseButton1Click:Connect(function()
			finishPrompt(false)
		end)

		local function createMorphButton(name, text, position, size, backgroundColor, decisionValue)
			local button = track(Instance.new("TextButton"))
			button.Name = "Morph"..name
			button.Parent = scriptSearch
			button.AutoButtonColor = false
			button.BackgroundColor3 = backgroundColor
			button.BackgroundTransparency = 1
			button.BorderSizePixel = 0
			button.Font = Enum.Font.GothamBold
			button.Position = position
			button.Size = size
			button.Text = text
			button.TextColor3 = Color3.fromRGB(255, 255, 255)
			button.TextSize = 15
			button.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
			button.TextStrokeTransparency = 0.82
			button.TextTransparency = 1
			button.ZIndex = 44

			local corner = Instance.new("UICorner")
			corner.CornerRadius = UDim.new(0, 8)
			corner.Parent = button

			local stroke = Instance.new("UIStroke")
			stroke.Color = decisionValue and accentColor:Lerp(Color3.fromRGB(255, 255, 255), 0.22) or scriptSearchBrand.stroke
			stroke.Transparency = 1
			stroke.Parent = button

			local buttonGradient = Instance.new("UIGradient")
			buttonGradient.Color = decisionValue and ColorSequence.new({
					ColorSequenceKeypoint.new(0, accentColor:Lerp(Color3.fromRGB(255, 255, 255), 0.08)),
					ColorSequenceKeypoint.new(1, accentColor:Lerp(Color3.fromRGB(5, 7, 9), 0.34))
			}) or ColorSequence.new({
				ColorSequenceKeypoint.new(0, Color3.fromRGB(27, 28, 31)),
				ColorSequenceKeypoint.new(1, Color3.fromRGB(18, 19, 21))
			})
			buttonGradient.Rotation = 90
			buttonGradient.Offset = Vector2.new(0, decisionValue and 0.25 or 0)
			buttonGradient.Parent = button
			buttonGradient.Enabled = false

			local buttonScale = Instance.new("UIScale")
			buttonScale.Scale = 1
			buttonScale.Parent = button

			button.MouseEnter:Connect(function()
				local hoverTween = TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
				button.TextColor3 = Color3.fromRGB(255, 255, 255)
				button.TextStrokeTransparency = 0.72
				tweenService:Create(button, hoverTween, {BackgroundTransparency = decisionValue and 0.04 or 0.08, TextTransparency = 0}):Play()
				tweenService:Create(stroke, hoverTween, {Transparency = decisionValue and 0.42 or 0.58}):Play()
				tweenService:Create(buttonGradient, hoverTween, {Offset = Vector2.new(0, decisionValue and -0.08 or -0.05)}):Play()
				tweenService:Create(buttonScale, hoverTween, {Scale = 1}):Play()
			end)

			button.MouseLeave:Connect(function()
				local idleTween = TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
				button.TextColor3 = Color3.fromRGB(255, 255, 255)
				button.TextStrokeTransparency = 0.82
				tweenService:Create(button, idleTween, {BackgroundTransparency = decisionValue and 0.1 or 0.16, TextTransparency = 0}):Play()
				tweenService:Create(stroke, idleTween, {Transparency = decisionValue and 0.58 or 0.72}):Play()
				tweenService:Create(buttonGradient, idleTween, {Offset = Vector2.new(0, decisionValue and 0.25 or 0)}):Play()
				tweenService:Create(buttonScale, idleTween, {Scale = 1}):Play()
			end)

			button.MouseButton1Click:Connect(function()
				if promptClosing or decision ~= nil then return end

				tweenService:Create(buttonScale, TweenInfo.new(0.08, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Scale = 0.94}):Play()
				task.wait(0.08)
				tweenService:Create(buttonScale, TweenInfo.new(0.18, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = 1}):Play()
				task.wait(0.05)
				finishPrompt(decisionValue)
			end)

			return button
		end

		local cancelButton = createMorphButton("Cancel", "Cancel", UDim2.new(1, -248, 1, -48), UDim2.new(0, 104, 0, 34), Color3.fromRGB(22, 23, 25), false)
		local runButton = createMorphButton("Run", "Run", UDim2.new(1, -132, 1, -48), UDim2.new(0, 104, 0, 34), accentColor, true)

		local openMotion = TweenInfo.new(0.58, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
		local contentMotion = TweenInfo.new(0.44, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
		tweenService:Create(scriptSearch, openMotion, {Position = UDim2.new(0.5, 0, 0.5, 0), Size = UDim2.new(0, 520, 0, 205), BackgroundTransparency = 0.02}):Play()
		tweenService:Create(scriptSearch.Shadow, openMotion, {ImageTransparency = 0.86}):Play()
		tweenService:Create(scriptSearch.UIGradient, openMotion, {Offset = Vector2.new(0, 0.5)}):Play()
		if morphShadowGradient then
			tweenService:Create(morphShadowGradient, openMotion, {Offset = Vector2.new(0, 0.38)}):Play()
		end

		task.wait(0.05)
		tweenService:Create(backButton.BackArrowIcon, contentMotion, {ImageTransparency = 0}):Play()
		tweenService:Create(title, contentMotion, {TextTransparency = 0}):Play()
		tweenService:Create(subtitle, contentMotion, {TextTransparency = 1}):Play()
		tweenService:Create(headerAccent, TweenInfo.new(0.52, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 1}):Play()
		tweenService:Create(panelStroke, TweenInfo.new(0.58, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.24}):Play()
		tweenService:Create(panelStrokeGradient, TweenInfo.new(0.72, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Offset = Vector2.new(-0.08, -0.04)}):Play()

		task.wait(0.05)
		for _, cardData in ipairs({
			{scriptCard, scriptCardStroke}
		}) do
			tweenService:Create(cardData[1], contentMotion, {BackgroundTransparency = 1}):Play()
			tweenService:Create(cardData[2], contentMotion, {Transparency = 1}):Play()
		end
		tweenService:Create(scriptCardGradient, TweenInfo.new(0.72, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Offset = Vector2.new(0, 0.3)}):Play()
		tweenService:Create(preview, contentMotion, {ImageTransparency = preview.Visible and 0.08 or 1, BackgroundTransparency = 0.84}):Play()
		tweenService:Create(previewStroke, contentMotion, {Transparency = preview.Visible and 0.72 or 1}):Play()
		tweenService:Create(scriptName, contentMotion, {TextTransparency = 0}):Play()
		tweenService:Create(description, contentMotion, {TextTransparency = 0.14}):Play()
		tweenService:Create(sourcePill, contentMotion, {BackgroundTransparency = 1}):Play()
		tweenService:Create(sourcePillStroke, contentMotion, {Transparency = 1}):Play()
		tweenService:Create(sourceLabel, contentMotion, {TextTransparency = 0.2}):Play()

		task.wait(0.08)
		cancelButton.TextColor3 = Color3.fromRGB(255, 255, 255)
		cancelButton.TextStrokeTransparency = 0.82
		runButton.TextColor3 = Color3.fromRGB(255, 255, 255)
		runButton.TextStrokeTransparency = 0.82
		tweenService:Create(cancelButton, contentMotion, {BackgroundTransparency = 0.16, TextTransparency = 0}):Play()
		tweenService:Create(cancelButton.UIStroke, contentMotion, {Transparency = 0.72}):Play()
		tweenService:Create(runButton, contentMotion, {BackgroundTransparency = 0.1, TextTransparency = 0}):Play()
		tweenService:Create(runButton.UIStroke, contentMotion, {Transparency = 0.58}):Play()

		repeat task.wait() until decision ~= nil or scriptSaveState.promptToken ~= morphToken or not scriptSearch.Visible
		if decision == nil then
			destroyPromptObjects()
			debounce = false
			return false
		end

		repeat task.wait() until promptClosed or scriptSaveState.promptToken ~= morphToken or not scriptSearch.Visible
		return decision
	end

end

local function confirmScriptRun(scriptTitle, providerName, imageUrl, result)
	if not checkSirius() then return true end
	if scriptSearch.Visible then
		local morphDecision = confirmScriptRunMorph(scriptTitle, providerName, imageUrl, result)
		if morphDecision ~= nil then
			return morphDecision
		end
	end

	local accentColor = Color3.fromRGB(166, 42, 58)
	local accentSoft = accentColor:Lerp(Color3.fromRGB(255, 255, 255), 0.22)
	local accentDeep = accentColor:Lerp(scriptSearchBrand.base, 0.58)

	local overlay = Instance.new("TextButton")
	overlay.Name = "RunScriptConfirmationOverlay"
	overlay.Parent = UI
	overlay.AutoButtonColor = false
	overlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	overlay.BackgroundTransparency = 1
	overlay.BorderSizePixel = 0
	overlay.Size = UDim2.new(1, 0, 1, 0)
	overlay.Text = ""
	overlay.Visible = true
	overlay.ZIndex = 900

	local newPrompt = Instance.new("Frame")
	newPrompt.Name = "RunScriptConfirmation"
	newPrompt.Parent = overlay
	newPrompt.AnchorPoint = Vector2.new(0.5, 0.5)
	newPrompt.BackgroundColor3 = scriptSearchBrand.panel
	newPrompt.BackgroundTransparency = 1
	newPrompt.BorderSizePixel = 0
	newPrompt.ClipsDescendants = true
	newPrompt.Position = UDim2.new(0.5, 0, 0.5, 12)
	newPrompt.Size = UDim2.new(0, 500, 0, 0)
	newPrompt.ZIndex = 901

	local promptScale = Instance.new("UIScale")
	promptScale.Scale = 0.96
	promptScale.Parent = newPrompt

	local promptCorner = Instance.new("UICorner")
	promptCorner.CornerRadius = UDim.new(0, 14)
	promptCorner.Parent = newPrompt

	local promptStroke = Instance.new("UIStroke")
	promptStroke.Color = Color3.fromRGB(255, 255, 255)
	promptStroke.Transparency = 1
	promptStroke.Parent = newPrompt

	local promptStrokeGradient = Instance.new("UIGradient")
	promptStrokeGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, accentSoft),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(30, 30, 30))
	})
	promptStrokeGradient.Rotation = 45
	promptStrokeGradient.Offset = Vector2.new(-0.42, -0.28)
	promptStrokeGradient.Parent = promptStroke

	local promptGradient = Instance.new("UIGradient")
	promptGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, scriptSearchBrand.panel),
		ColorSequenceKeypoint.new(0.55, scriptSearchBrand.base),
		ColorSequenceKeypoint.new(0.84, accentDeep),
		ColorSequenceKeypoint.new(1, accentColor:Lerp(Color3.fromRGB(0, 0, 0), 0.36))
	})
	promptGradient.Rotation = 92
	promptGradient.Offset = Vector2.new(0, 1.2)
	promptGradient.Parent = newPrompt

	local promptShadow = Instance.new("ImageLabel")
	promptShadow.Name = "Shadow"
	promptShadow.Parent = overlay
	promptShadow.AnchorPoint = Vector2.new(0.5, 0.5)
	promptShadow.BackgroundTransparency = 1
	promptShadow.Image = "rbxassetid://8992230677"
	promptShadow.ImageColor3 = Color3.fromRGB(255, 255, 255)
	promptShadow.ImageTransparency = 1
	promptShadow.Position = UDim2.new(0.5, 0, 0.5, 28)
	promptShadow.Size = UDim2.new(0, 590, 0, 260)
	promptShadow.ScaleType = Enum.ScaleType.Slice
	promptShadow.SliceCenter = Rect.new(99, 99, 99, 99)
	promptShadow.ZIndex = 899

	local promptShadowGradient = Instance.new("UIGradient")
	promptShadowGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, accentSoft),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(30, 30, 30))
	})
	promptShadowGradient.Rotation = 45
	promptShadowGradient.Offset = Vector2.new(-0.52, -0.32)
	promptShadowGradient.Parent = promptShadow

	local preview = Instance.new("ImageLabel")
	preview.Name = "ScriptPreview"
	preview.Parent = newPrompt
	preview.BackgroundColor3 = Color3.fromRGB(26, 26, 30)
	preview.BackgroundTransparency = 1
	preview.ImageTransparency = 1
	preview.ScaleType = Enum.ScaleType.Crop
	preview.Size = UDim2.new(0, 86, 0, 86)
	preview.Position = UDim2.new(0, 24, 0, 72)
	preview.ZIndex = 902

	local previewCorner = Instance.new("UICorner")
	previewCorner.CornerRadius = UDim.new(0, 10)
	previewCorner.Parent = preview

	local previewStroke = Instance.new("UIStroke")
	previewStroke.Color = accentSoft
	previewStroke.Transparency = 1
	previewStroke.Parent = preview

	local title = Instance.new("TextLabel")
	title.Name = "Title"
	title.Parent = newPrompt
	title.BackgroundTransparency = 1
	title.Font = Enum.Font.GothamBold
	title.Position = UDim2.new(0, 24, 0, 22)
	title.Size = UDim2.new(1, -48, 0, 24)
	title.Text = "ScriptSearch"
	title.TextColor3 = Color3.fromRGB(255, 255, 255)
	title.TextSize = 19
	title.TextTransparency = 1
	title.TextXAlignment = Enum.TextXAlignment.Left
	title.ZIndex = 902

	local scriptName = Instance.new("TextLabel")
	scriptName.Name = "ScriptName"
	scriptName.Parent = newPrompt
	scriptName.BackgroundTransparency = 1
	scriptName.Font = Enum.Font.GothamBold
	scriptName.Position = UDim2.new(0, 132, 0, 70)
	scriptName.Size = UDim2.new(1, -158, 0, 28)
	scriptName.Text = scriptTitle
	scriptName.TextColor3 = Color3.fromRGB(235, 235, 240)
	scriptName.TextSize = 19
	scriptName.TextTransparency = 1
	scriptName.TextTruncate = Enum.TextTruncate.AtEnd
	scriptName.TextXAlignment = Enum.TextXAlignment.Left
	scriptName.ZIndex = 902

	local description = Instance.new("TextLabel")
	description.Name = "Description"
	description.Parent = newPrompt
	description.BackgroundTransparency = 1
	description.Font = Enum.Font.Gotham
	description.Position = UDim2.new(0, 132, 0, 101)
	description.Size = UDim2.new(1, -158, 0, 46)
	description.Text = "This will execute in the current Roblox session. Continue only when you trust the script and source."
	description.TextColor3 = Color3.fromRGB(178, 178, 186)
	description.TextSize = 13
	description.TextTransparency = 1
	description.TextWrapped = true
	description.TextXAlignment = Enum.TextXAlignment.Left
	description.TextYAlignment = Enum.TextYAlignment.Top
	description.ZIndex = 902

	local sourceLabel = Instance.new("TextLabel")
	sourceLabel.Name = "Source"
	sourceLabel.Parent = newPrompt
	sourceLabel.BackgroundTransparency = 1
	sourceLabel.Font = Enum.Font.GothamMedium
	sourceLabel.Position = UDim2.new(0, 132, 0, 150)
	sourceLabel.Size = UDim2.new(1, -158, 0, 20)
	sourceLabel.Text = "Source • "..providerName
	sourceLabel.TextColor3 = Color3.fromRGB(185, 185, 194)
	sourceLabel.TextSize = 12
	sourceLabel.TextTransparency = 1
	sourceLabel.TextXAlignment = Enum.TextXAlignment.Left
	sourceLabel.ZIndex = 902

	if imageUrl then
		preview.Image = imageUrl
		task.spawn(function()
			local cachedImage = getCachedScriptImage(imageUrl)

			if preview.Parent and cachedImage then
				preview.Image = cachedImage
			end
		end)
	else
		preview.Visible = false
		scriptName.Position = UDim2.new(0, 24, 0, 70)
		scriptName.Size = UDim2.new(1, -48, 0, 28)
		description.Position = UDim2.new(0, 24, 0, 101)
		description.Size = UDim2.new(1, -48, 0, 46)
		sourceLabel.Position = UDim2.new(0, 24, 0, 150)
		sourceLabel.Size = UDim2.new(1, -48, 0, 20)
	end

	local decision
	local promptClosing = false
	local promptClosed = false

	local function closePrompt()
		if promptClosing then return end
		promptClosing = true

		local closeMotion = TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut)
		local contentFade = TweenInfo.new(0.18, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

		tweenService:Create(overlay, TweenInfo.new(0.3, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
		tweenService:Create(newPrompt, closeMotion, {Position = UDim2.new(0.5, 0, 0.5, 12), Size = UDim2.new(0, 500, 0, 178), BackgroundTransparency = 1}):Play()
		tweenService:Create(promptScale, closeMotion, {Scale = 0.96}):Play()
		tweenService:Create(promptGradient, TweenInfo.new(0.34, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 1.2)}):Play()
		tweenService:Create(promptStrokeGradient, TweenInfo.new(0.34, Enum.EasingStyle.Quint), {Offset = Vector2.new(-0.42, -0.28)}):Play()
		tweenService:Create(promptShadow, closeMotion, {Position = UDim2.new(0.5, 0, 0.5, 22), ImageTransparency = 1, Size = UDim2.new(0, 590, 0, 260)}):Play()
		tweenService:Create(promptShadowGradient, TweenInfo.new(0.34, Enum.EasingStyle.Quint), {Offset = Vector2.new(-0.52, -0.32)}):Play()
		tweenService:Create(promptStroke, TweenInfo.new(0.25, Enum.EasingStyle.Quint), {Transparency = 1}):Play()
		tweenService:Create(title, contentFade, {TextTransparency = 1}):Play()
		tweenService:Create(scriptName, contentFade, {TextTransparency = 1}):Play()
		tweenService:Create(description, contentFade, {TextTransparency = 1}):Play()
		tweenService:Create(sourceLabel, contentFade, {TextTransparency = 1}):Play()
		tweenService:Create(preview, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {ImageTransparency = 1, BackgroundTransparency = 1}):Play()
		tweenService:Create(previewStroke, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {Transparency = 1}):Play()

		for _, button in ipairs(newPrompt:GetChildren()) do
			if button.ClassName == "TextButton" then
				tweenService:Create(button, contentFade, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
				if button:FindFirstChild("UIStroke") then
					tweenService:Create(button.UIStroke, contentFade, {Transparency = 1}):Play()
				end
			end
		end

		task.wait(0.34)
		overlay:Destroy()
		promptClosed = true
	end

	local function createActionButton(name, text, position, size, backgroundColor, decisionValue)
		local button = Instance.new("TextButton")
		button.Name = name
		button.Parent = newPrompt
		button.AutoButtonColor = false
		button.BackgroundColor3 = backgroundColor
		button.BackgroundTransparency = 1
		button.BorderSizePixel = 0
		button.Font = Enum.Font.GothamBold
		button.Position = position
		button.Size = size
		button.Text = text
		button.TextColor3 = Color3.fromRGB(255, 255, 255)
		button.TextSize = 15
		button.TextTransparency = 1
		button.ZIndex = 902

		local corner = Instance.new("UICorner")
			corner.CornerRadius = UDim.new(0, 10)
			corner.Parent = button

	local stroke = Instance.new("UIStroke")
	stroke.Color = decisionValue and accentSoft or scriptSearchBrand.stroke
	stroke.Transparency = 1
	stroke.Parent = button

		local buttonScale = Instance.new("UIScale")
		buttonScale.Scale = 1
		buttonScale.Parent = button

	button.MouseEnter:Connect(function()
		local hoverTween = TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
		tweenService:Create(button, hoverTween, {BackgroundTransparency = decisionValue and 0 or 0.04, TextColor3 = Color3.fromRGB(255, 255, 255), TextTransparency = 0}):Play()
		tweenService:Create(stroke, hoverTween, {Transparency = decisionValue and 0.46 or 0.64}):Play()
		tweenService:Create(buttonScale, hoverTween, {Scale = 1}):Play()
	end)

	button.MouseLeave:Connect(function()
		local idleTween = TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
		tweenService:Create(button, idleTween, {BackgroundTransparency = decisionValue and 0.08 or 0.14, TextColor3 = Color3.fromRGB(255, 255, 255), TextTransparency = 0}):Play()
		tweenService:Create(stroke, idleTween, {Transparency = decisionValue and 0.64 or 0.78}):Play()
		tweenService:Create(buttonScale, idleTween, {Scale = 1}):Play()
	end)

		button.MouseButton1Click:Connect(function()
			if promptClosing or decision ~= nil then return end

			local pressTween = TweenInfo.new(0.08, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
			local releaseTween = TweenInfo.new(0.18, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
			tweenService:Create(buttonScale, pressTween, {Scale = 0.94}):Play()
			task.wait(0.08)
			tweenService:Create(buttonScale, releaseTween, {Scale = 1}):Play()
			task.wait(0.05)
			decision = decisionValue
			closePrompt()
		end)

		return button
	end

local cancelButton = createActionButton("Cancel", "Cancel", UDim2.new(1, -248, 1, -50), UDim2.new(0, 104, 0, 34), Color3.fromRGB(22, 23, 25), false)
local runButton = createActionButton("Run", "Run", UDim2.new(1, -132, 1, -50), UDim2.new(0, 112, 0, 34), accentColor, true)

	local openMotion = TweenInfo.new(0.58, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
	local glowMotion = TweenInfo.new(0.72, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
	local contentMotion = TweenInfo.new(0.44, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

	tweenService:Create(overlay, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
	tweenService:Create(newPrompt, openMotion, {Position = UDim2.new(0.5, 0, 0.5, 0), Size = UDim2.new(0, 520, 0, 205), BackgroundTransparency = 0.04}):Play()
	tweenService:Create(promptScale, openMotion, {Scale = 1}):Play()
	tweenService:Create(promptGradient, glowMotion, {Offset = Vector2.new(0, 0.5)}):Play()
	tweenService:Create(promptStrokeGradient, glowMotion, {Offset = Vector2.new(-0.12, -0.06)}):Play()
	tweenService:Create(promptShadow, glowMotion, {Position = UDim2.new(0.5, 0, 0.5, 9), ImageTransparency = 0.72, Size = UDim2.new(0, 620, 0, 292)}):Play()
	tweenService:Create(promptShadowGradient, glowMotion, {Offset = Vector2.new(-0.16, -0.08)}):Play()
	tweenService:Create(promptStroke, openMotion, {Transparency = 0.18}):Play()
	task.wait(0.04)
	tweenService:Create(title, contentMotion, {TextTransparency = 0}):Play()
	tweenService:Create(scriptName, contentMotion, {TextTransparency = 0}):Play()
	tweenService:Create(description, contentMotion, {TextTransparency = 0.14}):Play()
	tweenService:Create(sourceLabel, contentMotion, {TextTransparency = 0.22}):Play()

	if preview.Visible then
		tweenService:Create(preview, contentMotion, {ImageTransparency = 0.08, BackgroundTransparency = 0.86}):Play()
		tweenService:Create(previewStroke, contentMotion, {Transparency = 0.68}):Play()
	end

	task.wait(0.08)

tweenService:Create(cancelButton, contentMotion, {BackgroundTransparency = 0.14, TextColor3 = Color3.fromRGB(255, 255, 255), TextTransparency = 0}):Play()
tweenService:Create(cancelButton.UIStroke, contentMotion, {Transparency = 0.78}):Play()
tweenService:Create(runButton, contentMotion, {BackgroundTransparency = 0.08, TextColor3 = Color3.fromRGB(255, 255, 255), TextTransparency = 0}):Play()
tweenService:Create(runButton.UIStroke, contentMotion, {Transparency = 0.64}):Play()

	repeat task.wait() until decision ~= nil
	repeat task.wait() until promptClosed or not overlay.Parent
	return decision
end

function scriptSaveState.getPath()
	return siriusValues.siriusFolder.."/"..siriusValues.scriptSavedFile
end

function scriptSaveState.getId(result)
	return tostring(result.provider or "ScriptBlox").."|"..tostring(result.slug or result.rawScriptUrl or result.detailUrl or result.title or result.name or "Untitled")
end

function scriptSaveState.serialise(result)
	return {
		provider = result.provider,
		title = result.title or result.name,
		description = result.description,
		author = result.author,
		verified = result.verified,
		likes = tonumber(result.likes) or 0,
		dislikes = tonumber(result.dislikes) or 0,
		slug = result.slug,
		script = result.script,
		rawScriptUrl = result.rawScriptUrl,
		detailUrl = result.detailUrl,
		image = result.image,
		thumbUrl = result.thumbUrl,
		thumb = result.thumb,
		isPatched = result.isPatched or false
	}
end

function scriptSaveState.rebuildIndex()
	scriptSaveState.index = {}

	for _, savedScript in ipairs(scriptSaveState.cache or {}) do
		scriptSaveState.index[scriptSaveState.getId(savedScript)] = true
	end
end

function scriptSaveState.load()
	if scriptSaveState.cache then
		return scriptSaveState.cache
	end

	scriptSaveState.cache = {}

	if isfile and readfile then
		local success, decoded = pcall(function()
			if isfile(scriptSaveState.getPath()) then
				return httpService:JSONDecode(readfile(scriptSaveState.getPath()))
			end
		end)

		if success and type(decoded) == "table" then
			scriptSaveState.cache = decoded
		end
	end

	scriptSaveState.rebuildIndex()
	return scriptSaveState.cache
end

function scriptSaveState.save()
	if writefile then
		return pcall(function()
			checkFolder()
			writefile(scriptSaveState.getPath(), httpService:JSONEncode(scriptSaveState.cache or {}))
		end)
	end

	return true
end

function scriptSaveState.isSaved(result)
	scriptSaveState.load()
	return scriptSaveState.index[scriptSaveState.getId(result)] == true
end

function scriptSaveState.toggle(result)
	local savedScripts = scriptSaveState.load()
	local scriptId = scriptSaveState.getId(result)

	for index, savedScript in ipairs(savedScripts) do
		if scriptSaveState.getId(savedScript) == scriptId then
			table.remove(savedScripts, index)
			scriptSaveState.rebuildIndex()
			scriptSaveState.save()
			return false
		end
	end

	table.insert(savedScripts, scriptSaveState.serialise(result))
	scriptSaveState.rebuildIndex()
	scriptSaveState.save()
	return true
end

function scriptSaveState.createLine(parent, name, position, size, rotation, color, zIndex)
	local line = Instance.new("Frame")
	line.Name = name
	line.Parent = parent
	line.BackgroundColor3 = color or Color3.fromRGB(255, 255, 255)
	line.BackgroundTransparency = 1
	line.BorderSizePixel = 0
	line.Position = position
	line.Rotation = rotation or 0
	line.Size = size
	line.ZIndex = zIndex

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(1, 0)
	corner.Parent = line

	return line
end

function scriptSaveState.createBookmarkVisual(parent, zIndex)
	local icon = Instance.new("ImageLabel")
	icon.Name = "BookmarkVisual"
	icon.Parent = parent
	icon.BackgroundTransparency = 1
	icon.Image = scriptSaveState.outlineIcon
	icon.ImageColor3 = Color3.fromRGB(255, 255, 255)
	icon.ImageTransparency = 1
	icon.ScaleType = Enum.ScaleType.Fit
	icon.Size = scriptSaveState.outlineIconSize
	icon.ZIndex = zIndex + 10

	return icon
end

function scriptSaveState.setBookmarkVisual(icon, saved, instant)
	if not icon then return end

	icon.Image = saved and scriptSaveState.filledIcon or scriptSaveState.outlineIcon
	icon.ImageColor3 = Color3.fromRGB(255, 255, 255)
	icon.Size = saved and scriptSaveState.filledIconSize or scriptSaveState.outlineIconSize
	icon.Visible = true
	icon.ZIndex = math.max(icon.ZIndex, (icon.Parent and icon.Parent.ZIndex or 1) + 10)

	if instant then
		icon.ImageTransparency = 0
	else
		tweenService:Create(icon, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {ImageTransparency = 0}):Play()
	end
end

function scriptSaveState.createBackArrow(parent, zIndex)
	local root = Instance.new("Frame")
	root.Name = "BackArrow"
	root.Parent = parent
	root.BackgroundTransparency = 1
	root.Position = UDim2.new(0.5, -13, 0.5, -12)
	root.Size = UDim2.new(0, 28, 0, 24)
	root.ZIndex = zIndex

	scriptSaveState.createLine(root, "Shaft", UDim2.new(0, 5, 0, 11), UDim2.new(0, 22, 0, 3), 0, nil, zIndex)
	scriptSaveState.createLine(root, "HeadTop", UDim2.new(0, 4, 0, 6), UDim2.new(0, 12, 0, 3), -38, nil, zIndex)
	scriptSaveState.createLine(root, "HeadBottom", UDim2.new(0, 4, 0, 16), UDim2.new(0, 12, 0, 3), 38, nil, zIndex)

	return root
end

function scriptSaveState.clearResults()
	for _, createdScript in ipairs(scriptSearch.List:GetChildren()) do
		if createdScript.Name ~= "Placeholder" and createdScript.Name ~= "Template" and createdScript.ClassName == "Frame" then
			createdScript:Destroy()
		end
	end
end

function scriptSaveState.setListPresentation(saved)
	local listLayout = scriptSearch.List:FindFirstChildOfClass("UIListLayout")

	if listLayout then
		listLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
		listLayout.VerticalAlignment = Enum.VerticalAlignment.Top
		listLayout.Padding = UDim.new(0, saved and 10 or 12)
	end
end

function scriptSaveState.nextViewToken()
	scriptSaveState.viewToken = (scriptSaveState.viewToken or 0) + 1
	scriptSaveState.refreshToken = (scriptSaveState.refreshToken or 0) + 1
	scriptSaveState.searchToken = (scriptSaveState.searchToken or 0) + 1
	scriptSaveState.promptToken = (scriptSaveState.promptToken or 0) + 1
	return scriptSaveState.viewToken
end

function scriptSaveState.isCurrentView(token)
	return scriptSaveState.viewToken == token and scriptSearch.Visible
end

function scriptSaveState.nextSearchToken()
	scriptSaveState.searchToken = (scriptSaveState.searchToken or 0) + 1
	return scriptSaveState.searchToken
end

function scriptSaveState.isCurrentSearch(token)
	return scriptSaveState.searchToken == token and scriptSearch.Visible and not scriptSaveState.savedMode and not scriptSaveState.runPromptActive
end

function scriptSaveState.nextPromptToken()
	scriptSaveState.promptToken = (scriptSaveState.promptToken or 0) + 1
	return scriptSaveState.promptToken
end

function scriptSaveState.beginViewSwitch()
	if scriptSaveState.viewSwitchLocked or scriptSaveState.runPromptActive or scriptSaveState.transition == "confirming" then
		return false
	end

	scriptSaveState.viewSwitchLocked = true
	scriptSaveState.viewSwitchToken = (scriptSaveState.viewSwitchToken or 0) + 1
	local switchToken = scriptSaveState.viewSwitchToken

	task.delay(0.72, function()
		if scriptSaveState.viewSwitchToken == switchToken then
			scriptSaveState.viewSwitchLocked = false
		end
	end)

	return true, switchToken
end

function scriptSaveState.finishViewSwitch(switchToken)
	if not switchToken or scriptSaveState.viewSwitchToken == switchToken then
		scriptSaveState.viewSwitchLocked = false
	end
end

function scriptSaveState.showSavedEmptyState()
	if not scriptSaveState.savedMode or not scriptSearch.Visible then return end
	if scriptSaveState.header and scriptSaveState.header:FindFirstChild("CountBadge") then
		scriptSaveState.header.CountBadge.Text = tostring(#scriptSaveState.load()).." saved"
	end

	local hasVisibleRow = false

	for _, createdScript in ipairs(scriptSearch.List:GetChildren()) do
		if createdScript.Name ~= "Placeholder" and createdScript.Name ~= "Template" and createdScript.ClassName == "Frame" then
			hasVisibleRow = true
			break
		end
	end

	if hasVisibleRow then return end

	scriptSearch.NoScriptsTitle.Text = "No Saved Scripts"
	scriptSearch.NoScriptsDesc.Text = "Tap the bookmark icon on any result and it will appear here."
	tweenService:Create(scriptSearch.NoScriptsTitle, TweenInfo.new(0.36, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
	task.wait(0.06)
	if not scriptSaveState.savedMode or not scriptSearch.Visible then return end
	tweenService:Create(scriptSearch.NoScriptsDesc, TweenInfo.new(0.36, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
end

function scriptSaveState.ensureControls()
	if scriptSaveState.topButton then return end
	scriptSaveState.listPosition = scriptSearch.List.Position
	scriptSaveState.listSize = scriptSearch.List.Size

	local savedButton = Instance.new("TextButton")
	savedButton.Name = "SavedScriptsButton"
	savedButton.Parent = scriptSearch
	savedButton.AutoButtonColor = false
	savedButton.BackgroundColor3 = Color3.fromRGB(42, 42, 42)
	savedButton.BackgroundTransparency = 1
	savedButton.BorderSizePixel = 0
	savedButton.Position = UDim2.new(1, -48, 0, 5)
	savedButton.Size = UDim2.new(0, 36, 0, 34)
	savedButton.Text = ""
	savedButton.Visible = true
	savedButton.ZIndex = 28

	local savedCorner = Instance.new("UICorner")
	savedCorner.CornerRadius = UDim.new(0, 8)
	savedCorner.Parent = savedButton

	local savedIcon = scriptSaveState.createBookmarkVisual(savedButton, 29)
	savedIcon.AnchorPoint = Vector2.new(0.5, 0.5)
	savedIcon.Position = UDim2.new(0.5, 0, 0.5, 0)

	savedButton.MouseEnter:Connect(function()
		tweenService:Create(savedButton, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.72}):Play()
	end)

	savedButton.MouseLeave:Connect(function()
		tweenService:Create(savedButton, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {BackgroundTransparency = 1}):Play()
	end)

	savedButton.MouseButton1Click:Connect(function()
		if scriptSaveState.transition ~= "open" and scriptSaveState.transition ~= "saved" then return end
		if scriptSaveState.viewSwitchLocked or scriptSaveState.runPromptActive then return end

		if scriptSaveState.savedMode then
			scriptSaveState.showSearchView()
		else
			scriptSaveState.showSavedList()
		end
	end)

	local header = Instance.new("Frame")
	header.Name = "SavedScriptsHeader"
	header.Parent = scriptSearch
	header.BackgroundTransparency = 1
	header.Position = UDim2.new(0, 0, 0, 0)
	header.Size = UDim2.new(1, 0, 0, 116)
	header.Visible = false
	header.ZIndex = 28

	local backButton = Instance.new("TextButton")
	backButton.Name = "Back"
	backButton.Parent = header
	backButton.AutoButtonColor = false
	backButton.BackgroundTransparency = 1
	backButton.BorderSizePixel = 0
	backButton.Position = UDim2.new(0, 20, 0, 16)
	backButton.Size = UDim2.new(0, 34, 0, 34)
	backButton.Text = ""
	backButton.ZIndex = 29

	local backIcon = Instance.new("ImageLabel")
	backIcon.Name = "BackArrowIcon"
	backIcon.Parent = backButton
	backIcon.AnchorPoint = Vector2.new(0.5, 0.5)
	backIcon.BackgroundTransparency = 1
	backIcon.Image = settingsPanel.Back.Image
	backIcon.ImageColor3 = settingsPanel.Back.ImageColor3
	backIcon.ImageRectOffset = settingsPanel.Back.ImageRectOffset
	backIcon.ImageRectSize = settingsPanel.Back.ImageRectSize
	backIcon.ImageTransparency = 1
	backIcon.Position = UDim2.new(0.5, 0, 0.5, 0)
	backIcon.ScaleType = settingsPanel.Back.ScaleType
	backIcon.Size = UDim2.new(0, 30, 0, 30)
	backIcon.Visible = true
	backIcon.ZIndex = 30

	local title = Instance.new("TextLabel")
	title.Name = "Title"
	title.Parent = header
	title.BackgroundTransparency = 1
	title.Font = Enum.Font.GothamBold
	title.Position = UDim2.new(0, 62, 0, 17)
	title.Size = UDim2.new(1, -96, 0, 34)
	title.Text = "Saved Scripts"
	title.TextColor3 = Color3.fromRGB(255, 255, 255)
	title.TextSize = 22
	title.TextTransparency = 1
	title.TextXAlignment = Enum.TextXAlignment.Left
	title.ZIndex = 29

	local description = Instance.new("TextLabel")
	description.Name = "Description"
	description.Parent = header
	description.BackgroundTransparency = 1
	description.Font = Enum.Font.Gotham
	description.Position = UDim2.new(0, 62, 0, 54)
	description.Size = UDim2.new(1, -118, 0, 54)
	description.Text = "Your bookmarked ScriptSearch results stay here for quick review, cleanup, and trusted reruns whenever you need them."
	description.TextColor3 = Color3.fromRGB(205, 205, 210)
	description.TextSize = 13
	description.TextTransparency = 1
	description.TextWrapped = true
	description.TextXAlignment = Enum.TextXAlignment.Left
	description.TextYAlignment = Enum.TextYAlignment.Top
	description.ZIndex = 29

	local headerAccent = Instance.new("Frame")
	headerAccent.Name = "HeaderAccent"
	headerAccent.Parent = header
	headerAccent.BackgroundColor3 = scriptSearchBrand.accent
	headerAccent.BackgroundTransparency = 1
	headerAccent.BorderSizePixel = 0
	headerAccent.Position = UDim2.new(0, 28, 0, 100)
	headerAccent.Size = UDim2.new(0, 0, 0, 2)
	headerAccent.Visible = false
	headerAccent.ZIndex = 29

	local headerAccentCorner = Instance.new("UICorner")
	headerAccentCorner.CornerRadius = UDim.new(1, 0)
	headerAccentCorner.Parent = headerAccent

	local headerAccentGradient = Instance.new("UIGradient")
	headerAccentGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, scriptSearchBrand.accent),
		ColorSequenceKeypoint.new(1, scriptSearchBrand.accentAlt)
	})
	headerAccentGradient.Rotation = 0
	headerAccentGradient.Parent = headerAccent

	local countBadge = Instance.new("TextLabel")
	countBadge.Name = "CountBadge"
	countBadge.Parent = header
	countBadge.BackgroundColor3 = Color3.fromRGB(16, 18, 21)
	countBadge.BackgroundTransparency = 1
	countBadge.BorderSizePixel = 0
	countBadge.Font = Enum.Font.GothamMedium
	countBadge.Position = UDim2.new(1, -116, 0, 25)
	countBadge.Size = UDim2.new(0, 88, 0, 24)
	countBadge.Text = "0 saved"
	countBadge.TextColor3 = Color3.fromRGB(202, 208, 216)
	countBadge.TextSize = 11
	countBadge.TextTransparency = 1
	countBadge.Visible = false
	countBadge.ZIndex = 29

	local countBadgeCorner = Instance.new("UICorner")
	countBadgeCorner.CornerRadius = UDim.new(1, 0)
	countBadgeCorner.Parent = countBadge

	local countBadgeStroke = Instance.new("UIStroke")
	countBadgeStroke.Color = scriptSearchBrand.stroke
	countBadgeStroke.Transparency = 1
	countBadgeStroke.Parent = countBadge

	backButton.MouseEnter:Connect(function()
		tweenService:Create(backButton.BackArrowIcon, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {ImageTransparency = 0.22}):Play()
	end)

	backButton.MouseLeave:Connect(function()
		tweenService:Create(backButton.BackArrowIcon, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {ImageTransparency = 0}):Play()
	end)

	backButton.MouseButton1Click:Connect(function()
		if scriptSaveState.viewSwitchLocked or scriptSaveState.runPromptActive then return end
		scriptSaveState.showSearchView()
	end)

	scriptSaveState.topButton = savedButton
	scriptSaveState.header = header
end

function scriptSaveState.showSearchView()
	scriptSaveState.ensureControls()
	local canSwitch, switchToken = scriptSaveState.beginViewSwitch()
	if not canSwitch then return end
	local viewToken = scriptSaveState.nextViewToken()
	scriptSaveState.transition = "saved-closing"
	scriptSaveState.savedMode = false
	scriptSaveState.setListPresentation(false)
	scriptSaveState.header.Visible = false
	scriptSaveState.topButton.Visible = true
	scriptSaveState.topButton.Position = UDim2.new(1, -48, 0, 5)
	scriptSaveState.topButton.Size = UDim2.new(0, 36, 0, 34)
	scriptSearch.Icon.Visible = true
	scriptSearch.SearchBox.Visible = true
	scriptSearch.SearchBox.PlaceholderText = "Search scripts"
	scriptSearch.SearchBox.Text = ""
	scriptSearch.List.Visible = false
	scriptSearch.List.Position = scriptSaveState.listPosition
	scriptSearch.List.Size = scriptSaveState.listSize
	scriptSaveState.clearResults()
	scriptSaveState.setBookmarkVisual(scriptSaveState.topButton.BookmarkVisual, false, false)
	scriptSearch.UIGradient.Color = scriptSaveState.searchGradientColor
	scriptSearch.UIGradient.Rotation = scriptSaveState.searchGradientRotation
	scriptSaveState.header.Title.TextTransparency = 1
	scriptSaveState.header.Description.TextTransparency = 1
	scriptSaveState.header.Back.BackArrowIcon.ImageTransparency = 1
	if scriptSaveState.header:FindFirstChild("HeaderAccent") then
		scriptSaveState.header.HeaderAccent.BackgroundTransparency = 1
		scriptSaveState.header.HeaderAccent.Size = UDim2.new(0, 0, 0, 2)
	end
	if scriptSaveState.header:FindFirstChild("CountBadge") then
		scriptSaveState.header.CountBadge.BackgroundTransparency = 1
		scriptSaveState.header.CountBadge.TextTransparency = 1
		if scriptSaveState.header.CountBadge:FindFirstChildOfClass("UIStroke") then
			scriptSaveState.header.CountBadge:FindFirstChildOfClass("UIStroke").Transparency = 1
		end
	end

	tweenService:Create(scriptSearch.NoScriptsTitle, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
	tweenService:Create(scriptSearch.NoScriptsDesc, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
	tweenService:Create(scriptSearch, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Size = UDim2.new(0, 580, 0, 43)}):Play()
	tweenService:Create(scriptSearch.Icon, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Position = UDim2.new(0.04, 0, 0.5, 0)}):Play()
	tweenService:Create(scriptSearch.SearchBox, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Position = UDim2.new(0.509, 0, 0.5, 0)}):Play()
	tweenService:Create(scriptSearch.UIGradient, TweenInfo.new(0.45, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 2)}):Play()
	task.wait(0.18)
	if not scriptSaveState.isCurrentView(viewToken) or scriptSaveState.savedMode then
		scriptSaveState.finishViewSwitch(switchToken)
		return
	end
	scriptSaveState.transition = "open"
	scriptSaveState.finishViewSwitch(switchToken)
	scriptSearch.SearchBox:CaptureFocus()
end

local function createScript(result, animationIndex, animationMode)
	local providerName = result.provider or "ScriptBlox"
	local scriptTitle = result.title or result.name or "Untitled Script"
	local savedAnimation = animationMode == "saved"
	local revealStep = savedAnimation and 0.16 or 0.085
	local settleDelay = savedAnimation and scriptSaveState.savedRevealSettleDelay or scriptSaveState.searchRevealSettleDelay
	local revealDelay = settleDelay + math.min(((animationIndex or 1) - 1) * revealStep, savedAnimation and 1.35 or 0.75)
	local newScript = UI.ScriptSearch.List.Template:Clone()
	newScript.Name = scriptTitle
	newScript.Parent = UI.ScriptSearch.List
	newScript.Visible = true
	local savedRowStroke = nil
	local savedRowAccent = nil
	local savedRowGradient = nil
	local savedExecuteStroke = nil
	local savedRowShadow = nil

	local rowScale = Instance.new("UIScale")
	rowScale.Scale = savedAnimation and 0.94 or 0.975
	rowScale.Parent = newScript

	for _, tag in ipairs(newScript.Tags:GetChildren()) do
		if tag.ClassName == "Frame" then
			tag.Shadow.ImageTransparency = 1
			tag.BackgroundTransparency = 1
			tag.Title.TextTransparency = 1
		end
	end

	local saveButton = Instance.new("TextButton")
	saveButton.Name = "SaveScript"
	saveButton.Parent = newScript
	saveButton.AutoButtonColor = false
	saveButton.BackgroundColor3 = Color3.fromRGB(48, 48, 48)
	saveButton.BackgroundTransparency = 1
	saveButton.BorderSizePixel = 0
	saveButton.AnchorPoint = Vector2.new(1, 0.5)
	saveButton.Size = UDim2.new(0, 34, 0, 34)
	saveButton.Text = ""
	saveButton.ZIndex = newScript.Execute.ZIndex + 20

	newScript.ClipsDescendants = true

	if savedAnimation then
		newScript.ClipsDescendants = false
		newScript.BackgroundColor3 = scriptSearchBrand.card
		newScript.BackgroundTransparency = 1
		newScript.BorderSizePixel = 0
		newScript.Size = UDim2.new(1, -2, 0, 78)
		newScript.ScriptName.Position = UDim2.new(0, 24, 0, 15)
		newScript.ScriptName.Size = UDim2.new(1, -224, 0, 20)
		newScript.ScriptName.TextColor3 = Color3.fromRGB(242, 245, 248)
		newScript.ScriptName.TextSize = 14
		newScript.ScriptName.TextScaled = false
		newScript.ScriptName.TextWrapped = false
		newScript.ScriptName.TextTruncate = Enum.TextTruncate.AtEnd
		newScript.ScriptDescription.Visible = false
		newScript.ScriptAuthor.Position = UDim2.new(0, 24, 0, 44)
		newScript.ScriptAuthor.Size = UDim2.new(1, -224, 0, 18)
		newScript.ScriptAuthor.TextColor3 = scriptSearchBrand.muted
		newScript.ScriptAuthor.TextSize = 12
		newScript.ScriptAuthor.TextScaled = false
		newScript.ScriptAuthor.TextWrapped = false
		newScript.ScriptAuthor.TextTruncate = Enum.TextTruncate.AtEnd
		newScript.Tags.Visible = false
		newScript.Execute.AnchorPoint = Vector2.new(1, 0.5)
		newScript.Execute.Position = UDim2.new(1, -18, 0.5, 0)
		newScript.Execute.Size = UDim2.new(0, 70, 0, 34)
		newScript.Execute.BackgroundColor3 = Color3.fromRGB(35, 35, 37)
		newScript.Execute.TextColor3 = Color3.fromRGB(246, 248, 250)
		newScript.Execute.TextSize = 13
		saveButton.Size = UDim2.new(0, 34, 0, 34)
		saveButton.BackgroundColor3 = Color3.fromRGB(31, 31, 33)

		savedRowShadow = Instance.new("ImageLabel")
		savedRowShadow.Name = "SavedRowShadow"
		savedRowShadow.Parent = newScript
		savedRowShadow.AnchorPoint = Vector2.new(0.5, 0.5)
		savedRowShadow.BackgroundTransparency = 1
		savedRowShadow.Image = "rbxassetid://8992230677"
		savedRowShadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
		savedRowShadow.ImageTransparency = 1
		savedRowShadow.Position = UDim2.new(0.5, 0, 0.5, 7)
		savedRowShadow.Size = UDim2.new(1, 24, 1, 26)
		savedRowShadow.ScaleType = Enum.ScaleType.Slice
		savedRowShadow.SliceCenter = Rect.new(99, 99, 99, 99)
		savedRowShadow.ZIndex = math.max(newScript.ZIndex - 1, 1)

		local rowCorner = newScript:FindFirstChildOfClass("UICorner")
		if not rowCorner then
			rowCorner = Instance.new("UICorner")
			rowCorner.Parent = newScript
		end
		rowCorner.CornerRadius = UDim.new(0, 8)

		savedRowStroke = newScript:FindFirstChildOfClass("UIStroke")
		if not savedRowStroke then
			savedRowStroke = Instance.new("UIStroke")
			savedRowStroke.Parent = newScript
		end
		savedRowStroke.Color = scriptSearchBrand.stroke
		savedRowStroke.Transparency = 1

		savedRowGradient = newScript:FindFirstChildOfClass("UIGradient")
		if not savedRowGradient then
			savedRowGradient = Instance.new("UIGradient")
			savedRowGradient.Parent = newScript
		end
		savedRowGradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, scriptSearchBrand.cardSoft),
			ColorSequenceKeypoint.new(0.78, scriptSearchBrand.card),
			ColorSequenceKeypoint.new(1, Color3.fromRGB(15, 16, 18))
		})
		savedRowGradient.Rotation = 90
		savedRowGradient.Offset = Vector2.new(0, 0.38)

		savedExecuteStroke = newScript.Execute:FindFirstChildOfClass("UIStroke")
		if not savedExecuteStroke then
			savedExecuteStroke = Instance.new("UIStroke")
			savedExecuteStroke.Parent = newScript.Execute
		end
		savedExecuteStroke.Color = scriptSearchBrand.stroke
		savedExecuteStroke.Transparency = 1

		savedRowAccent = Instance.new("Frame")
		savedRowAccent.Name = "SavedRowAccent"
		savedRowAccent.Parent = newScript
		savedRowAccent.BackgroundColor3 = scriptSearchBrand.accent
		savedRowAccent.BackgroundTransparency = 1
		savedRowAccent.BorderSizePixel = 0
		savedRowAccent.Position = UDim2.new(0, 0, 0, 0)
		savedRowAccent.Size = UDim2.new(0, 0, 1, 0)
		savedRowAccent.Visible = false
		savedRowAccent.ZIndex = newScript.ScriptName.ZIndex

		local savedRowAccentGradient = Instance.new("UIGradient")
		savedRowAccentGradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, scriptSearchBrand.accent),
			ColorSequenceKeypoint.new(1, scriptSearchBrand.accentAlt)
		})
		savedRowAccentGradient.Rotation = 90
		savedRowAccentGradient.Parent = savedRowAccent
	end

	local function alignSaveButton()
		local executeAnchor = newScript.Execute.AnchorPoint
		local executePosition = newScript.Execute.Position
		local executeSize = newScript.Execute.Size

		saveButton.Position = UDim2.new(
			executePosition.X.Scale - (executeSize.X.Scale * executeAnchor.X),
			executePosition.X.Offset - (executeSize.X.Offset * executeAnchor.X) - 10,
			executePosition.Y.Scale + (executeSize.Y.Scale * (0.5 - executeAnchor.Y)),
			executePosition.Y.Offset + (executeSize.Y.Offset * (0.5 - executeAnchor.Y))
		)
	end

	alignSaveButton()

	local saveCorner = Instance.new("UICorner")
	saveCorner.CornerRadius = UDim.new(1, 0)
	saveCorner.Parent = saveButton

	local saveIcon = scriptSaveState.createBookmarkVisual(saveButton, saveButton.ZIndex + 1)
	saveIcon.AnchorPoint = Vector2.new(0.5, 0.5)
	saveIcon.Position = UDim2.new(0.5, 0, 0.5, 0)
	local saveClickLocked = false
	local rowToken = scriptSaveState.viewToken
	local rowRevealed = false
	local removingRow = false
	local originalRowSize = newScript.Size

	local function scriptRowActive()
		return newScript.Parent == UI.ScriptSearch.List and scriptSearch.Visible and scriptSaveState.viewToken == rowToken and not removingRow
	end

	local function fadeTag(tag, motion)
		if tag.ClassName == "Frame" then
			tweenService:Create(tag.Shadow, motion, {ImageTransparency = 1}):Play()
			tweenService:Create(tag, motion, {BackgroundTransparency = 1}):Play()
			tweenService:Create(tag.Title, motion, {TextTransparency = 1}):Play()
		end
	end

	local function removeSavedRow()
		if removingRow then return end
		removingRow = true
		rowRevealed = false

		saveButton.Active = false
		newScript.Execute.Active = false
		newScript.ClipsDescendants = true

		local fadeMotion = TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
		local collapseMotion = TweenInfo.new(0.18, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut)
		local rowStroke = newScript:FindFirstChildOfClass("UIStroke")

		saveIcon.Image = scriptSaveState.outlineIcon
		saveIcon.Size = scriptSaveState.outlineIconSize

		tweenService:Create(rowScale, fadeMotion, {Scale = 0.96}):Play()
		tweenService:Create(newScript, fadeMotion, {BackgroundTransparency = 1}):Play()
		tweenService:Create(newScript.ScriptName, fadeMotion, {TextTransparency = 1}):Play()
		tweenService:Create(newScript.ScriptDescription, fadeMotion, {TextTransparency = 1}):Play()
		tweenService:Create(newScript.ScriptAuthor, fadeMotion, {TextTransparency = 1}):Play()
		tweenService:Create(newScript.Execute, fadeMotion, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
		tweenService:Create(saveButton, fadeMotion, {BackgroundTransparency = 1}):Play()
		tweenService:Create(saveIcon, fadeMotion, {ImageTransparency = 1, Rotation = 0}):Play()

		if rowStroke then
			tweenService:Create(rowStroke, fadeMotion, {Transparency = 1}):Play()
		end
		if savedRowShadow then
			tweenService:Create(savedRowShadow, fadeMotion, {ImageTransparency = 1}):Play()
		end
		if savedRowAccent then
			tweenService:Create(savedRowAccent, fadeMotion, {BackgroundTransparency = 1}):Play()
		end
		if savedExecuteStroke then
			tweenService:Create(savedExecuteStroke, fadeMotion, {Transparency = 1}):Play()
		end

		for _, tag in ipairs(newScript.Tags:GetChildren()) do
			fadeTag(tag, fadeMotion)
		end

		task.delay(0.24, function()
			if newScript.Parent then
				tweenService:Create(newScript, collapseMotion, {Size = UDim2.new(originalRowSize.X.Scale, originalRowSize.X.Offset, 0, 0)}):Play()
			end
		end)

		task.delay(0.44, function()
			if newScript.Parent then
				newScript:Destroy()
			end

			scriptSaveState.showSavedEmptyState()
		end)
	end

	local function updateSaveButton(instant)
		local saved = scriptSaveState.isSaved(result)
		saveButton.BackgroundColor3 = saved and (savedAnimation and Color3.fromRGB(42, 42, 44) or Color3.fromRGB(62, 62, 62)) or (savedAnimation and Color3.fromRGB(31, 31, 33) or Color3.fromRGB(48, 48, 48))
		scriptSaveState.setBookmarkVisual(saveIcon, saved, instant)
	end

	local function prepareSaveButton()
		local saved = scriptSaveState.isSaved(result)
		saveButton.BackgroundColor3 = saved and (savedAnimation and Color3.fromRGB(42, 42, 44) or Color3.fromRGB(62, 62, 62)) or (savedAnimation and Color3.fromRGB(31, 31, 33) or Color3.fromRGB(48, 48, 48))
		saveIcon.Image = saved and scriptSaveState.filledIcon or scriptSaveState.outlineIcon
		saveIcon.Size = saved and scriptSaveState.filledIconSize or scriptSaveState.outlineIconSize
		saveIcon.ImageTransparency = 1
	end

	prepareSaveButton()

	saveButton.MouseEnter:Connect(function()
		tweenService:Create(saveButton, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {BackgroundTransparency = savedAnimation and 0.42 or 0.58}):Play()
		scriptSaveState.setBookmarkVisual(saveIcon, scriptSaveState.isSaved(result), false)
	end)

	saveButton.MouseLeave:Connect(function()
		tweenService:Create(saveButton, TweenInfo.new(0.22, Enum.EasingStyle.Quint), {BackgroundTransparency = savedAnimation and 0.54 or 0.74}):Play()
		scriptSaveState.setBookmarkVisual(saveIcon, scriptSaveState.isSaved(result), false)
	end)

	if savedAnimation then
		newScript.MouseEnter:Connect(function()
			if not rowRevealed or removingRow then return end

			local hoverMotion = TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
			tweenService:Create(rowScale, hoverMotion, {Scale = 1}):Play()
			tweenService:Create(newScript, hoverMotion, {BackgroundTransparency = 0.06}):Play()
			tweenService:Create(newScript.Execute, hoverMotion, {BackgroundTransparency = 0.46}):Play()
			if savedRowStroke then
				tweenService:Create(savedRowStroke, hoverMotion, {Transparency = 0.38}):Play()
			end
			if savedRowShadow then
				tweenService:Create(savedRowShadow, hoverMotion, {ImageTransparency = 0.74, Position = UDim2.new(0.5, 0, 0.5, 8)}):Play()
			end
			if savedRowGradient then
				tweenService:Create(savedRowGradient, hoverMotion, {Offset = Vector2.new(0, 0.18)}):Play()
			end
			if savedExecuteStroke then
				tweenService:Create(savedExecuteStroke, hoverMotion, {Transparency = 0.58}):Play()
			end
		end)

		newScript.MouseLeave:Connect(function()
			if not rowRevealed or removingRow then return end

			local idleMotion = TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
			tweenService:Create(rowScale, idleMotion, {Scale = 1}):Play()
			tweenService:Create(newScript, idleMotion, {BackgroundTransparency = 0.1}):Play()
			tweenService:Create(newScript.Execute, idleMotion, {BackgroundTransparency = 0.54}):Play()
			if savedRowStroke then
				tweenService:Create(savedRowStroke, idleMotion, {Transparency = 0.5}):Play()
			end
			if savedRowShadow then
				tweenService:Create(savedRowShadow, idleMotion, {ImageTransparency = 0.8, Position = UDim2.new(0.5, 0, 0.5, 7)}):Play()
			end
			if savedRowGradient then
				tweenService:Create(savedRowGradient, idleMotion, {Offset = Vector2.new(0, 0.38)}):Play()
			end
			if savedExecuteStroke then
				tweenService:Create(savedExecuteStroke, idleMotion, {Transparency = 0.72}):Play()
			end
		end)
	end

	saveButton.MouseButton1Click:Connect(function()
		if saveClickLocked then return end
		saveClickLocked = true

		local success, saved = pcall(function()
			return scriptSaveState.toggle(result)
		end)

		if not success then
			saveClickLocked = false
			queueNotification("ScriptSearch", "Could not update saved scripts.", 4384402990)
			return
		end

		queueNotification("ScriptSearch", saved and "Saved script." or "Removed saved script.", 4384403532)

		if scriptSaveState.savedMode and not saved then
			removeSavedRow()
		else
			updateSaveButton(true)
		end

		task.delay(0.12, function()
			saveClickLocked = false
		end)
	end)

	task.spawn(function()
		local detail = {
			description = result.description or (providerName == "Rscripts.net" and "Powered by Rscripts.net" or (providerName == "Roscripts.io" and "Powered by Roscripts.io" or "Loading script details...")),
			author = result.author or providerName,
			verified = result.verified or false,
			likes = tonumber(result.likes) or 0,
			dislikes = tonumber(result.dislikes) or 0
		}

		if providerName == "ScriptBlox" and result.slug then
			local response

			local success = pcall(function()
				local responseRequest = httpRequest({
					Url = "https://www.scriptblox.com/api/script/"..result['slug'],
					Method = "GET"
				})

				response = httpService:JSONDecode(responseRequest.Body)
			end)

			if not success or not response or not response.script then return end

			detail.description = response.script.features or "No description."
			detail.likes = tonumber(response.script.likeCount) or 0
			detail.dislikes = tonumber(response.script.dislikeCount) or 0
			detail.author = response.script.owner and response.script.owner.username or "ScriptBlox"
			detail.verified = response.script.owner and response.script.owner.verified or false
		end

		if not scriptRowActive() then return end

		result.description = detail.description
		result.author = detail.author
		result.verified = detail.verified
		result.likes = detail.likes
		result.dislikes = detail.dislikes

		if savedAnimation then
			newScript.ScriptAuthor.Text = "by "..detail.author.." • "..providerName
			newScript.ScriptDescription.Visible = false
			newScript.Tags.Visible = false
			return
		end

		newScript.ScriptDescription.Text = detail.description or "No description."

		local likes = detail.likes
		local dislikes = detail.dislikes

		if likes ~= dislikes then
			newScript.Tags.Review.Title.Text = (likes > dislikes) and "Positive Reviews" or "Negative Reviews"
			newScript.Tags.Review.BackgroundColor3 = (likes > dislikes) and Color3.fromRGB(0, 139, 102) or Color3.fromRGB(180, 0, 0)
			newScript.Tags.Review.Size = (likes > dislikes) and UDim2.new(0, 145, 1, 0) or UDim2.new(0, 150, 1, 0)
		elseif likes > 0 then
			newScript.Tags.Review.Title.Text = "Mixed Reviews"
			newScript.Tags.Review.BackgroundColor3 = Color3.fromRGB(198, 132, 0)
			newScript.Tags.Review.Size = UDim2.new(0, 130, 1, 0)
		else
			newScript.Tags.Review.Visible = false
		end

		newScript.ScriptAuthor.Text = "uploaded by "..detail.author.." • "..providerName
		newScript.Tags.Verified.Visible = detail.verified or false
		newScript.Tags.Visible = rowRevealed
	end)

	wipeTransparency(newScript, 1, true)
	alignSaveButton()
	prepareSaveButton()

	newScript.ScriptName.Text = scriptTitle
	newScript.ScriptDescription.Text = result.description or (providerName == "Rscripts.net" and "Powered by Rscripts.net" or (providerName == "Roscripts.io" and "Powered by Roscripts.io" or "Loading script details..."))
	newScript.ScriptAuthor.Text = (savedAnimation and "by " or "uploaded by ")..tostring(result.author or providerName).." • "..providerName


	newScript.Tags.Visible = false
	newScript.ScriptDescription.Visible = not savedAnimation
	newScript.Tags.Patched.Visible = (not savedAnimation) and (result.isPatched or false)

	task.delay(revealDelay, function()
		if not scriptRowActive() then return end

		rowRevealed = true
		newScript.Size = originalRowSize
		alignSaveButton()
		newScript.Tags.Visible = not savedAnimation

		local rowMotion = TweenInfo.new(savedAnimation and 0.82 or 0.64, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
		tweenService:Create(rowScale, rowMotion, {Scale = 1}):Play()
		tweenService:Create(newScript, rowMotion,  {BackgroundTransparency = savedAnimation and 0.1 or 0.8}):Play()
		tweenService:Create(newScript.ScriptName, rowMotion,  {TextTransparency = 0}):Play()
		tweenService:Create(newScript.Execute, rowMotion,  {BackgroundTransparency = savedAnimation and 0.54 or 0.8}):Play()
		tweenService:Create(newScript.Execute, rowMotion,  {TextTransparency = 0}):Play()
		tweenService:Create(saveButton, rowMotion,  {BackgroundTransparency = savedAnimation and 0.54 or 0.74}):Play()
		scriptSaveState.setBookmarkVisual(saveIcon, scriptSaveState.isSaved(result), false)
		tweenService:Create(newScript.ScriptAuthor, rowMotion,  {TextTransparency = savedAnimation and 0.28 or 0.7}):Play()

		if savedRowStroke then
			tweenService:Create(savedRowStroke, rowMotion, {Transparency = 0.5}):Play()
		end
		if savedRowShadow then
			tweenService:Create(savedRowShadow, rowMotion, {ImageTransparency = 0.8}):Play()
		end
		if savedRowGradient then
			tweenService:Create(savedRowGradient, rowMotion, {Offset = Vector2.new(0, 0.38)}):Play()
		end
		if savedExecuteStroke then
			tweenService:Create(savedExecuteStroke, rowMotion, {Transparency = 0.72}):Play()
		end

		if not savedAnimation then
			tweenService:Create(newScript.ScriptDescription, rowMotion,  {TextTransparency = 0.3}):Play()

			for _, tag in ipairs(newScript.Tags:GetChildren()) do
				if tag.ClassName == "Frame" then
					tweenService:Create(tag.Shadow, rowMotion,  {ImageTransparency = 0.7}):Play()
					tweenService:Create(tag, rowMotion,  {BackgroundTransparency = 0}):Play()
					tweenService:Create(tag.Title, rowMotion,  {TextTransparency = 0}):Play()
				end
			end
		end
	end)

	newScript.Execute.MouseButton1Click:Connect(function()
		if scriptSaveState.runPromptActive or removingRow or not rowRevealed then return end

		scriptSaveState.runPromptActive = true
		local imageUrl = getScriptImageUrl(result)

		local confirmed = false
		local promptSuccess, promptResponse = pcall(function()
			return confirmScriptRun(scriptTitle, providerName, imageUrl, result)
		end)

		if promptSuccess then
			confirmed = promptResponse
		end

		if not promptSuccess then
			scriptSaveState.runPromptActive = false
			queueNotification("ScriptSearch", "Could not open run confirmation.", 4384402990)
			return
		end

		if not confirmed then
			scriptSaveState.runPromptActive = false
			return
		end

		if scriptSearch.Visible then
			closeScriptSearch()
		end

		local scriptToRun = result.script
		local rawScriptUrl = result.rawScriptUrl

		if not rawScriptUrl and providerName == "Roscripts.io" then
			local success, response = pcall(function()
				return fetchRoscriptsRawUrl(result)
			end)

			if success then
				rawScriptUrl = response
			end
		end

		if not scriptToRun and rawScriptUrl then
			local success, response = pcall(function()
				local responseRequest = httpRequest({
					Url = rawScriptUrl,
					Method = "GET"
				})

				return responseRequest and responseRequest.Body
			end)

			if success then
				scriptToRun = response
			end
		end

		if not scriptToRun or scriptToRun == "" then
			scriptSaveState.runPromptActive = false
			queueNotification("ScriptSearch", "Could not load "..providerName.." script source", 4384402990)
			return
		end

		local loadedScript, loadError = loadstring(scriptToRun)

		if not loadedScript then
			scriptSaveState.runPromptActive = false
			queueNotification("ScriptSearch", "Could not prepare script: "..tostring(loadError), 4384402990)
			return
		end

		queueNotification("ScriptSearch", "Running "..scriptTitle.. " via "..providerName , 4384403532)
		scriptSaveState.runPromptActive = false
		loadedScript()
	end)
end

function scriptSaveState.renderSavedResults()
	if not scriptSaveState.savedMode or not scriptSearch.Visible then return end

	local renderToken = scriptSaveState.viewToken
	scriptSaveState.setListPresentation(true)
	scriptSaveState.clearResults()

	if not scriptSaveState.isCurrentView(renderToken) or not scriptSaveState.savedMode then return end

	local savedScripts = scriptSaveState.load()
	if scriptSaveState.header and scriptSaveState.header:FindFirstChild("CountBadge") then
		scriptSaveState.header.CountBadge.Text = tostring(#savedScripts).." saved"
	end
	local created = false

	for index, savedScript in ipairs(savedScripts) do
		if not scriptSaveState.isCurrentView(renderToken) or not scriptSaveState.savedMode then return end

		local success = pcall(function()
			createScript(savedScript, index, "saved")
		end)

		if success then
			created = true
		end
	end

	if not scriptSaveState.isCurrentView(renderToken) or not scriptSaveState.savedMode then return end

	if not created then
		tweenService:Create(scriptSearch.NoScriptsTitle, TweenInfo.new(.35,Enum.EasingStyle.Quint),  {TextTransparency = 0}):Play()
		tweenService:Create(scriptSearch.NoScriptsDesc, TweenInfo.new(.35,Enum.EasingStyle.Quint),  {TextTransparency = 0}):Play()
	else
		tweenService:Create(scriptSearch.List, TweenInfo.new(.3,Enum.EasingStyle.Quint),  {ScrollBarImageTransparency = 1}):Play()
		task.defer(function()
			if not scriptSaveState.isCurrentView(renderToken) or not scriptSaveState.savedMode then return end

			local listLayout = scriptSearch.List:FindFirstChildOfClass("UIListLayout")
			if listLayout then
				local contentHeight = math.max(listLayout.AbsoluteContentSize.Y + 18, scriptSearch.List.AbsoluteWindowSize.Y)
				scriptSearch.List.CanvasSize = UDim2.new(0, 0, 0, contentHeight)
			end
		end)
	end
end

function scriptSaveState.scheduleSavedRefresh()
	scriptSaveState.refreshToken = (scriptSaveState.refreshToken or 0) + 1
	local refreshToken = scriptSaveState.refreshToken

	task.delay(0.08, function()
		if refreshToken ~= scriptSaveState.refreshToken then return end
		scriptSaveState.renderSavedResults()
	end)
end

function scriptSaveState.showSavedList()
	scriptSaveState.ensureControls()
	local canSwitch, switchToken = scriptSaveState.beginViewSwitch()
	if not canSwitch then return end
	local viewToken = scriptSaveState.nextViewToken()
	scriptSaveState.transition = "saved-opening"
	scriptSaveState.savedMode = true
	scriptSaveState.setListPresentation(true)
	scriptSearch.SearchBox:ReleaseFocus()
	scriptSearch.Icon.Visible = false
	scriptSearch.SearchBox.Visible = false
	scriptSearch.SearchBox.PlaceholderText = "Saved scripts"
	scriptSearch.SearchBox.Text = "Saved Scripts"
	scriptSearch.SearchBox.TextColor3 = Color3.fromRGB(150, 150, 150)
	scriptSaveState.topButton.Visible = true
	scriptSaveState.topButton.Position = UDim2.new(1, -48, 0, 5)
	scriptSaveState.topButton.Size = UDim2.new(0, 36, 0, 34)
	scriptSaveState.header.Visible = true
	scriptSearch.UIGradient.Color = scriptSaveState.searchGradientColor
	scriptSearch.UIGradient.Rotation = scriptSaveState.searchGradientRotation
	scriptSearch.UIGradient.Offset = Vector2.new(0, 2)
	scriptSearch.List.Visible = false
	scriptSearch.List.Position = UDim2.new(0, 28, 0, 146)
	scriptSearch.List.Size = UDim2.new(1, -56, 1, -164)
	scriptSearch.List.CanvasPosition = Vector2.new(0, 0)
	scriptSearch.List.ScrollBarImageTransparency = 1
	scriptSearch.NoScriptsTitle.Text = "No Saved Scripts"
	scriptSearch.NoScriptsDesc.Text = "Tap the bookmark icon on any result and it will appear here."
	scriptSaveState.setBookmarkVisual(scriptSaveState.topButton.BookmarkVisual, true, false)
	local savedCount = #scriptSaveState.load()
	if scriptSaveState.header:FindFirstChild("CountBadge") then
		scriptSaveState.header.CountBadge.Text = tostring(savedCount).." saved"
		scriptSaveState.header.CountBadge.BackgroundTransparency = 1
		scriptSaveState.header.CountBadge.TextTransparency = 1
		if scriptSaveState.header.CountBadge:FindFirstChildOfClass("UIStroke") then
			scriptSaveState.header.CountBadge:FindFirstChildOfClass("UIStroke").Transparency = 1
		end
	end
	if scriptSaveState.header:FindFirstChild("HeaderAccent") then
		scriptSaveState.header.HeaderAccent.Size = UDim2.new(0, 0, 0, 2)
		scriptSaveState.header.HeaderAccent.BackgroundTransparency = 1
	end
	scriptSaveState.header.Title.TextTransparency = 1
	scriptSaveState.header.Description.TextTransparency = 1
	scriptSaveState.header.Back.BackArrowIcon.ImageTransparency = 1

	tweenService:Create(scriptSearch.NoScriptsTitle, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
	tweenService:Create(scriptSearch.NoScriptsDesc, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()

	for _, createdScript in ipairs(scriptSearch.List:GetChildren()) do
		if createdScript.Name ~= "Placeholder" and createdScript.Name ~= "Template" and createdScript.ClassName == "Frame" then
			wipeTransparency(createdScript, 1, true)
		end
	end

	task.wait(0.18)
	if not scriptSaveState.isCurrentView(viewToken) or not scriptSaveState.savedMode then
		scriptSaveState.finishViewSwitch(switchToken)
		return
	end

	local pageMotion = TweenInfo.new(0.54, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

	tweenService:Create(scriptSearch, pageMotion, {Size = UDim2.new(0, 580, 0, 529)}):Play()
	tweenService:Create(scriptSearch.UIGradient, pageMotion, {Offset = Vector2.new(0, 0.6)}):Play()
	tweenService:Create(scriptSaveState.topButton, pageMotion, {Position = UDim2.new(1, -54, 0, 18), Size = UDim2.new(0, 38, 0, 38)}):Play()
	tweenService:Create(scriptSearch.List, pageMotion, {Position = UDim2.new(0, 28, 0, 146), Size = UDim2.new(1, -56, 1, -164)}):Play()
	tweenService:Create(scriptSaveState.header.Back.BackArrowIcon, pageMotion, {ImageTransparency = 0}):Play()
	tweenService:Create(scriptSaveState.header.Title, pageMotion, {TextTransparency = 0}):Play()
	tweenService:Create(scriptSaveState.header.Description, pageMotion, {TextTransparency = 0.12}):Play()
	if scriptSaveState.header:FindFirstChild("HeaderAccent") then
		scriptSaveState.header.HeaderAccent.Visible = false
	end
	if scriptSaveState.header:FindFirstChild("CountBadge") then
		scriptSaveState.header.CountBadge.Visible = false
	end

	task.wait(0.34)
	if not scriptSaveState.isCurrentView(viewToken) or not scriptSaveState.savedMode then
		scriptSaveState.finishViewSwitch(switchToken)
		return
	end

	scriptSearch.List.Visible = true
	scriptSaveState.renderSavedResults()
	scriptSaveState.transition = "saved"
	scriptSaveState.finishViewSwitch(switchToken)
end

local function extractDomain(link)
	link = tostring(link or "")
	local domainToReturn = link:match("([%w-_]+%.[%w-_%.]+)")
	return domainToReturn
end

local function securityDetection(title, content, link, gradient, actions)
	if not checkSirius() then return end

	link = tostring(link or "Unknown Link")
	actions = actions or {{"Allow", true}, {"Don't Allow", false}}

	local domain = extractDomain(link) or link
	checkFolder()
	local currentAllowlist = isfile and isfile(siriusValues.siriusFolder.."/".."allowedLinks.srs") and readfile(siriusValues.siriusFolder.."/".."allowedLinks.srs") or nil
	if currentAllowlist then
		local allowlistSuccess, allowlistData = pcall(function()
			return httpService:JSONDecode(currentAllowlist)
		end)

		currentAllowlist = allowlistSuccess and type(allowlistData) == "table" and allowlistData or {}
		if table.find(currentAllowlist, domain) then return true end
	end

	local newSecurityPrompt = securityPrompt:Clone()

	newSecurityPrompt.Parent = UI
	newSecurityPrompt.Name = "SecurityPrompt"

	wipeTransparency(newSecurityPrompt, 1, true)
	newSecurityPrompt.Size = UDim2.new(0, 478, 0, 150)

	newSecurityPrompt.Title.Text = title
	newSecurityPrompt.Subtitle.Text = content
	newSecurityPrompt.FoundLink.Text = domain

	newSecurityPrompt.Visible = true
	newSecurityPrompt.UIGradient.Color = gradient

	newSecurityPrompt.Buttons.Template.Visible = false

	local function closeSecurityPrompt()
		if not newSecurityPrompt or not newSecurityPrompt.Parent then return end

		tweenService:Create(newSecurityPrompt, TweenInfo.new(0.52, Enum.EasingStyle.Quint),  {Size = UDim2.new(0, 500, 0, 165)}):Play()
		tweenService:Create(newSecurityPrompt, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {BackgroundTransparency = 1}):Play()
		tweenService:Create(newSecurityPrompt.Title, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {TextTransparency = 1}):Play()
		tweenService:Create(newSecurityPrompt.Subtitle, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {TextTransparency = 1}):Play()
		tweenService:Create(newSecurityPrompt.FoundLink, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {TextTransparency = 1}):Play()


		for _, button in ipairs(newSecurityPrompt.Buttons:GetChildren()) do
			if button.Name ~= "Template" and button.ClassName == "TextButton" then
				tweenService:Create(button, TweenInfo.new(0.3, Enum.EasingStyle.Quint),  {BackgroundTransparency = 1}):Play()
				tweenService:Create(button, TweenInfo.new(0.3, Enum.EasingStyle.Quint),  {TextTransparency = 1}):Play()
			end
		end
		task.wait(0.55)
		newSecurityPrompt:Destroy()
	end

	local decision

	for _, action in ipairs(actions) do
		local newAction = newSecurityPrompt.Buttons.Template:Clone()
		newAction.Name = action[1]
		newAction.Text = action[1]
		newAction.Parent = newSecurityPrompt.Buttons
		newAction.Visible = true
		newAction.Size = UDim2.new(0, newAction.TextBounds.X + 50, 0, 36) -- textbounds

		newAction.MouseButton1Click:Connect(function()
			if action[2] then
				if action[3] then
					checkFolder()
					if currentAllowlist then
						table.insert(currentAllowlist, domain)
						pcall(function()
							writefile(siriusValues.siriusFolder.."/".."allowedLinks.srs", httpService:JSONEncode(currentAllowlist))
						end)
					else
						pcall(function()
							writefile(siriusValues.siriusFolder.."/".."allowedLinks.srs", httpService:JSONEncode({domain}))
						end)
					end
				end
				decision = true
			else
				decision = false
			end

			closeSecurityPrompt()
		end)
	end

	tweenService:Create(newSecurityPrompt, TweenInfo.new(0.4, Enum.EasingStyle.Quint),  {Size = UDim2.new(0, 576, 0, 181)}):Play()
	tweenService:Create(newSecurityPrompt, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {BackgroundTransparency = 0}):Play()
	tweenService:Create(newSecurityPrompt.Title, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {TextTransparency = 0}):Play()
	tweenService:Create(newSecurityPrompt.Subtitle, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {TextTransparency = 0.3}):Play()
	task.wait(0.03)
	tweenService:Create(newSecurityPrompt.FoundLink, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {TextTransparency = 0.2}):Play()

	task.wait(0.1)

	for _, button in ipairs(newSecurityPrompt.Buttons:GetChildren()) do
		if button.Name ~= "Template" and button.ClassName == "TextButton" then
			tweenService:Create(button, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {BackgroundTransparency = 0.7}):Play()
			tweenService:Create(button, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {TextTransparency = 0.05}):Play()
			task.wait(0.1)
		end
	end

	newSecurityPrompt.FoundLink.MouseEnter:Connect(function()
		newSecurityPrompt.FoundLink.Text = link
		tweenService:Create(newSecurityPrompt.FoundLink, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {TextTransparency = 0.4}):Play()
	end)

	newSecurityPrompt.FoundLink.MouseLeave:Connect(function()
		newSecurityPrompt.FoundLink.Text = domain
		tweenService:Create(newSecurityPrompt.FoundLink, TweenInfo.new(0.5, Enum.EasingStyle.Quint),  {TextTransparency = 0.2}):Play()
	end)

	repeat task.wait() until decision ~= nil or not newSecurityPrompt.Parent
	return decision
end

if Essential or Pro then
	if originalRequest then
		getgenv()[index] = function(data)
			data = type(data) == "table" and data or {Url = tostring(data or "Unknown Link")}

			if checkSirius() and checkSetting("Intelligent HTTP Interception").current then
				local title = "Do you trust this source?"
				local content = "Sirius has prevented data from being sent off-client, would you like to allow data to be sent or retrieved from this source?"
				local url = tostring(data.Url or data.url or "Unknown Link")
				local gradient = ColorSequence.new({ColorSequenceKeypoint.new(0, Color3.new(0, 0, 0)),ColorSequenceKeypoint.new(1, Color3.new(0.764706, 0.305882, 0.0941176))})
				local actions = {{"Always Allow", true, true}, {"Allow just this once", true}, {"Don't Allow", false}}

				if url == "http://127.0.0.1:6463/rpc?v=1" then
					local bodyDecoded
					pcall(function()
						bodyDecoded = httpService:JSONDecode(tostring(data.Body or data.body or "{}"))
					end)

					if type(bodyDecoded) == "table" and bodyDecoded.cmd == "INVITE_BROWSER" then
						title = "Would you like to join this Discord server?"
						content = "Sirius has prevented your Discord client from automatically joining this Discord server, would you like to continue and join, or block it?"
						url = bodyDecoded.args and bodyDecoded.args.code and "discord.gg/"..bodyDecoded.args.code or "Unknown Invite"
						gradient = ColorSequence.new({ColorSequenceKeypoint.new(0, Color3.new(0, 0, 0)),ColorSequenceKeypoint.new(1, Color3.new(0.345098, 0.396078, 0.94902))})
						actions = {{"Allow", true}, {"Don't Allow", false}}
					end
				end

				local answer = securityDetection(title, content, url, gradient, actions)


				if answer then 
					return originalRequest(data)
				else
					return
				end
			else
				return originalRequest(data)
			end
		end
	end

	if originalSetClipboard then
		getgenv()[indexSetClipboard] = function(data)
			if checkSirius() and checkSetting("Intelligent Clipboard Interception").current then
				local title = "Would you like to copy this to your clipboard?"
				local content = "Sirius has prevented a script from setting the below text to your clipboard, would you like to allow this, or prevent it from copying?"
				local url = tostring(data or "Unknown Clipboard")
				local gradient = ColorSequence.new({ColorSequenceKeypoint.new(0, Color3.new(0, 0, 0)),ColorSequenceKeypoint.new(1, Color3.new(0.776471, 0.611765, 0.529412))})
				local actions = {{"Allow", true}, {"Don't Allow", false}}

				local answer = securityDetection(title, content, url, gradient, actions)

				if answer then 
					return originalSetClipboard(data)
				else
					return
				end
			else
				return originalSetClipboard(data)
			end
		end
	end
end


local function scriptSourceEnabled(settingName)
	local setting = checkSetting(settingName)
	return not setting or setting.current
end

local function normaliseRscriptsResult(result)
	local user = result.user or {}
	local author = tostring(user.username or result.creator or "Rscripts.net")

	return {
		provider = "Rscripts.net",
		title = result.title or "Untitled Rscripts Script",
		description = result.description or "Powered by Rscripts.net",
		author = author,
		verified = user.verified or false,
		likes = tonumber(result.likes) or 0,
		dislikes = tonumber(result.dislikes) or 0,
		image = result.image or user.image,
		rawScriptUrl = result.rawScript,
		isPatched = false
	}
end

local function normaliseRoscriptsResult(result)
	local slug = result.slug

	return {
		provider = "Roscripts.io",
		title = result.title or "Untitled Roscripts.io Script",
		description = result.game and ("Game: "..tostring(result.game)) or "Powered by Roscripts.io",
		author = tostring(result.owner or "Roscripts.io"),
		verified = result.ownerVerified or false,
		likes = 0,
		dislikes = 0,
		thumbUrl = result.thumbUrl or result.thumb,
		detailUrl = slug and ("https://roscripts.io/s/"..slug) or nil,
		isPatched = false
	}
end

function searchScripts(query)
	local results = {}
	local enabledSources = 0
	local failedSources = 0
	query = tostring(query or ""):match("^%s*(.-)%s*$") or ""

	if scriptSaveState.savedMode or scriptSaveState.runPromptActive or scriptSaveState.transition == "confirming" then return end

	if query == "" then
		closeScriptSearch()
		return
	end

	scriptSaveState.viewSwitchLocked = false
	scriptSaveState.setListPresentation(false)
	if scriptSaveState.header then scriptSaveState.header.Visible = false end
	if scriptSaveState.topButton then
		scriptSaveState.topButton.Visible = true
		scriptSaveState.topButton.Position = UDim2.new(1, -48, 0, 5)
		scriptSaveState.topButton.Size = UDim2.new(0, 36, 0, 34)
	end
	scriptSearch.List.Position = scriptSaveState.listPosition
	scriptSearch.List.Size = scriptSaveState.listSize
	scriptSearch.List.CanvasPosition = Vector2.new(0, 0)

	local searchToken = scriptSaveState.nextSearchToken()

	local function currentSearch()
		return scriptSaveState.isCurrentSearch(searchToken)
	end

	local function expandSearchPanel()
		if not currentSearch() then return false end

		scriptSaveState.setListPresentation(false)
		if scriptSaveState.header then scriptSaveState.header.Visible = false end
		if scriptSaveState.topButton then
			scriptSaveState.topButton.Visible = true
			scriptSaveState.topButton.Position = UDim2.new(1, -48, 0, 5)
			scriptSaveState.topButton.Size = UDim2.new(0, 36, 0, 34)
			scriptSaveState.setBookmarkVisual(scriptSaveState.topButton.BookmarkVisual, false, false)
		end
		scriptSearch.Icon.Visible = true
		scriptSearch.SearchBox.Visible = true
		scriptSearch.List.Position = scriptSaveState.listPosition
		scriptSearch.List.Size = scriptSaveState.listSize
		scriptSearch.List.Visible = true
		tweenService:Create(scriptSearch, TweenInfo.new(.45,Enum.EasingStyle.Quint),  {Size = UDim2.new(0, 580, 0, 529)}):Play()
		tweenService:Create(scriptSearch.Icon, TweenInfo.new(.45,Enum.EasingStyle.Quint),  {Position = UDim2.new(0.054, 0, 0.056, 0)}):Play()
		tweenService:Create(scriptSearch.SearchBox, TweenInfo.new(.45,Enum.EasingStyle.Quint),  {Position = UDim2.new(0.523, 0, 0.056, 0)}):Play()
		tweenService:Create(scriptSearch.List, TweenInfo.new(.45,Enum.EasingStyle.Quint),  {Position = scriptSaveState.listPosition, Size = scriptSaveState.listSize}):Play()
		tweenService:Create(scriptSearch.UIGradient, TweenInfo.new(.45,Enum.EasingStyle.Quint),  {Offset = Vector2.new(0, 0.6)}):Play()

		return true
	end

	local function clearRenderedResults(fadeFirst)
		for _, createdScript in ipairs(scriptSearch.List:GetChildren()) do
			if createdScript.Name ~= "Placeholder" and createdScript.Name ~= "Template" and createdScript.ClassName == "Frame" then
				if fadeFirst then
					wipeTransparency(createdScript, 1, true)
				else
					createdScript:Destroy()
				end
			end
		end
	end

	local function showSearchState(title, description)
		if not expandSearchPanel() then return end

		clearRenderedResults(false)
		scriptSearch.List.CanvasPosition = Vector2.new(0,0)
		scriptSearch.NoScriptsTitle.Text = title
		scriptSearch.NoScriptsDesc.Text = description
		tweenService:Create(scriptSearch.NoScriptsTitle, TweenInfo.new(.35,Enum.EasingStyle.Quint),  {TextTransparency = 0}):Play()
		tweenService:Create(scriptSearch.NoScriptsDesc, TweenInfo.new(.35,Enum.EasingStyle.Quint),  {TextTransparency = 0}):Play()
	end

	if not httpRequest then
		showSearchState("Search Unavailable", "Your executor does not expose HTTP requests, so Sirius cannot contact script sources.")
		return
	end

	if not expandSearchPanel() then return end
	scriptSearch.NoScriptsTitle.Text = "Searching Scripts"
	scriptSearch.NoScriptsDesc.Text = "Checking selected sources..."
	tweenService:Create(scriptSearch.NoScriptsTitle, TweenInfo.new(.25,Enum.EasingStyle.Quint),  {TextTransparency = 0}):Play()
	tweenService:Create(scriptSearch.NoScriptsDesc, TweenInfo.new(.25,Enum.EasingStyle.Quint),  {TextTransparency = 0.1}):Play()
	clearRenderedResults(true)

	task.wait(0.18)
	if not currentSearch() then return end

	clearRenderedResults(false)
	scriptSearch.List.CanvasPosition = Vector2.new(0,0)

	if scriptSourceEnabled("ScriptBlox Script Source") then
		enabledSources += 1

		local success = pcall(function()
			local responseRequest = httpRequest({
				Url = "https://scriptblox.com/api/script/search?q="..httpService:UrlEncode(query).."&mode=free&max=20&page=1",
				Method = "GET"
			})

			local response = httpService:JSONDecode(responseRequest.Body)

			for _, scriptResult in pairs((response.result and response.result.scripts) or {}) do
				scriptResult.provider = "ScriptBlox"
				table.insert(results, scriptResult)
			end
		end)

		if not success then
			failedSources += 1
		end

		if not currentSearch() then return end
	end

	if scriptSourceEnabled("Rscripts.net Script Source") then
		enabledSources += 1

		local success = pcall(function()
			local responseRequest = httpRequest({
				Url = "https://rscripts.net/api/v2/scripts?page=1&orderBy=date&sort=desc&notPaid=true&q="..httpService:UrlEncode(query),
				Method = "GET"
			})

			local response = httpService:JSONDecode(responseRequest.Body)

			for _, scriptResult in pairs(response.scripts or {}) do
				table.insert(results, normaliseRscriptsResult(scriptResult))
			end
		end)

		if not success then
			failedSources += 1
		end

		if not currentSearch() then return end
	end

	if scriptSourceEnabled("Roscripts.io Script Source") then
		enabledSources += 1

		local success = pcall(function()
			local responseRequest = httpRequest({
				Url = "https://roscripts.io/api/search?q="..httpService:UrlEncode(query),
				Method = "GET"
			})

			local response = httpService:JSONDecode(responseRequest.Body)

			for _, scriptResult in pairs(response.results or {}) do
				table.insert(results, normaliseRoscriptsResult(scriptResult))
			end
		end)

		if not success then
			failedSources += 1
		end

		if not currentSearch() then return end
	end

	if enabledSources == 0 then
		showSearchState("No Sources Enabled", "Turn on at least one ScriptSearch source in General settings.")
		return
	end

	if enabledSources > 0 and failedSources == enabledSources then
		queueNotification("ScriptSearch", "ScriptSearch backend encountered an error, try again later", 4384402990)
		showSearchState("Search Failed", "Selected sources did not respond. Try again or switch sources in General settings.")
		return
	elseif failedSources > 0 then
		queueNotification("ScriptSearch", "One selected script source failed, showing the rest", 4384402990)
	end

	if not currentSearch() then return end

	tweenService:Create(scriptSearch.NoScriptsTitle, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 1}):Play()
	tweenService:Create(scriptSearch.NoScriptsDesc, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 1}):Play()

	scriptSearch.List.Visible = true
	scriptSearch.List.CanvasPosition = Vector2.new(0,0)
	expandSearchPanel()

	local scriptCreated = false
	local renderedIds = {}
	local renderIndex = 0
	for index, scriptResult in ipairs(results) do
		if not currentSearch() then return end
		local resultId = scriptSaveState.getId(scriptResult)

		if renderedIds[resultId] then
			continue
		end

		renderedIds[resultId] = true
		renderIndex += 1

		local success = pcall(function()
			createScript(scriptResult, renderIndex, "search")
		end)

		if success then
			scriptCreated = true
		end
	end

	if not currentSearch() then return end

	if not scriptCreated then
		showSearchState("No Scripts Found", "Try a different search or enable more sources in General settings.")
	else
		tweenService:Create(scriptSearch.List, TweenInfo.new(.3,Enum.EasingStyle.Quint),  {ScrollBarImageTransparency = 1}):Play()
	end
end

local function getSmartBarButtons()
	local buttons = smartBar.Buttons:GetChildren()
	table.sort(buttons, function(a, b)
		return a.Position.X.Scale < b.Position.X.Scale
	end)
	return buttons
end

function siriusValues.getSiriusViewportSize()
	local viewport = camera and camera.ViewportSize or Vector2.new(1280, 720)
	pcall(function()
		if UI.AbsoluteSize.X > 0 and UI.AbsoluteSize.Y > 0 then
			viewport = UI.AbsoluteSize
		end
	end)
	return viewport
end

function siriusValues.getResponsiveSmartBarSize(open)
	local width = open and 581 or 531
	local height = open and 70 or 64
	if siriusValues.betaControl and siriusValues.betaControl.adminEnabled then
		width += open and 76 or 62
	end
	local viewport = siriusValues.getSiriusViewportSize()
	width = math.min(width, math.max(336, viewport.X - 24))
	return UDim2.new(0, width, 0, height)
end

local function cancelSmartBarOptionTweens(button)
	if smartBarOptionTweens[button] then
		for _, tween in ipairs(smartBarOptionTweens[button]) do
			tween:Cancel()
		end
	end

	smartBarOptionTweens[button] = {}
end

local function playSmartBarOptionTween(button, object, tweenInfo, properties)
	if not smartBarOptionTweens[button] then
		smartBarOptionTweens[button] = {}
	end

	local tween = tweenService:Create(object, tweenInfo, properties)
	table.insert(smartBarOptionTweens[button], tween)
	tween:Play()
	return tween
end

local function openSmartBar()
	if siriusValues.betaControl and siriusValues.betaControl.disabled then
		siriusValues.showBetaControlOverlay("SIRIUS X Disabled", siriusValues.betaControl.message ~= "" and siriusValues.betaControl.message or "This beta build is temporarily disabled.", true)
		return
	end

	smartBarOpen = true
	smartBarAnimationId = smartBarAnimationId + 1
	local animationId = smartBarAnimationId

	coreGui.RobloxGui.Backpack.Position = UDim2.new(0,0,0,0)

	-- Set Values for frame properties
	smartBar.BackgroundTransparency = 1
	smartBar.Time.TextTransparency = 1
	smartBar.UIStroke.Transparency = 1
	smartBar.Shadow.ImageTransparency = 1
	smartBar.Visible = true
	smartBar.Position = UDim2.new(0.5, 0, 1.05, 0)
	smartBar.Size = siriusValues.getResponsiveSmartBarSize(false)
	if siriusValues.syncSmartBarCommandSearchLayout then
		siriusValues.syncSmartBarCommandSearchLayout()
	end
	toggle.Rotation = 180
	toggle.Visible = not checkSetting("Hide Toggle Button").current

	if checkTools() then
		toggle.Position = UDim2.new(0.5,0,1,-68)
	else
		toggle.Position = UDim2.new(0.5, 0, 1, -5)
	end

	for _, button in ipairs(getSmartBarButtons()) do
		cancelSmartBarOptionTweens(button)
		button.UIGradient.Rotation = -120
		button.UIStroke.UIGradient.Rotation = -120
		button.Size = UDim2.new(0,30,0,30)
		button.Position = UDim2.new(button.Position.X.Scale, 0, 1.3, 0)
		button.BackgroundTransparency = 1
		button.UIStroke.Transparency = 1
		applySharpIcon(button.Icon, 1)
	end

	tweenService:Create(coreGui.RobloxGui.Backpack, TweenInfo.new(0.6, Enum.EasingStyle.Quint), {Position = UDim2.new(-0.325,0,0,0)}):Play()

	tweenService:Create(toggle, TweenInfo.new(0.82, Enum.EasingStyle.Quint), {Rotation = 0}):Play()
	tweenService:Create(smartBar, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Position = UDim2.new(0.5, 0, 1, -12)}):Play()
	tweenService:Create(toastsContainer, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Position = UDim2.new(0.5, 0, 1, -110)}):Play()
	tweenService:Create(toggle, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Position = UDim2.new(0.5, 0, 1, -85)}):Play()
	tweenService:Create(smartBar, TweenInfo.new(0.6, Enum.EasingStyle.Quint), {Size = siriusValues.getResponsiveSmartBarSize(true)}):Play()
	tweenService:Create(smartBar, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {BackgroundTransparency = 0.18}):Play()
	tweenService:Create(smartBar.Shadow, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {ImageTransparency = 0.7}):Play()
	tweenService:Create(smartBar.Time, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
	tweenService:Create(smartBar.UIStroke, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {Transparency = 0.85}):Play()
	tweenService:Create(toggle, TweenInfo.new(0.3, Enum.EasingStyle.Quint), {ImageTransparency = 0}):Play()

	for index, button in ipairs(getSmartBarButtons()) do
		task.delay((index - 1) * 0.026, function()
			if not smartBarOpen or animationId ~= smartBarAnimationId then return end
			cancelSmartBarOptionTweens(button)
			playSmartBarOptionTween(button, button.UIStroke, TweenInfo.new(0.72, Enum.EasingStyle.Quint), {Transparency = 0})
			playSmartBarOptionTween(button, button, TweenInfo.new(0.72, Enum.EasingStyle.Quint), {Size = UDim2.new(0, 36, 0, 36)})
			playSmartBarOptionTween(button, button.UIGradient, TweenInfo.new(0.9, Enum.EasingStyle.Quint), {Rotation = 50})
			playSmartBarOptionTween(button, button.UIStroke.UIGradient, TweenInfo.new(0.9, Enum.EasingStyle.Quint), {Rotation = 50})
			playSmartBarOptionTween(button, button, TweenInfo.new(0.72, Enum.EasingStyle.Exponential), {Position = UDim2.new(button.Position.X.Scale, 0, 0.5, 0)})
			playSmartBarOptionTween(button, button, TweenInfo.new(0.72, Enum.EasingStyle.Quint), {BackgroundTransparency = 0})
			playSmartBarOptionTween(button, button.Icon, TweenInfo.new(0.72, Enum.EasingStyle.Quint), {ImageColor3 = actionIconColor, ImageTransparency = 0})
		end)
	end
end

local function closeSmartBar()
	smartBarOpen = false
	smartBarAnimationId = smartBarAnimationId + 1
	local animationId = smartBarAnimationId

	for _, otherPanel in ipairs(UI:GetChildren()) do
		if smartBar.Buttons:FindFirstChild(otherPanel.Name) then
			if isPanel(otherPanel.Name) and otherPanel.Visible then
				task.spawn(closePanel, otherPanel.Name, true)
				task.wait()
			end
		end
	end

	tweenService:Create(smartBar.Time, TweenInfo.new(0.4, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
	local smartBarButtons = getSmartBarButtons()
	for index, Button in ipairs(smartBarButtons) do
		task.delay((#smartBarButtons - index) * 0.012, function()
			if smartBarOpen or animationId ~= smartBarAnimationId then return end
			cancelSmartBarOptionTweens(Button)
			playSmartBarOptionTween(Button, Button.UIStroke, TweenInfo.new(0.28, Enum.EasingStyle.Quint), {Transparency = 1})
			playSmartBarOptionTween(Button, Button, TweenInfo.new(0.28, Enum.EasingStyle.Quint), {Size = UDim2.new(0, 30, 0, 30)})
			playSmartBarOptionTween(Button, Button, TweenInfo.new(0.28, Enum.EasingStyle.Quint), {BackgroundTransparency = 1})
			playSmartBarOptionTween(Button, Button.Icon, TweenInfo.new(0.28, Enum.EasingStyle.Quint), {ImageTransparency = 1})
		end)
	end

	tweenService:Create(coreGui.RobloxGui.Backpack, TweenInfo.new(0.6, Enum.EasingStyle.Quint), {Position = UDim2.new(0, 0, 0, 0)}):Play()

	tweenService:Create(smartBar, TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {BackgroundTransparency = 1}):Play()
	tweenService:Create(smartBar.UIStroke, TweenInfo.new(0.3, Enum.EasingStyle.Quint), {Transparency = 1}):Play()
	tweenService:Create(smartBar.Shadow, TweenInfo.new(0.3, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
	tweenService:Create(smartBar, TweenInfo.new(0.5, Enum.EasingStyle.Back), {Size = siriusValues.getResponsiveSmartBarSize(false)}):Play()
	tweenService:Create(smartBar, TweenInfo.new(0.5, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {Position = UDim2.new(0.5, 0,1, 73)}):Play()

	-- If tools, move the toggle
	if checkTools() then
		tweenService:Create(toggle, TweenInfo.new(0.5, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {Position = UDim2.new(0.5,0,1,-68)}):Play()
		tweenService:Create(toastsContainer, TweenInfo.new(0.5, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {Position = UDim2.new(0.5, 0, 1, -90)}):Play()
		tweenService:Create(toggle, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Rotation = 180}):Play()
	else
		tweenService:Create(toastsContainer, TweenInfo.new(0.5, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {Position = UDim2.new(0.5, 0, 1, -28)}):Play()
		tweenService:Create(toggle, TweenInfo.new(0.45, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {Position = UDim2.new(0.5, 0, 1, -5)}):Play()
		tweenService:Create(toggle, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Rotation = 180}):Play()
	end
end

local function windowFocusChanged(value)
	if checkSirius() then
		if value then -- Window Focused
			setfpscap(tonumber(checkSetting("Artificial FPS Limit").current))
			removeReverbs(0.5)
		else          -- Window unfocused
			if checkSetting("Muffle audio while unfocused").current then createReverb(0.7) end
			if checkSetting("Limit FPS while unfocused").current then setfpscap(60) end
		end
	end
end

function siriusValues.parseDetectionList(raw)
	local list = {}
	raw = tostring(raw or "")
	raw = string.gsub(raw, "[\r\n;|]+", ",")

	for token in string.gmatch(raw, "([^,]+)") do
		token = string.lower(string.gsub(token, "^%s*(.-)%s*$", "%1"))
		if string.sub(token, 1, 1) == "@" then
			token = string.sub(token, 2)
		end
		token = string.gsub(token, "^user:", "")
		token = string.gsub(token, "^username:", "")
		token = string.gsub(token, "^display:", "")
		token = string.gsub(token, "^id:", "")
		if token ~= "" then
			table.insert(list, token)
		end
	end

	return list
end

function siriusValues.matchWatchlistUser(player)
	if not player then return nil end

	local watchlistSetting = checkSetting("Watchlist Users")
	local watchlist = siriusValues.parseDetectionList(watchlistSetting and watchlistSetting.current or "")
	local username = string.lower(tostring(player.Name or ""))
	local displayName = string.lower(tostring(player.DisplayName or ""))
	local userId = tostring(player.UserId or "")

	for _, token in ipairs(watchlist) do
		local partial = string.sub(token, 1, 1) == "*" and string.sub(token, -1) == "*"
		local cleanToken = partial and string.sub(token, 2, -2) or token

		if cleanToken ~= "" and (cleanToken == username or cleanToken == displayName or cleanToken == userId) then
			return token
		end

		if partial and cleanToken ~= "" and (string.find(username, cleanToken, 1, true) or string.find(displayName, cleanToken, 1, true)) then
			return token
		end
	end

	return nil
end

function siriusValues.checkKnownUserWatchlist(player)
	local setting = checkSetting("Known User Watchlist")
	if not setting or not setting.current or not player or player == localPlayer then return end

	siriusValues.knownUserWatchlistSeen = siriusValues.knownUserWatchlistSeen or {}
	if siriusValues.knownUserWatchlistSeen[player.UserId] then return end

	local matchedToken = siriusValues.matchWatchlistUser(player)
	if not matchedToken then return end

	siriusValues.knownUserWatchlistSeen[player.UserId] = true
	local protectionSetting = checkSetting("Watchlist Protection Mode")
	if protectionSetting and protectionSetting.current then
		if siriusValues.stopActiveAbilitiesForModeratorDetection then
			siriusValues.stopActiveAbilitiesForModeratorDetection()
		end
		promptModerator(player, "Watchlist Match: "..matchedToken, "Watchlist User Joined", "Sirius matched your watchlist and disabled active abilities.")
		queueNotification("Watchlist User Joined", player.DisplayName.." (@"..player.Name..") matched your watchlist. Active abilities were stopped.", 3944670656)
	else
		queueNotification("Watchlist User Joined", player.DisplayName.." (@"..player.Name..") matched your watchlist.", 3944670656)
	end
end

function siriusValues.detectChatKeyword(player, message)
	local setting = checkSetting("Chat Keyword Detection")
	if not setting or not setting.current or not player or player == localPlayer or not message then return end

	local keywordSetting = checkSetting("Chat Keywords")
	local keywords = siriusValues.parseDetectionList(keywordSetting and keywordSetting.current or "")
	if #keywords == 0 then return end

	local rawMessage = tostring(message or "")
	local lowerMessage = string.lower(rawMessage)
	local wordMessage = " "..string.gsub(lowerMessage, "[%p%c%s]+", " ").." "

	for _, keyword in ipairs(keywords) do
		local matched = false

		if #keyword <= 3 and string.match(keyword, "^%w+$") then
			matched = string.find(wordMessage, " "..keyword.." ", 1, true) ~= nil
		else
			matched = string.find(lowerMessage, keyword, 1, true) ~= nil
		end

		if matched then
			siriusValues.chatKeywordDetectionSeen = siriusValues.chatKeywordDetectionSeen or {}
			local key = tostring(player.UserId)..":"..keyword
			local now = os.clock()
			local cooldownSetting = checkSetting("Detection Alert Cooldown")
			local cooldown = math.clamp(tonumber(cooldownSetting and cooldownSetting.current) or 10, 3, 60)
			if siriusValues.chatKeywordDetectionSeen[key] and now - siriusValues.chatKeywordDetectionSeen[key] < cooldown then
				return
			end
			siriusValues.chatKeywordDetectionSeen[key] = now

			local snippet = string.gsub(rawMessage, "[\r\n]+", " ")
			if string.len(snippet) > 80 then
				snippet = string.sub(snippet, 1, 77).."..."
			end

			local protectionSetting = checkSetting("Chat Keyword Protection Mode")
			if protectionSetting and protectionSetting.current then
				if siriusValues.stopActiveAbilitiesForModeratorDetection then
					siriusValues.stopActiveAbilitiesForModeratorDetection()
				end
				promptModerator(player, "Keyword: "..keyword, "Chat Keyword Detected", "Sirius matched a watched chat keyword and disabled active abilities.")
				queueNotification("Chat Keyword Detected", player.DisplayName.." matched \""..keyword.."\". Active abilities were stopped.", 3944670656)
			else
				queueNotification("Chat Keyword Detected", player.DisplayName.." matched \""..keyword.."\": "..snippet, 3944670656)
			end
			return
		end
	end
end

function siriusValues.checkModeratorAdminJoin(player)
	if not player or player == localPlayer then return end
	if not Pro or siriusValues.currentCreator ~= "group" then return end

	local setting = checkSetting("Moderator / Admin Join Detection")
	if not setting or not setting.current then return end

	siriusValues.moderatorDetectionSeen = siriusValues.moderatorDetectionSeen or {}
	if siriusValues.moderatorDetectionSeen[player.UserId] then return end

	local roleSuccess, roleFound = pcall(function()
		return player:GetRoleInGroup(creatorId)
	end)

	if not roleSuccess or type(roleFound) ~= "string" or roleFound == "" or roleFound == "Guest" then return end

	local roleLower = string.lower(roleFound)
	for _, role in pairs(siriusValues.administratorRoles) do 
		role = string.lower(tostring(role))

		if role ~= "" and string.find(roleLower, role, 1, true) then
			siriusValues.moderatorDetectionSeen[player.UserId] = true
			if siriusValues.stopActiveAbilitiesForModeratorDetection then
				siriusValues.stopActiveAbilitiesForModeratorDetection()
			end
			promptModerator(player, roleFound)
			queueNotification("Moderator / Admin Joined", siriusValues.currentGroup .." "..roleFound.." ".. player.DisplayName .." has joined your session. Active abilities were stopped.", 3944670656)
			break
		end
	end
end

local function onChatted(player, message)
	if not message or not checkSirius() then return end

	if siriusValues.spamGuard and siriusValues.spamGuard.evaluate then
		local flagged, reason = siriusValues.spamGuard:evaluate(player, message)
		if flagged then
			if reason ~= "repeat" then
				siriusValues.spamGuard:report(player and player.UserId)
			end
			siriusValues.spamGuard:notify()
			task.spawn(function()
				siriusValues.spamGuard:scrub()
			end)
		end
	end

	siriusValues.detectChatKeyword(player, message)

	if checkSetting("Log Messages").current then
		local logData = {
			["content"] = message,
			["avatar_url"] = "https://www.roblox.com/headshot-thumbnail/image?userId="..player.UserId.."&width=420&height=420&format=png",
			["username"] = player.DisplayName,
			["allowed_mentions"] = {parse = {}}
		}

		logData = httpService:JSONEncode(logData)

		pcall(function()
			local req = originalRequest({
				Url = checkSetting("Message Webhook URL").current,
				Method = 'POST',
				Headers = {
					['Content-Type'] = 'application/json',
				},
				Body = logData
			})
		end)
	end
end

if game:FindFirstChildOfClass("TextChatService") then
	game:FindFirstChildOfClass("TextChatService").MessageReceived:Connect(function(message)
		if not message or not message.TextSource then return end

		local speaker = players:GetPlayerByUserId(message.TextSource.UserId)
		if speaker then
			siriusValues.detectChatKeyword(speaker, message.Text or "")
		end
	end)
end

local function sortPlayers()
	local newTable = playerlistPanel.Interactions.List:GetChildren()

	for index, player in ipairs(newTable) do
		if player.ClassName ~= "Frame" or player.Name == "Placeholder" then
			table.remove(newTable, index)
		end
	end

	table.sort(newTable, function(playerA, playerB)
		return playerA.Name < playerB.Name
	end)

	for index, frame in ipairs(newTable) do
		if frame.ClassName == "Frame" then
			if frame.Name ~= "Placeholder" then
				frame.LayoutOrder = index 
			end
		end
	end
end

local function kill(player)
	-- kill
end

local function teleportTo(player)
	local targetCharacter = workspace:FindFirstChild(player.Name)
	local targetRoot = targetCharacter and targetCharacter:FindFirstChild("HumanoidRootPart")
	local localCharacter = localPlayer.Character
	local localRoot = localCharacter and localCharacter:FindFirstChild("HumanoidRootPart")

	if players:FindFirstChild(player.Name) and targetRoot and localRoot then
		queueNotification("Teleportation", "Teleporting to "..player.DisplayName..".")
		localRoot.CFrame = CFrame.new(targetRoot.Position.X, targetRoot.Position.Y, targetRoot.Position.Z)
	else
		queueNotification("Teleportation Error", player.DisplayName.." cannot be teleported to right now.")
	end
end

local function createPlayer(player)
	if not checkSirius() then return end

	if playerlistPanel.Interactions.List:FindFirstChild(player.Name) then return end

	local newPlayer = playerlistPanel.Interactions.List.Template:Clone()
	newPlayer.Name = player.Name
	newPlayer.Parent = playerlistPanel.Interactions.List
	newPlayer.Visible = not searchingForPlayer

	newPlayer.NoActions.Visible = false
	newPlayer.PlayerInteractions.Visible = false
	newPlayer.Role.Visible = false

	newPlayer.Size = UDim2.new(0, 539, 0, 45)
	newPlayer.DisplayName.Position = UDim2.new(0, 53, 0.5, 0)
	newPlayer.DisplayName.Size = UDim2.new(0, 224, 0, 16)
	newPlayer.Avatar.Size = UDim2.new(0, 30, 0, 30)

	sortPlayers()

	newPlayer.DisplayName.TextTransparency = 0
	newPlayer.DisplayName.TextScaled = true
	newPlayer.DisplayName.FontFace.Weight = Enum.FontWeight.Medium
	newPlayer.DisplayName.Text = player.DisplayName
	newPlayer.Avatar.Image = "https://www.roblox.com/headshot-thumbnail/image?userId="..player.UserId.."&width=420&height=420&format=png"

	if creatorType == Enum.CreatorType.Group then
		task.spawn(function()
			local role = player:GetRoleInGroup(creatorId)
			if role == "Guest" then
				newPlayer.Role.Text = "Group Rank: None"
			else
				newPlayer.Role.Text = "Group Rank: "..role
			end

			newPlayer.Role.Visible = true
			newPlayer.Role.TextTransparency = 1
		end)
	end

	local selectAction
	local clearPlayerActions
	local preservingSelectedAction = false
	local function prepareActionSlot()
		local actionParent = newPlayer.PlayerInteractions

		actionParent.AnchorPoint = Vector2.new(1, 0.5)
		actionParent.Position = UDim2.new(1, -28, 0.5, 0)
		actionParent.Size = UDim2.new(0, 112, 0, 40)
		actionParent.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
		actionParent.BackgroundTransparency = 1
		actionParent.BorderSizePixel = 0
		actionParent.ClipsDescendants = false
		actionParent.ZIndex = newPlayer.ZIndex + 10
		pcall(function()
			actionParent.Image = ""
			actionParent.ImageTransparency = 1
		end)

		for _, action in ipairs(actionParent:GetChildren()) do
			if action:IsA("GuiObject") and action.Name ~= "Select" then
				action.Visible = false
				pcall(function()
					action.BackgroundTransparency = 1
					action.ImageTransparency = 1
				end)
			elseif action:IsA("UIStroke") then
				action.Transparency = 1
			end
		end
	end

	local function ensureSelectAction()
		prepareActionSlot()

		if selectAction and selectAction.Parent then
			selectAction.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
			selectAction.BorderSizePixel = 0
			selectAction.TextColor3 = Color3.fromRGB(255, 255, 255)
			selectAction.TextStrokeTransparency = 1
			selectAction.ZIndex = newPlayer.PlayerInteractions.ZIndex + 1
			if selectAction:FindFirstChild("UIStroke") then
				selectAction.UIStroke.Color = Color3.fromRGB(58, 58, 58)
			end
			return selectAction
		end

		selectAction = Instance.new("TextButton")
		selectAction.Name = "Select"
		selectAction.AnchorPoint = Vector2.new(0.5, 0.5)
		selectAction.Position = UDim2.new(0.5, 0, 0.5, 0)
		selectAction.Size = UDim2.new(0, 96, 0, 36)
		selectAction.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
		selectAction.BackgroundTransparency = 1
		selectAction.BorderSizePixel = 0
		selectAction.AutoButtonColor = false
		selectAction.Text = "Select"
		selectAction.TextColor3 = Color3.fromRGB(255, 255, 255)
		selectAction.TextTransparency = 1
		selectAction.TextStrokeTransparency = 1
		selectAction.TextSize = 15
		selectAction.FontFace = Font.new("rbxassetid://12187365364", Enum.FontWeight.SemiBold)
		selectAction.ZIndex = newPlayer.PlayerInteractions.ZIndex + 1
		selectAction.Visible = false
		selectAction.Parent = newPlayer.PlayerInteractions

		local corner = Instance.new("UICorner")
		corner.CornerRadius = UDim.new(1, 0)
		corner.Parent = selectAction

		local stroke = Instance.new("UIStroke")
		stroke.Name = "UIStroke"
		stroke.Color = Color3.fromRGB(58, 58, 58)
		stroke.Thickness = 1
		stroke.Transparency = 1
		stroke.Parent = selectAction

		selectAction.MouseEnter:Connect(function()
			if not selectAction.Visible then return end
			tweenService:Create(selectAction, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {BackgroundColor3 = Color3.fromRGB(44, 44, 44)}):Play()
			tweenService:Create(stroke, TweenInfo.new(0.18, Enum.EasingStyle.Quint), {Color = Color3.fromRGB(74, 74, 74)}):Play()
		end)

		selectAction.MouseLeave:Connect(function()
			tweenService:Create(selectAction, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {BackgroundColor3 = Color3.fromRGB(35, 35, 35)}):Play()
			tweenService:Create(stroke, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {Color = Color3.fromRGB(58, 58, 58)}):Play()
		end)

		selectAction.MouseButton1Click:Connect(function()
			if debounce or not playerlistPanel.Visible then return end
			if playerActionSystem.openPicker then
				preservingSelectedAction = true
				local sourceSnapshot = {
					AbsolutePosition = selectAction.AbsolutePosition,
					AbsoluteSize = selectAction.AbsoluteSize
				}

				task.spawn(function()
					tweenService:Create(toggle, TweenInfo.new(0.45, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {Position = UDim2.new(0.5, 0, 1, -85)}):Play()
					closePanel("Playerlist", true)
					preservingSelectedAction = false
					clearPlayerActions()
					newPlayer.Size = UDim2.new(0, 539, 0, 45)
					newPlayer.DisplayName.Position = UDim2.new(0, 53, 0.5, 0)
					newPlayer.Avatar.Size = UDim2.new(0, 30, 0, 30)
					newPlayer.Role.TextTransparency = 1

					if not players:FindFirstChild(player.Name) then return end

					playerActionSystem.openPicker(player, sourceSnapshot, function()
						if not checkSirius() then return end
						if not smartBarOpen or playerlistPanel.Visible or debounce then return end
						task.spawn(openPanel, "Playerlist")
					end)
				end)
			end
		end)

		return selectAction
	end

	clearPlayerActions = function()
		prepareActionSlot()
		newPlayer.PlayerInteractions.Visible = false

		if selectAction then
			selectAction.Visible = false
			selectAction.BackgroundTransparency = 1
			selectAction.TextTransparency = 1
			if selectAction:FindFirstChild("UIStroke") then
				selectAction.UIStroke.Transparency = 1
			end
		end
	end

	clearPlayerActions()

	local function openInteractions()
		if player == localPlayer then
			clearPlayerActions()
			return
		end

		local action = ensureSelectAction()
		prepareActionSlot()
		newPlayer.PlayerInteractions.Visible = true
		newPlayer.PlayerInteractions.BackgroundTransparency = 1
		action.Visible = true
		action.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
		action.BackgroundTransparency = 1
		action.TextColor3 = Color3.fromRGB(255, 255, 255)
		action.TextTransparency = 1
		action.TextStrokeTransparency = 1
		if action:FindFirstChild("UIStroke") then
			action.UIStroke.Color = Color3.fromRGB(58, 58, 58)
			action.UIStroke.Transparency = 1
		end

		tweenService:Create(action, TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0}):Play()
		tweenService:Create(action, TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {TextTransparency = 0}):Play()
		if action:FindFirstChild("UIStroke") then
			tweenService:Create(action.UIStroke, TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0}):Play()
		end
	end

	local function closeInteractions()
		if preservingSelectedAction then return end

		if not newPlayer.PlayerInteractions.Visible or not selectAction then
			clearPlayerActions()
			return
		end

		tweenService:Create(selectAction, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 1, TextTransparency = 1}):Play()
		if selectAction:FindFirstChild("UIStroke") then
			tweenService:Create(selectAction.UIStroke, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 1}):Play()
		end

		task.wait(0.24)
		clearPlayerActions()
	end

	newPlayer.MouseEnter:Connect(function()
		if debounce or not playerlistPanel.Visible then return end
		tweenService:Create(newPlayer.UIStroke, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Transparency = 1}):Play()
		tweenService:Create(newPlayer.DisplayName, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0.3}):Play()
	end)

	newPlayer.MouseLeave:Connect(function()
		if debounce or not playerlistPanel.Visible then return end
		if preservingSelectedAction then return end
		task.spawn(closeInteractions)
		tweenService:Create(newPlayer.DisplayName, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Position = UDim2.new(0, 53, 0.5, 0)}):Play()
		tweenService:Create(newPlayer, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Size = UDim2.new(0, 539, 0, 45)}):Play()
		tweenService:Create(newPlayer.Avatar, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Size = UDim2.new(0, 30, 0, 30)}):Play()
		tweenService:Create(newPlayer.UIStroke, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Transparency = 0}):Play()
		tweenService:Create(newPlayer.DisplayName, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
		tweenService:Create(newPlayer.Role, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 1}):Play()
	end)

	newPlayer.Interact.MouseButton1Click:Connect(function()
		if debounce or not playerlistPanel.Visible then return end
		if creatorType == Enum.CreatorType.Group then
			tweenService:Create(newPlayer.DisplayName, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Position = UDim2.new(0, 73, 0.39, 0)}):Play()
			tweenService:Create(newPlayer.Role, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0.3}):Play()
		else
			tweenService:Create(newPlayer.DisplayName, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Position = UDim2.new(0, 73, 0.5, 0)}):Play()
		end

		if player ~= localPlayer then openInteractions() end

		tweenService:Create(newPlayer, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Size = UDim2.new(0, 539, 0, 75)}):Play()

		tweenService:Create(newPlayer.DisplayName, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
		tweenService:Create(newPlayer.Avatar, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Size = UDim2.new(0, 50, 0, 50)}):Play()
		tweenService:Create(newPlayer.UIStroke, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Transparency = 0}):Play()
	end)

end

local function removePlayer(player)
	if not checkSirius() then return end

	if playerActionSystem.getPickerTarget and playerActionSystem.getPickerTarget() == player and playerActionSystem.closePicker then
		playerActionSystem.closePicker()
	end

	if playerActionSystem.getSpectateTarget and playerActionSystem.getSpectateTarget() == player and playerActionSystem.stopSpectating then
		playerActionSystem.stopSpectating(player.DisplayName.." left the server.")
	end

	local entry = playerlistPanel.Interactions.List:FindFirstChild(player.Name)
	if entry then
		entry:Destroy()
	end
end

local settingsAnimationId = 0
local dylanfocBackgroundAsset = nil

local function getDylanfocUtilityBackgroundImage()
	if dylanfocBackgroundAsset then
		return dylanfocBackgroundAsset
	end

	local image = siriusValues.dylanfocUtilityBackgroundImage

	if image and getCachedScriptImage then
		local success, cachedImage = pcall(getCachedScriptImage, image)
		if success and cachedImage then
			image = cachedImage
		end
	end

	dylanfocBackgroundAsset = image
	return image
end

function getDylanfocUtilityBackground()
	local background = settingsPanel:FindFirstChild("DylanfocUtilityBackground")
	if background then
		background.Image = getDylanfocUtilityBackgroundImage()
		return background
	end

	background = Instance.new("ImageLabel")
	background.Name = "DylanfocUtilityBackground"
	background.Parent = settingsPanel
	background.AnchorPoint = Vector2.new(0.5, 0.5)
	background.Position = UDim2.fromScale(0.5, 0.5)
	background.Size = UDim2.fromScale(1, 1)
	background.BackgroundTransparency = 1
	background.Image = getDylanfocUtilityBackgroundImage()
	background.ScaleType = Enum.ScaleType.Crop
	background.ImageTransparency = 1
	background.Visible = false
	background.ZIndex = math.max(0, settingsPanel.SettingLists.ZIndex - 1)

	local corner = Instance.new("UICorner")
	corner.Name = "DylanfocUtilityBackgroundCorner"
	corner.Parent = background
	corner.CornerRadius = settingsPanel:FindFirstChildOfClass("UICorner") and settingsPanel:FindFirstChildOfClass("UICorner").CornerRadius or UDim.new(0, 18)

	local fade = Instance.new("UIGradient")
	fade.Name = "DylanfocUtilityBackgroundFade"
	fade.Parent = background
	fade.Rotation = 90
	fade.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0.18),
		NumberSequenceKeypoint.new(0.5, 0.42),
		NumberSequenceKeypoint.new(1, 0.74)
	})

	return background
end

function setDylanfocUtilityBackgroundVisible(visible)
	local background = getDylanfocUtilityBackground()
	local tweenInfo = TweenInfo.new(visible and 0.34 or 0.18, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

	if visible then
		background.Visible = true
		background.ImageTransparency = math.min(background.ImageTransparency, 1)
		tweenService:Create(background, tweenInfo, {ImageTransparency = 0.42}):Play()
	else
		tweenService:Create(background, tweenInfo, {ImageTransparency = 1}):Play()
		task.delay(0.2, function()
			if background.ImageTransparency >= 0.98 then
				background.Visible = false
			end
		end)
	end
end

local function openSettings()
	debounce = true
	settingsAnimationId += 1
	local animationId = settingsAnimationId

	settingsPanel.BackgroundTransparency = 1
	settingsPanel.Title.TextTransparency = 1
	settingsPanel.Subtitle.TextTransparency = 1
	settingsPanel.Back.ImageTransparency = 1
	settingsPanel.Shadow.ImageTransparency = 1

	local dylanfocBackground = settingsPanel:FindFirstChild("DylanfocUtilityBackground")
		if dylanfocBackground then
			dylanfocBackground.Visible = false
			dylanfocBackground.ImageTransparency = 1
		end

		ensureSettingsCategoryScroller()
		wipeTransparency(settingsPanel.SettingTypes, 1, true)

		settingsPanel.Visible = true
	settingsPanel.UIGradient.Enabled = true
	settingsPanel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
		settingsPanel.UIGradient.Color = ColorSequence.new({ColorSequenceKeypoint.new(0, Color3.new(0.0470588, 0.0470588, 0.0470588)),ColorSequenceKeypoint.new(1, Color3.new(0.0470588, 0.0470588, 0.0470588))})
		settingsPanel.UIGradient.Offset = Vector2.new(0, 1.7)
		settingsPanel.SettingTypes.Visible = true
		settingsPanel.SettingLists.Visible = false
		if settingsPanel.SettingTypes:IsA("ScrollingFrame") then
			settingsPanel.SettingTypes.CanvasPosition = Vector2.new(0, 0)
			settingsPanel.SettingTypes.ScrollBarImageTransparency = 1
		end
		refreshSettingsCategoryScroll()
		settingsPanel.Size = UDim2.new(0, 552, 0, 346)
		settingsPanel.Title.Position = UDim2.new(0.045, 0, 0.057, 0)

	settingsPanel.Title.Text = "Settings"
	settingsPanel.Subtitle.Text = "Adjust your preferences, set new keybinds, test out new features and more."

	local panelOpenTween = TweenInfo.new(0.48, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
	local panelFadeTween = TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
	local contentTween = TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
	local cardTween = TweenInfo.new(0.4, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

	tweenService:Create(settingsPanel, panelOpenTween, {Size = UDim2.new(0, 613, 0, 384)}):Play()
	tweenService:Create(settingsPanel, panelFadeTween, {BackgroundTransparency = 0}):Play()
	tweenService:Create(settingsPanel.UIGradient, TweenInfo.new(0.58, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Offset = Vector2.new(0, 1.05)}):Play()
	tweenService:Create(settingsPanel.Shadow, panelOpenTween, {ImageTransparency = 0.66}):Play()
		tweenService:Create(settingsPanel.Title, contentTween, {TextTransparency = 0}):Play()
		tweenService:Create(settingsPanel.Subtitle, contentTween, {TextTransparency = 0}):Play()
		task.defer(refreshSettingsCategoryScroll)
		task.delay(0.12, refreshSettingsCategoryScroll)
		task.delay(0.32, refreshSettingsCategoryScroll)
		task.delay(0.52, refreshSettingsCategoryScroll)

	task.wait(0.04)

	local cardIndex = 0
	for _, settingType in ipairs(settingsPanel.SettingTypes:GetChildren()) do
		if settingType.ClassName == "Frame" then
			cardIndex = cardIndex + 1
			local gradientRotation = math.random(78, 95)

			task.delay(cardIndex * 0.018, function()
				if animationId ~= settingsAnimationId or not settingsPanel.Visible then return end

				tweenService:Create(settingType.UIGradient, cardTween, {Rotation = gradientRotation}):Play()
				tweenService:Create(settingType.Shadow.UIGradient, cardTween, {Rotation = gradientRotation}):Play()
				tweenService:Create(settingType.UIStroke.UIGradient, cardTween, {Rotation = gradientRotation}):Play()
				tweenService:Create(settingType, cardTween, {BackgroundTransparency = 0}):Play()
				tweenService:Create(settingType.Shadow, cardTween, {ImageTransparency = 0.66}):Play()
				tweenService:Create(settingType.UIStroke, cardTween, {Transparency = 0}):Play()
				tweenService:Create(settingType.Title, cardTween, {TextTransparency = 0.2}):Play()
			end)
		end
	end

	task.wait(0.26)
	if animationId ~= settingsAnimationId then return end

	for _, settingList in ipairs(settingsPanel.SettingLists:GetChildren()) do
		if settingList.ClassName == "ScrollingFrame" then
			for _, setting in ipairs(settingList:GetChildren()) do
				if setting.ClassName == "Frame" then
					if settingList.Name == "Servers" and setting.Name == "Placeholder" then
						setting.Visible = false
						setting.Size = UDim2.new(0, 0, 0, 0)
					else
						setting.Visible = true
					end
				end
			end
		end
	end

	debounce = false
end

local function closeSettings()
	debounce = true
	settingsAnimationId += 1
	local animationId = settingsAnimationId
	settingsPanel.ClipsDescendants = true

	local contentFadeTween = TweenInfo.new(0.14, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
	local panelCloseTween = TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut)
	local panelFadeTween = TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.In)

	for _, settingType in ipairs(settingsPanel.SettingTypes:GetChildren()) do
		if settingType.ClassName == "Frame" then
			tweenService:Create(settingType, contentFadeTween, {BackgroundTransparency = 1}):Play()
			tweenService:Create(settingType.Shadow, contentFadeTween, {ImageTransparency = 1}):Play()
			tweenService:Create(settingType.UIStroke, contentFadeTween, {Transparency = 1}):Play()
			tweenService:Create(settingType.Title, contentFadeTween, {TextTransparency = 1}):Play()
		end
	end

	tweenService:Create(settingsPanel.Shadow, panelFadeTween, {ImageTransparency = 1}):Play()
	tweenService:Create(settingsPanel.Back, contentFadeTween, {ImageTransparency = 1}):Play()
	tweenService:Create(settingsPanel.Title, contentFadeTween, {TextTransparency = 1}):Play()
	tweenService:Create(settingsPanel.Subtitle, contentFadeTween, {TextTransparency = 1}):Play()
	tweenService:Create(settingsPanel.UIGradient, panelCloseTween, {Offset = Vector2.new(0, 1.65)}):Play()

	local dylanfocBackground = settingsPanel:FindFirstChild("DylanfocUtilityBackground")
	if dylanfocBackground then
		tweenService:Create(dylanfocBackground, contentFadeTween, {ImageTransparency = 1}):Play()
	end

	for _, settingList in ipairs(settingsPanel.SettingLists:GetChildren()) do
		if settingList.ClassName == "ScrollingFrame" then
			tweenService:Create(settingList, contentFadeTween, {ScrollBarImageTransparency = 1}):Play()
		end
	end

	tweenService:Create(settingsPanel, panelCloseTween, {Size = UDim2.new(0, 584, 0, 18)}):Play()
	tweenService:Create(settingsPanel, panelFadeTween, {BackgroundTransparency = 1}):Play()

	task.wait(0.3)
	if animationId ~= settingsAnimationId then return end

	for _, settingList in ipairs(settingsPanel.SettingLists:GetChildren()) do
		if settingList.ClassName == "ScrollingFrame" then
			for _, setting in ipairs(settingList:GetChildren()) do
				if setting.ClassName == "Frame" then
					setting.Visible = false
				end
			end
		end
	end

	settingsPanel.SettingLists.Visible = false
	if dylanfocBackground then
		dylanfocBackground.Visible = false
	end
	settingsPanel.Visible = false
	debounce = false
end

local function saveSettings()
	checkFolder()

	if typeof(writefile) == "function" then
		pcall(writefile, siriusValues.siriusFolder.."/"..siriusValues.settingsFile, httpService:JSONEncode(siriusSettings))
	end
end

siriusValues.keybindRows = siriusValues.keybindRows or {}
siriusValues.keybindDefaults = siriusValues.keybindDefaults or {}

function siriusValues.normaliseKeybind(value)
	if value == nil then
		return nil
	end

	value = tostring(value)
	if value == "" or value == "No Keybind" or value == "Press any key..." then
		return nil
	end

	local parts = {}
	for part in string.gmatch(value, "[^+]+") do
		part = string.gsub(part, "%s+", "")
		if part ~= "" then
			table.insert(parts, part)
		end
	end

	if #parts == 0 then
		return nil
	end

	return table.concat(parts, "+")
end

function siriusValues.formatKeybind(value)
	local normalised = siriusValues.normaliseKeybind(value)
	if not normalised then
		return "No Keybind"
	end

	return string.gsub(normalised, "+", " + ")
end

function siriusValues.isKeybindModifier(keyName)
	return keyName == "LeftControl"
		or keyName == "RightControl"
		or keyName == "LeftShift"
		or keyName == "RightShift"
		or keyName == "LeftAlt"
		or keyName == "RightAlt"
		or keyName == "LeftMeta"
		or keyName == "RightMeta"
end

function siriusValues.keybindSettingKey(setting)
	return setting and (setting.id or setting.name)
end

function siriusValues.captureKeybindDefaults()
	if siriusValues.keybindDefaultsCaptured then
		return
	end

	for _, category in ipairs(siriusSettings) do
		if category.name == keybindCategoryName then
			for _, setting in ipairs(category.categorySettings) do
				if setting.settingType == "Key" then
					siriusValues.keybindDefaults[siriusValues.keybindSettingKey(setting)] = setting.current
				end
			end
			break
		end
	end

	siriusValues.keybindDefaultsCaptured = true
end

function siriusValues.updateKeybindRow(setting)
	local row = siriusValues.keybindRows[siriusValues.keybindSettingKey(setting)]
	if not row or not row:FindFirstChild("InputFrame") then
		return
	end

	local inputBox = row.InputFrame:FindFirstChild("InputBox")
	if not inputBox then
		return
	end

	inputBox.Text = siriusValues.formatKeybind(setting.current)
	row.InputFrame.Size = UDim2.new(0, inputBox.TextBounds.X + 24, 0, 30)
end

function siriusValues.updateAllKeybindRows()
	for _, category in ipairs(siriusSettings) do
		if category.name == keybindCategoryName then
			for _, setting in ipairs(category.categorySettings) do
				if setting.settingType == "Key" then
					siriusValues.updateKeybindRow(setting)
				end
			end
			break
		end
	end
end

function siriusValues.findKeybindConflict(value, currentSetting)
	local target = siriusValues.normaliseKeybind(value)
	if not target then
		return nil
	end

	for _, category in ipairs(siriusSettings) do
		if category.name == keybindCategoryName then
			for _, setting in ipairs(category.categorySettings) do
				if setting.settingType == "Key" and setting ~= currentSetting then
					if siriusValues.normaliseKeybind(setting.current) == target then
						return setting
					end
				end
			end
			break
		end
	end

	return nil
end

function siriusValues.buildKeybindFromInput(input, storedModifiers)
	if input.KeyCode == Enum.KeyCode.Unknown then
		return nil
	end

	local keyName = input.KeyCode.Name
	local parts = {}
	local added = {}

	local function addModifier(modifierName)
		if modifierName ~= keyName and Enum.KeyCode[modifierName] and not added[modifierName] then
			table.insert(parts, modifierName)
			added[modifierName] = true
		end
	end

	if storedModifiers then
		for _, modifierName in ipairs(storedModifiers) do
			addModifier(modifierName)
		end
	end

	for _, modifierName in ipairs({"LeftControl", "RightControl", "LeftShift", "RightShift", "LeftAlt", "RightAlt", "LeftMeta", "RightMeta"}) do
		if Enum.KeyCode[modifierName] and userInputService:IsKeyDown(Enum.KeyCode[modifierName]) then
			addModifier(modifierName)
		end
	end

	table.insert(parts, keyName)
	return table.concat(parts, "+")
end

function siriusValues.keybindMatchesInput(value, input)
	local normalised = siriusValues.normaliseKeybind(value)
	if not normalised or input.KeyCode == Enum.KeyCode.Unknown then
		return false
	end

	local parts = {}
	for part in string.gmatch(normalised, "[^+]+") do
		table.insert(parts, part)
	end

	if #parts == 0 then
		return false
	end

	local mainKey = parts[#parts]
	if input.KeyCode.Name ~= mainKey then
		return false
	end

	for index = 1, #parts - 1 do
		local modifierName = parts[index]
		if Enum.KeyCode[modifierName] and not userInputService:IsKeyDown(Enum.KeyCode[modifierName]) then
			return false
		end
	end

	return true
end

function siriusValues.keybindSection(setting)
	local name = setting.name or ""

	if name == "Toggle Players Panel" then
		return "Players"
	elseif name == "Toggle Music Panel" then
		return "Music"
	elseif string.find(name, "Ghost", 1, true) then
		return "Ghost Mode"
	elseif name == "Toggle Character Panel"
		or name == "NoClip"
		or name == "Flight"
		or name == "Fly Up Key"
		or name == "Fly Down Key"
		or name == "Refresh"
		or name == "Respawn"
		or name == "Fling"
		or name == "ESP"
		or name == "Night and Day"
		or name == "Global Audio"
		or name == "Infinite Jump"
		or name == "Walk in Air"
		or name == "Anti Void"
		or name == "Auto Find [works with auto aim]"
		or name == "click teleport tool"
		or name == "Shaders"
		or name == "Invisibility" then
		return "Character"
	end

	return "UI"
end

function siriusValues.getKeybindRenderSettings(settings)
	local sectionOrder = {"UI", "Character", "Ghost Mode", "Players", "Music"}
	local descriptions = {
		UI = "Menu, search, panel, reset, and emergency controls.",
		Character = "Character actions and movement ability controls.",
		["Ghost Mode"] = "Spectate and free-camera movement controls.",
		Players = "Player list and player-focused controls.",
		Music = "Music panel controls."
	}
	local groups = {}
	local rendered = {}

	for _, sectionName in ipairs(sectionOrder) do
		groups[sectionName] = {}
	end

	for _, setting in ipairs(settings) do
		local sectionName = siriusValues.keybindSection(setting)
		table.insert(groups[sectionName] or groups.UI, setting)
	end

	for _, sectionName in ipairs(sectionOrder) do
		local group = groups[sectionName]
		if group and #group > 0 then
			table.insert(rendered, {
				name = sectionName,
				description = descriptions[sectionName],
				settingType = "Info"
			})

			for _, setting in ipairs(group) do
				table.insert(rendered, setting)
			end
		end
	end

	return rendered
end

function siriusValues.resetKeybinds()
	siriusValues.captureKeybindDefaults()

	for _, category in ipairs(siriusSettings) do
		if category.name == keybindCategoryName then
			for _, setting in ipairs(category.categorySettings) do
				if setting.settingType == "Key" then
					setting.current = siriusValues.keybindDefaults[siriusValues.keybindSettingKey(setting)]
					siriusValues.updateKeybindRow(setting)
				end
			end
			break
		end
	end

	saveSettings()
	queueNotification("Keybinds Reset", "All keybinds were restored to their Sirius defaults.", 4400696294)
end

local dylanfocClient = {
	infoRows = {},
	inputRows = {}
}

function dylanfocClient.getEnv()
	if getgenv then
		local success, env = pcall(getgenv)
		if success and env then
			return env
		end
	end

	return _G
end

function dylanfocClient.shortValue(value, limit)
	value = tostring(value or "Unavailable")
	limit = limit or 160

	if string.len(value) > limit then
		return string.sub(value, 1, limit - 3).."..."
	end

	return value
end

function dylanfocClient.getValue(valueKey)
	if valueKey == "hwid" then
		if gethwid then
			local success, value = pcall(gethwid)
			if success then
				return value
			end
		end

		return "Unavailable"
	elseif valueKey == "clientId" then
		local success, value = pcall(function()
			return game:GetService("RbxAnalyticsService"):GetClientId()
		end)

		return success and value or "Unavailable"
	elseif valueKey == "executor" then
		if identifyexecutor then
			local success, value = pcall(identifyexecutor)
			if success then
				return value
			end
		end

		return "Unknown executor"
	elseif valueKey == "userId" then
		return localPlayer and localPlayer.UserId or "Unavailable"
	elseif valueKey == "username" then
		return localPlayer and localPlayer.Name or "Unavailable"
	end

	return "Unavailable"
end

function dylanfocClient.setRowDescription(row, text)
	if not row or not row:FindFirstChild("Description") then
		return
	end

	row.Description.Visible = true
	row.Description.TextWrapped = true
	row.Description.Size = UDim2.new(0, 333, 0, 999)
	row.Description.Text = tostring(text or "")
	row.Description.Size = UDim2.new(0, 333, 0, math.max(row.Description.TextBounds.Y + 10, 28))
	row.Size = UDim2.new(0, 558, 0, math.max(row.Description.TextBounds.Y + 44, 68))
end

function dylanfocClient.registerInfoRow(setting, row)
	if setting.valueKey then
		dylanfocClient.infoRows[setting.valueKey] = row
		dylanfocClient.setRowDescription(row, dylanfocClient.shortValue(dylanfocClient.getValue(setting.valueKey), 170))
	elseif setting.description then
		dylanfocClient.setRowDescription(row, setting.description)
	end
end

function dylanfocClient.registerInputRow(setting, row)
	if setting.id then
		dylanfocClient.inputRows[setting.id] = row
	end
end

function dylanfocClient.refreshInfoRows()
	for valueKey, row in pairs(dylanfocClient.infoRows) do
		dylanfocClient.setRowDescription(row, dylanfocClient.shortValue(dylanfocClient.getValue(valueKey), 170))
	end
end

function dylanfocClient.getSettingValue(settingName)
	local setting = checkSetting(settingName, dylanfocCategoryName)
	if setting and setting.current ~= nil then
		return tostring(setting.current)
	end

	return ""
end

function dylanfocClient.notify(title, description, image)
	queueNotification(title, description, image or 4400696294)
end

function dylanfocClient.copyValue(label, valueKey)
	local value = dylanfocClient.getValue(valueKey)

	if not setclipboard then
		dylanfocClient.notify("Clipboard Unavailable", "Your executor does not expose setclipboard, so Sirius could not copy "..label..".", 4370336704)
		return
	end

	local success = pcall(function()
		setclipboard(tostring(value))
	end)

	if success then
		dylanfocClient.notify(label.." Copied", "Sirius copied the current "..label.." to your clipboard.", 4400696294)
	else
		dylanfocClient.notify("Copy Failed", "Sirius could not copy "..label.." to your clipboard.", 4370336704)
	end
end

function dylanfocClient.spoofClientId()
	local spoofValue = dylanfocClient.getSettingValue("Spoof ClientID Value")

	if spoofValue == "" then
		dylanfocClient.notify("Missing ClientID", "Enter a spoofed ClientID value before applying this hook.", 4370336704)
		return
	end

	if not (hookmetamethod and newcclosure and getnamecallmethod) then
		dylanfocClient.notify("Hook Unavailable", "Your executor does not expose the metamethod tools needed to spoof ClientID.", 4370336704)
		return
	end

	local env = dylanfocClient.getEnv()
	env.spoofedshit = spoofValue

	if not env.SiriusDylanfocClientIdHooked then
		local oldNamecall
		oldNamecall = hookmetamethod(game, "__namecall", newcclosure(LPH_NO_VIRTUALIZE(function(self, ...)
			if getnamecallmethod() == "GetClientId" and env.spoofedshit then
				return env.spoofedshit
			end

			return oldNamecall(self, ...)
		end)))

		env.SiriusDylanfocClientIdHooked = true
	end

	dylanfocClient.notify("ClientID Spoofed", "GetClientId now returns your selected ClientID value.", 4400699701)
	dylanfocClient.refreshInfoRows()
end

function dylanfocClient.spoofHwid()
	local spoofValue = dylanfocClient.getSettingValue("Spoof HWID Value")

	if spoofValue == "" then
		dylanfocClient.notify("Missing HWID", "Enter a spoofed HWID value before applying this hook.", 4370336704)
		return
	end

	if not (hookfunction and gethwid) then
		dylanfocClient.notify("Hook Unavailable", "Your executor does not expose hookfunction or gethwid.", 4370336704)
		return
	end

	local env = dylanfocClient.getEnv()
	env.spoofhwid = spoofValue

	if not env.SiriusDylanfocHwidHooked then
		hookfunction(gethwid, LPH_NO_VIRTUALIZE(function(...)
			return env.spoofhwid
		end))

		env.SiriusDylanfocHwidHooked = true
	end

	dylanfocClient.notify("HWID Spoofed", "gethwid now returns your selected HWID value.", 4400699701)
	dylanfocClient.refreshInfoRows()
end

function dylanfocClient.spoofExecutor()
	local spoofValue = dylanfocClient.getSettingValue("Spoof Executor Value")

	if spoofValue == "" then
		dylanfocClient.notify("Missing Executor", "Enter or select an executor name before applying this hook.", 4370336704)
		return
	end

	if not (hookfunction and identifyexecutor) then
		dylanfocClient.notify("Hook Unavailable", "Your executor does not expose hookfunction or identifyexecutor.", 4370336704)
		return
	end

	local env = dylanfocClient.getEnv()
	env.spoofedexec = spoofValue

	if not env.SiriusDylanfocExecutorHooked then
		hookfunction(identifyexecutor, LPH_NO_VIRTUALIZE(function(...)
			return env.spoofedexec
		end))

		env.SiriusDylanfocExecutorHooked = true
	end

	dylanfocClient.notify("Executor Spoofed", "identifyexecutor now returns "..dylanfocClient.shortValue(spoofValue, 48)..".", 4400699701)
	dylanfocClient.refreshInfoRows()
end

function dylanfocClient.spoofUserId()
	local spoofValue = tonumber(dylanfocClient.getSettingValue("Spoof UserID Value"))

	if not spoofValue then
		dylanfocClient.notify("Invalid UserID", "Enter a numeric UserId before applying this spoof.", 4370336704)
		return
	end

	local success = pcall(function()
		localPlayer.UserId = spoofValue
	end)

	if success then
		dylanfocClient.getEnv().spoofuserID = spoofValue
		dylanfocClient.notify("UserID Spoofed", "Sirius wrote the LocalPlayer UserId for this session.", 4400699701)
		dylanfocClient.refreshInfoRows()
	else
		dylanfocClient.notify("UserID Locked", "Roblox blocked writing LocalPlayer.UserId in this environment.", 4370336704)
	end
end

function dylanfocClient.spoofUsername()
	local spoofValue = dylanfocClient.getSettingValue("Spoof Username Value")

	if spoofValue == "" then
		dylanfocClient.notify("Missing Username", "Enter a spoofed username before applying this spoof.", 4370336704)
		return
	end

	local success = pcall(function()
		localPlayer.Name = spoofValue
	end)

	if success then
		dylanfocClient.getEnv().spoofuser = spoofValue
		dylanfocClient.notify("Username Spoofed", "Sirius wrote the LocalPlayer name for this session.", 4400699701)
		dylanfocClient.refreshInfoRows()
	else
		dylanfocClient.notify("Username Locked", "Roblox blocked writing LocalPlayer.Name in this environment.", 4370336704)
	end
end

function dylanfocClient.disableRestoreFunction()
	if not (hookfunction and restorefunction) then
		dylanfocClient.notify("Restore Hook Unavailable", "Your executor does not expose restorefunction or hookfunction.", 4370336704)
		return
	end

	local env = dylanfocClient.getEnv()
	if env.SiriusDylanfocRestoreDisabled then
		dylanfocClient.notify("Already Disabled", "restorefunction is already blocked in this session.", 4400696294)
		return
	end

	hookfunction(restorefunction, LPH_NO_VIRTUALIZE(function(...)
		return "no."
	end))

	env.SiriusDylanfocRestoreDisabled = true
	dylanfocClient.notify("restorefunction Disabled", "Hook rollback has been blocked for this session. Rejoin to fully reset.", 4370336704)
end

function dylanfocClient.resetUtilityState()
	local env = dylanfocClient.getEnv()
	env.SpoofLoaded = false
	env.spoofedshit = nil
	env.spoofhwid = nil
	env.spoofedexec = nil
	env.spoofuserID = nil
	env.spoofuser = nil

	dylanfocClient.notify("Utility State Reset", "Stored spoof values were cleared. Existing hooks may need a rejoin to fully restore.", 4400696294)
	dylanfocClient.refreshInfoRows()
end

function dylanfocClient.runOriginalScript()
	local success, result = pcall(function()
		return loadstring(game:HttpGet("https://raw.githubusercontent.com/Dylanfoc2/spoofer/refs/heads/main/spoof"))()
	end)

	if success then
		dylanfocClient.notify("Pine Protect Started", "Sirius launched the original Pine Protect script.", 4400699701)
	else
		dylanfocClient.notify("Pine Protect Failed", "Sirius could not run the original Pine Protect script.", 4370336704)
		warn("[Sirius] Pine Protect original script failed:", result)
	end
end

function dylanfocClient.runAction(setting)
	local action = setting.action

	if action == "copy_hwid" then
		dylanfocClient.copyValue("HWID", "hwid")
	elseif action == "copy_clientid" then
		dylanfocClient.copyValue("ClientID", "clientId")
	elseif action == "copy_executor" then
		dylanfocClient.copyValue("Executor", "executor")
	elseif action == "copy_userid" then
		dylanfocClient.copyValue("UserID", "userId")
	elseif action == "copy_username" then
		dylanfocClient.copyValue("Username", "username")
	elseif action == "refresh_client_info" then
		dylanfocClient.refreshInfoRows()
		dylanfocClient.notify("Client Info Refreshed", "Sirius updated the client values shown in this page.", 4400696294)
	elseif action == "run_original_dylanfoc" then
		dylanfocClient.runOriginalScript()
	elseif action == "spoof_clientid" then
		dylanfocClient.spoofClientId()
	elseif action == "spoof_hwid" then
		dylanfocClient.spoofHwid()
	elseif action == "spoof_executor" then
		dylanfocClient.spoofExecutor()
	elseif action == "spoof_userid" then
		dylanfocClient.spoofUserId()
	elseif action == "spoof_username" then
		dylanfocClient.spoofUsername()
	elseif action == "disable_restorefunction" then
		dylanfocClient.disableRestoreFunction()
	elseif action == "reset_utility_state" then
		dylanfocClient.resetUtilityState()
	end
end

function dylanfocClient.updateInputRow(setting)
	local row = dylanfocClient.inputRows[setting.id]
	if not row or not row:FindFirstChild("InputFrame") then
		return
	end

	local value = tostring(setting.current or "")
	row.InputFrame.InputBox.Text = string.len(value) > 24 and string.sub(value, 1, 22)..".." or value
	row.InputFrame.Size = UDim2.new(0, row.InputFrame.InputBox.TextBounds.X + 24, 0, 30)
end

function dylanfocClient.cycleDropdown(setting, row)
	if not setting.values or #setting.values == 0 then
		return
	end

	local currentIndex = 1
	for index, value in ipairs(setting.values) do
		if value == setting.current then
			currentIndex = index
			break
		end
	end

	currentIndex += 1
	if currentIndex > #setting.values then
		currentIndex = 1
	end

	setting.current = setting.values[currentIndex]

	if setting.targetSetting then
		local targetSetting = checkSetting(setting.targetSetting, dylanfocCategoryName)
		if targetSetting then
			targetSetting.current = setting.current
			dylanfocClient.updateInputRow(targetSetting)
		end
	end

	saveSettings()
	dylanfocClient.setRowDescription(row, "Current: "..dylanfocClient.shortValue(setting.current, 96))
end

do local b = _G.__SIRIUSX_BRIDGE; if b then b.settingsList = siriusSettings; b.checkSetting = checkSetting; b.saveSettings = saveSettings; b.dylanfocClient = dylanfocClient end end -- [NextGen bridge: settings]
local function assembleSettings()
	if siriusValues.captureKeybindDefaults then
		siriusValues.captureKeybindDefaults()
	end

	if isfile and isfile(siriusValues.siriusFolder.."/"..siriusValues.settingsFile) then
		local currentSettings

		local success, response = pcall(function()
			currentSettings = httpService:JSONDecode(readfile(siriusValues.siriusFolder.."/"..siriusValues.settingsFile))
		end)

		if success then
			for _, liveCategory in ipairs(siriusSettings) do
				for _, liveSetting in ipairs(liveCategory.categorySettings) do
					for _, category in ipairs(currentSettings) do
						for _, setting in ipairs(category.categorySettings) do
							if liveSetting.id == setting.id then
								liveSetting.current = setting.current
							end
						end
					end
				end
			end

			writefile(siriusValues.siriusFolder.."/"..siriusValues.settingsFile, httpService:JSONEncode(siriusSettings)) -- Update file with any new settings added
		end
	else
		if writefile then
			checkFolder()
			if not isfile(siriusValues.siriusFolder.."/"..siriusValues.settingsFile) then
				writefile(siriusValues.siriusFolder.."/"..siriusValues.settingsFile, httpService:JSONEncode(siriusSettings))
			end
		end 
	end

	bindSettingsCategoryScroll()
	refreshSettingsCategoryScroll()

	settingsPanel.Back.MouseButton1Click:Connect(function()
		setDylanfocUtilityBackgroundVisible(false)
		tweenService:Create(settingsPanel.Back, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {ImageTransparency = 1}):Play()
		tweenService:Create(settingsPanel.Back, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Position = UDim2.new(0.002, 0, 0.052, 0)}):Play()
		tweenService:Create(settingsPanel.Title, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Position = UDim2.new(0.045, 0, 0.057, 0)}):Play()
		tweenService:Create(settingsPanel.UIGradient, TweenInfo.new(1, Enum.EasingStyle.Exponential), {Offset = Vector2.new(0, 1.3)}):Play()
		settingsPanel.Title.Text = "Settings"
		settingsPanel.Subtitle.Text = "Adjust your preferences, set new keybinds, test out new features and more"

		for _, settingList in ipairs(settingsPanel.SettingLists:GetChildren()) do
			if settingList:IsA("ScrollingFrame") then
				settingList.ScrollBarImageTransparency = 1
			end
		end

		settingsPanel.SettingTypes.Visible = true
		settingsPanel.SettingLists.Visible = false

		if settingsPanel.SettingTypes:IsA("ScrollingFrame") then
			settingsPanel.SettingTypes.CanvasPosition = Vector2.new(0, 0)
			settingsPanel.SettingTypes.ScrollBarImageTransparency = 1
		end
		refreshSettingsCategoryScroll()
		task.delay(0.12, refreshSettingsCategoryScroll)
		task.delay(0.32, refreshSettingsCategoryScroll)
	end)

	for _, category in siriusSettings do
		local newCategory = settingsPanel.SettingTypes.Template:Clone()
		newCategory.Name = category.name
		newCategory.Title.Text = string.upper(category.name)
		newCategory.Parent = settingsPanel.SettingTypes
		newCategory.UIGradient.Color = ColorSequence.new({ColorSequenceKeypoint.new(0, Color3.new(0.0392157, 0.0392157, 0.0392157)),ColorSequenceKeypoint.new(1, category.color)})

		newCategory.Visible = true

		local hue, sat, val = Color3.toHSV(category.color)

		hue = math.clamp(hue + 0.01, 0, 1) sat = math.clamp(sat + 0.1, 0, 1) val = math.clamp(val + 0.2, 0, 1)

		local newColor = Color3.fromHSV(hue, sat, val)
		newCategory.UIStroke.UIGradient.Color = ColorSequence.new({ColorSequenceKeypoint.new(0, Color3.new(0.117647, 0.117647, 0.117647)),ColorSequenceKeypoint.new(1, newColor)})
		newCategory.Shadow.UIGradient.Color = ColorSequence.new({ColorSequenceKeypoint.new(0, Color3.new(0.117647, 0.117647, 0.117647)),ColorSequenceKeypoint.new(1, newColor)})

		local newList = settingsPanel.SettingLists.Template:Clone()
		newList.Name = category.name
		newList.Parent = settingsPanel.SettingLists
		attachScrollbarFade(newList, 0.55, 0.65)

		newList.Visible = true

		for _, obj in ipairs(newList:GetChildren()) do if obj.Name ~= "Placeholder" and obj.Name ~= "UIListLayout" then obj:Destroy() end end 

		newCategory.Interact.MouseButton1Click:Connect(function()
			if settingsPanel.SettingLists:FindFirstChild(category.name) then
				local selectedList = settingsPanel.SettingLists[category.name]
				setDylanfocUtilityBackgroundVisible(category.name == dylanfocCategoryName)
				settingsPanel.UIGradient.Color = ColorSequence.new({ColorSequenceKeypoint.new(0, Color3.new(0.0470588, 0.0470588, 0.0470588)),ColorSequenceKeypoint.new(1, category.color)})
				settingsPanel.SettingTypes.Visible = false
				settingsPanel.SettingLists.Visible = true
				settingsPanel.SettingLists.UIPageLayout:JumpTo(selectedList)
				settingsPanel.Subtitle.Text = category.description
				settingsPanel.Back.Visible = true
				settingsPanel.Title.Text = category.name

				if selectedList:IsA("ScrollingFrame") then
					selectedList.CanvasPosition = Vector2.new(0, 0)
					selectedList.ScrollBarImageTransparency = 1

					for _, child in ipairs(selectedList:GetChildren()) do
						if child:IsA("Frame") then
							if selectedList.Name == "Servers" and child.Name == "Placeholder" then
								child.Visible = false
								child.Size = UDim2.new(0, 0, 0, 0)
							else
								child.Visible = true
							end
						end
					end

					if category.name == dylanfocCategoryName then
						dylanfocClient.refreshInfoRows()
					end

					task.defer(function()
						selectedList.CanvasPosition = Vector2.new(0, 0)

						local listLayout = selectedList:FindFirstChildOfClass("UIListLayout")
						if listLayout then
							local contentHeight = math.max(listLayout.AbsoluteContentSize.Y + 26, 340)
							selectedList.CanvasSize = UDim2.new(0, 0, 0, contentHeight)
							selectedList.ScrollBarImageTransparency = contentHeight > selectedList.AbsoluteWindowSize.Y + 8 and 0.55 or 1
						end
					end)
				end

				local gradientRotation = math.random(78, 95)
				settingsPanel.UIGradient.Rotation = gradientRotation
				tweenService:Create(settingsPanel.UIGradient, TweenInfo.new(0.5, Enum.EasingStyle.Exponential), {Offset = Vector2.new(0, 0.65)}):Play()
				tweenService:Create(settingsPanel.Back, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {ImageTransparency = 0}):Play()
				tweenService:Create(settingsPanel.Back, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Position = UDim2.new(0.041, 0, 0.052, 0)}):Play()
				tweenService:Create(settingsPanel.Title, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {Position = UDim2.new(0.091, 0, 0.057, 0)}):Play()
			else
				-- error
				closeSettings()
			end
		end)

		newCategory.MouseEnter:Connect(function()
			tweenService:Create(newCategory.Title, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0}):Play()
			tweenService:Create(newCategory.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 0.4)}):Play()
			tweenService:Create(newCategory.UIStroke.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 0.2)}):Play()
			tweenService:Create(newCategory.Shadow.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 0.2)}):Play()
		end)

		newCategory.MouseLeave:Connect(function()
			tweenService:Create(newCategory.Title, TweenInfo.new(0.5, Enum.EasingStyle.Quint), {TextTransparency = 0.2}):Play()
			tweenService:Create(newCategory.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 0.65)}):Play()
			tweenService:Create(newCategory.UIStroke.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 0.4)}):Play()
			tweenService:Create(newCategory.Shadow.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0, 0.4)}):Play()
		end)

		local settingsToRender = category.categorySettings
		if category.name == keybindCategoryName and siriusValues.getKeybindRenderSettings then
			settingsToRender = siriusValues.getKeybindRenderSettings(category.categorySettings)
		end

		for _, setting in ipairs(settingsToRender) do
			if not setting.hidden then
				local settingType = setting.settingType
				local minimumLicense = setting.minimumLicense
				local object = nil

				if settingType == "Info" then
					local newInfo = settingsPanel.SettingLists.Template.SwitchTemplate:Clone()
					object = newInfo
					newInfo.Name = setting.name
					newInfo.Parent = newList
					newInfo.Visible = true
					newInfo.Title.Text = setting.name

					if newInfo:FindFirstChild("Switch") then
						newInfo.Switch.Visible = false
					end

					dylanfocClient.registerInfoRow(setting, newInfo)

				elseif settingType == "Action" then
					local newAction = settingsPanel.SettingLists.Template.SwitchTemplate:Clone()
					object = newAction
					newAction.Name = setting.name
					newAction.Parent = newList
					newAction.Visible = true
					newAction.Title.Text = setting.name

					if newAction:FindFirstChild("Switch") then
						if setting.action == "run_original_dylanfoc" then
							local runButton = newAction.Switch
							runButton.Visible = true
							runButton.Size = UDim2.new(0, 88, 0, 36)
							runButton.BackgroundColor3 = Color3.fromRGB(34, 34, 34)
							runButton.BackgroundTransparency = 0.06

							if runButton:FindFirstChild("Indicator") then
								runButton.Indicator.Visible = false
							end

							local runStroke = runButton:FindFirstChildOfClass("UIStroke") or Instance.new("UIStroke")
							runStroke.Parent = runButton
							runStroke.Color = category.color
							runStroke.Thickness = 1
							runStroke.Transparency = 0.58

							local runCorner = runButton:FindFirstChildOfClass("UICorner") or Instance.new("UICorner")
							runCorner.Parent = runButton
							runCorner.CornerRadius = UDim.new(1, 0)

							local runLabel = runButton:FindFirstChild("RunLabel") or Instance.new("TextLabel")
							runLabel.Name = "RunLabel"
							runLabel.Parent = runButton
							runLabel.BackgroundTransparency = 1
							runLabel.Size = UDim2.new(1, 0, 1, 0)
							runLabel.Font = Enum.Font.GothamSemibold
							runLabel.Text = "Run"
							runLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
							runLabel.TextSize = 16
							runLabel.TextTransparency = 0
							runLabel.ZIndex = runButton.ZIndex + 1
						else
							newAction.Switch.Visible = false
						end
					end

					newAction.Interact.MouseButton1Click:Connect(function()
						if setting.callback then
							task.spawn(setting.callback)
						else
							dylanfocClient.runAction(setting)
						end
					end)

				elseif settingType == "Dropdown" then
					local newDropdown = settingsPanel.SettingLists.Template.SwitchTemplate:Clone()
					object = newDropdown
					newDropdown.Name = setting.name
					newDropdown.Parent = newList
					newDropdown.Visible = true
					newDropdown.Title.Text = setting.name

					if newDropdown:FindFirstChild("Switch") then
						newDropdown.Switch.Visible = false
					end

					dylanfocClient.setRowDescription(newDropdown, "Current: "..dylanfocClient.shortValue(setting.current, 96).."\nTap this row to cycle presets.")

					newDropdown.Interact.MouseButton1Click:Connect(function()
						dylanfocClient.cycleDropdown(setting, newDropdown)
					end)

				elseif settingType == "Boolean" then
					local newSwitch = settingsPanel.SettingLists.Template.SwitchTemplate:Clone()
					object = newSwitch
					newSwitch.Name = setting.name
					newSwitch.Parent = newList
					newSwitch.Visible = true
					newSwitch.Title.Text = setting.name

					if setting.current == true then
						newSwitch.Switch.Indicator.Position = UDim2.new(1, -20, 0.5, 0)
						newSwitch.Switch.Indicator.UIStroke.Color = Color3.fromRGB(220, 220, 220)
						newSwitch.Switch.Indicator.BackgroundColor3 = Color3.fromRGB(255, 255, 255)			
						newSwitch.Switch.Indicator.BackgroundTransparency = 0.6
					end


					if minimumLicense then
						if (minimumLicense == "Pro" and not Pro) or (minimumLicense == "Essential" and not (Pro or Essential)) then
							newSwitch.Switch.Indicator.Position = UDim2.new(1, -40, 0.5, 0)
							newSwitch.Switch.Indicator.UIStroke.Color = Color3.fromRGB(255, 255, 255)
							newSwitch.Switch.Indicator.BackgroundColor3 = Color3.fromRGB(235, 235, 235)			
							newSwitch.Switch.Indicator.BackgroundTransparency = 0.75
						end
					end

					newSwitch.Interact.MouseButton1Click:Connect(function()
						if minimumLicense then
							if (minimumLicense == "Pro" and not Pro) or (minimumLicense == "Essential" and not (Pro or Essential)) then
								queueNotification("This feature is locked", "You must be "..minimumLicense.." or higher to use "..setting.name..". \n\nUpgrade at https://sirius.menu.", 4483345875)
								return
							end
						end

						setting.current = not setting.current
						saveSettings()
						if setting.current == true then
							tweenService:Create(newSwitch.Switch.Indicator, TweenInfo.new(0.5, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Position = UDim2.new(1, -20, 0.5, 0)}):Play()
							tweenService:Create(newSwitch.Switch.Indicator, TweenInfo.new(0.4, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0,12,0,12)}):Play()
							tweenService:Create(newSwitch.Switch.Indicator.UIStroke, TweenInfo.new(0.55, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Color = Color3.fromRGB(200, 200, 200)}):Play()
							tweenService:Create(newSwitch.Switch.Indicator, TweenInfo.new(0.8, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
							tweenService:Create(newSwitch.Switch.Indicator.UIStroke, TweenInfo.new(0.55, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.5}):Play()
							tweenService:Create(newSwitch.Switch.Indicator, TweenInfo.new(0.55, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.6}):Play()
							task.wait(0.05)
							tweenService:Create(newSwitch.Switch.Indicator, TweenInfo.new(0.45, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0,17,0,17)}):Play()							
						else
							tweenService:Create(newSwitch.Switch.Indicator, TweenInfo.new(0.45, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Position = UDim2.new(1, -40, 0.5, 0)}):Play()
							tweenService:Create(newSwitch.Switch.Indicator, TweenInfo.new(0.4, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0,12,0,12)}):Play()
							tweenService:Create(newSwitch.Switch.Indicator.UIStroke, TweenInfo.new(0.55, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Color = Color3.fromRGB(255, 255, 255)}):Play()
							tweenService:Create(newSwitch.Switch.Indicator.UIStroke, TweenInfo.new(0.55, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.7}):Play()
							tweenService:Create(newSwitch.Switch.Indicator, TweenInfo.new(0.8, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundColor3 = Color3.fromRGB(235, 235, 235)}):Play()
							tweenService:Create(newSwitch.Switch.Indicator, TweenInfo.new(0.55, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.75}):Play()
							task.wait(0.05)
							tweenService:Create(newSwitch.Switch.Indicator, TweenInfo.new(0.4, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0,17,0,17)}):Play()
						end
					end)

				elseif settingType == "Input" then
					local newInput = settingsPanel.SettingLists.Template.InputTemplate:Clone()
					object = newInput
					dylanfocClient.registerInputRow(setting, newInput)

					newInput.Name = setting.name
					newInput.InputFrame.InputBox.Text = setting.current
					newInput.InputFrame.InputBox.PlaceholderText = setting.placeholder or "input"
					newInput.Parent = newList

					if string.len(setting.current) > 19 then
						newInput.InputFrame.InputBox.Text = string.sub(tostring(setting.current), 1,17)..".."
					else
						newInput.InputFrame.InputBox.Text = setting.current
					end

					newInput.Visible = true
					newInput.Title.Text = setting.name
					newInput.InputFrame.InputBox.TextWrapped = false
					newInput.InputFrame.Size = UDim2.new(0, newInput.InputFrame.InputBox.TextBounds.X + 24, 0, 30)

					newInput.InputFrame.InputBox.FocusLost:Connect(function()
						if minimumLicense then
							if (minimumLicense == "Pro" and not Pro) or (minimumLicense == "Essential" and not (Pro or Essential)) then
								queueNotification("This feature is locked", "You must be "..minimumLicense.." or higher to use "..setting.name..". \n\nUpgrade at https://sirius.menu.", 4483345875)
								newInput.InputFrame.InputBox.Text = setting.current
								return
							end
						end

						if newInput.InputFrame.InputBox.Text ~= nil and newInput.InputFrame.InputBox.Text ~= "" then
							setting.current = newInput.InputFrame.InputBox.Text
							saveSettings()
						end
						if string.len(setting.current) > 24 then
							newInput.InputFrame.InputBox.Text = string.sub(tostring(setting.current), 1,22)..".."
						else
							newInput.InputFrame.InputBox.Text = setting.current
						end
					end)

					newInput.InputFrame.InputBox:GetPropertyChangedSignal("Text"):Connect(function()
						tweenService:Create(newInput.InputFrame, TweenInfo.new(0.5, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Size = UDim2.new(0, newInput.InputFrame.InputBox.TextBounds.X + 24, 0, 30)}):Play()
					end)

				elseif settingType == "Number" then
					local newInput = settingsPanel.SettingLists.Template.InputTemplate:Clone()
					object = newInput

					newInput.Name = setting.name
					newInput.InputFrame.InputBox.Text = tostring(setting.current)
					newInput.InputFrame.InputBox.PlaceholderText = setting.placeholder or "number"
					newInput.Parent = newList

					if string.len(setting.current) > 19 then
						newInput.InputFrame.InputBox.Text = string.sub(tostring(setting.current), 1,17)..".."
					else
						newInput.InputFrame.InputBox.Text = setting.current
					end

					newInput.Visible = true
					newInput.Title.Text = setting.name
					newInput.InputFrame.InputBox.TextWrapped = false
					newInput.InputFrame.Size = UDim2.new(0, newInput.InputFrame.InputBox.TextBounds.X + 24, 0, 30)

					newInput.InputFrame.InputBox.FocusLost:Connect(function()

						if minimumLicense then
							if (minimumLicense == "Pro" and not Pro) or (minimumLicense == "Essential" and not (Pro or Essential)) then
								queueNotification("This feature is locked", "You must be "..minimumLicense.." or higher to use "..setting.name..". \n\nUpgrade at https://sirius.menu.", 4483345875)
								newInput.InputFrame.InputBox.Text = setting.current
								return
							end
						end

						local inputValue = tonumber(newInput.InputFrame.InputBox.Text)

						if inputValue then
							if setting.values then
								local minValue = setting.values[1]
								local maxValue = setting.values[2]

								if inputValue < minValue then
									setting.current = minValue
								elseif inputValue > maxValue then
									setting.current = maxValue
								else
									setting.current = inputValue
								end

								saveSettings()
							else
								setting.current = inputValue
								saveSettings()
							end
						else
							newInput.InputFrame.InputBox.Text = tostring(setting.current)
						end

						if string.len(setting.current) > 24 then
							newInput.InputFrame.InputBox.Text = string.sub(tostring(setting.current), 1,22)..".."
						else
							newInput.InputFrame.InputBox.Text = tostring(setting.current)
						end
					end)

					newInput.InputFrame.InputBox:GetPropertyChangedSignal("Text"):Connect(function()
						tweenService:Create(newInput.InputFrame, TweenInfo.new(0.5, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Size = UDim2.new(0, newInput.InputFrame.InputBox.TextBounds.X + 24, 0, 30)}):Play()
					end)

				elseif settingType == "Key" then
					local newKeybind = settingsPanel.SettingLists.Template.InputTemplate:Clone()
					object = newKeybind
					newKeybind.Name = setting.name
					newKeybind.InputFrame.InputBox.PlaceholderText = setting.placeholder or "Press any key..."
					newKeybind.InputFrame.InputBox.Text = siriusValues.formatKeybind(setting.current)
					newKeybind.Parent = newList
					siriusValues.keybindRows[siriusValues.keybindSettingKey(setting)] = newKeybind

					newKeybind.Visible = true
					newKeybind.Title.Text = setting.name
					newKeybind.InputFrame.InputBox.TextWrapped = false
					newKeybind.InputFrame.Size = UDim2.new(0, newKeybind.InputFrame.InputBox.TextBounds.X + 24, 0, 30)

					newKeybind.InputFrame.InputBox.FocusLost:Connect(function()
						checkingForKey = false

						if minimumLicense then
							if (minimumLicense == "Pro" and not Pro) or (minimumLicense == "Essential" and not (Pro or Essential)) then
								queueNotification("This feature is locked", "You must be "..minimumLicense.." or higher to use "..setting.name..". \n\nUpgrade at https://sirius.menu.", 4483345875)
								newKeybind.InputFrame.InputBox.Text = siriusValues.formatKeybind(setting.current)
								return
							end
						end

						local keybindText = newKeybind.InputFrame.InputBox.Text
						if keybindText == nil or keybindText == "" then
							newKeybind.InputFrame.InputBox.Text = "No Keybind"
							setting.current = nil
							newKeybind.InputFrame.InputBox:ReleaseFocus()
							saveSettings()
						elseif keybindText == "Press any key..." or string.find(keybindText, "%.%.%.") then
							newKeybind.InputFrame.InputBox.Text = siriusValues.formatKeybind(setting.current)
						end
					end)

					newKeybind.InputFrame.InputBox.Focused:Connect(function()
						checkingForKey = {data = setting, object = newKeybind, modifiers = {}}
						newKeybind.InputFrame.InputBox.Text = "Press any key..."
					end)

					newKeybind.InputFrame.InputBox:GetPropertyChangedSignal("Text"):Connect(function()
						tweenService:Create(newKeybind.InputFrame, TweenInfo.new(0.5, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Size = UDim2.new(0, newKeybind.InputFrame.InputBox.TextBounds.X + 24, 0, 30)}):Play()
					end)

				elseif settingType == "Servers" then
					createServersBrowser(newList)

				end

				if object then
					if setting.description and settingType ~= "Dropdown" then
						object.Description.Visible = true
						object.Description.TextWrapped = true
						object.Description.Size = UDim2.new(0, 333, 5, 0)
						object.Description.Size = UDim2.new(0, 333, 0, 999)
						object.Description.Text = setting.description
						object.Description.Size = UDim2.new(0, 333, 0, object.Description.TextBounds.Y + 10)
						object.Size = UDim2.new(0, 558, 0, object.Description.TextBounds.Y + 44)
					end

					if minimumLicense then
						object.LicenseDisplay.Visible = true
						object.Title.Position = UDim2.new(0, 18, 0, 26)
						object.Description.Position = UDim2.new(0, 18, 0, 43)
						object.Size = UDim2.new(0, 558, 0, object.Size.Y.Offset + 13)
						object.LicenseDisplay.Text = string.upper(minimumLicense).." FEATURE"
					end

					local objectTouching
					object.MouseEnter:Connect(function()
						objectTouching = true
						tweenService:Create(object.UIStroke, TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.45}):Play()
						tweenService:Create(object, TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.83}):Play()
					end)

					object.MouseLeave:Connect(function()
						objectTouching = false
						tweenService:Create(object.UIStroke, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.6}):Play()
						tweenService:Create(object, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.9}):Play()
					end)

					if object:FindFirstChild('Interact') then
						object.Interact.MouseButton1Click:Connect(function()
							tweenService:Create(object.UIStroke, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 1}):Play()
							tweenService:Create(object, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.8}):Play()
							task.wait(0.1)
							if objectTouching then
								tweenService:Create(object.UIStroke, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.45}):Play()
								tweenService:Create(object, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.83}):Play()
							else
								tweenService:Create(object.UIStroke, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.6}):Play()
								tweenService:Create(object, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.9}):Play()
							end
						end)
					end
				end
			end
		end
	end

	refreshSettingsCategoryScroll()
end

local function initialiseAntiKick()
	if checkSetting("Client-Based Anti Kick").current then
		if hookmetamethod and getnamecallmethod then 
			local originalIndex
			local originalNamecall

			originalIndex = hookmetamethod(game, "__index", LPH_NO_VIRTUALIZE(function(self, method)
				if self == localPlayer and method:lower() == "kick" and checkSetting("Client-Based Anti Kick").current and checkSirius() then
					queueNotification("Kick Prevented", "Sirius has prevented you from being kicked by the client.", 4400699701)
					return error("Expected ':' not '.' calling member function Kick", 2)
				end
				return originalIndex(self, method)
			end))

			originalNamecall = hookmetamethod(game, "__namecall", LPH_NO_VIRTUALIZE(function(self, ...)
				if self == localPlayer and getnamecallmethod():lower() == "kick" and checkSetting("Client-Based Anti Kick").current and checkSirius() then
					queueNotification("Kick Prevented", "Sirius has prevented you from being kicked by the client.", 4400699701)
					return
				end
				return originalNamecall(self, ...)
			end))
		end
	end
end

local function boost()
	local success, result = pcall(function()
		loadstring(game:HttpGet('https://raw.githubusercontent.com/SiriusSoftwareLtd/Sirius/refs/heads/request/boost.lua'))()
	end)

	if not success then
		print('Error with boost file.')
		print(result)
	end
end

siriusValues.runKeybindAction = function(actionName)
	actionName = tostring(actionName or "")

	if siriusValues.betaControl and siriusValues.betaControl.disabled and actionName ~= "close" then
		siriusValues.showBetaControlOverlay("SIRIUS X Disabled", siriusValues.betaControl.message ~= "" and siriusValues.betaControl.message or "This beta build is temporarily disabled.", true)
		return
	end

	if actionName == "stopabilities" then
		if siriusValues.stopActiveAbilitiesForModeratorDetection then
			siriusValues.stopActiveAbilitiesForModeratorDetection("the stop keybind was used")
		end
		return
	end

	if actionName == "panic" then
		if siriusValues.stopActiveAbilitiesForModeratorDetection then
			siriusValues.stopActiveAbilitiesForModeratorDetection("the panic keybind was used")
		end

		if not debounce then
			siriusValues.runKeybindAction("close")
		end

		queueNotification("Panic Keybind", "Stopped active abilities and closed available Sirius panels.", 4400696294)
		return
	end

	if actionName == "commandsearch" then
		if debounce then return end

		local function focusCommandSearch()
			if not CmdInput then return end

			if CmdInput:IsFocused() then
				CmdInput:ReleaseFocus()
			else
				CmdInput:CaptureFocus()
				task.wait()
				CmdInput.Text = ""
			end
		end

		if not smartBarOpen then
			openSmartBar()
			task.delay(0.55, focusCommandSearch)
		else
			focusCommandSearch()
		end
		return
	end

	if debounce then return end

	if actionName == "home" then
		if homeContainer.Visible then closeHome() else openHome() end
		return
	end

	if actionName == "music" then
		if musicPanel.Visible then musicLibraryState.close() else musicLibraryState.open() end
		return
	end

	if actionName == "settings" then
		if settingsPanel.Visible then closeSettings() else openSettings() end
		return
	end

	if actionName == "character" or actionName == "scripts" or actionName == "players" then
		local panelName = actionName == "character" and "Character" or actionName == "scripts" and "Scripts" or "Playerlist"
		local panel = UI:FindFirstChild(panelName)
		if panel and panel.Visible then
			task.spawn(closePanel, panelName)
		else
			task.spawn(openPanel, panelName)
		end
		return
	end

	if actionName == "close" then
		local closed = false

		if playerActionSystem.isPickerOpen and playerActionSystem.isPickerOpen() and playerActionSystem.closePicker then
			playerActionSystem.closePicker(true)
			closed = true
		end

		if scriptSearch.Visible and not scriptSaveState.runPromptActive then
			closeScriptSearch()
			closed = true
		end

		if settingsPanel.Visible then
			closeSettings()
			closed = true
		end
		if musicPanel.Visible then
			musicLibraryState.close()
			closed = true
		end
		if homeContainer.Visible then
			closeHome()
			closed = true
		end

		for _, panelName in ipairs({"Character", "Scripts", "Playerlist"}) do
			local panel = UI:FindFirstChild(panelName)
			if panel and panel.Visible then
				task.spawn(closePanel, panelName)
				closed = true
			end
		end

		if not closed and smartBarOpen then
			closeSmartBar()
		end
	end
end

function siriusValues.betaControlFeatureDisabled(featureName)
	featureName = string.lower(tostring(featureName or ""))
	for _, disabledFeature in ipairs(siriusValues.betaControl.disabledFeatures or {}) do
		if string.lower(tostring(disabledFeature)) == featureName then
			return true
		end
	end

	return false
end

function siriusValues.betaControlGetClientId()
	if siriusValues.betaControl.clientId then
		return siriusValues.betaControl.clientId
	end

	local generatedId = httpService:GenerateGUID(false)
	pcall(function()
		checkFolder()
		local path = siriusValues.siriusFolder.."/"..siriusValues.betaControl.clientIdFile
		if typeof(isfile) == "function" and typeof(readfile) == "function" and isfile(path) then
			local storedId = tostring(readfile(path) or "")
			if storedId ~= "" then
				generatedId = storedId
			end
		elseif typeof(writefile) == "function" then
			writefile(path, generatedId)
		end
	end)

	siriusValues.betaControl.clientId = generatedId
	return generatedId
end

function siriusValues.executeOwnerAssistTeleport(x, y, z)
	local character = localPlayer.Character
	local rootPart = characterRuntime.getRootPart and characterRuntime.getRootPart(character) or character and character:FindFirstChild("HumanoidRootPart")
	if rootPart then
		rootPart.CFrame = CFrame.new(x, y, z)
		siriusValues.showBetaControlOverlay("Teleport Complete", "Your character was moved by owner assist.", false)
	else
		siriusValues.showBetaControlOverlay("Teleport Failed", "SIRIUS X could not find your character right now.", false)
	end
end

function siriusValues.applyBetaRemoteControl(control)
	if type(control) ~= "table" then return end

	local beta = siriusValues.betaControl
	beta.failClosed = control.failClosed == true
	beta.disabledFeatures = type(control.disabledFeatures) == "table" and control.disabledFeatures or {}
	local adminControl = type(control.admin) == "table" and control.admin or {}
	local nextAdminEnabled = adminControl.enabled == true
	local adminChanged = beta.adminEnabled ~= nextAdminEnabled
	beta.adminEnabled = nextAdminEnabled
	beta.adminRole = tostring(adminControl.role or "")
	beta.adminPermissions = type(adminControl.permissions) == "table" and adminControl.permissions or {}
	beta.adminSessions = type(adminControl.sessions) == "table" and adminControl.sessions or {}
	if siriusValues.syncDelegateAdminUi and (adminChanged or beta.adminEnabled) then
		task.defer(siriusValues.syncDelegateAdminUi)
	end

	local disabled = control.enabled == false or control.clientDisabled == true
	local title = tostring(control.title or "")
	if title == "" then
		title = "SIRIUS X Disabled"
	end
	local message = tostring(control.clientReason or control.message or "")
	if message == "" then
		message = "SIRIUS X beta is temporarily disabled."
	end

	if disabled then
		if beta.disabled and beta.message == message then return end
		beta.disabled = true
		beta.message = message

		if siriusValues.stopActiveAbilitiesForModeratorDetection then
			pcall(siriusValues.stopActiveAbilitiesForModeratorDetection, "remote beta control")
		end

		pcall(siriusValues.runKeybindAction, "close")
		pcall(function()
			UI.Enabled = true
			toggle.Visible = false
		end)
		siriusValues.showBetaControlOverlay(title, message, true)
		return
	end

	if beta.disabled then
		beta.disabled = false
		beta.message = ""
		pcall(function()
			UI.Enabled = true
			toggle.Visible = not checkSetting("Hide Toggle Button").current
		end)
		siriusValues.hideBetaControlOverlay()
	end

	local command = control.command
	if type(command) == "table" then
		local commandId = tostring(command.id or "")
		if commandId ~= "" and beta.lastCommandId ~= commandId then
			beta.lastCommandId = commandId
			local commandType = tostring(command.type or "")
			local commandTitle = tostring(command.title or "SIRIUS X Owner")
			local commandMessage = tostring(command.message or "Message from the SIRIUS X owner.")

				if commandType == "stopAbilities" then
					if siriusValues.stopActiveAbilitiesForModeratorDetection then
						pcall(siriusValues.stopActiveAbilitiesForModeratorDetection, "owner control")
					end
					siriusValues.showBetaControlOverlay(commandTitle, commandMessage, false)
				elseif commandType == "disableAbility" then
					local ability = tostring(command.ability or "")
					local success, resolvedName = false, "Unknown ability"
					if siriusValues.disableSiriusAbilityByName then
						success, resolvedName = siriusValues.disableSiriusAbilityByName(ability, "owner control")
					end
					if success then
						siriusValues.showBetaControlOverlay(commandTitle, commandMessage ~= "" and commandMessage or (tostring(resolvedName).." was disabled by SIRIUS X admin control."), false)
					else
						siriusValues.showBetaControlOverlay("Ability Not Found", "SIRIUS X could not find an ability named "..ability..".", false)
					end
				elseif commandType == "sealJumpscare" then
					if siriusValues.showSealJumpscare then
						siriusValues.showSealJumpscare(tostring(command.imageUrl or "https://siriusx.vercel.app/jumpscare/seal.png"), tonumber(command.duration) or 2.4)
					end
				elseif commandType == "image" then
					if siriusValues.showAdminImageBroadcast then
						siriusValues.showAdminImageBroadcast(tostring(command.imageUrl or ""), tonumber(command.duration) or 5, commandTitle, commandMessage)
					end
				elseif commandType == "teleportRequest" then
					local x = tonumber(command.x)
				local y = tonumber(command.y)
				local z = tonumber(command.z)
				if x and y and z then
					if beta.ownerAssistTeleportAllowed then
						siriusValues.executeOwnerAssistTeleport(x, y, z)
					else
						siriusValues.showBetaControlChoiceOverlay(commandTitle, commandMessage.."\n\nDestination: "..math.floor(x)..", "..math.floor(y)..", "..math.floor(z), "Accept", "Decline", function()
							siriusValues.executeOwnerAssistTeleport(x, y, z)
						end, function()
							siriusValues.showBetaControlOverlay("Teleport Declined", "The teleport request was declined.", false)
						end, "Always allow this session", function()
							beta.ownerAssistTeleportAllowed = true
							siriusValues.executeOwnerAssistTeleport(x, y, z)
						end)
					end
				end
			elseif commandType == "teleport" then
				local x = tonumber(command.x)
				local y = tonumber(command.y)
				local z = tonumber(command.z)
				if x and y and z then
					siriusValues.executeOwnerAssistTeleport(x, y, z)
					local detail = commandMessage ~= "" and commandMessage or "A SIRIUS X admin teleported your character."
					siriusValues.showBetaControlOverlay(commandTitle ~= "" and commandTitle or "Admin Teleport", detail, false)
				end
			elseif commandType == "serverTeleport" then
				local targetPlaceId = tonumber(command.targetPlaceId)
				local targetJobId = tostring(command.targetJobId or "")
				if targetPlaceId and targetJobId ~= "" then
					if targetPlaceId == game.PlaceId and targetJobId == game.JobId then
						siriusValues.showBetaControlOverlay(commandTitle ~= "" and commandTitle or "Force Join", "You are already in the admin's server.", false)
					else
						siriusValues.showBetaControlOverlay(commandTitle ~= "" and commandTitle or "Force Join", commandMessage ~= "" and commandMessage or "A SIRIUS X admin is pulling you into their server.", false)
						pcall(function()
							teleportService:TeleportToPlaceInstance(targetPlaceId, targetJobId, localPlayer)
						end)
					end
				end
			elseif commandType == "message" then
				siriusValues.showBetaControlOverlay(commandTitle, commandMessage, false)
			end
		end
	end
end

function siriusValues.betaControlHeartbeat()
	if not httpRequest then
		if siriusValues.betaControl.failClosed then
			siriusValues.applyBetaRemoteControl({
				enabled = false,
				message = "SIRIUS X beta control is unreachable in this executor.",
			})
		end
		return
	end

	local payload = {
		clientId = siriusValues.betaControlGetClientId(),
		version = siriusValues.siriusVersion,
		executor = siriusValues.getExecutorDisplayName and siriusValues.getExecutorDisplayName() or "Unknown Executor",
		username = localPlayer and localPlayer.Name or "",
		displayName = localPlayer and localPlayer.DisplayName or "",
		userId = localPlayer and tostring(localPlayer.UserId) or "",
		placeId = tostring(game.PlaceId),
		jobId = tostring(game.JobId),
		status = siriusValues.betaControl.disabled and "disabled" or "online",
	}

	pcall(function()
		local character = localPlayer and localPlayer.Character
		local root = character and character:FindFirstChild("HumanoidRootPart")
		if root then
			local pivot = root.Position
			payload.x = pivot.X
			payload.y = pivot.Y
			payload.z = pivot.Z
		end
		local humanoid = character and character:FindFirstChildOfClass("Humanoid")
		if humanoid then
			payload.health = humanoid.Health
			payload.maxHealth = humanoid.MaxHealth
		end
		if localPlayer and localPlayer.Team then
			payload.team = tostring(localPlayer.Team.Name)
		end
	end)

	local success, response = pcall(function()
		return httpRequest({
			Url = siriusValues.betaControl.endpoint,
			Method = "POST",
			Headers = {
				["Content-Type"] = "application/json",
			},
			Body = httpService:JSONEncode(payload),
		})
	end)

	if not success or not response then
		if siriusValues.betaControl.failClosed then
			siriusValues.applyBetaRemoteControl({
				enabled = false,
				message = "SIRIUS X beta control is unreachable.",
			})
		end
		return
	end

	local body = response.Body or response.body or ""
	local decodeSuccess, decoded = pcall(function()
		return httpService:JSONDecode(body)
	end)

	if decodeSuccess and decoded and decoded.ok then
		if decoded.clientId and decoded.clientId ~= "" then
			siriusValues.betaControl.clientId = tostring(decoded.clientId)
		end
		siriusValues.applyBetaRemoteControl(decoded.control)
	end
end

function siriusValues.startBetaRemoteControl()
	if siriusValues.betaControl.started then return end
	siriusValues.betaControl.started = true

	task.spawn(function()
		while checkSirius() do
			siriusValues.betaControlHeartbeat()
			task.wait(siriusValues.betaControl.heartbeatInterval or 8)
		end
	end)
end

local function start()
	if siriusValues.releaseType == "Experimental" then -- Make this more secure.
		if not Pro then localPlayer:Kick("This is an experimental release, you must be Pro to run this. \n\nUpgrade at https://sirius.menu/") return end
	end
	windowFocusChanged(true)

	siriusValues.betaControlHeartbeat()
	siriusValues.startBetaRemoteControl()
	UI.Enabled = true

	assembleSettings()
	ensureFrameProperties()
	sortActions()
	initialiseAntiKick()
	checkLastVersion()
	task.spawn(boost)

	smartBar.Time.Text = os.date("%H")..":"..os.date("%M")

	toggle.Visible = not checkSetting("Hide Toggle Button").current

	if siriusValues.betaControl and siriusValues.betaControl.disabled then
		toggle.Visible = false
	elseif not checkSetting("Load Hidden").current then 
		--if checkSetting("Startup Sound Effect").current then
		--	local startupPath = siriusValues.siriusFolder.."/Assets/startup.wav"
		--	local startupAsset

		--	if isfile(startupPath) then
		--		startupAsset = getcustomasset(startupPath) or nil
		--	else
		--		startupAsset = fetchFromCDN("startup.wav", true, "Assets/startup.wav")
		--		startupAsset = isfile(startupPath) and getcustomasset(startupPath) or nil
		--	end

		--	if not startupAsset then return end

		--	local startupSound = Instance.new("Sound")
		--	startupSound.Parent = UI
		--	startupSound.SoundId = startupAsset
		--	startupSound.Name = "startupSound"
		--	startupSound.Volume = 0.85
		--	startupSound.PlayOnRemove = true
		--	startupSound:Destroy()	
		--end

		openSmartBar()
	else 
		closeSmartBar() 
	end

	if script_key and not (Essential or Pro) then
		queueNotification("License Error", "We've detected a key being placed above Sirius loadstring, however your key seems to be invalid. Make a support request at sirius.menu/discord to get this solved within minutes.", "document-minus")
	end

	if siriusValues.enableExperienceSync then
		task.spawn(syncExperienceInformation) 
	end

    local fetchSuccess, fetchResult = pcall((game :: any).HttpGet, game, "https://raw.githubusercontent.com/SiriusSoftwareLtd/Rayfield/refs/heads/main/reporter.lua")

    if fetchSuccess and #fetchResult > 0 then
        local execSuccess, Analytics = pcall(function()
            return (loadstring(fetchResult) :: any)()
        end)

        if execSuccess and Analytics then
            local reporter = Analytics.new({
                url          = "https://rayfield-collect.sirius-software-ltd.workers.dev",
                token        = "e5b910510792f6604f36a3dd4a3be739da07e2b5f0f502acbc4282afbfc2706a",
                product_name = "Sirius",
                category     = "Script", 
            })

            reporter:windowCreated({
                script_name    = "Sirius",
                script_version = siriusValues.siriusVersion,
            })
        end
    end
end

-- Sirius Events

start()

toggle.MouseButton1Click:Connect(function()
	if smartBarOpen then
		closeSmartBar()
	else
		openSmartBar()
	end
end)

characterPanel.Interactions.Reset.MouseButton1Click:Connect(function()
	resetSliders()

	characterPanel.Interactions.Reset.Rotation = 360
	queueNotification("Slider Values Reset","Successfully reset all character panel sliders", 4400696294)
	tweenService:Create(characterPanel.Interactions.Reset, TweenInfo.new(.5,Enum.EasingStyle.Back),  {Rotation = 0}):Play()
end)

characterPanel.Interactions.Reset.MouseEnter:Connect(function() if debounce then return end tweenService:Create(characterPanel.Interactions.Reset, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {ImageTransparency = 0}):Play() end)
characterPanel.Interactions.Reset.MouseLeave:Connect(function() if debounce then return end tweenService:Create(characterPanel.Interactions.Reset, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {ImageTransparency = 0.7}):Play() end)

local playerSearch = playerlistPanel.Interactions.SearchFrame.SearchBox -- move this up to Variables once finished

playerSearch:GetPropertyChangedSignal("Text"):Connect(function()
	local query = string.lower(playerSearch.Text)

	for _, player in ipairs(playerlistPanel.Interactions.List:GetChildren()) do
		if player.ClassName == "Frame" and player.Name ~= "Placeholder" and player.Name ~= "Template" then
			local displayName = player:FindFirstChild("DisplayName")
			local displayText = displayName and string.lower(displayName.Text) or ""
			if string.find(string.lower(player.Name), query, 1, true) or string.find(displayText, query, 1, true) then
				player.Visible = true
			else
				player.Visible = false
			end
		end
	end

	if #playerSearch.Text == 0 then
		searchingForPlayer = false
		for _, player in ipairs(playerlistPanel.Interactions.List:GetChildren()) do
			if player.ClassName == "Frame" and player.Name ~= "Placeholder" and player.Name ~= "Template" then
				player.Visible = true
			end
		end
	else
		searchingForPlayer = true
	end
end)

characterPanel.Interactions.Serverhop.MouseEnter:Connect(function()
	if debounce then return end
	tweenService:Create(characterPanel.Interactions.Serverhop, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {BackgroundTransparency = 0.5}):Play()
	tweenService:Create(characterPanel.Interactions.Serverhop.Title, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 0.1}):Play()
	tweenService:Create(characterPanel.Interactions.Serverhop.UIStroke, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {Transparency = 1}):Play()
end)

characterPanel.Interactions.Serverhop.MouseLeave:Connect(function()
	if debounce then return end
	tweenService:Create(characterPanel.Interactions.Serverhop, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {BackgroundTransparency = 0}):Play()
	tweenService:Create(characterPanel.Interactions.Serverhop.Title, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 0.5}):Play()
	tweenService:Create(characterPanel.Interactions.Serverhop.UIStroke, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {Transparency = 0}):Play()
end)

characterPanel.Interactions.Rejoin.MouseEnter:Connect(function()
	if debounce then return end
	tweenService:Create(characterPanel.Interactions.Rejoin, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {BackgroundTransparency = 0.5}):Play()
	tweenService:Create(characterPanel.Interactions.Rejoin.Title, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 0.1}):Play()
	tweenService:Create(characterPanel.Interactions.Rejoin.UIStroke, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {Transparency = 1}):Play()
end)

characterPanel.Interactions.Rejoin.MouseLeave:Connect(function()
	if debounce then return end
	tweenService:Create(characterPanel.Interactions.Rejoin, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {BackgroundTransparency = 0}):Play()
	tweenService:Create(characterPanel.Interactions.Rejoin.Title, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 0.5}):Play()
	tweenService:Create(characterPanel.Interactions.Rejoin.UIStroke, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {Transparency = 0}):Play()
end)

musicPanel.Close.MouseButton1Click:Connect(function()
	if musicPanel.Visible and not debounce then
		musicLibraryState.close()
	end
end)

musicPanel.Add.Interact.MouseButton1Click:Connect(function()
	musicPanel.AddBox.Input:ReleaseFocus()
	musicLibraryState.addToQueue(musicPanel.AddBox.Input.Text)
end)

if musicPanel.AddBox:FindFirstChild("Input") then
	musicPanel.AddBox.Input:GetPropertyChangedSignal("Text"):Connect(function()
		if not musicPanel.Visible then return end

		musicLibraryState.searchSerial += 1
		local thisSerial = musicLibraryState.searchSerial

		task.delay(0.12, function()
			if thisSerial ~= musicLibraryState.searchSerial or not musicPanel.Visible then return end
			musicLibraryState.render()
		end)
	end)
end

musicPanel.Menu.TogglePlaying.MouseButton1Click:Connect(function()
	if musicLibraryState.audio then
		musicLibraryState.audio.Playing = not musicLibraryState.audio.Playing
		musicPanel.Menu.TogglePlaying.ImageRectOffset = musicLibraryState.audio.Playing and Vector2.new(804, 124) or Vector2.new(764, 244)
		if musicLibraryState.updateProgress then musicLibraryState.updateProgress() end
	else
		musicLibraryState.addToQueue()
	end
end)

musicPanel.Menu.Next.MouseButton1Click:Connect(function()
	musicLibraryState.playNext()
end)

characterPanel.Interactions.Rejoin.Interact.MouseButton1Click:Connect(rejoin)
characterPanel.Interactions.Serverhop.Interact.MouseButton1Click:Connect(serverhop)

homeContainer.Interactions.Server.JobId.Interact.MouseButton1Click:Connect(function()
	if setclipboard then 
		originalSetClipboard([[
-- This script will teleport you to ' ]]..game:GetService("MarketplaceService"):GetProductInfo(placeId).Name..[['
-- If it doesn't work after a few seconds, try going into the same game, and then run the script to join ]]..localPlayer.DisplayName.. [['s specific server

game:GetService("TeleportService"):TeleportToPlaceInstance(']]..placeId..[[', ']]..jobId..[[')]]
		)
		queueNotification("Copied Join Script","Successfully set clipboard to join script, players can use this script to join your specific server.", 4335479121)
	else
		queueNotification("Unable to copy join script","Missing setclipboard() function, can't set data to your clipboard.", 4335479658)
	end
end)

homeContainer.Interactions.Discord.Interact.MouseButton1Click:Connect(function()
	if setclipboard then 
		originalSetClipboard("https://sirius.menu/discord")
		queueNotification("Discord Invite Copied", "We've set your clipboard to the Sirius discord invite.", 4335479121)
	else
		queueNotification("Unable to copy Discord invite", "Missing setclipboard() function, can't set data to your clipboard.", 4335479658)
	end
end)

for _, button in ipairs(scriptsPanel.Interactions.Selection:GetChildren()) do
	local origsize = button.Size

	button.MouseEnter:Connect(function()
		if not debounce then
			tweenService:Create(button, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {BackgroundTransparency = 0}):Play()
			tweenService:Create(button, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {Size = UDim2.new(0, button.Size.X.Offset - 5, 0, button.Size.Y.Offset - 3)}):Play()
			tweenService:Create(button.UIStroke, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {Transparency = 1}):Play()
			tweenService:Create(button.Title, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 0.1}):Play()
		end
	end)

	button.MouseLeave:Connect(function()
		if not debounce then
			tweenService:Create(button, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {BackgroundTransparency = 0}):Play()
			tweenService:Create(button, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {Size = origsize}):Play()
			tweenService:Create(button.UIStroke, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {Transparency = 0}):Play()
			tweenService:Create(button.Title, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 0}):Play()
		end
	end)

	button.Interact.MouseButton1Click:Connect(function()
		tweenService:Create(button, TweenInfo.new(.4,Enum.EasingStyle.Quint),  {Size = UDim2.new(0, origsize.X.Offset - 9, 0, origsize.Y.Offset - 6)}):Play()
		task.wait(0.1)
		tweenService:Create(button, TweenInfo.new(.25,Enum.EasingStyle.Quint),  {Size = origsize}):Play()

		if button.Name == "Library" then
			if not scriptSearch.Visible and not debounce then openScriptSearch() end
		end
		-- run action
	end)
end

smartBar.Buttons.Music.Interact.MouseButton1Click:Connect(function()
	if debounce then return end
	if musicPanel.Visible then musicLibraryState.close() else musicLibraryState.open() end
end)

smartBar.Buttons.Home.Interact.MouseButton1Click:Connect(function()
	if debounce then return end
	if homeContainer.Visible then closeHome() else openHome() end
end)

smartBar.Buttons.Settings.Interact.MouseButton1Click:Connect(function()
	if debounce then return end
	if settingsPanel.Visible then closeSettings() else openSettings() end
end)

local smartBarClickAnimationId = {}
local smartBarClickTweens = {}
local smartBarClickPressed = {}

local function cancelSmartBarClickTweens(button)
	if smartBarClickTweens[button] then
		for _, tween in ipairs(smartBarClickTweens[button]) do
			tween:Cancel()
		end
	end

	smartBarClickTweens[button] = {}
end

local function playSmartBarClickTween(button, object, tweenInfo, properties)
	local tween = tweenService:Create(object, tweenInfo, properties)
	table.insert(smartBarClickTweens[button], tween)
	tween:Play()
end

local function playSmartBarDecline(button)
	smartBarClickAnimationId[button] = (smartBarClickAnimationId[button] or 0) + 1
	local declineId = smartBarClickAnimationId[button]
	cancelSmartBarClickTweens(button)

	local originalStrokeColor = button.UIStroke and button.UIStroke.Color
	button.Rotation = 0

	if button.UIStroke then
		tweenService:Create(button.UIStroke, TweenInfo.new(0.12, Enum.EasingStyle.Quint), {Transparency = 0, Color = Color3.fromRGB(255, 116, 80)}):Play()
	end

	tweenService:Create(button, TweenInfo.new(0.08, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {Size = UDim2.new(0, 32, 0, 32), BackgroundTransparency = 0.18, Rotation = 0}):Play()
	tweenService:Create(button.Icon, TweenInfo.new(0.08, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {ImageTransparency = 0.12}):Play()

	task.delay(0.11, function()
		if smartBarClickAnimationId[button] ~= declineId then return end
		tweenService:Create(button, TweenInfo.new(0.2, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0, 36, 0, 36), BackgroundTransparency = 0, Rotation = 0}):Play()
		tweenService:Create(button.Icon, TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {ImageTransparency = 0}):Play()
		if button.UIStroke and originalStrokeColor then
			tweenService:Create(button.UIStroke, TweenInfo.new(0.22, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Color = originalStrokeColor, Transparency = 0}):Play()
		end
	end)
end

for _, button in ipairs(smartBar.Buttons:GetChildren()) do
	if UI:FindFirstChild(button.Name) and button:FindFirstChild("Interact") then
		button.Interact.MouseButton1Click:Connect(function()
			if button.Name == "Playerlist" and playerActionSystem.isPickerOpen and playerActionSystem.isPickerOpen() then
				playSmartBarDecline(button)
				queueNotification("Select Actions Still Open", "Close the Select Actions panel before reopening Players.")
				return
			end

			if isPanel(button.Name) then
				if not debounce and UI:FindFirstChild(button.Name).Visible then
					task.spawn(closePanel, button.Name)
				else
					task.spawn(openPanel, button.Name)
				end
			end

			button.Rotation = 0
			smartBarClickAnimationId[button] = (smartBarClickAnimationId[button] or 0) + 1
			local clickAnimationId = smartBarClickAnimationId[button]

			if not smartBarClickPressed[button] then
				smartBarClickPressed[button] = true
				cancelSmartBarClickTweens(button)
				playSmartBarClickTween(button, button, TweenInfo.new(0.11, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {Size = UDim2.new(0,28,0,28), BackgroundTransparency = 0.6})
				playSmartBarClickTween(button, button.Icon, TweenInfo.new(0.1, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {ImageTransparency = 0.3})
			else
				playSmartBarClickTween(button, button.Icon, TweenInfo.new(0.08, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {ImageTransparency = 0.24})
			end

			task.delay(0.115, function()
				if smartBarClickAnimationId[button] ~= clickAnimationId then return end
				smartBarClickPressed[button] = false
				cancelSmartBarClickTweens(button)
				playSmartBarClickTween(button, button, TweenInfo.new(0.26, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0,36,0,36), BackgroundTransparency = 0, Rotation = 0})
				playSmartBarClickTween(button, button.Icon, TweenInfo.new(0.22, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {ImageColor3 = actionIconColor, ImageTransparency = 0})
			end)
		end)

		button.MouseEnter:Connect(function()
			tweenService:Create(button.UIGradient, TweenInfo.new(1.4, Enum.EasingStyle.Quint), {Rotation = 360}):Play()
			tweenService:Create(button.UIStroke.UIGradient, TweenInfo.new(1.4, Enum.EasingStyle.Quint), {Rotation = 360}):Play()
			tweenService:Create(button.UIStroke, TweenInfo.new(0.8, Enum.EasingStyle.Quint), {Transparency = 1}):Play()
			tweenService:Create(button.Icon, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {ImageColor3 = actionIconColor, ImageTransparency = 0}):Play()
			tweenService:Create(button.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0,-0.5)}):Play()
		end)

		button.MouseLeave:Connect(function()
			tweenService:Create(button.UIStroke.UIGradient, TweenInfo.new(0.6, Enum.EasingStyle.Quint), {Rotation = 50}):Play()
			tweenService:Create(button.UIGradient, TweenInfo.new(0.9, Enum.EasingStyle.Quint), {Rotation = 50}):Play()
			tweenService:Create(button.UIStroke, TweenInfo.new(0.6, Enum.EasingStyle.Quint), {Transparency = 0}):Play()
			tweenService:Create(button.Icon, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {ImageColor3 = actionIconColor, ImageTransparency = 0}):Play()
			tweenService:Create(button.UIGradient, TweenInfo.new(0.7, Enum.EasingStyle.Quint), {Offset = Vector2.new(0,0)}):Play()
		end)
	end
end

userInputService.InputBegan:Connect(function(input, processed)
	if not checkSirius() then return end

	if checkingForKey then
		if input.KeyCode ~= Enum.KeyCode.Unknown then
			local inputBox = checkingForKey.object.InputFrame.InputBox
			local setting = checkingForKey.data
			local keyName = input.KeyCode.Name

			if input.KeyCode == Enum.KeyCode.Escape then
				checkingForKey = false
				inputBox.Text = siriusValues.formatKeybind(setting.current)
				inputBox:ReleaseFocus()
				return
			end

			if input.KeyCode == Enum.KeyCode.Backspace or input.KeyCode == Enum.KeyCode.Delete then
				setting.current = nil
				checkingForKey = false
				inputBox.Text = "No Keybind"
				inputBox:ReleaseFocus()
				saveSettings()
				return
			end

			if siriusValues.isKeybindModifier(keyName) then
				checkingForKey.modifiers = checkingForKey.modifiers or {}

				if not table.find(checkingForKey.modifiers, keyName) then
					table.insert(checkingForKey.modifiers, keyName)
				end

				inputBox.Text = siriusValues.formatKeybind(table.concat(checkingForKey.modifiers, "+").."+...")
				return
			end

			local newKeybind = siriusValues.buildKeybindFromInput(input, checkingForKey.modifiers)
			local conflict = siriusValues.findKeybindConflict(newKeybind, setting)

			if conflict then
				queueNotification("Keybind Conflict", siriusValues.formatKeybind(newKeybind).." is already used by "..conflict.name..".", 4370336704)
				checkingForKey = false
				inputBox.Text = siriusValues.formatKeybind(setting.current)
				inputBox:ReleaseFocus()
				return
			end

			setting.current = siriusValues.normaliseKeybind(newKeybind)
			checkingForKey = false
			inputBox.Text = siriusValues.formatKeybind(setting.current)
			inputBox:ReleaseFocus()
			saveSettings()
		end

		return
	end

	if siriusValues.betaControl and siriusValues.betaControl.disabled then
		if not processed then
			siriusValues.showBetaControlOverlay("SIRIUS X Disabled", siriusValues.betaControl.message ~= "" and siriusValues.betaControl.message or "This beta build is temporarily disabled.", true)
		end
		return
	end

	for _, category in ipairs(siriusSettings) do
		for _, setting in ipairs(category.categorySettings) do
			if setting.settingType == "Key" then
				if setting.current ~= nil and setting.current ~= "" then
					if siriusValues.keybindMatchesInput(setting.current, input) and not processed then
						if setting.callback then
							task.spawn(setting.callback)

								local action = checkAction(setting.name) or nil
								if action and action.action and action.object then
									local object = action.object
									action = action.action

								if action.enabled then
									transitionActionIcon(object.Icon, action.images[1], actionIconActiveTransparency)
									playActionVisualTween(object, "surface", TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.1})
									playActionVisualTween(object.UIStroke, "stroke", TweenInfo.new(0.34, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 1})

									if action.disableAfter then
										task.delay(action.disableAfter, function()
											action.enabled = false
											transitionActionIcon(object.Icon, action.images[2], actionIconIdleTransparency)
											playActionVisualTween(object, "surface", TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.55})
											playActionVisualTween(object.UIStroke, "stroke", TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.4})
										end)
									end

									if action.rotateWhileEnabled then
										repeat
											object.Icon.Rotation = 0
											tweenService:Create(object.Icon, TweenInfo.new(0.75, Enum.EasingStyle.Quint), {Rotation = 360}):Play()
											task.wait(1)
										until not action.enabled
										object.Icon.Rotation = 0
									end
								else
									transitionActionIcon(object.Icon, action.images[2], actionIconIdleTransparency)
									playActionVisualTween(object, "surface", TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.55})
									playActionVisualTween(object.UIStroke, "stroke", TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Transparency = 0.4})
								end
							end
						end
					end
				end
			end
		end
	end

	local scriptSearchKeybind = checkSetting("Open ScriptSearch")
	if scriptSearchKeybind and siriusValues.keybindMatchesInput(scriptSearchKeybind.current, input) and not processed and not debounce and not scriptSaveState.runPromptActive then
		if scriptSearch.Visible then
			closeScriptSearch()
		else
			openScriptSearch()
		end
	end

	local smartBarKeybind = checkSetting("Toggle smartBar")
	if smartBarKeybind and siriusValues.keybindMatchesInput(smartBarKeybind.current, input) and not processed and not debounce then
		if smartBarOpen then 
			closeSmartBar()
		else
			openSmartBar()
		end
	end
end)

userInputService.InputEnded:Connect(function(input, processed)
	if not checkSirius() then return end

	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		for _, slider in pairs(siriusValues.sliders) do
			if slider.activeInput and slider.activeInput.UserInputType == Enum.UserInputType.Touch and input ~= slider.activeInput then
				continue
			end

			slider.active = false
			slider.activeInput = nil
			slider.pointerX = nil

			if characterPanel.Visible and not debounce and slider.object and checkSirius() then
				tweenService:Create(slider.object, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {BackgroundTransparency = 0.8}):Play()
				tweenService:Create(slider.object.UIStroke, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {Transparency = 0.5}):Play()
				tweenService:Create(slider.object.Information, TweenInfo.new(0.4, Enum.EasingStyle.Exponential), {TextTransparency = 0.3}):Play()
			end
		end
	end
end)

camera:GetPropertyChangedSignal('ViewportSize'):Connect(function()
	task.wait(.5)
	updateSliderPadding()
	if smartBarOpen then
		smartBar.Size = siriusValues.getResponsiveSmartBarSize(true)
		if siriusValues.syncSmartBarCommandSearchLayout then
			siriusValues.syncSmartBarCommandSearchLayout()
		end
	end
end)

scriptSearch.SearchBox:GetPropertyChangedSignal("Text"):Connect(function()
	if scriptSearch.Visible and not scriptSaveState.savedMode then
		scriptSaveState.nextSearchToken()
	end

	if #scriptSearch.SearchBox.Text > 0 then
		tweenService:Create(scriptSearch.Icon, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {ImageColor3 = Color3.fromRGB(255, 255, 255)}):Play()
		tweenService:Create(scriptSearch.SearchBox, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
	else
		tweenService:Create(scriptSearch.Icon, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {ImageColor3 = Color3.fromRGB(150, 150, 150)}):Play()
		tweenService:Create(scriptSearch.SearchBox, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextColor3 = Color3.fromRGB(150, 150, 150)}):Play()
	end
end)

scriptSearch.SearchBox.FocusLost:Connect(function(enterPressed)
	if scriptSaveState.savedMode then return end
	if scriptSaveState.runPromptActive then return end

	tweenService:Create(scriptSearch.Icon, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {ImageColor3 = Color3.fromRGB(150, 150, 150)}):Play()
	tweenService:Create(scriptSearch.SearchBox, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextColor3 = Color3.fromRGB(150, 150, 150)}):Play()

	if #scriptSearch.SearchBox.Text > 0 then
		if enterPressed then
			local query = scriptSearch.SearchBox.Text

			task.spawn(function()
				local success = pcall(function()
					searchScripts(query)
				end)

				if not success and scriptSearch.Visible and not scriptSaveState.savedMode then
					queueNotification("ScriptSearch", "Search failed unexpectedly.", 4384402990)
				end
			end)
		end
	else
		local focusToken = scriptSaveState.viewToken

		task.delay(0.12, function()
			if scriptSaveState.viewToken ~= focusToken then return end
			if scriptSaveState.savedMode or not scriptSearch.Visible then return end
			if scriptSaveState.transition ~= "open" then return end
			if #scriptSearch.SearchBox.Text > 0 then return end

			closeScriptSearch()
		end)
	end
end)

scriptSearch.SearchBox.Focused:Connect(function()
	if #scriptSearch.SearchBox.Text > 0 then
		tweenService:Create(scriptSearch.Icon, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {ImageColor3 = Color3.fromRGB(255, 255, 255)}):Play()
		tweenService:Create(scriptSearch.SearchBox, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
	end
end)

userInputService.InputChanged:Connect(function(input)
	if input.UserInputType ~= Enum.UserInputType.MouseMovement and input.UserInputType ~= Enum.UserInputType.Touch then return end

	for _, slider in pairs(siriusValues.sliders) do
		if slider.active then
			if slider.activeInput and slider.activeInput.UserInputType == Enum.UserInputType.Touch and input ~= slider.activeInput then
				continue
			end

			slider.pointerX = siriusValues.getSliderPointerX(input)
			refreshSliderPadding(slider)
			updateSlider(slider, nil, nil, slider.pointerX)
		end
	end
end)

mouse.Move:Connect(function()
	for _, slider in pairs(siriusValues.sliders) do
		if slider.active then
			slider.pointerX = siriusValues.getSliderPointerX()
			refreshSliderPadding(slider)
			updateSlider(slider, nil, nil, slider.pointerX)
		end
	end
end)

userInputService.WindowFocusReleased:Connect(function() windowFocusChanged(false) end)
userInputService.WindowFocused:Connect(function() windowFocusChanged(true) end)

for index, player in ipairs(players:GetPlayers()) do
	createPlayer(player)
	createEsp(player)
	siriusValues.checkKnownUserWatchlist(player)
	siriusValues.checkModeratorAdminJoin(player)
	player.Chatted:Connect(function(message) onChatted(player, message) end)
end

players.PlayerAdded:Connect(function(player)
	if not checkSirius() then return end

	createPlayer(player)
	createEsp(player)
	siriusValues.checkKnownUserWatchlist(player)
	siriusValues.checkModeratorAdminJoin(player)

	player.Chatted:Connect(function(message) onChatted(player, message) end)

	if checkSetting("Log PlayerAdded and PlayerRemoving").current then
		local logData = {
			["content"] = player.DisplayName.." (@"..player.Name..") joined the server.",
			["avatar_url"] = "https://www.roblox.com/headshot-thumbnail/image?userId="..player.UserId.."&width=420&height=420&format=png",
			["username"] = player.DisplayName,
			["allowed_mentions"] = {parse = {}}
		}

		logData = httpService:JSONEncode(logData)

		pcall(function()
			local req = originalRequest({
				Url = checkSetting("Player Added and Removing Webhook URL").current,
				Method = 'POST',
				Headers = {
					['Content-Type'] = 'application/json',
				},
				Body = logData
			})
		end)

	end

	if checkSetting("Friend Notifications").current then
		if localPlayer:IsFriendsWith(player.UserId) then
			queueNotification("Friend Joined", "Your friend "..player.DisplayName.." has joined your server.", 4370335364)
		end
	end
end)

players.PlayerRemoving:Connect(function(player)
	if siriusValues.moderatorDetectionSeen then
		siriusValues.moderatorDetectionSeen[player.UserId] = nil
	end

	if siriusValues.knownUserWatchlistSeen then
		siriusValues.knownUserWatchlistSeen[player.UserId] = nil
	end

	if checkSetting("Log PlayerAdded and PlayerRemoving").current then
		local logData = {
			["content"] = player.DisplayName.." (@"..player.Name..") left the server.",
			["avatar_url"] = "https://www.roblox.com/headshot-thumbnail/image?userId="..player.UserId.."&width=420&height=420&format=png",
			["username"] = player.DisplayName,
			["allowed_mentions"] = {parse = {}}
		}

		logData = httpService:JSONEncode(logData)

		pcall(function()
			local req = originalRequest({
				Url = checkSetting("Player Added and Removing Webhook URL").current,
				Method = 'POST',
				Headers = {
					['Content-Type'] = 'application/json',
				},
				Body = logData
			})
		end)
	end

	removePlayer(player)
	locatedPlayers[player.Name] = nil

	if espConnections[player] then
		espConnections[player]:Disconnect()
		espConnections[player] = nil
	end

	local highlight = espContainer:FindFirstChild(player.Name)
	if highlight then
		highlight:Destroy()
	end
end)

localPlayer.CharacterAdded:Connect(function()
	task.delay(0.2, function()
		if not checkSirius() then
			return
		end

		for _, slider in ipairs(siriusValues.sliders) do
			if slider.callback and slider.value ~= nil then
				pcall(slider.callback, slider.value)
			end
		end

		if characterRuntime.clientAuraStrength > 0 then
			characterRuntime.applyClientAura(characterRuntime.clientAuraStrength)
		end

		if characterRuntime.ghostModeEnabled then
			characterRuntime.applyGhostMode(true)
		end

		if characterRuntime.shadersEnabled then
			characterRuntime.applyShaders(true)
		end

		characterRuntime.ensureClickTeleportTool()
	end)
end)

userInputService.InputChanged:Connect(function(input, gameProcessed)
	if gameProcessed or not characterRuntime.ghostModeEnabled then
		return
	end

	if input.UserInputType ~= Enum.UserInputType.MouseWheel then
		return
	end

	local wheelDelta = 0
	if input.Position then
		wheelDelta = input.Position.Z
	end
	if wheelDelta == 0 and input.Delta then
		wheelDelta = input.Delta.Z
	end
	if wheelDelta == 0 then
		return
	end

	local zoomStep = characterRuntime.ghostZoomStep or 1
	local zoomMin = characterRuntime.ghostZoomMin or 0
	local zoomMax = characterRuntime.ghostZoomMax or 18
	local currentZoom = characterRuntime.ghostThirdPersonDistance or 8

	if wheelDelta > 0 then
		currentZoom -= zoomStep
	else
		currentZoom += zoomStep
	end

	characterRuntime.ghostThirdPersonDistance = math.clamp(currentZoom, zoomMin, zoomMax)
end)

runService.RenderStepped:Connect(function(deltaTime)
	if not checkSirius() then
		return
	end

	characterRuntime.updateGhostMode(deltaTime)

	local character = localPlayer.Character

	if not characterRuntime.ghostModeEnabled and characterRuntime.autoAimAssistStrength > 0 then
		local targetData = characterRuntime.scanTargetData
		characterRuntime.currentAutoAimTarget = targetData and targetData.player or nil
		local closestHeadTip = targetData and characterRuntime.getPredictedAimPoint(targetData) or nil
		if closestHeadTip then
			local desiredCFrame = CFrame.lookAt(camera.CFrame.Position, closestHeadTip)
			if characterRuntime.autoAimAssistStrength >= 100 then
				camera.CFrame = desiredCFrame
			else
				local speedAlpha = characterRuntime.autoAimAssistStrength / 100
				local blend = math.clamp((speedAlpha ^ 1.7) * deltaTime * 32, 0, 1)
				camera.CFrame = camera.CFrame:Lerp(desiredCFrame, blend)
			end
		end
	else
		characterRuntime.currentAutoAimTarget = nil
	end

	if characterRuntime.clientAuraStrength > 0 then
		local auraObjects = characterRuntime.ensureAuraObjects()
		if auraObjects then
			local now = os.clock()
			if now - characterRuntime.auraLastTick >= 0.06 then
				characterRuntime.auraLastTick = now

				local alpha = characterRuntime.clientAuraStrength / 100
				local pulse = 0.5 + (0.5 * math.sin(now * 3.4))
				local hueA = (0.56 + now * 0.065) % 1
				local hueB = (hueA + 0.12) % 1
				local hueC = (hueA + 0.23) % 1
				local colorA = Color3.fromHSV(hueA, 0.72, 1)
				local colorB = Color3.fromHSV(hueB, 0.68, 1)
				local colorC = Color3.fromHSV(hueC, 0.62, 1)

				auraObjects.sparkEmitter.Color = ColorSequence.new({
					ColorSequenceKeypoint.new(0, colorA),
					ColorSequenceKeypoint.new(0.45, colorB),
					ColorSequenceKeypoint.new(1, colorC),
				})
				auraObjects.mistEmitter.Color = ColorSequence.new({
					ColorSequenceKeypoint.new(0, colorB),
					ColorSequenceKeypoint.new(1, colorC),
				})
				auraObjects.starEmitter.Color = ColorSequence.new({
					ColorSequenceKeypoint.new(0, colorC),
					ColorSequenceKeypoint.new(1, colorA),
				})
				auraObjects.auraBeam.Color = ColorSequence.new({
					ColorSequenceKeypoint.new(0, colorA),
					ColorSequenceKeypoint.new(0.5, colorB),
					ColorSequenceKeypoint.new(1, colorC),
				})
				auraObjects.auraBeam.Width0 = 0.9 + (alpha * 1.5) + (pulse * 0.22)
				auraObjects.auraBeam.Width1 = 0.24 + (alpha * 0.65) + (pulse * 0.08)
				auraObjects.auraLight.Color = colorB
				auraObjects.auraLight.Brightness = 2.4 + (alpha * 6.5) + (pulse * 1.6)
				auraObjects.auraLight.Range = 11 + (alpha * 24) + (pulse * 4)
				auraObjects.auraHighlight.FillColor = colorA:Lerp(colorB, 0.35)
				auraObjects.auraHighlight.OutlineColor = colorB:Lerp(colorC, 0.45)
			end
		end
	end
end)

runService.RenderStepped:Connect(function(frame)
	if not checkSirius() then return end
	local fps = math.round(1/frame)

	table.insert(siriusValues.frameProfile.fpsQueue, fps)
	siriusValues.frameProfile.totalFPS += fps

	if #siriusValues.frameProfile.fpsQueue > siriusValues.frameProfile.fpsQueueSize then
		siriusValues.frameProfile.totalFPS -= siriusValues.frameProfile.fpsQueue[1]
		table.remove(siriusValues.frameProfile.fpsQueue, 1)
	end
end)

runService.Stepped:Connect(function()
	if not checkSirius() then return end

	local character = localPlayer.Character
	if character then
			-- No Clip
			local noclipEnabled = siriusValues.actions[1].enabled
			local flingEnabled = siriusValues.actions[5].enabled

			for _, part in ipairs(character:GetDescendants()) do
				if part:IsA("BasePart") then
					if noclipDefaults[part] == nil then
						task.wait()
						noclipDefaults[part] = part.CanCollide
					else
						if noclipEnabled or flingEnabled then
							part.CanCollide = false
						else
							part.CanCollide = noclipDefaults[part]
						end
					end
				end
			end
	end
end)

runService.Heartbeat:Connect(function(frame)
	if not checkSirius() then return end

	local character = localPlayer.Character
	local rootPart = character and characterRuntime.getRootPart(character)
	local humanoid = character and character:FindFirstChildOfClass("Humanoid")
	local flightEnabled = siriusValues.actions[2] and siriusValues.actions[2].enabled
	characterRuntime.updateTargetScan(frame)
	characterRuntime.updateWalkInAir(character, rootPart)
	characterRuntime.updateAutoFind(character, humanoid, rootPart, flightEnabled, frame)
	characterRuntime.ensureClickTeleportTool()
	local primaryPart = character and (character.PrimaryPart or rootPart)
	if primaryPart then
		local bodyVelocity, bodyGyro = unpack(movers)

		-- Drop cached movers if the old character was destroyed and took them with it.
		-- Setting Parent on a destroyed instance throws, so probe before using.
		if bodyVelocity then
			local alive = pcall(function() bodyVelocity.Parent = bodyVelocity.Parent end)
			if not alive then
				movers = {}
				bodyVelocity, bodyGyro = nil, nil
			end
		end

		if not bodyVelocity then
			bodyVelocity = Instance.new("BodyVelocity")
			bodyVelocity.MaxForce = Vector3.one * 9e9

			bodyGyro = Instance.new("BodyGyro")
			bodyGyro.MaxTorque = Vector3.one * 9e9
			bodyGyro.P = 9e4

			local bodyAngularVelocity = Instance.new("BodyAngularVelocity")
			bodyAngularVelocity.AngularVelocity = Vector3.yAxis * 9e9
			bodyAngularVelocity.MaxTorque = Vector3.yAxis * 9e9
			bodyAngularVelocity.P = 9e9

			movers = { bodyVelocity, bodyGyro, bodyAngularVelocity }
		end

		-- Fly
		if flightEnabled then
			local camCFrame = camera.CFrame
			local velocity = Vector3.zero
			local rotation = camCFrame.Rotation

			if userInputService:IsKeyDown(Enum.KeyCode.W) then
				velocity += camCFrame.LookVector
				rotation *= CFrame.Angles(math.rad(-40), 0, 0)
			end
			if userInputService:IsKeyDown(Enum.KeyCode.S) then
				velocity -= camCFrame.LookVector
				rotation *= CFrame.Angles(math.rad(40), 0, 0)
			end
				if userInputService:IsKeyDown(Enum.KeyCode.D) then
					velocity += camCFrame.RightVector
					rotation *= CFrame.Angles(0, 0, math.rad(-40))
				end
				if userInputService:IsKeyDown(Enum.KeyCode.A) then
					velocity -= camCFrame.RightVector
					rotation *= CFrame.Angles(0, 0, math.rad(40))
				end
				local flyUpKey, flyDownKey = getFlyVerticalControlKeys()
				if userInputService:IsKeyDown(flyUpKey) then
					velocity += Vector3.yAxis
				end
				if userInputService:IsKeyDown(flyDownKey) then
					velocity -= Vector3.yAxis
				end

				local tweenInfo = TweenInfo.new(0.5)
			tweenService:Create(bodyVelocity, tweenInfo, { Velocity = velocity * siriusValues.sliders[3].value * 45 }):Play()
			bodyVelocity.Parent = primaryPart

				if not siriusValues.actions[5].enabled then
					tweenService:Create(bodyGyro, tweenInfo, { CFrame = rotation }):Play()
					bodyGyro.Parent = primaryPart
				end
		else
			bodyVelocity.Parent = nil
			bodyGyro.Parent = nil
		end
	end
end)

-- Anonymous Client throttle/transition state
local anonymousTickCounter = 0
local anonymousWasEnabled = false
local ANONYMOUS_TICK_INTERVAL = 15 -- run roughly 4x/sec instead of every frame

runService.Heartbeat:Connect(function(frame)
	if not checkSirius() then return end
	if Pro then
		if checkSetting("Spatial Shield").current and tonumber(checkSetting("Spatial Shield Threshold").current) then
			local threshold = math.clamp(tonumber(checkSetting("Spatial Shield Threshold").current) or 300, 100, 1000)
			-- iterate backwards so table.remove doesn't skip entries
			for i = #soundInstances, 1, -1 do
				local sound = soundInstances[i]
				if not sound then
					table.remove(soundInstances, i)
				elseif not sound.Parent then
					cachedIds[sound] = nil
					table.remove(soundInstances, i)
				elseif sound.Name == "SiriusMusicAudio" or sound:IsDescendantOf(UI) then
					cachedIds[sound] = nil
					table.remove(soundInstances, i)
				elseif sound.IsPlaying and sound.Volume > 0 and gameSettings.MasterVolume * sound.PlaybackLoudness * sound.Volume >= threshold then
					local soundId = tostring(sound.SoundId or "")

					if sound.Volume > 0.55 then
						if soundId ~= "" then suppressedSounds[soundId] = "S" end
						sound.Volume = 0.5
					elseif sound.Volume > 0.2 and sound.Volume < 0.55 then
						if soundId ~= "" then suppressedSounds[soundId] = "S2" end
						sound.Volume = 0.1
					elseif sound.Volume < 0.2 then
						if soundId ~= "" then suppressedSounds[soundId] = "Mute" end
						sound.Volume = 0
					end
					if soundSuppressionNotificationCooldown == 0 then
						queueNotification("Spatial Shield","A high-volume audio is being played ("..sound.Name..") and it has been suppressed.", 4483362458)
						soundSuppressionNotificationCooldown = 15
					end
					cachedIds[sound] = nil
					table.remove(soundInstances, i)
				end
			end
		end

		if soundSuppressionNotificationCooldown > 0 then
			soundSuppressionNotificationCooldown -= 1
		end
	end

	local anonymousEnabled = checkSetting("Anonymous Client [Client]").current

	if anonymousEnabled then
		-- Throttle: do the scan on every Nth heartbeat rather than every frame.
		anonymousTickCounter += 1
		if anonymousTickCounter >= ANONYMOUS_TICK_INTERVAL then
			anonymousTickCounter = 0

			for i = #cachedText, 1, -1 do
				local text = cachedText[i]
				if not text or not text.Parent then
					-- Drop destroyed/orphaned labels so we stop scanning them.
					table.remove(cachedText, i)
				elseif originalTextValues[text] == nil then
					-- Only inspect labels we haven't already anonymized.
					local raw = text.Text
					local lowerText = string.lower(raw)
					if string.find(lowerText, lowerName, 1, true) or string.find(lowerText, lowerDisplayName, 1, true) then
						storeOriginalText(text)
						local newText = string.gsub(string.gsub(lowerText, lowerName, randomUsername), lowerDisplayName, randomUsername)
						text.Text = string.gsub(newText, "^%l", string.upper)
					end
				end
			end
		end
	elseif anonymousWasEnabled then
		-- Only undo once on the off-transition, not every frame.
		undoAnonymousChanges()
		table.clear(originalTextValues)
	end

	anonymousWasEnabled = anonymousEnabled
end)

for _, instance in next, game:GetDescendants() do
	if instance:IsA("Sound") then
		if instance.Name == "SiriusMusicAudio" or instance:IsDescendantOf(UI) then
			continue
		elseif suppressedSounds[instance.SoundId] then
			if suppressedSounds[instance.SoundId] == "S" then
				instance.Volume = 0.5
			elseif suppressedSounds[instance.SoundId] == "S2" then
				instance.Volume = 0.1
			else
				instance.Volume = 0
			end
		else
			if not cachedIds[instance] then
				table.insert(soundInstances, instance)
				cachedIds[instance] = true
			end
		end
	elseif instance:IsA("TextLabel") or instance:IsA("TextButton") then
		if not table.find(cachedText, instance) then
			table.insert(cachedText, instance)
		end
	end
end

descendantAddedConn = game.DescendantAdded:Connect(function(instance)
	if checkSirius() then
		if instance:IsA("Sound") then
			if instance.Name == "SiriusMusicAudio" or instance:IsDescendantOf(UI) then
				return
			elseif suppressedSounds[instance.SoundId] then
				if suppressedSounds[instance.SoundId] == "S" then
					instance.Volume = 0.5
				elseif suppressedSounds[instance.SoundId] == "S2" then
					instance.Volume = 0.1
				else
					instance.Volume = 0
				end
			else
				if not cachedIds[instance] then
					table.insert(soundInstances, instance)
					cachedIds[instance] = true
				end
			end
		elseif instance:IsA("TextLabel") or instance:IsA("TextButton") then
			if not table.find(cachedText, instance) then
				table.insert(cachedText, instance)
			end
		end
	end
end)


task.spawn(function()
while task.wait(1) do
	if not checkSirius() then
		if espContainer then espContainer:Destroy() end
		if descendantAddedConn then descendantAddedConn:Disconnect() descendantAddedConn = nil end
		for player, conn in pairs(espConnections) do
			conn:Disconnect()
			espConnections[player] = nil
		end
		undoAnonymousChanges()
		break
	end

	smartBar.Time.Text = os.date("%H")..":"..os.date("%M")
	task.spawn(UpdateHome)

	if getconnections then
		for _, connection in getconnections(localPlayer.Idled) do
			if not checkSetting("Anti Idle").current then connection:Enable() else connection:Disable() end
		end
	end

	toggle.Visible = not checkSetting("Hide Toggle Button").current

	-- Disconnected Check
	local disconnectedRobloxUI = coreGui.RobloxPromptGui.promptOverlay:FindFirstChild("ErrorPrompt")

		if disconnectedRobloxUI and not promptedDisconnected then
			local reasonPrompt = disconnectedRobloxUI.MessageArea.ErrorFrame.ErrorMessage.Text

			promptedDisconnected = true
			disconnectedPrompt.Parent = coreGui.RobloxPromptGui
			disconnectedPrompt.ZIndex = 10000
			for _, promptObject in ipairs(disconnectedPrompt:GetDescendants()) do
				pcall(function()
					promptObject.ZIndex = math.max(promptObject.ZIndex, 10001)
				end)
			end

			local disconnectType
			local foundString

		for _, preDisconnectType in ipairs(siriusValues.disconnectTypes) do
			for _, typeString in pairs(preDisconnectType[2]) do
				if string.find(reasonPrompt, typeString) then
					disconnectType = preDisconnectType[1]
					foundString = true
					break
				end
			end
		end

		if not foundString then disconnectType = "kick" end

		wipeTransparency(disconnectedPrompt, 1, true)
		disconnectedPrompt.Visible = true

		if disconnectType == "ban" then
			disconnectedPrompt.Content.Text = "You've been banned, would you like to leave this server?"
			disconnectedPrompt.Action.Text = "Leave"
			disconnectedPrompt.Action.Size = UDim2.new(0, 77, 0, 36) -- use textbounds

			disconnectedPrompt.UIGradient.Color = ColorSequence.new({
				ColorSequenceKeypoint.new(0, Color3.new(0,0,0)),
				ColorSequenceKeypoint.new(1, Color3.new(0.819608, 0.164706, 0.164706))
			})
		elseif disconnectType == "kick" then
			disconnectedPrompt.Content.Text = "You've been kicked, would you like to serverhop?"
			disconnectedPrompt.Action.Text = "Serverhop"
			disconnectedPrompt.Action.Size = UDim2.new(0, 114, 0, 36)

			disconnectedPrompt.UIGradient.Color = ColorSequence.new({
				ColorSequenceKeypoint.new(0, Color3.new(0,0,0)),
				ColorSequenceKeypoint.new(1, Color3.new(0.0862745, 0.596078, 0.835294))
			})
		elseif disconnectType == "network" then
			disconnectedPrompt.Content.Text = "You've lost connection, would you like to rejoin?"
			disconnectedPrompt.Action.Text = "Rejoin"
			disconnectedPrompt.Action.Size = UDim2.new(0, 82, 0, 36)

			disconnectedPrompt.UIGradient.Color = ColorSequence.new({
				ColorSequenceKeypoint.new(0, Color3.new(0,0,0)),
				ColorSequenceKeypoint.new(1, Color3.new(0.862745, 0.501961, 0.0862745))
			})
		end

		tweenService:Create(disconnectedPrompt, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {BackgroundTransparency = 0}):Play()
		tweenService:Create(disconnectedPrompt.Title, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 0}):Play()
		tweenService:Create(disconnectedPrompt.Content, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 0.3}):Play()
		tweenService:Create(disconnectedPrompt.Action, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {BackgroundTransparency = 0.7}):Play()
		tweenService:Create(disconnectedPrompt.Action, TweenInfo.new(.5,Enum.EasingStyle.Quint),  {TextTransparency = 0}):Play()

		disconnectedPrompt.Action.MouseButton1Click:Connect(function()
			if disconnectType == "ban" then
				game:Shutdown() -- leave
			elseif disconnectType == "kick" then
				serverhop()
			elseif disconnectType == "network" then
				rejoin()
			end
		end)
	end

	if Pro then
		-- all Pro checks here!

		-- Two-Way Adaptive Latency Checks
		if checkHighPing() then
			if siriusValues.pingProfile.pingNotificationCooldown <= 0 then
				if checkSetting("Adaptive Latency Warning").current then
					queueNotification("High Latency Warning","We've noticed your latency has reached a higher value than usual, you may find that you are lagging or your actions are delayed in-game. Consider checking for any background downloads on your machine.", 4370305588)
					siriusValues.pingProfile.pingNotificationCooldown = 120
				end
			end
		end

		if siriusValues.pingProfile.pingNotificationCooldown > 0 then
			siriusValues.pingProfile.pingNotificationCooldown -= 1
		end

		-- Adaptive frame time checks
		if siriusValues.frameProfile.frameNotificationCooldown <= 0 then
			if #siriusValues.frameProfile.fpsQueue > 0 then
				local avgFPS = siriusValues.frameProfile.totalFPS / #siriusValues.frameProfile.fpsQueue

				if avgFPS < siriusValues.frameProfile.lowFPSThreshold then
					if checkSetting("Adaptive Performance Warning").current then
						queueNotification("Degraded Performance","We've noticed your client's frames per second have decreased. Consider checking for any background tasks or programs on your machine.", 4384400106)
						siriusValues.frameProfile.frameNotificationCooldown = 120	
					end
				end
			end
		end

	if siriusValues.frameProfile.frameNotificationCooldown > 0 then
		siriusValues.frameProfile.frameNotificationCooldown -= 1
	end
end
end
end)



--[[ ==========================================================================
	Sirius X - NextGen
	Real Sirius X (above) runs underneath. Bridge injections expose its own
	feature tables / functions, and this Gen3 Rayfield front-end rebuilds every
	panel (Home, Character, Tuning, Players, Scripts, Music, Settings) as a tab
	that drives the exact same Sirius code paths.
============================================================================ ]]

task.spawn(function()
	local players = game:GetService("Players")
	local localPlayer = players.LocalPlayer
	local statsService = game:GetService("Stats")
	local httpService = game:GetService("HttpService")

	-- Wait for the Sirius bridge injected into siriusValues -------------------
	local bridge
	do
		local t0 = os.clock()
		repeat
			bridge = (getgenv and getgenv().__SIRIUSX_BRIDGE) or _G.__SIRIUSX_BRIDGE
			if bridge then break end
			task.wait(0.1)
		until (os.clock() - t0) > 20
	end
	if not bridge or not bridge.actions or not bridge.sliders then
		warn("[Sirius X - NextGen] Could not reach the Sirius feature bridge; native Sirius UI is still available.")
		return
	end

	-- Load the Gen3 Rayfield concept UI library -------------------------------
	local ok, Rayfield = pcall(function()
		return loadstring(game:HttpGet(
			"https://raw.githubusercontent.com/SyncOfficialSpec/Rayfield_Gen_3_Concept/main/source.lua"
		))()
	end)
	if not ok or not Rayfield then
		warn("[Sirius X - NextGen] Failed to load Gen3 UI library:", Rayfield)
		return
	end

	local function notify(title, content, dur)
		pcall(function()
			Rayfield:Notify({ Title = title, Content = content, Duration = dur or 4, Image = "sparkles" })
		end)
	end

	-- Clipboard + command helpers --------------------------------------------
	local setclip = setclipboard or set_clipboard or toclipboard or (syn and syn.write_clipboard)

	-- Run an Infinite Yield command through Sirius's own integrated bridge.
	-- runIntegratedIYCommand is a global in the Sirius chunk; the IY bridge is a
	-- second fallback. Handles rejoin / serverhop / goto <player> / etc.
	local function runCommand(text)
		if not text or text == "" then return false end
		if type(runIntegratedIYCommand) == "function" then
			pcall(runIntegratedIYCommand, text)
			return true
		end
		local iy = _G.__SIRIUS_IY_BRIDGE
		if iy and type(iy.execCmd) == "function" then
			pcall(iy.execCmd, text, localPlayer, true)
			return true
		end
		return false
	end

	-- Feature-table helpers (Character + Tuning) ------------------------------
	local function findAction(name)
		local target = tostring(name):lower()
		for _, a in ipairs(bridge.actions) do
			if a.name and a.name:lower():find(target, 1, true) then return a end
		end
	end
	local function findSlider(name)
		local target = tostring(name):lower()
		for _, s in ipairs(bridge.sliders) do
			if s.name and s.name:lower():find(target, 1, true) then return s end
		end
	end
	local function setToggle(action, value)
		if not action then return end
		if action.enabled ~= value then
			action.enabled = value
			pcall(action.callback, value)
		end
	end
	local function fireAction(action)
		if not action then return end
		action.enabled = true
		pcall(action.callback)
		local after = action.disableAfter or 0
		task.delay(after, function() action.enabled = false end)
	end
	local function setSlider(slider, value)
		if not slider then return end
		slider.value = value
		pcall(slider.callback, value)
	end

	-- Native Sirius ScreenGui (hide it; Gen3 is the face) ---------------------
	local nativeGui
	do
		local name = bridge.siriusName or "Sirius"
		local guiParent = (typeof(gethui) == "function" and gethui()) or game:GetService("CoreGui")
		nativeGui = guiParent:FindFirstChild(name)
		if not nativeGui then
			nativeGui = game:GetService("CoreGui"):FindFirstChild(name)
		end
		if nativeGui and nativeGui:IsA("ScreenGui") then
			nativeGui.Enabled = false
		end
	end
	local function showNative(state)
		if nativeGui then nativeGui.Enabled = state end
	end

	-- Window ------------------------------------------------------------------
	local Window = Rayfield:CreateWindow({
		Name = "Sirius X - NextGen",
		Subtitle = "Rayfield Gen3",
		Icon = "sparkles",
		Badge = {Text = "sirius-x", Icon = "shield"},
		LoadingTitle = "Sirius X - NextGen",
		LoadingSubtitle = "Sirius X reskinned in Gen3",
		ToggleUIKeybind = "K",
		-- Config saving OFF: restoring state would force-apply features on load.
		ConfigurationSaving = { Enabled = false },
	})

	-- ==========================================================================
	-- HOME tab
	-- ==========================================================================
	local Home = Window:CreateTab("Home", "house")
	Home:CreateSection("Server")
	local pingStat    = Home:CreateStat({ Name = "Ping",    Icon = "activity", Value = "--" })
	local playersStat = Home:CreateStat({ Name = "Players", Icon = "users",    Value = "--" })
	local fpsStat     = Home:CreateStat({ Name = "FPS",     Icon = "gauge",    Value = "--" })
	task.spawn(function()
		while true do
			pcall(function()
				local ping = statsService.Network.ServerStatsItem["Data Ping"]:GetValue()
				pingStat:Set({ Value = math.floor(ping) .. "ms" })
			end)
			pcall(function()
				playersStat:Set({ Value = #players:GetPlayers() .. "/" .. players.MaxPlayers })
			end)
			pcall(function()
				fpsStat:Set({ Value = tostring(math.floor(workspace:GetRealPhysicsFPS())) })
			end)
			task.wait(1)
		end
	end)

	Home:CreateSection("Quick Actions")
	Home:CreateButton({ Name = "Rejoin Server", Icon = "rotate-ccw",
		Callback = function() if not runCommand("rejoin") then notify("Home", "Commands not ready yet.") end end })
	Home:CreateButton({ Name = "Server Hop", Icon = "shuffle",
		Callback = function() if not runCommand("serverhop") then notify("Home", "Commands not ready yet.") end end })
	Home:CreateButton({ Name = "Copy Job Id", Icon = "clipboard",
		Callback = function()
			if setclip then pcall(setclip, tostring(game.JobId)); notify("Home", "Job Id copied.")
			else notify("Home", "Clipboard unavailable on this executor.") end
		end })

	Home:CreateSection("Command Console")
	Home:CreateInput({
		Name = "Run Command",
		PlaceholderText = "fly, goto Player, god, ws 50 ...",
		CurrentValue = "",
		Callback = function(text)
			if not runCommand(text) then
				notify("Commands", "Infinite Yield is still loading, try again in a moment.")
			end
		end,
	})
	for _, c in ipairs({
		{ "Anti AFK", "antiafk", "coffee" },
		{ "Full Bright", "fullbright", "sun" },
		{ "Boost FPS", "boostfps", "zap" },
	}) do
		Home:CreateButton({ Name = c[1], Icon = c[3], Callback = function() runCommand(c[2]) end })
	end

	-- ==========================================================================
	-- CHARACTER tab (16 action toggles)
	-- ==========================================================================
	local Character = Window:CreateTab("Character", "user")

	local function addToggle(tab, key, icon)
		local a = findAction(key)
		if a then
			tab:CreateToggle({
				Name = a.name,
				Icon = icon,
				CurrentValue = a.enabled == true,
				Flag = "SX_" .. a.name,
				Callback = function(v) setToggle(a, v) end,
			})
		end
	end

	Character:CreateSection("Movement")
	addToggle(Character, "Noclip", "ghost")
	addToggle(Character, "Flight", "plane")
	addToggle(Character, "Infinite Jump", "arrow-up")
	addToggle(Character, "Walk in Air", "footprints")
	addToggle(Character, "Anti Void", "shield")
	addToggle(Character, "Ghost Mode", "eye")
	addToggle(Character, "Invisibility", "eye-off")

	Character:CreateSection("Combat & Visuals")
	addToggle(Character, "Extrasensory", "radar")
	addToggle(Character, "Auto Find", "crosshair")
	addToggle(Character, "Fling", "wind")
	addToggle(Character, "Shaders", "sun")

	Character:CreateSection("World")
	addToggle(Character, "Night and Day", "moon")
	addToggle(Character, "Global Audio", "volume-2")

	Character:CreateSection("Actions")
	do
		local refresh = findAction("Refresh")
		if refresh then
			Character:CreateButton({ Name = "Refresh Character", Icon = "refresh-cw",
				Callback = function() fireAction(refresh) end })
		end
		local respawn = findAction("Respawn")
		if respawn then
			Character:CreateButton({ Name = "Respawn", Icon = "rotate-ccw",
				Callback = function() fireAction(respawn) end })
		end
		addToggle(Character, "click teleport", "mouse-pointer-click")
	end

	-- ==========================================================================
	-- TUNING tab (7 sliders)
	-- ==========================================================================
	local Tuning = Window:CreateTab("Tuning", "sliders-horizontal")
	Tuning:CreateSection("Character Tuning")
	local sliderSpecs = {
		{ key = "player speed",       icon = "gauge",      suffix = "" },
		{ key = "jump",               icon = "arrow-up",   suffix = "" },   -- "jump power" or "jump height"
		{ key = "flight speed",       icon = "plane",      suffix = "" },
		{ key = "field of view",      icon = "eye",        suffix = "\194\176" },
		{ key = "gravity",            icon = "arrow-down", suffix = "" },
		{ key = "smooth screen blur", icon = "droplet",    suffix = "" },
		{ key = "auto aim",           icon = "crosshair",  suffix = "%" },
	}
	for _, spec in ipairs(sliderSpecs) do
		local s = findSlider(spec.key)
		if s then
			local range = s.values or { 0, 100 }
			local lo, hi = range[1] or 0, range[2] or 100
			local initial = math.clamp(math.floor((s.value or s.default or lo) + 0.5), lo, hi)
			Tuning:CreateSlider({
				Name = s.name:gsub("^%l", string.upper),
				Icon = spec.icon,
				Range = { lo, hi },
				Increment = 1,
				Suffix = spec.suffix,
				CurrentValue = initial,
				Flag = "SX_" .. s.name,
				Callback = function(v) setSlider(s, v) end,
			})
		end
	end

	-- ==========================================================================
	-- PLAYERS tab
	-- ==========================================================================
	local pa = bridge.playerActions or {}
	local Playerlist = Window:CreateTab("Players", "users")
	local selectedName

	local function playerNames()
		local t = {}
		for _, p in ipairs(players:GetPlayers()) do
			if p ~= localPlayer then table.insert(t, p.Name) end
		end
		table.sort(t)
		return t
	end
	local function target()
		if not selectedName then return nil end
		return players:FindFirstChild(selectedName)
	end
	local function withTarget(fn)
		local p = target()
		if not p then notify("Players", "Select a player first."); return end
		pcall(fn, p)
	end

	Playerlist:CreateSection("Target")
	local playerDropdown = Playerlist:CreateDropdown({
		Name = "Select Player",
		Options = playerNames(),
		CurrentOption = {},
		Callback = function(sel) selectedName = sel[1] end,
	})
	Playerlist:CreateButton({ Name = "Refresh List", Icon = "refresh-cw",
		Callback = function() pcall(function() playerDropdown:Refresh(playerNames()) end) end })
	players.PlayerAdded:Connect(function() pcall(function() playerDropdown:Refresh(playerNames()) end) end)
	players.PlayerRemoving:Connect(function() pcall(function() playerDropdown:Refresh(playerNames()) end) end)

	Playerlist:CreateSection("View")
	Playerlist:CreateButton({ Name = "Spectate", Icon = "eye",
		Callback = function() withTarget(function(p) if pa.spectate then pa.spectate(p) end end) end })
	Playerlist:CreateButton({ Name = "Stop Spectate", Icon = "eye-off",
		Callback = function() if pa.stopSpectate then pcall(pa.stopSpectate, "Stopped from NextGen") end end })
	Playerlist:CreateButton({ Name = "Teleport To", Icon = "move",
		Callback = function() withTarget(function(p) runCommand("goto " .. p.Name) end) end })

	Playerlist:CreateSection("Markers")
	local function markerBtn(label, icon, fnKey)
		Playerlist:CreateButton({ Name = label, Icon = icon,
			Callback = function()
				withTarget(function(p)
					local fn = pa[fnKey]
					if not fn then notify("Players", label .. " unavailable."); return end
					local enabled = fn(p)
					if type(enabled) == "boolean" then
						notify(label, p.Name .. ": " .. (enabled and "on" or "off"), 3)
					end
				end)
			end })
	end
	markerBtn("Toggle ESP", "radar", "esp")
	markerBtn("Toggle Tracer", "git-commit-horizontal", "tracer")
	markerBtn("Toggle Hide (local)", "eye-off", "hide")
	markerBtn("Track", "map-pin", "track")
	markerBtn("Live Track", "navigation", "liveTrack")

	Playerlist:CreateSection("Wielding")
	local wield = pa.wieldingTools
	local function wieldBtn(label, ability)
		Playerlist:CreateButton({ Name = label,
			Callback = function()
				withTarget(function(p)
					if wield and wield.Toggle then pcall(wield.Toggle, p, ability)
					else notify("Players", "Wielding unavailable.") end
				end)
			end })
	end
	wieldBtn("Fling Player", "FlingPlayer")
	wieldBtn("Fling 2.0", "Fling2")
	wieldBtn("Platform Fly", "PlatformFly")
	wieldBtn("Magic Carpet", "MagicCarpet")
	wieldBtn("Elevator", "Elevator")
	wieldBtn("Anti Jump", "AntiJump")
	wieldBtn("Infinite Jump", "InfiniteJump")
	wieldBtn("Speed Boost Slow", "SpeedSlow")
	wieldBtn("Speed Boost Medium", "SpeedMedium")
	wieldBtn("Speed Boost Fast", "SpeedFast")
	wieldBtn("Movement Breaker", "MovementBreaker")
	Playerlist:CreateButton({ Name = "Stop Wielding", Icon = "octagon-x",
		Callback = function() if wield and wield.Stop then pcall(wield.Stop, false) end end })

	Playerlist:CreateSection("Rockets")
	Playerlist:CreateButton({ Name = "Rocket Target",
		Callback = function() withTarget(function(p) if wield and wield.Rocket then pcall(wield.Rocket, p, 50, "Rocket Target") end end) end })
	Playerlist:CreateButton({ Name = "GODSPEED Rocket",
		Callback = function() withTarget(function(p) if wield and wield.Rocket then pcall(wield.Rocket, p, 5000, "GODSPEED Rocket") end end) end })

	-- ==========================================================================
	-- VISUALS tab -- ESP colour, using the Gen2 colour picker (Gen3 copied it
	-- from Gen2 verbatim; Glow=false runs the identical Gen2 code path).
	-- ==========================================================================
	do
		local espContainer = bridge.espContainer
		local Visuals = Window:CreateTab("Visuals", "palette")
		local espOutline = Color3.new(1, 1, 1)
		local espFill = Color3.fromRGB(215, 224, 235)
		local espFillT = 1  -- 1 = fill invisible (Sirius default), 0 = solid

		local function paintHighlight(h)
			if h and h:IsA("Highlight") then
				h.OutlineColor = espOutline
				h.FillColor = espFill
				h.FillTransparency = espFillT
			end
		end
		local function applyEsp()
			if not espContainer then return end
			for _, h in ipairs(espContainer:GetChildren()) do paintHighlight(h) end
		end
		if espContainer then
			espContainer.ChildAdded:Connect(function(child)
				task.defer(paintHighlight, child)
			end)
		end

		Visuals:CreateSection("ESP Colour")
		if not espContainer then
			Visuals:CreateParagraph({ Title = "ESP unavailable",
				Content = "This Sirius build did not expose the ESP container." })
		else
			Visuals:CreateColorPicker({
				Name = "Outline Colour", Color = espOutline, Glow = false,
				Flag = "SX_esp_outline",
				Callback = function(c) espOutline = c; applyEsp() end,
			})
			Visuals:CreateColorPicker({
				Name = "Fill Colour", Color = espFill, Glow = false,
				Flag = "SX_esp_fill",
				Callback = function(c) espFill = c; applyEsp() end,
			})
			Visuals:CreateSlider({
				Name = "Fill Opacity", Icon = "droplet",
				Range = { 0, 100 }, Increment = 1, Suffix = "%",
				CurrentValue = 0, Flag = "SX_esp_fillop",
				Callback = function(v) espFillT = 1 - (v / 100); applyEsp() end,
			})
			Visuals:CreateButton({
				Name = "Apply to current ESP", Icon = "check",
				Callback = function() applyEsp() end,
			})
		end
	end

	-- ==========================================================================
	-- SCRIPTS tab (reimplemented data layer: 3 marketplaces + loadstring)
	-- ==========================================================================
	local Scripts = Window:CreateTab("Scripts", "code")
	do
		local httpRequest = (syn and syn.request) or (http and http.request) or http_request or request
			or (fluxus and fluxus.request)
		local sourceEnabled = { scriptblox = true, rscripts = true, roscripts = true }
		local lastResults = {}      -- label -> normalized result
		local resultDropdown

		local function httpGet(url)
			if httpRequest then
				local res = httpRequest({ Url = url, Method = "GET" })
				return res and res.Body
			end
			return game:HttpGet(url)
		end

		local function fetchScripts(query)
			local out = {}
			local q = httpService:UrlEncode(query)
			if sourceEnabled.scriptblox then
				pcall(function()
					local body = httpGet("https://scriptblox.com/api/script/search?q=" .. q .. "&mode=free&max=20&page=1")
					local data = httpService:JSONDecode(body)
					for _, s in ipairs(data.result and data.result.scripts or {}) do
						table.insert(out, {
							provider = "ScriptBlox",
							title = s.title or s.name or "Untitled",
							author = (s.owner and s.owner.username) or "?",
							script = s.script,
							slug = s.slug,
						})
					end
				end)
			end
			if sourceEnabled.rscripts then
				pcall(function()
					local body = httpGet("https://rscripts.net/api/v2/scripts?page=1&orderBy=date&sort=desc&notPaid=true&q=" .. q)
					local data = httpService:JSONDecode(body)
					for _, s in ipairs(data.scripts or {}) do
						table.insert(out, {
							provider = "Rscripts.net",
							title = s.title or s.name or "Untitled",
							author = (s.user and s.user.username) or "?",
							rawScriptUrl = s.rawScript,
						})
					end
				end)
			end
			if sourceEnabled.roscripts then
				pcall(function()
					local body = httpGet("https://roscripts.io/api/search?q=" .. q)
					local data = httpService:JSONDecode(body)
					for _, s in ipairs(data.results or {}) do
						table.insert(out, {
							provider = "Roscripts.io",
							title = s.title or s.name or "Untitled",
							author = s.author or "?",
							detailUrl = "https://roscripts.io/s/" .. tostring(s.slug),
						})
					end
				end)
			end
			return out
		end

		local function resolveSource(result)
			if result.script and result.script ~= "" then return result.script end
			-- ScriptBlox: fetch the script detail by slug for the inline source
			if result.provider == "ScriptBlox" and result.slug then
				local body = httpGet("https://scriptblox.com/api/script/" .. result.slug)
				local ok2, data = pcall(function() return httpService:JSONDecode(body) end)
				if ok2 and data and data.script and data.script.script then
					return data.script.script
				end
			end
			local url = result.rawScriptUrl
			if not url and result.provider == "Roscripts.io" and result.detailUrl then
				local page = httpGet(result.detailUrl) or ""
				url = page:match('href="(https://script%.roscripts%.io/[^"]+)"')
			end
			if url then return httpGet(url) end
			return nil
		end

		Scripts:CreateSection("Sources")
		Scripts:CreateToggle({ Name = "ScriptBlox", CurrentValue = true, Flag = "SX_src_sb",
			Callback = function(v) sourceEnabled.scriptblox = v end })
		Scripts:CreateToggle({ Name = "Rscripts.net", CurrentValue = true, Flag = "SX_src_rs",
			Callback = function(v) sourceEnabled.rscripts = v end })
		Scripts:CreateToggle({ Name = "Roscripts.io", CurrentValue = true, Flag = "SX_src_ro",
			Callback = function(v) sourceEnabled.roscripts = v end })

		Scripts:CreateSection("Search")
		Scripts:CreateInput({
			Name = "Search Scripts",
			PlaceholderText = "e.g. Blox Fruits, Arsenal ...",
			CurrentValue = "",
			Callback = function(query)
				if query == "" then return end
				notify("Scripts", "Searching...")
				task.spawn(function()
					local results = fetchScripts(query)
					lastResults = {}
					local labels = {}
					for _, r in ipairs(results) do
						local label = ("[%s] %s - %s"):format(r.provider, r.title, r.author)
						if not lastResults[label] then
							lastResults[label] = r
							table.insert(labels, label)
						end
						if #labels >= 60 then break end
					end
					if resultDropdown then pcall(function() resultDropdown:Refresh(labels) end) end
					notify("Scripts", #labels .. " result(s). Pick one below and Execute.", 5)
				end)
			end,
		})
		resultDropdown = Scripts:CreateDropdown({
			Name = "Results",
			Options = {},
			CurrentOption = {},
			Callback = function(sel) Scripts._selectedScript = sel[1] end,
		})
		Scripts:CreateButton({
			Name = "Execute Selected",
			Icon = "play",
			Callback = function()
				local label = Scripts._selectedScript
				local result = label and lastResults[label]
				if not result then notify("Scripts", "Search, then pick a result first."); return end
				task.spawn(function()
					local src = resolveSource(result)
					if not src then notify("Scripts", "Could not fetch script source."); return end
					local fn, err = loadstring(src)
					if not fn then notify("Scripts", "Compile error: " .. tostring(err), 6); return end
					local okRun, runErr = pcall(fn)
					if not okRun then notify("Scripts", "Runtime error: " .. tostring(runErr), 6)
					else notify("Scripts", "Executed: " .. result.title, 4) end
				end)
			end,
		})
	end

	-- ==========================================================================
	-- MUSIC tab
	-- ==========================================================================
	local music = bridge.music
	if music and music.state then
		if music.parse then pcall(music.parse) end
		local state = music.state
		local Music = Window:CreateTab("Music", "music")

		local sections = { "All" }
		for _, s in ipairs(state.sections or {}) do table.insert(sections, s) end

		local currentList = {}   -- ordered {label=..., track=...}
		local labelToTrack = {}
		local selectedLabel
		local selectedIndex = 0

		local function buildList(genre)
			currentList = {}
			labelToTrack = {}
			for _, tr in ipairs(state.library or {}) do
				if genre == "All" or tr.section == genre then
					local label = (tr.title or "?") .. " - " .. (tr.artist or "?")
					if not labelToTrack[label] then
						labelToTrack[label] = tr
						table.insert(currentList, label)
						if #currentList >= 150 then break end
					end
				end
			end
			return currentList
		end

		local function audioObj()
			if music.ensureAudio then local a = music.ensureAudio(); if a then return a end end
			return state.audio
		end

		local function playLabel(label)
			local tr = labelToTrack[label]
			if tr and music.play then
				pcall(music.play, tr)
				selectedLabel = label
				for i, l in ipairs(currentList) do if l == label then selectedIndex = i break end end
			end
		end

		Music:CreateSection("Library")
		buildList("All")
		local trackDropdown
		local genreDropdown = Music:CreateDropdown({
			Name = "Genre",
			Options = sections,
			CurrentOption = { "All" },
			Callback = function(sel)
				buildList(sel[1] or "All")
				if trackDropdown then pcall(function() trackDropdown:Refresh(currentList) end) end
			end,
		})
		trackDropdown = Music:CreateDropdown({
			Name = "Track",
			Options = currentList,
			CurrentOption = {},
			Callback = function(sel) selectedLabel = sel[1] end,
		})

		Music:CreateSection("Playback")
		Music:CreateButton({ Name = "Play", Icon = "play",
			Callback = function()
				if selectedLabel then playLabel(selectedLabel)
				else notify("Music", "Pick a track first.") end
			end })
		Music:CreateButton({ Name = "Pause / Resume", Icon = "pause",
			Callback = function()
				local a = audioObj()
				if a then
					if a.Playing then a:Pause() else pcall(function() a:Resume() end) end
				end
			end })
		Music:CreateButton({ Name = "Stop", Icon = "square",
			Callback = function() if music.stop then pcall(music.stop) end end })
		Music:CreateButton({ Name = "Next", Icon = "skip-forward",
			Callback = function()
				if #currentList == 0 then return end
				selectedIndex = (selectedIndex % #currentList) + 1
				playLabel(currentList[selectedIndex])
			end })
		Music:CreateButton({ Name = "Previous", Icon = "skip-back",
			Callback = function()
				if #currentList == 0 then return end
				selectedIndex = ((selectedIndex - 2) % #currentList) + 1
				playLabel(currentList[selectedIndex])
			end })
		Music:CreateSlider({
			Name = "Volume",
			Icon = "volume-2",
			Range = { 0, 100 },
			Increment = 1,
			Suffix = "%",
			CurrentValue = 75,
			Flag = "SX_music_vol",
			Callback = function(v)
				local a = audioObj()
				if a then a.Volume = v / 100 end
			end,
		})
	end

	-- ==========================================================================
	-- SETTINGS tab (rebuilds all siriusSettings categories)
	-- ==========================================================================
	if bridge.settingsList then
		local Settings = Window:CreateTab("Settings", "settings")
		local function save()
			if bridge.saveSettings then pcall(bridge.saveSettings) end
		end

		for _, category in ipairs(bridge.settingsList) do
			Settings:CreateSection(category.name or "General")
			for _, setting in ipairs(category.categorySettings or {}) do
				local st = setting.settingType
				local nm = setting.name or "Setting"

				if st == "Boolean" then
					Settings:CreateToggle({
						Name = nm, Description = setting.description,
						CurrentValue = setting.current == true, Flag = "SET_" .. nm,
						Callback = function(v) setting.current = v; save() end,
					})
				elseif st == "Number" then
					local rng = setting.values or { 0, 100 }
					local rlo, rhi = rng[1] or 0, rng[2] or 100
					Settings:CreateSlider({
						Name = nm, Description = setting.description,
						Range = { rlo, rhi }, Increment = 1,
						CurrentValue = math.clamp(math.floor((tonumber(setting.current) or rlo) + 0.5), rlo, rhi),
						Flag = "SET_" .. nm,
						Callback = function(v) setting.current = v; save() end,
					})
				elseif st == "Input" then
					Settings:CreateInput({
						Name = nm, PlaceholderText = setting.placeholder or "",
						CurrentValue = tostring(setting.current or ""),
						Callback = function(text) setting.current = text; save() end,
					})
				elseif st == "Dropdown" then
					Settings:CreateDropdown({
						Name = nm, Options = setting.values or {},
						CurrentOption = { setting.current },
						Callback = function(sel)
							local v = sel[1]
							setting.current = v
							if setting.targetSetting and bridge.checkSetting then
								local t = bridge.checkSetting(setting.targetSetting)
								if t then t.current = v end
							end
							save()
						end,
					})
				elseif st == "Key" then
					Settings:CreateInput({
						Name = nm, PlaceholderText = "KeyCode e.g. K, Slash, Space",
						CurrentValue = tostring(setting.current or ""),
						Callback = function(text)
							setting.current = (text ~= "" and text) or nil
							save()
						end,
					})
				elseif st == "Action" then
					Settings:CreateButton({
						Name = nm, Description = setting.description, Icon = "zap",
						Callback = function()
							if setting.callback then pcall(setting.callback)
							elseif bridge.dylanfocClient and bridge.dylanfocClient.runAction then
								pcall(bridge.dylanfocClient.runAction, setting)
							else notify("Settings", nm .. " unavailable.") end
						end,
					})
				elseif st == "Info" then
					Settings:CreateParagraph({
						Title = nm,
						Content = tostring(setting.current or "--"),
					})
				end
				-- "Servers" type intentionally skipped (embedded native browser).
			end
		end
	end

	-- ==========================================================================
	-- INTERFACE tab (native UI control)
	-- ==========================================================================
	local Interface = Window:CreateTab("Interface", "layout-grid")
	local nativeToggle

	Interface:CreateSection("Safety")
	Interface:CreateButton({ Name = "Panic (stop abilities)", Icon = "octagon-x",
		Callback = function()
			if bridge.runKeybindAction then pcall(bridge.runKeybindAction, "stopabilities") end
			if pa.stopAll then pcall(pa.stopAll, "Panic") end
		end })

	Interface:CreateSection("Native UI")
	nativeToggle = Interface:CreateToggle({
		Name = "Show original Sirius X UI",
		Icon = "panel-top",
		CurrentValue = false,
		Flag = "SX_ShowNative",
		Description = "Bring back Sirius X's built in interface. It shares state with this panel, so a native button may look stale after you flip the same feature here.",
		Callback = function(v) showNative(v) end,
	})

	notify("Sirius X - NextGen", "Loaded. Every tab drives real Sirius X. Press K to hide.", 6)
end)
